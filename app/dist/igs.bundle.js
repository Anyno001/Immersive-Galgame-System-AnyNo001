// Generated by app/scripts/build.js. Do not edit this file directly.
// IGS version: 0.23.94
const __igsModules = new Map();
const __igsCache = new Map();
function __igsRegister(id, factory) { __igsModules.set(id, factory); }
function __igsDefine(target, name, getter) { Object.defineProperty(target, name, { enumerable: true, get: getter }); }
function __igsReExport(target, source, pairs) { for (const pair of pairs) __igsDefine(target, pair[1], () => source[pair[0]]); }
function __igsRequire(id) {
    if (__igsCache.has(id)) return __igsCache.get(id).exports;
    const factory = __igsModules.get(id);
    if (!factory) throw new Error(`IGS module not found: ${id}`);
    const module = { exports: {} };
    __igsCache.set(id, module);
    factory(module, module.exports, __igsRequire);
    return module.exports;
}
__igsRegister("src/index.js", function(module, exports, require) {
const __igsReExportSource1 = require("src/core/bootstrap.js");
__igsReExport(exports, __igsReExportSource1, [["bootstrapIGS","bootstrapIGS"],["destroyIGS","destroyIGS"]]);
const __igsReExportSource2 = require("src/api/public-api.js");
__igsReExport(exports, __igsReExportSource2, [["createPublicApi","createPublicApi"],["attachPublicApi","attachPublicApi"]]);
const __igsReExportSource3 = require("src/host/tavern-helper-adapter.js");
__igsReExport(exports, __igsReExportSource3, [["createTavernHelperAdapter","createTavernHelperAdapter"]]);
const __igsReExportSource4 = require("src/host/magic-wand-entry.js");
__igsReExport(exports, __igsReExportSource4, [["createMagicWandEntry","createMagicWandEntry"]]);
const __igsReExportSource5 = require("src/scene/text-parser.js");
__igsReExport(exports, __igsReExportSource5, [["parseSceneText","parseSceneText"]]);
const __igsReExportSource6 = require("src/scene/scene-resolver.js");
__igsReExport(exports, __igsReExportSource6, [["resolveScene","resolveScene"]]);
const __igsReExportSource7 = require("src/visual/stage-renderer.js");
__igsReExport(exports, __igsReExportSource7, [["createStageRenderer","createStageRenderer"]]);
const __igsReExportSource8 = require("src/visual/layer-controller.js");
__igsReExport(exports, __igsReExportSource8, [["createLayerController","createLayerController"]]);
const __igsReExportSource9 = require("src/visual/reader-state.js");
__igsReExport(exports, __igsReExportSource9, [["createReaderState","createReaderState"]]);
const __igsReExportSource10 = require("src/visual/stage-model.js");
__igsReExport(exports, __igsReExportSource10, [["createStageModel","createStageModel"]]);
const __igsReExportSource11 = require("src/visual/visual-mode.js");
__igsReExport(exports, __igsReExportSource11, [["resolveVisualMode","resolveVisualMode"],["VISUAL_MODES","VISUAL_MODES"]]);
const __igsReExportSource12 = require("src/visual/igs-ui/reader-host.js");
__igsReExport(exports, __igsReExportSource12, [["createIgsReaderHost","createIgsReaderHost"]]);
const __igsReExportSource13 = require("src/generated-images/generation-queue.js");
__igsReExport(exports, __igsReExportSource13, [["createGenerationQueue","createGenerationQueue"]]);
const __igsReExportSource14 = require("src/prompts/adapters/prompt-adapter.js");
__igsReExport(exports, __igsReExportSource14, [["createPromptAdapter","createPromptAdapter"]]);
const __igsReExportSource15 = require("src/presets/preset-registry.js");
__igsReExport(exports, __igsReExportSource15, [["createPresetGroup","createPresetGroup"],["createPresetRegistry","createPresetRegistry"],["importPresetBundle","importPresetBundle"]]);
const __igsReExportSource16 = require("src/data/shujuku/client.js");
__igsReExport(exports, __igsReExportSource16, [["createShujukuClient","createShujukuClient"]]);
const __igsReExportSource17 = require("src/media/resource-cache.js");
__igsReExport(exports, __igsReExportSource17, [["createResourceCache","createResourceCache"]]);
const __igsReExportSource18 = require("src/registry/import-dispatcher.js");
__igsReExport(exports, __igsReExportSource18, [["dispatchImportBundle","dispatchImportBundle"]]);
const __igsReExportSource19 = require("src/storage/preset-store.js");
__igsReExport(exports, __igsReExportSource19, [["createMemoryStorage","createMemoryStorage"],["createPresetStore","createPresetStore"],["PRESET_STORE_KEY","PRESET_STORE_KEY"]]);
const __igsReExportSource20 = require("src/storage/legacy-igs.js");
__igsReExport(exports, __igsReExportSource20, [["writeLegacyIgsSettings","writeLegacyIgsSettings"]]);
});
__igsRegister("src/core/bootstrap.js", function(module, exports, require) {
const { createPublicApi, attachPublicApi, detachPublicApi } = require("src/api/public-api.js");
const { createTavernHelperAdapter } = require("src/host/tavern-helper-adapter.js");
const { createPresetRegistry } = require("src/presets/preset-registry.js");
const { createInputChannel } = require("src/host/input-channel.js");
const { parseSceneText } = require("src/scene/text-parser.js");
const { createSceneState } = require("src/scene/scene-state.js");
const { resolveScene } = require("src/scene/scene-resolver.js");
const { LEGACY_READER_MODES, readLegacyIgsSettings, writeLegacyIgsSettings, resolveLegacyReaderMode } = require("src/storage/legacy-igs.js");
const { createPresetStore } = require("src/storage/preset-store.js");
const { createLayerController } = require("src/visual/layer-controller.js");
const { createStageRenderer } = require("src/visual/stage-renderer.js");
const { resolveVisualMode } = require("src/visual/visual-mode.js");
const { normalizeScenePromptRule } = require("src/visual/igs-ui/reader-host-constants.js");
const { createIgsReaderHost } = require("src/visual/igs-ui/reader-host.js");
const { createEventBus } = require("src/core/event-bus.js");
const { createMagicWandEntry } = require("src/host/magic-wand-entry.js");
const { createExtensionPanel } = require("src/host/extension-panel.js");
const { createReaderImageService } = require("src/generated-images/reader-image-service.js");
const { createPromptInjector } = require("src/host/prompt-injector.js");
const { buildMoodGroupsText, buildGroupsText, buildSceneGroupsText, MOOD_GROUPS_PLACEHOLDER, SCENE_GROUPS_PLACEHOLDER, TIME_GROUPS_PLACEHOLDER, WEATHER_GROUPS_PLACEHOLDER } = require("src/scene/mood-groups.js");
const IGS_VERSION = '0.23.94';
const SCENE_ASSETS_INJECTION_INITIAL_DELAY_MS = 3000;
const SCENE_ASSETS_INJECTION_RETRY_MS = 1500;
const SCENE_ASSETS_INJECTION_MAX_ATTEMPTS = 5;
function bootstrapIGS(options = {}) {
    const globalObject = options.global || globalThis.window || globalThis;
    const events = options.events || createEventBus();
    const hostAdapter = options.hostAdapter || createTavernHelperAdapter(globalObject);
    const storageLike = options.storage || getStorageLike(globalObject);
    const legacyIgs = options.legacyIgsSettings || readLegacyIgsSettings(storageLike);
    const presetStore = options.presetStore || createPresetStore(storageLike);
    const presetRegistry = options.presetRegistry || createPresetRegistry({ store: presetStore });
    const inputChannel = createInputChannel(hostAdapter);
    const layerController = options.layerController || createLayerController(options.layers || {});
    const renderer = options.renderer || createStageRenderer(layerController);
    const readerImageService = options.readerImageService || createReaderImageService({
        global: globalObject,
        hostAdapter,
        imageGenerator: options.imageGenerator,
        providers: options.imageProviders,
        fetch: options.fetch,
    });
    const promptInjector = options.promptInjector || createPromptInjector(globalObject);
    const state = {
        status: 'booting',
        config: mergeInitialConfig(options.config, legacyIgs),
        legacyIgs,
        currentScene: createSceneState(),
        lastRender: null,
        destroyed: false,
    };

    const app = {
        version: IGS_VERSION,
        global: globalObject,
        events,
        hostAdapter,
        storage: storageLike,
        presetRegistry,
        refresh,
        typeAndSend,
        generateImage,
        collectMessageImages,
        getState,
        getPresetRegistry,
        getLegacyIgsSettings,
        getUnifiedSettingsSnapshot,
        saveUnifiedSettings,
        destroy,
        igsUi: null,
        magicWandEntry: null,
        extensionPanel: null,
    };
    let publicApi = null;
    let sceneAssetsInjectionTimer = null;
    app.igsUi = options.igsUi || createIgsReaderHost({
        global: globalObject,
        version: app.version,
        getUnifiedSettings: getUnifiedSettingsSnapshot,
        saveUnifiedSettings,
        typeAndSend,
        setInputText(text) {
            return typeof hostAdapter.setInputText === 'function' ? hostAdapter.setInputText(text) : { ok: false, reason: 'missing-input-api' };
        },
        getAdjacentMessage: hasAdjacentMessageCapability() ? resolveAdjacentMessage : null,
        jumpToMessage: jumpToMessage,
        openViewerFromMessage(messageId, mode, openOptions = {}) {
            if (!publicApi || typeof publicApi.openViewerFromMessage !== 'function') {
                return { ok: false, reason: 'public-api-not-ready', messageId, mode, openOptions };
            }
            return publicApi.openViewerFromMessage(messageId, mode, openOptions);
        },
        collectMessageImages(context = {}) {
            return readerImageService.collect({
                ...context,
                providers: getImageProviders(),
                unifiedSettings: getUnifiedSettingsSnapshot({ mode: context.mode }),
            });
        },
        fetchImageModels(context = {}) {
            return readerImageService.fetchModels({
                ...context,
                providers: getImageProviders(),
                unifiedSettings: context.settings || context.unifiedSettings || getUnifiedSettingsSnapshot({ mode: context.mode }),
            });
        },
        testImageApi(context = {}) {
            return readerImageService.test({
                ...context,
                providers: getImageProviders(),
                unifiedSettings: context.settings || context.unifiedSettings || getUnifiedSettingsSnapshot({ mode: context.mode }),
            });
        },
        regenerateImage(context = {}) {
            return readerImageService.regenerate({
                ...context,
                providers: getImageProviders(),
                unifiedSettings: getUnifiedSettingsSnapshot({ mode: context.mode }),
            });
        },
        saveImage(context = {}) {
            return readerImageService.save(context);
        },
        getCurrentMessage: () => hostAdapter.getCurrentMessage(),
    });
    publicApi = createPublicApi(app);
    readerImageService.registerProviders(publicApi.api.imageProviders);
    attachPublicApi(globalObject, publicApi);
    app.magicWandEntry = options.magicWandEntry || createMagicWandEntry({
        ...(options.magicWandEntryOptions || {}),
        global: globalObject,
        version: app.version,
        label: '沉浸式Galgame系统',
        open: (mode) => publicApi.openLatestAvailable(mode),
        resolveMode: () => {
            const snapshot = publicApi.getUnifiedSettings({});
            return snapshot && (snapshot.readerMode || snapshot.bridge && snapshot.bridge.openMode) || 'pc';
        },
    });
    app.extensionPanel = options.extensionPanel || createExtensionPanel({
        global: globalObject,
        label: '沉浸式Galgame系统',
        openSettings: () => publicApi.openSettings({}),
        openReader: () => publicApi.openLatestAvailable(),
        getEntryConfig: () => resolveEntryConfig(),
        setEntryConfig: (next) => saveEntryConfig(next),
    });
    function resolveEntryConfig() {
        const snapshot = publicApi.getUnifiedSettings({});
        const entry = snapshot && snapshot.bridge && snapshot.bridge.entry;
        return { magic: !entry || entry.magic !== false };
    }
    function saveEntryConfig(next) {
        const cfg = { magic: next.magic !== false };
        if (typeof saveUnifiedSettings === 'function') {
            saveUnifiedSettings({ bridge: { entry: cfg } });
        }
        applyEntryConfig(cfg);
    }
    function applyEntryConfig(cfg) {
        const entry = cfg || resolveEntryConfig();
        if (entry.magic) app.magicWandEntry.attach();
        else if (typeof app.magicWandEntry.destroy === 'function') app.magicWandEntry.destroy();
    }
    if (options.autoAttachMagicWand !== false) {
        applyEntryConfig(resolveEntryConfig());
        if (app.extensionPanel && typeof app.extensionPanel.attach === 'function') {
            app.extensionPanel.attach();
        }
    }
    state.status = 'ready';
    scheduleSceneAssetsInjection(SCENE_ASSETS_INJECTION_INITIAL_DELAY_MS, 1);
    attachChatChangedReinjection();
    events.emit('igs:ready', publicApi);

    return publicApi;

    async function refresh(context = {}) {
        ensureAlive();
        const message = context.message
            || await resolveContextMessage(context.messageId)
            || await hostAdapter.getCurrentMessage();
        const textScene = context.textScene || parseSceneText(getMessageText(message), {
            messageId: message && message.id,
            textFilterPreset: resolvePresetInput(context, 'textFilterPreset', 'text-filter-preset', state.config.textFilterPreset),
            textFormatPreset: resolvePresetInput(context, 'textFormatPreset', 'text-format-preset', state.config.textFormatPreset),
            sceneRegexPreset: resolvePresetInput(context, 'sceneRegexPreset', 'scene-regex-preset', state.config.sceneRegexPreset),
        });
        const scene = resolveScene({
            ...context,
            previousScene: state.currentScene,
            textScene,
        });
        const visualMode = resolveVisualMode(scene, context.visualSettings || state.config.visual || {});
        const renderedScene = createSceneState({ ...scene, visualMode });
        const renderResult = renderer.render(renderedScene, {
            mode: context.mode,
            viewerMode: context.viewerMode,
            visualSettings: context.visualSettings || state.config.visual || {},
            readerSettings: context.readerSettings,
            layoutSettings: context.layoutSettings,
            viewport: context.viewport || getViewport(globalObject),
            isMobile: context.isMobile,
            legacyIgs: state.legacyIgs,
            systemMessages: context.systemMessages,
            choiceState: context.choiceState,
        });
        state.currentScene = renderedScene;
        state.lastRender = renderResult;
        events.emit('igs:scene', renderedScene);
        return { ok: true, scene: renderedScene, render: renderResult };
    }

    async function typeAndSend(text) {
        ensureAlive();
        return inputChannel.typeAndSend(text);
    }

    function generateImage(request, generateOptions = {}) {
        return readerImageService.generate({
            request: typeof request === 'string' ? { prompt: request } : cloneData(request),
            prompt: typeof request === 'string'
                ? request
                : request && (request.prompt || request.input) || '',
            message: generateOptions.message || request && request.message || null,
            messageId: generateOptions.messageId || request && request.messageId || null,
            mode: generateOptions.mode,
            unifiedSettings: generateOptions.unifiedSettings || getUnifiedSettingsSnapshot({ mode: generateOptions.mode }),
            providers: getImageProviders(),
            generateOptions: cloneData(generateOptions),
        });
    }

    async function collectMessageImages(context = {}) {
        return readerImageService.collect({
            ...context,
            providers: getImageProviders(),
            unifiedSettings: context.unifiedSettings || getUnifiedSettingsSnapshot({ mode: context.mode }),
        });
    }

    function getState() {
        return {
            status: state.status,
            config: state.config,
            legacyIgs: state.legacyIgs,
            presets: presetRegistry.snapshot(),
            currentScene: state.currentScene,
            lastRender: state.lastRender,
            destroyed: state.destroyed,
            igsUi: app.igsUi ? app.igsUi.getState() : null,
            magicWandEntry: app.magicWandEntry && typeof app.magicWandEntry.getState === 'function'
                ? app.magicWandEntry.getState()
                : null,
        };
    }

    function getPresetRegistry() {
        return presetRegistry;
    }

    function getLegacyIgsSettings() {
        return cloneData(state.legacyIgs);
    }

    function getUnifiedSettingsSnapshot(input = {}) {
        const bridge = {
            ...cloneData(state.legacyIgs && state.legacyIgs.bridge || {}),
            ...cloneData(state.config || {}),
        };
        if (bridge.sceneAssets && typeof bridge.sceneAssets === 'object' && !Array.isArray(bridge.sceneAssets)) {
            bridge.sceneAssets = {
                ...cloneData(bridge.sceneAssets),
                promptRule: normalizeScenePromptRule(bridge.sceneAssets.promptRule),
            };
        }
        const readerMode = resolveLegacyReaderMode(
            input && typeof input === 'object' ? input.mode : input,
            state.legacyIgs && state.legacyIgs.displayMode,
            bridge,
        );
        const readerSettingsByMode = cloneData(state.legacyIgs && state.legacyIgs.readerSettingsByMode || {});
        // 优先 default 桶；老用户 default 为空时回退到旧的 pc/mobile 分桶或顶层 readerSettings。
        const hasKeys = (obj) => obj && typeof obj === 'object' && Object.keys(obj).length > 0;
        const resolvedReaderSettings = hasKeys(readerSettingsByMode['default']) ? readerSettingsByMode['default']
            : hasKeys(readerSettingsByMode['pc']) ? readerSettingsByMode['pc']
            : hasKeys(readerSettingsByMode['mobile']) ? readerSettingsByMode['mobile']
            : state.legacyIgs.readerSettings || {};
        return {
            version: app.version,
            bridge,
            imageApi: cloneData(bridge.imageApi || {}),
            readerSettings: cloneData(resolvedReaderSettings),
        };
    }

    function saveUnifiedSettings(payload = {}) {
        const currentLegacy = state.legacyIgs || readLegacyIgsSettings(storageLike);
        const nextBridge = {
            ...cloneData(currentLegacy.bridge || {}),
            ...cloneData(state.config || {}),
            ...cloneData(payload.bridge || {}),
        };
        const displayMode = resolveLegacyReaderMode(
            nextBridge.openMode,
            currentLegacy.displayMode,
            nextBridge,
        );
        const readerMode = resolveLegacyReaderMode(
            payload.readerMode,
            displayMode,
            nextBridge,
        );
        const readerSettingsByMode = cloneData(currentLegacy.readerSettingsByMode || {});
        // v0.21.4 起全模式共用一套设置，统一存取 'default' 桶。
        // 历史上保存按 readerMode 分桶、读取却固定读 'default'，导致移动端存了读不回。
        for (const mode of LEGACY_READER_MODES) {
            if (!readerSettingsByMode[mode]) readerSettingsByMode[mode] = {};
        }
        if (payload.readerSettings && typeof payload.readerSettings === 'object') {
            readerSettingsByMode['default'] = cloneData(payload.readerSettings);
        }
        const nextLegacy = {
            ok: true,
            bridge: nextBridge,
            displayMode,
            readerMode,
            readerSettings: cloneData(readerSettingsByMode['default'] || {}),
            readerSettingsByMode,
        };
        const writeResult = storageLike
            ? writeLegacyIgsSettings(storageLike, nextLegacy)
            : { ok: true, legacy: nextLegacy, persisted: false };
        if (writeResult.ok === false) return writeResult;
        state.legacyIgs = cloneData(writeResult.legacy);
        state.config = {
            ...cloneData(state.config || {}),
            ...cloneData(nextBridge),
        };
        events.emit('igs:legacy-settings-updated', cloneData(state.legacyIgs));
        syncSceneAssetsInjectionWithRetry(1);
        return {
            ok: true,
            legacy: cloneData(state.legacyIgs),
            unified: getUnifiedSettingsSnapshot({ mode: readerMode }),
        };
    }

    function syncSceneAssetsInjection() {
        const unified = getUnifiedSettingsSnapshot();
        const sceneAssets = unified.bridge && unified.bridge.sceneAssets;
        if (sceneAssets && sceneAssets.enabled && sceneAssets.promptRule) {
            return promptInjector.inject(resolvePromptRuleContent(sceneAssets));
        }
        promptInjector.clear();
        return { ok: true, reason: 'scene-assets-disabled' };
    }

    function resolvePromptRuleContent(sceneAssets) {
        let rule = String(sceneAssets.promptRule || '');
        if (rule.includes(MOOD_GROUPS_PLACEHOLDER)) {
            rule = rule.split(MOOD_GROUPS_PLACEHOLDER).join(buildMoodGroupsText(sceneAssets.moodGroups));
        }
        if (rule.includes(SCENE_GROUPS_PLACEHOLDER)) {
            rule = rule.split(SCENE_GROUPS_PLACEHOLDER).join(buildSceneGroupsText(sceneAssets.scenes));
        }
        if (rule.includes(TIME_GROUPS_PLACEHOLDER)) {
            rule = rule.split(TIME_GROUPS_PLACEHOLDER).join(buildGroupsText(sceneAssets.timeGroups));
        }
        if (rule.includes(WEATHER_GROUPS_PLACEHOLDER)) {
            rule = rule.split(WEATHER_GROUPS_PLACEHOLDER).join(buildGroupsText(sceneAssets.weatherGroups));
        }
        return rule;
    }

    function syncSceneAssetsInjectionWithRetry(attempt) {
        const result = syncSceneAssetsInjection();
        if (shouldRetrySceneAssetsInjection(result, attempt)) {
            scheduleSceneAssetsInjection(SCENE_ASSETS_INJECTION_RETRY_MS, attempt + 1);
        }
        return result;
    }

    function scheduleSceneAssetsInjection(delayMs, attempt) {
        clearSceneAssetsInjectionTimer();
        const schedule = typeof globalObject.setTimeout === 'function'
            ? globalObject.setTimeout.bind(globalObject)
            : setTimeout;
        sceneAssetsInjectionTimer = schedule(() => {
            sceneAssetsInjectionTimer = null;
            syncSceneAssetsInjectionWithRetry(attempt);
        }, delayMs);
    }

    function clearSceneAssetsInjectionTimer() {
        if (sceneAssetsInjectionTimer == null) return;
        const cancel = typeof globalObject.clearTimeout === 'function'
            ? globalObject.clearTimeout.bind(globalObject)
            : typeof clearTimeout === 'function' ? clearTimeout : null;
        if (cancel) cancel(sceneAssetsInjectionTimer);
        sceneAssetsInjectionTimer = null;
    }

    function shouldRetrySceneAssetsInjection(result, attempt) {
        if (!result || result.ok !== false) return false;
        if (attempt >= SCENE_ASSETS_INJECTION_MAX_ATTEMPTS) return false;
        return result.reason !== 'empty-content';
    }

    function resolveTavernContext() {
        try {
            const candidates = [globalObject, globalThis, typeof window !== 'undefined' ? window : null];
            for (const root of candidates) {
                if (root && root.SillyTavern && typeof root.SillyTavern.getContext === 'function') {
                    return root.SillyTavern.getContext();
                }
            }
        } catch (error) { /* */ }
        return null;
    }

    function attachChatChangedReinjection() {
        const context = resolveTavernContext();
        const eventSource = context && context.eventSource;
        const eventTypes = context && (context.event_types || context.eventTypes);
        const eventName = eventTypes && eventTypes.CHAT_CHANGED;
        if (!eventSource || !eventName || typeof eventSource.on !== 'function') return;
        const handler = () => {
            if (state.destroyed) return;
            syncSceneAssetsInjectionWithRetry(1);
        };
        try {
            eventSource.on(eventName, handler);
        } catch (error) { return; }
        state.chatChangedCleanup = () => {
            try {
                if (typeof eventSource.removeListener === 'function') eventSource.removeListener(eventName, handler);
                else if (typeof eventSource.off === 'function') eventSource.off(eventName, handler);
            } catch (error) { /* */ }
        };
    }

    function detachChatChangedReinjection() {
        if (typeof state.chatChangedCleanup === 'function') {
            state.chatChangedCleanup();
            state.chatChangedCleanup = null;
        }
    }

    function destroy() {
        if (state.destroyed) return { ok: true, reason: 'already-destroyed' };
        state.destroyed = true;
        state.status = 'destroyed';
        clearSceneAssetsInjectionTimer();
        detachChatChangedReinjection();
        promptInjector.clear();
        if (app.igsUi && typeof app.igsUi.destroy === 'function') {
            app.igsUi.destroy();
        }
        if (app.magicWandEntry && typeof app.magicWandEntry.destroy === 'function') {
            app.magicWandEntry.destroy();
        }
        if (app.extensionPanel && typeof app.extensionPanel.destroy === 'function') {
            app.extensionPanel.destroy();
        }
        detachPublicApi(globalObject, publicApi);
        events.emit('igs:destroy', publicApi);
        events.clear();
        return { ok: true };
    }

    function ensureAlive() {
        if (state.destroyed) {
            throw new Error('IGS instance has been destroyed.');
        }
    }

    async function resolveContextMessage(messageId) {
        if (messageId == null || !hostAdapter || typeof hostAdapter.getMessageById !== 'function') {
            return null;
        }
        return hostAdapter.getMessageById(messageId);
    }

    async function resolveAdjacentMessage(messageId, delta = 1) {
        if (messageId == null || !hostAdapter) return null;
        if (typeof hostAdapter.getAdjacentMessage === 'function') {
            return hostAdapter.getAdjacentMessage(messageId, delta);
        }
        if (typeof hostAdapter.listMessages !== 'function') {
            return null;
        }
        const normalizedId = Number(messageId);
        const messages = await hostAdapter.listMessages();
        const step = Number(delta) < 0 ? -1 : 1;
        const aiTurns = Array.isArray(messages) ? messages.filter(isVisibleAiTurn) : [];
        const currentTurnIndex = aiTurns.findIndex((message) => Number(message && message.id) === normalizedId);
        if (currentTurnIndex >= 0) {
            return aiTurns[currentTurnIndex + step] || null;
        }
        const currentIndex = messages.findIndex((message) => Number(message && message.id) === normalizedId);
        if (currentIndex < 0) return null;
        for (let index = currentIndex + step; index >= 0 && index < messages.length; index += step) {
            if (isVisibleAiTurn(messages[index])) return messages[index];
        }
        return null;
    }

    function hasAdjacentMessageCapability() {
        return Boolean(
            hostAdapter
            && (
                typeof hostAdapter.getAdjacentMessage === 'function'
                || typeof hostAdapter.listMessages === 'function'
            ),
        );
    }

    function isVisibleAiTurn(message) {
        return Boolean(
            message
            && !isTruthyTurnFlag(message.isUser)
            && !isTruthyTurnFlag(message.isSystem)
            && !isTruthyTurnFlag(message.isHidden),
        );
    }

    function isTruthyTurnFlag(value) {
        return value === true || value === 1 || value === '1' || value === 'true';
    }

    async function jumpToMessage(messageId) {
        if (!hostAdapter || typeof hostAdapter.jumpToMessage !== 'function') {
            return { ok: false, reason: 'missing-jump-api', messageId };
        }
        return hostAdapter.jumpToMessage(messageId);
    }

    function getImageProviders() {
        if (!publicApi || !publicApi.api || !publicApi.api.imageProviders || typeof publicApi.api.imageProviders.list !== 'function') {
            return [];
        }
        return publicApi.api.imageProviders.list();
    }

    function resolvePresetInput(context, contextKey, presetType, fallbackConfig) {
        if (Object.prototype.hasOwnProperty.call(context, contextKey)) {
            return context[contextKey];
        }

        const currentPreset = presetRegistry.getCurrentData(presetType);
        if (currentPreset) return currentPreset;

        return fallbackConfig;
    }
}
function destroyIGS(globalObject = globalThis.window || globalThis) {
    if (globalObject && globalObject.IGS && typeof globalObject.IGS.destroy === 'function') {
        return globalObject.IGS.destroy();
    }
    return { ok: true, reason: 'not-running' };
}

function getMessageText(message) {
    if (typeof message === 'string') return message;
    if (!message || typeof message !== 'object') return '';
    return message.text || message.message || message.content || message.mes || '';
}

function getStorageLike(globalObject) {
    try {
        return globalObject && globalObject.localStorage ? globalObject.localStorage : null;
    } catch (error) {
        return null;
    }
}

function getViewport(globalObject) {
    const visualViewport = globalObject && globalObject.visualViewport;
    if (visualViewport && Number.isFinite(visualViewport.width) && Number.isFinite(visualViewport.height)) {
        return {
            width: visualViewport.width,
            height: visualViewport.height,
        };
    }

    const width = Number(globalObject && globalObject.innerWidth);
    const height = Number(globalObject && globalObject.innerHeight);
    if (Number.isFinite(width) && Number.isFinite(height)) {
        return { width, height };
    }

    return { width: 0, height: 0 };
}

function mergeInitialConfig(explicitConfig, legacyIgs) {
    const nextConfig = cloneData(explicitConfig || {});
    if (!legacyIgs || legacyIgs.ok === false) return nextConfig;
    return {
        ...cloneData(legacyIgs.bridge || {}),
        ...nextConfig,
    };
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

__igsDefine(exports, "bootstrapIGS", () => bootstrapIGS);
__igsDefine(exports, "destroyIGS", () => destroyIGS);
});
__igsRegister("src/api/public-api.js", function(module, exports, require) {
const { createPresetGroup, createPresetRegistry } = require("src/presets/preset-registry.js");
const { createIgsCompatApi } = require("src/api/igs-compat.js");
const API_GROUPS = [
    'imageProviders',
    'imageRequestBuilders',
    'actions',
    'components',
    'choiceComponents',
    'themePresets',
    'uiSkins',
    'sceneRegexPresets',
    'textFilterPresets',
    'textFormatPresets',
    'backgroundPacks',
    'characterPacks',
    'promptPresets',
];

const PRESET_GROUP_TO_TYPE = Object.freeze({
    sceneRegexPresets: 'scene-regex-preset',
    textFilterPresets: 'text-filter-preset',
    textFormatPresets: 'text-format-preset',
});
function createPublicApi(app) {
    const presetRegistry = resolvePresetRegistry(app);
    const groupedApi = Object.fromEntries(API_GROUPS.map((name) => [
        name,
        createApiGroup(name, presetRegistry),
    ]));
    const igsCompatApi = createIgsCompatApi(app);

    return {
        name: 'Immersive Galgame System',
        version: app.version,
        api: groupedApi,

        refresh: app.refresh,
        renderScene(textScene) {
            return app.refresh({ textScene });
        },
        typeAndSend: app.typeAndSend,
        getState: app.getState,
        ensureMagicWandEntry() {
            if (!app.magicWandEntry || typeof app.magicWandEntry.ensure !== 'function') {
                return { ok: false, reason: 'magic-wand-entry-not-mounted' };
            }
            return app.magicWandEntry.ensure();
        },
        getMagicWandEntryState() {
            return app.magicWandEntry && typeof app.magicWandEntry.getState === 'function'
                ? app.magicWandEntry.getState()
                : { attached: false, reason: 'magic-wand-entry-not-mounted' };
        },
        destroy: app.destroy,
        ...igsCompatApi,
    };
}
function attachPublicApi(globalObject, api) {
    if (!globalObject) return api;
    globalObject.IGS = api;
    globalObject.ImmersiveGalgameSystem = api;
    return api;
}
function detachPublicApi(globalObject, api) {
    if (!globalObject) return;
    if (globalObject.IGS === api) delete globalObject.IGS;
    if (globalObject.ImmersiveGalgameSystem === api) delete globalObject.ImmersiveGalgameSystem;
}

function createApiGroup(groupName, presetRegistry) {
    const presetType = PRESET_GROUP_TO_TYPE[groupName];
    if (presetType) {
        return createPresetGroup(presetRegistry, presetType);
    }
    return createRegistryGroup(groupName);
}

function createRegistryGroup(groupName) {
    const items = new Map();

    return {
        register(item) {
            if (!item || !item.id) {
                return { ok: false, reason: 'missing-id', groupName };
            }
            items.set(item.id, item);
            return { ok: true, item };
        },
        unregister(id) {
            return { ok: items.delete(id), id };
        },
        get(id) {
            return items.get(id) || null;
        },
        list() {
            return Array.from(items.values());
        },
    };
}

function resolvePresetRegistry(app) {
    if (app && app.presetRegistry) return app.presetRegistry;
    if (app && typeof app.getPresetRegistry === 'function') {
        const registry = app.getPresetRegistry();
        if (registry) return registry;
    }
    return createPresetRegistry();
}

__igsDefine(exports, "createPublicApi", () => createPublicApi);
__igsDefine(exports, "attachPublicApi", () => attachPublicApi);
__igsDefine(exports, "detachPublicApi", () => detachPublicApi);
});
__igsRegister("src/presets/preset-registry.js", function(module, exports, require) {
const { dispatchImportBundle } = require("src/registry/import-dispatcher.js");
const { createEmptyPresetSnapshot, createPresetStore, normalizePresetSnapshot } = require("src/storage/preset-store.js");
const { PRESET_TYPES, validatePresetByType } = require("src/presets/preset-types.js");
const PRESET_TYPE_SET = new Set(PRESET_TYPES);
function createPresetRegistry(options = {}) {
    const store = options.store || createPresetStore(options.storage, options.storeOptions);
    const state = {
        snapshot: createEmptyPresetSnapshot(),
    };

    const registry = {
        store,
        hydrate,
        snapshot,
        register,
        unregister,
        get,
        list,
        setCurrent,
        getCurrent,
        getCurrentData,
        exportPreset,
        exportGroup,
        importBundle,
    };

    if (options.autoHydrate !== false) {
        hydrate();
    }

    return registry;

    function hydrate() {
        const loadResult = store.load();
        const normalized = sanitizeSnapshot(loadResult.snapshot);
        state.snapshot = normalized.snapshot;

        return {
            ...loadResult,
            snapshot: cloneData(state.snapshot),
            rejected: normalized.rejected,
        };
    }

    function snapshot() {
        return cloneData(state.snapshot);
    }

    function register(preset, options = {}) {
        const candidate = withExpectedType(preset, options.type);
        const validation = validatePresetByType(candidate);
        if (!validation.ok) {
            return {
                ok: false,
                item: cloneData(candidate),
                normalized: validation.normalized ? cloneData(validation.normalized) : null,
                errors: validation.errors.map(cloneData),
            };
        }

        const nextSnapshot = cloneData(state.snapshot);
        const type = validation.normalized.type;
        const id = validation.normalized.id;

        if (!nextSnapshot.items[type]) {
            nextSnapshot.items[type] = {};
        }

        const replaced = Object.prototype.hasOwnProperty.call(nextSnapshot.items[type], id);
        nextSnapshot.items[type][id] = cloneData(validation.normalized);
        const currentChanged = false;
        const commitResult = commitSnapshot(nextSnapshot);
        if (!commitResult.ok) {
            return {
                ok: false,
                reason: commitResult.reason,
                item: cloneData(candidate),
                normalized: cloneData(validation.normalized),
                errors: validation.errors.map(cloneData),
                message: commitResult.message,
            };
        }

        return {
            ok: true,
            item: cloneData(candidate),
            normalized: cloneData(validation.normalized),
            replaced,
            currentChanged,
            errors: [],
        };
    }

    function unregister(type, id) {
        const normalizedType = normalizeType(type);
        const normalizedId = normalizeId(id);
        if (!normalizedType || !normalizedId) {
            return { ok: false, reason: 'missing-id', type: normalizedType, id: normalizedId };
        }

        const nextSnapshot = cloneData(state.snapshot);
        if (!nextSnapshot.items[normalizedType] || !Object.prototype.hasOwnProperty.call(nextSnapshot.items[normalizedType], normalizedId)) {
            return { ok: false, reason: 'missing-preset', type: normalizedType, id: normalizedId };
        }

        delete nextSnapshot.items[normalizedType][normalizedId];
        if (Object.keys(nextSnapshot.items[normalizedType]).length === 0) {
            delete nextSnapshot.items[normalizedType];
        }
        if (nextSnapshot.current[normalizedType] === normalizedId) {
            delete nextSnapshot.current[normalizedType];
        }

        const commitResult = commitSnapshot(nextSnapshot);
        if (!commitResult.ok) {
            return { ok: false, reason: commitResult.reason, type: normalizedType, id: normalizedId, message: commitResult.message };
        }

        return { ok: true, type: normalizedType, id: normalizedId };
    }

    function get(type, id) {
        const normalizedType = normalizeType(type);
        const normalizedId = normalizeId(id);
        if (!normalizedType || !normalizedId) return null;
        const item = state.snapshot.items[normalizedType] && state.snapshot.items[normalizedType][normalizedId];
        return item ? cloneData(item) : null;
    }

    function list(type) {
        const normalizedType = normalizeType(type);
        if (!normalizedType) return [];
        return Object.values(state.snapshot.items[normalizedType] || {}).map(cloneData);
    }

    function setCurrent(type, id) {
        const normalizedType = normalizeType(type);
        const normalizedId = normalizeId(id);
        if (!normalizedType || !normalizedId) {
            return { ok: false, reason: 'missing-id', type: normalizedType, id: normalizedId };
        }

        const item = state.snapshot.items[normalizedType] && state.snapshot.items[normalizedType][normalizedId];
        if (!item) {
            return { ok: false, reason: 'missing-preset', type: normalizedType, id: normalizedId };
        }

        const validation = validatePresetByType(item);
        if (!validation.ok) {
            return { ok: false, reason: 'invalid-preset', type: normalizedType, id: normalizedId, errors: validation.errors.map(cloneData) };
        }

        const nextSnapshot = cloneData(state.snapshot);
        nextSnapshot.current[normalizedType] = normalizedId;
        if (!nextSnapshot.items[normalizedType]) nextSnapshot.items[normalizedType] = {};
        nextSnapshot.items[normalizedType][normalizedId] = cloneData(validation.normalized);

        const commitResult = commitSnapshot(nextSnapshot);
        if (!commitResult.ok) {
            return { ok: false, reason: commitResult.reason, type: normalizedType, id: normalizedId, message: commitResult.message };
        }

        return {
            ok: true,
            type: normalizedType,
            id: normalizedId,
            item: cloneData(validation.normalized),
        };
    }

    function getCurrent(type) {
        const normalizedType = normalizeType(type);
        if (!normalizedType) return null;
        const id = state.snapshot.current[normalizedType];
        return id ? get(normalizedType, id) : null;
    }

    function getCurrentData(type) {
        const current = getCurrent(type);
        return current ? cloneData(current.data) : null;
    }

    function exportPreset(type, id) {
        return get(type, id);
    }

    function exportGroup(type) {
        const normalizedType = normalizeType(type);
        return {
            type: 'igs-import-bundle',
            items: normalizedType ? list(normalizedType) : [],
        };
    }

    function importBundle(bundle, options = {}) {
        return importPresetBundle(registry, bundle, options);
    }

    function commitSnapshot(nextSnapshot) {
        const normalized = normalizePresetSnapshot(nextSnapshot);
        const saveResult = store.save(normalized);
        if (!saveResult.ok) {
            return saveResult;
        }
        state.snapshot = normalizePresetSnapshot(saveResult.snapshot);
        return { ok: true, snapshot: cloneData(state.snapshot) };
    }
}
function createPresetGroup(registry, type) {
    return {
        register(item) {
            return registry.register(item, { type });
        },
        unregister(id) {
            return registry.unregister(type, id);
        },
        get(id) {
            return registry.get(type, id);
        },
        list() {
            return registry.list(type);
        },
        setCurrent(id) {
            return registry.setCurrent(type, id);
        },
        getCurrent() {
            return registry.getCurrent(type);
        },
        export(id) {
            return registry.exportPreset(type, id);
        },
        exportAll() {
            return registry.exportGroup(type);
        },
    };
}
function importPresetBundle(registry, bundle, options = {}) {
    const dispatchResult = dispatchImportBundle(bundle, {});
    const accepted = [];
    const rejected = dispatchResult.rejected.map(cloneData);

    for (const item of dispatchResult.accepted) {
        if (!PRESET_TYPE_SET.has(item.type)) {
            rejected.push({
                item: cloneData(item),
                reason: 'no-preset-handler',
            });
            continue;
        }

        const result = registry.register(item, {
            type: item.type,
            ...options,
        });
        if (!result.ok) {
            rejected.push({
                item: cloneData(item),
                reason: 'invalid-preset',
                errors: (result.errors || []).map(cloneData),
            });
            continue;
        }
        accepted.push(cloneData(result.normalized));
    }

    return {
        ok: rejected.length === 0,
        accepted,
        rejected,
    };
}

function sanitizeSnapshot(snapshot) {
    const source = normalizePresetSnapshot(snapshot);
    const nextSnapshot = createEmptyPresetSnapshot();
    nextSnapshot.version = source.version;
    nextSnapshot.updatedAt = source.updatedAt;
    const rejected = [];

    for (const [type, records] of Object.entries(source.items || {})) {
        if (!PRESET_TYPE_SET.has(type)) continue;
        for (const [id, item] of Object.entries(records || {})) {
            const validation = validatePresetByType(withExpectedType(item, type, id));
            if (!validation.ok) {
                rejected.push({
                    item: cloneData(item),
                    reason: 'invalid-stored-preset',
                    errors: validation.errors.map(cloneData),
                });
                continue;
            }
            if (!nextSnapshot.items[type]) nextSnapshot.items[type] = {};
            nextSnapshot.items[type][validation.normalized.id] = cloneData(validation.normalized);
        }
    }

    for (const [type, id] of Object.entries(source.current || {})) {
        if (!nextSnapshot.items[type] || !Object.prototype.hasOwnProperty.call(nextSnapshot.items[type], id)) {
            continue;
        }
        nextSnapshot.current[type] = id;
    }

    return {
        snapshot: normalizePresetSnapshot(nextSnapshot),
        rejected,
    };
}

function withExpectedType(preset, expectedType, fallbackId) {
    if (!expectedType) return cloneData(preset);
    const source = isPlainObject(preset) ? preset : {};
    return {
        ...cloneData(source),
        type: expectedType,
        id: normalizeId(source.id || fallbackId),
    };
}

function normalizeType(type) {
    const text = typeof type === 'string' ? type.trim() : '';
    return text || '';
}

function normalizeId(id) {
    const text = typeof id === 'string' ? id.trim() : '';
    return text || '';
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

function isPlainObject(value) {
    return Boolean(value) && typeof value === 'object' && !Array.isArray(value);
}

__igsDefine(exports, "createPresetRegistry", () => createPresetRegistry);
__igsDefine(exports, "createPresetGroup", () => createPresetGroup);
__igsDefine(exports, "importPresetBundle", () => importPresetBundle);
});
__igsRegister("src/registry/import-dispatcher.js", function(module, exports, require) {
const ALLOWED_TYPES = new Set([
    'image-provider',
    'image-provider-preset',
    'image-request-builder',
    'image-request-builder-preset',
    'workflow-preset',
    'ui-component',
    'choice-component',
    'ui-skin-preset',
    'ui-layout-preset',
    'theme-preset',
    'css-preset',
    'scene-regex-preset',
    'text-filter-preset',
    'text-format-preset',
    'choice-parser-preset',
    'background-pack',
    'character-pack',
    'background-rule-preset',
    'prompt-preset',
]);

const FORBIDDEN_TYPES = new Set(['global-mod-manager', 'hotkey-preset', 'shujuku-template']);
function dispatchImportBundle(bundle = {}, handlers = {}) {
    const accepted = [];
    const rejected = [];
    const items = Array.isArray(bundle.items) ? bundle.items : [];

    for (const item of items) {
        if (!item || !item.type || FORBIDDEN_TYPES.has(item.type) || !ALLOWED_TYPES.has(item.type)) {
            rejected.push({ item, reason: 'unsupported-type' });
            continue;
        }
        const handler = handlers[item.type];
        if (handler) handler(item);
        accepted.push(item);
    }

    return { ok: rejected.length === 0, accepted, rejected };
}

__igsDefine(exports, "dispatchImportBundle", () => dispatchImportBundle);
});
__igsRegister("src/storage/preset-store.js", function(module, exports, require) {
const PRESET_STORE_KEY = 'igs:preset-registry:v1';
function createPresetStore(storageLike, options = {}) {
    const key = options.key || PRESET_STORE_KEY;
    const target = isStorageLike(storageLike) ? storageLike : createMemoryStorage();

    return {
        key,
        storage: target,
        load() {
            return readPresetSnapshot(target, key);
        },
        save(snapshot) {
            return writePresetSnapshot(target, snapshot, key);
        },
        clear() {
            return removePresetSnapshot(target, key);
        },
    };
}
function createMemoryStorage(initialData = {}) {
    const data = new Map();

    for (const [key, value] of Object.entries(initialData || {})) {
        data.set(String(key), normalizeStorageValue(value));
    }

    return {
        getItem(key) {
            const storageKey = String(key);
            return data.has(storageKey) ? data.get(storageKey) : null;
        },
        setItem(key, value) {
            data.set(String(key), normalizeStorageValue(value));
        },
        removeItem(key) {
            data.delete(String(key));
        },
        dump() {
            return Object.fromEntries(data.entries());
        },
    };
}
function createEmptyPresetSnapshot() {
    return {
        version: 1,
        current: {},
        items: {},
        drafts: {},
        updatedAt: '',
    };
}
function normalizePresetSnapshot(snapshot) {
    const source = isPlainObject(snapshot) ? snapshot : {};
    const normalized = createEmptyPresetSnapshot();
    normalized.version = normalizePositiveInteger(source.version, 1);
    normalized.updatedAt = typeof source.updatedAt === 'string' ? source.updatedAt : '';

    if (isPlainObject(source.current)) {
        for (const [type, id] of Object.entries(source.current)) {
            const normalizedId = typeof id === 'string' ? id.trim() : '';
            if (normalizedId) normalized.current[type] = normalizedId;
        }
    }

    if (isPlainObject(source.items)) {
        for (const [type, records] of Object.entries(source.items)) {
            if (!isPlainObject(records)) continue;
            normalized.items[type] = {};
            for (const [id, item] of Object.entries(records)) {
                normalized.items[type][id] = cloneData(item);
            }
        }
    }

    if (isPlainObject(source.drafts)) {
        normalized.drafts = cloneData(source.drafts);
    }

    return normalized;
}
function readPresetSnapshot(storageLike, key = PRESET_STORE_KEY) {
    if (!isStorageLike(storageLike)) {
        return {
            ok: true,
            key,
            snapshot: createEmptyPresetSnapshot(),
        };
    }

    try {
        const raw = storageLike.getItem(key);
        if (raw == null || raw === '') {
            return {
                ok: true,
                key,
                snapshot: createEmptyPresetSnapshot(),
            };
        }

        return {
            ok: true,
            key,
            snapshot: normalizePresetSnapshot(JSON.parse(raw)),
        };
    } catch (error) {
        return {
            ok: false,
            reason: 'invalid-preset-snapshot',
            key,
            message: error instanceof Error ? error.message : String(error),
            snapshot: createEmptyPresetSnapshot(),
        };
    }
}
function writePresetSnapshot(storageLike, snapshot, key = PRESET_STORE_KEY) {
    if (!isStorageLike(storageLike)) {
        return {
            ok: false,
            reason: 'missing-storage-like',
            key,
        };
    }

    const normalized = normalizePresetSnapshot(snapshot);
    normalized.updatedAt = new Date().toISOString();

    try {
        storageLike.setItem(key, JSON.stringify(normalized));
        return {
            ok: true,
            key,
            snapshot: normalized,
        };
    } catch (error) {
        return {
            ok: false,
            reason: 'preset-store-write-failed',
            key,
            message: error instanceof Error ? error.message : String(error),
            snapshot: normalized,
        };
    }
}

function removePresetSnapshot(storageLike, key = PRESET_STORE_KEY) {
    if (!isStorageLike(storageLike)) {
        return {
            ok: false,
            reason: 'missing-storage-like',
            key,
        };
    }

    try {
        storageLike.removeItem(key);
        return { ok: true, key };
    } catch (error) {
        return {
            ok: false,
            reason: 'preset-store-remove-failed',
            key,
            message: error instanceof Error ? error.message : String(error),
        };
    }
}

function normalizeStorageValue(value) {
    if (typeof value === 'string') return value;
    return JSON.stringify(value);
}

function normalizePositiveInteger(value, fallback) {
    const number = Number(value);
    if (!Number.isInteger(number) || number <= 0) return fallback;
    return number;
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

function isPlainObject(value) {
    return Boolean(value) && typeof value === 'object' && !Array.isArray(value);
}

function isStorageLike(value) {
    return !!value
        && typeof value.getItem === 'function'
        && typeof value.setItem === 'function'
        && typeof value.removeItem === 'function';
}

__igsDefine(exports, "createPresetStore", () => createPresetStore);
__igsDefine(exports, "createMemoryStorage", () => createMemoryStorage);
__igsDefine(exports, "createEmptyPresetSnapshot", () => createEmptyPresetSnapshot);
__igsDefine(exports, "normalizePresetSnapshot", () => normalizePresetSnapshot);
__igsDefine(exports, "readPresetSnapshot", () => readPresetSnapshot);
__igsDefine(exports, "writePresetSnapshot", () => writePresetSnapshot);
__igsDefine(exports, "PRESET_STORE_KEY", () => PRESET_STORE_KEY);
});
__igsRegister("src/presets/preset-types.js", function(module, exports, require) {
const { normalizeSceneRegexPreset, normalizeTextFilterPreset, validateSceneRegexPreset, validateTextFormatPreset } = require("src/presets/text-presets.js");
const PRESET_TYPES = Object.freeze([
    'theme-preset',
    'css-preset',
    'ui-skin-preset',
    'ui-layout-preset',
    'scene-regex-preset',
    'text-filter-preset',
    'text-format-preset',
    'prompt-preset',
    'image-provider-preset',
    'image-request-builder-preset',
    'workflow-preset',
    'background-rule-preset',
    'choice-parser-preset',
    'visual-mode-preset',
]);
const TEXT_PRESET_TYPES = Object.freeze([
    'scene-regex-preset',
    'text-filter-preset',
    'text-format-preset',
]);
const PRESET_TYPE_TO_API_GROUP = Object.freeze({
    'theme-preset': 'themePresets',
    'css-preset': 'themePresets',
    'ui-skin-preset': 'uiSkins',
    'ui-layout-preset': 'uiSkins',
    'scene-regex-preset': 'sceneRegexPresets',
    'text-filter-preset': 'textFilterPresets',
    'text-format-preset': 'textFormatPresets',
    'prompt-preset': 'promptPresets',
    'image-provider-preset': null,
    'image-request-builder-preset': null,
    'workflow-preset': null,
    'background-rule-preset': null,
    'choice-parser-preset': null,
    'visual-mode-preset': null,
});
const PRESET_TYPE_TO_CONFIG_KEY = Object.freeze({
    'scene-regex-preset': 'sceneRegexPreset',
    'text-filter-preset': 'textFilterPreset',
    'text-format-preset': 'textFormatPreset',
    'prompt-preset': 'promptPreset',
    'image-provider-preset': 'imageProviderPreset',
    'image-request-builder-preset': 'imageRequestBuilderPreset',
    'workflow-preset': 'workflowPreset',
    'theme-preset': 'themePreset',
    'css-preset': 'cssPreset',
    'ui-skin-preset': 'uiSkinPreset',
    'ui-layout-preset': 'uiLayoutPreset',
    'background-rule-preset': 'backgroundRulePreset',
    'choice-parser-preset': 'choiceParserPreset',
    'visual-mode-preset': 'visualModePreset',
});

const MANIFEST_FIELDS = new Set(['format', 'type', 'id', 'name', 'version', 'data']);
const PRESET_TYPE_SET = new Set(PRESET_TYPES);
function normalizePresetManifest(preset) {
    const source = isPlainObject(preset) ? preset : {};
    const type = normalizeString(source.type, '');
    const id = normalizeString(source.id, '');
    const name = normalizeString(source.name, id || type || 'preset');
    const format = normalizeString(source.format, 'igs_preset_v1');
    const version = normalizePositiveInteger(source.version, 1);
    const data = source.data && isPlainObject(source.data)
        ? cloneData(source.data)
        : collectInlineData(source);

    return Object.freeze({
        format,
        type,
        id,
        name,
        version,
        data,
    });
}
function validatePresetManifest(preset) {
    const normalized = normalizePresetManifest(preset);
    const errors = [];

    if (!normalized.type) {
        errors.push(createPresetError('', 'type', 'missing-type', 'Preset type is required.'));
    } else if (!PRESET_TYPE_SET.has(normalized.type)) {
        errors.push(createPresetError(normalized.type, 'type', 'unsupported-type', `Unsupported preset type: ${normalized.type}`));
    }

    if (!normalized.id) {
        errors.push(createPresetError(normalized.type, 'id', 'missing-id', 'Preset id is required.'));
    }

    return Object.freeze({
        ok: errors.length === 0,
        normalized,
        errors,
    });
}
function validatePresetByType(preset) {
    const manifestResult = validatePresetManifest(preset);
    const errors = [...manifestResult.errors];

    if (!manifestResult.ok) {
        return Object.freeze({
            ok: false,
            normalized: manifestResult.normalized,
            errors,
        });
    }

    const normalized = manifestResult.normalized;
    let normalizedData = cloneData(normalized.data);

    if (normalized.type === 'text-filter-preset') {
        normalizedData = normalizeTextFilterPreset(normalized);
    } else if (normalized.type === 'text-format-preset') {
        const result = validateTextFormatPreset(normalized);
        normalizedData = result.preset;
        errors.push(...result.errors.map(cloneData));
    } else if (normalized.type === 'scene-regex-preset') {
        const result = validateSceneRegexPreset(normalized);
        normalizedData = result.preset;
        errors.push(...result.errors.map(cloneData));
    }

    return Object.freeze({
        ok: errors.length === 0,
        normalized: Object.freeze({
            ...normalized,
            data: normalizedData,
        }),
        errors,
    });
}

function collectInlineData(source) {
    const data = {};

    for (const [key, value] of Object.entries(source)) {
        if (MANIFEST_FIELDS.has(key)) continue;
        data[key] = cloneData(value);
    }

    return data;
}

function createPresetError(presetType, field, reason, message) {
    return Object.freeze({
        presetType,
        field,
        reason,
        message,
    });
}

function normalizeString(value, fallback) {
    if (value == null) return fallback;
    const text = String(value).trim();
    return text || fallback;
}

function normalizePositiveInteger(value, fallback) {
    const number = Number(value);
    if (!Number.isInteger(number) || number <= 0) return fallback;
    return number;
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

function isPlainObject(value) {
    return Boolean(value) && typeof value === 'object' && !Array.isArray(value);
}

__igsDefine(exports, "normalizePresetManifest", () => normalizePresetManifest);
__igsDefine(exports, "validatePresetManifest", () => validatePresetManifest);
__igsDefine(exports, "validatePresetByType", () => validatePresetByType);
__igsDefine(exports, "PRESET_TYPES", () => PRESET_TYPES);
__igsDefine(exports, "TEXT_PRESET_TYPES", () => TEXT_PRESET_TYPES);
__igsDefine(exports, "PRESET_TYPE_TO_API_GROUP", () => PRESET_TYPE_TO_API_GROUP);
__igsDefine(exports, "PRESET_TYPE_TO_CONFIG_KEY", () => PRESET_TYPE_TO_CONFIG_KEY);
});
__igsRegister("src/presets/text-presets.js", function(module, exports, require) {
const SCENE_REGEX_FIELDS = Object.freeze([
    'speaker',
    'emotion',
    'time',
    'weather',
    'location',
    'generatedImage',
]);
const DEFAULT_TEXT_FILTER_PRESET = Object.freeze({
    enabled: true,
    stripHtmlComments: true,
    allowUntaggedFallback: false,
    textIncludeTags: 'content',
    textExcludeTags: 'thinking\nSubtext_think\nStatus_block\ntext_to_image\nparallel_world\naftertalk',
    imageIncludeTags: 'image\ntext_to_image',
    imageExcludeTags: '',
});
const DEFAULT_TEXT_FORMAT_PRESET = Object.freeze({
    enabled: false,
    pattern: '',
    flags: '',
    replacement: '',
});
const DEFAULT_SCENE_REGEX_PRESET = Object.freeze({
    enabled: false,
    overrideExplicitTags: false,
    speaker: Object.freeze({ pattern: '', flags: '' }),
    emotion: Object.freeze({ pattern: '', flags: '' }),
    time: Object.freeze({ pattern: '', flags: '' }),
    weather: Object.freeze({ pattern: '', flags: '' }),
    location: Object.freeze({ pattern: '', flags: '' }),
    generatedImage: Object.freeze({ pattern: '', flags: '' }),
});
function normalizeTextFilterPreset(presetOrData) {
    const data = getPresetData(presetOrData);

    return Object.freeze({
        enabled: normalizeBoolean(data.enabled, DEFAULT_TEXT_FILTER_PRESET.enabled),
        stripHtmlComments: normalizeBoolean(data.stripHtmlComments, DEFAULT_TEXT_FILTER_PRESET.stripHtmlComments),
        allowUntaggedFallback: normalizeBoolean(data.allowUntaggedFallback, DEFAULT_TEXT_FILTER_PRESET.allowUntaggedFallback),
        textIncludeTags: normalizeTagValue(data.textIncludeTags, DEFAULT_TEXT_FILTER_PRESET.textIncludeTags),
        textExcludeTags: normalizeTagValue(data.textExcludeTags, DEFAULT_TEXT_FILTER_PRESET.textExcludeTags),
        imageIncludeTags: normalizeTagValue(data.imageIncludeTags, DEFAULT_TEXT_FILTER_PRESET.imageIncludeTags),
        imageExcludeTags: normalizeTagValue(data.imageExcludeTags, DEFAULT_TEXT_FILTER_PRESET.imageExcludeTags),
    });
}
function normalizeTextFormatPreset(presetOrData) {
    const data = getPresetData(presetOrData);

    return Object.freeze({
        enabled: normalizeBoolean(data.enabled, DEFAULT_TEXT_FORMAT_PRESET.enabled),
        pattern: normalizeString(data.pattern, DEFAULT_TEXT_FORMAT_PRESET.pattern),
        flags: normalizeRegexFlags(data.flags, DEFAULT_TEXT_FORMAT_PRESET.flags),
        replacement: typeof data.replacement === 'string'
            ? data.replacement
            : data.replacement == null
                ? DEFAULT_TEXT_FORMAT_PRESET.replacement
                : String(data.replacement),
    });
}
function normalizeSceneRegexPreset(presetOrData) {
    const data = getPresetData(presetOrData);
    const patterns = isPlainObject(data.patterns) ? data.patterns : {};
    const normalized = {
        enabled: normalizeBoolean(data.enabled, DEFAULT_SCENE_REGEX_PRESET.enabled),
        overrideExplicitTags: normalizeBoolean(data.overrideExplicitTags, DEFAULT_SCENE_REGEX_PRESET.overrideExplicitTags),
    };

    for (const field of SCENE_REGEX_FIELDS) {
        normalized[field] = normalizeRegexEntry(
            data[field] != null ? data[field] : patterns[field],
        );
    }

    return Object.freeze(normalized);
}
function validateTextFormatPreset(presetOrData) {
    const preset = normalizeTextFormatPreset(presetOrData);
    const errors = [];
    let regex = null;

    if (preset.enabled && preset.pattern) {
        try {
            regex = new RegExp(preset.pattern, preset.flags);
        } catch (error) {
            errors.push(createRegexError('text-format-preset', 'pattern', error));
        }
    }

    return Object.freeze({
        ok: errors.length === 0,
        preset,
        regex,
        errors,
    });
}
function validateSceneRegexPreset(presetOrData) {
    const preset = normalizeSceneRegexPreset(presetOrData);
    const compiled = {};
    const errors = [];

    for (const field of SCENE_REGEX_FIELDS) {
        const entry = preset[field];
        if (!entry.pattern || !preset.enabled) {
            compiled[field] = null;
            continue;
        }

        try {
            compiled[field] = new RegExp(entry.pattern, entry.flags);
        } catch (error) {
            compiled[field] = null;
            errors.push(createRegexError('scene-regex-preset', field, error));
        }
    }

    return Object.freeze({
        ok: errors.length === 0,
        preset,
        compiled: Object.freeze(compiled),
        errors,
    });
}

function getPresetData(presetOrData) {
    if (!isPlainObject(presetOrData)) return {};
    if (isPlainObject(presetOrData.data)) return presetOrData.data;
    return presetOrData;
}

function normalizeRegexEntry(value) {
    if (typeof value === 'string') {
        return Object.freeze({
            pattern: value.trim(),
            flags: '',
        });
    }

    if (!isPlainObject(value)) {
        return Object.freeze({
            pattern: '',
            flags: '',
        });
    }

    return Object.freeze({
        pattern: normalizeString(value.pattern ?? value.regex, ''),
        flags: normalizeRegexFlags(value.flags, ''),
    });
}

function normalizeString(value, fallback) {
    if (value == null) return fallback;
    const text = String(value).trim();
    return text || fallback;
}

function normalizeTagValue(value, fallback) {
    if (Array.isArray(value)) {
        return value
            .map((item) => String(item || '').trim())
            .filter(Boolean)
            .join('\n');
    }
    return normalizeString(value, fallback);
}

function normalizeRegexFlags(value, fallback) {
    if (value == null) return fallback;
    const text = String(value).replace(/\s+/g, '');
    if (!text) return fallback;

    const seen = new Set();
    let flags = '';

    for (const char of text) {
        if (seen.has(char)) continue;
        seen.add(char);
        flags += char;
    }

    return flags;
}

function normalizeBoolean(value, fallback) {
    if (typeof value === 'boolean') return value;
    if (typeof value === 'string') {
        const normalized = value.trim().toLowerCase();
        if (normalized === 'true') return true;
        if (normalized === 'false') return false;
    }
    return fallback;
}

function createRegexError(presetType, field, error) {
    return Object.freeze({
        presetType,
        field,
        message: error instanceof Error ? error.message : String(error),
    });
}

function isPlainObject(value) {
    return Boolean(value) && typeof value === 'object' && !Array.isArray(value);
}

__igsDefine(exports, "normalizeTextFilterPreset", () => normalizeTextFilterPreset);
__igsDefine(exports, "normalizeTextFormatPreset", () => normalizeTextFormatPreset);
__igsDefine(exports, "normalizeSceneRegexPreset", () => normalizeSceneRegexPreset);
__igsDefine(exports, "validateTextFormatPreset", () => validateTextFormatPreset);
__igsDefine(exports, "validateSceneRegexPreset", () => validateSceneRegexPreset);
__igsDefine(exports, "DEFAULT_TEXT_FILTER_PRESET", () => DEFAULT_TEXT_FILTER_PRESET);
__igsDefine(exports, "DEFAULT_TEXT_FORMAT_PRESET", () => DEFAULT_TEXT_FORMAT_PRESET);
__igsDefine(exports, "DEFAULT_SCENE_REGEX_PRESET", () => DEFAULT_SCENE_REGEX_PRESET);
});
__igsRegister("src/api/igs-compat.js", function(module, exports, require) {
const { resolveLegacyReaderMode } = require("src/storage/legacy-igs.js");
const { parseSceneText } = require("src/scene/text-parser.js");
const { buildIgsTextPayload } = require("src/scene/message-source.js");
const { PUBLIC_READER_MODES } = require("src/schemas/reader-mode.js");
function createIgsCompatApi(app) {
    return {
        openSettings(options = {}) {
            const payload = cloneData(options);
            if (app.events && typeof app.events.emit === 'function') {
                app.events.emit('igs:settings-requested', payload);
            }
            if (!app.igsUi || typeof app.igsUi.openSettings !== 'function') {
                return { ok: false, reason: 'settings-ui-not-mounted', options: payload };
            }
            return app.igsUi.openSettings(payload);
        },

        getConfig() {
            return cloneData(app.getState().config || {});
        },

        getUnifiedSettings(options = {}) {
            if (typeof app.getUnifiedSettingsSnapshot === 'function') {
                return app.getUnifiedSettingsSnapshot(options);
            }

            const legacy = getLegacySettings(app);
            const bridge = cloneData(app.getState().config || {});
            const readerMode = resolveReaderMode(options, legacy, bridge);
            const readerSettingsByMode = legacy.readerSettingsByMode || {};
            // 全模式共用 default 桶；老用户 default 空时回退旧分桶。
            const hasKeys = (obj) => obj && typeof obj === 'object' && Object.keys(obj).length > 0;
            const readerSettings = cloneData(
                hasKeys(readerSettingsByMode['default']) ? readerSettingsByMode['default']
                    : hasKeys(readerSettingsByMode[readerMode]) ? readerSettingsByMode[readerMode]
                    : legacy.readerSettings || {}
            );

            return {
                version: app.version,
                bridge,
                imageApi: cloneData(bridge.imageApi || {}),
                readerMode,
                readerSettings,
            };
        },

        async openViewerFromMessage(messageId, mode, openOptions = {}) {
            const normalizedId = normalizeMessageId(messageId);
            const resolved = normalizeViewerRequest(mode, openOptions);
            const readerMode = resolveReaderMode({ mode: resolved.mode }, getLegacySettings(app), app.getState().config || {});
            if (normalizedId == null) {
                return { ok: false, reason: 'invalid-message-id', messageId, mode: readerMode, options: cloneViewerOptions(resolved.options) };
            }
            if (!app.hostAdapter || typeof app.hostAdapter.getMessageById !== 'function') {
                return { ok: false, reason: 'message-lookup-not-supported', messageId: normalizedId, mode: readerMode };
            }
            const message = resolved.options.message || await app.hostAdapter.getMessageById(normalizedId);
            if (!message) {
                return { ok: false, reason: 'message-not-found', messageId: normalizedId, mode: readerMode };
            }
            const readerPayload = buildReaderPayload(app, message, normalizedId, readerMode);
            const refreshed = await app.refresh({
                message,
                messageId: normalizedId,
                viewerMode: readerMode,
                textScene: readerPayload.textScene,
            });
            const basePayload = {
                ...readerPayload,
                render: refreshed.render,
                scene: refreshed.scene,
                startAtEnd: resolved.options.startAtEnd === true,
            };
            const payload = resolved.options.skipImageCollection === true
                ? basePayload
                : await enrichReaderPayload(app, basePayload);
            if (resolved.options.replaceActive === true
                && app.igsUi && typeof app.igsUi.replaceReader === 'function') {
                const reader = app.igsUi.replaceReader({
                    ...payload,
                    startAtEnd: false,
                }, {
                    keepEmbeddedMount: resolved.options.keepEmbeddedMount === true,
                    turnOffset: resolved.options.turnOffset,
                });
                return { ...refreshed, ok: refreshed.ok !== false && reader.ok !== false, reader };
            }
            return openReaderUi(app, {
                ...payload,
            }, refreshed);
        },

        async openLatestAvailable(mode, options = {}) {
            const readerMode = resolveReaderMode({ mode }, getLegacySettings(app), app.getState().config || {});
            if (!app.hostAdapter || typeof app.hostAdapter.getCurrentMessage !== 'function') {
                return { ok: false, reason: 'missing-host-message-api', mode: readerMode, options: cloneData(options) };
            }
            const message = await app.hostAdapter.getCurrentMessage();
            if (!message) {
                return { ok: false, reason: 'no-message', mode: readerMode, options: cloneData(options) };
            }
            const readerPayload = buildReaderPayload(app, message, message.id, readerMode);
            const refreshed = await app.refresh({
                message,
                messageId: message.id,
                viewerMode: readerMode,
                textScene: readerPayload.textScene,
            });
            const payload = await enrichReaderPayload(app, {
                ...readerPayload,
                render: refreshed.render,
                scene: refreshed.scene,
            });
            return openReaderUi(app, {
                ...payload,
            }, refreshed);
        },

        generateImage(request, options = {}) {
            if (typeof app.generateImage === 'function') {
                return app.generateImage(request, options);
            }
            return {
                ok: false,
                reason: 'provider-not-enabled',
                request: cloneData(request),
                options: cloneData(options),
            };
        },
    };
}

function getLegacySettings(app) {
    if (!app || typeof app.getLegacyIgsSettings !== 'function') {
        return {
            ok: true,
            bridge: {},
            displayMode: '',
            readerMode: 'pc',
            readerSettings: {},
            readerSettingsByMode: {},
        };
    }
    return app.getLegacyIgsSettings();
}

function resolveReaderMode(options, legacySettings, bridgeConfig) {
    const requestedMode = options && typeof options === 'object' ? options.mode : options;
    const legacyDisplayMode = legacySettings && legacySettings.ok !== false ? legacySettings.displayMode : '';
    const legacyBridge = legacySettings && legacySettings.ok !== false ? legacySettings.bridge : {};
    const resolved = resolveLegacyReaderMode(
        requestedMode,
        legacyDisplayMode,
        bridgeConfig && Object.keys(bridgeConfig).length ? bridgeConfig : legacyBridge,
    );
    return PUBLIC_READER_MODES.includes(resolved) ? resolved : 'pc';
}

function normalizeMessageId(messageId) {
    const value = Number(messageId);
    if (!Number.isFinite(value) || value < 0) return null;
    return value;
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

function buildReaderPayload(app, message, messageId, readerMode) {
    const unifiedSettings = typeof app.getUnifiedSettingsSnapshot === 'function'
        ? app.getUnifiedSettingsSnapshot({ mode: readerMode })
        : null;
    const bridge = cloneData(
        unifiedSettings && unifiedSettings.bridge && typeof unifiedSettings.bridge === 'object'
            ? unifiedSettings.bridge
            : app.getState().config || {},
    );
    const visualNovelText = buildIgsTextPayload(message, {
        sourceFilter: bridge.sourceFilter,
        virtualRegex: bridge.virtualRegex,
        sceneAssets: bridge.sceneAssets,
        sentencePaging: bridge.sentencePaging,
    });
    const textScene = parseSceneText(
        visualNovelText.formattedText || visualNovelText.visibleText || visualNovelText.cleanedRaw || '',
        { messageId },
    );
    const readerText = Array.isArray(visualNovelText.textSegments) && visualNovelText.textSegments.length
        ? visualNovelText.textSegments.join('\n')
        : textScene.text;

    return {
        ...visualNovelText,
        message,
        messageId,
        mode: readerMode,
        viewerMode: readerMode,
        sourceFilter: bridge.sourceFilter,
        virtualRegex: bridge.virtualRegex,
        textScene: {
            ...textScene,
            text: readerText,
        },
    };
}

async function enrichReaderPayload(app, payload) {
    if (!app || typeof app.collectMessageImages !== 'function') {
        return payload;
    }
    const preferredImageIndex = resolvePreferredImageIndex(payload);
    const imageState = await app.collectMessageImages({
        message: payload.message,
        messageId: payload.messageId,
        scene: payload.scene,
        render: payload.render,
        currentIndex: payload.startAtEnd === true ? Number.MAX_SAFE_INTEGER : 0,
        textIndex: payload.startAtEnd === true
            ? resolveLastTextIndex(payload)
            : 0,
        preferredImageIndex,
        imageSource: payload.imageSource,
        imageSlots: payload.imageSlots,
        segmentImageSlots: payload.segmentImageSlots,
        requiresMessageScope: Array.isArray(payload.imageSlots) && payload.imageSlots.length > 0,
        mode: payload.mode,
    });
    return {
        ...payload,
        imageState,
    };
}

function openReaderUi(app, payload, refreshed) {
    if (!app.igsUi || typeof app.igsUi.openReader !== 'function') {
        return refreshed;
    }
    const reader = app.igsUi.openReader(payload, { mode: payload.mode });
    return {
        ...refreshed,
        ok: refreshed.ok !== false && reader.ok !== false,
        reader,
    };
}

function normalizeViewerRequest(mode, openOptions) {
    if (mode && typeof mode === 'object' && !Array.isArray(mode)) {
        return {
            mode: mode.mode,
            options: cloneViewerOptions(mode),
        };
    }
    return {
        mode,
        options: cloneViewerOptions(openOptions || {}),
    };
}

function cloneViewerOptions(options = {}) {
    const clone = { ...options };
    if (Object.prototype.hasOwnProperty.call(clone, 'message')) {
        clone.message = options.message || null;
    }
    return clone;
}

function resolveLastTextIndex(payload) {
    const textSegments = Array.isArray(payload && payload.textSegments) ? payload.textSegments : [];
    return textSegments.length ? textSegments.length - 1 : 0;
}

function resolvePreferredImageIndex(payload) {
    const segmentImageSlots = Array.isArray(payload && payload.segmentImageSlots)
        ? payload.segmentImageSlots
        : [];
    if (!segmentImageSlots.length) return 0;
    const textIndex = payload && payload.startAtEnd === true
        ? segmentImageSlots.length - 1
        : 0;
    const preferred = Number(segmentImageSlots[textIndex]);
    if (!Number.isFinite(preferred) || preferred < 0) return 0;
    return Math.floor(preferred);
}

__igsDefine(exports, "createIgsCompatApi", () => createIgsCompatApi);
});
__igsRegister("src/storage/legacy-igs.js", function(module, exports, require) {
const { LEGACY_READER_MODE_KEYS } = require("src/schemas/reader-mode.js");
const LEGACY_VN_KEYS = Object.freeze({
    bridge: 'igs_bridge_config',
    readerPrefix: 'igs-reader-settings-v9-',
    displayMode: 'igs-display-mode',
});
const LEGACY_READER_MODES = LEGACY_READER_MODE_KEYS;
function readLegacyIgsSettings(storageLike, preferredMode) {
    const result = {
        ok: true,
        bridge: {},
        displayMode: '',
        readerMode: 'pc',
        readerSettings: {},
        readerSettingsByMode: {},
    };
    if (!isStorageLike(storageLike)) {
        result.readerMode = resolveLegacyReaderMode(preferredMode, result.displayMode, result.bridge);
        return result;
    }

    const bridgeRecord = readJsonStorage(storageLike, LEGACY_VN_KEYS.bridge);
    if (!bridgeRecord.ok) return bridgeRecord;
    result.bridge = normalizeLegacyIgsSettings(bridgeRecord.value);

    const displayModeRecord = readStringStorage(storageLike, LEGACY_VN_KEYS.displayMode);
    if (!displayModeRecord.ok) return displayModeRecord;
    result.displayMode = displayModeRecord.value;

    for (const mode of LEGACY_READER_MODES) {
        const record = readJsonStorage(storageLike, `${LEGACY_VN_KEYS.readerPrefix}${mode}`);
        if (!record.ok) return record;
        result.readerSettingsByMode[mode] = normalizeLegacyIgsSettings(record.value);
    }

    result.readerMode = resolveLegacyReaderMode(preferredMode, result.displayMode, result.bridge);
    result.readerSettings = cloneData(result.readerSettingsByMode[result.readerMode] || {});
    return result;
}
function writeLegacyIgsSettings(storageLike, nextState = {}) {
    if (!isStorageLike(storageLike)) {
        return {
            ok: false,
            reason: 'missing-storage-like',
        };
    }

    const normalized = normalizeLegacySnapshot(nextState);

    try {
        storageLike.setItem(LEGACY_VN_KEYS.bridge, JSON.stringify(normalized.bridge));
        storageLike.setItem(LEGACY_VN_KEYS.displayMode, normalized.displayMode);
        for (const mode of LEGACY_READER_MODES) {
            storageLike.setItem(`${LEGACY_VN_KEYS.readerPrefix}${mode}`, JSON.stringify(normalized.readerSettingsByMode[mode]));
        }
        return {
            ok: true,
            legacy: normalized,
        };
    } catch (error) {
        return {
            ok: false,
            reason: 'legacy-storage-write-failed',
            message: error instanceof Error ? error.message : String(error),
            legacy: normalized,
        };
    }
}
function normalizeLegacyIgsSettings(raw) {
    if (!raw || typeof raw !== 'object' || Array.isArray(raw)) return {};
    return cloneData(raw);
}
function resolveLegacyReaderMode(preferredMode, displayMode, bridgeConfig) {
    const candidates = [
        preferredMode,
        displayMode,
        bridgeConfig && bridgeConfig.openMode,
        'pc',
    ];
    for (const candidate of candidates) {
        if (LEGACY_READER_MODES.includes(candidate)) return candidate;
    }
    return 'pc';
}

function readJsonStorage(storageLike, key) {
    const raw = readStorageValue(storageLike, key);
    if (raw == null || raw === '') {
        return { ok: true, key, value: {} };
    }
    try {
        return { ok: true, key, value: JSON.parse(raw) };
    } catch (error) {
        return {
            ok: false,
            reason: 'invalid-legacy-json',
            key,
            message: error instanceof Error ? error.message : String(error),
        };
    }
}

function readStringStorage(storageLike, key) {
    const raw = readStorageValue(storageLike, key);
    return { ok: true, key, value: raw == null ? '' : String(raw).trim() };
}

function normalizeLegacySnapshot(raw) {
    const bridge = normalizeLegacyIgsSettings(raw.bridge);
    const displayMode = resolveLegacyReaderMode(raw.displayMode, raw.displayMode, bridge);
    const readerMode = resolveLegacyReaderMode(raw.readerMode, displayMode, bridge);
    const readerSettingsByMode = {};

    for (const mode of LEGACY_READER_MODES) {
        const sourceByMode = raw.readerSettingsByMode && raw.readerSettingsByMode[mode];
        const source = mode === readerMode
            ? firstDefined(raw.readerSettings, sourceByMode, {})
            : firstDefined(sourceByMode, {});
        readerSettingsByMode[mode] = normalizeLegacyIgsSettings(source);
    }

    return {
        ok: true,
        bridge,
        displayMode,
        readerMode,
        readerSettings: cloneData(readerSettingsByMode[readerMode] || {}),
        readerSettingsByMode,
    };
}

function readStorageValue(storageLike, key) {
    if (!isStorageLike(storageLike)) return null;
    try {
        return storageLike.getItem(key);
    } catch (error) {
        return null;
    }
}

function isStorageLike(value) {
    return !!value && typeof value.getItem === 'function';
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

function firstDefined(...values) {
    for (const value of values) {
        if (value !== undefined && value !== null) return value;
    }
    return undefined;
}

__igsDefine(exports, "readLegacyIgsSettings", () => readLegacyIgsSettings);
__igsDefine(exports, "writeLegacyIgsSettings", () => writeLegacyIgsSettings);
__igsDefine(exports, "normalizeLegacyIgsSettings", () => normalizeLegacyIgsSettings);
__igsDefine(exports, "resolveLegacyReaderMode", () => resolveLegacyReaderMode);
__igsDefine(exports, "LEGACY_VN_KEYS", () => LEGACY_VN_KEYS);
__igsDefine(exports, "LEGACY_READER_MODES", () => LEGACY_READER_MODES);
});
__igsRegister("src/schemas/reader-mode.js", function(module, exports, require) {
const EMBEDDED_READER_MODE = 'embedded';

const READER_MODES = Object.freeze(['pc', 'mobile', 'web', 'fullscreen', EMBEDDED_READER_MODE]);
const STORED_READER_MODES = Object.freeze(['pc', 'mobile', 'web', 'fullscreen', EMBEDDED_READER_MODE, 'default']);

const READER_MODE_LABELS = Object.freeze({
    pc: '电脑',
    mobile: '手机',
    web: '网页全屏',
    fullscreen: '全屏',
    [EMBEDDED_READER_MODE]: '楼层内嵌',
});
const PUBLIC_READER_MODES = READER_MODES;
const LEGACY_READER_MODE_KEYS = STORED_READER_MODES;
function getReaderModeLabel(mode) {
    return READER_MODE_LABELS[mode] || READER_MODE_LABELS.pc;
}

// 五种可用模式用于设置页选择器与校验；存储层额外允许 'default' 桶名。
// 旧值、空值、未知值统一回落 pc，保证历史配置无需迁移即可读取。
function normalizePublicReaderMode(mode) {
    const value = typeof mode === 'string' ? mode.trim() : mode;
    return READER_MODES.includes(value) ? value : 'pc';
}
function isEmbeddedReaderMode(mode) {
    return mode === EMBEDDED_READER_MODE;
}

__igsDefine(exports, "getReaderModeLabel", () => getReaderModeLabel);
__igsDefine(exports, "normalizePublicReaderMode", () => normalizePublicReaderMode);
__igsDefine(exports, "isEmbeddedReaderMode", () => isEmbeddedReaderMode);
__igsDefine(exports, "EMBEDDED_READER_MODE", () => EMBEDDED_READER_MODE);
__igsDefine(exports, "PUBLIC_READER_MODES", () => PUBLIC_READER_MODES);
__igsDefine(exports, "LEGACY_READER_MODE_KEYS", () => LEGACY_READER_MODE_KEYS);
});
__igsRegister("src/scene/text-parser.js", function(module, exports, require) {
const { runTextPipeline } = require("src/scene/text-pipeline.js");
const TAG_ALIASES = Object.freeze({
    speaker: ['speaker', '角色', '说话人'],
    emotion: ['emotion', '情绪'],
    time: ['time', '时间'],
    weather: ['weather', '天气'],
    location: ['location', '地点', '位置'],
    generatedImage: ['generatedImage', '生图', '插图'],
});
function parseSceneText(text = '', options = {}) {
    const rawText = String(text || '');
    const pipeline = shouldUseTextPipeline(options)
        ? runTextPipeline(rawText, options)
        : createDefaultPipeline(rawText);
    const scene = {
        messageId: options.messageId == null ? null : options.messageId,
        text: '',
        textSource: pipeline.textSource,
        formattedText: pipeline.formattedText,
        sourceKind: pipeline.sourceKind,
        formatSourceKind: pipeline.formatSourceKind,
        textPipelineWarnings: pipeline.warnings,
        textPipelineErrors: pipeline.errors,
    };
    const body = [];
    const explicitKeys = new Set();

    for (const line of String(pipeline.formattedText || '').split(/\r?\n/)) {
        const tag = parseTag(line);
        if (tag) {
            const appliedKey = applyTag(scene, tag);
            if (appliedKey) explicitKeys.add(appliedKey);
            continue;
        }
        body.push(line);
    }

    scene.text = body.join('\n').trim();
    parseSpeakerPrefix(scene);
    applyScenePatch(scene, pipeline.scenePatch, {
        explicitKeys,
        overrideExplicitTags: pipeline.overrideExplicitTags,
    });
    return scene;
}

function parseTag(line) {
    const match = String(line).match(/^\s*\[([^:\]：]+)\s*[:：]\s*(.*?)\]\s*$/);
    if (!match) return null;
    return {
        key: match[1].trim(),
        value: match[2].trim(),
    };
}

function applyTag(scene, tag) {
    const normalizedKey = normalizeKey(tag.key);
    if (!normalizedKey) return null;
    if (normalizedKey === 'generatedImage') {
        scene.generatedImage = tag.value ? { source: 'text-tag', value: tag.value } : null;
        return normalizedKey;
    }
    scene[normalizedKey] = tag.value;
    return normalizedKey;
}

function normalizeKey(key) {
    return Object.entries(TAG_ALIASES).find(([, aliases]) => aliases.includes(key))?.[0] || null;
}

function parseSpeakerPrefix(scene) {
    if (scene.speaker || !scene.text) return;
    const match = scene.text.match(/^([^:：\n]{1,24})\s*[:：]\s*(.+)$/s);
    if (!match) return;
    if (match[1].trim().startsWith('@')) return;
    scene.speaker = match[1].trim();
    scene.text = match[2].trim();
}

function applyScenePatch(scene, patch = {}, options = {}) {
    for (const [key, value] of Object.entries(patch)) {
        if (!hasSceneValue(value)) continue;
        if (options.overrideExplicitTags !== true && options.explicitKeys && options.explicitKeys.has(key)) {
            continue;
        }
        scene[key] = key === 'generatedImage'
            ? normalizeGeneratedImage(value)
            : value;
    }
}

function normalizeGeneratedImage(value) {
    if (!value) return null;
    if (typeof value === 'string') {
        return {
            source: 'scene-regex-preset',
            value,
        };
    }
    return {
        ...value,
        source: value.source || 'scene-regex-preset',
    };
}

function hasSceneValue(value) {
    if (value == null) return false;
    if (typeof value === 'string') return value.trim().length > 0;
    if (typeof value === 'object' && 'value' in value) {
        return String(value.value || '').trim().length > 0;
    }
    return true;
}

function shouldUseTextPipeline(options = {}) {
    return Boolean(
        options.textFilterPreset
        || options.textFormatPreset
        || options.sceneRegexPreset,
    );
}

function createDefaultPipeline(rawText) {
    const normalizedText = String(rawText || '').trim();
    return {
        ok: true,
        raw: rawText,
        textSource: normalizedText,
        formattedText: normalizedText,
        imageSource: '',
        scenePatch: {},
        sourceKind: 'raw-text',
        formatSourceKind: 'raw-text',
        expectedTags: [],
        warnings: [],
        errors: [],
        overrideExplicitTags: false,
    };
}

__igsDefine(exports, "parseSceneText", () => parseSceneText);
});
__igsRegister("src/scene/text-pipeline.js", function(module, exports, require) {
const { normalizeTextFilterPreset, validateSceneRegexPreset, validateTextFormatPreset } = require("src/presets/text-presets.js");
const REGEX_CAPTURE_FIELDS = Object.freeze([
    'speaker',
    'emotion',
    'time',
    'weather',
    'location',
    'generatedImage',
]);
function parseTagList(text) {
    const source = Array.isArray(text) ? text.join('\n') : String(text || '');
    return source
        .split(/[\r\n,]+/)
        .map((item) => item.trim())
        .filter(Boolean);
}
function extractTagBlocks(raw, tags, options = {}) {
    const text = String(raw || '');
    const tagList = parseTagList(tags);
    const blocks = [];

    for (const tag of tagList) {
        const regex = new RegExp(`<${escapeRegex(tag)}\\b[^>]*>([\\s\\S]*?)<\\/${escapeRegex(tag)}>`, 'gi');
        for (const match of text.matchAll(regex)) {
            const block = options.trim === false ? match[1] : normalizeMultiline(match[1]);
            if (block) blocks.push(block);
        }
    }

    return blocks;
}
function removeTagBlocks(raw, tags) {
    let text = String(raw || '');

    for (const tag of parseTagList(tags)) {
        const regex = new RegExp(`<${escapeRegex(tag)}\\b[^>]*>([\\s\\S]*?)<\\/${escapeRegex(tag)}>`, 'gi');
        text = text.replace(regex, '');
    }

    return normalizeMultiline(text);
}
function buildFilteredTextSource(raw, filterPreset, options = {}) {
    const preset = normalizeTextFilterPreset(filterPreset);
    const baseRaw = String(raw || '');
    const strippedRaw = preset.stripHtmlComments ? stripHtmlComments(baseRaw) : baseRaw;
    const textIncludeTags = parseTagList(preset.textIncludeTags);
    const textExcludeTags = parseTagList(preset.textExcludeTags);
    const imageIncludeTags = parseTagList(preset.imageIncludeTags);
    const imageExcludeTags = parseTagList(preset.imageExcludeTags);
    const warnings = [];
    const errors = [];

    if (!preset.enabled) {
        return {
            ok: true,
            textSource: normalizeMultiline(strippedRaw),
            imageSource: '',
            sourceKind: 'raw-text',
            expectedTags: textIncludeTags,
            warnings,
            errors,
            rawTextSource: normalizeMultiline(strippedRaw),
        };
    }

    const includedBlocks = extractTagBlocks(strippedRaw, textIncludeTags, options);
    const rawTextSource = includedBlocks.length > 0
        ? normalizeMultiline(includedBlocks.join('\n'))
        : normalizeMultiline(strippedRaw);
    let textSource = '';
    let sourceKind = 'tagged-content';

    if (includedBlocks.length > 0) {
        textSource = normalizeMultiline(
            includedBlocks
                .map((block) => removeTagBlocks(block, textExcludeTags))
                .filter(Boolean)
                .join('\n'),
        );
    } else if (preset.allowUntaggedFallback) {
        textSource = removeTagBlocks(strippedRaw, textExcludeTags);
        sourceKind = 'untagged-fallback';
        if (textIncludeTags.length > 0) {
            warnings.push({
                code: 'missing-include-tags',
                message: `Missing include tags: ${textIncludeTags.join(', ')}`,
            });
        }
    } else {
        sourceKind = 'missing-include-tags';
        warnings.push({
            code: 'missing-include-tags',
            message: `Missing include tags: ${textIncludeTags.join(', ')}`,
        });
    }

    const imageBlocks = extractTagBlocks(
        removeTagBlocks(strippedRaw, imageExcludeTags),
        imageIncludeTags,
        options,
    );
    const imageSource = normalizeMultiline(imageBlocks.join('\n'));

    return {
        ok: errors.length === 0,
        textSource,
        imageSource,
        sourceKind,
        expectedTags: textIncludeTags,
        warnings,
        errors,
        rawTextSource,
    };
}
function applyTextFormatPreset(text, formatPreset) {
    const sourceText = String(text || '');
    const validation = validateTextFormatPreset(formatPreset);
    const warnings = [];
    const errors = validation.errors.map(cloneIssue);

    if (!validation.preset.enabled || !validation.preset.pattern) {
        return {
            ok: errors.length === 0,
            text: normalizeMultiline(sourceText),
            sourceKind: 'format-disabled',
            warnings,
            errors,
        };
    }

    if (!validation.ok || !validation.regex) {
        return {
            ok: false,
            text: normalizeMultiline(sourceText),
            sourceKind: 'invalid-format-regex',
            warnings,
            errors,
        };
    }

    const formatted = sourceText.replace(validation.regex, validation.preset.replacement);
    return {
        ok: true,
        text: normalizeMultiline(formatted),
        sourceKind: formatted === sourceText ? 'regex-noop' : 'regex-replace',
        warnings,
        errors,
    };
}
function applySceneRegexPreset(text, regexPreset) {
    const sourceText = String(text || '');
    const validation = validateSceneRegexPreset(regexPreset);
    const warnings = [];
    const errors = validation.errors.map(cloneIssue);
    const scenePatch = {};

    if (!validation.preset.enabled) {
        return {
            ok: errors.length === 0,
            scenePatch,
            warnings,
            errors,
            overrideExplicitTags: validation.preset.overrideExplicitTags,
        };
    }

    for (const field of REGEX_CAPTURE_FIELDS) {
        const regex = validation.compiled[field];
        if (!regex) continue;
        regex.lastIndex = 0;
        const match = regex.exec(sourceText);
        if (!match) continue;

        const value = normalizeMultiline(getFirstCapture(match));
        if (!value) continue;

        scenePatch[field] = field === 'generatedImage'
            ? { source: 'scene-regex-preset', value }
            : value;
    }

    return {
        ok: errors.length === 0,
        scenePatch,
        warnings,
        errors,
        overrideExplicitTags: validation.preset.overrideExplicitTags,
    };
}
function runTextPipeline(raw, options = {}) {
    const filtered = buildFilteredTextSource(raw, options.textFilterPreset, options);
    const formatted = applyTextFormatPreset(filtered.textSource, options.textFormatPreset);
    const regexResult = applySceneRegexPreset(
        formatted.text,
        options.sceneRegexPreset,
    );

    return {
        ok: filtered.ok && formatted.ok && regexResult.ok,
        raw: String(raw || ''),
        textSource: filtered.textSource,
        formattedText: formatted.text,
        imageSource: filtered.imageSource,
        scenePatch: regexResult.scenePatch,
        sourceKind: filtered.sourceKind,
        formatSourceKind: formatted.sourceKind,
        expectedTags: filtered.expectedTags,
        warnings: [...filtered.warnings, ...formatted.warnings, ...regexResult.warnings],
        errors: [...filtered.errors, ...formatted.errors, ...regexResult.errors],
        overrideExplicitTags: regexResult.overrideExplicitTags,
    };
}

function getFirstCapture(match) {
    for (let index = 1; index < match.length; index += 1) {
        if (match[index]) return match[index];
    }
    return match[0] || '';
}

function stripHtmlComments(text) {
    return text.replace(/<!--[\s\S]*?-->/g, '');
}

function normalizeMultiline(text) {
    return String(text || '')
        .replace(/\r\n?/g, '\n')
        .replace(/\n{3,}/g, '\n\n')
        .trim();
}

function escapeRegex(text) {
    return String(text).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function cloneIssue(issue) {
    return issue && typeof issue === 'object' ? { ...issue } : issue;
}

__igsDefine(exports, "parseTagList", () => parseTagList);
__igsDefine(exports, "extractTagBlocks", () => extractTagBlocks);
__igsDefine(exports, "removeTagBlocks", () => removeTagBlocks);
__igsDefine(exports, "buildFilteredTextSource", () => buildFilteredTextSource);
__igsDefine(exports, "applyTextFormatPreset", () => applyTextFormatPreset);
__igsDefine(exports, "applySceneRegexPreset", () => applySceneRegexPreset);
__igsDefine(exports, "runTextPipeline", () => runTextPipeline);
});
__igsRegister("src/scene/message-source.js", function(module, exports, require) {
const { buildNarrativeSegments, buildSegmentImageMap, parseImageSlots } = require("src/scene/image-slots.js");
const { extractSceneDirectives } = require("src/scene/scene-directives.js");
const { parseSceneText } = require("src/scene/text-parser.js");
const DEFAULT_SOURCE_FILTER = Object.freeze({
    enabled: true,
    stripHtmlComments: true,
    allowUntaggedFallback: false,
    textIncludeTags: 'content',
    textExcludeTags: 'thinking\nSubtext_think\nStatus_block\ntext_to_image\nparallel_world\naftertalk\nimage',
    imageIncludeTags: 'image\ntext_to_image',
    imageExcludeTags: '',
});
const DEFAULT_VIRTUAL_REGEX = Object.freeze({
    enabled: true,
    pattern: '\\[igs-char:([^|\\]]+)\\|[^|\\]]+\\|([^\\]]+)\\]',
    flags: 'gm',
    replacement: '[$1]：$2',
});

const SENTENCE_PAGING_TERMINATOR = '。';

const THOUGHT_RE_GLOBAL = /\[igs-thought:([^|\]]+)\|[^|\]]+\|([^\]]+)\]/gm;

// 数据层正文里是否含 IGS 场景标签（给 AI 看的格式标记）。宿主前端会用正则把这些
// 标签从渲染层 .mes_text 隐藏，所以「数据层有标签、DOM 无标签」是渲染清洗造成的
// 结构性差异，绝不能当成关键词插件改词而用 DOM 覆盖数据层。
const IGS_DIRECTIVE_TAG_RE = /\[igs-(?:scene|char|thought):/;
const IGS_DIRECTIVE_LINE_RE = /^\[igs-(?:scene|char|thought):[^\]]*\]/;

function hasIgsDirectiveTags(text) {
    return IGS_DIRECTIVE_TAG_RE.test(String(text || ''));
}

const HOST_UI_HTML_MARKERS = Object.freeze([
    'api connections',
    'rightnavholder',
    'drawer-opener',
    'sys-settings-button',
    'menu_button',
    'flex-container',
    'extensionsmenubutton',
    'send_textarea',
    'right-nav',
]);

const MESSAGE_TEXT_KEYS = Object.freeze([
    'rawHtml',
    'html',
    'mes_html',
    'message_html',
    'text',
    'message',
    'content',
    'mes',
]);
function getMessagePrimaryText(message) {
    if (typeof message === 'string') return message;
    if (!message || typeof message !== 'object') return '';

    for (const key of MESSAGE_TEXT_KEYS) {
        const value = message[key];
        if (typeof value === 'string' && value.trim()) {
            return value;
        }
    }

    if (typeof message.raw === 'string' && message.raw.trim()) {
        return message.raw;
    }

    if (message.raw && typeof message.raw === 'object' && message.raw !== message) {
        return getMessagePrimaryText(message.raw);
    }

    return '';
}
function getVisibleMessageTextFromElement(mesElement) {
    if (!mesElement || typeof mesElement.querySelector !== 'function') return '';
    const mesText = mesElement.querySelector('.mes_text') || mesElement;
    if (!mesText || typeof mesText.cloneNode !== 'function') return '';
    const cloneNode = mesText.cloneNode(true);
    if (typeof cloneNode.querySelectorAll === 'function') {
        cloneNode.querySelectorAll('script,style,iframe,button,[role="button"],[data-igs-internal-reader],.igs-img-ph,.igs-image-placeholder,[data-igs-image-placeholder="1"],.mes_buttons,.extraMesButtons').forEach((node) => {
            if (node && typeof node.remove === 'function') node.remove();
        });
    }
    const text = typeof cloneNode.innerText === 'string' && cloneNode.innerText
        ? cloneNode.innerText
        : cloneNode.textContent;
    return normalizeWhitespace(text);
}
function looksLikeHostUiHtml(text) {
    const source = String(text || '').trim();
    if (!source) return false;
    const lower = source.toLowerCase();
    const htmlLike = /<[^>]+>/.test(source);

    if (HOST_UI_HTML_MARKERS.some((marker) => lower.includes(marker))) {
        return true;
    }

    if (!htmlLike) return false;
    if (/<(?:button|input|textarea|select|nav|header|footer|aside)\b/i.test(source)) {
        return true;
    }
    if (!/<content\b/i.test(source) && /<(?:div|span|section|article|svg|path)\b/i.test(source) && /(class=|data-i18n=|aria-|role=)/i.test(source)) {
        return true;
    }
    return false;
}
function cleanNarrativeSource(text) {
    let cleaned = String(text || '');
    cleaned = cleaned.replace(/<\/content>\s*<content[^>]*>/gi, '\n\n');
    cleaned = cleaned.replace(/<image[^>]*>[\s\S]*?<\/image>/gi, ' \x00IMG\x00 ');
    cleaned = cleaned.replace(/<imgthink[^>]*>[\s\S]*?<\/imgthink>/gi, '');
    cleaned = cleaned.replace(/image###[\s\S]*?###/gi, '');
    cleaned = cleaned.replace(/<!--([\s\S]*?)-->/g, ' $1 ');
    cleaned = cleaned.replace(/<style[\s\S]*?<\/style>/gi, '');
    cleaned = cleaned.replace(/<script[^>]*>[\s\S]*?<\/script>/gi, '');
    cleaned = cleaned.replace(/<iframe[^>]*>[\s\S]*?<\/iframe>/gi, '');
    cleaned = cleaned.replace(/【[^】\n]{0,100}】/g, '');
    cleaned = cleaned.replace(/<[^>]+>/g, '');
    cleaned = cleaned.replace(/\r/g, '');
    return cleaned;
}
function normalizeSourceFilter(value) {
    const source = isPlainObject(value) ? value : {};
    const merged = { ...DEFAULT_SOURCE_FILTER, ...source };
    return {
        enabled: Boolean(merged.enabled),
        stripHtmlComments: Boolean(merged.stripHtmlComments),
        allowUntaggedFallback: merged.allowUntaggedFallback !== false,
        textIncludeTags: normalizeTagText(merged.textIncludeTags),
        textExcludeTags: normalizeTagText(merged.textExcludeTags),
        imageIncludeTags: normalizeTagText(merged.imageIncludeTags),
        imageExcludeTags: normalizeTagText(merged.imageExcludeTags),
    };
}
function normalizeVirtualRegex(value) {
    const source = isPlainObject(value) ? value : {};
    const merged = { ...DEFAULT_VIRTUAL_REGEX, ...source };
    return {
        enabled: merged.enabled !== false,
        pattern: String(merged.pattern == null ? DEFAULT_VIRTUAL_REGEX.pattern : merged.pattern),
        flags: String(merged.flags == null ? DEFAULT_VIRTUAL_REGEX.flags : merged.flags).replace(/\s+/g, ''),
        replacement: String(merged.replacement == null ? DEFAULT_VIRTUAL_REGEX.replacement : merged.replacement),
    };
}
function buildBridgeImageSource(raw, filter) {
    const cfg = normalizeSourceFilter(filter);
    let source = String(raw || '');
    if (!cfg.enabled) return source;
    if (cfg.stripHtmlComments) source = stripHtmlComments(source);
    source = removeTagBlocks(source, cfg.imageExcludeTags);
    const includeTags = parseTagList(cfg.imageIncludeTags);
    if (includeTags.length) {
        const includeLower = includeTags.map((tag) => tag.toLowerCase());
        let searchable = source;
        if (!includeLower.includes('text_to_image')) searchable = removeTagBlocks(searchable, 'text_to_image');
        source = extractTagBlocks(searchable, includeTags, true).join('\n\n');
    }
    source = removeTagBlocks(source, cfg.imageExcludeTags);
    return source.trim();
}
function applyImmersiveGalgameSystemBodyFormat(raw, rule) {
    const source = String(raw || '');
    const cfg = normalizeVirtualRegex(rule);
    const result = {
        raw: source,
        formattedRaw: source,
        formatSourceKind: cfg.enabled ? 'body-format' : 'raw',
        virtualRegexChanged: false,
        virtualRegexError: '',
    };

    if (!cfg.enabled || !cfg.pattern) {
        result.formatSourceKind = 'raw';
        return result;
    }

    try {
        const regex = new RegExp(cfg.pattern, cfg.flags);
        const bounded = breakAfterIgsDirectiveClose(source);
        result.formattedRaw = bounded.replace(regex, cfg.replacement);
        result.formattedRaw = result.formattedRaw.replace(THOUGHT_RE_GLOBAL, '*$2*');
        result.virtualRegexChanged = result.formattedRaw !== source;
        if (!result.virtualRegexChanged) result.formatSourceKind = 'raw';
    } catch (error) {
        result.formattedRaw = source;
        result.formatSourceKind = 'body-format-error';
        result.virtualRegexError = error instanceof Error ? error.message : String(error);
    }

    return result;
}

// [igs-scene/char/thought:…] 与旁白写在同一段时，正文替换只消费到 "]" 之前，
// "]" 之后残留的旁白会被替换结果粘在同一行，随后被当成台词一起渲染进对话框。
// 这里严格以 "]" 为分界，在指令闭合符后补一个换行，让旁白独立成段。
// 只对 igs 指令标签生效，不改动其他方括号文本；已有换行时不重复插入。
const IGS_DIRECTIVE_CLOSE_RE = /\[\s*igs-(?:scene|char|thought)\s*:[^\]\n]*\]([^\n]*)/gi;

function breakAfterIgsDirectiveClose(text) {
    return String(text || '').replace(
        IGS_DIRECTIVE_CLOSE_RE,
        (match, trailing) => {
            const normalized = String(trailing || '').replace(/^[ \t]+/, '');
            if (!normalized) return match;
            return `${match.slice(0, match.length - trailing.length)}\n${normalized}`;
        },
    );
}
function buildFormattedReaderSource(formattedText, imageSource) {
    const parts = [
        '<now_plot>',
        '<content data-igs-formatted="1">',
        String(formattedText || '').trim(),
        '</content>',
        '</now_plot>',
    ];
    const images = String(imageSource || '').trim();
    if (images) parts.push(images);
    return parts.join('\n');
}
function applySentencePaging(text, options = {}) {
    const source = String(text || '');
    if (!source.trim()) return source;
    const narrationOnly = Boolean(options.narrationOnly);
    return source
        .split('\n')
        .map((line) => splitSentenceLine(line, narrationOnly))
        .join('\n');
}

function splitSentenceLine(line, narrationOnly) {
    const trimmed = line.trim();
    if (!trimmed) return line;
    // 跳过非旁白行：场景素材模式下，对话([名字]：…)和心理活动(*…*)整行保留，
    // 只在旁白行按句号断页，否则会把对话/心理拆碎、丢失气泡样式和角色名归属。
    if (narrationOnly && !isNarrationLine(trimmed)) return line;
    // \x00IMG\x00 是图片占位，不能被句号切断；保护标签/心理/插图块内部的句号。
    if (/\x00IMG\x00/.test(line) || /image###/i.test(line) || /^\s*\[[^\]]+\]\s*$/.test(trimmed)) return line;
    return line.replace(
        new RegExp(`${SENTENCE_PAGING_TERMINATOR}(?!\\s*$)(?![\\s${SENTENCE_PAGING_TERMINATOR}」』”’）)\\]】])`, 'g'),
        `${SENTENCE_PAGING_TERMINATOR}\n`,
    );
}

function isNarrationLine(trimmed) {
    if (/^\*[\s\S]*\*$/.test(trimmed)) return false;
    if (/^\[[^\]]+\]\s*[:：]/.test(trimmed)) return false;
    if (/^[^:：\n]{1,24}\s*[:：]\s*\S/.test(trimmed) && !trimmed.startsWith('@')) return false;
    return true;
}
function buildFormattedTextPipeline(raw, sourceFilter, formatRule, options = {}) {
    const cfg = normalizeSourceFilter(sourceFilter);
    const visibleText = typeof options.visibleText === 'string' ? options.visibleText : '';
    const filtered = buildFilteredTextSource(raw, cfg, visibleText);
    const imageSource = buildBridgeImageSource(raw, cfg);
    const directiveResult = options.sceneAssetsEnabled
        ? extractSceneDirectives(filtered.textSource)
        : { directives: [], strippedText: filtered.textSource };
    const formatted = applyImmersiveGalgameSystemBodyFormat(filtered.textSource, formatRule);
    const formattedText = String(formatted.formattedRaw || '').trim();

    return {
        raw: String(raw || ''),
        tagText: filtered.textSource,
        textSource: filtered.textSource,
        formattedText,
        formattedRaw: buildFormattedReaderSource(formattedText, imageSource),
        imageSource,
        sourceKind: filtered.sourceKind,
        expectedTags: filtered.expectedTags || '',
        formatSourceKind: formatted.formatSourceKind,
        virtualRegexChanged: formatted.virtualRegexChanged,
        virtualRegexError: formatted.virtualRegexError,
        sceneDirectives: directiveResult.directives,
    };
}
function buildIgsTextPayload(message, options = {}) {
    const raw = getMessagePrimaryText(message);
    const sourceFilter = normalizeSourceFilter(options.sourceFilter);
    const virtualRegex = normalizeVirtualRegex(options.virtualRegex);
    const visibleText = resolveVisibleText(message, options.visibleText);
    const sceneAssetsEnabled = Boolean(options.sceneAssets && options.sceneAssets.enabled);
    const sentencePagingEnabled = Boolean(options.sentencePaging);
    const strictPayload = buildFormattedTextPipeline(raw, sourceFilter, virtualRegex, { visibleText, sceneAssetsEnabled });
    const cleanedRaw = normalizeWhitespace(cleanNarrativeSource(raw));
    const warnings = [];
    const errors = [];

    let formattedText = strictPayload.formattedText;
    let sourceKind = strictPayload.sourceKind;
    let formatSourceKind = strictPayload.formatSourceKind;
    let sceneDirectives = strictPayload.sceneDirectives || [];
    let sceneDirectiveSource = strictPayload.tagText || raw;
    let usedFallback = false;
    let usedDomOverride = false;

    if (looksLikeHostUiHtml(raw)) {
        warnings.push({ code: 'host-ui-html-raw', message: 'Raw message looks like host UI HTML.' });
    }

    if (!formattedText || looksLikeHostUiHtml(formattedText)) {
        formattedText = firstNonEmpty(
            !looksLikeHostUiHtml(strictPayload.formattedText) ? strictPayload.formattedText : '',
            visibleText,
            cleanedRaw,
            String(raw || '').trim(),
        );
        sourceKind = formattedText ? 'forced-fallback' : (sourceKind || 'empty-forced');
        formatSourceKind = strictPayload.formattedText ? strictPayload.formatSourceKind : 'forced-fallback';
        usedFallback = formattedText !== strictPayload.formattedText;
    }

    if (looksLikeHostUiHtml(formattedText)) {
        errors.push({ code: 'host-ui-html-leaked', message: 'Extracted text still looks like host UI HTML.' });
        formattedText = firstNonEmpty(
            visibleText,
            cleanedRaw && !looksLikeHostUiHtml(cleanedRaw) ? cleanedRaw : '',
            '',
        );
    }

    formattedText = normalizeWhitespace(formattedText);

    // DOM 差异优先：第三方关键词过滤插件（如 Veridis）会改渲染层 .mes_text，
    // 且在生成结束后写回 chat[n].mes，但回写可能延迟或在历史消息上未触发。
    // 当 DOM 可见文本与数据层纯文本只是局部（词级）不同时，信任 DOM。
    // 指令标签单独处理：宿主前端会把 [igs-scene/char/thought:] 从 .mes_text 清洗掉，
    // 所以"DOM 无标签、数据层有标签"是宿主正常行为，不是插件改词。
    // 文本覆盖和指令来源解耦：
    //   - DOM 文本覆盖照常进行（让 Veridis 替换词进阅读器）
    //   - 场景指令始终从数据层原文提取（DOM 里没有标签，不能从 DOM 重提）
    // 长度比对用 domCompareBase：domClobbersDirectiveTags 时去掉指令行，
    // 避免标签内容撑大数据层长度后被 localizedTextDiffers 误判为"不同内容"。
    const domVisibleText = normalizeWhitespace(visibleText);
    const domClobbersDirectiveTags = hasIgsDirectiveTags(raw) && !hasIgsDirectiveTags(domVisibleText);
    const domCompareBase = domClobbersDirectiveTags
        ? normalizeWhitespace(buildDomCompareBase(formattedText))
        : cleanedRaw;
    if (domVisibleText
        && !looksLikeHostUiHtml(domVisibleText)
        && localizedTextDiffers(domCompareBase, domVisibleText)) {
        // DOM 文本可能仍含 [igs-char/thought:] 原始标签（宿主没清洗）。必须先跑正文格式化，
        // 把标签转成气泡/心理话形态（[名]：… 与 *…*），否则阅读器把整段当旁白、丢失角色名。
        const domFormatted = applyImmersiveGalgameSystemBodyFormat(domVisibleText, virtualRegex);
        formattedText = normalizeWhitespace(domFormatted.formattedRaw || domVisibleText);
        sourceKind = 'dom-visible-override';
        formatSourceKind = 'dom-visible-override';
        usedDomOverride = true;
        // domClobbersDirectiveTags：宿主把指令标签从 DOM 清洗掉了，DOM 里没有标签，
        // 不能从 DOM 重提指令——保留数据层已提取的 sceneDirectives。
        if (!domClobbersDirectiveTags) {
            if (sceneAssetsEnabled) {
                const extractedDirectives = extractSceneDirectives(domVisibleText);
                sceneDirectives = extractedDirectives.directives;
                sceneDirectiveSource = domVisibleText;
            }
        }
    }

    if (sceneAssetsEnabled && !sceneDirectives.length) {
        sceneDirectiveSource = firstNonEmpty(
            formattedText,
            visibleText,
            cleanedRaw,
            String(raw || '').trim(),
        );
        sceneDirectives = extractSceneDirectives(sceneDirectiveSource).directives;
    }
    const readerScene = parseSceneText(formattedText, {});
    const readerText = normalizeReaderSegmentText(firstNonEmpty(
        readerScene.text,
        formattedText,
        visibleText,
        cleanedRaw,
        String(raw || '').trim(),
    ), readerScene.speaker);
    const pagedReaderText = sentencePagingEnabled
        ? applySentencePaging(readerText, { narrationOnly: sceneAssetsEnabled })
        : readerText;
    if (sceneAssetsEnabled && sceneDirectives.length) {
        sceneDirectives = remapSceneDirectiveSegments(sceneDirectives, sceneDirectiveSource, virtualRegex, {
            sentencePaging: sentencePagingEnabled,
            narrationOnly: true,
        });
    }
    const textSegments = buildNarrativeSegments(pagedReaderText);
    const imageSlots = parseImageSlots(raw, strictPayload.imageSource, sourceFilter);
    const segmentImageSlots = pagedReaderText
        ? buildSegmentImageMap(raw, textSegments, imageSlots, { sceneAssetsMode: sceneAssetsEnabled })
        : [];

    if (usedFallback) {
        warnings.push({ code: 'forced-fallback', message: 'Fell back to visible text or cleaned raw source.' });
    }
    if (usedDomOverride) {
        warnings.push({ code: 'dom-visible-override', message: 'Used rendered DOM text over stale message data.' });
    }
    if (!formattedText) {
        errors.push({ code: 'empty-igs-text', message: 'No readable Immersive Galgame System text could be extracted.' });
    }

    return {
        raw,
        cleanedRaw,
        visibleText,
        tagText: strictPayload.tagText,
        textSource: formattedText,
        formattedText,
        formattedRaw: buildFormattedReaderSource(formattedText, strictPayload.imageSource),
        imageSource: strictPayload.imageSource,
        imageSlots,
        textSegments,
        segmentImageSlots,
        sceneDirectives,
        sourceKind,
        expectedTags: strictPayload.expectedTags,
        formatSourceKind,
        virtualRegexChanged: strictPayload.virtualRegexChanged,
        virtualRegexError: strictPayload.virtualRegexError,
        usedFallback,
        usedDomOverride,
        warnings,
        errors,
    };
}

function resolveVisibleText(message, fallbackVisibleText) {
    const explicit = normalizeWhitespace(fallbackVisibleText);
    if (explicit) return explicit;
    if (!message || typeof message !== 'object') return '';

    const fromMessage = normalizeWhitespace(message.visibleText);
    if (fromMessage) return fromMessage;

    const element = message.element || message.mesElement || message.domElement || message.node || null;
    return getVisibleMessageTextFromElement(element);
}

function buildFilteredTextSource(raw, filter, fallbackText) {
    const cfg = normalizeSourceFilter(filter);
    const source = String(raw || '');
    const includeTags = parseTagList(cfg.textIncludeTags);
    const hasIncludedTags = cfg.enabled && includeTags.length && hasTagBlocks(source, includeTags);

    if (cfg.enabled && includeTags.length && !hasIncludedTags) {
        return {
            textSource: '',
            sourceKind: 'tag-not-found',
            expectedTags: includeTags.join(', '),
        };
    }

    const taggedText = buildTagFilteredTextSource(source, cfg);
    let textSource = taggedText;
    let sourceKind = textSource ? (cfg.enabled ? 'tagged-content' : 'raw') : '';

    if (!textSource && hasIncludedTags) {
        return {
            textSource: '',
            sourceKind: 'tagged-empty',
            expectedTags: includeTags.join(', '),
        };
    }

    if (!textSource && (!cfg.enabled || !includeTags.length) && /<now_plot\b/i.test(source)) {
        const nowPlotMatch = source.match(/<now_plot\b[^>]*>([\s\S]*?)<\/now_plot>/i);
        if (nowPlotMatch && nowPlotMatch[1]) {
            textSource = removeTagBlocks(
                cfg.stripHtmlComments ? stripHtmlComments(nowPlotMatch[1]) : nowPlotMatch[1],
                cfg.textExcludeTags,
            ).trim();
            sourceKind = textSource ? 'tagged-now-plot' : '';
        }
    }

    if (!textSource && cfg.allowUntaggedFallback && (!cfg.enabled || !includeTags.length)) {
        textSource = String(fallbackText || '').trim() || cleanNarrativeSource(source).trim();
        sourceKind = textSource ? 'untagged-clean-text' : '';
    }

    return {
        textSource: String(textSource || '').trim(),
        sourceKind: sourceKind || 'empty',
        expectedTags: includeTags.join(', '),
    };
}

function buildTagFilteredTextSource(raw, filter) {
    const cfg = normalizeSourceFilter(filter);
    let source = String(raw || '');
    if (!cfg.enabled) return source;
    if (cfg.stripHtmlComments) source = stripHtmlComments(source);
    const includeTags = parseTagList(cfg.textIncludeTags);
    if (includeTags.length) {
        source = extractTagBlocks(source, includeTags).join('\n\n');
        if (!source) return '';
    }
    source = removeTagBlocks(source, cfg.textExcludeTags);
    return source.trim();
}

function parseTagList(text) {
    return String(text || '')
        .split(/[\n,，]+/)
        .map((tag) => tag.trim().replace(/^<+/, '').replace(/^\/+/, '').replace(/>+$/, '').replace(/\/+$/, '').trim())
        .filter(Boolean);
}

function normalizeTagText(value) {
    if (Array.isArray(value)) {
        return value.map((item) => String(item || '').trim()).filter(Boolean).join('\n');
    }
    if (value == null) return '';
    return String(value);
}

function stripHtmlComments(raw) {
    return String(raw || '').replace(/<!--[\s\S]*?-->/g, '');
}

function extractTagBlocks(raw, tags, keepWrapper = false) {
    const source = String(raw || '');
    const parts = [];
    for (const tag of parseTagList(tags)) {
        const regex = new RegExp(`(^|[\\s>])(<${escapeRegExp(tag)}\\b[^>]*>([\\s\\S]*?)<\\/${escapeRegExp(tag)}>)`, 'gi');
        let match = null;
        while ((match = regex.exec(source)) !== null) {
            parts.push(keepWrapper ? (match[2] || '') : (match[3] || ''));
        }
    }
    return parts;
}

function removeTagBlocks(raw, tags) {
    let output = String(raw || '');
    for (const tag of parseTagList(tags)) {
        const regex = new RegExp(`(^|[\\s>])<${escapeRegExp(tag)}\\b[^>]*>[\\s\\S]*?<\\/${escapeRegExp(tag)}>`, 'gi');
        output = output.replace(regex, (match, prefix) => prefix || '');
    }
    return output;
}

function hasTagBlocks(raw, tags) {
    const source = String(raw || '');
    return parseTagList(tags).some((tag) => {
        const regex = new RegExp(`(^|[\\s>])<${escapeRegExp(tag)}\\b[^>]*>[\\s\\S]*?<\\/${escapeRegExp(tag)}>`, 'i');
        return regex.test(source);
    });
}

function normalizeWhitespace(text) {
    return String(text || '')
        .replace(/\u00a0/g, ' ')
        .replace(/\r\n?/g, '\n')
        .replace(/[ \t]+\n/g, '\n')
        .replace(/\n{3,}/g, '\n\n')
        .trim();
}

function firstNonEmpty(...values) {
    for (const value of values) {
        const text = normalizeWhitespace(value);
        if (text) return text;
    }
    return '';
}

function compactForCompare(text) {
    return String(text || '')
        .replace(/\x00IMG\x00/g, '')
        .replace(/\s+/g, '')
        .trim();
}

// 判断 DOM 可见文本相对数据层纯文本是否只是"词级"差异（关键词过滤插件改词）。
// 返回 true 才允许用 DOM 覆盖。两者长度量级接近且高度相似才认定为同一段被改词的正文，
// 避免把"不同消息 / 被截断的 DOM / 宿主 UI 噪声"误判成改后正文。
function localizedTextDiffers(dataText, domText) {
    const dataCompact = compactForCompare(dataText);
    const domCompact = compactForCompare(domText);
    if (!domCompact || domCompact === dataCompact) return false;
    if (!dataCompact) return false;

    const longer = Math.max(dataCompact.length, domCompact.length);
    const shorter = Math.min(dataCompact.length, domCompact.length);
    // 长度差超过 40% 视为结构性差异（截断/不同消息），不覆盖。
    if (shorter / longer < 0.6) return false;

    const distance = boundedLevenshtein(dataCompact, domCompact, Math.ceil(longer * 0.5));
    if (distance < 0) return false;
    // 改动字符占比超过 50% 视为内容不同，不覆盖。
    return distance / longer <= 0.5;
}

function boundedLevenshtein(a, b, maxDistance) {
    const lenA = a.length;
    const lenB = b.length;
    if (Math.abs(lenA - lenB) > maxDistance) return -1;
    let prev = new Array(lenB + 1);
    let curr = new Array(lenB + 1);
    for (let j = 0; j <= lenB; j++) prev[j] = j;
    for (let i = 1; i <= lenA; i++) {
        curr[0] = i;
        let rowMin = curr[0];
        const charA = a.charCodeAt(i - 1);
        for (let j = 1; j <= lenB; j++) {
            const cost = charA === b.charCodeAt(j - 1) ? 0 : 1;
            curr[j] = Math.min(prev[j] + 1, curr[j - 1] + 1, prev[j - 1] + cost);
            if (curr[j] < rowMin) rowMin = curr[j];
        }
        if (rowMin > maxDistance) return -1;
        const swap = prev;
        prev = curr;
        curr = swap;
    }
    return prev[lenB];
}

function normalizeReaderSegmentText(text, speaker = '') {
    let normalized = normalizeWhitespace(String(text || '').replace(/\x00IMG\x00/g, ' '));
    const speakerName = normalizeWhitespace(speaker);
    if (!speakerName || !normalized) return normalized;
    const escaped = escapeRegExp(speakerName);
    normalized = normalized.replace(new RegExp(`^${escaped}\\s*[:：]\\s*`), '');
    return normalizeWhitespace(normalized);
}

function remapSceneDirectiveSegments(directives, source, virtualRegex, options = {}) {
    const sourceLines = String(source || '').split('\n');
    const markers = new Map();
    const markerLines = new Map();
    directives.forEach((directive, index) => {
        const lineIndex = Number(directive && directive.lineIndex);
        if (!Number.isFinite(lineIndex) || lineIndex < 0 || lineIndex >= sourceLines.length) return;
        const marker = `\uE000IGS${index}\uE001`;
        markers.set(marker, index);
        if (!markerLines.has(lineIndex)) markerLines.set(lineIndex, []);
        markerLines.get(lineIndex).push(marker);
    });
    if (!markers.size) return directives.map((directive) => ({ ...directive }));

    const markedSource = sourceLines.flatMap((line, index) => [
        ...(markerLines.get(index) || []),
        line,
    ]).join('\n');
    const formatted = applyImmersiveGalgameSystemBodyFormat(markedSource, virtualRegex);
    const readerScene = parseSceneText(formatted.formattedRaw || markedSource, {});
    const readerText = normalizeReaderSegmentText(firstNonEmpty(
        readerScene.text,
        formatted.formattedRaw,
        markedSource,
    ), readerScene.speaker);
    const pagedText = options.sentencePaging
        ? applySentencePaging(readerText, { narrationOnly: options.narrationOnly === true })
        : readerText;
    const segments = buildNarrativeSegments(pagedText);
    const mapped = [];
    let visibleIndex = 0;
    for (const segment of segments) {
        const markerIndex = markers.get(String(segment || '').trim());
        if (markerIndex != null) {
            mapped[markerIndex] = visibleIndex;
        } else {
            visibleIndex += 1;
        }
    }
    return directives.map((directive, index) => ({
        ...directive,
        segmentIndex: mapped[index] == null ? directive.segmentIndex : mapped[index],
    }));
}

function escapeRegExp(value) {
    return String(value || '').replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function isPlainObject(value) {
    return Boolean(value) && typeof value === 'object' && !Array.isArray(value);
}

function stripIgsDirectiveLines(text) {
    return String(text || '').split('\n')
        .filter(line => !IGS_DIRECTIVE_LINE_RE.test(line.trim()))
        .join('\n');
}

// domClobbersDirectiveTags 时的对比基准：从已格式化文本去掉 [igs-scene:] 行，
// 并把 *心理话* 和 [名字]：对白 还原为纯内容，使其与 DOM（宿主隐藏标签后的纯文本）
// 结构对齐——只剩词级差异，让 localizedTextDiffers 正确判断 Veridis 是否改过词。
function buildDomCompareBase(formattedText) {
    return String(formattedText || '').split('\n')
        .filter(line => !IGS_DIRECTIVE_LINE_RE.test(line.trim()))
        .map(line => {
            const s = line.trim();
            const thoughtMatch = s.match(/^\*([\s\S]*)\*$/);
            if (thoughtMatch) return thoughtMatch[1];
            const dialogueMatch = s.match(/^\[[^\]]+\]\s*[:：]\s*([\s\S]*)$/);
            if (dialogueMatch) return dialogueMatch[1];
            return line;
        })
        .join('\n');
}

__igsDefine(exports, "getMessagePrimaryText", () => getMessagePrimaryText);
__igsDefine(exports, "getVisibleMessageTextFromElement", () => getVisibleMessageTextFromElement);
__igsDefine(exports, "looksLikeHostUiHtml", () => looksLikeHostUiHtml);
__igsDefine(exports, "cleanNarrativeSource", () => cleanNarrativeSource);
__igsDefine(exports, "normalizeSourceFilter", () => normalizeSourceFilter);
__igsDefine(exports, "normalizeVirtualRegex", () => normalizeVirtualRegex);
__igsDefine(exports, "buildBridgeImageSource", () => buildBridgeImageSource);
__igsDefine(exports, "applyImmersiveGalgameSystemBodyFormat", () => applyImmersiveGalgameSystemBodyFormat);
__igsDefine(exports, "buildFormattedReaderSource", () => buildFormattedReaderSource);
__igsDefine(exports, "applySentencePaging", () => applySentencePaging);
__igsDefine(exports, "buildFormattedTextPipeline", () => buildFormattedTextPipeline);
__igsDefine(exports, "buildIgsTextPayload", () => buildIgsTextPayload);
__igsDefine(exports, "DEFAULT_SOURCE_FILTER", () => DEFAULT_SOURCE_FILTER);
__igsDefine(exports, "DEFAULT_VIRTUAL_REGEX", () => DEFAULT_VIRTUAL_REGEX);
});
__igsRegister("src/scene/image-slots.js", function(module, exports, require) {
function buildNarrativeSegments(text) {
    const source = String(text || '').trim();
    if (!source) return [''];
    const segments = source
        .split(/\n+/)
        .map((item) => item.replace(/\s+/g, ' ').trim())
        .filter(Boolean);
    return segments.length ? segments : [''];
}
function parseImageSlots(raw, imageSource, sourceFilter = {}) {
    if (!isImageSlotBindingEnabled(sourceFilter)) return [];
    const source = String(imageSource || raw || '').trim();
    if (!source) return [];
    return extractImageBlocks(source).map((block, index) => {
        const title = normalizeImageSlotTitle(block, index);
        const promptText = extractPromptText(block);
        return {
            slotIndex: index,
            title,
            tagName: block.tagName,
            rawBlock: block.rawBlock,
            promptText,
            locationHash: buildImageSlotHash(promptText || block.rawBlock),
            source: block.source,
        };
    });
}
function extractImageBlocks(source) {
    const sourceText = String(source || '');
    const output = [];
    const seen = new Set();

    function add(rawBlock, tagName, sourceKind) {
        const normalized = String(rawBlock || '').trim();
        if (!normalized || seen.has(normalized)) return;
        seen.add(normalized);
        output.push({
            rawBlock: normalized,
            tagName: tagName || 'image',
            source: sourceKind || 'image-tag',
        });
    }

    const imageRegex = /<image\b[^>]*>[\s\S]*?<\/image>/gi;
    let match = null;
    while ((match = imageRegex.exec(sourceText)) !== null) {
        add(match[0], 'image', 'image-tag');
    }

    const imageRemainder = sourceText.replace(imageRegex, '\n');
    const promptRegex = /image###[\s\S]*?###/gi;
    while ((match = promptRegex.exec(imageRemainder)) !== null) {
        add(match[0], 'image-prompt', 'image-prompt');
    }

    return output;
}
function buildSegmentImageMap(raw, segmentTexts = [], imageSlots = [], options = {}) {
    const segments = Array.isArray(segmentTexts) ? segmentTexts : [];
    const totalImageCount = Array.isArray(imageSlots) ? imageSlots.length : 0;
    if (!segments.length || totalImageCount <= 0) return [];

    if (options.sceneAssetsMode) {
        return buildSceneAssetsMapping(raw, segments, totalImageCount);
    }

    const sentenceMappings = buildSentenceImageMappings(raw, totalImageCount);
    if (!sentenceMappings.length) {
        return spreadImageSlots(segments.length, totalImageCount);
    }

    if (sentenceMappings.length === segments.length) {
        return sentenceMappings.map((item) => clampImageIndex(item.imgIdx, totalImageCount));
    }

    return segments.map((_, index) => {
        const mappedIndex = Math.min(
            sentenceMappings.length - 1,
            Math.floor(index * sentenceMappings.length / Math.max(segments.length, 1)),
        );
        return clampImageIndex(sentenceMappings[mappedIndex].imgIdx, totalImageCount);
    });
}
function normalizeImageSlotTitle(block, index = 0) {
    const source = String(block && block.rawBlock || block || '').trim();
    const lines = source
        .replace(/<image\b[^>]*>/i, '')
        .replace(/<\/image>/i, '')
        .split(/\n+/)
        .map((line) => line.trim())
        .filter(Boolean);
    for (const line of lines) {
        const titleMatch = line.match(/^\[([^\]\r\n]+)\]$/);
        if (titleMatch && titleMatch[1]) return titleMatch[1].trim();
    }
    return `图 ${index + 1}`;
}
function buildImageSlotHash(text) {
    const source = normalizeSlotHashSource(text);
    let hash = 2166136261;
    for (let index = 0; index < source.length; index += 1) {
        hash ^= source.charCodeAt(index);
        hash = Math.imul(hash, 16777619);
    }
    return `slot-${(hash >>> 0).toString(16).padStart(8, '0')}`;
}

function isImageSlotBindingEnabled(sourceFilter = {}) {
    if (sourceFilter && sourceFilter.enabled === false) return false;
    return parseTagList(sourceFilter && sourceFilter.imageIncludeTags).length > 0;
}

function extractPromptText(block) {
    const source = String(block && block.rawBlock || '').trim();
    const promptMatch = source.match(/image###[\s\S]*?###/i);
    return promptMatch ? promptMatch[0].trim() : '';
}

function buildSentenceImageMappings(raw, totalImageCount) {
    const source = String(raw || '');
    if (!source.trim()) return [];

    let cleaned = source.replace(/<\/content>\s*<content[^>]*>/gi, '\n\n');
    cleaned = cleaned.replace(/<image\b[^>]*>[\s\S]*?<\/image>/gi, ' \x00IMG\x00 ');
    cleaned = cleaned.replace(/<imgthink[^>]*>[\s\S]*?<\/imgthink>/gi, '');
    cleaned = cleaned.replace(/image###[\s\S]*?###/gi, '');
    cleaned = cleaned.replace(/<!--[\s\S]*?-->/g, '');
    cleaned = cleaned.replace(/<style[\s\S]*?<\/style>/gi, '');
    cleaned = cleaned.replace(/<script[^>]*>[\s\S]*?<\/script>/gi, '');
    cleaned = cleaned.replace(/<iframe[^>]*>[\s\S]*?<\/iframe>/gi, '');
    cleaned = cleaned.replace(/【[^】\n]{0,100}】/g, '');
    cleaned = cleaned.replace(/<[^>]+>/g, '');
    cleaned = cleaned.replace(/\r/g, '');

    const sentences = [];
    let imageCount = 0;
    const lines = cleaned.split(/\n+/);
    for (const line of lines) {
        const parts = line.split('\x00IMG\x00');
        for (let index = 0; index < parts.length; index += 1) {
            const text = parts[index].replace(/\s+/g, ' ').trim();
            if (text) sentences.push({ text, imgIdx: imageCount });
            if (index < parts.length - 1) imageCount += 1;
        }
    }

    if (!sentences.length) return [];
    if (!imageCount && totalImageCount > 0) {
        return sentences.map((sentence, index) => ({
            ...sentence,
            imgIdx: Math.min(totalImageCount - 1, Math.floor(index * totalImageCount / sentences.length)),
        }));
    }
    return sentences.map((sentence) => ({
        ...sentence,
        imgIdx: clampImageIndex(sentence.imgIdx, totalImageCount),
    }));
}

function buildSceneAssetsMapping(raw, segments, totalImageCount) {
    const source = String(raw || '');
    if (!source.trim() || !segments.length || totalImageCount <= 0) {
        return new Array(segments.length).fill(null);
    }

    let cleaned = source.replace(/<\/content>\s*<content[^>]*>/gi, '\n\n');
    cleaned = cleaned.replace(/<image\b[^>]*>[\s\S]*?<\/image>/gi, ' \x00IMG\x00 ');
    cleaned = cleaned.replace(/<imgthink[^>]*>[\s\S]*?<\/imgthink>/gi, '');
    cleaned = cleaned.replace(/image###[\s\S]*?###/gi, '');
    cleaned = cleaned.replace(/<!--[\s\S]*?-->/g, '');
    cleaned = cleaned.replace(/<style[\s\S]*?<\/style>/gi, '');
    cleaned = cleaned.replace(/<script[^>]*>[\s\S]*?<\/script>/gi, '');
    cleaned = cleaned.replace(/<iframe[^>]*>[\s\S]*?<\/iframe>/gi, '');
    cleaned = cleaned.replace(/【[^】\n]{0,100}】/g, '');
    cleaned = cleaned.replace(/<[^>]+>/g, '');
    cleaned = cleaned.replace(/\r/g, '');

    const sentences = [];
    let imageCount = 0;
    const lines = cleaned.split(/\n+/);
    const imageAfterSentence = new Set();
    for (const line of lines) {
        const parts = line.split('\x00IMG\x00');
        for (let index = 0; index < parts.length; index += 1) {
            const text = parts[index].replace(/\s+/g, ' ').trim();
            if (text) sentences.push({ text, imgIdx: imageCount });
            if (index < parts.length - 1) {
                if (sentences.length > 0) {
                    imageAfterSentence.add(sentences.length - 1);
                }
                imageCount += 1;
            }
        }
    }

    if (!sentences.length || !imageCount) {
        return new Array(segments.length).fill(null);
    }

    const result = new Array(segments.length).fill(null);
    for (const sentenceIdx of imageAfterSentence) {
        const segIdx = Math.min(
            segments.length - 1,
            sentences.length === segments.length
                ? sentenceIdx
                : Math.floor(sentenceIdx * segments.length / Math.max(sentences.length, 1)),
        );
        const targetIdx = Math.max(0, segIdx);
        if (result[targetIdx] === null) {
            result[targetIdx] = clampImageIndex(sentences[sentenceIdx].imgIdx, totalImageCount);
        }
    }
    return result;
}

function spreadImageSlots(segmentCount, totalImageCount) {
    if (segmentCount <= 0 || totalImageCount <= 0) return [];
    return Array.from({ length: segmentCount }, (_, index) => {
        return Math.min(totalImageCount - 1, Math.floor(index * totalImageCount / segmentCount));
    });
}

function clampImageIndex(value, totalImageCount) {
    if (totalImageCount <= 0) return 0;
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric < 0) return 0;
    return Math.max(0, Math.min(totalImageCount - 1, Math.floor(numeric)));
}

function normalizeSlotHashSource(text) {
    return String(text || '')
        .replace(/\r/g, '')
        .replace(/\n+/g, '\n')
        .replace(/[ \t]+/g, ' ')
        .trim();
}

function parseTagList(text) {
    return String(text || '')
        .split(/[\n,，]+/)
        .map((tag) => tag.trim().replace(/^<+/, '').replace(/^\/+/, '').replace(/>+$/, '').replace(/\/+$/, '').trim())
        .filter(Boolean);
}

__igsDefine(exports, "buildNarrativeSegments", () => buildNarrativeSegments);
__igsDefine(exports, "parseImageSlots", () => parseImageSlots);
__igsDefine(exports, "extractImageBlocks", () => extractImageBlocks);
__igsDefine(exports, "buildSegmentImageMap", () => buildSegmentImageMap);
__igsDefine(exports, "normalizeImageSlotTitle", () => normalizeImageSlotTitle);
__igsDefine(exports, "buildImageSlotHash", () => buildImageSlotHash);
});
__igsRegister("src/scene/scene-directives.js", function(module, exports, require) {
const { resolveMoodGroup } = require("src/scene/mood-groups.js");
// 指令可独占整行，也可紧跟在正文之后（同一行内混排），因此不做行首锚定。
const SCENE_RE = /\[igs-scene:([^|\]]+)\|([^|\]]+)\|([^|\]]+)(?:\|([^\]]*))?\]/;
const CHAR_RE = /\[igs-char:([^|\]]+)\|([^|\]]+)\|([^|\]]+)\]/;
const THOUGHT_RE = /\[igs-thought:([^|\]]+)\|([^|\]]+)\|([^|\]]+)\]/;
// 锚定在当前位置的版本：用于「紧贴当前位置」的指令识别。
const SCENE_AT_RE = /^\[igs-scene:([^|\]]+)\|([^|\]]+)\|([^|\]]+)(?:\|([^\]]*))?\]/;
const CHAR_AT_RE = /^\[igs-char:([^|\]]+)\|([^|\]]+)\|([^|\]]+)\]/;
const THOUGHT_AT_RE = /^\[igs-thought:([^|\]]+)\|([^|\]]+)\|([^|\]]+)\]/;
// 找出当前位置之后最近一条 igs 指令的起始下标；没有则返回 -1。
function nextDirectiveIndex(text) {
    const m = String(text || '').match(/\[igs-(?:scene|char|thought):/);
    return m ? m.index : -1;
}
function extractSceneDirectives(text) {
    const source = String(text || '');
    if (!source.trim()) return { directives: [], strippedText: source };

    const directives = [];
    const lines = source.split('\n');
    let segmentCount = 0;
    // 待结算的正文本：只有遇到 [igs-scene] 或行尾时才确定它属于哪一段。
    let pending = '';
    // 行首偏移：用于按「字符位置」定位指令，弥补按行计数的段索引与文本管线分段之间的错位。
    let lineOffset = 0;

    for (let i = 0; i < lines.length; i++) {
        const rawLine = lines[i];
        const trimmed = rawLine.trim();
        const indent = rawLine.length - rawLine.trimStart().length;
        if (!trimmed) continue;
        // 场景切换的唯一依据是 [igs-scene] 标签本身：标签出现在哪里，它之后的正文就属于哪个场景。
        // 一行内可混排任意指令与正文（位置不限），因此按出现顺序消费：
        // 先结算「标签之前的正文」为当前段，标签本身归属「其后的正文段」。
        let rest = trimmed;
        let m;
        let cursor = lineOffset + indent;
        while (rest) {
            // 只认「紧贴当前位置」的指令：否则 SCENE_RE 会越过行首的 thought/char
            // 标签去匹配后面的 scene，把前面的指令当成正文吞掉。
            if ((m = rest.match(SCENE_AT_RE))) {
                // 标签之前的正文自成一段（若有）。
                if (pending.trim()) { segmentCount += 1; pending = ''; }
                directives.push({
                    type: 'scene',
                    scene: m[1].trim(),
                    time: m[2].trim(),
                    weather: m[3].trim(),
                    nsfw: String(m[4] || '').trim().toLowerCase() === 'nsfw',
                    segmentIndex: segmentCount,
                    lineIndex: i,
                    offset: cursor,
                });
            } else if ((m = rest.match(CHAR_AT_RE))) {
                if (pending.trim()) { segmentCount += 1; pending = ''; }
                directives.push({ type: 'char', character: m[1].trim(), mood: m[2].trim(), dialogue: m[3].trim(), segmentIndex: segmentCount, lineIndex: i, offset: cursor });
            } else if ((m = rest.match(THOUGHT_AT_RE))) {
                if (pending.trim()) { segmentCount += 1; pending = ''; }
                directives.push({ type: 'thought', character: m[1].trim(), mood: m[2].trim(), thought: m[3].trim(),segmentIndex: segmentCount, lineIndex: i, offset: cursor });
            } else {
                // 当前位置不是指令：把到「下一条指令之前」的文本计入正文。
                const nextAt = nextDirectiveIndex(rest);
                if (nextAt < 0) { pending += rest; rest = ''; break; }
                pending += rest.slice(0, nextAt);
                rest = rest.slice(nextAt);
                continue;
            }
            rest = rest.slice(m[0].length);
            cursor += m[0].length;
        }
        pending += rest;
        lineOffset += rawLine.length + 1;
    }

    return { directives, strippedText: source };
}
// 按「字符位置」找最近一条 char/thought 指令：用于恢复说话人，
// 不依赖按行计数的 segmentIndex，因此不受文本管线重排版影响。
function resolveNearestCharacterBefore(directives, offset) {
    const out = { character: '', mood: '', lastDirectiveType: '' };
    if (!Array.isArray(directives) || !directives.length) return out;
    const limit = Number(offset);
    let best = null;
    for (const d of directives) {
        if (d.type !== 'char' && d.type !== 'thought') continue;
        if (Number.isFinite(limit) && Number(d.offset) > limit) continue;
        if (!best || Number(d.offset) >= Number(best.offset)) best = d;
    }
    if (best) {
        out.character = best.character || '';
        out.mood = best.mood || '';
        out.lastDirectiveType = best.type;
    }
    return out;
}



// 取「指定字符位置前方最近的 [igs-scene] 标签」：直接在原文里 lastIndexOf 查找，
// 不做任何偏移累加，因此不受排版、行内混排、段数错位影响。
function resolveSceneAtSourceOffset(source, position) {
    const empty = { scene: '', time: '', weather: '', nsfw: false, character: '', mood: '', dialogue: '', thought: '', lastDirectiveType: '' };
    const src = String(source || '');
    const limit = Math.max(0, Math.min(src.length, Number(position) || 0));
    const at = src.slice(0, limit).lastIndexOf('[igs-scene:');
    if (at < 0) return empty;
    const m = src.slice(at).match(SCENE_AT_RE);
    if (!m) return empty;
    return {
        scene: m[1].trim(),
        time: m[2].trim(),
        weather: m[3].trim(),
        nsfw: String(m[4] || '').trim().toLowerCase() === 'nsfw',
        character: '', mood: '', dialogue: '', thought: '', lastDirectiveType: 'scene',
    };
}
function resolveSceneStateAtIndex(directives, segmentIndex) {
    const state = { scene: '', time: '', weather: '', nsfw: false, character: '', mood: '', dialogue: '', thought: '', lastDirectiveType: '' };
    if (!Array.isArray(directives) || !directives.length) return state;
    const targetIndex = normalizeSegmentIndex(segmentIndex);

    for (const directive of directives) {
        const directiveIndex = normalizeSegmentIndex(directive && directive.segmentIndex);
        if (directiveIndex != null && targetIndex != null && directiveIndex > targetIndex) break;
        if (directive.type === 'scene') {
            if (directive.scene) state.scene = directive.scene;
            if (directive.time) state.time = directive.time;
            if (directive.weather) state.weather = directive.weather;
            state.nsfw = directive.nsfw === true;
            state.lastDirectiveType = 'scene';
        } else if (directive.type === 'char') {
            if (directive.character) state.character = directive.character;
            if (directive.mood) state.mood = directive.mood;
            if (directive.dialogue) state.dialogue = directive.dialogue;
            state.lastDirectiveType = 'char';
        } else if (directive.type === 'thought') {
            if (directive.character) state.character = directive.character;
            if (directive.mood) state.mood = directive.mood;
            if (directive.thought) state.thought = directive.thought;
            state.lastDirectiveType = 'thought';
        }
    }

    return state;
}
function lookupSceneAssetUrls(sceneState, sceneAssets) {
    if (!sceneAssets || !sceneState) return { backgroundUrl: null, spriteUrl: null, spriteSlot: '', spriteCharacter: '' };

    const scenes = sceneAssets.scenes || {};
    const backgroundUrl = lookupSceneUrl(scenes, sceneState.scene, sceneState.time, sceneState.weather, sceneAssets);

    let spriteUrl = null;
    let spriteSlot = '';
    const characters = sceneAssets.characters || {};
    const spriteCharacter = resolveCharacterKey(characters, sceneAssets.characterAliases, sceneState.character);
    if (spriteCharacter) {
        const hit = lookupAssetValue(characters[spriteCharacter], sceneState.mood, sceneAssets.moodGroups);
        spriteUrl = hit.url;
        spriteSlot = hit.slot;
    }

    return { backgroundUrl, spriteUrl, spriteSlot, spriteCharacter: spriteCharacter || '' };
}
function resolveCharacterKey(characters, characterAliases, characterName) {
    const target = String(characterName || '').trim();
    if (!target || !characters || typeof characters !== 'object') return null;
    if (characters[target] != null) return target;
    const aliases = characterAliases && typeof characterAliases === 'object' ? characterAliases : {};
    for (const key of Object.keys(characters)) {
        const values = Array.isArray(aliases[key]) ? aliases[key] : [];
        if (values.some((value) => String(value || '').trim() === target)) return key;
    }
    return null;
}

// 场景别名沿用 scenes[名].words 存储；先精确命中主名称，再按别名归约。
// 角色别名则由 characterAliases 映射到原角色，避免复制整套立绘槽。
function resolveSceneKey(scenes, sceneName) {
    const target = String(sceneName || '').trim();
    if (!target) return null;
    if (scenes[target] != null) return target;
    for (const key of Object.keys(scenes)) {
        const entry = scenes[key];
        const words = entry && typeof entry === 'object' && Array.isArray(entry.words) ? entry.words : [];
        if (words.some((w) => String(w || '').trim() === target)) return key;
    }
    return null;
}

// 时间/天气词库是全局组：[{label,words}]。先精确命中 record 的 key，
// 再用 resolveMoodGroup 把细分词归约到组 label 当 key。
function resolveLayerKey(record, requestedKey, groups) {
    const target = String(requestedKey || '').trim();
    if (!target || !record || typeof record !== 'object') return null;
    if (record[target] != null) return target;
    const groupLabel = resolveMoodGroup(target, groups);
    if (groupLabel && record[groupLabel] != null) return groupLabel;
    return null;
}

function lookupSceneUrl(scenes, sceneName, time, weather, sceneAssets) {
    const sceneKey = resolveSceneKey(scenes, sceneName);
    const raw = sceneKey != null ? scenes[sceneKey]
        : (scenes['默认'] != null ? scenes['默认'] : null);
    if (!raw) return null;
    const entry = typeof raw === 'string' ? { url: raw } : raw;
    const timeGroups = sceneAssets && sceneAssets.timeGroups;
    const weatherGroups = sceneAssets && sceneAssets.weatherGroups;
    const timeKey = resolveLayerKey(entry.times, time, timeGroups);
    if (timeKey != null) {
        const timeRaw = entry.times[timeKey];
        const timeEntry = typeof timeRaw === 'string' ? { url: timeRaw } : timeRaw;
        const weatherKey = resolveLayerKey(timeEntry.weathers, weather, weatherGroups);
        if (weatherKey != null) {
            const weatherRaw = timeEntry.weathers[weatherKey];
            const weatherEntry = typeof weatherRaw === 'string' ? { url: weatherRaw } : weatherRaw;
            return (weatherEntry && weatherEntry.url) || (typeof weatherRaw === 'string' ? weatherRaw : null) || null;
        }
        return timeEntry.url || null;
    }
    return entry.url || null;
}

function lookupAssetValue(record, requestedKey, moodGroups) {
    if (!record || typeof record !== 'object') return { url: null, slot: '' };
    if (requestedKey && record[requestedKey]) return { url: record[requestedKey], slot: requestedKey };
    const groupLabel = resolveMoodGroup(requestedKey, moodGroups);
    if (groupLabel && record[groupLabel]) return { url: record[groupLabel], slot: groupLabel };
    if (record['默认']) return { url: record['默认'], slot: '默认' };
    return { url: null, slot: '' };
}

function normalizeSegmentIndex(value) {
    if (value == null || value === '') return null;
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric < 0) return null;
    return Math.floor(numeric);
}

__igsDefine(exports, "extractSceneDirectives", () => extractSceneDirectives);
__igsDefine(exports, "resolveNearestCharacterBefore", () => resolveNearestCharacterBefore);
__igsDefine(exports, "resolveSceneAtSourceOffset", () => resolveSceneAtSourceOffset);
__igsDefine(exports, "resolveSceneStateAtIndex", () => resolveSceneStateAtIndex);
__igsDefine(exports, "lookupSceneAssetUrls", () => lookupSceneAssetUrls);
__igsDefine(exports, "resolveCharacterKey", () => resolveCharacterKey);
});
__igsRegister("src/scene/mood-groups.js", function(module, exports, require) {
const MOOD_GROUPS_PLACEHOLDER = '{{mood_groups}}';
const SCENE_GROUPS_PLACEHOLDER = '{{scene_groups}}';
const TIME_GROUPS_PLACEHOLDER = '{{time_groups}}';
const WEATHER_GROUPS_PLACEHOLDER = '{{weather_groups}}';
const DEFAULT_MOOD_GROUPS = [
    { label: '喜悦', words: ['开心', '欢喜', '欣喜', '愉悦', '满足', '幸福', '甜蜜', '狂喜', '兴奋', '雀跃', '畅快', '陶醉', '得意', '骄傲', '自豪', '自信'] },
    { label: '愤怒', words: ['愤怒', '暴怒', '气愤', '愤慨', '暴躁', '怨恨', '敌意', '恼火', '窝火', '生气', '烦躁', '烦闷'] },
    { label: '悲伤', words: ['难过', '伤心', '心酸', '忧伤', '惆怅', '失落', '低落', '沮丧', '悲伤', '心痛', '悲痛', '痛苦', '委屈', '不甘', '失望', '受伤', '孤独', '寂寞', '落寞'] },
    { label: '紧张', words: ['焦虑', '紧张', '不安', '忐忑', '担忧', '慌张', '焦躁', '害怕', '恐惧', '惊恐', '畏惧', '胆怯', '心慌', '警惕', '戒备'] },
    { label: '平和', words: ['平静', '淡然', '冷静', '沉稳', '从容', '坦然', '淡定', '温馨', '舒畅', '惬意', '温暖', '欣慰', '释然', '感动', '感恩'] },
    { label: '害羞', words: ['害羞', '尴尬', '窘迫', '难堪', '困惑', '迷茫', '疑惑', '纠结', '犹豫', '无奈', '无语'] },
    { label: '嫌弃', words: ['厌恶', '嫌弃', '鄙视', '反感', '排斥', '抗拒', '不屑', '冷淡', '冷漠', '疏离', '麻木'] },
    { label: '爱恋', words: ['喜欢', '爱慕', '迷恋', '倾慕', '宠溺', '依恋', '心动', '认真'] },
];
function normalizeMoodGroups(value) {
    if (!Array.isArray(value)) return cloneDefaultMoodGroups();
    const groups = [];
    for (const item of value) {
        if (!item || typeof item !== 'object') continue;
        const label = String(item.label || '').trim();
        if (!label) continue;
        const words = Array.isArray(item.words)
            ? item.words.map((w) => String(w || '').trim()).filter(Boolean)
            : [];
        groups.push({ label, words });
    }
    return groups.length ? groups : cloneDefaultMoodGroups();
}
function resolveMoodGroup(word, groups) {
    const target = String(word || '').trim();
    if (!target) return null;
    const list = Array.isArray(groups) && groups.length ? groups : DEFAULT_MOOD_GROUPS;
    for (const group of list) {
        if (!group || typeof group !== 'object') continue;
        const label = String(group.label || '').trim();
        if (label === target) return label;
        const words = Array.isArray(group.words) ? group.words : [];
        if (words.some((w) => String(w || '').trim() === target)) return label;
    }
    return null;
}
function buildMoodGroupsText(groups) {
    const list = Array.isArray(groups) && groups.length ? groups : DEFAULT_MOOD_GROUPS;
    return list.map((group) => {
        const label = String(group && group.label || '').trim();
        const words = Array.isArray(group && group.words) ? group.words.filter(Boolean) : [];
        return `${label}组：${words.join('、')}`;
    }).filter((line) => line && line !== '组：').join('\n');
}

// 通用组文本：任意 [{label,words}] 列表，无默认池兜底（场景/时间/天气专用）。
function buildGroupsText(groups) {
    const list = Array.isArray(groups) ? groups : [];
    return list.map((group) => {
        const label = String(group && group.label || '').trim();
        const words = Array.isArray(group && group.words) ? group.words.filter(Boolean) : [];
        return `${label}组：${words.join('、')}`;
    }).filter((line) => line && line !== '组：').join('\n');
}

// 场景名词库是内嵌式（scenes[名].words），先转成 [{label,words}] 再生成组文本。
function buildSceneGroupsText(scenes) {
    if (!scenes || typeof scenes !== 'object') return '';
    const list = Object.keys(scenes).map((name) => {
        const entry = scenes[name];
        const words = entry && typeof entry === 'object' && Array.isArray(entry.words) ? entry.words : [];
        return { label: name, words };
    });
    return buildGroupsText(list);
}

function cloneDefaultMoodGroups() {
    return DEFAULT_MOOD_GROUPS.map((group) => ({ label: group.label, words: group.words.slice() }));
}

__igsDefine(exports, "normalizeMoodGroups", () => normalizeMoodGroups);
__igsDefine(exports, "resolveMoodGroup", () => resolveMoodGroup);
__igsDefine(exports, "buildMoodGroupsText", () => buildMoodGroupsText);
__igsDefine(exports, "buildGroupsText", () => buildGroupsText);
__igsDefine(exports, "buildSceneGroupsText", () => buildSceneGroupsText);
__igsDefine(exports, "MOOD_GROUPS_PLACEHOLDER", () => MOOD_GROUPS_PLACEHOLDER);
__igsDefine(exports, "SCENE_GROUPS_PLACEHOLDER", () => SCENE_GROUPS_PLACEHOLDER);
__igsDefine(exports, "TIME_GROUPS_PLACEHOLDER", () => TIME_GROUPS_PLACEHOLDER);
__igsDefine(exports, "WEATHER_GROUPS_PLACEHOLDER", () => WEATHER_GROUPS_PLACEHOLDER);
__igsDefine(exports, "DEFAULT_MOOD_GROUPS", () => DEFAULT_MOOD_GROUPS);
});
__igsRegister("src/host/tavern-helper-adapter.js", function(module, exports, require) {
const { getMessagePrimaryText, getVisibleMessageTextFromElement, looksLikeHostUiHtml } = require("src/scene/message-source.js");
const { collectDomRegenerateButtonCandidates } = require("src/generated-images/dom-image-candidates.js");
const INTERNAL_READER_ATTR = 'data-igs-internal-reader';
const HIDDEN_CACHE_WINDOW_MS = 300;
const VN_IMAGE_PLACEHOLDER_SELECTOR = '[data-igs-image-placeholder="1"], .igs-image-placeholder';
const LEGACY_IMAGE_PLACEHOLDER_SELECTOR = '.igs-img-ph';
function createTavernHelperAdapter(globalObject = globalThis.window || globalThis) {
    let hiddenMessageCache = { at: 0, ids: null };

    return {
        async getLastMessageId() {
            return getLastMessageId(globalObject);
        },

        async getCurrentMessage() {
            const normalizedMessages = getNormalizedMessages(globalObject, hiddenMessageCache);
            return pickCurrentMessage(normalizedMessages) || normalizedMessages[normalizedMessages.length - 1] || null;
        },

        async getMessageById(messageId) {
            const normalizedId = normalizeMessageId(messageId);
            if (normalizedId == null) return null;
            const domMessageMap = createDomMessageMap(globalObject);
            const hiddenIds = getHiddenMessageIdSet(globalObject, hiddenMessageCache);
            const direct = getChatMessages(globalObject, normalizedId, normalizedId).find((message, index) => {
                return resolveMessageId(message, normalizedId + index) === normalizedId;
            });
            if (direct) return normalizeMessage(direct, normalizedId, domMessageMap, hiddenIds);

            const entries = getChatMessageEntries(globalObject);
            const fallback = entries.find((item) => item.id === normalizedId);
            return normalizeMessage(fallback && fallback.message, normalizedId, domMessageMap, hiddenIds);
        },

        async listMessages() {
            return getNormalizedMessages(globalObject, hiddenMessageCache);
        },

        async getAdjacentMessage(messageId, delta = 1) {
            const normalizedId = normalizeMessageId(messageId);
            if (normalizedId == null) return null;
            const step = Number(delta) < 0 ? -1 : 1;
            const messages = getNormalizedMessages(globalObject, hiddenMessageCache).filter(isTurnCandidate);
            const currentIndex = messages.findIndex((message) => message.id === normalizedId);
            if (currentIndex < 0) return null;
            return messages[currentIndex + step] || null;
        },

        async jumpToMessage(messageId) {
            const helper = getTavernHelper(globalObject);
            if (!helper) return { ok: false, reason: 'missing-tavern-helper' };
            const normalizedId = normalizeMessageId(messageId);
            if (normalizedId == null) return { ok: false, reason: 'invalid-message-id', messageId };
            if (typeof helper.triggerSlash !== 'function') {
                return { ok: false, reason: 'missing-trigger-slash', messageId: normalizedId };
            }
            await helper.triggerSlash(`/chat-jump ${normalizedId}`);
            return { ok: true, messageId: normalizedId };
        },

        async findRegenerateButton(messageId, imageIndex = 0, imageState = null) {
            const message = await this.getMessageById(messageId);
            if (!message) return null;
            return findMessageRegenerateButton(message, imageIndex, imageState);
        },

        async ensureMessageImagePlaceholders(messageId, imageSlots = [], message = null) {
            const scopedMessage = message || await this.getMessageById(messageId);
            return ensureMessageImagePlaceholders(scopedMessage, imageSlots);
        },

        async removeMessageImagePlaceholders(messageId, message = null) {
            const scopedMessage = message || await this.getMessageById(messageId);
            return removeMessageImagePlaceholders(scopedMessage);
        },

        async setInputText(text) {
            const helper = getTavernHelper(globalObject);
            if (helper && typeof helper.setInputText === 'function') {
                await helper.setInputText(String(text == null ? '' : text));
                return { ok: true, reason: 'tavern-helper-fill' };
            }
            const domResult = setInputTextViaHostDom(globalObject, text);
            if (domResult.ok) return { ok: true, reason: domResult.reason };
            return { ok: false, reason: domResult.reason || 'missing-input-api' };
        },

        async typeAndSend(text) {
            const helper = getTavernHelper(globalObject);
            if (helper && typeof helper.typeAndSend === 'function') {
                await helper.typeAndSend(text);
                return { ok: true };
            }
            if (helper && typeof helper.setInputText === 'function' && typeof helper.send === 'function') {
                await helper.setInputText(text);
                await helper.send();
                return { ok: true };
            }
            const domResult = sendViaHostDom(globalObject, text);
            if (domResult.ok) return domResult;
            if (!helper) return { ok: false, reason: 'missing-tavern-helper' };
            return { ok: false, reason: domResult.reason || 'missing-send-api' };
        },
    };
}

function setInputTextViaHostDom(globalObject, text) {
    const value = String(text == null ? '' : text);
    for (const doc of getCandidateDocuments(globalObject)) {
        const textarea = safeQuery(doc, '#send_textarea');
        if (!textarea) continue;
        try {
            const setter = resolveNativeValueSetter(textarea, doc);
            if (setter) setter.call(textarea, value);
            else textarea.value = value;
            dispatchInputEvents(textarea, doc);
            if (typeof textarea.focus === 'function') textarea.focus();
            return { ok: true, reason: 'host-dom-fill', doc };
        } catch (error) {
            return { ok: false, reason: 'host-dom-fill-failed' };
        }
    }
    return { ok: false, reason: 'missing-host-input' };
}

function sendViaHostDom(globalObject, text) {
    const filled = setInputTextViaHostDom(globalObject, text);
    if (!filled.ok) return { ok: false, reason: 'missing-host-dom-send' };
    const button = safeQuery(filled.doc, '#send_but');
    if (!button || typeof button.click !== 'function') return { ok: false, reason: 'missing-host-dom-send' };
    try {
        button.click();
        return { ok: true, reason: 'host-dom-send' };
    } catch (error) {
        return { ok: false, reason: 'host-dom-send-failed' };
    }
}

function resolveNativeValueSetter(element, doc) {
    const view = (doc && doc.defaultView) || globalThis.window || globalThis;
    const proto = element && typeof element.tagName === 'string' && element.tagName.toLowerCase() === 'textarea'
        ? view.HTMLTextAreaElement && view.HTMLTextAreaElement.prototype
        : view.HTMLInputElement && view.HTMLInputElement.prototype;
    const descriptor = proto && Object.getOwnPropertyDescriptor(proto, 'value');
    return descriptor && typeof descriptor.set === 'function' ? descriptor.set : null;
}

function dispatchInputEvents(element, doc) {
    if (!element || typeof element.dispatchEvent !== 'function') return;
    const view = (doc && doc.defaultView) || globalThis.window || globalThis;
    const EventCtor = view && view.Event || globalThis.Event;
    for (const type of ['input', 'change']) {
        try {
            if (typeof EventCtor === 'function') {
                element.dispatchEvent(new EventCtor(type, { bubbles: true }));
                continue;
            }
        } catch (error) {
            // Fall back to plain-object dispatch for fake documents.
        }
        try {
            element.dispatchEvent({ type, bubbles: true });
        } catch (error) {
            // Ignore dispatch failures from host shims.
        }
    }
}

function safeQuery(root, selector) {
    try {
        return root && typeof root.querySelector === 'function'
            ? root.querySelector(selector)
            : null;
    } catch (error) {
        return null;
    }
}
function getTavernHelper(globalObject = globalThis.window || globalThis) {
    try {
        return globalObject && globalObject.TavernHelper
            || globalObject && globalObject.top && globalObject.top.TavernHelper
            || (typeof window !== 'undefined' ? window.TavernHelper : null)
            || null;
    } catch (error) {
        return null;
    }
}
function getSillyTavernContext(globalObject = globalThis.window || globalThis) {
    return (
        getContextFromTarget(globalObject) ||
        getContextFromTarget(globalObject && globalObject.parent) ||
        getContextFromTarget(globalObject && globalObject.top) ||
        null
    );
}
function getChatFromContext(globalObject = globalThis.window || globalThis) {
    const context = getSillyTavernContext(globalObject);
    return context && Array.isArray(context.chat) ? context.chat : null;
}
function getCandidateDocuments(globalObject = globalThis.window || globalThis) {
    const docs = [];
    const addDoc = (doc) => {
        if (doc && !docs.includes(doc)) docs.push(doc);
    };

    addDoc(safeDocument(globalObject));
    try {
        addDoc(safeDocument(globalObject && globalObject.top));
    } catch (error) {
        // Cross-origin top windows are ignored.
    }
    if (typeof document !== 'undefined') addDoc(document);
    return docs;
}
function getMessageScopedRoots(message, targetDocument = null) {
    const roots = [];
    const seen = new Set();
    const addRoot = (root) => {
        if (!root || typeof root.querySelectorAll !== 'function' || seen.has(root)) return;
        seen.add(root);
        roots.push(root);
    };

    const element = message && message.element;
    if (element) {
        addRoot(element);
        addRoot(getMessageTextRoot(element));
        getMessageScopedDocs(element).forEach(addRoot);
        if (targetDocument && documentBelongsToMessage(targetDocument, element)) {
            addRoot(targetDocument);
        }
        return roots;
    }

    addRoot(targetDocument);
    return roots;
}
function ensureMessageImagePlaceholders(message, imageSlots = []) {
    const placeholders = normalizeImagePlaceholderBlocks(imageSlots);
    if (!placeholders.length) {
        return removeMessageImagePlaceholders(message);
    }
    const mesText = getMessageTextRoot(resolveMessageElementFromInput(message));
    if (!mesText || typeof mesText.appendChild !== 'function') {
        return {
            ok: false,
            reason: 'message-text-root-not-found',
            count: placeholders.length,
        };
    }
    const owned = getOwnedImagePlaceholders(mesText);
    const legacy = !owned.length && typeof mesText.querySelector === 'function'
        ? mesText.querySelector(LEGACY_IMAGE_PLACEHOLDER_SELECTOR)
        : null;
    if (legacy) {
        return {
            ok: true,
            reason: 'placeholder-present-legacy',
            count: placeholders.length,
        };
    }
    const signature = placeholders.map(buildImagePlaceholderSignature).join('\n');
    const ownedSignature = owned.map((node) => String(
        node && typeof node.getAttribute === 'function'
            ? node.getAttribute('data-igs-placeholder-signature') || ''
            : '',
    )).join('\n');
    if (owned.length === placeholders.length && ownedSignature === signature) {
        return {
            ok: true,
            reason: 'placeholder-present',
            count: placeholders.length,
        };
    }
    owned.forEach(removeNodeSafely);
    for (const placeholder of placeholders) {
        mesText.appendChild(createMessageImagePlaceholderNode(mesText.ownerDocument, placeholder));
    }
    dispatchMessagePlaceholderMutation(mesText);
    return {
        ok: true,
        reason: 'placeholder-injected',
        count: placeholders.length,
    };
}
function removeMessageImagePlaceholders(message) {
    const mesText = getMessageTextRoot(resolveMessageElementFromInput(message));
    if (!mesText) {
        return {
            ok: false,
            reason: 'message-text-root-not-found',
            count: 0,
        };
    }
    const owned = getOwnedImagePlaceholders(mesText);
    owned.forEach(removeNodeSafely);
    return {
        ok: true,
        reason: owned.length ? 'placeholder-removed' : 'placeholder-none',
        count: owned.length,
    };
}

function getLastMessageId(globalObject) {
    const helper = getTavernHelper(globalObject);
    if (helper && typeof helper.getLastMessageId === 'function') {
        try {
            return normalizeMessageId(helper.getLastMessageId());
        } catch (error) {
            return resolveContextLastMessageId(globalObject);
        }
    }
    return resolveContextLastMessageId(globalObject);
}

function resolveContextLastMessageId(globalObject) {
    const chat = getChatFromContext(globalObject);
    return Array.isArray(chat) && chat.length ? chat.length - 1 : null;
}

function resolveMessageElementFromInput(message) {
    if (!message || typeof message !== 'object') return null;
    if (isDomLikeMessageElement(message.element)) return message.element;
    return isDomLikeMessageElement(message) ? message : null;
}

function normalizeImagePlaceholderBlocks(imageSlots) {
    const output = [];
    for (const slot of Array.isArray(imageSlots) ? imageSlots : []) {
        const rawBlock = String(slot && slot.rawBlock || '').trim();
        const promptText = String(slot && slot.promptText || '').trim();
        const block = rawBlock || promptText;
        if (!block || output.some((item) => item.block === block)) continue;
        const slotIndex = normalizeMessageSlotIndex(slot && slot.slotIndex, output.length);
        output.push({
            block,
            slotIndex,
            imageId: String(slot && slot.imageId || `igs-slot-${slotIndex + 1}`).trim(),
            locationHash: String(slot && slot.locationHash || '').trim(),
            title: String(slot && slot.title || `图 ${slotIndex + 1}`).trim(),
        });
    }
    return output;
}

function getOwnedImagePlaceholders(mesText) {
    if (!mesText || typeof mesText.querySelectorAll !== 'function') return [];
    try {
        return Array.from(mesText.querySelectorAll(VN_IMAGE_PLACEHOLDER_SELECTOR));
    } catch (error) {
        return [];
    }
}

function createMessageImagePlaceholderNode(ownerDocument, placeholder) {
    const node = ownerDocument && typeof ownerDocument.createElement === 'function'
        ? ownerDocument.createElement('div')
        : buildFallbackPlaceholderNode();
    if (typeof node.setAttribute === 'function') {
        node.setAttribute('data-igs-placeholder', '1');
        node.setAttribute('data-igs-image-placeholder', '1');
        node.setAttribute('data-igs-image-slot', String(placeholder.slotIndex));
        node.setAttribute('data-slot-index', String(placeholder.slotIndex));
        node.setAttribute('data-image-index', String(placeholder.slotIndex));
        node.setAttribute('data-image-id', placeholder.imageId);
        node.setAttribute('data-igs-image-id', placeholder.imageId);
        if (placeholder.locationHash) {
            node.setAttribute('data-location-hash', placeholder.locationHash);
            node.setAttribute('data-igs-location-hash', placeholder.locationHash);
        }
        node.setAttribute('data-igs-placeholder-signature', buildImagePlaceholderSignature(placeholder));
    }
    node.className = 'igs-img-ph igs-image-placeholder';
    node.style = node.style || {};
    node.style.cssText = 'position:absolute;width:0;height:0;overflow:hidden;opacity:0;pointer-events:none;font-size:0';
    node.textContent = String(placeholder.block || '');
    return node;
}

function buildImagePlaceholderSignature(placeholder) {
    return [
        placeholder && placeholder.slotIndex,
        placeholder && placeholder.locationHash,
        placeholder && placeholder.imageId,
        placeholder && placeholder.block,
    ].map((part) => String(part || '')).join('|');
}

function buildFallbackPlaceholderNode() {
    return {
        className: '',
        style: {},
        textContent: '',
        attributes: new Map(),
        parentNode: null,
        parentElement: null,
        setAttribute(name, value) {
            this.attributes.set(name, String(value));
        },
        getAttribute(name) {
            return this.attributes.has(name) ? this.attributes.get(name) : null;
        },
        remove() {
            if (!this.parentNode || !Array.isArray(this.parentNode.children)) return;
            this.parentNode.children = this.parentNode.children.filter((child) => child !== this);
            this.parentNode = null;
            this.parentElement = null;
        },
    };
}

function dispatchMessagePlaceholderMutation(mesText) {
    if (!mesText || typeof mesText.dispatchEvent !== 'function') return;
    const ownerWindow = mesText.ownerDocument && mesText.ownerDocument.defaultView || globalThis.window || globalThis;
    const EventCtor = ownerWindow && ownerWindow.Event || globalThis.Event;
    try {
        if (typeof EventCtor === 'function') {
            mesText.dispatchEvent(new EventCtor('DOMSubtreeModified', { bubbles: true }));
            return;
        }
    } catch (error) {
        // Fall back to plain-object dispatch for fake documents.
    }
    try {
        mesText.dispatchEvent({ type: 'DOMSubtreeModified', bubbles: true });
    } catch (error) {
        // Ignore dispatch failures from host shims.
    }
}

function removeNodeSafely(node) {
    if (!node) return;
    if (typeof node.remove === 'function') {
        node.remove();
        return;
    }
    if (node.parentNode && Array.isArray(node.parentNode.children)) {
        node.parentNode.children = node.parentNode.children.filter((child) => child !== node);
    }
}

function getChatMessages(globalObject, startId, endId, extraOptions = {}) {
    const helper = getTavernHelper(globalObject);
    const safeStart = normalizeMessageId(startId) ?? 0;
    const safeEnd = normalizeMessageId(endId) ?? safeStart;
    if (helper && typeof helper.getChatMessages === 'function') {
        try {
            const range = `${Math.min(safeStart, safeEnd)}-${Math.max(safeStart, safeEnd)}`;
            const messages = helper.getChatMessages(range, {
                include_swipes: false,
                ...extraOptions,
            });
            return Array.isArray(messages) ? messages : [];
        } catch (error) {
            return [];
        }
    }
    const chat = getChatFromContext(globalObject);
    if (!Array.isArray(chat)) return [];
    return chat.slice(Math.min(safeStart, safeEnd), Math.max(safeStart, safeEnd) + 1);
}

function getChatMessageEntries(globalObject, extraOptions = {}) {
    const output = [];
    const seen = new Set();
    const lastId = getLastMessageId(globalObject);
    const helperMessages = lastId != null ? getChatMessages(globalObject, 0, lastId, extraOptions) : [];

    helperMessages.forEach((message, index) => {
        const id = resolveMessageId(message, index);
        if (id == null || seen.has(id)) return;
        seen.add(id);
        output.push({ id, message, index });
    });
    if (output.length) return output;

    const chat = getChatFromContext(globalObject);
    if (!Array.isArray(chat)) return output;
    chat.forEach((message, index) => {
        const id = resolveMessageId(message, index);
        if (id == null || seen.has(id)) return;
        seen.add(id);
        output.push({ id, message, index });
    });
    return output;
}

function normalizeMessage(message, fallbackId, domMessageMap = new Map(), hiddenIds = null) {
    if (typeof message === 'string') {
        return { id: fallbackId, text: message, rawHtml: message, raw: message };
    }
    if (!message || typeof message !== 'object') return null;
    const id = resolveMessageId(message, fallbackId);
    if (id == null) return null;
    const element = resolveMessageElement(message, id, domMessageMap);
    const rawText = getMessagePrimaryText(message);
    const visibleText = normalizeText(
        message.visibleText
        || message.displayText
        || message.plainText
        || getVisibleMessageTextFromElement(element),
    );
    return {
        id,
        text: rawText,
        rawHtml: rawText,
        visibleText,
        looksLikeHostUiHtml: looksLikeHostUiHtml(rawText),
        element,
        isUser: isUserMessage(message, element),
        isSystem: isSystemMessage(message, element),
        isHidden: isHiddenMessage(message, id, element, hiddenIds),
        raw: message,
    };
}

function getNormalizedMessages(globalObject, hiddenMessageCache) {
    const domMessageMap = createDomMessageMap(globalObject);
    const hiddenIds = getHiddenMessageIdSet(globalObject, hiddenMessageCache);
    return getChatMessageEntries(globalObject)
        .map((entry) => normalizeMessage(entry.message, entry.id, domMessageMap, hiddenIds))
        .filter(Boolean)
        .sort((left, right) => (left.id ?? 0) - (right.id ?? 0));
}

function createDomMessageMap(globalObject) {
    const map = new Map();
    for (const doc of getCandidateDocuments(globalObject)) {
        const nodes = safeQueryAll(doc, '#chat .mes, .mes');
        for (const node of nodes) {
            const id = getElementMessageId(node);
            if (id == null || map.has(id)) continue;
            map.set(id, node);
        }
    }
    return map;
}

function getContextFromTarget(target) {
    try {
        if (target && target.SillyTavern && typeof target.SillyTavern.getContext === 'function') {
            return target.SillyTavern.getContext();
        }
    } catch (error) {
        return null;
    }
    return null;
}

function readHiddenMessageIdsFromTavernHelper(globalObject) {
    const helper = getTavernHelper(globalObject);
    if (!helper || typeof helper.getChatMessages !== 'function') return null;
    const lastId = getLastMessageId(globalObject);
    if (lastId == null) return null;
    try {
        const messages = helper.getChatMessages(`0-${lastId}`, { hide_state: 'hidden', include_swipes: false });
        if (!Array.isArray(messages)) return null;
        const ids = new Set();
        messages.forEach((entry) => {
            const id = resolveMessageId(entry);
            if (id != null) ids.add(id);
        });
        return ids;
    } catch (error) {
        return null;
    }
}

function getHiddenMessageIdSet(globalObject, hiddenMessageCache) {
    const now = Date.now();
    if (hiddenMessageCache.ids && now - hiddenMessageCache.at < HIDDEN_CACHE_WINDOW_MS) {
        return hiddenMessageCache.ids;
    }
    const ids = readHiddenMessageIdsFromTavernHelper(globalObject);
    hiddenMessageCache.at = now;
    hiddenMessageCache.ids = ids;
    return ids;
}

function pickCurrentMessage(messages) {
    const readable = messages.filter(isTurnCandidate);
    return readable[readable.length - 1] || null;
}

function isTurnCandidate(message) {
    return Boolean(
        message
        && !message.isSystem
        && !message.isHidden
        && !message.isUser,
    );
}

function findMessageRegenerateButton(message, imageIndex, imageState = null) {
    const candidates = collectDomRegenerateButtonCandidates(getMessageScopedRoots(message));
    const targetIndex = Math.max(0, Math.floor(Number(imageIndex) || 0));
    const currentSlot = imageState
        && Array.isArray(imageState.slots)
        && imageState.slots[targetIndex]
        ? imageState.slots[targetIndex]
        : null;
    if (currentSlot && currentSlot.locationHash) {
        const candidate = candidates.find((item) => item.locationHash && item.locationHash === currentSlot.locationHash);
        if (candidate) return candidate.button;
    }
    if (currentSlot && currentSlot.imageId) {
        const candidate = candidates.find((item) => item.imageId && item.imageId === currentSlot.imageId);
        if (candidate) return candidate.button;
    }
    if (currentSlot && currentSlot.buttonIndex != null) {
        const candidate = candidates.find((item) => item.buttonIndex === currentSlot.buttonIndex);
        if (candidate) return candidate.button;
    }
    const fallback = candidates[targetIndex] || candidates[0] || null;
    return fallback ? fallback.button : null;
}

function getMessageScopedDocs(messageElement) {
    const docs = [];
    const seen = new Set();
    for (const frame of safeQueryAll(messageElement, 'iframe')) {
        const doc = getFrameDocument(frame);
        if (doc && !seen.has(doc)) {
            seen.add(doc);
            docs.push(doc);
        }
    }
    return docs;
}

function getFrameDocument(frame) {
    if (!frame || isInternalReaderFrame(frame)) return null;
    try {
        return frame.contentDocument || frame.contentWindow && frame.contentWindow.document || null;
    } catch (error) {
        return null;
    }
}

function isInternalReaderFrame(frame) {
    return Boolean(frame && typeof frame.getAttribute === 'function' && frame.getAttribute(INTERNAL_READER_ATTR) === '1');
}

function documentBelongsToMessage(doc, messageElement) {
    if (!doc || !messageElement) return false;
    if (getMessageScopedDocs(messageElement).includes(doc)) return true;
    try {
        const frame = doc.defaultView && doc.defaultView.frameElement;
        const owner = frame && !isInternalReaderFrame(frame) && typeof frame.closest === 'function'
            ? frame.closest('.mes[mesid]')
            : null;
        return Boolean(owner && sameMessageByMesId(owner, messageElement));
    } catch (error) {
        return false;
    }
}

function sameMessageByMesId(left, right) {
    if (!left || !right) return left === right;
    if (left === right) return true;
    const leftId = getElementMessageId(left);
    const rightId = getElementMessageId(right);
    return leftId != null && rightId != null && leftId === rightId;
}

function getMessageTextRoot(element) {
    try {
        return element && typeof element.querySelector === 'function'
            ? element.querySelector('.mes_text')
            : null;
    } catch (error) {
        return null;
    }
}

function getElementMessageId(element) {
    if (!element || typeof element.getAttribute !== 'function') return null;
    return normalizeMessageId(
        element.getAttribute('mesid')
        ?? element.getAttribute('data-mesid')
        ?? element.getAttribute('data-message-id')
        ?? element.getAttribute('data-id')
        ?? element.id,
    );
}

function resolveMessageElement(message, id, domMessageMap) {
    const direct = getMessageDomElement(message);
    if (direct) return direct;
    return id != null ? domMessageMap.get(id) || null : null;
}

function getMessageDomElement(message) {
    if (!message || typeof message !== 'object') return null;
    if (isDomLikeMessageElement(message.element)) return message.element;
    return isDomLikeMessageElement(message) ? message : null;
}

function isDomLikeMessageElement(value) {
    return Boolean(value && typeof value === 'object' && typeof value.getAttribute === 'function');
}

function resolveMessageId(message, fallbackId) {
    return normalizeMessageId(message && (message.id ?? message.message_id ?? message.mesid ?? fallbackId));
}

function normalizeMessageId(value) {
    const id = Number(value);
    if (!Number.isFinite(id) || id < 0) return null;
    return id;
}

function normalizeMessageSlotIndex(value, fallback) {
    const index = Number(value);
    if (!Number.isFinite(index) || index < 0) return Math.max(0, Math.floor(Number(fallback) || 0));
    return Math.floor(index);
}

function isUserMessage(message, element) {
    return Boolean(
        message && (
            isTruthyFlag(message.is_user)
            || isTruthyFlag(message.isUser)
            || isTruthyFlag(message.user)
            || message.role === 'user'
            || element && element.getAttribute('is_user') === 'true'
        ),
    );
}

function isSystemMessage(message, element) {
    return Boolean(
        message && (
            isTruthyFlag(message.is_system)
            || isTruthyFlag(message.isSystem)
            || isTruthyFlag(message.system)
            || element && element.getAttribute('is_system') === 'true'
        ),
    );
}

function isHiddenMessage(message, messageId, element, hiddenIds) {
    return Boolean(
        message && (
            isTruthyFlag(message.is_hidden)
            || isTruthyFlag(message.isHidden)
            || isTruthyFlag(message.hidden)
            || element && element.getAttribute('is_hidden') === 'true'
            || hiddenIds instanceof Set && hiddenIds.has(messageId)
        ),
    );
}

function isTruthyFlag(value) {
    return value === true || value === 1 || value === '1' || value === 'true';
}

function safeDocument(target) {
    try {
        return target && target.document || null;
    } catch (error) {
        return null;
    }
}

function safeQueryAll(root, selector) {
    try {
        return root && typeof root.querySelectorAll === 'function'
            ? Array.from(root.querySelectorAll(selector))
            : [];
    } catch (error) {
        return [];
    }
}

function normalizeText(value) {
    return String(value || '')
        .replace(/\u00a0/g, ' ')
        .replace(/\r\n?/g, '\n')
        .replace(/[ \t]+\n/g, '\n')
        .replace(/\n{3,}/g, '\n\n')
        .trim();
}

__igsDefine(exports, "createTavernHelperAdapter", () => createTavernHelperAdapter);
__igsDefine(exports, "getTavernHelper", () => getTavernHelper);
__igsDefine(exports, "getSillyTavernContext", () => getSillyTavernContext);
__igsDefine(exports, "getChatFromContext", () => getChatFromContext);
__igsDefine(exports, "getCandidateDocuments", () => getCandidateDocuments);
__igsDefine(exports, "getMessageScopedRoots", () => getMessageScopedRoots);
__igsDefine(exports, "ensureMessageImagePlaceholders", () => ensureMessageImagePlaceholders);
__igsDefine(exports, "removeMessageImagePlaceholders", () => removeMessageImagePlaceholders);
});
__igsRegister("src/generated-images/dom-image-candidates.js", function(module, exports, require) {
const ADAPTER_SELECTORS = Object.freeze({
    chatu8: Object.freeze({
        images: Object.freeze([
            'img.st-chatu8-image-tag-image',
            '[class*="st-chatu8"] img',
            '[class*="chatu8"] img',
        ]),
        buttons: Object.freeze([
            'button.image-tag-button',
            'button[class*="image-tag-button"]',
            'button[class*="st-chatu8-image"]',
        ]),
    }),
    chami: Object.freeze({
        images: Object.freeze([
            '.tsp-generated-image',
            '.tsp-inline-image',
            '.tsp-image-slot img',
            'img[src*="tsp-images"]',
        ]),
        buttons: Object.freeze([
            '.tsp-inline-gen-btn',
            '.tsp-regenerate-btn',
        ]),
        metadata: Object.freeze([
            '[data-image-id]',
            '[data-location-hash]',
            '[data-slot-index]',
            '[data-image-index]',
            '[data-igs-image-slot]',
            'img[data-image-id]',
            'img[data-location-hash]',
            '[data-image-id] img',
            '[data-location-hash] img',
        ]),
    }),
    generic: Object.freeze({
        images: Object.freeze([
            '.mes_text img[src]',
            '.mes_text img[data-src]',
            'img[src]',
            'img[data-src]',
            'img[src^="blob:"]',
            'img[src^="data:image"]',
            'video',
            'a[href^="blob:"]',
            'a[href^="data:image"]',
            '[style*="background-image"]',
        ]),
    }),
});
function getAdapterSelectors() {
    return ADAPTER_SELECTORS;
}
function resolveDomRoots(input = {}) {
    const roots = [];
    const seen = new Set();
    const scopePolicy = input.scopePolicy && typeof input.scopePolicy === 'object'
        ? input.scopePolicy
        : {};
    const lockToScopedRoots = scopePolicy.hasMessageScope === true;

    function add(root) {
        if (!root || typeof root.querySelectorAll !== 'function' || seen.has(root)) return;
        seen.add(root);
        roots.push(root);
    }

    const sourceRoots = Array.isArray(input.roots) ? input.roots : [];
    sourceRoots.forEach(add);
    add(input.root);
    if (!lockToScopedRoots) add(input.document);
    return roots;
}
function getGlobalDetectionRoots(globalObject = globalThis.window || globalThis) {
    const roots = [];
    const seen = new Set();
    const add = (root) => {
        if (!root || typeof root.querySelectorAll !== 'function' || seen.has(root)) return;
        seen.add(root);
        roots.push(root);
    };

    add(safeDocument(globalObject));
    try {
        add(safeDocument(globalObject.top));
    } catch (error) {
        // Cross-origin parents are ignored.
    }
    if (typeof document !== 'undefined') add(document);
    return roots;
}
function detectExternalImageAdapter(context = {}) {
    const roots = resolveDomRoots(context);
    const globalObject = context.global || globalThis.window || globalThis;
    const selectors = getAdapterSelectors();
    const chatu8Images = countMatches(roots, selectors.chatu8.images);
    const chatu8Buttons = countMatches(roots, selectors.chatu8.buttons);
    const chamiImages = countMatches(roots, selectors.chami.images);
    const chamiButtons = countMatches(roots, selectors.chami.buttons);
    const chamiMetadata = countMatches(roots, selectors.chami.metadata);
    const topWindow = resolveTopWindow(globalObject);
    const hasEventSource = Boolean(topWindow && topWindow.eventSource && typeof topWindow.eventSource.emit === 'function');
    const hasChamiApi = Boolean(topWindow && (topWindow.TSP || topWindow.tsp || topWindow.tspPlugin || topWindow.TavernScenePlugin));
    const requested = normalizeAdapterSelection(context.imageApi && context.imageApi.externalAdapter);

    let adapter = 'none';
    if (requested === 'chami') {
        adapter = chamiImages || chamiButtons || chamiMetadata || hasEventSource || hasChamiApi ? 'chami' : 'none';
    } else if (requested === 'chatu8') {
        adapter = chatu8Images || chatu8Buttons ? 'chatu8' : 'none';
    } else if (chamiImages || chamiButtons || chamiMetadata || hasEventSource || hasChamiApi) {
        adapter = 'chami';
    } else if (chatu8Images || chatu8Buttons) {
        adapter = 'chatu8';
    }

    const summary = [];
    if (chamiImages || chamiButtons || chamiMetadata || hasEventSource || hasChamiApi) {
        summary.push(`chami: 图片 ${chamiImages}，按钮 ${chamiButtons}，元数据 ${chamiMetadata}${hasEventSource ? '，事件可用' : ''}${hasChamiApi ? '，页面对象可见' : ''}`);
    }
    if (chatu8Images || chatu8Buttons) {
        summary.push(`chatu8: 图片 ${chatu8Images}，按钮 ${chatu8Buttons}`);
    }

    return {
        ok: adapter !== 'none',
        adapter,
        details: {
            chamiImages,
            chamiButtons,
            chamiMetadata,
            chatu8Images,
            chatu8Buttons,
            hasEventSource,
            hasChamiApi,
        },
        message: adapter === 'none'
            ? `未检测到可用插图扩展。${summary.length ? ` 当前探测：${summary.join('；')}` : ''}`
            : `已检测到 ${adapter} 插图扩展。${summary.length ? ` ${summary.join('；')}` : ''}`,
    };
}
function collectDomImageCandidates(roots, options = {}) {
    const adapterKeys = normalizeAdapterKeyList(options.adapterKeys);
    const selectors = buildCandidateSelectorList(adapterKeys);
    const seenNodes = new Set();
    const grouped = new Map();
    const plain = [];
    let order = 0;

    function add(node, adapterKey, root, selector) {
        if (!node || seenNodes.has(node)) return;
        seenNodes.add(node);
        const imageNode = getImageElement(node) || node;
        if (adapterKey === 'generic' && isLikelyHostDecorImage(node, imageNode)) return;
        if (adapterKey === 'generic' && !isGenericCandidateAllowed(node, imageNode, root, selector, options.scopePolicy)) return;
        const url = rawImageUrl(imageNode);
        if (!url) return;
        const groupKey = imageCandidateGroupKey(node, imageNode, url, order);
        const metadata = collectNodeMetadata(node, imageNode);
        const candidate = {
            url,
            groupKey,
            order: order + 1,
            adapterKey,
            imageId: metadata.imageId,
            locationHash: metadata.locationHash,
            slotIndex: metadata.slotIndex,
            buttonIndex: metadata.buttonIndex,
        };
        order += 1;
        if (groupKey.startsWith('node:')) {
            plain.push(candidate);
            return;
        }
        const previous = grouped.get(groupKey);
        if (!previous || candidate.order >= previous.order) {
            grouped.set(groupKey, candidate);
        }
    }

    for (const entry of selectors) {
        for (const root of Array.isArray(roots) ? roots : []) {
            for (const node of safeQueryAll(root, entry.selector)) {
                add(node, entry.adapterKey, root, entry.selector);
            }
        }
    }

    return plain.concat(Array.from(grouped.values())).sort((left, right) => left.order - right.order);
}
function findDomRegenerateButtons(roots) {
    return collectDomRegenerateButtonCandidates(roots).map((candidate) => candidate.button);
}
function collectDomRegenerateButtonCandidates(roots) {
    const selectors = [
        ...ADAPTER_SELECTORS.chatu8.buttons.map((selector) => ({ selector, adapterKey: 'chatu8', requireHint: false })),
        ...ADAPTER_SELECTORS.chami.buttons.map((selector) => ({ selector, adapterKey: 'chami', requireHint: false })),
        { selector: 'button', adapterKey: 'generic', requireHint: true },
        { selector: '[role="button"]', adapterKey: 'generic', requireHint: true },
        { selector: 'input[type="button"]', adapterKey: 'generic', requireHint: true },
        { selector: 'input[type="submit"]', adapterKey: 'generic', requireHint: true },
        { selector: 'a[role="button"]', adapterKey: 'generic', requireHint: true },
    ];
    const buttons = [];
    const seen = new Set();
    let order = 0;
    for (const entry of selectors) {
        for (const root of Array.isArray(roots) ? roots : []) {
            for (const button of safeQueryAll(root, entry.selector)) {
                if (!button || seen.has(button)) continue;
                if (entry.requireHint && !isLikelyRegenerateButton(button)) continue;
                seen.add(button);
                const metadata = collectNodeMetadata(button);
                buttons.push({
                    button,
                    adapterKey: entry.adapterKey,
                    order: order + 1,
                    imageId: metadata.imageId,
                    locationHash: metadata.locationHash,
                    slotIndex: metadata.slotIndex,
                    buttonIndex: metadata.buttonIndex,
                });
                order += 1;
            }
        }
    }
    return buttons;
}
function normalizeImageUrl(value, key) {
    let url = decodeImageUrlText(value);
    if (!url || (/\s/.test(url) && !/^data:image\//i.test(url))) return '';
    url = url.replace(/^url\(["']?/, '').replace(/["']?\)$/, '');
    if (/^data:image\//i.test(url) || /^blob:/i.test(url)) return url;
    const imageish = /\.(?:png|jpe?g|webp|gif|bmp|svg|avif)(?:[?#].*)?$/i.test(url)
        || /(?:\/|=)(?:image|img|thumbnail|thumb|cover|photo|picture|tsp-images|chatu8|scene)(?:[\/._=?&-]|$)/i.test(url)
        || imageKeyHint(key);
    if (/^https?:\/\//i.test(url)) return imageish ? url : '';
    if (/^(?:\/|\.\.?\/)[^\s"'<>]+/i.test(url)) return imageish ? url : '';
    if (/^(?:[A-Za-z0-9_.~%-]+\/)*[A-Za-z0-9_.~%-]+\.(?:png|jpe?g|webp|gif|bmp|svg|avif)(?:[?#].*)?$/i.test(url)) return url;
    return '';
}

function normalizeAdapterKeyList(value) {
    const keys = Array.isArray(value) ? value : [];
    if (!keys.length) return ['chatu8', 'chami', 'generic'];
    const output = [];
    for (const key of keys) {
        const normalized = normalizeAdapterSelection(key);
        if (!normalized || output.includes(normalized)) continue;
        output.push(normalized);
    }
    return output.length ? output : ['chatu8', 'chami', 'generic'];
}

function buildCandidateSelectorList(adapterKeys) {
    const output = [];
    for (const key of adapterKeys) {
        const config = ADAPTER_SELECTORS[key];
        if (!config) continue;
        for (const selector of config.images || []) {
            output.push({ adapterKey: key, selector });
        }
        if (key === 'chami') {
            for (const selector of config.metadata || []) {
                output.push({ adapterKey: key, selector });
            }
        }
    }
    return output;
}

function countMatches(roots, selectors) {
    const seen = new Set();
    for (const selector of Array.isArray(selectors) ? selectors : []) {
        for (const root of Array.isArray(roots) ? roots : []) {
            for (const node of safeQueryAll(root, selector)) {
                seen.add(node);
            }
        }
    }
    return seen.size;
}

function getImageElement(node) {
    if (!node) return null;
    if (isImageLikeNode(node)) return node;
    return typeof node.querySelector === 'function'
        ? node.querySelector('img[src], img[data-src]')
        : null;
}

function rawImageUrl(node) {
    if (!node) return '';
    if (isUnloadedPlaceholderImage(node)) return '';
    if (node.tagName === 'IMG' || hasImageSource(node)) {
        return normalizeImageUrl(node.currentSrc || node.src || safeGetAttribute(node, 'data-src') || '', 'src');
    }
    if (node.tagName === 'VIDEO') {
        return normalizeImageUrl(node.currentSrc || node.src || '', 'src');
    }
    if (node.tagName === 'A' || hasHref(node)) {
        return normalizeImageUrl(node.href || safeGetAttribute(node, 'href') || '', 'href');
    }
    const backgroundImage = node.style && node.style.backgroundImage;
    if (backgroundImage && backgroundImage !== 'none') {
        const match = backgroundImage.match(/url\(["']?([^"')]+)["']?\)/);
        if (match) return normalizeImageUrl(match[1], 'backgroundImage');
    }
    const nested = getImageElement(node);
    return nested ? rawImageUrl(nested) : '';
}

function imageCandidateGroupKey(sourceNode, imageNode, url, order) {
    const source = sourceNode && typeof sourceNode === 'object' ? sourceNode : imageNode;
    const slot = safeClosest(source, '.tsp-image-slot,[class*="tsp-image-slot"]');
    if (slot) return `slot:${nodePathKey(slot)}`;
    const metadataNode = safeClosest(source, '[data-location-hash],[data-image-id]');
    const locationHash = safeGetAttribute(source, 'data-location-hash')
        || safeGetAttribute(imageNode, 'data-location-hash')
        || safeGetAttribute(metadataNode, 'data-location-hash')
        || '';
    if (locationHash) return `loc:${locationHash}`;
    const imageId = safeGetAttribute(source, 'data-image-id')
        || safeGetAttribute(imageNode, 'data-image-id')
        || safeGetAttribute(metadataNode, 'data-image-id')
        || '';
    if (imageId) return `id:${imageId}`;
    const isChami = Boolean(safeClosest(source, '[class*="tsp-"],[data-location-hash],[data-image-id]'));
    if (isChami) return `chami-url:${url}`;
    return `node:${order}`;
}

function collectNodeMetadata(sourceNode, imageNode = null) {
    const source = sourceNode && typeof sourceNode === 'object' ? sourceNode : imageNode;
    const metadataNode = safeClosest(source, '[data-location-hash],[data-image-id],[data-slot-index],[data-image-index],[data-igs-image-slot]');
    return {
        imageId: safeGetAttribute(source, 'data-image-id')
            || safeGetAttribute(imageNode, 'data-image-id')
            || safeGetAttribute(metadataNode, 'data-image-id')
            || safeGetAttribute(source, 'data-igs-image-id')
            || safeGetAttribute(imageNode, 'data-igs-image-id')
            || safeGetAttribute(metadataNode, 'data-igs-image-id')
            || '',
        locationHash: safeGetAttribute(source, 'data-location-hash')
            || safeGetAttribute(imageNode, 'data-location-hash')
            || safeGetAttribute(metadataNode, 'data-location-hash')
            || safeGetAttribute(source, 'data-igs-location-hash')
            || safeGetAttribute(imageNode, 'data-igs-location-hash')
            || safeGetAttribute(metadataNode, 'data-igs-location-hash')
            || '',
        slotIndex: readIndexAttribute(source, imageNode, metadataNode, ['data-slot-index', 'data-image-index', 'data-igs-image-slot']),
        buttonIndex: readIndexAttribute(source, imageNode, metadataNode, ['data-button-index', 'data-image-index', 'data-slot-index', 'data-igs-image-slot']),
    };
}

function readIndexAttribute(sourceNode, imageNode, metadataNode, names) {
    for (const name of Array.isArray(names) ? names : []) {
        const value = safeGetAttribute(sourceNode, name)
            || safeGetAttribute(imageNode, name)
            || safeGetAttribute(metadataNode, name)
            || '';
        if (value === '') continue;
        const numeric = Number(value);
        if (Number.isFinite(numeric) && numeric >= 0) return Math.floor(numeric);
    }
    return null;
}

function nodePathKey(node) {
    const parts = [];
    let cursor = node;
    let depth = 0;
    while (cursor && depth < 8) {
        const parent = cursor.parentElement;
        let index = 0;
        if (parent && parent.children && parent.children.length != null) {
            index = Math.max(0, Array.prototype.indexOf.call(parent.children, cursor));
        }
        parts.unshift(`${String(cursor.tagName || 'node').toLowerCase()}:${index}`);
        if (cursor.classList && cursor.classList.contains('mes')) break;
        cursor = parent;
        depth += 1;
    }
    return parts.join('/');
}

function decodeImageUrlText(text) {
    return String(text || '')
        .trim()
        .replace(/&amp;/g, '&')
        .replace(/&quot;/g, '"')
        .replace(/&#39;/g, '\'')
        .replace(/&lt;/g, '<')
        .replace(/&gt;/g, '>');
}

function imageKeyHint(key) {
    const name = String(key || '');
    if (/(?:api|base|endpoint|model|prompt|negative|token|key|authorization)/i.test(name)) return false;
    return /(?:image|img|src|href|url|thumbnail|thumb|cover|background|bg|avatar|picture|photo)/i.test(name);
}

function normalizeAdapterSelection(value) {
    const normalized = String(value || 'auto').trim().toLowerCase();
    if (normalized === 'chatu8' || normalized === 'chami' || normalized === 'generic') return normalized;
    return normalized === 'auto' ? 'auto' : normalized;
}

function isGenericCandidateAllowed(sourceNode, imageNode, root, selector, scopePolicy = {}) {
    const source = sourceNode && typeof sourceNode === 'object' ? sourceNode : imageNode;
    const normalizedSelector = String(selector || '');
    if (String(scopePolicy && scopePolicy.kind || '') === 'legacy-global' && scopePolicy && scopePolicy.allowGlobalGeneric === false) {
        return false;
    }
    if (isDocumentLikeRoot(root)) {
        if (String(scopePolicy && scopePolicy.kind || '') !== 'message' && normalizedSelector.includes('background-image')) {
            return false;
        }
        return true;
    }
    if (isMessageTextRoot(root)) return true;
    if (isMessageRoot(root)) {
        if (normalizedSelector.includes('background-image')) {
            return Boolean(safeClosest(source, '[data-location-hash],[data-image-id],[class*="tsp-"],.mes_text'));
        }
        return isWithinMessageText(source);
    }
    return true;
}

function isDocumentLikeRoot(root) {
    return Boolean(root && typeof root === 'object' && typeof root.querySelectorAll === 'function' && (root.defaultView || root.documentElement || root.body));
}

function isMessageRoot(root) {
    if (!root || typeof root !== 'object') return false;
    const classText = [
        safeGetAttribute(root, 'class'),
        root.className,
    ].filter(Boolean).join(' ');
    return Boolean(
        safeGetAttribute(root, 'mesid')
        || safeGetAttribute(root, 'data-mesid')
        || safeGetAttribute(root, 'data-message-id')
        || /(^|\s)mes(\s|$)/.test(classText)
    );
}

function isMessageTextRoot(root) {
    if (!root || typeof root !== 'object') return false;
    const classText = [
        safeGetAttribute(root, 'class'),
        root.className,
    ].filter(Boolean).join(' ');
    return /(^|\s)mes_text(\s|$)/.test(classText);
}

function isWithinMessageText(node) {
    return Boolean(node && safeClosest(node, '.mes_text'));
}

function safeQueryAll(root, selector) {
    try {
        return root && typeof root.querySelectorAll === 'function'
            ? Array.from(root.querySelectorAll(selector))
            : [];
    } catch (error) {
        return [];
    }
}

function safeClosest(node, selector) {
    try {
        return node && typeof node.closest === 'function' ? node.closest(selector) : null;
    } catch (error) {
        return null;
    }
}

function safeGetAttribute(node, name) {
    try {
        return node && typeof node.getAttribute === 'function' ? node.getAttribute(name) : null;
    } catch (error) {
        return null;
    }
}

function isLikelyRegenerateButton(button) {
    const text = [
        safeGetAttribute(button, 'title'),
        safeGetAttribute(button, 'aria-label'),
        safeGetAttribute(button, 'data-action'),
        safeGetAttribute(button, 'data-command'),
        safeGetAttribute(button, 'data-tooltip'),
        safeGetAttribute(button, 'value'),
        safeGetAttribute(button, 'class'),
        button && button.textContent,
        button && button.innerText,
        button && button.value,
    ].filter(Boolean).join(' ');
    return /(?:重新生成|生成图片|生成图|重绘|重画|换图|再来一张|regen|regenerate|reroll|generate\s*image|image\s*gen)/i.test(text);
}

function safeDocument(target) {
    try {
        return target && target.document ? target.document : null;
    } catch (error) {
        return null;
    }
}

function resolveTopWindow(globalObject) {
    try {
        return globalObject && globalObject.top ? globalObject.top : globalObject;
    } catch (error) {
        return globalObject;
    }
}

function isImageLikeNode(node) {
    return Boolean(
        node
        && typeof node === 'object'
        && (
            node.tagName === 'IMG'
            || node.tagName === 'VIDEO'
            || node.tagName === 'A'
        ),
    );
}

function hasImageSource(node) {
    return Boolean(node && typeof node === 'object' && (node.currentSrc || node.src || safeGetAttribute(node, 'data-src')));
}

const PLACEHOLDER_GIF_PREFIX = 'data:image/gif;base64,R0lGODlhAQABAIAAA';

function isUnloadedPlaceholderImage(node) {
    if (!node || node.tagName !== 'IMG') return false;
    const src = node.currentSrc || node.src || '';
    if (src.startsWith(PLACEHOLDER_GIF_PREFIX)) return true;
    return false;
}

function hasHref(node) {
    return Boolean(node && typeof node === 'object' && (node.href || safeGetAttribute(node, 'href')));
}

function isLikelyHostDecorImage(sourceNode, imageNode) {
    const source = sourceNode && typeof sourceNode === 'object' ? sourceNode : imageNode;
    const classText = [
        safeGetAttribute(source, 'class'),
        safeGetAttribute(imageNode, 'class'),
        source && source.className,
        imageNode && imageNode.className,
    ].filter(Boolean).join(' ');
    if (/(^|\s)(avatar|mesAvatar|mes_avatar|ch_name|name|timestamp)(\s|$)/i.test(classText)) return true;
    return Boolean(safeClosest(source, '.avatar,.mesAvatar,.mes_avatar,.ch_name,.timestamp'));
}


__igsDefine(exports, "getAdapterSelectors", () => getAdapterSelectors);
__igsDefine(exports, "resolveDomRoots", () => resolveDomRoots);
__igsDefine(exports, "getGlobalDetectionRoots", () => getGlobalDetectionRoots);
__igsDefine(exports, "detectExternalImageAdapter", () => detectExternalImageAdapter);
__igsDefine(exports, "collectDomImageCandidates", () => collectDomImageCandidates);
__igsDefine(exports, "findDomRegenerateButtons", () => findDomRegenerateButtons);
__igsDefine(exports, "collectDomRegenerateButtonCandidates", () => collectDomRegenerateButtonCandidates);
__igsDefine(exports, "normalizeImageUrl", () => normalizeImageUrl);
});
__igsRegister("src/host/input-channel.js", function(module, exports, require) {
function createInputChannel(hostAdapter) {
    if (!hostAdapter || typeof hostAdapter.typeAndSend !== 'function') {
        throw new Error('IGS input channel requires a host adapter with typeAndSend(text).');
    }

    return {
        async typeAndSend(text) {
            if (typeof text !== 'string' || !text.trim()) {
                return { ok: false, reason: 'empty-input' };
            }
            return hostAdapter.typeAndSend(text);
        },
        async typeIntoInputAndSend(text) {
            return this.typeAndSend(text);
        },
    };
}

__igsDefine(exports, "createInputChannel", () => createInputChannel);
});
__igsRegister("src/scene/scene-state.js", function(module, exports, require) {
const EMPTY_SCENE_STATE = Object.freeze({
    messageId: null,
    speaker: '',
    emotion: '',
    text: '',
    textSource: '',
    formattedText: '',
    sourceKind: 'raw-text',
    formatSourceKind: 'raw-text',
    textPipelineWarnings: [],
    textPipelineErrors: [],
    time: '',
    weather: '',
    location: '',
    background: null,
    character: null,
    generatedImage: null,
    visualMode: 'background-character',
});
function createSceneState(patch = {}) {
    return Object.freeze({
        ...EMPTY_SCENE_STATE,
        ...patch,
    });
}

__igsDefine(exports, "createSceneState", () => createSceneState);
__igsDefine(exports, "EMPTY_SCENE_STATE", () => EMPTY_SCENE_STATE);
});
__igsRegister("src/scene/scene-resolver.js", function(module, exports, require) {
const { createSceneState } = require("src/scene/scene-state.js");
const { matchBackgroundRule } = require("src/scene/background-rules.js");
const { matchCharacterRule } = require("src/scene/character-rules.js");
function resolveScene(context = {}) {
    const scene = createSceneState({
        ...context.previousScene,
        ...context.shujukuScene,
        ...context.textScene,
    });

    return createSceneState({
        ...scene,
        background: context.background || matchBackgroundRule(scene, context.backgroundRules || []),
        character: context.character || matchCharacterRule(scene, context.characterRules || []),
    });
}

__igsDefine(exports, "resolveScene", () => resolveScene);
});
__igsRegister("src/scene/background-rules.js", function(module, exports, require) {
function matchBackgroundRule(scene, rules = []) {
    const candidates = rules
        .filter((rule) => matchesRule(scene, rule.match || {}))
        .sort((a, b) => (b.priority || 0) - (a.priority || 0));

    return candidates[0] || null;
}

function matchesRule(scene, match) {
    return ['location', 'time', 'weather'].every((key) => {
        if (!match[key] || !match[key].length) return true;
        return match[key].includes(scene[key]);
    });
}

__igsDefine(exports, "matchBackgroundRule", () => matchBackgroundRule);
});
__igsRegister("src/scene/character-rules.js", function(module, exports, require) {
function matchCharacterRule(scene, rules = []) {
    const speaker = scene.speaker || '';
    const emotion = scene.emotion || '';

    return (
        rules.find((rule) => rule.character === speaker && rule.emotion === emotion) ||
        rules.find((rule) => rule.character === speaker && rule.emotion === 'default') ||
        rules.find((rule) => (rule.aliases || []).includes(speaker) && rule.emotion === emotion) ||
        rules.find((rule) => rule.character === 'default') ||
        null
    );
}

__igsDefine(exports, "matchCharacterRule", () => matchCharacterRule);
});
__igsRegister("src/visual/layer-controller.js", function(module, exports, require) {
function createLayerController(layers) {
    return {
        render(stage) {
            const renderedLayers = [];
            const layerResults = {};

            for (const [name, layer] of Object.entries(layers || {})) {
                if (layer && typeof layer.render === 'function') {
                    renderedLayers.push(name);
                    layerResults[name] = layer.render(stage, stage && stage.scene);
                }
            }

            return { ok: true, renderedLayers, layerResults };
        },
    };
}

__igsDefine(exports, "createLayerController", () => createLayerController);
});
__igsRegister("src/visual/stage-renderer.js", function(module, exports, require) {
const { createReaderState } = require("src/visual/reader-state.js");
const { createStageModel } = require("src/visual/stage-model.js");
function createStageRenderer(layerController) {
    if (!layerController || typeof layerController.render !== 'function') {
        throw new Error('IGS stage renderer requires a layer controller.');
    }

    return {
        render(scene, options = {}) {
            const readerState = createReaderState(options);
            const stage = createStageModel(scene, readerState, options);
            const layerResult = layerController.render(stage);

            return {
                ok: layerResult.ok !== false,
                stage,
                renderedLayers: layerResult.renderedLayers || [],
                layerResults: layerResult.layerResults || {},
            };
        },
    };
}

__igsDefine(exports, "createStageRenderer", () => createStageRenderer);
});
__igsRegister("src/visual/reader-state.js", function(module, exports, require) {
const { resolveLegacyReaderMode } = require("src/storage/legacy-igs.js");
const { getResponsiveLayout } = require("src/visual/responsive-layout.js");
const { PUBLIC_READER_MODES } = require("src/schemas/reader-mode.js");
const READER_MODES = PUBLIC_READER_MODES;
const TOOLBAR_LAYOUTS = Object.freeze(['horizontal', 'vertical']);
const TOOLBAR_PLACEMENTS = Object.freeze(['top-left', 'top-right', 'top', 'bottom', 'bottom-right', 'custom']);
const DIALOGUE_STYLES = Object.freeze(['panel', 'subtitle', 'bubble']);
function createReaderState(input = {}) {
    const legacy = getLegacySettings(input.legacyIgs);
    const mode = resolveReaderMode(input, legacy);
    const isMobile = resolveIsMobile(input, mode);
    const viewport = normalizeViewport(input.viewport);
    const layout = getResponsiveLayout(viewport, { mode, isMobile });
    const mergedSettings = mergeSettings(
        legacy.readerSettingsByMode && legacy.readerSettingsByMode[mode],
        legacy.readerSettings,
        // default 桶为权威，放在旧 mode 桶之后覆盖它（全模式共用一套设置）。
        legacy.readerSettingsByMode && legacy.readerSettingsByMode['default'],
        input.visualSettings,
        input.layoutSettings,
        input.readerSettings,
    );
    const toolbarLayout = normalizeToolbarLayout(
        firstDefined(mergedSettings.toolbarLayout, mergedSettings.toolbarDirection, mergedSettings.toolbarMode),
        isMobile,
    );
    const toolbarPlacement = normalizeToolbarPlacement(
        firstDefined(mergedSettings.toolbarPlacement, mergedSettings.toolbarPosition, mergedSettings.toolbarSlot),
    );
    const dialogueStyle = normalizeDialogueStyle(
        firstDefined(mergedSettings.dialogueStyle, mergedSettings.dialogStyle, mergedSettings.panelStyle),
    );
    const nameplateVisible = normalizeBoolean(
        firstDefined(mergedSettings.nameplateVisible, mergedSettings.showNameplate),
        true,
    );
    const avatarVisible = normalizeBoolean(
        firstDefined(mergedSettings.avatarVisible, mergedSettings.showAvatar),
        false,
    );
    const cssVars = {
        '--igs-dialogue-font-size': normalizePixelValue(
            firstDefined(mergedSettings.dialogueFontSize, mergedSettings.fontSize),
            defaultFontSize(layout),
        ),
        '--igs-dialogue-width': normalizeSizeValue(
            firstDefined(mergedSettings.dialogueWidth, mergedSettings.dialogWidth),
            defaultDialogueWidth(layout),
        ),
        '--igs-dialogue-height': normalizeSizeValue(
            firstDefined(mergedSettings.dialogueHeight, mergedSettings.dialogHeight),
            'auto',
        ),
        '--igs-dialogue-opacity': normalizeOpacity(
            firstDefined(mergedSettings.dialogueOpacity, mergedSettings.dialogOpacity),
            0.72,
        ),
        '--igs-toolbar-placement-x': normalizePixelValue(
            firstDefined(mergedSettings.toolbarPlacementX, mergedSettings.toolbarX, mergedSettings.positionX),
            16,
        ),
        '--igs-toolbar-placement-y': normalizePixelValue(
            firstDefined(mergedSettings.toolbarPlacementY, mergedSettings.toolbarY, mergedSettings.positionY),
            16,
        ),
    };
    const attributes = {
        'data-igs-toolbar-layout': toolbarLayout,
        'data-igs-toolbar-placement': toolbarPlacement,
        'data-igs-dialogue-style': dialogueStyle,
        'data-igs-nameplate-visible': String(nameplateVisible),
        'data-igs-avatar-visible': String(avatarVisible),
    };

    return Object.freeze({
        mode,
        layout,
        isMobile,
        viewport,
        toolbarLayout,
        toolbarPlacement,
        dialogueStyle,
        nameplateVisible,
        avatarVisible,
        cssVars,
        attributes,
        datasetKeys: Object.keys(attributes),
        settings: mergedSettings,
    });
}

function getLegacySettings(legacy) {
    if (!legacy || legacy.ok === false) {
        return {
            readerMode: 'pc',
            displayMode: '',
            bridge: {},
            readerSettings: {},
            readerSettingsByMode: {},
        };
    }
    return legacy;
}

function resolveReaderMode(input, legacy) {
    const requestedMode = firstDefined(
        input.mode,
        input.viewerMode,
        input.readerMode,
        input.readerSettings && input.readerSettings.mode,
        input.layoutSettings && input.layoutSettings.mode,
        input.visualSettings && input.visualSettings.mode,
    );
    const resolved = resolveLegacyReaderMode(requestedMode, legacy.displayMode, legacy.bridge);
    return READER_MODES.includes(resolved) ? resolved : 'pc';
}

function resolveIsMobile(input, mode) {
    if (typeof input.isMobile === 'boolean') return input.isMobile;
    return mode === 'mobile';
}

function normalizeViewport(viewport) {
    const width = Number(viewport && viewport.width);
    const height = Number(viewport && viewport.height);

    return Object.freeze({
        width: Number.isFinite(width) && width > 0 ? width : 0,
        height: Number.isFinite(height) && height > 0 ? height : 0,
    });
}

function mergeSettings(...sources) {
    const merged = {};
    for (const source of sources) {
        if (!source || typeof source !== 'object' || Array.isArray(source)) continue;
        Object.assign(merged, cloneData(source));
    }
    return Object.freeze(merged);
}

function normalizeToolbarLayout(value, isMobile) {
    const normalized = typeof value === 'string' ? value.trim().toLowerCase() : '';
    if (TOOLBAR_LAYOUTS.includes(normalized)) return normalized;
    if (normalized === 'auto') return isMobile ? 'vertical' : 'horizontal';
    return isMobile ? 'vertical' : 'horizontal';
}

function normalizeToolbarPlacement(value) {
    const normalized = typeof value === 'string' ? value.trim().toLowerCase() : '';
    return TOOLBAR_PLACEMENTS.includes(normalized) ? normalized : 'top-right';
}

function normalizeDialogueStyle(value) {
    const normalized = typeof value === 'string' ? value.trim().toLowerCase() : '';
    if (normalized === 'box') return 'panel';
    return DIALOGUE_STYLES.includes(normalized) ? normalized : 'panel';
}

function normalizeBoolean(value, fallback) {
    if (typeof value === 'boolean') return value;
    if (typeof value === 'number') return value !== 0;
    if (typeof value === 'string') {
        const normalized = value.trim().toLowerCase();
        if (normalized === 'true' || normalized === '1' || normalized === 'yes' || normalized === 'on') return true;
        if (normalized === 'false' || normalized === '0' || normalized === 'no' || normalized === 'off') return false;
    }
    return fallback;
}

function normalizePixelValue(value, fallback) {
    if (typeof value === 'number' && Number.isFinite(value)) return `${value}px`;
    if (typeof value === 'string') {
        const normalized = value.trim();
        if (!normalized) return `${fallback}px`;
        if (/^-?\d+(\.\d+)?$/.test(normalized)) return `${normalized}px`;
        return normalized;
    }
    return `${fallback}px`;
}

function normalizeSizeValue(value, fallback) {
    if (typeof value === 'number' && Number.isFinite(value)) return `${value}px`;
    if (typeof value === 'string') {
        const normalized = value.trim();
        if (!normalized) return fallback;
        if (/^-?\d+(\.\d+)?$/.test(normalized)) return `${normalized}px`;
        return normalized;
    }
    return fallback;
}

function normalizeOpacity(value, fallback) {
    const normalized = Number(value);
    if (!Number.isFinite(normalized)) return String(fallback);
    return String(Math.min(1, Math.max(0, normalized)));
}

function defaultFontSize(layout) {
    if (layout === 'mobile-landscape') return 15;
    if (layout === 'mobile-portrait') return 16;
    return 18;
}

function defaultDialogueWidth(layout) {
    if (layout === 'mobile-portrait') return 'calc(100vw - 24px)';
    return 'min(920px, calc(100vw - 32px))';
}

function firstDefined(...values) {
    for (const value of values) {
        if (value !== undefined && value !== null) return value;
    }
    return undefined;
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

__igsDefine(exports, "createReaderState", () => createReaderState);
});
__igsRegister("src/visual/responsive-layout.js", function(module, exports, require) {
function getResponsiveLayout(viewport, settings = {}) {
    const width = viewport && viewport.width ? viewport.width : 0;
    const height = viewport && viewport.height ? viewport.height : 0;
    const isLandscape = width > height;
    const mode = settings.mode || 'pc';

    if ((mode === 'web' || mode === 'fullscreen') && settings.isMobile && isLandscape) {
        return 'mobile-landscape';
    }

    if (settings.isMobile) return 'mobile-portrait';
    return 'desktop';
}

__igsDefine(exports, "getResponsiveLayout", () => getResponsiveLayout);
});
__igsRegister("src/visual/stage-model.js", function(module, exports, require) {
const STAGE_SLOTS = Object.freeze([
    '.igs-stage',
    '.igs-background-layer',
    '.igs-generated-layer',
    '.igs-effect-layer',
    '.igs-character-layer',
    '.igs-avatar-layer',
    '.igs-dialogue-layer',
    '.igs-hud-layer',
    '.igs-toolbar',
    '.igs-choice-layer',
    '.igs-system-layer',
]);
function createStageModel(scene = {}, readerState, options = {}) {
    const safeScene = cloneData(scene || {});
    const safeReaderState = cloneData(readerState || {});
    const visualMode = safeScene.visualMode || 'background-character';
    const hasGenerated = Boolean(safeScene.generatedImage);
    const showDialogue = visualMode !== 'off';
    const showGenerated = shouldShowGenerated(visualMode, hasGenerated);
    const showBackground = shouldShowBackground(visualMode, hasGenerated, safeScene.background);
    const showCharacter = shouldShowCharacter(visualMode, hasGenerated, safeScene.character);
    const showAvatar = Boolean(safeReaderState.avatarVisible) && Boolean(safeScene.speaker) && visualMode !== 'off';
    const attributes = cloneData(safeReaderState.attributes || {});
    const cssVars = cloneData(safeReaderState.cssVars || {});

    return Object.freeze({
        type: 'igs-stage-model',
        layout: safeReaderState.layout || 'desktop',
        visualMode,
        scene: safeScene,
        readerState: safeReaderState,
        slots: STAGE_SLOTS.slice(),
        attributes,
        datasetKeys: Object.keys(attributes),
        cssVars,
        layers: {
            background: {
                slot: '.igs-background-layer',
                visible: showBackground,
                resource: cloneData(safeScene.background) || null,
            },
            generated: {
                slot: '.igs-generated-layer',
                visible: showGenerated,
                resource: cloneData(safeScene.generatedImage) || null,
            },
            effect: {
                slot: '.igs-effect-layer',
                visible: Boolean(safeScene.weather || safeScene.time || safeScene.location),
                weather: safeScene.weather || '',
                time: safeScene.time || '',
                location: safeScene.location || '',
            },
            character: {
                slot: '.igs-character-layer',
                visible: showCharacter,
                character: cloneData(safeScene.character) || null,
            },
            avatar: {
                slot: '.igs-avatar-layer',
                visible: showAvatar,
                speaker: safeScene.speaker || '',
                emotion: safeScene.emotion || '',
            },
            dialogue: {
                slot: '.igs-dialogue-layer',
                visible: showDialogue,
                style: safeReaderState.dialogueStyle || 'panel',
                speaker: safeScene.speaker || '',
                text: safeScene.text || '',
                nameplateVisible: Boolean(safeReaderState.nameplateVisible),
                avatarVisible: Boolean(safeReaderState.avatarVisible),
            },
            hud: {
                slot: '.igs-hud-layer',
                visible: showDialogue,
                toolbar: {
                    slot: '.igs-toolbar',
                    visible: showDialogue,
                    layout: safeReaderState.toolbarLayout || 'horizontal',
                    placement: safeReaderState.toolbarPlacement || 'top-right',
                    attributes: {
                        'data-placement': safeReaderState.toolbarPlacement || 'top-right',
                    },
                },
            },
            choice: {
                slot: '.igs-choice-layer',
                visible: Boolean(options.choiceState && options.choiceState.visible),
                items: cloneData(options.choiceState && options.choiceState.items) || [],
            },
            system: {
                slot: '.igs-system-layer',
                visible: true,
                messages: cloneData(options.systemMessages) || [],
            },
        },
    });
}

function shouldShowGenerated(visualMode, hasGenerated) {
    if (!hasGenerated) return false;
    return [
        'generated-only',
        'generated-first',
        'generated-with-avatar',
        'mixed-overlay',
    ].includes(visualMode);
}

function shouldShowBackground(visualMode, hasGenerated, background) {
    if (!background) return visualMode === 'default-background';
    if (visualMode === 'off' || visualMode === 'text-only' || visualMode === 'generated-only') return false;
    if (visualMode === 'generated-first' || visualMode === 'generated-with-avatar') return !hasGenerated;
    return true;
}

function shouldShowCharacter(visualMode, hasGenerated, character) {
    if (!character) return false;
    if (visualMode === 'off' || visualMode === 'text-only' || visualMode === 'generated-only') return false;
    if (visualMode === 'generated-first' || visualMode === 'generated-with-avatar') return !hasGenerated;
    return true;
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

__igsDefine(exports, "createStageModel", () => createStageModel);
});
__igsRegister("src/visual/visual-mode.js", function(module, exports, require) {
const VISUAL_MODES = Object.freeze({
    OFF: 'off',
    TEXT_ONLY: 'text-only',
    DEFAULT_BACKGROUND: 'default-background',
    BACKGROUND_CHARACTER: 'background-character',
    GENERATED_ONLY: 'generated-only',
    GENERATED_FIRST: 'generated-first',
    GENERATED_WITH_AVATAR: 'generated-with-avatar',
    MIXED_OVERLAY: 'mixed-overlay',
});
function resolveVisualMode(scene, settings = {}) {
    if (settings.visualMode) return settings.visualMode;
    if (scene.generatedImage) return VISUAL_MODES.GENERATED_FIRST;
    return VISUAL_MODES.BACKGROUND_CHARACTER;
}

__igsDefine(exports, "resolveVisualMode", () => resolveVisualMode);
__igsDefine(exports, "VISUAL_MODES", () => VISUAL_MODES);
});
__igsRegister("src/visual/igs-ui/reader-host-constants.js", function(module, exports, require) {
const DEFAULT_IMAGE_API = Object.freeze({
    mode: 'extension',
    externalAdapter: 'auto',
    endpoint: '',
    apiKey: '',
    model: '',
    size: '832x1216',
    steps: 28,
    sampler: 'k_euler_ancestral',
    requestTimeoutMs: 30000,
    pollIntervalMs: 2000,
    pollAttempts: 60,
    promptPrefix: '',
    availableModels: [],
    modelsFetchedAt: '',
});
const VN_THEME_PRESETS = Object.freeze({
    genshin: Object.freeze({
        nameAlign: 'center',
        textAlign: 'left',
        narrationAlign: 'left',
        thoughtAlign: 'left',
        dividerSymbol: '───◇───',
        nameFont: 'inherit',
        textFont: 'inherit',
        thoughtFont: 'inherit',
        narrationFont: 'inherit',
        nameColor: '#ffeeb8',
        textColor: '#f4f4f6',
        thoughtColor: '#c8c8dc',
        narrationColor: '#f4f4f6',
        dividerColor: '#ffeeb8',
    }),
    honkai: Object.freeze({
        nameAlign: 'center',
        textAlign: 'left',
        narrationAlign: 'left',
        thoughtAlign: 'left',
        dividerSymbol: '──✦──',
        nameFont: 'inherit',
        textFont: 'inherit',
        thoughtFont: 'inherit',
        narrationFont: 'inherit',
        nameColor: '#c8e0ff',
        textColor: '#e8ecf4',
        thoughtColor: '#a0beff',
        narrationColor: '#e8ecf4',
        dividerColor: '#c8e0ff',
    }),
    minimal: Object.freeze({
        nameAlign: 'left',
        textAlign: 'left',
        narrationAlign: 'left',
        thoughtAlign: 'left',
        dividerSymbol: 'none',
        nameFont: 'inherit',
        textFont: 'inherit',
        thoughtFont: 'inherit',
        narrationFont: 'inherit',
        nameColor: '#b3b3b3',
        textColor: '#f4f4f6',
        thoughtColor: '#808080',
        narrationColor: '#f4f4f6',
        dividerColor: '#404040',
    }),
});
const READER_REQUIRED_SETTINGS_PATHS = Object.freeze([
    'readerSettings.fontSize',
    'readerSettings.dialogSkin',
    'readerSettings.classicDialogWidthPercent',
    'readerSettings.optionFontSize',
    'readerSettings.dialogWidth',
    'readerSettings.dialogHeight',
    'readerSettings.glassOpacity',
    'readerSettings.glassBackdropFilter',
    'readerSettings.imageCountOverride',
    'readerSettings.inputScale',
    'readerSettings.toolbarScale',
    'readerSettings.toolbarDock',
    'readerSettings.imgMode',
    'readerSettings.imgBrightness',
    'readerSettings.showStatusLine',
    'readerSettings.pinnedBtns',
    'readerSettings.hiddenBtns',
    'readerSettings.btnOrder',
    'readerSettings.spriteLayouts',
    'readerSettings.vnTheme.preset',
    'readerSettings.classicVnTheme.preset',
]);
const SETTINGS_PANEL_REQUIRED_SELECTORS = Object.freeze([
    '#igs-unified-settings',
    '.igs-settings-shell',
    '.igs-settings-head',
    '.igs-settings-theme-toggle',
    '.igs-settings-tabs',
    '.igs-settings-body',
    '.igs-segmented',
    '.igs-scene-settings-subtabs',
    '.igs-reader-subtabs',
    '.igs-source-filter',
    '.igs-settings-preview',
]);
const SETTINGS_PANEL_TAB_CONTRACT = Object.freeze({
    basic: Object.freeze({
        label: '基础',
        requiredPaths: Object.freeze([
            'bridge.openMode',
            'bridge.showToasts',
            'bridge.settingsTheme',
        ]),
    }),
    regex: Object.freeze({
        label: '正文替换',
        requiredPaths: Object.freeze([
            'bridge.sourceFilter.enabled',
            'bridge.sourceFilter.textIncludeTags',
            'bridge.sourceFilter.textExcludeTags',
            'bridge.sourceFilter.imageIncludeTags',
            'bridge.virtualRegex.enabled',
            'bridge.virtualRegex.pattern',
            'bridge.virtualRegex.flags',
            'bridge.virtualRegex.replacement',
        ]),
        requiredActions: Object.freeze([
            'reset-virtual-regex',
            'test-virtual-regex',
        ]),
    }),
    image: Object.freeze({
        label: '图像',
        requiredPaths: Object.freeze([
            'bridge.imageApi.mode',
            'bridge.imageApi.externalAdapter',
            'bridge.imageApi.endpoint',
            'bridge.imageApi.apiKey',
            'bridge.imageApi.model',
            'bridge.imageApi.size',
            'bridge.imageApi.steps',
            'bridge.imageApi.sampler',
            'bridge.imageApi.requestTimeoutMs',
            'bridge.imageApi.pollIntervalMs',
            'bridge.imageApi.pollAttempts',
            'bridge.imageApi.promptPrefix',
        ]),
        requiredActions: Object.freeze([
            'fetch-image-models',
            'test-image',
        ]),
    }),
    scene: Object.freeze({
        label: '场景',
        requiredPaths: Object.freeze([
            'bridge.sceneAssets.enabled',
            'bridge.sceneAssets.promptRule',
        ]),
        requiredActions: Object.freeze([
            'reset-prompt-rule',
            'save-prompt-rule',
        ]),
    }),
    reader: Object.freeze({
        label: '阅读器',
        requiredPaths: READER_REQUIRED_SETTINGS_PATHS,
    }),
});
const TOOLBAR_ACTIONS = Object.freeze([
    ['db-panel', '数据库'],
    ['prev-turn', '上一轮'],
    ['first-page', '第一页'],
    ['prev', '上一页'],
    ['next', '下一页'],
    ['last-page', '最后一页'],
    ['next-turn', '下一轮'],
    ['regen', '重新生图'],
    ['save', '保存图片'],
    ['hide', '隐藏对话框'],
    ['sprite-edit', '调整立绘'],
    ['rescan', '刷新'],
    ['settings', '设置'],
]);
const DEFAULT_PINNED_TOOLBAR_BUTTONS = Object.freeze([]);
const READER_SETTINGS_SCHEMA_VERSION = '0.5.4';
const INITIAL_IMAGE_POLL_ATTEMPTS = 8;
const INITIAL_IMAGE_POLL_INTERVAL_MS = 250;
const DEFAULT_SCENE_PROMPT_RULE = `[igs标签语法]
以下标签供前端渲染系统读取，是附加在正文上的元数据注释，不改变正文本身的写法。

[igs-scene:场景名|时间|天气]
[igs-scene:场景名|时间|天气|NSFW]（仅NSFW场景使用）
[igs-char:角色名|表情|对白]
[igs-thought:角色名|表情|心里话]

语法要求：
1. 每条标签独立成行，头尾用方括号包裹
2. 字段之间用 | 分隔
3. [igs-scene] 在本轮场景首次出现、以及任何场景切换时各输出一次；即使与上一轮场景相同，新一轮开头也要重新输出一次
4. 场景属于NSFW内容时，[igs-scene]第四栏必须填写大写NSFW；其他场景保持三栏，禁止输出第四栏
5. [igs-char] 在角色开口时使用
6. [igs-thought] 在需要表现角色内心独白时使用
7. 角色名必须输出完整全名
8. 场景名必须定位到空间概念（如教室、走廊），禁止描述家具
9. 不知名角色用「？？？」；路人用「男路人A」「女同学B」等
10. 禁止发明新标签

[表情词约束]
表情字段从固定池选取（2-3字词），禁止自造：
{{mood_groups}}

[时间字段约束]
仅使用笼统时间段：早晨/上午/中午/下午/傍晚/晚上/深夜
{{time_groups}}

[天气字段约束]
仅使用天气类型词：晴天/多云/小雨/大雨/雷雨/小雪/大雪等
{{weather_groups}}

[场景字段约束]
仅定位空间概念，禁止定位家具摆设。
{{scene_groups}}

[核心原则]
igs标签是透明的元数据层。正文的文风、叙事密度、修辞手法、段落节奏完全由其他文风指令决定，不受标签存在的影响。标签插在段落之间，读者略去所有标签后，剩余正文应当是一篇完整的、符合当前文风要求的文章。其中，表情字段为角色可外在观察的神态表情，禁止理解成语气或说话方式。`.trim();
const LEGACY_DEFAULT_SCENE_PROMPT_RULE = `[igs标签语法]
以下三种标签供前端渲染系统读取，是附加在正文上的元数据注释，不改变正文本身的写法。

[igs-scene:场景名|时间|天气]
[igs-char:角色名|情绪|对白]
[igs-thought:角色名|情绪|心里话]

语法要求：
1. 每条标签独占一行，方括号为固定边界，不可拆行
2. 字段之间用 | 分隔，字段内不得含 | 或 ]
3. [igs-scene] 在场景首次出现和换场景时各出现一次
4. [igs-char] 在角色开口时使用
5. [igs-thought] 在需要表现角色内心声音时使用
6. 角色名必须输出完整全名，每次一致（立绘索引标识）
7. 场景名必须定位到空间概念（如教室、走廊），每次一致（背景图索引标识）
8. 不知名角色用「？？？」；路人用「男路人A」「女同学B」等
9. 仅有以上三种标签，不要发明新标签

[情绪词约束]
情绪字段从固定池选取（2-3字词），仅用于前端索引立绘，禁止自造：
{{mood_groups}}

[时间字段约束]
仅使用笼统时间段：早晨/上午/中午/下午/傍晚/晚上/深夜
{{time_groups}}

[天气字段约束]
仅使用天气类型词：晴天/多云/小雨/大雨/雷雨/小雪/大雪等
{{weather_groups}}

[场景字段约束]
仅定位空间概念，禁止定位家具摆设。
{{scene_groups}}

[核心原则]
igs标签是透明的元数据层。正文的文风、叙事密度、修辞手法、段落节奏完全由其他文风指令决定，不受标签存在的影响。标签插在段落之间，如同脚注——读者略去所有标签后，剩余正文应当是一篇完整的、符合当前文风要求的文章。情绪字段是机械索引值，不替代也不影响正文中的情感表达。`.trim();
function normalizeScenePromptRule(value) {
    const rule = String(value || '');
    return !rule || rule === LEGACY_DEFAULT_SCENE_PROMPT_RULE
        ? DEFAULT_SCENE_PROMPT_RULE
        : rule;
}

__igsDefine(exports, "normalizeScenePromptRule", () => normalizeScenePromptRule);
__igsDefine(exports, "DEFAULT_IMAGE_API", () => DEFAULT_IMAGE_API);
__igsDefine(exports, "VN_THEME_PRESETS", () => VN_THEME_PRESETS);
__igsDefine(exports, "READER_REQUIRED_SETTINGS_PATHS", () => READER_REQUIRED_SETTINGS_PATHS);
__igsDefine(exports, "SETTINGS_PANEL_REQUIRED_SELECTORS", () => SETTINGS_PANEL_REQUIRED_SELECTORS);
__igsDefine(exports, "SETTINGS_PANEL_TAB_CONTRACT", () => SETTINGS_PANEL_TAB_CONTRACT);
__igsDefine(exports, "TOOLBAR_ACTIONS", () => TOOLBAR_ACTIONS);
__igsDefine(exports, "DEFAULT_PINNED_TOOLBAR_BUTTONS", () => DEFAULT_PINNED_TOOLBAR_BUTTONS);
__igsDefine(exports, "READER_SETTINGS_SCHEMA_VERSION", () => READER_SETTINGS_SCHEMA_VERSION);
__igsDefine(exports, "INITIAL_IMAGE_POLL_ATTEMPTS", () => INITIAL_IMAGE_POLL_ATTEMPTS);
__igsDefine(exports, "INITIAL_IMAGE_POLL_INTERVAL_MS", () => INITIAL_IMAGE_POLL_INTERVAL_MS);
__igsDefine(exports, "DEFAULT_SCENE_PROMPT_RULE", () => DEFAULT_SCENE_PROMPT_RULE);
__igsDefine(exports, "LEGACY_DEFAULT_SCENE_PROMPT_RULE", () => LEGACY_DEFAULT_SCENE_PROMPT_RULE);
});
__igsRegister("src/visual/igs-ui/reader-host.js", function(module, exports, require) {
const { buildIgsTextPayload, getMessagePrimaryText, getVisibleMessageTextFromElement, normalizeSourceFilter, normalizeVirtualRegex } = require("src/scene/message-source.js");
const { resolveSceneStateAtIndex, resolveSceneAtSourceOffset, resolveNearestCharacterBefore, lookupSceneAssetUrls } = require("src/scene/scene-directives.js");
const { resolveCharacterKey } = require("src/scene/scene-directives.js");
const { normalizeMoodGroups } = require("src/scene/mood-groups.js");
const { getOriginalReaderHtml, getOriginalReaderSource, getOriginalReaderStyleText, ORIGINAL_READER_REQUIRED_SELECTORS, ORIGINAL_READER_STYLE_CONTRACT } = require("src/visual/igs-ui/original-reader-source.js");
const { getSettingsShellTemplate } = require("src/visual/igs-ui/settings-shell.js");
const { getSettingsStyleText } = require("src/visual/igs-ui/settings-style.js");
const { getSceneSettingsSubTabTemplate, getReaderSubTabTemplate, getSettingsTabTemplate, normalizeSceneSettingsSubTab, normalizeReaderSubTab, SCENE_SETTINGS_SUBTAB_DEFS, READER_SUBTAB_DEFS, SETTINGS_TAB_DEFS } = require("src/visual/igs-ui/settings-tabs.js");
const { getReaderModeIcon, getSettingsThemeIcon } = require("src/visual/igs-ui/icons.js");
const { DEFAULT_IMAGE_API, DEFAULT_PINNED_TOOLBAR_BUTTONS, DEFAULT_SCENE_PROMPT_RULE, normalizeScenePromptRule, READER_SETTINGS_SCHEMA_VERSION, SETTINGS_PANEL_REQUIRED_SELECTORS, SETTINGS_PANEL_TAB_CONTRACT, TOOLBAR_ACTIONS, VN_THEME_PRESETS } = require("src/visual/igs-ui/reader-host-constants.js");
const { PUBLIC_READER_MODES, getReaderModeLabel, isEmbeddedReaderMode } = require("src/schemas/reader-mode.js");
const { cloneData, esc, firstDefined, firstNonEmptyString, firstRenderableText, clampNumber, normalizeBoolean, normalizeFiniteIndex, normalizeFiniteNumber, normalizeNullableNumber, normalizeOpacity, toHex } = require("src/visual/igs-ui/reader-value-utils.js");
const { checkbox, colorInput, field, renderCharacterAssetList, renderPinnedButtons, renderSceneAssetList, renderScenePresetBar, renderTemplate, secretInput, segmentedInput, selectInput, textInput, textareaInput, numberInput, disabledAttr, modelPicker, tableMultiSelect } = require("src/visual/igs-ui/settings-fields.js");
const { ensureEmbeddedHost, findEmbeddedHost, hideEmbeddedSourceText, resolveEmbeddedHostParent, restoreEmbeddedSourceText } = require("src/visual/igs-ui/embedded-reader-runtime.js");
const { buildReaderSourceSignature, createReaderSourceCache } = require("src/visual/igs-ui/reader-source-cache.js");
const { createImageResourceCache } = require("src/media/resource-cache.js");
const { createChatStreamObserver } = require("src/host/chat-stream-observer.js");
const { applyImageCountOverride, buildImageActionContext, buildProgressText, countBoundImageSlots, normalizePollAttempts, normalizePollInterval, normalizeSnapshotImageState, resolveSegmentImageIndex, shouldPollReaderImages, waitForReaderImagePoll } = require("src/visual/igs-ui/reader-image-state.js");
const { attachSettingsViewportEvents, clearChildren, detachSettingsViewportEvents, ensureImageLoadingSpinner, ensureStyleTag, getOwnerWindow, getRootDocument, removeImageLoadingSpinner, syncSettingsViewportVars, unmountNode } = require("src/visual/igs-ui/reader-dom-utils.js");
const { buildTextSegments, getPath, normalizeBtnOrder, normalizeHiddenButtons, normalizePinnedButtons, normalizeReaderMode, normalizeSettingsTab, normalizeSettingsValue, normalizeSpriteLayouts, setPath } = require("src/visual/igs-ui/settings-normalize.js");
const { clearReaderModeRuntime, exitDocumentFullscreen } = require("src/visual/igs-ui/reader-runtime.js");
const { enterSpriteEditMode } = require("src/visual/igs-ui/sprite-edit.js");
const { createDbPanelController } = require("src/shujuku-panel/panel-controller.js");
const { createShujukuClient } = require("src/data/shujuku/client.js");
const { buildStatusHudModel, listStatusHudTables, normalizeStatusAvatars, normalizeStatusHudSettings, resolveStatusAvatar } = require("src/data/shujuku/status-hud-model.js");
const { readOptionItems } = require("src/choices/option-table.js");
const { handleSettingsAction: runSettingsAction } = require("src/visual/igs-ui/settings-actions.js");
const { loadScenePresets } = require("src/scene/scene-preset-store.js");
const { LEGACY_READER_MODES } = require("src/storage/legacy-igs.js");
const { CLASSIC_DIALOG_HEIGHT, CLASSIC_DIALOG_WIDTH_PERCENT_DEFAULT, CLASSIC_DIALOG_THEME_DEFAULTS, DIALOG_SKIN_WESTERN_CLASSIC, normalizeClassicDialogWidthPercent, normalizeDialogSkin } = require("src/visual/igs-ui/classic-dialog-skin.js");
const { applyReaderSnapshotToDom, applyToolbarState, buildFallbackReaderOverlay, buildFallbackSettingsOverlay, normalizeReaderStableLayers } = require("src/visual/igs-ui/reader-dom-render.js");
function createIgsReaderHost(options = {}) {
    let statusHudClient = null;
    let statusHudCallback = null;
    const state = {
        activeReader: null,
        activeSettings: null,
    };
    const sourceCache = createReaderSourceCache({
        parse: (input) => buildIgsTextPayload(input.liveMessage, input.parseOptions),
    });
    const imageResourceCache = createImageResourceCache(options.global || globalThis);
    const streamObserver = createChatStreamObserver({
        global: options.global || globalThis,
        getDocument: () => resolveEmbeddedDocument(state.activeReader),
        onActivity: () => handleChatStreamActivity(),
        onStable: () => handleChatStreamStable(),
        onTimeout: async () => {
            const completed = await handleChatStreamStable();
            if (completed === false) exitEmbeddedLoading();
        },
    });

    const host = {
        openReader,
        replaceReader,
        openSettings,
        closeReader,
        closeSettings,
        getState,
        destroy,
        getReaderSnapshotContract() {
            return {
                selectors: Array.from(ORIGINAL_READER_REQUIRED_SELECTORS),
                styleContract: { ...ORIGINAL_READER_STYLE_CONTRACT },
            };
        },
        getSettingsSnapshotContract() {
            return {
                selectors: Array.from(SETTINGS_PANEL_REQUIRED_SELECTORS),
                tabs: SETTINGS_TAB_DEFS.map(([id, label]) => ({
                    id,
                    label,
                    requiredPaths: Array.from((SETTINGS_PANEL_TAB_CONTRACT[id] || {}).requiredPaths || []),
                    requiredActions: Array.from((SETTINGS_PANEL_TAB_CONTRACT[id] || {}).requiredActions || []),
                })),
            };
        },
    };

    return host;

    function openReader(payload = {}, openOptions = {}) {
        closeReader({ keepFullscreen: true });
        const nextMode = normalizeReaderMode(
            firstDefined(
                openOptions.mode,
                payload.mode,
                payload.viewerMode,
                payload.readerMode,
            ),
            resolveBridgeConfigSnapshot({ mode: openOptions.mode }).bridge,
        );
        const unified = resolveBridgeConfigSnapshot({ mode: nextMode });
        const readerSettings = normalizeReaderSettings(unified.readerSettings, unified.bridge.vnTheme);
        readerSettings._sceneAssets = unified.bridge.sceneAssets || null;
        readerSettings._sentencePaging = Boolean(unified.bridge.sentencePaging);
        readerSettings._vnTheme = readerSettings.vnTheme || null;
        const snapshot = buildReaderSnapshot(
            payload,
            nextMode,
            readerSettings,
            payload.startAtEnd === true ? Number.MAX_SAFE_INTEGER : 0,
        );
        const controller = createReaderController();
        const domState = mountReaderDom(snapshot, controller, payload);

        state.activeReader = {
            payload: cloneReaderPayload(payload),
            mode: nextMode,
            index: snapshot.content.currentIndex,
            mountMessageId: snapshot.messageId,
            contentMessageId: snapshot.messageId,
            turnOffset: 0,
            streamPhase: 'idle',
            mountBaselineRaw: getMessagePrimaryText(payload.message && (payload.message.raw || payload.message) || payload.raw || ''),
            mountBaselineVisible: String(payload.visibleText || ''),
            inputValue: '',
            hidden: false,
            dragSuppressClick: false,
            toolbarCollapsed: true,
            lastAction: '',
            toastMessage: '',
            snapshot,
            controller,
            dom: domState,
            floatingState: {
                dragged: false,
                left: null,
                top: null,
            },
            runtime: null,
            toastTimer: null,
            imagePollToken: 0,
            assetLoadRequests: new Set(),
        };
        updateMountedReader(snapshot);
        startReaderImagePolling(state.activeReader);
        syncStatusHudSubscription();
        if (isEmbeddedReaderMode(nextMode)) {
            streamObserver.start();
        } else {
            streamObserver.stop();
        }

        return {
            ok: true,
            mode: nextMode,
            readerMode: nextMode,
            snapshot: cloneData(snapshot),
            domMounted: Boolean(domState),
            controller,
        };
    }

    // 已打开阅读器时原地替换阅读源：复用同一 reader root、controller 与事件绑定，
    // 不 closeReader→openReader 重建，避免内嵌轮次切换和流式完成时反复重建 DOM。
    function replaceReader(payload = {}, replaceOptions = {}) {
        const current = state.activeReader;
        if (!current) return openReader(payload, replaceOptions);
        current.contentMessageId = payload.messageId != null ? payload.messageId : current.contentMessageId;
        if (replaceOptions.turnOffset != null) current.turnOffset = Math.max(0, Number(replaceOptions.turnOffset) || 0);
        current.payload = cloneReaderPayload(payload);
        if (current.turnOffset === 0 || payload.messageId === current.mountMessageId) {
            current.mountBaselineRaw = getMessagePrimaryText(payload.message && (payload.message.raw || payload.message) || payload.raw || '');
            current.mountBaselineVisible = String(payload.visibleText || '');
        }
        current.index = 0;
        current.inputValue = '';
        current.mountMessageId = replaceOptions.mountMessageId != null ? replaceOptions.mountMessageId : current.mountMessageId;
        const mode = normalizeReaderMode(current.mode, resolveBridgeConfigSnapshot({ mode: current.mode }).bridge);
        current.mode = mode;
        const merged = applyReaderPayloadToState(current, mode);
        updateMountedReader(merged);
        exitEmbeddedLoading();
        if (isEmbeddedReaderMode(mode) && current.turnOffset === 0) startReaderImagePolling(current);
        return {
            ok: true,
            mode,
            readerMode: mode,
            snapshot: cloneData(merged),
            domMounted: Boolean(current.dom),
            controller: current.controller,
            replaced: true,
        };
    }

    // 由当前 payload 重新构建 snapshot：正文解析走 source 缓存，普通换源不会重复整楼解析。
    function applyReaderPayloadToState(current, mode, optionsForRender = {}) {
        const unified = resolveBridgeConfigSnapshot({ mode });
        const readerSettings = normalizeReaderSettings(unified.readerSettings, unified.bridge.vnTheme);
        readerSettings._sceneAssets = unified.bridge.sceneAssets || null;
        readerSettings._sentencePaging = Boolean(unified.bridge.sentencePaging);
        readerSettings._vnTheme = readerSettings.vnTheme || null;
        const snapshot = buildReaderSnapshot(current.payload, mode, readerSettings, optionsForRender.index || current.index);
        current.snapshot = snapshot;
        return snapshot;
    }

    function openSettings(openOptions = {}) {
        const normalizedTab = normalizeSettingsTab(openOptions.tab);
        const fallbackMode = state.activeReader ? state.activeReader.mode : undefined;
        if (state.activeSettings) {
            state.activeSettings.tab = normalizedTab;
            return rerenderSettings();
        }

        const initialSnapshot = resolveBridgeConfigSnapshot({ mode: 'default' });
        const controller = createSettingsController();
        const settingsState = {
            tab: normalizedTab,
            draft: cloneData(initialSnapshot),
            asyncState: {},
            controller,
            dom: null,
        };
        state.activeSettings = settingsState;
        settingsState.dom = mountSettingsDom(controller);
        return rerenderSettings();
    }

    function rerenderSettings() {
        if (!state.activeSettings) {
            return { ok: false, reason: 'settings-not-open' };
        }
        const snapshot = buildSettingsSnapshot(state.activeSettings);
        state.activeSettings.snapshot = snapshot;
        updateMountedSettings(snapshot);
        return {
            ok: true,
            tab: state.activeSettings.tab,
            snapshot: cloneData(snapshot),
            domMounted: Boolean(state.activeSettings.dom),
            controller: state.activeSettings.controller,
        };
    }

    function teardownStatusHudSubscription() {
        if (statusHudClient && statusHudCallback) {
            statusHudClient.unregisterCallback(statusHudCallback);
        }
        statusHudClient = null;
        statusHudCallback = null;
    }

    function syncStatusHudSubscription() {
        const current = state.activeReader;
        const settings = current && current.snapshot && current.snapshot.readerSettings;
        const statusHud = normalizeStatusHudSettings(settings && settings.statusHud);
        const shouldSubscribe = Boolean(current) && statusHud.enabled && statusHud.tables.length > 0;
        if (!shouldSubscribe) {
            teardownStatusHudSubscription();
            return;
        }
        if (statusHudClient && statusHudCallback) return;
        const api = (options.global || globalThis).AutoCardUpdaterAPI || null;
        statusHudClient = createShujukuClient(api);
        statusHudCallback = () => {
            if (!state.activeReader) return;
            refreshStatusHudInActiveReader();
        };
        statusHudClient.registerCallback(statusHudCallback);
    }

    function readStatusHudTablesSafe() {
        const api = (options.global || globalThis).AutoCardUpdaterAPI || null;
        try {
            return createShujukuClient(api).readTables();
        } catch (error) {
            return { ok: false, reason: String((error && error.message) || 'read-failed') };
        }
    }

    function refreshStatusHudInActiveReader() {
        const current = state.activeReader;
        if (!current || !current.dom || !current.dom.overlay) return;
        const settings = current.snapshot && current.snapshot.readerSettings;
        const content = current.snapshot && current.snapshot.content;
        const next = buildStatusHudModel({
            settings: normalizeStatusHudSettings(settings && settings.statusHud),
            sceneAssets: (settings && settings._sceneAssets) || {},
            location: content && content.sceneLocation,
            time: content && content.sceneTime,
            weather: content && content.sceneWeather,
            character: content && !content.sceneNsfw ? content.speaker : '',
            emotion: content && !content.sceneNsfw ? content.statusEmotion : '',
            isNarration: Boolean(content && (content.textType === 'narration' || content.textType === 'thought')),
            readResult: readStatusHudTablesSafe(),
        });
        current.snapshot.content.statusHud = next;
        applyReaderSnapshotToDom(current.dom.overlay, current.snapshot, current, {
            hasActiveSettings: () => Boolean(state.activeSettings),
            resolveAssetUrl: (url) => resolveReaderAssetUrl(url, current),
        });
    }

    function resolveReaderAssetUrl(url, current) {
        const source = String(url || '').trim();
        if (!source) return '';
        const ready = imageResourceCache.get(source);
        if (ready) return ready;
        if (!current.assetLoadRequests.has(source)) {
            current.assetLoadRequests.add(source);
            const loading = imageResourceCache.load(source);
            const immediate = imageResourceCache.get(source);
            loading.then(() => {
                current.assetLoadRequests.delete(source);
                if (state.activeReader !== current) return;
                const content = current.snapshot && current.snapshot.content;
                if (!content || (content.backgroundImage !== source && content.spriteImage !== source)) return;
                updateMountedReader(current.snapshot);
            });
            if (immediate) return immediate;
        }
        return '';
    }


    function closeReader(closeOptions = {}) {
        const current = state.activeReader;
        teardownStatusHudSubscription();
        if (!current) return { ok: true, reason: 'reader-not-open' };
        closeSettings();
        clearReaderToast(current);
        clearReaderModeRuntime(current);
        if (closeOptions.keepFullscreen !== true) {
            exitDocumentFullscreen(getRootDocument(options.global));
        }
        current.imagePollToken += 1;
        streamObserver.stop();
        sourceCache.invalidate();
        if (current.dom && typeof current.dom.dispose === 'function') {
            current.dom.dispose();
        }
        unmountNode(current.dom && current.dom.root);
        state.activeReader = null;
        return { ok: true };
    }

    function closeSettings() {
        const current = state.activeSettings;
        if (!current) return { ok: true, reason: 'settings-not-open' };
        if (current.dom && typeof current.dom.dispose === 'function') {
            current.dom.dispose();
        }
        unmountNode(current.dom && current.dom.root);
        state.activeSettings = null;
        return { ok: true };
    }

    function getState() {
        return {
            activeReader: state.activeReader ? {
                mode: state.activeReader.mode,
                index: state.activeReader.index,
                hidden: state.activeReader.hidden,
                dragSuppressClick: state.activeReader.dragSuppressClick,
                toolbarCollapsed: state.activeReader.toolbarCollapsed,
                lastAction: state.activeReader.lastAction,
                inputValue: state.activeReader.inputValue,
                toastMessage: state.activeReader.toastMessage,
                floatingState: cloneData(state.activeReader.floatingState),
                snapshot: cloneData(state.activeReader.snapshot),
            } : null,
            activeSettings: state.activeSettings ? {
                tab: state.activeSettings.tab,
                snapshot: cloneData(state.activeSettings.snapshot),
            } : null,
        };
    }

    function destroy() {
        teardownStatusHudSubscription();
        closeSettings();
        closeReader();
        streamObserver.stop();
        imageResourceCache.clear();
        return { ok: true };
    }

    function handleChatStreamActivity() {
        const current = state.activeReader;
        if (!current || !isEmbeddedReaderMode(current.mode)) return;
        syncEmbeddedStreamMount(current);
        enterEmbeddedLoading();
    }

    function resolveEmbeddedDocument(current) {
        const mount = current && current.dom && current.dom.embeddedMount;
        return mount && mount.host && mount.host.ownerDocument
            || current && current.dom && current.dom.root && current.dom.root.ownerDocument
            || current && current.dom && current.dom.doc
            || getRootDocument(options.global);
    }

    function syncEmbeddedStreamMount(current) {
        const message = resolveLatestLiveAiMessage(resolveEmbeddedDocument(current));
        return message ? syncEmbeddedReaderMount(current, message) : false;
    }

    function resolveLatestLiveAiMessage(doc) {
        const chat = doc && typeof doc.querySelector === 'function' ? doc.querySelector('#chat') : null;
        const children = chat && chat.children ? Array.from(chat.children) : [];
        for (let index = children.length - 1; index >= 0; index -= 1) {
            const element = children[index];
            if (!element || !element.classList || !element.classList.contains('mes')) continue;
            if (readHostBooleanAttribute(element, ['is_user', 'data-is-user'])) continue;
            if (readHostBooleanAttribute(element, ['is_system', 'data-is-system'])) continue;
            const messageId = readLiveMessageId(element);
            if (messageId != null) return { id: messageId, element };
        }
        return null;
    }

    function readHostBooleanAttribute(element, names) {
        if (!element || typeof element.getAttribute !== 'function') return false;
        return names.some((name) => {
            const value = element.getAttribute(name);
            return value === true || value === 1 || value === '1' || value === 'true';
        });
    }

    function readLiveMessageId(element) {
        if (!element || typeof element.getAttribute !== 'function') return null;
        for (const name of ['mesid', 'data-mesid', 'data-message-id', 'data-id']) {
            const raw = element.getAttribute(name);
            const id = Number(raw);
            if (raw != null && Number.isFinite(id) && id >= 0) return id;
        }
        return null;
    }

    function syncEmbeddedReaderMount(current, message) {
        if (!current || !current.dom || !current.dom.root || !message || message.id == null) return false;
        const doc = resolveEmbeddedDocument(current);
        const element = message.element || resolveLiveMessageElement(doc, message.id);
        const resolved = resolveEmbeddedHostParent(element);
        if (!resolved) return false;
        const mount = current.dom.embeddedMount;
        const sameMount = Boolean(
            mount
            && mount.messageId === message.id
            && mount.mesText === resolved.mesText
            && mount.host
            && mount.host.parentNode === resolved.parent
        );
        if (sameMount) {
            hideEmbeddedSourceText(resolved.mesText);
            return false;
        }
        remountEmbeddedReader(current, { ...message, element });
        if (current.dom.embeddedMount) current.mountMessageId = message.id;
        return Boolean(current.dom.embeddedMount);
    }

    // 观察器只在“稳定”后调用一次：读取最新 AI 消息并原地换源。
    // 流式 token 期间不解析正文、不收集图片，避免酒馆卡顿与页面抖动。
    async function handleChatStreamStable() {
        const current = state.activeReader;
        if (!current || !isEmbeddedReaderMode(current.mode)) return true;
        if (typeof options.getCurrentMessage !== 'function'
            || typeof options.openViewerFromMessage !== 'function') {
            exitEmbeddedLoading();
            return true;
        }
        const message = await options.getCurrentMessage();
        if (!message || message.id == null) {
            exitEmbeddedLoading();
            return true;
        }
        const nextRaw = getMessagePrimaryText(message.raw || message);
        const nextVisible = String(message.visibleText || '');
        const changed = message.id !== current.contentMessageId
            || nextRaw !== current.streamBaselineRaw
            || nextVisible !== current.streamBaselineVisible;
        if (!changed) {
            exitEmbeddedLoading();
            return true;
        }
        syncEmbeddedReaderMount(current, message);
        current.turnOffset = 0;
        current.mountMessageId = message.id;
        try {
            await options.openViewerFromMessage(message.id, current.mode, {
                replaceActive: true,
                turnOffset: 0,
                mountMessageId: message.id,
                message,
            });
        } catch (error) {
            // 宿主异常不得打断阅读器；保持当前内容即可。
        } finally {
            exitEmbeddedLoading();
        }
        return true;
    }

    function enterEmbeddedLoading() {
        const current = state.activeReader;
        const mount = current && current.dom && current.dom.embeddedMount;
        if (!current || !isEmbeddedReaderMode(current.mode) || !mount || !mount.host) return;
        if (current.streamPhase !== 'streaming') {
            current.imagePollToken += 1;
            current.imagePolling = false;
            current.streamBaselineRaw = String(current.mountBaselineRaw || '');
            current.streamBaselineVisible = String(current.mountBaselineVisible || '');
        }
        current.streamPhase = 'streaming';
        current.turnOffset = 0;
        const host = mount.host;
        host.setAttribute('data-igs-embedded-loading', '1');
        if (current.dom.root && current.dom.root.style) current.dom.root.style.display = 'none';
        let loading = host.querySelector && host.querySelector('.igs-embedded-loading');
        if (!loading && host.ownerDocument && typeof host.ownerDocument.createElement === 'function') {
            loading = host.ownerDocument.createElement('div');
            loading.className = 'igs-embedded-loading';
            loading.setAttribute('role', 'status');
            loading.setAttribute('aria-live', 'polite');
            loading.innerHTML = '<span class="igs-embedded-loading-dot"></span><span class="igs-embedded-loading-dot"></span><span class="igs-embedded-loading-dot"></span><span class="igs-embedded-loading-text">正在生成…</span>';
            host.appendChild(loading);
        }
    }

    function exitEmbeddedLoading() {
        const current = state.activeReader;
        if (!current) return;
        current.streamBaselineRaw = '';
        current.streamBaselineVisible = '';
        current.streamPhase = 'idle';
        const mount = current.dom && current.dom.embeddedMount;
        const host = mount && mount.host;
        if (host && host.removeAttribute) host.removeAttribute('data-igs-embedded-loading');
        const loading = host && host.querySelector ? host.querySelector('.igs-embedded-loading') : null;
        if (loading && typeof loading.remove === 'function') loading.remove();
        if (current.dom && current.dom.root && current.dom.root.style) current.dom.root.style.display = '';
    }

    function remountEmbeddedReader(current, message) {
        if (!current || !current.dom || !current.dom.root) return;
        const root = current.dom.root;
        const doc = resolveEmbeddedDocument(current);
        teardownEmbeddedMount(current.dom.embeddedMount);
        current.dom.embeddedMount = mountEmbeddedRoot(doc, root, message, message && message.id);
        if (!current.dom.embeddedMount) {
            (doc.documentElement || doc.body).appendChild(root);
        }
    }

    function syncReaderMountForMode(current, mode) {
        if (!current || !current.dom || !current.dom.root) return;
        const doc = current.dom.doc;
        const root = current.dom.root;
        if (isEmbeddedReaderMode(mode)) {
            if (!current.dom.embeddedMount) {
                current.dom.embeddedMount = mountEmbeddedRoot(doc, root, current.payload.message, current.mountMessageId);
            }
            streamObserver.start();
            return;
        }
        streamObserver.stop();
        exitEmbeddedLoading();
        if (current.dom.embeddedMount) {
            (doc.documentElement || doc.body).appendChild(root);
            teardownEmbeddedMount(current.dom.embeddedMount);
            current.dom.embeddedMount = null;
        }
    }

    function createReaderController() {
        return {
            getSnapshot() {
                return state.activeReader ? cloneData(state.activeReader.snapshot) : null;
            },
            setInputValue(value) {
                if (!state.activeReader) return { ok: false, reason: 'reader-not-open' };
                state.activeReader.inputValue = String(value || '');
                if (state.activeReader.dom && state.activeReader.dom.input) {
                    state.activeReader.dom.input.value = state.activeReader.inputValue;
                }
                return { ok: true, value: state.activeReader.inputValue };
            },
            async submit(text) {
                return submitReaderInput(text);
            },
            async keydown(event = {}) {
                if (event.key !== 'Enter') {
                    return { ok: true, sent: false, reason: 'ignored-key' };
                }
                if (event.shiftKey) {
                    return { ok: true, sent: false, reason: 'shift-enter-kept' };
                }
                return submitReaderInput(firstDefined(event.value, state.activeReader && state.activeReader.inputValue, ''));
            },
            toggleHidden() {
                if (!state.activeReader) return { ok: false, reason: 'reader-not-open' };
                state.activeReader.hidden = !state.activeReader.hidden;
                rerenderActiveReader();
                return { ok: true, hidden: state.activeReader.hidden };
            },
            toggleToolbar() {
                if (!state.activeReader) return { ok: false, reason: 'reader-not-open' };
                state.activeReader.toolbarCollapsed = !state.activeReader.toolbarCollapsed;
                applyToolbarState(state.activeReader.dom && state.activeReader.dom.overlay, state.activeReader);
                return { ok: true, collapsed: state.activeReader.toolbarCollapsed };
            },
            toggleStatusHud() {
                const current = state.activeReader;
                if (!current) return { ok: false, reason: 'reader-not-open' };
                const hudSettings = current.snapshot && current.snapshot.readerSettings && current.snapshot.readerSettings.statusHud || {};
                const effectiveCollapsed = hudSettings.collapsed === true || current.toolbarCollapsed === false;
                if (effectiveCollapsed) {
                    current.toolbarCollapsed = true;
                    if (hudSettings.collapsed === true) {
                        const result = saveReaderSettingsPatch({ statusHud: { ...hudSettings, collapsed: false } });
                        if (result && result.ok === false) return result;
                    } else {
                        applyToolbarState(current.dom && current.dom.overlay, current);
                    }
                    return { ok: true, collapsed: false };
                }
                const result = saveReaderSettingsPatch({ statusHud: { ...hudSettings, collapsed: true } });
                if (result && result.ok === false) return result;
                return { ok: true, collapsed: true };
            },
            invokeAction(action) {
                return handleReaderAction(action);
            },
            openSettings(tab = 'basic') {
                return openSettings({ tab, mode: state.activeReader ? state.activeReader.mode : 'pc' });
            },
            close() {
                return closeReader();
            },
        };
    }

    function createSettingsController() {
        return {
            getSnapshot() {
                return state.activeSettings ? cloneData(state.activeSettings.snapshot) : null;
            },
            switchTab(tab) {
                if (!state.activeSettings) return { ok: false, reason: 'settings-not-open' };
                state.activeSettings.tab = normalizeSettingsTab(tab);
                return rerenderSettings();
            },
            switchReaderSubTab(subTab) {
                if (!state.activeSettings) return { ok: false, reason: 'settings-not-open' };
                state.activeSettings.asyncState.readerSubTab = normalizeReaderSubTab(subTab);
                return rerenderSettings();
            },
            switchSceneSettingsSubTab(subTab) {
                if (!state.activeSettings) return { ok: false, reason: 'settings-not-open' };
                state.activeSettings.asyncState.sceneSettingsSubTab = normalizeSceneSettingsSubTab(subTab);
                return rerenderSettings();
            },
            switchSceneSubTab(subTab) {
                if (!state.activeSettings) return { ok: false, reason: 'settings-not-open' };
                state.activeSettings.asyncState.sceneSubTab = subTab === 'characters' ? 'characters' : 'scenes';
                return rerenderSettings();
            },
            setValue(path, value) {
                return updateSettingsValue(path, value);
            },
            toggle(path) {
                const current = getPath(state.activeSettings && state.activeSettings.draft, path);
                return updateSettingsValue(path, !current);
            },
            async invoke(action) {
                return handleSettingsAction(action);
            },
            close() {
                return closeSettings();
            },
        };
    }

    async function submitReaderInput(text) {
        if (!state.activeReader) return { ok: false, reason: 'reader-not-open' };
        const embedded = isEmbeddedReaderMode(state.activeReader.mode);
        if (embedded) {
            enterEmbeddedLoading();
        }
        const nextText = String(firstDefined(text, state.activeReader.inputValue, '') || '');
        const send = typeof options.typeAndSend === 'function'
            ? options.typeAndSend
            : async () => ({ ok: false, reason: 'missing-send-handler' });
        const result = await send(nextText);
        if (embedded && result.ok === false) exitEmbeddedLoading();
        else if (embedded) streamObserver.noteActivity();
        state.activeReader.inputValue = '';
        if (state.activeReader.dom && state.activeReader.dom.input) {
            state.activeReader.dom.input.value = '';
        }
        writeToast(result.ok === false ? (result.reason || '发送失败') : '已发送');
        return {
            ok: result.ok !== false,
            sent: result.ok !== false,
            text: nextText,
            result,
        };
    }

    function getOptionBubbleConfig() {
        const mode = state.activeReader && state.activeReader.mode ? state.activeReader.mode : undefined;
        const unified = resolveBridgeConfigSnapshot({ mode });
        const bridge = unified.bridge;
        const ob = bridge.optionBubble && typeof bridge.optionBubble === 'object' ? bridge.optionBubble : {};
        const reader = normalizeReaderSettings(unified.readerSettings, bridge.vnTheme);
        const position = (ob.position === 'top-center' || ob.position === 'top-right') ? ob.position : 'top-left';
        return {
            enabled: ob.enabled === true,
            position,
            clickAction: ob.clickAction === 'fill' ? 'fill' : 'send',
            widthFollowsText: ob.widthFollowsText === true,
            fontSize: reader.optionFontSize,
        };
    }

    function isReaderLastPage(snapshot) {
        const segs = snapshot && snapshot.content && Array.isArray(snapshot.content.segments)
            ? snapshot.content.segments.length : 0;
        const idx = snapshot && snapshot.content ? Number(snapshot.content.currentIndex) : 0;
        return segs <= 0 || idx >= segs - 1;
    }

    function handleOptionBubbleBlankClick(current, snapshot) {
        const cfg = getOptionBubbleConfig();
        if (!cfg.enabled || !isReaderLastPage(snapshot)) return false;
        const overlay = current && current.dom && current.dom.overlay;
        const container = overlay && overlay.querySelector ? overlay.querySelector('#igs-option-bubbles') : null;
        if (!container) return false;
        if (!container.hasAttribute('hidden')) {
            hideOptionBubbles(container);
            return true;
        }
        showOptionBubbles(container, cfg);
        return true;
    }

    function syncStatusHudOptionSuppression(container, visible) {
        const doc = container && container.ownerDocument;
        const overlay = doc && typeof doc.getElementById === 'function' ? doc.getElementById('igs-overlay') : null;
        const hud = doc && typeof doc.getElementById === 'function' ? doc.getElementById('igs-status-hud') : null;
        if (overlay && overlay.classList) {
            if (visible) overlay.classList.add('igs-options-visible');
            else overlay.classList.remove('igs-options-visible');
        }
        if (hud && hud.classList) {
            if (visible) hud.classList.add('igs-hud-suppressed');
            else hud.classList.remove('igs-hud-suppressed');
        }
    }


    function hideOptionBubbles(container) {
        if (!container) return;
        container.setAttribute('hidden', '');
        syncStatusHudOptionSuppression(container, false);
        clearChildren(container);
    }

    function showOptionBubbles(container, cfg, optionsForShow = {}) {
        const doc = container.ownerDocument || getRootDocument(options.global);
        const api = (options.global || globalThis).AutoCardUpdaterAPI || null;
        const items = readOptionItems(createShujukuClient(api));
        if (!items.length) {
            hideOptionBubbles(container);
            if (!optionsForShow.silent) writeToastSafe('未找到选项表（选项 / 选项表 / 行动选项 / 检定建议表）或表为空');
            return;
        }
        container.setAttribute('data-igs-pos', cfg.position);
        container.setAttribute('data-igs-width', cfg.widthFollowsText ? 'text' : 'dialog');
        if (container.style && typeof container.style.setProperty === 'function') container.style.setProperty('--igs-option-font-size', `${cfg.fontSize}px`);
        clearChildren(container);
        for (const item of items) {
            const display = (item && typeof item === 'object') ? String(item.display || '') : String(item || '');
            const send = (item && typeof item === 'object') ? String(item.send || item.display || '') : String(item || '');
            const bubble = doc.createElement('button');
            bubble.type = 'button';
            bubble.className = 'igs-option-bubble igs-bubble';
            bubble.textContent = display;
            bubble.addEventListener('click', (event) => {
                if (event && typeof event.stopPropagation === 'function') event.stopPropagation();
                onOptionBubbleClick(container, send, cfg);
            });
            container.appendChild(bubble);
        }
        container.removeAttribute('hidden');
        syncStatusHudOptionSuppression(container, true);
    }

    async function onOptionBubbleClick(container, text, cfg) {
        hideOptionBubbles(container);
        if (cfg.clickAction === 'fill') {
            if (state.activeReader && isEmbeddedReaderMode(state.activeReader.mode)) {
                const fill = typeof options.setInputText === 'function'
                    ? options.setInputText
                    : async () => ({ ok: false, reason: 'missing-input-api' });
                const result = await fill(text);
                if (!result || result.ok === false) writeToastSafe(result && result.reason || '酒馆输入框不可用');
            } else if (state.activeReader) {
                state.activeReader.inputValue = text;
                const input = state.activeReader.dom && state.activeReader.dom.input;
                if (input) { input.value = text; if (typeof input.focus === 'function') input.focus(); }
            }
            return;
        }
        await submitReaderInput(text);
    }

    function writeToastSafe(message) {
        try { if (state.activeReader) writeToast(message); } catch (error) { /* ignore */ }
    }

    function updateSettingsValue(path, value) {
        if (!state.activeSettings) return { ok: false, reason: 'settings-not-open' };
        const draft = state.activeSettings.draft;

        if (path === 'bridge.openMode') {
            const nextMode = normalizeReaderMode(value, draft.bridge);
            setPath(draft, path, nextMode);
            const persisted = persistSettingsDraft({ syncActiveModeFromSettings: true });
            if (persisted.ok === false) return persisted;
            return rerenderSettings();
        }

        setPath(draft, path, normalizeSettingsValue(path, value));
        const themeKey = path.startsWith('readerSettings.classicVnTheme.') ? 'classicVnTheme' : 'vnTheme';
        const themeRoot = `readerSettings.${themeKey}`;
        if (path === `${themeRoot}.preset` && value === 'custom') {
            const currentTheme = draft.readerSettings[themeKey] || {};
            const prevName = currentTheme._prevPreset || 'genshin';
            const source = themeKey === 'classicVnTheme'
                ? CLASSIC_DIALOG_THEME_DEFAULTS
                : (VN_THEME_PRESETS[prevName] || VN_THEME_PRESETS.genshin);
            const fields = ['nameAlign', 'textAlign', 'narrationAlign', 'thoughtAlign', 'dividerSymbol', 'nameFont', 'textFont', 'thoughtFont', 'narrationFont', 'nameColor', 'textColor', 'thoughtColor', 'narrationColor', 'dividerColor'];
            for (const f of fields) {
                setPath(draft, `${themeRoot}.${f}`, source[f]);
            }
        }
        if (path === `${themeRoot}.preset` && value !== 'custom') {
            setPath(draft, `${themeRoot}._prevPreset`, value);
        }
        if (path.startsWith(`${themeRoot}.`) && path !== `${themeRoot}.preset` && path !== `${themeRoot}._prevPreset`) {
            setPath(draft, `${themeRoot}.preset`, 'custom');
        }
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    function persistSettingsDraft(optionsForPersist = {}) {
        if (!state.activeSettings) return { ok: false, reason: 'settings-not-open' };
        const draft = state.activeSettings.draft;
        const save = typeof options.saveUnifiedSettings === 'function'
            ? options.saveUnifiedSettings
            : null;
        if (!save) return { ok: false, reason: 'missing-save-handler' };

        const result = save({
            bridge: draft.bridge,
            readerMode: 'default',
            readerSettings: draft.readerSettings,
        });
        if (!result || result.ok === false) {
            return result || { ok: false, reason: 'save-failed' };
        }

        const snapshot = resolveBridgeConfigSnapshot({ mode: 'default' });
        state.activeSettings.draft = cloneData(snapshot);
        rerenderActiveReader({
            syncModeFromSettings: optionsForPersist.syncActiveModeFromSettings === true,
        });
        return result;
    }

    async function handleSettingsAction(action) {
        return runSettingsAction(action, {
            state,
            options,
            closeSettings,
            persistSettingsDraft,
            rerenderSettings,
            buildRegexPreview,
        });
    }

    async function handleReaderAction(action) {
        if (!state.activeReader) return { ok: false, reason: 'reader-not-open' };
        const normalizedAction = String(action || '').trim();
        state.activeReader.lastAction = normalizedAction;

        if (normalizedAction === 'settings') {
            return state.activeReader.controller.openSettings('basic');
        }
        if (normalizedAction === 'hide') {
            return state.activeReader.controller.toggleHidden();
        }
        if (normalizedAction === 'close') {
            return state.activeReader.controller.close();
        }
        if (normalizedAction === 'toggle-status-hud') {
            return state.activeReader.controller.toggleStatusHud();
        }
        if (normalizedAction === 'toggle-bar') {
            return state.activeReader.controller.toggleToolbar();
        }
        if (normalizedAction === 'prev') {
            return moveReaderSegment(-1);
        }
        if (normalizedAction === 'next') {
            return moveReaderSegment(1);
        }
        if (normalizedAction === 'first-page') {
            return jumpReaderSegment(0);
        }
        if (normalizedAction === 'last-page') {
            return jumpReaderSegment(Number.MAX_SAFE_INTEGER);
        }
        if (normalizedAction === 'regen') {
            return regenerateCurrentImage();
        }
        if (normalizedAction === 'rescan') {
            return rescanCurrentImages();
        }
        if (normalizedAction === 'save') {
            return saveCurrentImage();
        }
        if (['prev-turn', 'next-turn'].includes(normalizedAction)) {
            return moveReaderTurn(normalizedAction === 'prev-turn' ? -1 : 1);
        }
        if (normalizedAction === 'sprite-edit') {
            const overlay = state.activeReader.dom && state.activeReader.dom.overlay;
            if (overlay) enterSpriteEditMode(overlay, state.activeReader, buildSpriteEditContext());
            return { ok: true };
        }
        if (normalizedAction === 'db-panel') {
            const db = state.activeReader.dom && state.activeReader.dom.dbController;
            if (db) db.toggle(
                state.activeReader.dom.overlay,
                state.activeReader.snapshot && state.activeReader.snapshot.readerSettings,
            );
            return { ok: true };
        }

        return { ok: false, reason: 'unknown-reader-action', action: normalizedAction };
    }

    function moveReaderSegment(delta) {
        if (!state.activeReader) return { ok: false, reason: 'reader-not-open' };
        const segments = state.activeReader.snapshot && state.activeReader.snapshot.content
            ? state.activeReader.snapshot.content.segments || []
            : [];
        const maxIndex = Math.max(0, segments.length - 1);
        const nextIndex = Math.max(0, Math.min(maxIndex, Number(state.activeReader.index || 0) + delta));
        if (nextIndex === state.activeReader.index) {
            writeToast(delta > 0 ? '已经是最后一段' : '已经是第一段');
            return {
                ok: true,
                moved: false,
                index: state.activeReader.index,
                progress: state.activeReader.snapshot && state.activeReader.snapshot.content
                    ? state.activeReader.snapshot.content.progress
                    : '',
            };
        }
        state.activeReader.index = nextIndex;
        rerenderActiveReader();
        return {
            ok: true,
            moved: true,
            index: state.activeReader.index,
            progress: state.activeReader.snapshot.content.progress,
        };
    }

    function jumpReaderSegment(targetIndex) {
        if (!state.activeReader) return { ok: false, reason: 'reader-not-open' };
        const segments = state.activeReader.snapshot && state.activeReader.snapshot.content
            ? state.activeReader.snapshot.content.segments || []
            : [];
        const maxIndex = Math.max(0, segments.length - 1);
        const nextIndex = Math.max(0, Math.min(maxIndex, Number(targetIndex) || 0));
        if (nextIndex === state.activeReader.index) {
            return { ok: true, moved: false, index: state.activeReader.index };
        }
        state.activeReader.index = nextIndex;
        rerenderActiveReader();
        return { ok: true, moved: true, index: state.activeReader.index };
    }

    function rerenderActiveReader(optionsForRender = {}) {
        if (!state.activeReader) return { ok: true, reason: 'reader-not-open' };
        // Veridis 等关键词过滤插件在生成结束后异步写回 DOM，用 readLiveVisibleText
        // 拿当前最新渲染文本，确保翻页/重渲染时阅读器反映真实替换后的词。
        const freshVisible = readLiveVisibleText(state.activeReader);
        if (freshVisible) state.activeReader.payload.visibleText = freshVisible;
        // 普通设置保存必须保留当前 reader mode；只有 openMode 设置本身变化时才同步切换。
        // 确保 readerSettings 与立绘位置始终来自同一个 mode，避免 spriteLayouts 取错 key。
        const syncModeFromSettings = optionsForRender.syncModeFromSettings === true;
        const baseSnapshot = resolveBridgeConfigSnapshot({ mode: state.activeReader.mode });
        const nextMode = syncModeFromSettings
            ? normalizeReaderMode(
                firstDefined(baseSnapshot.bridge.openMode, state.activeReader.mode),
                baseSnapshot.bridge,
            )
            : normalizeReaderMode(state.activeReader.mode, baseSnapshot.bridge);
        const unified = resolveBridgeConfigSnapshot({ mode: nextMode });
        state.activeReader.mode = nextMode;
        syncReaderMountForMode(state.activeReader, nextMode);
        const readerSettings = normalizeReaderSettings(unified.readerSettings, unified.bridge.vnTheme);
        readerSettings._sceneAssets = unified.bridge.sceneAssets || null;
        readerSettings._sentencePaging = Boolean(unified.bridge.sentencePaging);
        readerSettings._vnTheme = readerSettings.vnTheme || null;
        state.activeReader.snapshot = buildReaderSnapshot(state.activeReader.payload, nextMode, readerSettings, state.activeReader.index);
        updateMountedReader(state.activeReader.snapshot);
        return { ok: true };
    }

    async function moveReaderTurn(delta) {
        const current = state.activeReader;
        if (!current) return { ok: false, reason: 'reader-not-open' };
        // 楼层内嵌：容器始终留在最新 AI 楼层，只切换内部阅读源，不调用宿主跳楼，
        // 避免酒馆滚动、楼层重排和阅读器重建带来的卡顿与跳动。
        if (isEmbeddedReaderMode(current.mode)) {
            return moveEmbeddedReaderTurn(current, delta);
        }
        const currentMessageId = current.snapshot && current.snapshot.messageId;
        const getAdjacentMessage = typeof options.getAdjacentMessage === 'function'
            ? options.getAdjacentMessage
            : null;
        if (currentMessageId == null || !getAdjacentMessage) {
            writeToast('楼层切换需要宿主消息列表。');
            return { ok: true, moved: false, reason: 'turn-switch-host-required' };
        }
        const target = await getAdjacentMessage(currentMessageId, delta);
        if (!target) {
            writeToast(delta > 0 ? '没有下一轮' : '没有上一轮');
            return { ok: true, moved: false, reason: 'turn-not-found', messageId: currentMessageId };
        }
        if (typeof options.jumpToMessage === 'function') {
            try {
                await options.jumpToMessage(target.id);
            } catch (error) {
                // 宿主跳转失败不阻断阅读器切层。
            }
        }
        if (typeof options.openViewerFromMessage !== 'function') {
            return { ok: false, reason: 'missing-open-viewer-handler', messageId: target.id };
        }
        const result = await options.openViewerFromMessage(target.id, current.mode, {
            startAtEnd: false,
            message: target,
        });
        if (result && result.ok !== false) {
            writeToast(delta > 0 ? '已切到下一轮' : '已切到上一轮');
            return { ok: true, moved: true, messageId: target.id, reader: result.reader };
        }
        return {
            ok: false,
            moved: false,
            reason: result && result.reason || 'turn-open-failed',
            messageId: target.id,
        };
    }

    async function moveEmbeddedReaderTurn(current, delta) {
        const getAdjacentMessage = typeof options.getAdjacentMessage === 'function'
            ? options.getAdjacentMessage
            : null;
        const currentMessageId = current.contentMessageId != null
            ? current.contentMessageId
            : (current.snapshot && current.snapshot.messageId);
        if (currentMessageId == null || !getAdjacentMessage) {
            writeToast('楼层切换需要宿主消息列表。');
            return { ok: true, moved: false, reason: 'turn-switch-host-required' };
        }
        const target = await getAdjacentMessage(currentMessageId, delta);
        if (!target) {
            writeToast(delta > 0 ? '没有下一轮' : '没有上一轮');
            return { ok: true, moved: false, reason: 'turn-not-found', messageId: currentMessageId };
        }
        if (typeof options.openViewerFromMessage !== 'function') {
            return { ok: false, reason: 'missing-open-viewer-handler', messageId: target.id };
        }
        // 历史轮次优先读文字：跳过 provider 图片收集，不跳楼、不扫不可见 DOM、不开轮询。
        const nextOffset = Math.max(0, (Number(current.turnOffset) || 0) - Number(delta));
        const result = await options.openViewerFromMessage(target.id, current.mode, {
            startAtEnd: false,
            message: target,
            replaceActive: true,
            skipImageCollection: nextOffset > 0,
            turnOffset: nextOffset,
        });
        if (result && result.ok !== false) {
            writeToast(delta > 0 ? '已切到下一轮' : '已切到上一轮');
            return { ok: true, moved: true, messageId: target.id, reader: result.reader };
        }
        return {
            ok: false,
            moved: false,
            reason: result && result.reason || 'turn-open-failed',
            messageId: target.id,
        };
    }

    function formatReaderProgress(snapshot) {
        if (isEmbeddedReaderMode(snapshot && snapshot.mode)) return '';
        return snapshot && snapshot.content ? snapshot.content.progress : '';
    }

    async function regenerateCurrentImage() {
        const current = state.activeReader;
        if (!current) return { ok: false, reason: 'reader-not-open' };
        if (typeof options.regenerateImage !== 'function') {
            writeToast('当前未接入图片重画能力。');
            return { ok: false, reason: 'provider-not-enabled' };
        }

        const result = await options.regenerateImage(buildImageActionContext(
            current,
            resolveBridgeConfigSnapshot({ mode: current.mode }),
        ));
        if (result && result.imageState && state.activeReader) {
            state.activeReader.payload.imageState = cloneData(result.imageState);
            rerenderActiveReader();
        }
        writeToast(resolveReaderActionToast(result, {
            success: '背景图已更新。',
            fallback: '当前未检测到新的背景图。',
        }));
        return result;
    }

    function readLiveVisibleText(current) {
        if (!current) return '';
        const messageId = current.snapshot && current.snapshot.messageId != null
            ? current.snapshot.messageId
            : (current.payload && current.payload.messageId);
        const doc = getRootDocument(options.global);
        let element = null;
        if (doc && messageId != null) {
            element = doc.querySelector(`#chat .mes[mesid="${messageId}"]`)
                || doc.querySelector(`#chat .mes[data-mesid="${messageId}"]`);
        }
        if (!element && current.payload && current.payload.message) {
            element = current.payload.message.element || null;
        }
        if (!element) return '';
        return getVisibleMessageTextFromElement(element);
    }

    async function rescanCurrentImages() {
        const current = state.activeReader;
        if (!current) return { ok: false, reason: 'reader-not-open' };
        sourceCache.invalidate();
        if (typeof options.collectMessageImages !== 'function') {
            writeToast('图片收集不可用。');
            return { ok: false, reason: 'collect-not-available' };
        }
        const overlay = current.dom && current.dom.root || (options.global || globalThis).document && (options.global || globalThis).document.querySelector('#igs-overlay');
        const bgContainer = overlay && overlay.querySelector('#igs-bg');
        ensureImageLoadingSpinner(bgContainer);

        const unified = resolveBridgeConfigSnapshot({ mode: current.mode });
        const context = buildImageActionContext(current, unified);
        // 并行：重扫图片 + 重扫正文（用最新 bridge 配置重新解析）。
        const [imageResult] = await Promise.all([
            options.collectMessageImages({
                ...context,
                messageId: current.snapshot && current.snapshot.messageId,
                preferredImageIndex: context.imageIndex,
                skipCache: true,
                requiresMessageScope: Array.isArray(context.imageState && context.imageState.slots)
                    && context.imageState.slots.length > 0,
            }),
            Promise.resolve().then(() => {
                // 重扫正文：把最新筛选/格式化配置写回 payload，并清掉缓存的分段，
                // 让 rerender 时 buildReaderSnapshot 用最新配置重新解析正文。
                current.payload.sourceFilter = unified.bridge.sourceFilter;
                current.payload.virtualRegex = unified.bridge.virtualRegex;
                current.payload.textSegments = null;
                current.payload.segmentImageSlots = null;
                current.payload.sceneDirectives = null;
                // 重抓 DOM 可见文本：关键词过滤插件（如 Veridis）只改 .mes_text 渲染层、
                // 移动端宿主回写 chat[n].mes 滞后时，刷新需拿到最新渲染文本而非打开时的旧快照。
                const freshVisible = readLiveVisibleText(current);
                if (freshVisible) current.payload.visibleText = freshVisible;
            }),
        ]);

        removeImageLoadingSpinner(bgContainer);
        if (!imageResult || imageResult.ok === false) {
            // 图片没扫到也要应用正文重扫并回到第一页。
            current.index = 0;
            rerenderActiveReader();
            writeToast('已刷新正文（未扫描到图片）。');
            return imageResult || { ok: false, reason: 'rescan-failed' };
        }
        const nextBoundCount = countBoundImageSlots(imageResult);
        current.payload.imageState = cloneData(imageResult);
        current.index = 0;
        rerenderActiveReader();
        writeToast(`已刷新：绑定 ${nextBoundCount}/${imageResult.expectedCount || imageResult.count || 0} 张图。`);
        return { ok: true, boundCount: nextBoundCount, imageState: imageResult };
    }

    async function saveCurrentImage() {
        const current = state.activeReader;
        if (!current) return { ok: false, reason: 'reader-not-open' };
        const context = buildImageActionContext(
            current,
            resolveBridgeConfigSnapshot({ mode: current.mode }),
        );
        const saveImage = typeof options.saveImage === 'function'
            ? options.saveImage
            : async () => ({ ok: false, reason: 'missing-save-handler' });
        const result = await saveImage({
            ...context,
            url: context.currentUrl,
        });
        writeToast(resolveReaderActionToast(result, {
            success: '背景图保存命令已发出。',
            fallback: '当前背景图不可保存。',
        }));
        return result;
    }

    function startReaderImagePolling(current) {
        if (!current || typeof options.collectMessageImages !== 'function') return;
        if (!shouldPollReaderImages(current.snapshot && current.snapshot.content)) return;
        const token = (current.imagePollToken || 0) + 1;
        current.imagePollToken = token;
        current.imagePolling = true;
        pollReaderImages(current, token);
    }

    async function pollReaderImages(current, token) {
        const unified = resolveBridgeConfigSnapshot({ mode: current.mode });
        const imageApi = unified.bridge && unified.bridge.imageApi || {};
        const intervalMs = normalizePollInterval(imageApi.initialPollIntervalMs || imageApi.pollIntervalMs);
        const attempts = normalizePollAttempts(imageApi.initialPollAttempts);
        let previousSignature = String(current.snapshot && current.snapshot.content && current.snapshot.content.imageSignature || '');
        let previousBoundCount = Number(current.snapshot && current.snapshot.content && current.snapshot.content.imageBoundCount || 0) || 0;

        for (let attempt = 0; attempt < attempts; attempt += 1) {
            await waitForReaderImagePoll(intervalMs, options.global);
            if (!state.activeReader || state.activeReader !== current || current.imagePollToken !== token) return;
            const context = buildImageActionContext(current, resolveBridgeConfigSnapshot({ mode: current.mode }));
            const result = await options.collectMessageImages({
                ...context,
                messageId: current.snapshot && current.snapshot.messageId,
                preferredImageIndex: context.imageIndex,
                requiresMessageScope: Array.isArray(context.imageState && context.imageState.slots)
                    && context.imageState.slots.length > 0,
            });
            if (!result || result.ok === false) continue;
            const nextBoundCount = countBoundImageSlots(result);
            const nextSignature = String(result.signature || '');
            const currentUrl = String(result.currentUrl || result.displayUrl || '').trim();
            if (nextSignature !== previousSignature || nextBoundCount > previousBoundCount || currentUrl) {
                current.payload.imageState = cloneData(result);
                rerenderActiveReader();
                previousSignature = nextSignature;
                previousBoundCount = nextBoundCount;
                if (!shouldPollReaderImages(current.snapshot && current.snapshot.content)) {
                    current.imagePolling = false;
                    return;
                }
            }
        }
        if (state.activeReader === current && current.imagePollToken === token) {
            current.imagePolling = false;
            rerenderActiveReader();
        }
    }

    function buildReaderSnapshot(payload, mode, readerSettings, index = 0) {
        const scene = cloneData(payload.scene || (payload.render && payload.render.scene) || {});
        const buildStatusHudForSnapshot = (settings, speaker, emotion, sceneInfo, isNarration) => {
            const statusHud = normalizeStatusHudSettings(settings && settings.statusHud);
            if (!statusHud.enabled) return buildStatusHudModel({ settings: statusHud, character: '', emotion: '', location: '' });
            const sceneAssets = (settings && settings._sceneAssets) || {};
            const readResult = statusHud.tables.length ? readStatusHudTables() : null;
            const info = sceneInfo && typeof sceneInfo === 'object' ? sceneInfo : {};
            return buildStatusHudModel({ settings: statusHud, sceneAssets, character: speaker, emotion, location: info.location, time: info.time, weather: info.weather, isNarration, readResult });
        };
        const readStatusHudTables = () => {
            const api = (options.global || globalThis).AutoCardUpdaterAPI || null;
            return createShujukuClient(api).readTables();
        };
        const render = payload.render || {};
        const stage = render.stage || {};
        const liveMessage = (payload.message && payload.message.raw) || payload.message || payload.raw || '';
        const parseOptions = {
            sourceFilter: payload.sourceFilter,
            virtualRegex: payload.virtualRegex,
            visibleText: payload.visibleText,
            sceneAssets: readerSettings._sceneAssets,
            sentencePaging: readerSettings._sentencePaging,
        };
        const sourceSignature = buildReaderSourceSignature({
            messageId: payload.messageId,
            rawText: getMessagePrimaryText(liveMessage),
            visibleText: payload.visibleText,
            sourceFilter: payload.sourceFilter,
            virtualRegex: payload.virtualRegex,
            sceneAssetsEnabled: Boolean(readerSettings._sceneAssets && readerSettings._sceneAssets.enabled),
            sentencePaging: Boolean(readerSettings._sentencePaging),
        });
        const cached = sourceCache.get(sourceSignature, { liveMessage, parseOptions });
        const extracted = cached.value || buildIgsTextPayload(liveMessage, parseOptions);
        const text = firstRenderableText(
            scene.text,
            scene.formattedText,
            payload.formattedText,
            extracted.formattedText,
            extracted.visibleText,
            extracted.cleanedRaw,
            getMessagePrimaryText(liveMessage),
            payload.raw,
        );
        const extractedSegments = Array.isArray(extracted.textSegments) ? extracted.textSegments : [];
        const hasExtractedSegments = extractedSegments.some((segment) => String(segment || '').trim());
        let segments = Array.isArray(payload.textSegments) && payload.textSegments.length
            ? cloneData(payload.textSegments)
            : hasExtractedSegments
                ? cloneData(extractedSegments)
                : buildTextSegments(stripSceneDirectiveLines(text));
        // 场景指令可能紧贴正文（如「正文。[igs-scene:…]」），所有分段来源都必须再剥离一次，
        // 否则标签会作为正文渲染进对话框。
        // 剥离后为空的段（例如整行只有 [igs-scene:]）直接丢弃，避免多出一个空白页。
        const visibleSegments = segments
            .map((seg) => stripSceneDirectivesInline(seg))
            .filter((seg) => String(seg || '').trim().length > 0);
        segments = visibleSegments.length ? visibleSegments : [''];


        const normalizedIndex = Math.max(0, Math.min(segments.length - 1, Number(index) || 0));
        const segmentImageSlots = Array.isArray(payload.segmentImageSlots) && payload.segmentImageSlots.length
            ? payload.segmentImageSlots
            : extracted.segmentImageSlots;
        const imageState = normalizeSnapshotImageState(
            payload.imageState,
            resolveSegmentImageIndex({ imageState: payload.imageState, segmentImageSlots }, normalizedIndex),
        );
        const displayImageState = applyImageCountOverride(imageState, readerSettings.imageCountOverride);
        // 分段缺失时给空串：绝不用整篇 text 兜底，否则会凭空多出一页「全文」。
        const currentText = segments[normalizedIndex] == null ? '' : String(segments[normalizedIndex]);
        const sceneAssetsEnabled = readerSettings._sceneAssets && readerSettings._sceneAssets.enabled;
        const backgroundImage = firstNonEmptyString(
            displayImageState.displayUrl,
            displayImageState.currentUrl,
            scene.generatedImage && scene.generatedImage.value,
            stage.layers && stage.layers.generated && stage.layers.generated.resource && stage.layers.generated.resource.value,
            stage.layers && stage.layers.background && stage.layers.background.resource && stage.layers.background.resource.url,
            '',
        );
        const sceneAssets = readerSettings._sceneAssets || null;
        const sceneDirectives = Array.isArray(extracted.sceneDirectives) ? extracted.sceneDirectives
            : Array.isArray(payload.sceneDirectives) ? payload.sceneDirectives : [];
        const hasIgsDirectives = sceneDirectives.length > 0;
        let finalBackgroundImage = backgroundImage;
        const hideSpriteOnNsfw = !normalizeStatusHudSettings(readerSettings && readerSettings.statusHud).showSpriteOnNsfw;
        let spriteImage = null;
        let resolvedSpeaker = scene.speaker || '';
        let spriteCharacter = '';
        const extractedSegmentImageSlots = Array.isArray(extracted.segmentImageSlots) ? extracted.segmentImageSlots : [];
        const rawSegmentSlotValue = extractedSegmentImageSlots[normalizedIndex];
        const segmentHasBoundSlot = rawSegmentSlotValue != null
            && Number.isFinite(Number(rawSegmentSlotValue))
            && Number(rawSegmentSlotValue) >= 0;
        const slotBoundUrl = segmentHasBoundSlot
            && Array.isArray(displayImageState.slots)
            && displayImageState.slots[Math.floor(Number(rawSegmentSlotValue))]
            ? String(displayImageState.slots[Math.floor(Number(rawSegmentSlotValue))].url || '').trim()
            : '';
        // 场景切换只看 [igs-scene] 标签：直接在原文里定位「当前页正文」，
        // 再取它前方最近的场景标签。不依赖段数、不做偏移累加。
        const sceneSourceForOffset = String(payload.raw || text || '');
        const currentOffset = sceneDirectives.length
            ? locateTextOffsetInSource(sceneSourceForOffset, currentText)
            : -1;
        const sceneStateForBg = sceneDirectives.length
            ? (currentOffset >= 0
                ? resolveSceneAtSourceOffset(sceneSourceForOffset, currentOffset)
                : resolveSceneStateAtIndex(sceneDirectives, normalizedIndex))
            : null;
        if (slotBoundUrl) {
            finalBackgroundImage = slotBoundUrl;
            spriteImage = null;
        } else if (sceneAssets && sceneAssets.enabled) {
            if (sceneDirectives.length) {
                const bgUrls = lookupSceneAssetUrls(sceneStateForBg, sceneAssets);
                finalBackgroundImage = bgUrls.backgroundUrl || '';
            } else {
                finalBackgroundImage = '';
            }
            spriteImage = null;
        }
        // Per-segment classification from the formatted segment text itself.
        // Order matters: thought (*...*) is checked before dialogue ([名字]：) because
        // a thought segment looks like *[名字]：...* and would otherwise match dialogue.
        // Fallback: when the prefix was stripped (single-segment messages), use the
        // directive that lands on this exact segment index.
        // 角色台词若被成对引号整体包裹（AI 偶发额外输出中英文引号），前端不渲染引号。
        const stripWrappingQuotes = (text) => {
            const value = String(text == null ? '' : text).trim();
            if (value.length < 2) return value;
            const pairs = [['“', '”'], ['‘', '’'], ['「', '」'], ['『', '』'], ['"', '"'], ["'", "'"]];
            for (const [open, close] of pairs) {
                if (value.startsWith(open) && value.endsWith(close)) {
                    return value.slice(open.length, value.length - close.length).trim();
                }
            }
            return value;
        };

        let textType = 'narration';
        let bubbleSpeaker = '';
        let segmentBody = currentText;
        let bubbleMood = '';
        let spriteMood = '';
        if (sceneAssetsEnabled) {
            const charThoughtDirectives = sceneDirectives.filter((d) => d.type === 'char' || d.type === 'thought');
            // Match this bubble back to its directive by speaker + dialogue/thought text
            // fingerprint, not by row ordinal. Reformatting (image blocks, italic narration,
            // merged/stripped lines) desyncs any positional counter, so we look the source
            // text up directly. normalizeFingerprint strips the translation tail *（…）*,
            // brackets and whitespace so a substring compare is stable.
            const normalizeFingerprint = (s) => String(s || '')
                .replace(/\*（[^）]*）\*/g, '')
                .replace(/[\[\]\*（）]/g, '')
                .replace(/\s+/g, '')
                .trim();
            const findDirectiveByText = (speaker, body) => {
                const bodyKey = normalizeFingerprint(body);
                if (!bodyKey) return null;
                const probe = bodyKey.slice(0, 12);
                const pool = charThoughtDirectives.filter((d) => !speaker || d.character === speaker);
                for (const d of pool) {
                    const src = normalizeFingerprint(d.type === 'thought' ? d.thought : d.dialogue);
                    if (src && (src.includes(probe) || probe.includes(src.slice(0, 12)))) return d;
                }
                return null;
            };
            const classifySegment = (segText) => {
                // 没有任何 [igs-*:] 指令时禁止按文本外形猜测台词/心理话，统一按旁白兜底。
                if (!hasIgsDirectives) return null;
                const seg = String(segText || '');
                const tMatch = seg.match(/^\s*\*\s*(?:\[([^\]]+)\]\s*[:：]\s*)?([\s\S]*?)\s*\*\s*$/);
                const dMatch = seg.match(/^\s*\[([^\]]+)\]\s*[:：]\s*([\s\S]*)$/);
                if (tMatch) {
                    let sp = tMatch[1] ? tMatch[1].trim() : '';
                    // 正文可能写作 **…**（成对双星号），匹配指令前剥掉残留星号。
                    const bodyText = String(tMatch[2] || '').replace(/^\s*\*+\s*/, '').replace(/\s*\*+\s*$/, '').trim();
                    const matched = findDirectiveByText(sp, bodyText) || findDirectiveByText(sp, tMatch[2]);
                    if (matched && !sp) sp = matched.character || '';
                    return { textType: 'thought', speaker: sp, mood: matched ? (matched.mood || '') : '', body: seg };
                }
                if (dMatch) {
                    const sp = dMatch[1].trim();
                    const matched = findDirectiveByText(sp, dMatch[2]);
                    return { textType: 'dialogue', speaker: sp, mood: matched ? (matched.mood || '') : '', body: stripWrappingQuotes(dMatch[2]) };
                }
                return null;
            };
            const classified = classifySegment(currentText);
            if (classified) {
                textType = classified.textType;
                bubbleSpeaker = classified.speaker;
                bubbleMood = classified.mood;
                segmentBody = classified.body;
            }
            // Single-segment fallback: parseSpeakerPrefix strips the "[名字]：" prefix off
            // a lone segment, so the bubble regex no longer matches. Recover speaker/mood
            // from the directive that lands on this segment index.
            // 只要还没确定说话人就需要兜底：classifySegment 可能只判定出类型
            // （如 dialogue/thought）却没带出角色名，此时也必须补 speaker。
            if (!bubbleSpeaker) {
                const segDirective = sceneDirectives.find((d) => Number(d.segmentIndex) === normalizedIndex
                    && (d.type === 'char' || d.type === 'thought'));
                if (segDirective) {
                    textType = segDirective.type === 'thought' ? 'thought' : 'dialogue';
                    bubbleSpeaker = segDirective.character || scene.speaker;
                    bubbleMood = segDirective.mood || '';
                } else {
                    // 按行计数的 segmentIndex 与文本管线分段会错位，索引匹配失败时改用字符位置：
                    // 在原文里定位当前段正文，取它前方最近的一条 char/thought 指令。
                    // 仅在「当前段正文能与某条指令的台词/心里话对上」时才认角色：
                    // 旁白句不与任何指令匹配，因此不会被误判成角色页。
                    const probeBody = String(currentText || '')
                        .replace(/^\s*\*+\s*/, '').replace(/\s*\*+\s*$/, '')
                        .replace(/^\s*\[[^\]]+\]\s*[:：]\s*/, '')
                        .trim();
                    const matchedDirective = findDirectiveByText('', probeBody);
                    if (globalThis.__IGS_HUD_DEBUG__) console.log('[FB]', JSON.stringify({ cur: String(currentText || '').slice(0, 30), hit: matchedDirective ? matchedDirective.character : null }));
                    if (matchedDirective) {
                        textType = matchedDirective.type === 'thought' ? 'thought' : 'dialogue';
                        bubbleSpeaker = matchedDirective.character || scene.speaker;
                        bubbleMood = matchedDirective.mood || '';
                    }
                }
            }
            resolvedSpeaker = bubbleSpeaker;
            // Sprite resolves from the bubble's own speaker/mood, not the row-counted
            // segmentIndex (which desyncs once char/thought tags are reformatted into
            // visible bubble lines). Background still follows directive accumulation.
            let spriteChar = bubbleSpeaker;
            spriteMood = bubbleMood;
            // Narration pages carry no char/thought tag of their own. Walk backwards
            // through prior segments and inherit the nearest one that classifies as a
            // char/thought bubble, so the sprite stays consistent across narration runs.
            if (!spriteChar) {
                for (let i = normalizedIndex - 1; i >= 0; i--) {
                    const prev = classifySegment(segments[i]);
                    if (prev && prev.speaker) {
                        spriteChar = prev.speaker;
                        spriteMood = prev.mood;
                        break;
                    }
                }
            }
            // 回溯也失败时（段落前缀被剥离，文本已不含「[名字]：」）按段索引取 char/thought 指令。
            // 只用于立绘继承，不改 textType，避免把旁白页误判成角色页。
            if (!spriteChar) {
                for (let i = normalizedIndex; i >= 0; i--) {
                    const d = sceneDirectives.find((x) => Number(x.segmentIndex) === i
                        && (x.type === 'char' || x.type === 'thought') && x.character);
                    if (d) {
                        spriteChar = d.character;
                        spriteMood = d.mood || '';
                  break;
                    }
                }
            }
            if (!spriteChar && sceneStateForBg && sceneStateForBg.character) {
                // Fallback for untransformed/legacy paths where the bubble text still
                // carries the raw tag (no reformatted "[名字]：" line to parse).
                spriteChar = sceneStateForBg.character;
                spriteMood = sceneStateForBg.mood || '';
            }
            if (!slotBoundUrl && sceneAssets && sceneAssets.enabled && spriteChar && !(sceneStateForBg && sceneStateForBg.nsfw && hideSpriteOnNsfw)) {
                const spriteUrls = lookupSceneAssetUrls({ character: spriteChar, mood: spriteMood }, sceneAssets);
                spriteImage = spriteUrls.spriteUrl || null;
                if (spriteImage) {
                    spriteCharacter = spriteUrls.spriteCharacter || spriteChar;
                    // Position keys follow the resolved image slot (exact mood / group /
                    // 默认), not the raw mood word, so every mood that maps to the same
                    // sprite image shares one position across pages.
                    spriteMood = spriteUrls.spriteSlot || spriteMood;
                }
            }
        }
        const statusSceneInfo = {
            location: firstDefined(sceneStateForBg && sceneStateForBg.scene, scene.location, ''),
            time: firstDefined(sceneStateForBg && sceneStateForBg.time, scene.time, ''),
            weather: firstDefined(sceneStateForBg && sceneStateForBg.weather, scene.weather, ''),
        };
        const isDialogueText = textType === 'dialogue' || (!sceneAssetsEnabled && Boolean(scene.speaker) && Boolean(currentText));
        const displayText = (!sceneAssetsEnabled && scene.speaker && currentText)
            ? `${scene.speaker}: ${stripWrappingQuotes(currentText)}`
            : (sceneAssetsEnabled
                ? (isDialogueText ? stripWrappingQuotes(segmentBody) : segmentBody)
                : currentText);
        const overlayClasses = ['igs-stage', 'igs-mode-' + mode];
        if (mode === 'pc' || mode === 'mobile') overlayClasses.push('igs-floating');
        if (mode === 'mobile') overlayClasses.push('igs-floating-mobile');
        if (isEmbeddedReaderMode(mode)) overlayClasses.push('igs-embedded-overlay');

        return {
            mode,
            messageId: firstDefined(payload.messageId, payload.message && payload.message.id, scene.messageId, null),
            selectors: Array.from(ORIGINAL_READER_REQUIRED_SELECTORS),
            classes: overlayClasses,
            styles: {
                '#igs-overlay': {
                    zIndex: ORIGINAL_READER_STYLE_CONTRACT.overlayZIndex,
                },
                '.igs-dialog': {
                    width: ORIGINAL_READER_STYLE_CONTRACT.dialogWidth,
                    borderRadius: '8px',
                    padding: '22px 26px 18px',
                },
                '.igs-input': {
                    height: ORIGINAL_READER_STYLE_CONTRACT.inputHeight,
                },
                '.igs-send-btn': {
                    minWidth: ORIGINAL_READER_STYLE_CONTRACT.sendButtonMinWidth,
                },
                '.igs-icon-btn': {
                    width: ORIGINAL_READER_STYLE_CONTRACT.toolbarButtonSize,
                    height: ORIGINAL_READER_STYLE_CONTRACT.toolbarButtonSize,
                },
                '#igs-sprite': {
                    display: spriteImage ? 'block' : 'none',
                },
            },
            content: {
                speaker: resolvedSpeaker,
                spriteCharacter,
                spriteMood,
                statusEmotion: bubbleMood,
                textType,
                text: currentText,
                fullText: text,
                displayText,
                segments: cloneData(segments),
                currentIndex: normalizedIndex,
                progress: buildProgressText(normalizedIndex, segments.length, displayImageState),
                backgroundImage: finalBackgroundImage,
                spriteImage,
                images: cloneData(displayImageState.images),
                imageSlots: cloneData(displayImageState.slots),
                unboundImages: cloneData(displayImageState.unboundImages),
                imageCount: displayImageState.count,
                imageExpectedCount: displayImageState.expectedCount,
                imageBoundCount: displayImageState.boundCount,
                imageUnboundCount: displayImageState.unboundCount,
                imageAvailableCount: displayImageState.availableCount,
                imageSignature: displayImageState.signature,
                activeImageIndex: displayImageState.currentIndex,
                currentImageUrl: displayImageState.displayUrl || displayImageState.currentUrl,
                currentSlotImageUrl: displayImageState.slotUrl,
                imageLoading: Boolean(state.activeReader && state.activeReader.imagePolling),
                sourceKind: firstDefined(scene.sourceKind, payload.sourceKind, 'raw-text'),
                warnings: extracted.warnings,
                errors: extracted.errors,
                sceneLocation: statusSceneInfo.location,
                sceneTime: statusSceneInfo.time,
                sceneWeather: statusSceneInfo.weather,
                sceneNsfw: Boolean(sceneStateForBg && sceneStateForBg.nsfw),
                statusHud: buildStatusHudForSnapshot(readerSettings, sceneStateForBg && sceneStateForBg.nsfw ? '' : resolvedSpeaker, sceneStateForBg && sceneStateForBg.nsfw ? '' : bubbleMood, statusSceneInfo, textType === 'narration' || textType === 'thought'),
            },
            readerSettings: cloneData(readerSettings),
            input: {
                placeholder: '输入内容后按 Enter 发送',
                enterSends: true,
                shiftEnterSends: false,
            },
            html: `<div id="igs-overlay" class="${overlayClasses.join(' ')}" data-igs-igs-ui="true">${getOriginalReaderHtml()}</div>`,
            source: getOriginalReaderSource(options.version || '0.5.4'),
        };
    }

    function buildSettingsSnapshot(settingsState) {
        const draft = normalizeUnifiedSettings(settingsState.draft);
        const tab = normalizeSettingsTab(settingsState.tab);
        const readerSubTab = tab === 'reader' ? normalizeReaderSubTab(settingsState.asyncState.readerSubTab) : null;
        const sceneSettingsSubTab = tab === 'scene' ? normalizeSceneSettingsSubTab(settingsState.asyncState.sceneSettingsSubTab) : null;
        const sceneSubTab = tab === 'scene' ? (settingsState.asyncState.sceneSubTab === 'characters' ? 'characters' : 'scenes') : null;
        const settingsTheme = draft.bridge.settingsTheme === 'day' ? 'day' : 'night';
        const nextSettingsTheme = settingsTheme === 'day' ? 'night' : 'day';
        const settingsThemeLabel = nextSettingsTheme === 'day' ? '切换到日间模式' : '切换到夜间模式';
        const body = renderSettingsBody(tab, draft, settingsState.asyncState);
        const tabsHtml = SETTINGS_TAB_DEFS.map(([id, label]) => {
            return `<button type="button" class="igs-settings-tab${tab === id ? ' is-active' : ''}" data-tab="${id}">${label}</button>`;
        }).join('');

        return {
            tab,
            readerSubTab,
            sceneSettingsSubTab,
            sceneSubTab,
            settingsTheme,
            settingsThemeIcon: getSettingsThemeIcon(nextSettingsTheme),
            settingsThemeLabel,
            settingsThemePressed: settingsTheme === 'day' ? 'true' : 'false',
            selectors: Array.from(SETTINGS_PANEL_REQUIRED_SELECTORS),
            tabs: SETTINGS_TAB_DEFS.map(([id, label]) => ({
                id,
                label,
                active: id === tab,
                requiredPaths: Array.from((SETTINGS_PANEL_TAB_CONTRACT[id] || {}).requiredPaths || []),
                requiredActions: Array.from((SETTINGS_PANEL_TAB_CONTRACT[id] || {}).requiredActions || []),
            })),
            activeContract: SETTINGS_PANEL_TAB_CONTRACT[tab],
            html: `<div id="igs-unified-settings" data-igs-igs-ui="true" data-igs-settings-theme="${settingsTheme}">${renderTemplate(getSettingsShellTemplate(), {
                version: esc(options.version || '0.5.4'),
                tabs: tabsHtml,
                body,
                settingsThemeIcon: getSettingsThemeIcon(nextSettingsTheme),
                settingsThemeLabel,
                settingsThemePressed: settingsTheme === 'day' ? 'true' : 'false',
            })}</div>`,
            resultText: {
                image: settingsState.asyncState.imageResult || '',
                imageModels: settingsState.asyncState.imageModelsMessage || '',
                virtualRegex: settingsState.asyncState.virtualRegexPreview || '',
                promptRule: settingsState.asyncState.promptRuleStatus || '',
                promptRuleDraft: settingsState.asyncState.promptRuleDraft,
            },
            draft,
        };
    }

    function renderSettingsBody(tab, draft, asyncState) {
        function buildStatusHudSettingsHtml(reader, options) {
            const statusHud = normalizeStatusHudSettings(reader && reader.statusHud);
            const sectionClass = 'igs-settings-section igs-settings-full igs-status-hud-section';
            const toggle = `<div class="igs-settings-row">${checkbox('readerSettings.statusHud.enabled', statusHud.enabled, '显示左上角状态栏')}</div>`;
            if (!statusHud.enabled) {
                return `<div class="${sectionClass}" data-status-hud>${toggle}</div>`;
            }
            const api = (options.global || globalThis).AutoCardUpdaterAPI || null;
            const listed = api ? listStatusHudTables(createShujukuClient(api).readTables()) : { ok: false, reason: 'missing-api', tables: [] };
            const catalogNote = listed.ok
                ? '勾选后额外显示当前角色匹配到的 HUD 条；留空则只显示已开启的人物信息与旁白场景信息。'
                : '数据库插件未就绪，人物信息与旁白场景信息仍可显示；已保存的表格选择会保留。';
            const body = [
                toggle,
                `<div class="igs-settings-row">${checkbox('readerSettings.statusHud.showEmotion', statusHud.showEmotion, '显示情绪标签')}</div>`,
                `<div class="igs-settings-row">${checkbox('readerSettings.statusHud.showLocation', statusHud.showLocation, '显示地点栏（仅旁白）')}</div>`,
                statusHud.showLocation ? `<div class="igs-settings-row">${checkbox('readerSettings.statusHud.showLocationDetails', statusHud.showLocationDetails, '显示更多的场景信息')}</div>` : '',
                `<div class="igs-settings-row">${field('readerSettings.statusHud.avatarRadius', '头像圆角', selectInput('readerSettings.statusHud.avatarRadius', statusHud.avatarRadius, [['square', '方角'], ['soft', '微圆角'], ['small', '小圆角'], ['medium', '中圆角'], ['large', '大圆角'], ['circle', '圆形']]))}</div>`,
                `<div class="igs-settings-row">${field('readerSettings.statusHud.size', '状态栏大小', segmentedInput('readerSettings.statusHud.size', statusHud.size, [['small', '小'], ['medium', '中'], ['large', '大']], '状态栏大小'))}</div>`,
                `<div class="igs-settings-row">${field('readerSettings.statusHud.background', '状态栏背景', segmentedInput('readerSettings.statusHud.background', statusHud.background, [['none', '无背景'], ['dialog', '跟随对话框']], '状态栏背景'))}</div>`,
                `<div class="igs-settings-row">${field('readerSettings.statusHud.barColor', 'HUD条配色', segmentedInput('readerSettings.statusHud.barColor', statusHud.barColor, [['color', '彩色'], ['grayscale', '灰白']], 'HUD条配色'))}</div>`,
                `<div class="igs-settings-row igs-settings-full"><div class="igs-settings-field"><span>读取表格</span>${tableMultiSelect('readerSettings.statusHud.tables', statusHud.tables, listed.tables, { note: catalogNote })}</div></div>`,
            ].join('');
            return `<div class="${sectionClass}" data-status-hud>${body}</div>`;
        }

        const bridge = draft.bridge;
        const imageApi = bridge.imageApi;
        const sourceFilter = bridge.sourceFilter;
        const reader = draft.readerSettings;

        if (tab === 'basic') {
            return renderTemplate(getSettingsTabTemplate('basic'), {
                openModeField: `<div class="igs-settings-full igs-segmented-field">${field(
                    'bridge.openMode',
                    '切换模式',
                    segmentedInput(
                        'bridge.openMode',
                        bridge.openMode,
                        PUBLIC_READER_MODES.map((id) => [id, getReaderModeLabel(id), getReaderModeIcon(id)]),
                        '切换模式',
                    ),
                )}</div>`,
                settingsToggles: checkbox('bridge.showToasts', bridge.showToasts, '显示提示 toast'),
            });
        }

        if (tab === 'regex') {
            return renderTemplate(getSettingsTabTemplate('regex'), {
                filterToggles: checkbox('bridge.sourceFilter.enabled', sourceFilter.enabled, '启用标签筛选')
                    + checkbox('bridge.sourceFilter.stripHtmlComments', sourceFilter.stripHtmlComments, '排除 HTML 注释'),
                untaggedToggle: checkbox(
                    'bridge.sourceFilter.allowUntaggedFallback',
                    sourceFilter.allowUntaggedFallback,
                    '正文保留标签为空时读取清洗全文',
                ),
                textIncludeField: field('bridge.sourceFilter.textIncludeTags', '正文保留标签', textareaInput('bridge.sourceFilter.textIncludeTags', sourceFilter.textIncludeTags, 'content')),
                textExcludeField: field('bridge.sourceFilter.textExcludeTags', '正文排除标签', textareaInput('bridge.sourceFilter.textExcludeTags', sourceFilter.textExcludeTags)),
                imageIncludeField: field('bridge.sourceFilter.imageIncludeTags', '图片保留标签', textareaInput('bridge.sourceFilter.imageIncludeTags', sourceFilter.imageIncludeTags, 'image&#10;text_to_image')),
                regexToggle: checkbox('bridge.virtualRegex.enabled', bridge.virtualRegex.enabled, '启用正文格式化'),
                regexPatternField: field('bridge.virtualRegex.pattern', '查找表达式', textareaInput('bridge.virtualRegex.pattern', bridge.virtualRegex.pattern, '^@bubble:([^|\\n]+)\\|[^|\\n]*\\|\\[?([^\\n]*?)\\]?$')),
                regexFlagsField: field('bridge.virtualRegex.flags', 'flags', textInput('bridge.virtualRegex.flags', bridge.virtualRegex.flags, 'i'), '例如 i、g、s、m；留空表示无 flags。'),
                regexReplacementField: field('bridge.virtualRegex.replacement', '替换文本', textareaInput('bridge.virtualRegex.replacement', bridge.virtualRegex.replacement, '[$1]：$2')),
                regexPreview: esc(asyncState.virtualRegexPreview || '点击“测试当前楼层”预览最终 IGS 正文。'),
            });
        }

        if (tab === 'image') {
            const apiDisabled = imageApi.mode !== 'nai';
            const imageModeNote = imageApi.mode === 'nai'
                ? '内置模式会直接调用图像 API，不再依赖外部插图扩展。'
                : '外部扩展模式会优先按适配器检测 chatu8 / chami。';
            const promptPrefixInput = `<textarea data-path="bridge.imageApi.promptPrefix" placeholder="可选，生成图片时追加到正文前"${disabledAttr(apiDisabled)}>${esc(imageApi.promptPrefix || '')}</textarea>`;
            return renderTemplate(getSettingsTabTemplate('image'), {
                imageModeField: field('bridge.imageApi.mode', '图像模式', selectInput('bridge.imageApi.mode', imageApi.mode, [['extension', '使用现有插图扩展'], ['nai', 'IGS 内置 NAI API']])),
                adapterField: field('bridge.imageApi.externalAdapter', '插图扩展', selectInput('bridge.imageApi.externalAdapter', imageApi.externalAdapter, [['auto', '自动检测'], ['chatu8', 'st-chatu8 / chatu8'], ['chami', 'chami_tavern-scene-plugin']], imageApi.mode === 'nai'), imageModeNote),
                apiGroupClass: 'igs-settings-api-group' + (apiDisabled ? ' is-disabled' : ''),
                endpointField: field('bridge.imageApi.endpoint', '图像 API 地址', textInput('bridge.imageApi.endpoint', imageApi.endpoint, 'https://...', 'text', apiDisabled), apiDisabled ? '内置 NAI API 模式启用时可编辑。' : ''),
                apiKeyField: field('bridge.imageApi.apiKey', 'API Key', secretInput('bridge.imageApi.apiKey', imageApi.apiKey, '留空则不发送 Authorization', apiDisabled)),
                modelField: field('bridge.imageApi.model', '模型', modelPicker('bridge.imageApi.model', imageApi.model, imageApi.availableModels, 'fetch-image-models', 'nai-diffusion-3', apiDisabled)),
                sizeField: field('bridge.imageApi.size', '尺寸', textInput('bridge.imageApi.size', imageApi.size, '832x1216', 'text', apiDisabled)),
                stepsField: field('bridge.imageApi.steps', '步数', numberInput('bridge.imageApi.steps', imageApi.steps, 1, 100, apiDisabled)),
                samplerField: field('bridge.imageApi.sampler', '采样器', textInput('bridge.imageApi.sampler', imageApi.sampler, 'k_euler_ancestral', 'text', apiDisabled)),
                timeoutField: field('bridge.imageApi.requestTimeoutMs', '请求超时 ms', numberInput('bridge.imageApi.requestTimeoutMs', imageApi.requestTimeoutMs, 5000, 300000, apiDisabled)),
                pollIntervalField: field('bridge.imageApi.pollIntervalMs', '轮询间隔 ms', numberInput('bridge.imageApi.pollIntervalMs', imageApi.pollIntervalMs, 500, 30000, apiDisabled)),
                pollAttemptsField: field('bridge.imageApi.pollAttempts', '轮询次数', numberInput('bridge.imageApi.pollAttempts', imageApi.pollAttempts, 1, 240, apiDisabled)),
                promptPrefixField: field('bridge.imageApi.promptPrefix', '图像提示词前缀', promptPrefixInput),
                imageModelsMessage: esc(asyncState.imageModelsMessage || (apiDisabled ? '使用现有插图扩展时无需拉取内置 API 模型；检测插件会检查 chatu8 / chami 链路。' : '可手填模型，也可点击拉取模型获取候选列表。')),
                imageTestActionLabel: imageApi.mode === 'nai' ? '测试生成' : '检测插件',
                imageTestHelp: esc(asyncState.imageResult || (imageApi.mode === 'nai' ? '测试会对当前内置 API 发起真实生成请求；建议先填写 endpoint、模型和 key。' : '检测当前页面可用的外部插图扩展与图片/按钮链路。')),
            });
        }

        if (tab === 'scene') {
            const sceneAssets = bridge.sceneAssets || {};
            const disabled = !sceneAssets.enabled;
            const sceneSettingsSubTab = normalizeSceneSettingsSubTab(asyncState.sceneSettingsSubTab);
            const subTab = asyncState.sceneSubTab === 'characters' ? 'characters' : 'scenes';
            const scenesHtml = renderSceneAssetList(sceneAssets.scenes || {}, {
                expandedSlots: asyncState.expandedSceneSlots instanceof Set ? asyncState.expandedSceneSlots : new Set(),
                timeGroups: sceneAssets.timeGroups || [],
                weatherGroups: sceneAssets.weatherGroups || [],
            });
            const charsHtml = renderCharacterAssetList(sceneAssets.characters || {}, {
                aliases: sceneAssets.characterAliases || {},
                moodGroups: sceneAssets.moodGroups || [],
                expandedSlots: asyncState.expandedSpriteSlots instanceof Set ? asyncState.expandedSpriteSlots : new Set(),
                statusAvatars: sceneAssets.statusAvatars || {},
            });
            const scenePresets = loadScenePresets((options.global || globalThis).localStorage);
            const scenePresetBarHtml = renderScenePresetBar(scenePresets, asyncState.scenePresetName || '');
            const subTabsHtml = `<div class="igs-scene-subtabs" role="tablist">`
                + `<button type="button" class="igs-scene-subtab${subTab === 'scenes' ? ' is-active' : ''}" data-scene-subtab="scenes">场景素材</button>`
                + `<button type="button" class="igs-scene-subtab${subTab === 'characters' ? ' is-active' : ''}" data-scene-subtab="characters">角色立绘</button>`
                + `</div>`;
            const scenesPane = `<div class="igs-source-filter">
        <div style="display:flex;align-items:center;justify-content:space-between">
          <div class="igs-source-filter-title">背景场景</div>
          <button class="igs-btn-mgr-icon" data-action="scene-add-bg" type="button" title="添加背景图">+</button>
        </div>
        <div class="igs-source-filter-note">场景名及其别名共用同一套背景；名为「默认」的条目在无匹配时兜底。子层级（时间→天气）优先级依次升高。</div>
        ${scenesHtml}
      </div>`;
            const charactersPane = `<div class="igs-source-filter">
        <div style="display:flex;align-items:center;justify-content:space-between">
          <div class="igs-source-filter-title">角色立绘</div>
          <button class="igs-btn-mgr-icon" data-action="scene-add-char" type="button" title="添加角色">+</button>
        </div>
        <div class="igs-source-filter-note">角色名及其别名共用同一套情绪立绘与位置；展开情绪槽可预览立绘并编辑情绪词库。</div>
        <div class="igs-settings-row">${checkbox('bridge.sceneAssets.unifiedSpriteLayout', sceneAssets.unifiedSpriteLayout, '统一角色立绘位置（各情绪共用一套位置）')}</div>
        ${charsHtml}
        <div class="igs-settings-row"><button class="igs-settings-action" data-action="reset-mood-groups" type="button">恢复默认词库</button></div>
      </div>`;
            const sceneSettingsSubTabs = SCENE_SETTINGS_SUBTAB_DEFS.map(([id, label]) => (
                `<button type="button" class="igs-scene-settings-subtab${sceneSettingsSubTab === id ? ' is-active' : ''}" data-scene-settings-subtab="${id}" role="tab" aria-selected="${sceneSettingsSubTab === id ? 'true' : 'false'}">${label}</button>`
            )).join('');
            const promptRuleDraft = typeof asyncState.promptRuleDraft === 'string'
                ? asyncState.promptRuleDraft
                : String(sceneAssets.promptRule || '');
            const sceneValues = {
                sceneGroupClass: `igs-settings-section igs-settings-full${disabled ? ' igs-settings-api-group is-disabled' : ''}`,
                promptRuleField: field('bridge.sceneAssets.promptRule', '注入提示词', `<textarea data-prompt-rule-draft="1" placeholder="格式规则..."${disabled ? ' disabled' : ''}>${esc(promptRuleDraft)}</textarea>`),
                promptRuleStatus: esc(asyncState.promptRuleStatus || '修改后点击右侧“保存提示词”才会覆盖当前规则。'),
                scenePresetBar: scenePresetBarHtml,
                sceneSubTabs: subTabsHtml,
                sceneSubPane: subTab === 'characters' ? charactersPane : scenesPane,
            };
            return renderTemplate(getSettingsTabTemplate('scene'), {
                sceneToggle: checkbox('bridge.sceneAssets.enabled', sceneAssets.enabled, '启用场景素材模式'),
                sceneSettingsSubTabs,
                sceneSettingsSubPane: renderTemplate(getSceneSettingsSubTabTemplate(sceneSettingsSubTab), sceneValues),
            });
        }

        const readerSubTab = normalizeReaderSubTab(asyncState.readerSubTab);
        const sceneEnabled = !!(bridge.sceneAssets && bridge.sceneAssets.enabled);
        const classicDialog = reader.dialogSkin === DIALOG_SKIN_WESTERN_CLASSIC;
        const themeDisabled = !sceneEnabled && !classicDialog;
        const themePath = classicDialog ? 'readerSettings.classicVnTheme' : 'readerSettings.vnTheme';
        const vnTheme = reader.vnTheme || {};
        const classicVnTheme = reader.classicVnTheme || CLASSIC_DIALOG_THEME_DEFAULTS;
        // 对话主题已取消预设选择，恒为自定义：自定义项始终可编辑（仅受场景素材开关 themeDisabled 控制）。
        const themeCustom = true;
        const displayTheme = classicDialog ? classicVnTheme : vnTheme;
        const dialogHeightItems = [['null', '自适应'], [60, '60px'], [80, '80px'], [120, '120px'], [160, '160px'], [180, '180px'], [200, '200px'], [250, '250px'], [300, '300px'], [400, '400px'], [500, '500px'], [600, '600px']];
        if (reader.dialogHeight != null && !dialogHeightItems.some(([value]) => String(value) === String(reader.dialogHeight))) {
            dialogHeightItems.splice(1, 0, [reader.dialogHeight, `${reader.dialogHeight}px（保留）`]);
        }
        const readerSubTabs = READER_SUBTAB_DEFS.map(([id, label]) => (
            `<button type="button" class="igs-reader-subtab${readerSubTab === id ? ' is-active' : ''}" data-reader-subtab="${id}" role="tab" aria-selected="${readerSubTab === id ? 'true' : 'false'}">${label}</button>`
        )).join('');
        const readerValues = {
            fontSizeField: field('readerSettings.fontSize', '字体大小', selectInput('readerSettings.fontSize', reader.fontSize, [12, 13, 14, 15, 16, 18, 20, 22, 24, 26, 28, 30].map((n) => [n, `${n}px`]))),
            dialogSkinField: field('readerSettings.dialogSkin', '对话框风格', selectInput('readerSettings.dialogSkin', reader.dialogSkin, [['default', '默认'], ['western-classic', '西欧古典']])),
            classicDialogWidthPercentField: classicDialog ? field('readerSettings.classicDialogWidthPercent', '电脑端宽度', selectInput('readerSettings.classicDialogWidthPercent', reader.classicDialogWidthPercent, [60, 70, 80, 90, 100].map((n) => [n, `${n}%`])), '按阅读器可用宽度自动计算；手机端保持 100%。') : '',
            optionFontSizeField: field('readerSettings.optionFontSize', '选项字体大小', selectInput('readerSettings.optionFontSize', reader.optionFontSize, [10, 11, 12, 13, 14, 15, 16, 18, 20, 22, 24].map((n) => [n, `${n}px`]))),
            dialogWidthField: field('readerSettings.dialogWidth', '对话框宽度', selectInput('readerSettings.dialogWidth', reader.dialogWidth === null ? 'null' : reader.dialogWidth, [['null', '自动'], [200, '200px'], [280, '280px'], [360, '360px'], [440, '440px'], [520, '520px'], [600, '600px'], [680, '680px'], [760, '760px'], [840, '840px'], [920, '920px'], [1000, '1000px'], [1080, '1080px'], [1160, '1160px'], [1280, '1280px']], classicDialog), classicDialog ? '西欧古典请在「主题」页按比例调整；原像素值会保留。' : ''),
            dialogHeightField: field('readerSettings.dialogHeight', '对话框高度', selectInput('readerSettings.dialogHeight', reader.dialogHeight === null ? 'null' : reader.dialogHeight, dialogHeightItems, classicDialog), classicDialog ? `当前风格使用固定 ${CLASSIC_DIALOG_HEIGHT}px；原设置值会保留。` : ''),
            glassOpacityField: field('readerSettings.glassOpacity', '玻璃浓度', selectInput('readerSettings.glassOpacity', reader.glassOpacity, [0, .1, .2, .35, .5, .62, .74, .88, 1].map((n) => [n, `${Math.round(n * 100)}%`])), classicDialog ? '不影响素材对话框，仍作用于工具栏、选项和数据库。' : ''),
            imageCountField: field('readerSettings.imageCountOverride', '检测图像数量', selectInput('readerSettings.imageCountOverride', reader.imageCountOverride === null ? 'null' : reader.imageCountOverride, [['null', '自动']].concat(Array.from({ length: 20 }, (_, index) => [index + 1, `${index + 1}张`])))),
            inputScaleField: field('readerSettings.inputScale', '输入框高度', selectInput('readerSettings.inputScale', reader.inputScale, [20, 40, 60, 80, 100, 120, 140, 160, 180, 200].map((n) => [n, `${n}%`]))),
            toolbarScaleField: field('readerSettings.toolbarScale', '工具栏大小', selectInput('readerSettings.toolbarScale', reader.toolbarScale, [20, 40, 60, 80, 100, 120, 140, 160, 180, 200].map((n) => [n, `${n}%`]))),
            toolbarDockField: field('readerSettings.toolbarDock', '工具栏位置', selectInput('readerSettings.toolbarDock', reader.toolbarDock || 'float', [['float', '悬浮'], ['top', '顶部固定']])),
            imgModeField: field('readerSettings.imgMode', '图像显示模式', selectInput('readerSettings.imgMode', reader.imgMode, [['adaptive', '自适应'], ['contain', '完整']])),
            imgBrightnessField: field('readerSettings.imgBrightness', '图片亮度', selectInput('readerSettings.imgBrightness', reader.imgBrightness, [50, 60, 70, 80, 88, 90, 100].map((n) => [n, `${n}%`]))),
            readerToggles: checkbox('readerSettings.glassBackdropFilter', reader.glassBackdropFilter, '启用背景滤镜')
                + checkbox('readerSettings.statusHud.dimSpriteOnNarration', reader.statusHud && reader.statusHud.dimSpriteOnNarration !== false, '启用人物过场滤镜（仅旁白）')
                + checkbox('bridge.sentencePaging', Boolean(bridge.sentencePaging), '按照句号自动分页（仅旁白）')
                + checkbox('readerSettings.statusHud.showSpriteOnNsfw', !reader.statusHud || reader.statusHud.showSpriteOnNsfw !== false, '显示NSFW场景下的人物立绘')
                + checkbox('readerSettings.showStatusLine', reader.showStatusLine, '显示对话框内状态行'),
            nsfwVeilLevelField: `<div class="igs-settings-row">${field('readerSettings.statusHud.nsfwVeilLevel', 'NSFW黑幕强度', segmentedInput('readerSettings.statusHud.nsfwVeilLevel', (reader.statusHud && reader.statusHud.nsfwVeilLevel) ||'medium', [['light', '弱'], ['medium', '中'], ['strong', '强']], 'NSFW黑幕强度'))}</div>`,
            statusHudSection: buildStatusHudSettingsHtml(reader, options),
            optionBubbleToggle: checkbox('bridge.optionBubble.enabled', Boolean(bridge.optionBubble && bridge.optionBubble.enabled), '启用选项气泡'),
            optionBubblePositionField: field('bridge.optionBubble.position', '气泡位置', segmentedInput('bridge.optionBubble.position', (bridge.optionBubble && bridge.optionBubble.position) || 'top-left', [['top-left', '左上角'], ['top-center', '正上方居中'], ['top-right', '右上角']], '气泡位置')),
            optionBubbleActionField: field('bridge.optionBubble.clickAction', '点击选项', segmentedInput('bridge.optionBubble.clickAction', (bridge.optionBubble && bridge.optionBubble.clickAction) || 'send', [['send', '自动发送'], ['fill', '填入输入框']], '点击行为')),
            optionBubbleWidthToggle: checkbox('bridge.optionBubble.widthFollowsText', Boolean(bridge.optionBubble && bridge.optionBubble.widthFollowsText), '气泡宽度随文本变化'),
            pinnedButtonsField: renderPinnedButtons(reader.pinnedBtns, reader.hiddenBtns, reader.btnOrder),
            themeGroupClass: `igs-source-filter igs-settings-full${themeDisabled ? ' igs-settings-api-group is-disabled' : ''}`,
            themeNote: classicDialog ? '当前编辑西欧古典风格的文字外观；默认风格配置会保留。' : '仅在启用场景素材模式时可用；默认风格配置独立保存。',
            themePresetField: '',
            nameAlignField: field(`${themePath}.nameAlign`, '对齐', selectInput(`${themePath}.nameAlign`, displayTheme.nameAlign || 'left', [['left', '左对齐'], ['center', '居中'], ['indent', '首行缩进']], themeDisabled || !themeCustom)),
            textAlignField: field(`${themePath}.textAlign`, '对齐', selectInput(`${themePath}.textAlign`, displayTheme.textAlign || 'left', [['left', '左对齐'], ['center', '居中'], ['indent', '首行缩进']], themeDisabled || !themeCustom)),
            narrationAlignField: field(`${themePath}.narrationAlign`, '对齐', selectInput(`${themePath}.narrationAlign`, displayTheme.narrationAlign || 'left', [['left', '左对齐'], ['center', '居中'], ['indent', '首行缩进']], themeDisabled || !themeCustom)),
            thoughtAlignField: field(`${themePath}.thoughtAlign`, '对齐', selectInput(`${themePath}.thoughtAlign`, displayTheme.thoughtAlign || 'left', [['left', '左对齐'], ['center', '居中'], ['indent', '首行缩进']], themeDisabled || !themeCustom)),
            dividerField: field(`${themePath}.dividerSymbol`, '样式', selectInput(`${themePath}.dividerSymbol`, displayTheme.dividerSymbol || 'none', [['gradient', '渐变线'], ['none', '无']], themeDisabled || classicDialog || !themeCustom), classicDialog ? '姓名牌风格不显示额外分隔线。' : ''),
            nameFontField: field(`${themePath}.nameFont`, '字体', selectInput(`${themePath}.nameFont`, displayTheme.nameFont || 'inherit', [['inherit', '默认'], ['"KaiTi","STKaiti",serif', '楷体'], ['"SimHei",sans-serif', '黑体'], ['"FangSong","STFangsong",serif', '仿宋'], ['"Microsoft YaHei",sans-serif', '微软雅黑']], themeDisabled || !themeCustom)),
            textFontField: field(`${themePath}.textFont`, '字体', selectInput(`${themePath}.textFont`, displayTheme.textFont || 'inherit', [['inherit', '默认'], ['"KaiTi","STKaiti",serif', '楷体'], ['"SimHei",sans-serif', '黑体'], ['"FangSong","STFangsong",serif', '仿宋'], ['"Microsoft YaHei",sans-serif', '微软雅黑']], themeDisabled || !themeCustom)),
            thoughtFontField: field(`${themePath}.thoughtFont`, '字体', selectInput(`${themePath}.thoughtFont`, displayTheme.thoughtFont || 'inherit', [['inherit', '默认'], ['"KaiTi","STKaiti",serif', '楷体'], ['"SimHei",sans-serif', '黑体'], ['"FangSong","STFangsong",serif', '仿宋'], ['"Microsoft YaHei",sans-serif', '微软雅黑']], themeDisabled || !themeCustom)),
            nameColorField: field(`${themePath}.nameColor`, '颜色', colorInput(`${themePath}.nameColor`, toHex(displayTheme.nameColor || '#ffeeb8'), themeDisabled || !themeCustom)),
            textColorField: field(`${themePath}.textColor`, '颜色', colorInput(`${themePath}.textColor`, toHex(displayTheme.textColor || '#f4f4f6'), themeDisabled || !themeCustom)),
            thoughtColorField: field(`${themePath}.thoughtColor`, '颜色', colorInput(`${themePath}.thoughtColor`, toHex(displayTheme.thoughtColor || '#c8c8dc'), themeDisabled || !themeCustom)),
            narrationFontField: field(`${themePath}.narrationFont`, '字体', selectInput(`${themePath}.narrationFont`, displayTheme.narrationFont || 'inherit', [['inherit', '默认'], ['"KaiTi","STKaiti",serif', '楷体'], ['"SimHei",sans-serif', '黑体'], ['"FangSong","STFangsong",serif', '仿宋'], ['"Microsoft YaHei",sans-serif', '微软雅黑']], themeDisabled || !themeCustom)),
            narrationColorField: field(`${themePath}.narrationColor`, '颜色', colorInput(`${themePath}.narrationColor`, toHex(displayTheme.narrationColor || '#f4f4f6'), themeDisabled || !themeCustom)),
            dividerColorField: field(`${themePath}.dividerColor`, '颜色', colorInput(`${themePath}.dividerColor`, toHex(displayTheme.dividerColor || '#ffeeb8'), themeDisabled || classicDialog || !themeCustom)),
            dialogBgField: field(`${themePath}.dialogBg`, '背景色', colorInput(`${themePath}.dialogBg`, toHex(displayTheme.dialogBg || '#1f2225'), themeDisabled || classicDialog || !themeCustom)),
            themeAdvancedClass: themeCustom ? '' : 'igs-settings-api-group is-disabled',
        };
        return renderTemplate(getSettingsTabTemplate('reader'), {
            readerSubTabs,
            readerSubPane: renderTemplate(getReaderSubTabTemplate(readerSubTab), readerValues),
        });
    }

    // 顶部固定工具栏按钮区横向滚动：移动端 overlay 区域吞掉了原生触摸滚动，
    // 故照搬数据库标签栏的 JS 拖拽滚动（pointerdown 起点 → pointermove 改 scrollLeft），
    // 仅在内容溢出时启动，拖动后抑制紧随的 click 避免误触按钮。参照 panel-controller 的标签栏拖拽。
    function installToolbarDragScroll(root, doc) {
        if (!root || !doc || typeof root.addEventListener !== 'function') return;
        let strip = null;
        let active = false;
        let moved = false;
        let startX = 0;
        let startScroll = 0;
        let pointerId = null;
        let captured = false;
        let suppressClick = false;

        root.addEventListener('pointerdown', (event) => {
            const s = event.target && event.target.closest ? event.target.closest('#igs-bar-btns') : null;
            if (!s) return;
            if (s.scrollWidth <= s.clientWidth + 1) return;
            strip = s;
            active = true;
            moved = false;
            startX = Number(event.clientX) || 0;
            startScroll = Number(s.scrollLeft) || 0;
            pointerId = event.pointerId !== undefined ? event.pointerId : null;
            captured = false;
        });
        root.addEventListener('pointermove', (event) => {
            if (!active || !strip) return;
            const dx = (Number(event.clientX) || 0) - startX;
            if (!moved && Math.abs(dx) < 4) return;
            if (!moved) {
                moved = true;
                try {
                    if (pointerId != null && strip.setPointerCapture) {
                        strip.setPointerCapture(pointerId);
                        captured = true;
                    }
                } catch (err) { /* ignore */ }
                strip.classList.add('igs-bar-dragging');
            }
            strip.scrollLeft = startScroll - dx;
            if (event.cancelable) event.preventDefault();
        });
        const end = () => {
            if (!active) return;
            active = false;
            if (strip) {
                try { if (captured && pointerId != null && strip.releasePointerCapture) strip.releasePointerCapture(pointerId); } catch (err) { /* ignore */ }
                strip.classList.remove('igs-bar-dragging');
            }
            if (moved) {
                suppressClick = true;
                const win = doc.defaultView || globalThis;
                if (win && typeof win.setTimeout === 'function') win.setTimeout(() => { suppressClick = false; }, 160);
            }
            strip = null;
            pointerId = null;
            captured = false;
        };
        root.addEventListener('pointerup', end);
        root.addEventListener('pointercancel', end);
        // 拖动后抑制紧随的 click（capture 阶段拦在按钮 click 处理之前）。
        root.addEventListener('click', (event) => {
            if (!suppressClick) return;
            suppressClick = false;
            event.stopPropagation();
            event.preventDefault();
        }, true);
    }

    function mountReaderDom(snapshot, controller, payload = {}) {
        const rootDoc = getRootDocument(options.global);
        const messageDoc = isEmbeddedReaderMode(snapshot.mode)
            && payload.message
            && payload.message.element
            && payload.message.element.ownerDocument;
        const doc = messageDoc || rootDoc;
        if (!doc) return null;
        ensureStyleTag(doc, 'igs-overlay-style', getOriginalReaderStyleText());
        const existing = doc.getElementById('igs-overlay');
        if (existing) existing.remove();

        const root = doc.createElement('div');
        const embeddedMount = isEmbeddedReaderMode(snapshot.mode) ? mountEmbeddedRoot(doc, root, payload.message, snapshot.messageId) : null;
        if (!embeddedMount) {
            // 挂到 documentElement 而非 body：宿主移动端把 body 设为 position:fixed 且尺寸受限，
            // 会成为 overlay fixed 定位的包含块，导致 100% 取到 body 尺寸而非视口（阅读器被压成一小块）。
            (doc.documentElement || doc.body).appendChild(root);
        }
        installToolbarDragScroll(root, doc);
        root.addEventListener('click', async (event) => {
            const button = event.target.closest('[data-act]');
            if (!button) return;
            event.preventDefault();
            event.stopPropagation();
            const action = button.getAttribute('data-act');
            await controller.invokeAction(action);
        });
        root.addEventListener('keydown', async (event) => {
            if (event.target && event.target.id === 'igs-input') {
                const result = await controller.keydown({
                    key: event.key,
                    shiftKey: event.shiftKey,
                    value: event.target.value,
                });
                if (result.sent) {
                    event.preventDefault();
                }
            }
        });
        const keydownHandler = (event) => {
            if (!state.activeReader) return;
            const input = state.activeReader.dom && state.activeReader.dom.input;
            if (doc.activeElement === input && event.key !== 'Escape') return;
            if (event.key === 'Escape') {
                event.preventDefault();
                controller.close();
                return;
            }
            if (event.key === 'ArrowRight' || event.key === ' ') {
                event.preventDefault();
                controller.invokeAction('next');
                return;
            }
            if (event.key === 'ArrowLeft') {
                event.preventDefault();
                controller.invokeAction('prev');
                return;
            }
            if (event.key === 'h' || event.key === 'H') {
                event.preventDefault();
                controller.invokeAction('hide');
            }
        };
        if (typeof doc.addEventListener === 'function') {
            doc.addEventListener('keydown', keydownHandler, true);
        }
        const dbController = createDbPanelController(doc, options.global);
        const domState = {
            root,
            doc,
            dbController,
            embeddedMount,
            dispose() {
                if (typeof doc.removeEventListener === 'function') {
                    doc.removeEventListener('keydown', keydownHandler, true);
                }
                dbController.close();
                teardownEmbeddedMount(domState.embeddedMount);
                domState.embeddedMount = null;
            },
        };
        return domState;
    }

    // 把唯一 reader root 挂到最新 AI 楼层 .mes_text 的兄弟容器中，并隐藏宿主原文。
    // 定位不到楼层节点时返回 null，由调用方回退到全局挂载，避免整块 UI 打不开。
    function mountEmbeddedRoot(doc, root, message, messageId) {
        const element = message && message.element ? message.element : resolveLiveMessageElement(doc, messageId);
        if (!element) return null;
        const resolved = resolveEmbeddedHostParent(element);
        if (!resolved) return null;
        const targetDoc = resolved.mesText.ownerDocument || element.ownerDocument || doc;
        const host = ensureEmbeddedHost(resolved.parent, targetDoc, findEmbeddedHost(targetDoc));
        if (!host) return null;
        hideEmbeddedSourceText(resolved.mesText);
        if (root.classList) root.classList.add('igs-embedded-root');
        if (root.style) {
            root.style.width = '100%';
            root.style.height = '100%';
            root.style.position = 'relative';
            root.style.overflow = 'hidden';
        }
        host.appendChild(root);
        return { host, mesText: resolved.mesText, messageId, root };
    }

    function teardownEmbeddedMount(mount) {
        if (!mount) return;
        restoreEmbeddedSourceText(mount.mesText);
        if (mount.root && mount.root.classList) mount.root.classList.remove('igs-embedded-root');
        if (mount.root && mount.root.style) {
            mount.root.style.width = '';
            mount.root.style.height = '';
            mount.root.style.position = '';
            mount.root.style.overflow = '';
        }
        if (mount.host && typeof mount.host.remove === 'function') mount.host.remove();
    }

    function resolveLiveMessageElement(doc, messageId) {
        if (doc && messageId != null && typeof doc.querySelector === 'function') {
            const found = doc.querySelector(`#chat .mes[mesid="${messageId}"]`)
                || doc.querySelector(`#chat .mes[data-mesid="${messageId}"]`);
            if (found) return found;
        }
        const message = state.activeReader && state.activeReader.payload && state.activeReader.payload.message;
        return message && message.element ? message.element : null;
    }

    function mountSettingsDom(controller) {
        const doc = getRootDocument(options.global);
        if (!doc) return null;
        ensureStyleTag(doc, 'igs-unified-settings-style', getSettingsStyleText());
        const existing = doc.getElementById('igs-unified-settings');
        if (existing) existing.remove();

        const root = doc.createElement('div');
        // 与 #igs-overlay 一致挂到 documentElement：宿主移动端 body 为 position:fixed 时会成为
        // 独立层叠上下文，设置面板挂在 body 内时整体被压在 overlay 之下（z-index 翻不出 body），
        // 且 100vw/100dvh 取到受限的 body 尺寸而非视口。
        (doc.documentElement || doc.body).appendChild(root);
        root.addEventListener('click', async (event) => {
            const tab = event.target.closest('[data-tab]');
            if (tab) {
                controller.switchTab(tab.getAttribute('data-tab'));
                return;
            }
            const readerSubTab = event.target.closest('[data-reader-subtab]');
            if(readerSubTab) {
                controller.switchReaderSubTab(readerSubTab.getAttribute('data-reader-subtab'));
                return;
            }
            const sceneSettingsSubTab = event.target.closest('[data-scene-settings-subtab]');
            if (sceneSettingsSubTab) {
                controller.switchSceneSettingsSubTab(sceneSettingsSubTab.getAttribute('data-scene-settings-subtab'));
                return;
            }
            const sceneSubTab = event.target.closest('[data-scene-subtab]');
            if (sceneSubTab) {
                controller.switchSceneSubTab(sceneSubTab.getAttribute('data-scene-subtab'));
                return;
            }
            const sw = event.target.closest('[data-switch]');
            if (sw) {
                controller.toggle(sw.getAttribute('data-switch'));
                return;
            }
            const segment = event.target.closest('[data-segment-path]');
            if (segment) {
                controller.setValue(segment.getAttribute('data-segment-path'), segment.getAttribute('data-segment-value'));
                return;
            }
            const action = event.target.closest('[data-action]');
            if (action) {
                const actName = action.getAttribute('data-action');
                if (actName === 'toggle-secret') {
                    const wrap = action.closest('.igs-settings-secret');
                    const input = wrap ? wrap.querySelector('input') : null;
                    if (input) {
                        const show = input.type === 'password';
                        input.type = show ? 'text' : 'password';
                        action.textContent = show ? '隐藏' : '显示';
                        action.setAttribute('aria-pressed', show ? 'true' : 'false');
                    }
                    return;
                }
                if (actName.startsWith('sprite-preview:')) {
                    event.preventDefault();
                    const url = decodeURIComponent(actName.slice('sprite-preview:'.length));
                    showSpritePreviewOverlay(root, url);
                    return;
                }
                event.preventDefault();
                await controller.invoke(actName);
                return;
            }
        });
        root.addEventListener('input', (event) => {
            const target = event.target;
            if (!target || !target.getAttribute) return;
            if (target.getAttribute('data-prompt-rule-draft') !== null) {
                state.activeSettings.asyncState.promptRuleDraft = target.value;
                state.activeSettings.asyncState.promptRuleStatus = '有未保存的修改。';
                const status = root.querySelector('[data-result="prompt-rule"]');
                if (status) status.textContent = state.activeSettings.asyncState.promptRuleStatus;
                return;
            }
            if (target.type === 'color') return;
            const path = target.getAttribute('data-path');
            if (path) {
                controller.setValue(path, target.value);
                return;
            }
            const sceneBg = target.getAttribute('data-scene-bg');
            if (sceneBg) {
                controller.invoke('scene-set-bg-url:' + encodeURIComponent(sceneBg) + ':' + target.value);
                return;
            }
            const sceneTimeBg = target.getAttribute('data-scene-time-bg');
            const sceneTime = target.getAttribute('data-scene-time');
            const sceneWeatherBg = target.getAttribute('data-scene-weather-bg');
            const sceneWeather = target.getAttribute('data-scene-weather');
            if (sceneWeatherBg && sceneTime && sceneWeather) {
                controller.invoke('scene-set-weather-url:' + encodeURIComponent(sceneWeatherBg) + ':' + encodeURIComponent(sceneTime) + ':' + encodeURIComponent(sceneWeather) + ':' + target.value);
                return;
            }
            if (sceneTimeBg && sceneTime) {
                controller.invoke('scene-set-time-url:' + encodeURIComponent(sceneTimeBg) + ':' + encodeURIComponent(sceneTime) + ':' + target.value);
                return;
            }
            const sceneChar = target.getAttribute('data-scene-char');
            const sceneMood = target.getAttribute('data-scene-mood');
            if (sceneChar && sceneMood) {
                controller.invoke('scene-set-mood-url:' + encodeURIComponent(sceneChar) + ':' + encodeURIComponent(sceneMood) + ':' + target.value);
            }
        });
        root.addEventListener('change', (event) => {
            const modelSync = event.target && event.target.getAttribute ? event.target.getAttribute('data-model-sync') : '';
            if (modelSync) {
                controller.setValue(modelSync, event.target.value);
                return;
            }
            if (event.target && event.target.getAttribute && event.target.getAttribute('data-preset-select') !== null) {
                controller.invoke('scene-preset-apply:' + encodeURIComponent(event.target.value));
                return;
            }
            const statusAvatarChar = event.target && event.target.getAttribute ? event.target.getAttribute('data-status-avatar-char') : '';
            if (statusAvatarChar) {
                controller.invoke('status-avatar-set-url:' + encodeURIComponent(statusAvatarChar) + ':' + encodeURIComponent(event.target.value || ''));
                return;
            }
            const path = event.target && event.target.getAttribute ? event.target.getAttribute('data-path') : '';
            if (!path) return;
            controller.setValue(path, event.target.value);
        });
        root.addEventListener('keydown', (event) => {
            if (event.key === 'Escape') {
                event.preventDefault();
                controller.close();
            }
        });

        const domState = {
            root,
            doc,
            overlay: null,
            viewportHandler: null,
            viewportWindow: null,
            viewportRaf: null,
            dispose() {
                detachSettingsViewportEvents(domState);
            },
        };
        return domState;
    }

    function updateMountedReader(snapshot) {
        const current = state.activeReader;
        if (!current || !current.dom || !current.dom.root) return;
        const refs = hydrateReaderMount(current.dom.root, snapshot);
        current.dom.overlay = refs.overlay;
        current.dom.dialog = refs.dialog;
        current.dom.input = refs.input;
        current.dom.sendButton = refs.sendButton;
        current.dom.toast = refs.toast;
        current.dom.clickLayer = refs.clickLayer;
        current.dom.text = refs.text;
        current.dom.progress = refs.progress;
        if (current.dom.overlay) applyReaderSnapshotToDom(current.dom.overlay, snapshot, current, {
            hasActiveSettings: () => Boolean(state.activeSettings),
            closeSettings,
            handleReaderAction,
            handleBlankClick: () => handleOptionBubbleBlankClick(current, snapshot),
            isActiveReader: (reader) => state.activeReader === reader,
            closeReader,
            resolveAssetUrl: (url) => resolveReaderAssetUrl(url, current),
        });
        if (current.dom.progress) {
            current.dom.progress.textContent = formatReaderProgress(snapshot);
        }
        syncOptionBubblesAfterRender(current, snapshot);
    }

    function syncOptionBubblesAfterRender(current, snapshot) {
        const overlay = current && current.dom && current.dom.overlay;
        if (!overlay || !overlay.querySelector) return;
        const container = overlay.querySelector('#igs-option-bubbles');
        if (!container) return;
        const cfg = getOptionBubbleConfig();
        if (container.style && typeof container.style.setProperty === 'function') {
            container.style.setProperty('--igs-option-font-size', `${cfg.fontSize}px`);
        }
        const isLastPage = isReaderLastPage(snapshot);
        // 翻页离开最后一页 / 重渲染（如新回复到来）一律收起气泡，避免错页残留。
        if (!cfg.enabled || !isLastPage) {
            hideOptionBubbles(container);
        } else if (container.hasAttribute('hidden')) {
            showOptionBubbles(container, cfg, { silent: true });
        }
        // 把对话框实际高度/宽度写入 CSS 变量，供气泡定位在对话框正上方、宽度跟随对话框。
        const dialog = overlay.querySelector('#igs-dialog');
        if (dialog && typeof dialog.getBoundingClientRect === 'function') {
            const rect = dialog.getBoundingClientRect();
            const h = Math.round(rect.height || 0);
            if (h > 0) overlay.style.setProperty('--igs-dialog-h', `${h}px`);
            const w = Math.round(rect.width || 0);
            if (w > 0) overlay.style.setProperty('--igs-dialog-w', `${w}px`);
        }
        const toolbar = overlay.querySelector('#igs-ctrl-bar');
        if (toolbar && typeof toolbar.getBoundingClientRect === 'function') {
            const h = Math.round(toolbar.getBoundingClientRect().height || 0);
            if (h > 0) overlay.style.setProperty('--igs-toolbar-h', `${h}px`);
        }
    }

    function hydrateReaderMount(container, snapshot) {
        // 只在首次挂载时重建 DOM；后续渲染复用已有节点，
        // 避免浏览器对相同 URL 的背景图/立绘重新发起加载请求。
        let overlay = container.querySelector('#igs-overlay');
        if (!overlay) {
            clearChildren(container);
            container.innerHTML = snapshot.html;
            overlay = container.querySelector('#igs-overlay');
            if (!overlay) {
                overlay = buildFallbackReaderOverlay(container.ownerDocument || getRootDocument(options.global));
                if (overlay) container.appendChild(overlay);
            }
            normalizeReaderStableLayers(overlay);
        }
        return {
            overlay,
            dialog: overlay ? overlay.querySelector('#igs-dialog') : null,
            input: overlay ? overlay.querySelector('#igs-input') : null,
            sendButton: overlay ? overlay.querySelector('#igs-send-btn') : null,
            toast: overlay ? overlay.querySelector('#igs-toast') : null,
            clickLayer: overlay ? overlay.querySelector('#igs-click-layer') : null,
            text: overlay ? overlay.querySelector('#igs-text') : null,
            progress: overlay ? overlay.querySelector('#igs-progress') : null,
        };
    }



    function updateMountedSettings(snapshot) {
        const current = state.activeSettings;
        if (!current || !current.dom || !current.dom.root) return;
        const container = current.dom.root;
        const prevBody = container.querySelector('.igs-settings-body');
        const scrollTop = prevBody ? prevBody.scrollTop : 0;
        clearChildren(container);
        container.innerHTML = snapshot.html;
        current.dom.overlay = container.querySelector('#igs-unified-settings');
        if (!current.dom.overlay) {
            current.dom.overlay = buildFallbackSettingsOverlay(container.ownerDocument || getRootDocument(options.global), snapshot, {
                version: options.version,
                renderSettingsBody,
            });
            if (current.dom.overlay) container.appendChild(current.dom.overlay);
        }
        if (current.dom.overlay) {
            attachSettingsViewportEvents(current.dom, current.dom.overlay);
        }
        const nextBody = container.querySelector('.igs-settings-body');
        if (nextBody && scrollTop) nextBody.scrollTop = scrollTop;
    }


    function resolveBridgeConfigSnapshot(optionsForSnapshot = {}) {
        const getter = typeof options.getUnifiedSettings === 'function'
            ? options.getUnifiedSettings
            : () => ({ bridge: {}, readerSettings: {}, readerMode: 'pc', version: options.version || '0.5.4' });
        const snapshot = getter(optionsForSnapshot) || {};
        return normalizeUnifiedSettings(snapshot, optionsForSnapshot.mode);
    }

    function normalizeUnifiedSettings(snapshot, preferredMode) {
        const bridge = normalizeBridgeConfig(snapshot.bridge);
        const readerMode = normalizeReaderMode(firstDefined(snapshot.readerMode, preferredMode, bridge.openMode), bridge);
        const readerSettings = normalizeReaderSettings(snapshot.readerSettings, bridge.vnTheme);

        return {
            version: snapshot.version || options.version || '0.5.4',
            bridge,
            imageApi: bridge.imageApi,
            readerMode,
            readerSettings,
        };
    }

    function normalizeBridgeConfig(bridge) {
        const normalized = cloneData(bridge || {});
        normalized.openMode = normalizeReaderMode(normalized.openMode, normalized);
        normalized.showToasts = normalizeBoolean(normalized.showToasts, true);
        normalized.settingsTheme = normalized.settingsTheme === 'day' ? 'day' : 'night';
        normalized.debug = normalizeBoolean(normalized.debug, false);
        normalized.sourceFilter = normalizeSourceFilter(normalized.sourceFilter);
        normalized.virtualRegex = normalizeVirtualRegex(normalized.virtualRegex);
        normalized.sentencePaging = normalizeBoolean(normalized.sentencePaging, false);
        normalized.imageApi = normalizeImageApi(normalized.imageApi);
        normalized.sceneAssets = normalizeSceneAssets(normalized.sceneAssets);
        normalized.vnTheme = normalizeVnTheme(normalized.vnTheme);
        normalized.entry = normalizeEntryConfig(normalized.entry);
        normalized.optionBubble = normalizeOptionBubble(normalized.optionBubble);
        return normalized;
    }

    function normalizeOptionBubble(value) {
        const src = value && typeof value === 'object' ? value : {};
        const position = (src.position === 'top-center' || src.position === 'top-right')
            ? src.position
            : 'top-left';
        return {
            enabled: normalizeBoolean(src.enabled, false),
            position,
            clickAction: src.clickAction === 'fill' ? 'fill' : 'send',
            widthFollowsText: normalizeBoolean(src.widthFollowsText, false),
        };
    }

    function normalizeEntryConfig(value) {
        const src = value && typeof value === 'object' ? value : {};
        return {
            magic: normalizeBoolean(src.magic, true),
        };
    }

    function normalizeImageApi(value) {
        const normalized = {
            ...cloneData(DEFAULT_IMAGE_API),
            ...cloneData(value || {}),
        };
        normalized.availableModels = Array.isArray(normalized.availableModels)
            ? normalized.availableModels.filter(Boolean)
            : [];
        return normalized;
    }

    function normalizeSceneAssets(value) {
        const normalized = cloneData(value || {});
        normalized.enabled = normalizeBoolean(normalized.enabled, false);
        normalized.promptRule = normalizeScenePromptRule(normalized.promptRule);
        if (!normalized.scenes || typeof normalized.scenes !== 'object' || Array.isArray(normalized.scenes)) {
            normalized.scenes = {};
        }
        // migrate old string-value scenes to object format
        for (const key of Object.keys(normalized.scenes)) {
            const v = normalized.scenes[key];
            if (typeof v === 'string') normalized.scenes[key] = { url: v, times: {} };
            else if (v && typeof v === 'object' && !v.times) normalized.scenes[key].times = {};
            const sceneObj = normalized.scenes[key];
            for (const tKey of Object.keys(sceneObj.times || {})) {
                const tv = sceneObj.times[tKey];
                if (typeof tv === 'string') sceneObj.times[tKey] = { url: tv, weathers: {} };
                else if (tv && typeof tv === 'object' && !tv.weathers) sceneObj.times[tKey].weathers = {};
                const timeObj = sceneObj.times[tKey];
                for (const wKey of Object.keys(timeObj.weathers || {})) {
                    const wv = timeObj.weathers[wKey];
                    if (typeof wv === 'string') timeObj.weathers[wKey] = { url: wv, words: [] };
                }
            }
        }
        if (!normalized.characters || typeof normalized.characters !== 'object' || Array.isArray(normalized.characters)) {
            normalized.characters = {};
        }
        const rawCharacterAliases = normalized.characterAliases && typeof normalized.characterAliases === 'object' && !Array.isArray(normalized.characterAliases)
            ? normalized.characterAliases
            : {};
        normalized.characterAliases = {};
        const claimedCharacterNames = new Set(Object.keys(normalized.characters));
        for (const characterName of Object.keys(normalized.characters)) {
            const aliases = Array.isArray(rawCharacterAliases[characterName]) ? rawCharacterAliases[characterName] : [];
            const nextAliases = [];
            for (const aliasValue of aliases) {
                const alias = String(aliasValue || '').trim();
                if (!alias || claimedCharacterNames.has(alias)) continue;
                claimedCharacterNames.add(alias);
                nextAliases.push(alias);
            }
            normalized.characterAliases[characterName] = nextAliases;
        }
        normalized.moodGroups = normalizeMoodGroups(normalized.moodGroups);
        // init group arrays
        if (!Array.isArray(normalized.timeGroups)) normalized.timeGroups = [];
        if (!Array.isArray(normalized.weatherGroups)) normalized.weatherGroups = [];
        // migrate any embedded time/weather words into global groups
        for (const sceneObj of Object.values(normalized.scenes)) {
            for (const [tKey, timeObj] of Object.entries(sceneObj.times || {})) {
                if (!timeObj || typeof timeObj !== 'object') continue;
                if (Array.isArray(timeObj.words) && timeObj.words.length) {
                    let tg = normalized.timeGroups.find((g) => g.label === tKey);
                    if (!tg) { tg = { label: tKey, words: [] }; normalized.timeGroups.push(tg); }
                    for (const w of timeObj.words) if (!tg.words.includes(w)) tg.words.push(w);
                    delete timeObj.words;
                }
                for (const [wKey, wObj] of Object.entries(timeObj.weathers || {})) {
                    if (!wObj || typeof wObj !== 'object') continue;
                    if (Array.isArray(wObj.words) && wObj.words.length) {
                        let wg = normalized.weatherGroups.find((g) => g.label === wKey);
                        if (!wg) { wg = { label: wKey, words: [] }; normalized.weatherGroups.push(wg); }
                        for (const w of wObj.words) if (!wg.words.includes(w)) wg.words.push(w);
                        delete wObj.words;
                    }
                }
            }
        }
        normalized.unifiedSpriteLayout = normalizeBoolean(normalized.unifiedSpriteLayout, false);
        return normalized;
    }

    function normalizeVnTheme(value, fallback = VN_THEME_PRESETS.genshin) {
        const normalized = cloneData(value || {});
        // 对话主题已取消预设选择，恒为自定义；用 genshin 作为各字段的初值来源（fallback）。
        normalized.preset = 'custom';
        const normalizeAlign = (value, def) => (value === 'left' || value === 'center' || value === 'indent') ? value : def;
        normalized.nameAlign = normalizeAlign(normalized.nameAlign, fallback.nameAlign);
        normalized.textAlign = normalizeAlign(normalized.textAlign, fallback.textAlign || 'left');
        normalized.narrationAlign = normalizeAlign(normalized.narrationAlign, fallback.narrationAlign || 'left');
        normalized.thoughtAlign = normalizeAlign(normalized.thoughtAlign, fallback.thoughtAlign || 'left');
        // 分割线只保留「渐变线(gradient)」与「无(none)」；旧符号样式一律归到渐变线。
        const dividerSymbol = normalized.dividerSymbol == null ? fallback.dividerSymbol : normalized.dividerSymbol;
        normalized.dividerSymbol = dividerSymbol === 'none' ? 'none' : 'gradient';
        normalized.nameFont = normalized.nameFont || fallback.nameFont;
        normalized.textFont = normalized.textFont || fallback.textFont;
        normalized.thoughtFont = normalized.thoughtFont || fallback.thoughtFont;
        normalized.narrationFont = normalized.narrationFont || fallback.narrationFont;
        normalized.nameColor = normalized.nameColor || fallback.nameColor;
        normalized.textColor = normalized.textColor || fallback.textColor;
        normalized.thoughtColor = normalized.thoughtColor || fallback.thoughtColor;
        normalized.narrationColor = normalized.narrationColor || fallback.narrationColor;
        normalized.dividerColor = normalized.dividerColor || fallback.dividerColor;
        normalized.dialogBg = normalized.dialogBg || fallback.dialogBg;
        return normalized;
    }

    function normalizeReaderSettings(settings, legacyTheme) {
        const currentVersion = READER_SETTINGS_SCHEMA_VERSION;
        const src = (settings && typeof settings === 'object' && !Array.isArray(settings))
            ? cloneData(settings)
            : {};
        const base = {
            _v: currentVersion,
            dialogSkin: 'default',
            classicDialogWidthPercent: CLASSIC_DIALOG_WIDTH_PERCENT_DEFAULT,
            fontSize: 18,
            optionFontSize: 14,
            dialogWidth: null,
            dialogHeight: null,
            glassOpacity: 0.62,
            glassBackdropFilter: false,
            toolbarScale: 100,
            toolbarDock: 'float',
            inputScale: 100,
            imgMode: 'adaptive',
            imgBrightness: 88,
            showStatusLine: false,
            imageCountOverride: null,
            pinnedBtns: Array.from(DEFAULT_PINNED_TOOLBAR_BUTTONS),
            hiddenBtns: [],
            btnOrder: TOOLBAR_ACTIONS.map(([id]) => id),
            spriteLayouts: {},
        };
        const normalized = { ...base, ...src, _v: currentVersion };
        delete normalized.emptyBackgroundColor;
        normalized.dialogSkin = normalizeDialogSkin(normalized.dialogSkin);
        normalized.classicDialogWidthPercent = normalizeClassicDialogWidthPercent(normalized.classicDialogWidthPercent);
        normalized.fontSize = normalizeFiniteNumber(normalized.fontSize, base.fontSize);
        normalized.optionFontSize = clampNumber(normalizeFiniteNumber(normalized.optionFontSize, base.optionFontSize), 10, 30);
        normalized.dialogWidth = normalizeNullableNumber(normalized.dialogWidth);
        normalized.dialogHeight = normalizeNullableNumber(normalized.dialogHeight);
        if (normalized.dialogHeight != null && normalized.dialogSkin !== DIALOG_SKIN_WESTERN_CLASSIC) {
            normalized.dialogHeight = clampNumber(normalized.dialogHeight, 60, 600);
        }
        normalized.glassOpacity = normalizeOpacity(normalized.glassOpacity, base.glassOpacity);
        normalized.glassBackdropFilter = normalizeBoolean(normalized.glassBackdropFilter, base.glassBackdropFilter);
        normalized.toolbarScale = normalizeFiniteNumber(normalized.toolbarScale, base.toolbarScale);
        normalized.toolbarDock = normalized.toolbarDock === 'top' ? 'top' : 'float';
        normalized.inputScale = normalizeFiniteNumber(normalized.inputScale, base.inputScale);
        normalized.imgMode = normalized.imgMode === 'contain' ? 'contain' : 'adaptive';
        normalized.imgBrightness = clampNumber(normalizeFiniteNumber(normalized.imgBrightness, base.imgBrightness), 10, 100);
        normalized.showStatusLine = normalizeBoolean(normalized.showStatusLine, false);
        normalized.statusHud = normalizeStatusHudSettings(normalized.statusHud);
        normalized.imageCountOverride = normalizeNullableNumber(normalized.imageCountOverride);
        normalized.pinnedBtns = normalizePinnedButtons(normalized.pinnedBtns);
        normalized.hiddenBtns = normalizeHiddenButtons(normalized.hiddenBtns);
        normalized.btnOrder = normalizeBtnOrder(normalized.btnOrder);
        normalized.spriteLayouts = normalizeSpriteLayouts(normalized.spriteLayouts);
        // 对话主题（vnTheme）按模式存进 readerSettings。独立于 _v 门控处理，避免 schema 版本
        // 不符时被清空。settings.vnTheme 缺失时回退到旧的全局 bridge.vnTheme（legacyTheme），
        // 实现从全局存储到按模式存储的平滑迁移。
        const rawTheme = (src.vnTheme && typeof src.vnTheme === 'object')
            ? src.vnTheme
            : (legacyTheme && typeof legacyTheme === 'object' ? legacyTheme : null);
        normalized.vnTheme = normalizeVnTheme(rawTheme || {});
        normalized.classicVnTheme = normalizeVnTheme(src.classicVnTheme || {}, CLASSIC_DIALOG_THEME_DEFAULTS);
        return normalized;
    }

    function buildRegexPreview(bridge) {
        const filter = normalizeSourceFilter(bridge.sourceFilter);
        const virtualRegex = normalizeVirtualRegex(bridge.virtualRegex);
        const previewMessage = resolvePreviewMessage();
        const payload = buildIgsTextPayload(previewMessage, {
            sourceFilter: filter,
            virtualRegex,
        });
        if (!payload.formattedText) {
            return '当前没有可测试的正文内容。';
        }
        if (!virtualRegex.enabled || !virtualRegex.pattern) {
            return `formattedTextLength=${payload.formattedText.length}\n\n最终正文：\n${payload.formattedText}`;
        }
        return [
            `source=${payload.sourceKind}`,
            `tagTextLength=${String(payload.tagText || '').trim().length}`,
            `formattedTextLength=${payload.formattedText.length}`,
            `changed=${payload.virtualRegexChanged === true}`,
            payload.usedFallback ? 'fallback=true' : 'fallback=false',
            '',
            '最终正文：',
            payload.formattedText,
        ].join('\n');
    }

    function resolvePreviewMessage() {
        if (state.activeReader && state.activeReader.payload && state.activeReader.payload.message) {
            return state.activeReader.payload.message;
        }
        if (typeof options.getCurrentMessage === 'function') {
            const message = options.getCurrentMessage();
            if (message && typeof message === 'object' && typeof message.then !== 'function') {
                return message;
            }
        }
        return '';
    }

    function writeToast(message) {
        const current = state.activeReader;
        if (!current) return;
        const bridge = resolveBridgeConfigSnapshot({ mode: current.mode }).bridge;
        applyToastToReader(current, bridge.showToasts !== false, message);
    }

    function buildSpriteEditContext() {
        return {
            writeToast,
            closeSettings,
            resolveUnifiedSettings: (opts) => resolveBridgeConfigSnapshot(opts),
            saveReaderSettingsPatch,
        };
    }

    function saveReaderSettingsPatch(patch) {
        const save = typeof options.saveUnifiedSettings === 'function' ? options.saveUnifiedSettings : null;
        if (!save || !state.activeReader) return { ok: false, reason: 'missing-save-handler' };
        const mode = state.activeReader.mode;
        const unified = resolveBridgeConfigSnapshot({ mode });
        const result = save({ bridge: unified.bridge, readerMode: unified.readerMode, readerSettings: { ...unified.readerSettings, ...patch } });
        if (!result || result.ok === false) return result || { ok: false, reason: 'save-failed' };
        const refreshed = resolveBridgeConfigSnapshot({ mode });
        const readerSettings = normalizeReaderSettings(refreshed.readerSettings, refreshed.bridge.vnTheme);
        readerSettings._sceneAssets = refreshed.bridge.sceneAssets || null;
        readerSettings._sentencePaging = Boolean(refreshed.bridge.sentencePaging);
        readerSettings._vnTheme = readerSettings.vnTheme || null;
        state.activeReader.snapshot = buildReaderSnapshot(state.activeReader.payload, mode, readerSettings, state.activeReader.index);
        updateMountedReader(state.activeReader.snapshot);
        return result;
    }
}

function showSpritePreviewOverlay(root, url) {
    if (!root || !url) return;
    // 挂到设置面板的全屏容器 #igs-unified-settings（position:fixed + 视口变量，已知正常全屏），
    // 而非 doc.body —— 移动端宿主把 body 设为 position:fixed 且高度坍缩，挂 body 会被裁成顶部一条。
    const host = (root.id === 'igs-unified-settings' ? root : root.querySelector && root.querySelector('#igs-unified-settings'))
        || root;
    const doc = root.ownerDocument || root;
    const existing = host.querySelector ? host.querySelector('#igs-sprite-preview-overlay') : null;
    if (existing) existing.remove();
    const overlay = doc.createElement('div');
    overlay.id = 'igs-sprite-preview-overlay';
    overlay.className = 'igs-sprite-preview-overlay';
    const img = doc.createElement('img');
    img.className = 'igs-sprite-preview-img';
    img.src = url;
    overlay.appendChild(img);
    overlay.addEventListener('click', () => overlay.remove());
    host.appendChild(overlay);
}

function applyToastToReader(current, allowed, message) {
    if (!current || !message || allowed === false) return;
    clearReaderToast(current);
    current.toastMessage = String(message);
    const toast = current.dom && current.dom.overlay ? current.dom.overlay.querySelector("#igs-toast") : null;
    if (toast) {
        toast.textContent = current.toastMessage;
        toast.style.opacity = "1";
    }
    const win = current.dom && current.dom.overlay ? getOwnerWindow(current.dom.overlay) : null;
    const setter = win && typeof win.setTimeout === "function" ? win.setTimeout.bind(win) : setTimeout;
    current.toastTimer = setter(() => {
        current.toastMessage = "";
        if (toast) toast.style.opacity = "0";
        current.toastTimer = null;
    }, 1800);
}

function clearReaderToast(current) {
    if (!current) return;
    const win = current.dom && current.dom.overlay ? getOwnerWindow(current.dom.overlay) : null;
    const clearer = win && typeof win.clearTimeout === "function" ? win.clearTimeout.bind(win) : clearTimeout;
    if (current.toastTimer) {
        clearer(current.toastTimer);
        current.toastTimer = null;
    }
    current.toastMessage = "";
    const toast = current.dom && current.dom.overlay ? current.dom.overlay.querySelector("#igs-toast") : null;
    if (toast) {
        toast.textContent = "";
        toast.style.opacity = "0";
    }
}

function resolveReaderActionToast(result, messages) {
    if (!result) return messages.fallback;
    if (result.ok === false) {
        return result.reason || messages.fallback;
    }
    return messages.success;
}

function cloneReaderPayload(payload = {}) {
    const clone = {};
    for (const [key, value] of Object.entries(payload || {})) {
        clone[key] = key === "message" ? (value || null) : cloneData(value);
    }
    return clone;
}

const SCENE_TAG_LINE_RE = /^\[igs-scene:[^\]]*\]/;

function stripSceneDirectivesInline(rawText) {
    return String(rawText || '')
        .replace(/\[igs-scene:[^\]]*\]/g, '')
        .trim();
}

// 逐行剥离 [igs-scene:] 标签，丢弃剥离后为空的行，供兜底分段使用。
function stripSceneDirectiveLines(rawText) {
    return String(rawText || '').split('\n')
        .map((line) => line.replace(/\[igs-scene:[^\]]*\]/g, '').trim())
        .filter((line) => line.length > 0 && !SCENE_TAG_LINE_RE.test(line))
        .join('\n');
}


// 在原文里定位一段正文的位置：先精确匹配，失败再去掉排版符号后匹配。
// 纯函数、不做全局缓存，避免跨页状态残留。
function locateTextOffsetInSource(source, segText, from = 0) {
    const src = String(source || '');
    const needle = String(segText || '').trim();
    if (!src || !needle) return -1;
    const exact = src.indexOf(needle, Math.max(0, Number(from) || 0));
    if (exact >= 0) return exact;
    const loose = needle.replace(/[\s*（）\[\]]+/g, '');
    if (!loose) return -1;
    // 把原文与目标都去掉排版符号，再做一次真实匹配；命中后回推原文下标。
    const fromIndex = Math.max(0, Number(from) || 0);
    const map = [];
    let flat = '';
    for (let i = fromIndex; i < src.length; i += 1) {
        if (/[\s*（）\[\]]/.test(src[i])) continue;
        map.push(i);
        flat += src[i];
    }
    const hit = flat.indexOf(loose);
    if (hit >= 0) {
        const end = hit + loose.length - 1;
        if (end < map.length) return map[hit];
    }
    return -1;
}



__igsDefine(exports, "createIgsReaderHost", () => createIgsReaderHost);
});
__igsRegister("src/visual/igs-ui/original-reader-source.js", function(module, exports, require) {
const { CLASSIC_DIALOG_STYLE_TEXT } = require("src/visual/igs-ui/classic-dialog-skin.js");
const ORIGINAL_READER_ICONS = Object.freeze({
    db: '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" style="display:block"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M3 5v14c0 1.66 4.03 3 9 3s9-1.34 9-3V5"/><path d="M3 12c0 1.66 4.03 3 9 3s9-1.34 9-3"/></svg>',
    prev: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><polyline points="15 18 9 12 15 6"/></svg>',
    next: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><polyline points="9 18 15 12 9 6"/></svg>',
    regen: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>',
    rescan: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><path d="M1 4v6h6"/><path d="M23 20v-6h-6"/><path d="M20.49 9A9 9 0 0 0 5.64 5.64L1 10m22 4l-4.64 4.36A9 9 0 0 1 3.51 15"/></svg>',
    save: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>',
    settings: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><path d="M12 15.5a3.5 3.5 0 1 0 0-7 3.5 3.5 0 0 0 0 7Z"/><path d="M19.4 15a1.7 1.7 0 0 0 .34 1.88l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06A1.7 1.7 0 0 0 15 19.4a1.7 1.7 0 0 0-1 .6 1.7 1.7 0 0 0-.4 1.1V21a2 2 0 1 1-4 0v-.09A1.7 1.7 0 0 0 8.6 19a1.7 1.7 0 0 0-1.88.34l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06A1.7 1.7 0 0 0 4.6 15a1.7 1.7 0 0 0-.6-1 1.7 1.7 0 0 0-1.1-.4H3a2 2 0 1 1 0-4h.09A1.7 1.7 0 0 0 5 8.6a1.7 1.7 0 0 0-.34-1.88l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06A1.7 1.7 0 0 0 9 4.6a1.7 1.7 0 0 0 1-.6 1.7 1.7 0 0 0 .4-1.1V3a2 2 0 1 1 4 0v.09A1.7 1.7 0 0 0 15 5a1.7 1.7 0 0 0 1.88-.34l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06A1.7 1.7 0 0 0 19.4 9c.2.4.6.8 1 1 .3.2.7.3 1.1.3H21a2 2 0 1 1 0 4h-.09A1.7 1.7 0 0 0 19.4 15Z"/></svg>',
    hide: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>',
    prevTurn: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><polygon points="19 20 9 12 19 4 19 20" fill="currentColor" stroke="none"/><line x1="5" y1="19" x2="5" y2="5"/></svg>',
    nextTurn: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><polygon points="5 4 15 12 5 20 5 4" fill="currentColor" stroke="none"/><line x1="19" y1="5" x2="19" y2="19"/></svg>',
    toggleBar: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" style="display:block"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="15" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>',
    firstPage: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><polyline points="11 17 6 12 11 7"/><polyline points="18 17 13 12 18 7"/></svg>',
    lastPage: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><polyline points="13 17 18 12 13 7"/><polyline points="6 17 11 12 6 7"/></svg>',
    close: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" style="display:block"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>',
    spriteEdit: '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block"><polyline points="5 9 2 12 5 15"/><polyline points="9 5 12 2 15 5"/><polyline points="15 19 12 22 9 19"/><polyline points="19 15 22 12 19 9"/><line x1="2" y1="12" x2="22" y2="12"/><line x1="12" y1="2" x2="12" y2="22"/></svg>',
});
const ORIGINAL_READER_TOOLBAR_BUTTONS = Object.freeze([
    { id: 'db-panel', title: '数据库', html: ORIGINAL_READER_ICONS.db },
    { id: 'prev-turn', title: '上一轮', html: ORIGINAL_READER_ICONS.prevTurn },
    { id: 'first-page', title: '第一页', html: ORIGINAL_READER_ICONS.firstPage },
    { id: 'prev', title: '上一页', html: ORIGINAL_READER_ICONS.prev },
    { id: 'next', title: '下一页', html: ORIGINAL_READER_ICONS.next },
    { id: 'last-page', title: '最后一页', html: ORIGINAL_READER_ICONS.lastPage },
    { id: 'next-turn', title: '下一轮', html: ORIGINAL_READER_ICONS.nextTurn },
    { id: 'regen', title: '重新生图', html: ORIGINAL_READER_ICONS.regen },
    { id: 'save', title: '保存图片', html: ORIGINAL_READER_ICONS.save },
    { id: 'hide', title: '隐藏对话框', html: ORIGINAL_READER_ICONS.hide },
    { id: 'sprite-edit', title: '调整立绘', html: ORIGINAL_READER_ICONS.spriteEdit },
    { id: 'rescan', title: '刷新', html: ORIGINAL_READER_ICONS.rescan },
    { id: 'settings', title: '设置', html: ORIGINAL_READER_ICONS.settings },
]);

const ORIGINAL_READER_STYLE_TEXT = `
#igs-overlay{position:fixed;top:0;left:0;right:0;bottom:0;width:100vw;height:100vh;height:100dvh;z-index:900;background:var(--igs-empty-bg,#16181a);overflow:hidden;overscroll-behavior:none;font-family:-apple-system,BlinkMacSystemFont,"PingFang SC","Segoe UI",sans-serif;color:#d8d5cf;--igs-empty-bg:#16181a;--igs-glass-fill-alpha:.62;--igs-glass-density:.62;--igs-glass-opacity:.62;--igs-transparent-glass-bg:rgba(31,34,37,.62);--igs-glass-bg:var(--igs-transparent-glass-bg);--igs-glass-border:rgba(207,204,198,.08);--igs-glass-blur:none;--igs-glass-radius:8px;--igs-glass-shadow:none;--igs-choice-soft-shadow:none;--igs-dialog-bg:var(--igs-glass-bg);--igs-dialog-border:transparent;--igs-dialog-blur:var(--igs-glass-blur);--igs-dialog-radius:8px;--igs-dialog-shadow:none;--igs-toolbar-bg:var(--igs-glass-bg);--igs-toolbar-border:var(--igs-glass-border);--igs-toolbar-blur:var(--igs-glass-blur);--igs-toolbar-radius:8px;--igs-toolbar-shadow:none;--igs-choice-bg:var(--igs-glass-bg);--igs-choice-border:rgba(207,204,198,.08);--igs-choice-blur:var(--igs-glass-blur);--igs-choice-radius:6px;--igs-choice-shadow:none;--igs-db-bg:var(--igs-glass-bg);--igs-db-border:var(--igs-glass-border);--igs-db-blur:var(--igs-glass-blur);--igs-db-head-bg:var(--igs-db-bg);--igs-db-head-blur:var(--igs-db-blur);--igs-db-radius:var(--igs-glass-radius);--igs-db-shadow:none;--igs-toolbar-h:50px;}
#igs-overlay,#igs-overlay *{scrollbar-width:none;-ms-overflow-style:none;}
#igs-overlay ::-webkit-scrollbar{display:none;width:0;height:0;}
#igs-overlay.igs-floating,#igs-overlay.igs-mode-web,#igs-overlay.igs-mode-fullscreen{z-index:2147483000;}
#igs-overlay.igs-fading{opacity:0;transition:opacity .25s;}
#igs-bg{position:absolute;inset:0;background-color:var(--igs-empty-bg,#16181a);background-position:center;background-size:cover;background-repeat:no-repeat;transition:opacity .3s ease;filter:brightness(.88);}
#igs-bg-blur{position:absolute;inset:0;background-position:center;background-size:cover;background-repeat:no-repeat;transition:opacity .3s ease;filter:blur(40px) brightness(.55) saturate(1.3);transform:scale(1.12);opacity:0;pointer-events:none;}
#igs-bg::after{content:"";position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,.6) 0%,rgba(0,0,0,.1) 50%,rgba(0,0,0,0) 80%);pointer-events:none;}
#igs-bg:not([data-igs-has-image="1"])::after{display:none;}
#igs-overlay.igs-scene-nsfw #igs-bg{filter:blur(8px) brightness(var(--igs-nsfw-bg-brightness,.62)) saturate(.72);transform:scale(1.04);}
#igs-overlay.igs-scene-nsfw #igs-bg::after{display:block;background:radial-gradient(ellipse at center,rgba(12,14,18,var(--igs-nsfw-veil-center,.30)) 20%,rgba(12,14,18,var(--igs-nsfw-veil-edge,.72)) 100%);}
#igs-sprite{position:absolute;bottom:0;left:50%;transform:translateX(-50%);width:40%;height:85%;background-size:100%;background-repeat:no-repeat;background-position:50% 100%;pointer-events:none;z-index:2;display:none;}
#igs-overlay.igs-mode-embedded #igs-sprite.igs-sprite-narration{filter:brightness(.86) saturate(.86)!important;-webkit-filter:brightness(.86) saturate(.86)!important;}

#igs-sprite.igs-sprite-editing{pointer-events:all;cursor:grab;outline:2px dashed rgba(255,255,255,.5);outline-offset:-2px;}
#igs-sprite.igs-sprite-editing.is-dragging{cursor:grabbing;}
#igs-dialog-layer,#igs-toolbar-layer,#igs-option-layer,#igs-db-layer{position:absolute;inset:0;pointer-events:none;}
#igs-dialog-layer{z-index:4;}
#igs-option-layer{z-index:5;}
#igs-toolbar-layer{z-index:7;inset:auto auto 24px 50%;width:min(880px,calc(100vw - 32px));height:var(--igs-dialog-h,160px);transform:translateX(-50%);}
#igs-db-layer{z-index:10;}
#igs-sprite-edit-bar{position:absolute;top:14px;left:50%;transform:translateX(-50%);display:flex;align-items:center;gap:8px;z-index:10;background:rgba(16,16,20,.88);border:1px solid rgba(255,255,255,.18);backdrop-filter:blur(20px);border-radius:16px;padding:8px 14px;white-space:nowrap;}
#igs-sprite-edit-bar .igs-se-hint{font-size:12px;color:rgba(255,255,255,.55);margin-right:4px;}
#igs-sprite-edit-bar button{padding:5px 13px;border-radius:10px;border:1px solid rgba(255,255,255,.2);background:rgba(255,255,255,.1);color:#fff;font-size:13px;cursor:pointer;font-family:inherit;}
#igs-sprite-edit-bar button:hover{background:rgba(255,255,255,.22);}
#igs-sprite-edit-bar .igs-se-save{background:rgba(92,170,255,.25);border-color:rgba(92,170,255,.5);}
#igs-click-layer{position:absolute;inset:0;cursor:pointer;z-index:3;}
#igs-status-hud{position:absolute;z-index:8;top:14px;left:14px;pointer-events:none;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;column-gap:calc(6px * var(--igs-hud-scale,1));width:calc(340px * var(--igs-hud-scale,1));padding:calc(8px * var(--igs-hud-scale,1));border-radius:calc(10px * var(--igs-hud-scale,1));box-sizing:border-box;}
#igs-status-hud.igs-hud-bg-dialog{background:color-mix(in srgb,var(--igs-dialog-bg,var(--igs-glass-bg,rgba(31,34,37,.62))) 50%,transparent);-webkit-backdrop-filter:var(--igs-dialog-blur,none);backdrop-filter:var(--igs-dialog-blur,none);border-radius:calc(6px * var(--igs-hud-scale,1));}
#igs-overlay.igs-toolbar-top #igs-status-hud{top:14px;}
#igs-overlay.igs-mode-embedded #igs-status-hud{top:14px;left:12px;width:calc(320px * var(--igs-hud-scale,1));}
#igs-status-hud.igs-hud-suppressed{opacity:0;}
#igs-status-hud .igs-hud-toggle{position:absolute;inset:0;width:100%;height:100%;align-self:center;padding:0;pointer-events:auto;border:0;background:transparent;border-radius:0;box-shadow:none;color:rgba(255,255,255,.48);cursor:pointer;}
#igs-status-hud .igs-hud-toggle:hover{background:transparent;border-color:transparent;color:rgba(255,255,255,.9);}
#igs-status-hud .igs-hud-toggle svg{display:none;width:calc(18px * var(--igs-hud-scale,1));height:calc(18px * var(--igs-hud-scale,1));transform:translateY(4.5px);}

#igs-status-hud.igs-hud-collapsed .igs-hud-identity,#igs-status-hud.igs-hud-collapsed .igs-hud-metrics,#igs-status-hud.igs-hud-collapsed .igs-hud-overflow{display:none;}
#igs-status-hud.igs-hud-collapsed .igs-hud-toggle{width:calc(36px * var(--igs-hud-scale,1));height:calc(36px * var(--igs-hud-scale,1));border:0;background:transparent;border-radius:0;box-shadow:none;-webkit-backdrop-filter:none;backdrop-filter:none;}
#igs-status-hud.igs-hud-collapsed .igs-hud-toggle svg{display:block;}
.igs-hud-identity{display:flex;flex-direction:column;align-items:center;gap:calc(5px * var(--igs-hud-scale,1));}
.igs-hud-avatar{width:calc(46px * var(--igs-hud-scale,1));height:calc(46px * var(--igs-hud-scale,1));object-fit:cover;display:block;background:transparent;border:0;}
.igs-hud-avatar-empty{display:flex;align-items:center;justify-content:center;color:rgba(255,255,255,.42);}
.igs-hud-emotion{max-width:100%;padding:calc(2px * var(--igs-hud-scale,1)) calc(8px * var(--igs-hud-scale,1));border-radius:999px;border:0;background:rgba(255,255,255,.16);color:#e8e6e2;font-size:calc(11px * var(--igs-hud-scale,1));line-height:1.5;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.igs-hud-location{display:flex;align-items:center;justify-content:center;gap:calc(4px * var(--igs-hud-scale,1));max-width:100%;min-width:0;}
.igs-hud-location-label{max-width:100%;color:rgba(232,230,226,.82);font-size:calc(11px * var(--igs-hud-scale,1) * var(--igs-hud-location-scale,1));line-height:1.5;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-shadow:0 1px 3px rgba(0,0,0,.72);}
.igs-hud-location-icon{display:flex;flex:0 0 auto;width:calc(16px * var(--igs-hud-scale,1) * var(--igs-hud-location-scale,1));height:calc(16px * var(--igs-hud-scale,1) * var(--igs-hud-location-scale,1));color:rgba(255,255,255,.48);}
.igs-hud-location-icon svg{display:block;width:100%;height:100%;}
.igs-hud-location{position:relative;top:var(--igs-hud-location-offset,0px);}

.igs-hud-metrics{display:flex;flex-direction:column;justify-content:space-evenly;align-self:stretch;min-width:0;gap:calc(4px * var(--igs-hud-scale,1));}
.igs-hud-metric{display:grid;grid-template-columns:max-content minmax(0,1fr) auto;align-items:center;gap:calc(3px * var(--igs-hud-scale,1));}
.igs-hud-metric-label{font-size:calc(11px * var(--igs-hud-scale,1));color:rgba(255,255,255,.72);white-space:nowrap;}
.igs-hud-track{position:relative;min-width:calc(72px * var(--igs-hud-scale,1));height:calc(5px * var(--igs-hud-scale,1));border-radius:999px;background:rgba(255,255,255,.14);overflow:hidden;}
.igs-hud-fill{position:absolute;left:0;top:0;bottom:0;border-radius:999px;}
.igs-hud-metric-value{width:7ch;font-size:calc(10px * var(--igs-hud-scale,1));font-weight:600;color:rgba(255,255,255,.72);text-align:left;font-variant-numeric:tabular-nums;}
.igs-hud-overflow{grid-column:2;justify-self:end;font-size:calc(10px * var(--igs-hud-scale,1));color:rgba(255,255,255,.55);}
#igs-option-bubbles{position:absolute;z-index:6;display:flex;flex-direction:column;gap:8px;max-height:60%;overflow-y:auto;scrollbar-width:none;-ms-overflow-style:none;pointer-events:auto;}
#igs-option-bubbles::-webkit-scrollbar{display:none;}
#igs-option-bubbles[hidden]{display:none;}
/* 跟随对话框宽度模式（默认）：容器与对话框同宽居中，气泡宽度按位置取 100%/50%，文字超宽才换行。 */
#igs-option-bubbles[data-igs-width="dialog"]{left:50%;transform:translateX(-50%);width:var(--igs-dialog-w,min(70%,420px));max-width:calc(100vw - 24px);}
#igs-option-bubbles[data-igs-width="dialog"][data-igs-pos="top-left"]{align-items:flex-start;}
#igs-option-bubbles[data-igs-width="dialog"][data-igs-pos="top-center"]{align-items:center;}
#igs-option-bubbles[data-igs-width="dialog"][data-igs-pos="top-right"]{align-items:flex-end;}
#igs-option-bubbles[data-igs-width="dialog"][data-igs-pos="top-center"] .igs-option-bubble{width:100%;}
#igs-option-bubbles[data-igs-width="dialog"][data-igs-pos="top-left"] .igs-option-bubble{width:50%;}
#igs-option-bubbles[data-igs-width="dialog"][data-igs-pos="top-right"] .igs-option-bubble{width:50%;}
/* 随文本宽度模式：气泡宽度跟随文字（旧行为），按位置贴左/居中/贴右。 */
#igs-option-bubbles[data-igs-width="text"]{max-width:min(70%,420px);}
#igs-option-bubbles[data-igs-width="text"][data-igs-pos="top-left"]{left:16px;align-items:flex-start;}
#igs-option-bubbles[data-igs-width="text"][data-igs-pos="top-center"]{left:50%;transform:translateX(-50%);align-items:center;}
#igs-option-bubbles[data-igs-width="text"][data-igs-pos="top-right"]{right:16px;align-items:flex-end;}
/* 纵向定位：所有位置统一吊在对话框正上方。 */
#igs-option-bubbles[data-igs-pos]{bottom:calc(24px + var(--igs-dialog-h,220px) + var(--igs-toolbar-h,50px) + 12px);}
#igs-overlay.igs-floating #igs-option-bubbles[data-igs-pos]{bottom:calc(14px + var(--igs-dialog-h,220px) + var(--igs-toolbar-h,50px) + 10px);}
/* 顶部固定工具栏：工具栏在屏幕顶端而非底部，气泡须避让顶部工具栏（加 top 下限，超出走滚动），底部不再为工具栏留空。 */
#igs-overlay.igs-toolbar-top #igs-option-bubbles[data-igs-pos]{top:calc(var(--igs-toolbar-h,50px) + 12px);bottom:calc(24px + var(--igs-dialog-h,220px) + 12px);max-height:none;}
.igs-option-bubble{pointer-events:auto;cursor:pointer;border:1px solid var(--igs-choice-border,rgba(207,204,198,.08));background:var(--igs-choice-bg,var(--igs-glass-bg,rgba(31,34,37,.62)));-webkit-backdrop-filter:var(--igs-choice-blur,none);backdrop-filter:var(--igs-choice-blur,none);border-radius:var(--igs-choice-radius,6px);padding:10px 16px;color:#d8d5cf;font-size:var(--igs-option-font-size,14px);line-height:1.5;text-shadow:none;box-shadow:none;max-width:100%;word-break:break-word;transition:border-color .15s,color .15s,transform .1s;}
.igs-option-bubble:hover{background:var(--igs-choice-bg,var(--igs-glass-bg,rgba(31,34,37,.62)));border-color:rgba(207,204,198,.16);color:#fff;}
.igs-option-bubble:active{transform:scale(.97);}
.igs-dialog{position:absolute;left:50%;bottom:24px;transform:translateX(-50%);width:min(880px,calc(100vw - 32px));background:var(--igs-dialog-bg,var(--igs-glass-bg,rgba(31,34,37,.62)));border:0;-webkit-backdrop-filter:var(--igs-dialog-blur,none);backdrop-filter:var(--igs-dialog-blur,none);border-radius:var(--igs-dialog-radius,8px);box-shadow:none;padding:10px 26px 18px;z-index:4;overflow:visible;pointer-events:auto;transition:opacity .3s,transform .3s;}
.igs-dialog[data-igs-narration="1"]{padding-top:15px;}
.igs-dialog.igs-hidden{opacity:0;transform:translateX(-50%) translateY(20px);pointer-events:none;}
#igs-overlay.igs-floating #igs-click-layer{cursor:grab;touch-action:none;}
#igs-overlay.igs-floating.is-dragging #igs-click-layer{cursor:grabbing;}
#igs-overlay.igs-floating .igs-dialog{box-sizing:border-box;left:12px;right:12px;bottom:14px;width:auto;transform:translateZ(0);display:flex;flex-direction:column;max-height:none;overflow:visible;padding:7px 18px 14px;}
#igs-overlay.igs-floating .igs-dialog[data-igs-narration="1"]{padding-top:12px;}
#igs-overlay.igs-floating .igs-dialog.igs-hidden{transform:translateY(20px);}
#igs-overlay.igs-floating-mobile .igs-dialog{left:10px;right:10px;bottom:12px;max-height:none;padding:6px 14px 12px;}
#igs-overlay.igs-floating-mobile .igs-dialog[data-igs-narration="1"]{padding-top:11px;}
#igs-overlay.igs-mode-web .igs-dialog,#igs-overlay.igs-mode-fullscreen .igs-dialog{box-sizing:border-box;display:flex;flex-direction:column;max-height:none;overflow:hidden;}
#igs-overlay.igs-mode-web .igs-text,#igs-overlay.igs-mode-fullscreen .igs-text{min-height:0;overflow-y:auto;flex:1 1 auto;}
#igs-overlay.igs-mode-web .igs-progress,#igs-overlay.igs-mode-web .igs-speaker,#igs-overlay.igs-mode-web .igs-divider,#igs-overlay.igs-mode-web .igs-controls,#igs-overlay.igs-mode-fullscreen .igs-progress,#igs-overlay.igs-mode-fullscreen .igs-speaker,#igs-overlay.igs-mode-fullscreen .igs-divider,#igs-overlay.igs-mode-fullscreen .igs-controls{flex-shrink:0;}
#igs-overlay.igs-floating #igs-toolbar-layer{left:12px;right:12px;bottom:14px;width:auto;transform:none;}
#igs-overlay.igs-floating-mobile #igs-toolbar-layer{left:10px;right:10px;bottom:12px;}
.igs-ctrl-bar{position:absolute;top:-50px;right:0;display:flex;gap:6px;z-index:5;padding:6px;background:var(--igs-toolbar-bg,var(--igs-glass-bg,rgba(20,20,22,.62)));border:1px solid var(--igs-toolbar-border,rgba(255,255,255,.14));-webkit-backdrop-filter:var(--igs-toolbar-blur,none);backdrop-filter:var(--igs-toolbar-blur,none);border-radius:var(--igs-toolbar-radius,18px);box-shadow:var(--igs-toolbar-shadow,0 4px 24px rgba(0,0,0,.20));pointer-events:auto;transition:opacity .3s;}
.igs-ctrl-bar.igs-hidden{opacity:0;pointer-events:none;}
#igs-overlay.igs-toolbar-top #igs-toolbar-layer{inset:0 0 auto 0;width:auto;height:auto;transform:none;}
#igs-overlay.igs-toolbar-top .igs-ctrl-bar{position:static;top:auto;right:auto;width:100%;box-sizing:border-box;justify-content:space-between;gap:0;border-radius:0;border-left:none;border-right:none;border-top:none;box-shadow:none;flex-wrap:nowrap;}
#igs-overlay.igs-toolbar-top.igs-toolbar-expanded .igs-ctrl-bar{padding-left:56px;}
#igs-overlay.igs-toolbar-top #igs-bar-btns{flex:1 1 auto;min-width:0;justify-content:space-evenly;flex-wrap:nowrap;overflow-x:auto;overflow-y:hidden;scrollbar-width:none;-ms-overflow-style:none;touch-action:pan-x;-webkit-overflow-scrolling:touch;overscroll-behavior:contain;}
#igs-overlay.igs-toolbar-top #igs-bar-btns.igs-bar-overflow{justify-content:flex-start;cursor:grab;}
#igs-overlay.igs-toolbar-top #igs-bar-btns.igs-bar-dragging{cursor:grabbing;}
#igs-overlay.igs-toolbar-top #igs-bar-btns::-webkit-scrollbar{display:none;width:0;height:0;}
#igs-overlay.igs-toolbar-top #igs-bar-btns .igs-icon-btn{flex:0 0 auto;}
#igs-overlay.igs-toolbar-top #igs-bar-pinned{flex:0 0 auto;justify-content:flex-end;}
#igs-overlay.igs-toolbar-top .igs-ctrl-bar [data-act="close"]{flex:0 0 auto;}
.igs-icon-btn{width:36px;height:36px;border:1px solid transparent;cursor:pointer;background:transparent;color:rgba(255,255,255,.52);font-size:15px;border-radius:13px;display:inline-flex;align-items:center;justify-content:center;transition:background .18s,border-color .18s,color .18s,transform .12s;outline:none;}
.igs-icon-btn:hover{background:rgba(255,255,255,.12);border-color:rgba(255,255,255,.18);color:rgba(255,255,255,.96);}
.igs-icon-btn:active{transform:scale(.96);}
#igs-bar-btns{display:none;gap:6px;align-items:center;}
#igs-bar-pinned{display:flex;gap:6px;align-items:center;}
.igs-progress{font-size:11px;color:rgba(255,255,255,.55);margin-bottom:0;letter-spacing:1px;}
.igs-status-line{font-size:10px;color:rgba(255,255,255,.35);letter-spacing:1px;margin:2px 0 8px;text-align:center;display:none;}
.igs-speaker{font-size:14px;font-weight:600;letter-spacing:1px;margin-top:0;margin-bottom:4px;display:none;text-shadow:none;}
.igs-divider{font-size:11px;letter-spacing:4px;text-align:center;margin-bottom:4px;opacity:.6;display:none;}
.igs-thought{font-style:italic;opacity:.72;font-size:.96em;}
.igs-text{font-size:18px;line-height:1.7;letter-spacing:.5px;min-height:60px;color:#d8d5cf;text-shadow:none;margin-bottom:14px;margin-top:0;white-space:pre-wrap;word-break:break-word;}
.igs-controls{display:flex;align-items:center;gap:8px;border-top:1px solid rgba(255,255,255,.08);padding-top:12px;}
#igs-overlay.igs-floating .igs-progress{flex-shrink:0;}
#igs-overlay.igs-floating .igs-text{min-height:0;overflow-y:auto;margin-bottom:12px;flex:1 1 auto;}
#igs-overlay.igs-floating .igs-speaker,#igs-overlay.igs-floating .igs-divider{flex-shrink:0;}
#igs-overlay.igs-floating .igs-controls{flex-shrink:0;}
.igs-input{flex:1;min-width:0;height:32px;box-sizing:border-box;padding:0 12px;background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.14);border-radius:14px;color:#fff;font-size:14px;line-height:18px;outline:none;font-family:inherit;}
.igs-input:focus{background:rgba(255,255,255,.14);border-color:rgba(255,255,255,.3);}
.igs-input::placeholder{color:rgba(255,255,255,.4);}
.igs-send-btn{height:32px;box-sizing:border-box;display:inline-flex;align-items:center;justify-content:center;min-width:58px;padding:0 12px;border:1px solid rgba(255,255,255,.14);border-radius:12px;background:rgba(255,255,255,.08);color:rgba(255,255,255,.72);font-size:13px;line-height:18px;font-weight:600;letter-spacing:0;white-space:nowrap;cursor:pointer;}
.igs-send-btn:hover{background:rgba(255,255,255,.14);color:rgba(255,255,255,.92);}
.igs-send-btn:focus{border-color:rgba(92,170,255,.58);box-shadow:0 0 0 2px rgba(92,170,255,.18);outline:none;}
.igs-send-btn:disabled{opacity:.55;pointer-events:none;}
@keyframes igs-spin{to{transform:rotate(360deg);}}
.igs-spinner{display:inline-block;width:10px;height:10px;border:2px solid rgba(255,255,255,.3);border-top-color:#fff;border-radius:50%;animation:igs-spin .8s linear infinite;}
.igs-image-loading{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;pointer-events:none;z-index:1;}
.igs-image-spinner{width:32px;height:32px;border-width:3px;opacity:.7;}
.igs-image-empty{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;pointer-events:none;z-index:1;font-size:13px;color:rgba(255,255,255,.4);letter-spacing:.5px;}
#igs-send-status{display:none;flex:1;align-items:center;gap:8px;padding:8px 14px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.12);border-radius:14px;font-size:13px;color:rgba(255,255,255,.55);letter-spacing:.3px;}
#igs-settings{display:none;position:absolute;right:0;bottom:calc(100% + 10px);min-width:232px;background:rgba(16,16,20,.92);border:1px solid rgba(255,255,255,.14);backdrop-filter:blur(40px) saturate(180%);border-radius:18px;padding:16px 18px 14px;box-shadow:0 10px 40px rgba(0,0,0,.6);z-index:30;}
#igs-toast{position:absolute;left:50%;top:24px;transform:translateX(-50%);min-width:200px;max-width:min(420px,calc(100vw - 32px));padding:10px 14px;border-radius:12px;background:rgba(16,16,20,.88);border:1px solid rgba(255,255,255,.12);font-size:12px;line-height:1.45;opacity:0;pointer-events:none;}
/* 楼层内嵌：容器固定高度、不可拖动、不锁页面滚动，全部层约束在容器内。 */
.igs-embedded-host{position:relative;display:block;width:100%;margin:8px 0;border-radius:8px;overflow:hidden;isolation:isolate;background:#16181a;}
.igs-embedded-root{position:relative;width:100%;height:100%;overflow:hidden;}
.igs-embedded-host[data-igs-embedded-loading="1"]{display:flex;align-items:center;justify-content:center;}
#igs-overlay.igs-mode-embedded{position:relative;inset:auto;width:100%;height:100%;z-index:1;border-radius:6px;}
.igs-mode-embedded .igs-progress{display:none;}
.igs-mode-embedded .igs-dialog{box-sizing:border-box;left:12px;right:12px;bottom:14px;width:auto;height:auto;min-height:0;max-height:calc(100% - 28px);transform:none;display:flex;flex-direction:column;overflow:hidden;padding:9px 18px 14px;}
.igs-mode-embedded .igs-dialog[data-igs-narration="1"]{padding-top:14px;}
.igs-mode-embedded .igs-text{min-height:0;overflow-y:auto;margin-bottom:12px;flex:1 1 auto;}
.igs-mode-embedded .igs-speaker,.igs-mode-embedded .igs-divider{flex-shrink:0;}
.igs-mode-embedded .igs-controls{display:none;}
.igs-mode-embedded #igs-toolbar-layer{inset:14px 14px auto auto;width:auto;height:auto;transform:none;}
.igs-mode-embedded .igs-ctrl-bar{position:static;top:auto;right:auto;display:flex;align-items:center;gap:1.5px;padding:0;background:transparent;border:0;border-radius:0;box-shadow:none;-webkit-backdrop-filter:none;backdrop-filter:none;}
.igs-mode-embedded .igs-ctrl-bar .igs-icon-btn{width:32px;height:32px;border:0;background:transparent;border-radius:0;box-shadow:none;color:rgba(255,255,255,.32);}
.igs-mode-embedded .igs-ctrl-bar .igs-icon-btn svg{width:11px;height:11px;transform:scale(1.2);transform-origin:center;}
.igs-mode-embedded .igs-ctrl-bar .igs-icon-btn:hover{background:transparent;border-color:transparent;color:rgba(255,255,255,.52);}
.igs-mode-embedded #igs-bar-btns,.igs-mode-embedded #igs-bar-pinned,#igs-overlay.igs-default-reader-chrome #igs-bar-btns,#igs-overlay.igs-default-reader-chrome #igs-bar-pinned{display:flex;align-items:center;align-self:center;height:32px;line-height:32px;}
.igs-mode-embedded #igs-bar-btns .igs-icon-btn,.igs-mode-embedded #igs-bar-pinned .igs-icon-btn,#igs-overlay.igs-default-reader-chrome #igs-bar-btns .igs-icon-btn,#igs-overlay.igs-default-reader-chrome #igs-bar-pinned .igs-icon-btn{align-self:center;vertical-align:middle;}
.igs-mode-embedded .igs-ctrl-bar>.igs-icon-btn,#igs-overlay.igs-default-reader-chrome .igs-ctrl-bar>.igs-icon-btn{align-self:center;}
#igs-overlay.igs-default-reader-chrome .igs-dialog{box-sizing:border-box;display:flex;flex-direction:column;overflow:hidden;padding:9px 18px 14px;}
#igs-overlay.igs-default-reader-chrome .igs-dialog[data-igs-narration="1"]{padding-top:14px;}
#igs-overlay.igs-default-reader-chrome #igs-toolbar-layer{inset:14px 14px auto auto;width:auto;height:auto;transform:none;}
#igs-overlay.igs-default-reader-chrome .igs-ctrl-bar{position:static;top:auto;right:auto;display:flex;align-items:center;width:auto;gap:1.5px;padding:0;background:transparent;border:0;border-radius:0;box-shadow:none;-webkit-backdrop-filter:none;backdrop-filter:none;}
#igs-overlay.igs-default-reader-chrome .igs-ctrl-bar .igs-icon-btn{width:32px;height:32px;border:0;background:transparent;border-radius:0;box-shadow:none;color:rgba(255,255,255,.32);}
#igs-overlay.igs-default-reader-chrome .igs-ctrl-bar .igs-icon-btn svg{width:11px;height:11px;transform:scale(1.2);transform-origin:center;}
#igs-overlay.igs-default-reader-chrome .igs-ctrl-bar .igs-icon-btn:hover{background:transparent;border-color:transparent;color:rgba(255,255,255,.52);}
.igs-mode-embedded #igs-option-bubbles[data-igs-pos]{top:calc(14px + var(--igs-toolbar-h,32px) + 8px);bottom:calc(14px + var(--igs-dialog-h,220px) + 10px);max-height:none;overflow-y:auto;overscroll-behavior:contain;}
.igs-mode-embedded #igs-option-bubbles[data-igs-width="dialog"]{max-width:calc(100% - 24px);}
.igs-embedded-host{aspect-ratio:8 / 5;max-height:760px;}
.igs-embedded-loading{display:flex;align-items:center;justify-content:center;gap:6px;width:100%;height:100%;color:rgba(255,255,255,.72);font-size:13px;letter-spacing:.08em;}
.igs-embedded-loading-dot{width:6px;height:6px;border-radius:50%;background:currentColor;opacity:.35;animation:igs-embedded-pulse 1.1s ease-in-out infinite;}
.igs-embedded-loading-dot:nth-child(2){animation-delay:.18s;}
.igs-embedded-loading-dot:nth-child(3){animation-delay:.36s;}
.igs-embedded-loading-text{margin-left:6px;color:rgba(255,255,255,.5);}
@keyframes igs-embedded-pulse{0%,100%{opacity:.25}50%{opacity:.9}}
@media (max-width:640px){.igs-embedded-host{aspect-ratio:auto;height:min(74dvh,680px);}}
@media (prefers-reduced-motion: reduce){.igs-embedded-loading-dot{animation:none;opacity:.6;}}
${CLASSIC_DIALOG_STYLE_TEXT}
`.trim();

const ORIGINAL_READER_HTML = `
<div id="igs-bg-blur" class="igs-background-layer igs-background-blur-layer"></div>
<div id="igs-bg" class="igs-background-layer"></div>
<div id="igs-sprite" class="igs-character-layer"></div>
<div id="igs-click-layer"></div>
<div id="igs-option-layer" class="igs-choice-layer">
  <div id="igs-option-bubbles" data-igs-pos="top-left" data-igs-width="dialog" hidden></div>
</div>
<div id="igs-dialog-layer" class="igs-dialogue-layer">
<div class="igs-dialog" id="igs-dialog">
  <div class="igs-progress" id="igs-progress"></div>
  <div class="igs-speaker" id="igs-speaker"></div>
  <div class="igs-divider" id="igs-divider"></div>
  <div class="igs-text" id="igs-text"></div>
  <div class="igs-status-line" id="igs-status-line"></div>
  <div class="igs-controls" id="igs-controls-shujuku_v120-guard">
    <div id="igs-send-status" aria-live="polite"><span class="igs-spinner"></span><span id="igs-send-status-text">已发送，等待 AI 回复…</span></div>
    <input class="igs-input" id="igs-input" type="text" placeholder="输入内容后按 Enter 发送">
    <button class="igs-send-btn" id="igs-send-btn" type="button">发送</button>
  </div>
  <div id="igs-toast" aria-live="polite"></div>
</div>
</div>
<div id="igs-toolbar-layer" class="igs-hud-layer">
  <div class="igs-ctrl-bar igs-toolbar" id="igs-ctrl-bar" data-igs-toolbar-dock="float">
    <div id="igs-bar-btns">
      ${ORIGINAL_READER_TOOLBAR_BUTTONS.map((button) => (
        `<button class="igs-icon-btn" id="igs-btn-${button.id}" data-act="${button.id}" title="${button.title}" type="button">${button.html}</button>`
      )).join('')}
    </div>
    <div id="igs-settings" aria-hidden="true"></div>
    <div id="igs-bar-pinned"></div>
    <button class="igs-icon-btn" data-act="toggle-bar" title="收纳/展开按钮" type="button">${ORIGINAL_READER_ICONS.toggleBar}</button>
    <button class="igs-icon-btn" data-act="close" title="退出" type="button">${ORIGINAL_READER_ICONS.close}</button>
  </div>
</div>
<div id="igs-db-layer" class="igs-system-layer"></div>
<div id="igs-status-hud" hidden></div>
`.trim();
const ORIGINAL_READER_REQUIRED_SELECTORS = Object.freeze([
    '#igs-overlay',
    '#igs-bg',
    '#igs-bg-blur',
    '#igs-sprite',
    '#igs-click-layer',
    '#igs-dialog-layer',
    '.igs-dialogue-layer',
    '#igs-toolbar-layer',
    '.igs-hud-layer',
    '#igs-option-layer',
    '.igs-choice-layer',
    '#igs-db-layer',
    '.igs-system-layer',
    '.igs-dialog',
    '.igs-ctrl-bar',
    '.igs-toolbar',
    '#igs-bar-btns',
    '#igs-settings',
    '.igs-controls',
    '#igs-send-status',
    '#igs-input',
    '#igs-send-btn',
    '#igs-toast',
]);
const ORIGINAL_READER_STYLE_CONTRACT = Object.freeze({
    overlayZIndex: '2147483000',
    dialogWidth: 'min(880px,calc(100vw - 32px))',
    inputHeight: '32px',
    sendButtonMinWidth: '58px',
    toolbarButtonSize: '36px',
});
function getOriginalReaderStyleText() {
    return ORIGINAL_READER_STYLE_TEXT;
}
function getOriginalReaderHtml() {
    return ORIGINAL_READER_HTML;
}
function getOriginalReaderSource(version = '0.5.4') {
    return {
        version,
        styleText: ORIGINAL_READER_STYLE_TEXT,
        html: ORIGINAL_READER_HTML,
        selectors: Array.from(ORIGINAL_READER_REQUIRED_SELECTORS),
        styleContract: { ...ORIGINAL_READER_STYLE_CONTRACT },
    };
}

__igsDefine(exports, "getOriginalReaderStyleText", () => getOriginalReaderStyleText);
__igsDefine(exports, "getOriginalReaderHtml", () => getOriginalReaderHtml);
__igsDefine(exports, "getOriginalReaderSource", () => getOriginalReaderSource);
__igsDefine(exports, "ORIGINAL_READER_ICONS", () => ORIGINAL_READER_ICONS);
__igsDefine(exports, "ORIGINAL_READER_TOOLBAR_BUTTONS", () => ORIGINAL_READER_TOOLBAR_BUTTONS);
__igsDefine(exports, "ORIGINAL_READER_REQUIRED_SELECTORS", () => ORIGINAL_READER_REQUIRED_SELECTORS);
__igsDefine(exports, "ORIGINAL_READER_STYLE_CONTRACT", () => ORIGINAL_READER_STYLE_CONTRACT);
});
__igsRegister("src/visual/igs-ui/classic-dialog-skin.js", function(module, exports, require) {
const { CLASSIC_DIALOG_ASSETS } = require("src/visual/igs-ui/classic-dialog-assets.js");
const DIALOG_SKIN_DEFAULT = 'default';
const DIALOG_SKIN_WESTERN_CLASSIC = 'western-classic';
const CLASSIC_DIALOG_HEIGHT = 184;
const CLASSIC_DIALOG_EDGE_WIDTH = 110;
const CLASSIC_DIALOG_WIDTH_PERCENT_MIN = 60;
const CLASSIC_DIALOG_WIDTH_PERCENT_MAX = 100;
const CLASSIC_DIALOG_WIDTH_PERCENT_DEFAULT = 100;
const CLASSIC_NAMEPLATE_HEIGHT = 50;
const CLASSIC_NAMEPLATE_EDGE_WIDTH = 40;
const CLASSIC_NAMEPLATE_WIDTH = 300;
const CLASSIC_NAMEPLATE_LEFT = 35;
const CLASSIC_NAMEPLATE_TOP = -22;
const CLASSIC_DIALOG_THEME_DEFAULTS = Object.freeze({
    preset: 'custom',
    nameAlign: 'center',
    textAlign: 'left',
    narrationAlign: 'left',
    thoughtAlign: 'left',
    dividerSymbol: 'none',
    nameFont: 'inherit',
    textFont: 'inherit',
    thoughtFont: 'inherit',
    narrationFont: 'inherit',
    nameColor: '#312b1b',
    textColor: '#f2e5c4',
    thoughtColor: '#c7c4b2',
    narrationColor: '#e6dcc3',
    dividerColor: '#ffeeb8',
});
function normalizeDialogSkin(value) {
    return value === DIALOG_SKIN_WESTERN_CLASSIC ? DIALOG_SKIN_WESTERN_CLASSIC : DIALOG_SKIN_DEFAULT;
}
function normalizeClassicDialogWidthPercent(value) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) return CLASSIC_DIALOG_WIDTH_PERCENT_DEFAULT;
    return Math.max(CLASSIC_DIALOG_WIDTH_PERCENT_MIN, Math.min(CLASSIC_DIALOG_WIDTH_PERCENT_MAX, numeric));
}
function isClassicDialogSkin(readerSettings) {
    return normalizeDialogSkin(readerSettings && readerSettings.dialogSkin) === DIALOG_SKIN_WESTERN_CLASSIC;
}
function applyDialogSkinAssets(dialog, readerSettings) {
    if (!dialog) return;
    if (!isClassicDialogSkin(readerSettings)) {
        if (typeof dialog.removeAttribute === 'function') dialog.removeAttribute('data-igs-dialog-skin');
        else if (typeof dialog.setAttribute === 'function') dialog.setAttribute('data-igs-dialog-skin', '');
        return;
    }
    dialog.setAttribute('data-igs-dialog-skin', DIALOG_SKIN_WESTERN_CLASSIC);
}
const CLASSIC_DIALOG_STYLE_TEXT = `
#igs-overlay .igs-dialog[data-igs-dialog-skin="western-classic"]{box-sizing:border-box;height:184px;min-height:184px;max-height:184px;display:flex;flex-direction:column;overflow:visible;padding:20px 44px 18px;background-color:transparent;background-image:url("${CLASSIC_DIALOG_ASSETS.dialogLeft}"),url("${CLASSIC_DIALOG_ASSETS.dialogCenter}"),url("${CLASSIC_DIALOG_ASSETS.dialogRight}");background-position:left top,110px top,right top;background-size:110px 184px,calc(100% - 220px) 184px,110px 184px;background-repeat:no-repeat;border-radius:0;-webkit-backdrop-filter:none;backdrop-filter:none;}
.igs-dialog[data-igs-dialog-skin="western-classic"] .igs-progress,.igs-dialog[data-igs-dialog-skin="western-classic"] .igs-speaker,.igs-dialog[data-igs-dialog-skin="western-classic"] .igs-divider,.igs-dialog[data-igs-dialog-skin="western-classic"] .igs-controls{flex-shrink:0;}
.igs-dialog[data-igs-dialog-skin="western-classic"] .igs-text{min-height:0;overflow-y:auto;flex:1 1 auto;}
.igs-dialog[data-igs-dialog-skin="western-classic"] .igs-speaker{position:absolute;z-index:2;box-sizing:border-box;left:35px;top:-22px;width:min(300px,calc(100% - 70px));height:50px;line-height:49px;margin:0;padding:1px 36px 0;font-size:13px;font-weight:600;letter-spacing:.5px;-webkit-text-stroke:.6px rgba(255,255,255,.78);paint-order:stroke fill;text-shadow:0 1px 0 rgba(255,255,255,.32);background-color:transparent;background-image:url("${CLASSIC_DIALOG_ASSETS.nameLeft}"),url("${CLASSIC_DIALOG_ASSETS.nameCenter}"),url("${CLASSIC_DIALOG_ASSETS.nameRight}");background-position:left top,40px top,right top;background-size:40px 50px,calc(100% - 80px) 50px,40px 50px;background-repeat:no-repeat;}
.igs-dialog[data-igs-dialog-skin="western-classic"] .igs-divider{display:none;}
#igs-overlay.igs-mode-embedded .igs-dialog[data-igs-dialog-skin="western-classic"]{height:min(184px,calc(100% - 28px));min-height:min(184px,calc(100% - 28px));max-height:calc(100% - 28px);padding:20px 44px 18px;}
#igs-overlay .igs-dialog[data-igs-dialog-skin="western-classic"][data-igs-has-speaker="1"]{padding-top:44px;}
`.trim();

__igsDefine(exports, "normalizeDialogSkin", () => normalizeDialogSkin);
__igsDefine(exports, "normalizeClassicDialogWidthPercent", () => normalizeClassicDialogWidthPercent);
__igsDefine(exports, "isClassicDialogSkin", () => isClassicDialogSkin);
__igsDefine(exports, "applyDialogSkinAssets", () => applyDialogSkinAssets);
__igsDefine(exports, "DIALOG_SKIN_DEFAULT", () => DIALOG_SKIN_DEFAULT);
__igsDefine(exports, "DIALOG_SKIN_WESTERN_CLASSIC", () => DIALOG_SKIN_WESTERN_CLASSIC);
__igsDefine(exports, "CLASSIC_DIALOG_HEIGHT", () => CLASSIC_DIALOG_HEIGHT);
__igsDefine(exports, "CLASSIC_DIALOG_EDGE_WIDTH", () => CLASSIC_DIALOG_EDGE_WIDTH);
__igsDefine(exports, "CLASSIC_DIALOG_WIDTH_PERCENT_MIN", () => CLASSIC_DIALOG_WIDTH_PERCENT_MIN);
__igsDefine(exports, "CLASSIC_DIALOG_WIDTH_PERCENT_MAX", () => CLASSIC_DIALOG_WIDTH_PERCENT_MAX);
__igsDefine(exports, "CLASSIC_DIALOG_WIDTH_PERCENT_DEFAULT", () => CLASSIC_DIALOG_WIDTH_PERCENT_DEFAULT);
__igsDefine(exports, "CLASSIC_NAMEPLATE_HEIGHT", () => CLASSIC_NAMEPLATE_HEIGHT);
__igsDefine(exports, "CLASSIC_NAMEPLATE_EDGE_WIDTH", () => CLASSIC_NAMEPLATE_EDGE_WIDTH);
__igsDefine(exports, "CLASSIC_NAMEPLATE_WIDTH", () => CLASSIC_NAMEPLATE_WIDTH);
__igsDefine(exports, "CLASSIC_NAMEPLATE_LEFT", () => CLASSIC_NAMEPLATE_LEFT);
__igsDefine(exports, "CLASSIC_NAMEPLATE_TOP", () => CLASSIC_NAMEPLATE_TOP);
__igsDefine(exports, "CLASSIC_DIALOG_THEME_DEFAULTS", () => CLASSIC_DIALOG_THEME_DEFAULTS);
__igsDefine(exports, "CLASSIC_DIALOG_STYLE_TEXT", () => CLASSIC_DIALOG_STYLE_TEXT);
});
__igsRegister("src/visual/igs-ui/classic-dialog-assets.js", function(module, exports, require) {
// Generated from the approved western-classic PNG slices.
const CLASSIC_DIALOG_ASSETS = Object.freeze({
    dialogLeft: 'data:image/png;base64,' + [
        'iVBORw0KGgoAAAANSUhEUgAAAG4AAAC4CAYAAAACPNnJAAAACXBIWXMAAAsSAAALEgHS3X78AAAAG3RFWHRTb2Z0d2FyZQBDZWxzeXMgU3R1ZGlvIFRvb2zB',
        'p+F8AAAgAElEQVR4nOy9d5wV1f3//zxn5tYtlAV2QWAXEBZFUUHsXWOvsWuMxvpBjQ1rjD1GTezdWGM3xq6xJxq7KPaKFAGBpcOy5ZaZ8/tj7pk5M3fu3SXm',
        '+/sr5/Hgsdx7Z86c836dd3+fM0IpxZq01pamJLAjsC2wETAW6LdGnfyv/ewmegtca0vTSGAycBgw5P/loP7Xem49Atfa0pQGLgNOA2yA9TdYm623Gcc64xrY',
        'aHR/BmRsRLEISoHr/r8f9f9adeBaW5p2AG4FWgH2239rDjx4PBP72/z07Uy+/mYmq9pWke/Mo1yXNZS6/2s/o1UErrWl6STgZoBNNmvljNN3ZLzVxRtvfczS',
        '2Yv5ZuYqfmjrYnGHw4o8uAq6XMH/sPv/p8UC19rSNAW4GmDySTty2m6j+febH9L2/Xze+GQJ0xYWWZZf42c5gADkzxzz/xoxwLW2NE3GE49c8Yd92aJR8NFb',
        'n/LRJ/N5dXqOJd3edel0kg02Gsk2W41gy/FDyNqChj4ZBAqEQNk2n89ayqffLeKNf07ns09mADB2nWa222E86643gHHD6umXEuC4nm78n6ztdQsB19rStBnw',
        'HsAVF+7Jhg0OX773Oc++vYC35nlGR9++tUyevB1HbLEW3335PTO/n0vHsg6cvIPZl7Ql2T4ZWseP4rVZDtO+WsTRx27JVkPSzPl2Jt98M5P2Re0UuvIoV/0P',
        'szVsPnAl/+xHoOm4X23CvpP68d3UL/nbG/N5f553zW67jefq30zgzbc/ZsnMxXzwzTJ+XJpnYaci70K3K7xOBWRsQd/aBKOa+7DDpqPZcdJaTPv0W5bNXsJX',
        'M1fyQ1s3bR0O7QVwSvrxf633zQTuz8CZ67U2csOUzfjm4y954rXZ/GuGA8B5J23LRoMVP3zyA298vIgPfiqysqB7QaEoo/zwplqmHDYOK9fOqoUrePnzFXw6',
        'r5uVXTEuQ8iu0V2p0v+V8V21Vu263vaxps0UFdGxqtLHn/vc8rELpRStLU2DgDaAR/60J91Lf2LqB99z6xsdAJx/wpa09O1ixrRZPPL+cn5sL91tybxM2F2q',
        'UMwox00C1NSk6OjIsftO63Dpbzbk/Xc+ZdpHc3jx225GrFXHBus2ctsT33m3p5OrrISdU6CEEC6lOQo9yND8PXEqTSJIcB2FwsUSVvl8I/grpRBCBLTuBcau',
        '6yKlDI0DwFVuaZwCKUVwX6Rfx3WwpBX/LON611FIS4Sf7SikFLiud6EUwjftNHBXA1N+scUIjtt3bZbO/J7z7p/Dik7FbluN5KDtBjDzkxnc8vpSlnUDQrgy',
        'lVglbCunik7G7c7XA9x462RefWUaC+a2cceZWzL1w8955a05PPnRSgDuPGsj1ltvNBfd/j6vvDeHRE16hZ1OdkaJhQLXdTyCRQlt8qX07BkhSnZN6TelFEIG',
        'lCoWi9iW5XUQS0BVmSuUNzClFEp5kkII6RGZsD1lduF1qa9Rkd/DEzHnoK+J6nwhQLl6XgIxprnRxuO2/n+5eFdq1UqefeULHn1vNTWZBPdduAXzvp7Bjc/9',
        'xKwVCqRwZCa1TEjp4LhJp7O7vyUFjz94DKPW6sM+h9/NLedszYIf5/CPd37i41mdpJI2389Zxe6TBnDADsOYvdTh0rs+R9pWIdO3dlGwuhVSCJ+7AKQUIaNH',
        'lIgftYZFDCIKj8Mcx1v1+nNvmweWtyKEkCUCVr/f+1mUuDsYXWgOFbswAY32UaKPFDiOQoxpbtwZeHl0c3/+fOY2rG6byW+v/5Llq12mHLEBrf0LvPTGDJ78',
        'LAdCuHY2vRRLOEIIVVzd1aAcN/m74zZj3+1HcvBZz/PLHUawQUuaT75exE1Pz+ajp49ndWeerQ6+l8Z+SS4+ZDiJbJYTb/iSvOOqZDa9ClBCCtdK2DlpWY4J',
        'nBD44i2W8CVONcWJFmfBSnaNhSB9kesp5rBYdt2As7znSyxLVLR6zeEIEYg18zcvEugRvWz4ES5zHAfbtgyOVb40CaSIB9zVwJRDd1+fQ3YZwaeffMFF980i',
        'mZDce/FWLPvxR86570c68yDTyRUyYXdLKRQg8ys7GjNpm4eu3ImpXy7mqddncuHx4+lY0c65t3/FeSdswV47rsus2fPZ9YSnSNiCG44fQaqunmOu+jSWEFbC',
        '7rYzqdV2Ktmlv9M6RCmF67olIgTUtqTlE18TXgjpc2uxUATh6SLz2jLxrHSMwNBbcS1iK1QSb1G7JSTSjYWiF4brKnLdXdh2gmQqEXRTJsk9UfkasOPFJ23D',
        'hDG1/PXJqTzx5mLWW7s/5/5qLP9661vufn0ZQsqiVZNaKoRUUgqlXNfOr+ocOGJwDX88aSJ/uO8r9tthFOusleCB57+jUISj9x2DUyiQW72S467/ntFrZThs',
        '52Ze+nglMxZ0sPmWkxg6dBhdXTlmzpjDtI8+ZfVqzyCyknZ3qjazFCkdy5IIoUWHwHW1rinJe190GitSCP8epTyu80Hzr/JEoZSyzIANGQQG8U1AhLZZDOlQ',
        'zR+NAhzViQCdHV1IS5LJpPzfNLcCOI4HtBjT3PgjMPzhq/amb43LRbe8zdRvVnDMPmPZaaO+3PToF7z7bQcimWiXqUSHJYUSUijluHZ+Veeg9UfUcuAOzTzx',
        'VhsXHj+JrmXzOerKz9lqbIpDt6zBdVyemZbjpU87yaZthg/px06/WJ/fnrIbdHVDRydYFvTvSxd9eeqJz7nt5odZ1LYEIYVT079+kbRFQYhAxEWNFI/QrgdW',
        'ibohMUj0WhXbj6tUVQvRBMB1A7HtP7uK/tOLLjBU9L0qxN2O41AsFkmlUmFRSaA/AcSY5sYOIPvaXYehnBzHXvQSPy7s4LrTNmXoAJszb/qYH9u6sbLpZdKW',
        'eSGEklIqQHQvbx88qinJwH5ZBg2s5zd7jeXjz2byhwd/AKCxb4KiC0tXeQ7fumMGc8eNR6EWL2LRj98xZ+5CZszvIJOUtDb3paFxMGPGjadz6GjOOv0e3vzX',
        'VIQUbt3AvvOlJR1zwpWC41rPmEaOjCGoD2zUZSTCOSq4Xil8PaV/N3VaVLxGOdADO9C5ldyEXC5HIpEsW0RuqTMpPY5rB2r/ef+R4DocftZTLFjcwf2Xbk9t',
        'VnLspf9m2aoCCOGC0l6WayXtbqfoJHHdhFJw6Ykbs+E6Q7n98S/4x79nhAYipWDAwD688dy5zPrkM775bCr3PT+bL+cU/WtsCyauneTQnVsYPW4ia2+2MUed',
        'dA/vvfMVdtLurh3Qp00DY+oKZbCMQIQ+x32nP/v+XAXgICB6oVDEsqwwMFX8eZOzdPMA1v9kwPGR+4LO45vWd2JMc+MqoO7fj0wGFDsfdRfduSJPXb8Hr70/',
        'h1se/cK/qaamFoCOjtVlHT5z096kMvVceNPbDBi2Fj/NW0U6k+bwX+/Dk48/w3abt7DZiDQzv36f0278gXxRYScT3XbS7naLbiLfncsCIpUQXHDkWkzYeDMG',
        'r70e2+x3Fe3tXdT2r29LZlJdIa5wA87R4AgEbsSMDonMGDHrW6xx1wl8d8L8zgfH0I2ucpDSwnWdEnfKEmhuSZdaSCFCIjnw3+LEYriZ4tkH7r0nzkAIi+0P',
        'u56u7gK7bjWSl96eCcChhx/J4UcezdqjByKEy9w5K3jqiae4+45b6O7uJmFLnrvtUPIFl/NveIO/334yDBkEuTwsX8khJ97OVWftRrF9NlOufptvfuwAUMls',
        'ut1O2rlkNtUlkap92cqBhe58NmEJ7jpvPENHb8LrU+fzx5tfwE4muusG9lno0zVGZPqr0XQfIqCa4tP83u8DheN4hLdtq9Svi5RWPCPE+FmBERW2cIOxh51u',
        'U8/pBan7Nq91HNc3pHzgpj57PkibXx5/E3PnL/MfcvUNV7HXPhPpnvE5ufb54BaR6Qbq1l2f2bOSHHvk/zF3zhx23XoMJxy0ESdc9CynHdrKRmMH4Dgu9zz7',
        'A/MWF7jyzF2ZN38Rvz7vhfK5C+Fm6muWpWrTq1cvXTWo0J3Pbj+hP787cSdID2X3o26gUHToO7j/HCGlqwmtrbqQ70S5A19CJARknOHiKoVSDq7rEcqyLJ+D',
        'y4wWDZrJrWH/OWgRizUOvMBPi8+URCMrPnDTXrgYISQHnngLP8xuA+CQw/bgkt/uw+pFP/D8K+/ywLNf0JUrsun4wZx+9M7UDRrN8swAdtn+aPL5Anddvj+T',
        'L3qKQtElk/JWRlfeZa9tWjjrhF15/8tFnH3FkwAcdOivaGxsYtbMGTz/7FMAZPrWLE5lMx0r5i9psS3Bi7cdQKbvUH578d/55Ou51PSvb0ukPf9OT8QkqA6B',
        'BeIsEgqLM0gMELQeEiWfz9SjxaJDwrZjKBqxDKPARggf/b/3uVwn6mY67qF7NHCfvXwpCMl+x97IzDmLkFLwwoMXMri/4PmX3+P3170c6nDE0D48cPVh1A8a',
        'ww0PvM7d9z7PHtuvx1fTf2L2vOUIKRwUUikl9t52BOeevC/frrA4+rirufuBR9lq6+38vp575mHOPPUMANVncMOcjmWrBhZzhextF+3Fhuu3ctUdr/D0q5+T',
        '6ZNdmqrJtpvGg2Vpfy0cEzSJHhttiTTHLSKlFRvgNp3lYrFIMpHwLTz9vDKOi7Sozxf6rQf/L+4aMaa5sQtIf/Hq5SAT7H7EVcydv5TabJr3nr8cIZNssONp',
        'OI5DOp2mu7vbv/moX05kyv/txzez2zno+KtYu6WREcMG8epbX5DtV7fITtr5VW3Lh44cWs+jN/wGMXoi773TyS677WsMaQkwm9OOv5QXX/mQbN/axW7RSXav',
        '7upz7e8PYOtJo/nT7f/g8Rc/I1OfXZqqzZZyEy5ehEOGFLnjqFCGoaI/ByFRFxdnjnKC6zoUiy62bSEESGmF/LM40DXA0bBZLFhKAxy/Ahwn4D4xprlxAdD0',
        'zlPn06ehkX2PvJIfZs2ntibNBy9ejVhrPa7909/Yd/+DGThwEH/64yU88fijALQMHcDzD5wD0mbizmcjheDCMw7hvD/ej52yu+oG9G1buXDZUNdx7bsu3Z2J',
        'kzYju1YLpJKQL0BnF50rlqEK7Tz67Af86S+vka7NrnCLTiLfnat54i+nMWJILedd+Rgvvz2ddH12SaomvVoHfLWFpn0jKbUxEW9hmvqtTC8JwsQv0c/Un+AZ',
        'Gn56yOzTzGIY9+fzeWzbxrJkaWzl+qpyizOBPYTFmObGl4Bdrjv/AHbbdXv+cOtzPPjIS0gpeO7uKay9/oYwYAOgHlgJLGS7zQ9nwYLFrD1iLZ5/4EJUYTUb',
        '7Xouti3597NXs8MB57Ni+Sqy/esWCITsWLaqsWlAloev2Y8+ffqUlJELykG5RVBFLrjxbV58a4ZKplMdhVwh06cua7359B9xuxez37E3MfunFdQ01C+wk3bO',
        'siwfHE9EOliWHQpXmeLUFGVxWQXfuixZgjqE5ufhPFrFWqFldC5da4rXsrinyXlGasp8frRjnSnQakGMaW48A7hm60kjufOaU1jg1LD99scBsOOWY7nlkoNB',
        'pgJiC8kuv76OH+e2ccrpR3HiQdvx9RfT+OXxNzJiaH+ee/ASXvtyKaedfDFCCqduUN+fulZ29C905WtbhtRz0YmbsdHYQaFhvf/ZAk76479QCkdI4ThFJ3nk',
        '0Xtx1q93YeHcr9npiFsRQrh9mvrPVShlWZZPAD+aIIQvZlzX9YHSq7ssNURYjJqWKBBvnpvgRElbTU9FnfUYK7NaX9EAs+a4AcBigBfvO4WRYydy1zOf8Ocr',
        'rgdgrx3X57eHT6J5SB0Km78++w1X3PoPGhr689q/HyK7YhZ33PN3rr37nwzsX8MbfzsHe/h6nDHlHl547nmkbRVq+tUt7m7vrC9052sBtps0nEnrD6a+Nsk3',
        'M5by8Atf6xEqlBJSSt7/+AFqVsznhtsf5y+PfUgyk+qoG1C/2HWVn5WOE1PCyBp42YQAWFe5Zf6fFLIyUQnWa4hucUSuAILJQXqRuEoFYTgBjlPEkrbhKlTL',
        '35WAK2XA/wycue7ag3jq7jNhyFhuufUNbrzuWv/yxgF96M4XWLmqkwEDGnj48QdozizG7fiRrQ+4kiXLvUT2lVN2Y9+9foFo2Zijj7iAd956EyGFk6rJrHId',
        '13LyhbTruAmlVEUJf/UNf2LPjRvoWjqdLQ66hq7uAnUD+i6wU3bOzHkpFa5d0dkCy5I4jltyWh0frGh2wKdDhNBgcly8qR7lCl8/mpxUJSwWajHXRd2H8IAD',
        '4JLALGDI7tuO5bpLj4ZBo/nsO5u/PfIIzz39BLlcDoC99t2fs849mUYxFzrncc4fH+HpV79AWlbRdRxbCHjgmiPYeNKmiGGTuPySO7n/3rsAsBJ2XkjhKMe1',
        'XNe1lavKimMvuuxKDjt0HGruV/z293fz6jvTQ1ET7bs5jhO6TydIVSns5E0xrNeqGQTR6EnI34sS2aRhD62SCK3m10Wv0WMORViMKq9xwBeA2HrjFq44e38G',
        'DBmJGDoGGI5SSYTIAbNQc3+A/DIuuu5ZHnv+Y68jKVw8XSIFsOcO4zho3+3ZZO8Dee+dRdxz5228/e83zFyaMrluk8224JQzzmbSpCTM+Yob7niSWx9+D4C+',
        'TQ1zhSUck5gB13nJT81tWgT59o9B6ShwociFQSg/nmiIs4rhrkp6rQdQo4Fs8/k9fRaCsoLYLYC3AVFbk+TQPcaz7y4TaWkeUlIeguUrO3nyxY+49YE36O7O',
        'lzoUZLJZioUC+Xy4Nn1U8yAOO2BnDj/5DBbMd/nna68wb+4cli5dQiqVYsiQtdh2+50Yt34jLPwEuuZz6XWP89CznwBQ079uUTKT6jQtL9NgMH0wreuizrEm',
        'UNQt0P8vC11FiWxGZMp+jP++Yn1LJCRWKBSRUvujMc8WgUWpH6YUsSXoq4Ea87vBA+tIJhMsXLyKXL6Ibdv8Yted2HyLHRi33gYMGDiIQY0D6ezsom3hQr77',
        '9ms+//QTXn35H8ybOweAUc0DOXPyvuyw/VbQ2FR6RBFWLISOpVBYycv/+og7Hnqbr35YBEBNv7rFyWyqw5+zYaLHiZc4kWjqqEqiMi4dZP4WBW5Ni45iW0mv',
        'eX5eojyxa7qTJdC0sSWliAVuFVD3/APnc9alf+Wb6fP83yZO2ohfHXkou++5I7ACFs2he9USVHE1KAeQCDuDle5PYtjawEhefvF17r3zdj6Z9hEAm4wfxnGH',
        'bU02k8J1XOa3LeP7mQt44/2ZzJjrBbethJXP9KldaicTuaho0xMJES4m+uEHmUVgycVRT2fNQ4FpUQ5kKH8XQ9xqrZqr4AMXSfNU6wviOW4VUHfjH47h0msf',
        'Z8myVWyy2RacNmUKEycNQc37jK4Vc/n++5m8/fF0fpy3jIVLV7OiPUcqYTF4QC2jmhvYauPRrLvOaLIDRkDTBO6961mu/MPFVScoLauQqk23p2oyqyCw7PxS',
        'A/AjIWZCNVoOoFvoe4NbTKc8GkEJ6TafWj0TNNbRjraY+/OFPK6jSKWTwfh7YY1WBE5/Pm7ySZx5zmHkZk2la/ksnn/lA/7+8rd8M2tF9Z6BjcY2cOS+49li',
        's4n0WWcCH37qMvmYyaxe3Y6Q0rFsqyCkcKyEXbCTdi6ZSXWZ4ao48RWXa4sCoQ2MUkchooVCXmaYqgqhzBDXf9pCwEciK7nubhLJBHZc9qGsH1FunAC0tjR1',
        'AymAP117BfvsMYylX0zlvfemcutjnzNjnpf9FlI6iXSiS1pW3kpYBcu2HACnUEwUcoVUoTtfo1xlAeyz7VDOOmEn+g4ezw9dfdh/r+NwHBcvB+dxl2V5VqHO',
        'KpetfMOs1xMIEpUA0v+/B2TgaZgVXhpY/Z0Z7iqLVUKPK7+s8CiyUHrTT6FQJGHb5ZXZ5u3RYUWsyu2AfwFcc+NF7DmpgYXff8Ddj/6b+1+cC4C0rUIik2xP',
        'pNOdiaTleo6uUFIKBEKZ1cP5ju76rlWd/QE2WLue6879BXWDN+CDeV2cfMIVANQP6jdP2lbRC1kJL8srwslOs8DVbEGBqWmAhO9zjT3pfqgsQthoplw/K05k',
        'RgGJjrNiHFPfH9ciolGXFAZhvBggDT8uDcwDGk469RBO2ncDfpr+Prc+9A5PvbkEACudaE/WZFZZUiqFULYtVaE7ny7m8ynlqIRbdGyEUCWx1ykTVrdbLNqr',
        'l7YPUa6Sm42r57LTdmDAsAlceNvLPPfMOyTSyc6a/vWLICi90ysuCPiWO6NR8x4R+HblBo0LyHDezCCSd4+IZAIMIsW4EeYYysYS20nkc6XoijbAZDzXec8S',
        'IeDuAI6fsPEYHrjyCOZ89wH3PP4Oj7+xHIBENrXcSiW7hBRKCKHcvJMqdHXXO4ViigrNTia6axrq2pRCtrctH6qUEsfvPZj999yaPoNGs/WBV1IoONQN7PuT',
        'lbAKZbWJMSIz3joMVqdyw4HnaDl6NBtgEjEqKqPVYZVapaxBGTdGLVNhcFeMYeP7q2WuAX7IayQwA+C1h0+nuHI27777Lpc+0KZHrZJ1NW3Skq6QQhU6c/XF',
        'rlwdQHPLCHbdfS/GrjuO5paRdHd1Mu2jqdx/350samtDSOH0aWqYW8wXUquXrBxsW/DghRvQMHwCdzz+KX//xyekajMrM/U1y6ODllKEArzVHGtftEWdaUPn',
        'xIk1TVDTqa8aKQlQiW/Re02dF+ePqwigxANW9pgScLcAJ+610/qcdMgEVv30Ccdc+QXtXYqELSk4rg9csStXV+zO1wFMOec0jvu/0xAiHek2z8oVsznlxNN4',
        '/91pWAk7Vzew74LVS1c2FnOFzGHb9+OQvTdh5pIEp13uZRD6NPb7KZpWMR3n2MFHRGKoVtHw7aKcp++NpnQq5c1CnGdaojHNfGa+UMCyRLgkIuY6n9sjAIau',
        'j/wuxjQ3pvDSOnUP/OkAsmopT734MQ++sYpRw/oyf/FqunJFlazLtOGSyK/uagC49c4L2fEXOwN9gFogU3pEHq8cYRGwhB23OpV585ZSN6DPAmFZzqq2ZUNb',
        'BiW46uQNyfQdwuHnvUh7R576xn7zpGX5FbJ6Q58mcCURGQUBDF/P8NOELOnMyHW+IRQlWqUwVhxVK3Co44bTNVX9syq/RQ0w8IDbB3h6dHN//nTalrQvnMlv',
        'b/yalV2Ky3+7OX/4y4d05R2VrKtpK6zuHKhcZf3u9/tx5LG7AGm8zHh/oKH0eSUwB9RPMH8RTzzxPr+79lUS6URHTf8+i1cuXDpMucr6y+nr0b+piT/c8ymf',
        'freEmn51i5JZLyap0zW2bflFoFoXRCcUdZ7Dk43SN77SuUwExhkSVVqcT1nWf1R8xnyuVOfpzSciTsc0N94EnHzIbq3svWUjU6d+z3XPLGStQTXccs5WHPq7',
        '1+jKO8pOp1YWu3J9m4f05elbD2HlqtWsXt1JKpWkaVADyZHDoGYg0AGz5rJg7gKWLlvJipUdHH/pv3CVcOob+87tXL56UKE7nz33sFGsv84Q7nr6e/71cRvZ',
        'PjVLU7WZdmM6QQjK2GYVDWVFM9eVmhm7jC8PqGw1Vipjr0TkaJ8h6zFyieZ6x3UQIr40PXY+Y5ob/w1sfe6R6zG2KcED/5jO61+s5pfbDefQnUfy64vfoqvg',
        'KGnb3W6hmDl899HsunEDnUsWk1vdjlIKK5EgVVdPqq4eIQTF7m6K+RyUCH3qHdPpLijVZ3DDjx3L2wcUunK1Zxw4ko3GDuTuZ3/gjc+WkulTszRVk2k3nU8z',
        'JmmC5hUGyaq6r9pvlTYgRouJHMfL7UkZ6LWouOypyDZ4COV9RHSbq9xwnUukhfJxY5obfwKG3HDKONLS4bq/zeLb+TnOOmwsG43pz9F/fJ/uglJSioLruMlz',
        'Dx3J4HQ3SxYsxSp2+xSSlo2VTCBk4IAIS9KeE1z5UgdIWew7uGFe+5KVAwvd+Zoph4xi3eZa7nt+Fm99tcoHDoJ8mPa/IGw4xPlrZkVXJdBCe7hFdb1p3mNG',
        'YfRz43y4uLFUFM+6f9P6jYm2lJX/GaKyE8jc/tt1cIsOlzw8k7aVRS4/dl2GDspywtXT6C64SgjpKte1LjtkKGknx1MfLGPrIQXsKgpASMkniwQv/ADJdKoj',
        '279u8ao2r1zv+lPGk3Ly3PrMLL6YlyPbN5zC0QTy/ooqzjXosrnge1UCSYS+N7deRbMIum+T2yuBookaK/4q6ceowVoaozRPi4j4a34qKcYN8SuZbzx+NMpV',
        'XProbJauLvLnyWPpX5vkpBu+1MA5ynXts/dsYsigLFf8fQ7NtS7bNBUqLnFHwV++sukoQN3AvvMRQrUvWr7W0MYaLjl6HVbMW8RFj81ldV5RN6jfT5ZtFcz7',
        'e4rIx7U44se3ciukpyhI3I6g6DVlEY8K1mLVHa+h64KNHvp311UBcNcdszZKKc67fya5ouKmE8eStCWn3vYt3UVXCcvKq6KTOnhSH7bedC1e+3gJT7+3iF1H',
        'SFpr8mV8tzIveHmuZGGX8MNa7YtXDHEKxeRJB45j3BDJJ1Nnc+e77UjLKtQ39vtJEw/iCV49txY2JHrSf3EisKcWvdd0/OO2dsXFQGP7jRHzsT4q3lYupUzg',
        'jhoFwDkPziRfVFx97GjSSYspd35PrugqmbQ73Fyxdv2BFkftOpwBwwdx3FVTyRddRtQLRmQc+iVcul3B/C7BFyskRQWJVKKrbkDfttXL2xvynd11w5rquen0',
        'SXz30Xfc+9pPfLtcka7NrMz2qV0O5VZiVLxVcg30ZOMsw94CYn42748Xz/osFqtssUUNoDL3QPcVsUxDmXUV+UxgoLmuEwB39a9HgoDzH5pFrqi46shRpJOS',
        's++bQa7oqkRNZklhdddAS8CpW9Wz9sRmcrKe0699J5YY0pJFO53syNRlV3Wv6uiX68zVSil45rp9mfXNN0x7bzb3febtQ+jT1H+ukNIv24ojZBTMSoZIFLy4',
        '+6LXB4Qpf2acAQTeoeEpsAUAACAASURBVDfglRIkk8mQPo468q5yvfpNCOvG0u+x3/v3BnvuQtyugfvTYS0IAb9/7EdyRcUVh48glbQ474EZ5BylknU1bcWu',
        'XF+3UEyPqYdDtxjIupuP4fP5gsvveDtEDMu28lYykZNSOrnOrjrlKqtf/xpuu2h3Vs35ntlTZ3PLeytZWcDjtr61y4OttvEmtRD4KRpdgl5JLJp6o5KYqrYw',
        '9Hfmdabec0qnHnmb9qVPzLi95toy1IaIv9euWhTFG7h/r9ZzeutXSMdddagH3AV/84C7/NAWUknpcaCjVLIu0yaQMreqYxDADo2CbSYMZMJ26zM/OZDLrnmF',
        'n+Z5NSNCSke5rm8u7bHXxpx98Di+mjqNuZ/M5dGPljOrUyAtWagb1PensHmuXQBv5I7jhPJoWjzpFuWispVvEC8OCPOYjTiRZoqzsAjWZrrHfWZMMuqsa+c6',
        'GoaLbdoFIDwvHQJUpWpsH7grDh4OQnDx3+eQKyouPXA46aTkgsd+JOcoZddk2qQtXJV3soXOXF+A7QYqNl+vgZaNhrHVVhvzyGdL+WTaQhYvWsXQYQMYOaof',
        'u2wyGHvhT3z4zhf88MVCnv++iwU5YZymIIrepErKt0JOrJo/VamZUQgtakwRWDGFEwFdE0zv445yo/5N72A1QTAeUAaYW3JZyizLUr8a8LhWGbgDhpFOWh4H',
        'Okol6jJtUkpXWlLlV3fXO7lCHcDaWcWWw1O0jOjHgJENjB3TQn1dDR2d3UyfOYfFPy5lxfyVvPfDaj5aqsi5nv6raejTFjX/PaJVByP43eNMy9JVzfETrJQZ',
        'CPUZY/Yr5VIsuiQStm8QaNC06NJ/Pd3jlPbLVTZuNJfq5+ULeUCQSNj+tWa2IPpdNHISC9zF+w8llbC8zyUdp/NxQghV7M7X5Fd39fMmDqOyirXqLYb0T9En',
        'Y7G622H+8jxt7UVmdUBn6SBRO5XorGvos1SBo0sP3ConxEY5LSoae3IRokBVuswTRY6/ebCQLyIkJBLJqjow2m81N0NzueYkpVzy+XzJuDHilHFjNHw4fc6J',
        'B9yhzR5wJR3XE3BCSIXrJHKru+qLuUK2ItVKzUrY3clsapWVSnRaliw7acBP4QhrDbguLPr0Bo84wvXkt2lOchwX7wgqGfqtnI6+tVJ6gBZtAilkSCxGzX7H',
        'LXqcJAV+SM9YgFqfxZ3UUAZc1DjpDXC2JZVCIZSycp25rFN0kspxbNdVUgjhSksWS6V3XVbSyolSGbtZL6lFnlIuylUgNKFE6XxG/El6AMUT3wtxab/JKgPK',
        '1EtxoS/9myZinNsQJ2qjOi26rcp7jrYMBaGjrUotdKpDaE5GNiRyOI7hDowouQOzY3Vcsi5bAk76wEkpVOkUO+VNyPvbmwCuFpOO4+kT27ZC+7fjArM/VxxG',
        'r49zur0FEGcEedZnHPd592nrVPk+m6uC8kHPeLF9MGINkjjfjphTHQSVHfCoVRkA53EcCGVZUpWiBKrESaraqo226KGiPbU40z9O9/RGzEafHXdf3DXRcged',
        'cXAct3SADKU93+EDbbTo9PNuJZ3qPSccaPbHomKOBo4Cd+2Ro0AIzntwBrmi4rKDW0gnJb9/ZJZvVVqW5XMcoCxLqJJO8TkuIECwQsOOcrAywTOhHScIDfUG',
        'iGhtf6UQ2c9vri/adN964QQ6xwmF44JNKYFLEaovAd9q9Df2R4p1Kx1sqm/XJ8T6QWYhBOf8dQa5gsvlh7WQSlqc/+DMiDsgDFGpOQ5VAknpA8bc0pKRQsaE',
        'pyqHDXrDVdUIresnTZ8t2pfPOYbZHTUk/JGWTH1tNMXVX5YvzkggINSfmTgNL4LQvrzS2jaD10HIy+C4m44bAwLOvPcHcgWXKw8fQTppcU4p5OVxnKnjRAk4',
        '4es2/TcKQOWKrYArTW6MM/nXJOkZ57vFLZaoJRd10jVR9SFqtmWXEdIESX8Xt0D8e7zzD9DGV6hgyYyqGPqt9D9Ahs45WQXU3Ty5FYTwsgEFtyzInKyraRMS',
        'V1pWyDgJdFxYVJpOo55cOWi6idAx7YGl6a30NWlxRkpPXGueaFdpL53pPJtc6bqRPQhmpD8qVXwuxPfj/HGaGsTgNtdxkDqcZi4oDdxtJ6+DEHDq7d+RK7hc',
        'fczapBMWU+6e7gMnLekqXGVZthICzXWxHOcTRYoq4IUJHpcKqc5oYY41SxPixFel+8OLJjymuDHEOd9RF8Hk+Gg2Q3+2be+QAfM+IEa1lNdf+sDdcdq6IASn',
        '3vwN3QWXa45vJZ2SnHH7dyXgsm1CCt+qlNJWQiglBEhplYDzdJ0plkyiepMLoumUfBy9f7uSTqrWTKJ7xT3xDnhvmrefnCpcrsfbu/6jC8obo4tt26VxCv87',
        'U7z69xsiM6qLfeDuOms8ArxShbzLDSeOJZ2yPCANjkOokkWZUEKoCMehyveSBQDqNEi1ZlnCr/+Pcmtcq2R99eT3mQ56ucNcWa+a2YlqvmP0CPywteud5G5b',
        'NtLSwYewYWSKXL8U3wRVA3fvORNAwORrP6U773LzqePIJC2/5sQzTmxXSKEcx1G2bZcc8DBwIELcZR7ZZ+bcKnFTVDyZurKSVddTi3uWmS6KAy6+lTvnUffD',
        'FNdKOSgXpJGW0pkE7TqYxouv64jfH+h1UPqogbv/95siBBx31VS68y63nb4B6ZTlAekDJ10hLeW6rpJSKsuSvdJxnm+jcBzzdZxBYjHuHs/C80DWB84EIa3K',
        'L3AIWtiKNDkoym1rKp6j15ll7NH6TwhUgSl9wBB7MVuqvPtNt0ivaK8bH7iHL9kSgKMvf5/uvMNfzt6YTMrygPSBs0o6jpBVCaZVKfyH6iMghAhM32DCAfHi',
        'Bl3JJTDTK7of03eLIywQMvcrt8rSQCcw4/zPkCWNmakod0G01AjpLBF7aXhYEDr9wQfuscu3RQjBry9+i+68wz2/24xMyuI3l79Pd8FRybpMm7RsV0gPtEru',
        'gI6IlMgVsoa0mNDEic+h6fvDjrwnIk0dac602qzDrScATcc5el04cF1ugGlQguujvmTwjGgZe9yBOiEu09/ps/A0cE9etSMIweEX/IuunMP9F25NJm3z64v/',
        'TVfBKcUqRckBJwKcVCXiKrPsQAdGvWSj64e2THDiW0AUz0BxQ9d7hAqHo9akRfVopZRPbx1/0/+LA7y34jd6wHdc1ZefJdHAPX3trgjgkPNepSvn8NAftieT',
        'tjn8/H/SlXf8kJeZ1gkbJwpAmSvSjNvp1EZ1eVD+m2VJ9MEsprVYzbipTqRyA6NS6y3BdTPrLYMakXA/PRlCZrTF65QAQINEPnDP37QXIDjwzBfoyjk8ctUv',
        'yKYTHHL2yz5wQchLi0rli0pvgB4Heu9/sUIixNRHPUyfgLs8QliWpFj0rLVooNcjWO+JG9eihO1tf3FGTujgaxWIvmpOvG/kRDaXlI3TiK/6wL102/4gBPud',
        '+jRduSKPX7M7mXSCA894PgJc4ICX3mqFEEqVDATDHZD+qotWKAVObLCEXNfMXmvO1IaOG7o+GiHpidA9XRMlYjTC0du+ooeTVgKt2rOj35U/Q4aBe+WuQwHB',
        'Pif/ja7uIk/fuC+ZdIJ9T3mKrlwxBByEE6lhHQfgpe+FxC8FiL6XTV8XgBHoiDgzS69G7d/0PggdJ4LjxXKcD/mf5P9M40NzYaX3ykVBiupG72+UPoao/Od9',
        'RwKCPf/vQTq7izx76yHUZBLsOfkRfyux9N0BoYSwlJSeMAhCXkJFX0Gi3/SkVJiLAgIa4Rzf0tSE9e7RHPqf+lwBUTSn6ufEtyhhe/sMy9KbJvXcg0VmxivN',
        '05HixxkGzftSD46wO/Dmg8eDEOx2zD10dhd44Y4jyKaT7HHC/XTmCiXgAo6TUgMHUgrliUSdUJVlTrQmQJh4IXJF/vbceueb6aZfGBhEMXoCJa7/SofneCMP',
        'H8URlO8FqRozhGdmxONamaFifq+Be/uxk0EIdv717XR2F3jpnmPIppPsevRdZcCVzH7fHfBKFjzO80SjZUQ78M8g8dyBapZluKajmq9VSZx5zeTu4HkeITVw',
        'vfP9/HflGNZsaaSlXsLPNg0NbUmbRwr3ZieR/n+oP3+epYBD8FKkKYBgx1/dQGdXgdf+eiLZbJKdfn0Lnd0ecKLkDnhGSdgBD1uXQaGq/mxMzSdaT3qtGjHN',
        's5NNrggmb8oW/VkDqSM2LsVikUQiGe7fWAxat5aXXsQ3L39mhcZlld5g7DjRl/cGZezRFnXSg+8jOm7q0+eCEGx7yDV0duV546HTyGaSbHf49XR2543yPOXr',
        'OCEoGSdesZBnVQbFL67SG3CEX8JQHlYyCeoPrypxvBYAEwAZBcsnJ3E6zUvUyorPiu6DqziSCuXxQgiKRe/VL3Gx0kqGjTeDYEymI6653wfuoxcuAGCbA66i',
        'syvPv/92JtlMim0O+jOd3flQdsDLw1mlNze6eJEUXTRkEsHYw10KfWnLsAIJCIs33ZdJeNPPC+4Ji0yTK8Ic/p9uZqzW9LPNQwVMI8TrKNB72tKEwDDzLinX',
        'Z9Hp6CCGD9wnL16GELDlfpfR0ZXnrSfOoyabYutf/pEOH7hw5MTz5zTH6b/aD4uLKVYmjh54XJ1/mHCBJWpJqxdugR6LWyJonGUb7xpU8uW851fOMMQ/H8PS',
        'rmxdVuI0cwGEtll99uofAcEWe19ER1eed5+5gJpMmi32uSQEHN4Rh0qIIAMe1XF6MkEzQQxNPxhN6G/EAAmJQo9t/5suQk9cWM1PLI8O6RZfWGsmeKMbPADM',
        'jSSVnhMqXfjitT8Dgs32Op+OrhzvP3spNdkUm+11IR3dubJYpZS6PE/FOOCaKHE+k4r8X4uwMHgm8HFnnfTWkAnGsuZ7vqG86GlNWrFYjD31NWo9VtoDXmmu',
        '0gTuyzeuAQSb7n4eHZ05PnjhcmqyaTbd43d0dOVCxonnx9lGPk4ZOu6/SdCgr+hOlsoisTeiK8rh1ccshHeKq/fqzepKqHw++nQ+UebDmTFXM9JSKZqiuS0U',
        '8vrqzRsAwSa7nUVHZ44PX7yKmmyKTXY7pwRcthQ5UaHsgCeuVCjIHDxQGwPlk42bYNglCAwVUzeFnFLf0PHeqGjbuj9Tj5kGi47KqMh1lfYLBJ/1dqieWnjH',
        'bHjhufqkeAMAjxbxz6zWfOC+fesmEIKJO0+hozPHxy9fTU1N2vtcAs6yLRdhZsDNvQPgBZvLHkFgUJSvpnALShyCai1lgFx+c9RBLrdIo80DToemespqV/t/',
        '0F/Ykjbnqf1Y0/eMngftPxPTxyuvEDA50wfuu7duAQETdj6Djs4c016+hpqatPe5K6dS9TW+Ax5UMpsZcF3RbK5wc/DlRkRo+qHJBsHZkHz0m1na5/mNZiWw',
        'ua/757TKhkdPLbBkzbKKuL0HlVI55eBH6OcD986tAEz4xekecK9eS0027X3uypWFvHSQOShBNzPgbhlIpqLV0YLK3Ccq/D/oA0TZRv7w8wK3QX8XEDOq41yf',
        'MyqOaA1BNK8Pf29mCpR/apAZCovbJeTNxfu/67prApw2TogpXbCUx9qWMaVoCic0fMKcpP8f5ZSonop+Z14fplDUAAjGFL9b1XVVKTQV/f3ncG+8wRTlvN6X',
        'HAaL8T8ATkdOAuPEsyoDAHW1sjngStwVdhk0N0QNFLNV1l3ehvqI3jAMgUqcopRTElsW8Yvkv93MeSk/g2COr5I00fcYxomn4yaWdNzHL19DTTbNxF08HRcu',
        'z5MKKKvy0hlx/NMQdPGn+VBhfCdKn13js/nXFGvVVqTEXI0QWKHx4Ef7CnzHcnBV6Jqf18Jugz5+Qz/brGLTBozprIeSuhWtyhev9oDbbYoPXFyxUBg4qQLC',
        'BUq5PPNduQUgR629qGgMm/Rx8UKz6V0vQWVYPHjVxuXtTYhat6Z06Lmf0JgMrorbrWRcWZqDQsc8Az/uDe+dqJvsfo7nx/3jKmqyaSbtdnYpO5A1dJy5P04B',
        '2p8z6yqlkZMLthOFgak0+WiLhsyifliUq8x0SfikIjA5K17nmcaAycmO42DbiQpjNFtvNod44wk/S/8/XL+p+wpKEg3j5Mt/XQ0IL1LSmeODF66gpibDph6Q',
        'KlFvbiVWMUFmEczSIIAQMvSalDBxwis1bAFqoOM5LEz8an5bb1r8ngDdZXXDoZLxUkk6lD/bLMWPiyS5joOQgmLRwbKkV8kdxCqvAiHYdM/f09mV54PnLiOb',
        'zbDHEVcwZ/5S7Lr0Ytu2C7qSGYSRAZcGcOWTNOW29zkAxQQrzncJT9jsW8ZyRlhnanFt+lVmnwEhK5n5oSKlCvvoet+i4wi4SI8zny+STCYwVY6ugNP1pbZt',
        'm9mBKwHJFntfQEdXjrefuoDaTIYDTrieH35cRKI2u1gmREFKS5XK8SKlC4GuK5+8tyvFq6/Qxz9YVKo/MUH1xJNNuctgKGohI5GTuJRSXIWZwPThwjtt3GDs',
        'xgCrAddT3DUQn+VSwnVN/RnO0uvnSklw7JQGbtqLlyGEzd5H/5m585fxzJ0nM2ytBn55/M3MnreURG1msUzYBenrOGnoOOlv/gjmaOqj8GSCDRRaPJRI2Qvn',
        'Nt6tiIpPU9mbQAZWavjMZu/cruBEocClMXNuPbsVYUsRgnpRr4/wpk49dq88P9Cf5jzCm00CfR1kwJ+7AITk4BNvZcacxTx603GMahnM/ifczJz5y0jUpRdL',
        'O1GQZX4ceMAJPODKDQX9OUgkxh2aFo7xhUHRfzXHmd+HP5uR+CCwCyAj14b1WkAss0wgPJK4LH5P6aLg9/DzwveFN+aH/bnwG728ew3gPnj6LISQHHTSncye',
        't4yHbzqGkcMHc9w59/DFt/NJ1KSXyqSdk0KqICengbIMzov6XlGCBQP3CGb+Xr5ZwiBbRdEa9Bn4b+Ein8C3C/Rh+eIKQmUiRCQTaB2RAYwaGrO/oN9Ke/LK',
        'nxlY3d65L96mS734FCqUYBXCEJXvPnEGQlocOPkvzFu4koev/xXNQ5s44w+P88GnP2JnU8uslN0thVSWbSmlRGkrMUhhKy9AqpQpAoOJhJduYEW5sb+b+idK',
        'DD1pLdpA76uWEY4NiGzWopRXVZmENxdc4Dp4RA0vvnCCNc6yNF2gam6BDlJYmDFeD8BiaR+GLOvDB+7fj56MZac47twH+fqHNq48c2e2nLQOF13/Av987wes',
        'THKFnU50BpxmKyFc5Yk/S3OaCiYbteJMC88TZx5RHSqDZxI3+E6fyqPzY55+sIxrokDo1W9u1wrrvmoENsWo6c+FD5Or3OL8xvBeifimHfTo62i0H9cJZN58',
        '4BikneHMK5/hg8/mcsaRm7H3zhO56f43eeLlL7FSiVVWJrlalpxvIaWSEiWlxLIs5boKlFLmttgAQHxixqU2wsCYRCgniFKUDhYVPseZBIr6hppzteiJO2TN',
        'HF/5s6N6R+cHg4iHR+TouE2LNVyeobnePDEwGpEJq5LoOD2OWwA0PX3T/vTp28Blt7zKP9+fxZF7j+OI/bfg4Wencc8THyOTdoedTa2UUigppEJoV8AqVTZr',
        '81mndvAnFoiTcgvTHGhUp+l7okZCsVBESOkDZxK2PKgdcF7coTneFb3blWPqwICIwrc4y8Vm4LoEZzirEl08ne26qpSZ0G6HB3hwYp55kI3yARZjmhu/BVrv',
        'vmRnmpuHctP97/DMP79jl82HMuWY7Xj57R+45r73EQmrK1GTWi7Dxx7i/Q0c7yAnFwuPD4Q+QiIgWOV4ZpSAXpl7cE5IHIHNe83PlmX5W6DK82vhjsywlbkg',
        '4tyB8vRMmIMDP8373tvzF5wUG5ZO2vIO/MhocliMaW78ANjkz6dvzvrjRvHI85/z16c/Z0JrPy47ZXu+mL6Uc69/E2HJfLIus8RMpArhbfSwpFTeSnZDRx+G',
        'J6YHaGYHyv27uFYpguI6CiFF6HfddyWu84ghgm5KXBT2m8LE7G2FWBS8wK+T/iE0no8Xjt26jgodqVGt+cdtjGlufB7Y4/zfrM8Wk8byyjszuOGhabQMznLD',
        'WduwrAN+c8FLIISbrM8slJbeHyfwSvSsUobASwxUB047o2aBqkecsMOsBxkn+iqvePO6fN6ryvIMAH1VYJToLVGlMfvELhQK2LYVciHCG/YhThSGJEnpLMpi',
        '0Vuofl1Mycc0g8aBuO19ATF4HHcvcNTk/Uayy7Zj+eTbpVxy+wf0rbW58/fbYKdrOfrCl1m+Koddl1lk23ZReNurlBCydNioQoPpWZbxVlZ4JXsEMFH2Xoog',
        'S2/RqOyvRXqN/PWa5wuJyPemma8d9dIR8lKXhys/JghETPFKpr/3W9gH9Raq1oHRLV69BahSE2OaG/8InHfAdk0csvt6LF7pMPnyf5GwBPdfsDnJ2r5ccseH',
        'fD59CVY2tcxO2t3SMk9eKAGnhEKAlzWAsFlu/jUHDtHCH9MSDFunZgsHaoPW+435PgEihg3gA1iee6vUTBBM18Ibj6ffKp0pveZjBg+4k4Cbd9iwHyccsD5W',
        'KsvRF/+TVR15rj9pHEOGDebe577nxXfmYKUTq+xMcrXny3m7dVBKWbZEKY/TXNclkbBV+erT5m2Yyyp9F6Tzy7k3urW2vD//f8a3UTHnLSRdrKNrHr3mxojp',
        'SqLMFJPBc/XYg0Owe3LCK/uRcU2MaW7cFXhx/ZYs5xyxPqnaei64bSrfzl7BcbsNYfstRvH6x23c/fT3iITVlaxNLwehpBBKWpZyHIeEbSmEUIVCESkhmUwo',
        's4YkajBEw1rBpE2uDHb5BFGWEqkipr/+v9Gr30+5T+mW3R8+yY+Sj2VszIgYN+UtnGEIbYky1IP5rCj4ZhrH7DcecM8BHw18P6DO4s+Tx5Hu05+7nvme1z+c',
        'z9br1nHsfmOZs6TABXd8ipDCSdZn2/SxUFLayjtj31WlN2yqXK5AOpVU0gpSFOFygXj9FxBcEA51la/ucrFb3sIcYwZz468zRbZSnpqWlsmdPQMHUWDCIjSc',
        'qtFcWu2UpcpNjGluTAALpKDhyt+MZMjwJl6Z2sb9L8ygeUCC3x8xhlRtHZOv/oiOriKJuswiy5ZFIaWyLEtbmCUDBeU4DrZlK49WMuSAx5vqoeEYf8MWW1CA',
        '5ITuj7Mw/d58o0CUcXs5h0az6j+nTE9fG0ic+PH1Jqtgfhal/lyEUorWlqa3gS1P3mUgEzccyqxFeS699ysyCcHVx65NzYB+XP/YdD79fjlWJrnCSiU6LSmU',
        'ZdlKIfzCWA8ZEHiGSkCgqDVZNoWYSZeLknC5uOlOhP27eH2hIn/NZxuObQUHu3oLQDXFotefKR30s6otAvPhpsjXwJUCAiXg/gIct8+EWnbdahgileHU6z8l',
        'V3C59KChrDWqiZc+XMTjr89BJqzORG16hfCOxlDCC3lR4jo/kVoq4aN8JZeGFzFegsGFfbxA3wRmfNCnW5HIUZ1WKZwWJwZ1aaHmGvMl8lq0mTFI85nlOtV7',
        'hq4biX5fqfX09i4N3KnA9ROGpzhqt2Fk+/fhqge/Z/rcdvbdsI5dt23mp1Uul937dUnPZdpEyaosFQ3hnV3pHRFbmqAKTGJvsNF8WjWix4s205oMi5Foi3s3',
        'TTyBTAME9AHeOptQLDqhw7ZNsRtIkjhf0uTw6MMDCRFXDRYOVJQ3x3F84DYBPmisk5yz3zDqBzfw5FsL+Me7Cxg9wGbynsOpHVDPuX/5mmWr8iRq00ushJXH',
        'T/HobLhACku53oOVR7BgkKZbYBIuIGzYAg0TPKz/zAmbR06F/S/XeIbpfEeJqIjjAl1S4N0TzlBX5pzKDnrYUOnJ4DHujByJBQHHJYA2oN/vdhvI0LUbmb4o',
        'z3WPfU/Ghgv3GUK/oQ089MZC3vpsMVYq0W5nU+1SoIQPHEb5gp/V9sELxyfjRaZ3bfB9PJfE+1OWJf3DTMPGQRwhom5ItOk6kCAqr3eWhoMEUF6chPFbvM+n',
        'y+yCrWPmfb1bCH5gsbWl6WVg5wPHZ9l848Ek6mqYcuuX5AsuZ2zXnxHjBjO9rcCNf/8eIWUh2Se7GLwNILIkLo23gHjixXWVkMGK1mIlCDXFtzjOjP4O4UIc',
        'CCLp8TFPUxxGDwwtJ5x+T45SXvzSsiw/WRvmuKjrUski9UdvqIyeXIBw2UUgGcLAXQBcOrHJ4sBtBtN3rf7c9twcPvthBZsNTXLADsOoGVDP6Td/TnfeKdVZ',
        'WgXNceHioeCIXxE60l5P0JyQafJ7TZc2lItVbRxEnVT9OxGCVCqNCK6PimX9//C+9OD30ggjY/eviIwpOk5Tt5n9RqsFzMUhQhs9/TpMA7gtgHf6pwWn/mIQ',
        'A1oG8MH0Du5/aTb9UoLTd22ioaWBh/7ZxrtfaHGZbpcChdABZx8wZRBQ+3hlERMPIF26QMRCK/8ubF1KPzAcJkRpR6uQ2LYdKtb5T1uYu3SE37wiWmirf5Ql',
        'XzK6qSW4Nsx90RqbsMjVlimEOS4BLAT6/9/GtYxer5FCIsm5d34DwFnb9GXYuk3MXQl/fuRbhBTFZH1mkZB654403AJM40RVWsnl6ZIwZ+gVFjVUtJ8mhGeY',
        'aANC/xbcL3zxqcsdogCG+xS4jutHTKIiNqoXA46Njr9cD5dXtUV9z/KTJ4IWtlCVUoSSZ60tTQ8Av9p+LYsdN2mi37B+XP/kj0yf186WgxPsue0QBg1r4Ixb',
        'v2BlRwG7Jr3USli58BEaprj0AVCaaKYYUwp/L7T288LiLhB1ca5DYEFGD70ujzNq48VxnfKOqOwueE2GrunpZRSVm3kQQejpxv/jjJHy76LAHQ48OLQGjtxq',
        'AI2jBzFtZjf3/WMm9TacuuNAGscM4pVPVvLs2/OQCasrUZteHjjjejO/x4XGRP0QWFyWwCvkcUNi0R+gCN8X16LRDvM9p2bVdBAz1c67qfir92nGE7XzXy7S',
        'oCfH+j9rPQNXDywSkJo8IUvLuCbSfes49ebP9zmmiAAAF+ZJREFUUAr+b0INozccgsjWcsbNnwCQ7JNZKC3p6B07QVbcK9czogwhhzwISwWc56pwHDJ8HZgc',
        'GZ1Q9EXv+rnh6q6SbpWW/8I+83r9vGgf5u9eq5QPNF0RyogdGnmP5RDVF0BZnUFrS9PTwD7bNkq2nzSIASMauPeVBXz83TLG9ZMctv1ghoxu4oYnZ/Hp9GXI',
        'VKI9kU22C1DBQdsgvSOHQlyHiLOgAmUevL0x2NWiQ176BIZKAVsoJ36gIz0/TIOlRaz3PDcEajj+GVdZ7Ynm6GmAYYJHLU5T7BP5vZKFCmsK3EHAY40pOGqT',
        'vgxet5G2zgRXPPAltoCzt+vH4HGDWZ5Lc+Gd06BUi2KUpWPu5AkHnLXBEhZ9wcZ5jxu810LHm+nVfDxz+7CelwbGtm2EIFQHIgQ+l3oLJmrBxhsjQTMBCYNn',
        'GjnmIoiPBBG6NwByzYBLAUuA2qPHpWjdYDBDRjRy+s2fsWRlN3uMSrD9lsNobh3KlJumMWfhauxMcqWVTnR4ukyUXAMUShDHdbEDCflI0JNhUnFChptQSWdK',
        'YYWMFJOTqnF09Pq4jRvgicHoe87Lg+X6mVFdGfULVeRv6Ze4gba2NN0JHLtxA+w5aRCNY5v4cHoX974wnboEnLXrIAavM5TZSwSX3/MxSOF4J8j6h9cQ2iNu',
        'cl2IA8PGSrAqA9kfcJsZdyw3HqJE1QTUNZhlyj3GTyx3sPH/lofmwpsSg4335U5/NGMQnjPGtdGsRdns/N8qATcB+DhrwSlb1NI0dggD1mrkyEvfxnEVR2+c',
        'YcONmxk1toVj//AWC5Z0eHsLUt7eAgT+YdveuVvxXBcGDj99EnCNHluwX623LboLNvxbuRGiXx8TvJBPUDmaEY66rInlW254QbnYjIrLKNepeOAAWluapgEb',
        '7T3SYouJgxnSOpQn32rj6TdnMTArOHOvwTS1NjN7EVx0+3sghJPqk2lD6DObK3MdCqUjADqcoxShNIxHRFkSMa6/saOSJIsDw9wV1NN9uVweKSSJpA4mBzWQ',
        'lcS1YTGXrg0XusbraDABqgwe/jXB3x5EJUBrS9NhwEODMjB5u340tQ6jtv9AjrjwdQCm7FRD6wYjGb3OKI44/3XmtbVjpROrrHRytSW1Rekdg6grnbVx4g2v',
        'ur7Tw9L7xeJih6HpGb8rVwWhIcPl0P5dpefFGT6V8n5xLkt5YVMgMitxa7W5R58T9FkdOBtYAAw4amKCDTcaxvCxI7jz6Rm8+PZshtQLzjtoGI1j1mbJ6gQn',
        'XvoygErWZxYKS7pxug56Z6iUTyZeDIXN/8p+TzWxafZVyXI1QYl+rz/rBVbuUpSL3NLdxm9RXVeJMMEcKwIH0NrS9Dvg8rX7w/G/GMigMSPo09DIfqc9B8DZ',
        'e/RhvY1aaV1vXU68/DU+/mIeMmF32jWpFf52LA1gVUMlnuiVdEil1VqpVdNBcSs8ANDMI3p9mPuwA44yk8TmPkDzntiRUe67xV0TBbaKjgNobWmqARYB2dO2',
        'T7PO+i20rDuGu56ezuMvfUNDreCqY0bTMHIcifoG9vzNXQB+hjz8Hh6Utwilz3WuG4hRs+k9Y1G9p/0wL5xVDp4pIn9Oi3JfqXfzitLf8MbN6IaR4N0IVsh1',
        'CHNttG7FtK6jz+slxwG0tjRdDFw0tlFw8p6DaRjVSsuIEWx9xH0Uiy6Td+3DdltvwOhxG/Lnhz7kkcffR0hRSNRnFgdbsKB0sp4qbY9SYMYUDd0XAiSikCOT',
        'qmYE+D2EfCdv9caLzMpcHxAvTpTFibvgea6rT+bTB7yVR0l655hHxtUL4LJ4DnnmrN1qWH/8GIaNHcez78zlurveJJUQ3HPmaPoMn0jL+uPYeq8rWb58NTLl',
        'laubO1gF4HGhwtscqYfYs76L6o7yyEZ50atScUVD8UnWamQwrcco8HELKPysIGxXKZjttWg+zlxIMff1JlLQ2tL0e+CyEQMl5x8ylH7N6zNu/fXY+vBbWbyk',
        'nV02zjD50M0Ysc7mfLCwg+OPvgGAhHc4qWOmfUw9Fw2H9VZ3xe0X0ETo3f299QkD4sVFQuL7Lt0Zu6D0e17jACwvSwxzYRjM3gLnW5in7FHDlputx9AxG/P1',
        'kjzHnXQnAPeety5rrb0xzeMncNolj/Hi8+8ZIjM+ogK6wEgPL8x51Qm0Zg55tGl9qLdUBY53zLUVuaWa4WHeHwYhOMCg+j3Bc8p/7xVwAK0tTb8CHuhfJ7jl',
        'lGbqBm/E6PETOeGiv/HGvz6nqX+Sey7ehmzjhgyYMJ6tNjmZFctXIVN2h51JrRT4WXKFF+TVHKY00bxCaKGEjF995U5sWMxpfy981r8iny+SSNhV/cDq7oRx',
        'lYojZByAUR+uvJo7fl6VmhmGWwPgAFpbmj4Dxh+8bQ2H7rEudUM2oXbM2myz9dnkcgV+s2czvz5ge4aM25QPZ3ZyxMFTAPx9dZ6uKxUXge8imP6dN2URc3pD',
        'bxxWfYx99P0AUVDCGy6i5n+1Fs37efeFi4LigQj0Y7X56HFE3YQo164pcBOBjwD+cnYrw0dvyIhxm/LsB7M46/SbAPj7tbuz1ogN6b/Rxlz7p2e449a/gkD5',
        '+k7oupTg6Hvzr0FEw9Ks7kDHEbNai4aYfo7Ijeu7N4bOmvYX1XtrBBxAa0vTrcDkcSNquGbK5qQHrMvg8Rtywsm388Y/P6R/nxTP33Y4ib6jqV93AkcedgHv',
        'v/shQspCoj4d0nd4RycG4EU3iyhd8gCBgu5pv4DXeppWZQJWFpm96eu/CZyej3e6kGlhriHHAbS2NCWB+UDDKQeNZr9dJpAeMI702uuyzWZH0d6+ml9sNpwr',
        'zt6XdMNYuvuP5Rfb7s/yZcuRCavLrvFqVIQIKp8tSyj99o4gU+6b4AbnBb5Sb3w4s1WPdVYGK+pO9NR6E54LKr3iX8kSHnd8jHWNgQNobWnaFXhRSsHfrt6Z',
        'Ic3r0jBuEm9PXcIxR5wCwJVn7MCuO29HZuzGfPuNwz677QWATCXa7Uyy3QQviKwE4IV0nAGmVvpxjrbOMlRr8QAHwIWC1UqRzxWwE7ZPuJ6C3XF6Mv7yoKwi',
        'ujCi4wusXqPP/wQ4gNaWpnuA36wzsh/3XLEviT6jqRs7keuv/ju33XwnQghevPdY1hoxnmTzJrzw3DTO+O1kAKxMcqWdTnSUuMk4ekNXPce4BgaYUeKb72Pz',
        'flPkcgVs246sVA8gU/nHOc1rUlEW73hX28yI8VwzylKuy8LPCaQN/DzgLGAuMPiY/dbnxF9tj1U/kszoCRx1+Lm898671GaTvPfkOVi1LVhDN+GOWx/h2j/9',
        'EQArm1puJ+0urfM8jgpzXpy1WU39aG5QSpHrzpNKJSkUCyQSCY8QLr2KZVYSu9rni0sNVRqL2af3ffmzgnviv4sLu/3HwEGQKQe4/eLd2HTSeJJ9R5PvO57d',
        'd9qXBQvms3ZzA8/fcwpkhyEGb85Vl9/IPXfeBoBdk1pqJexcHHiBg7pm4IH3yjCn6JBI2hQKDomEHVu+UK1V4ryeRKV3r3fwNdArkHtj0ADGLp//wDiJNr0p',
        'siaT4MW7jqJ/0ygSozZi7pxadttxOwqFAtttMpI7rvwNZIdD0xacfcbveObJxwEPPGlbOellCXydZ7oIUfAAzCx6tHmc4emPQsE7nDp6trJ5rfl9dNep/72r',
        'EJJeOfPBvco/b7JaK88QVOY+/5qfCxxAa0vT34ADW0cM4Ilbj0PWDMMaPol33prJ0UccDMDBu4/n0rMOgpoWGLQVU045neeffQoIc14l8JTrRViieq9aFt11',
        'FLmcJzKrici4FV+pvL1SFr1afzpN1ROnrkmf/y3gBPAdMHr3bVu55oJDEdlhiCGb8uTfX+e8M08F4LiDJnHm5H2g7yjotwVTTjktAC+bWi6TdlfpNS++wYIy',
        'IywlD4bgDC5ROtxUA2ia75rzeuOcRwmTzxewLFmlzsUzLCrlAONcFZO7o0Frv9cKhk80mvJfAQ6gtaVpOPA1UHPiYZtx6rG7Q3YYNG3OvXc9yv9X3pkHR1ne',
        'cfzzPO/77p09stmwhBxLwmYDSUgwnEkQbJWxgq1TO9qxnvU+Wu9rdBTH2oqjVNFirVdrra0oStWOI0ZAZbzveqAiYoPcIpCQe/ftH+/77pFsYgKJoH5nEia7',
        '7z7v+z5ffvdvf++Nv5sPwIWn1HPmCXMQgSgEGrn84ktZtnQJYHibit3wNq1QQdeNIF1KoacSHsbEt4xyjSl9RvuA7FOAHeptdnf3oGlG6mxvPp+O7J6o5eFm',
        'Hps9rsv0goUYRuIAYpHwTOAFgGvP+zHH/XyWaddmsPj2+7jtlgUAXHjiVM48/jBEbjnkNbLghpuTDksqziOZTRHGYFPL5dcxWgXSiLRuTxiTakXfjrFvykP2',
        'Z+sGwmCO2Vf0R+ywEgcQi4R/CfwTYOGVRzD3sGngKoFwAwtuWJgk6OSfVXLxGUdgC8YgXM9dix9MhgpGhsVuZFgwK+amxykwVSdkDRmMlzPVZ2oT9q0UZGE4',
        'WiSyqdJsIUPvxl0hzLh1uIkDiEXCFwG3ANw5fx6HzJwE7rGI8EwW335PUvKOPnQs1/72cKS3HC3SwJP/foNLzj/HuEBFdqsu+w4hRby3x2k8psTq3TRvMIuj',
        'MpT6Xm8MhRxL8qxvBQ0l4Z1+XZn/AQceLzIixEGqV0UKwQO/n8fkgyrBU4YY08j99zyctHkNNSEWXX04irccZ6yed9/p5PyzT2PTpo0gREJxajsVm9aRTI+Z',
        'zomUSq/SkKVGMzcDMgkcaCrf3iKbDbOwr7bRWiPdwUEfQeIAYpHwLcBFUghuvXw2hzROQuaUoZY08tijq5LeZtkYF3/93SE4c2Pk1DTw1VdFXHbBZax+cRUA',
        '0q62Kg5ttxSSjJABkWH7DBOuE48ndE1VQWTaIUuFpudBhwuZ0pJKTw3XefpkYkaSOEiRB7Dg/On85EeTkDnj0EobePH5dZx7xil0dnbicyvceVk1pdFqAhPr',
        'wTOVhTfdz12L7zAuVJFdqtO2S6hKd+8yUGr8lDEjWk/o5lechE5Cx5g038tZGUJD7lCwrx7oYNccceIgpTYBrjipkmPmTkLxjsNRUc+nHyuce9bpfPH5OqSE',
        'S44Jc+ShtQQKJ6NVzGD1i5u45srr+HJDM4DZ5q61pjktaaM5pJ6IJxBSGG2A6TlPyJrx6s+R+SaMBEGDP/cwpLwGi1gkfDFwM8Cv5xZz9rE1KN4y3LX17N5d',
        'xoXnXcLqF1YCMKvKxjWnlmMPVhOqb6SlPcr8qxbx1BNPGReuyC7FobVITe0UIpmU1oWwhmhrupHdMHpYrIyLOUYqSaLBd5oTMACJ2asA+1KwHRrSu8DgW5I4',
        'C9ZwAIA5k/3MP3UCiq8Uf3UD5Ezl1pv/xp13LAIg5BVcf0KQsvIKiqvqoWw6Tcs/4w/X38aG5g0ASJvapti1FqHIuEVgPBHXU+ObDBVq9CdleqHW7X9TGDZU',
        'iRRJ0U7/rl9/UwPTX+tbzO3v2vSRdk6yIRYJzwKWAf7xxTbmn1zImJJyAoVTUGMzeGFVM1dfcS1bNm9GCDh6qo0T50ZwhSsoapzGHj3KLQuW8I8HHjUWFCKh',
        '2NVWxa61CmFKnimBxtupvKdp+1I1v6Q3Kq3tID2tlA3JJDRi4AOTaw9W4vRBl51gPxAHEIuEo8ASoNbrElx+jJ/ptRHswSpCDY3s2FnCVZfdxIqmFQAU+AWX',
        '/DSHceURisfX4KybwttvdXPHH5ew+sVXjRuRokfatVbFpraZnmZKhVqVdZFGGiDSC7d900y6ddw3SVt/gfNQ93YwPZ6pq9tPFtacZPR34FiAYxs1TpoTwp4b',
        'pbB2JhRO4/Glb3HD/JtpaWlBALPKJWfMCeEeXUjFlElQfhDPPN3M4kVLWPPRZ8YNSdEt7VqbtCl7rI6yNBWpZ4QGIp1E49hkhiL5KwU9kVlKStOKZoyR2Asn',
        'p282x3qsZn+LjUjKa6iIRcIXAgsBKgslZ81xM7asBFd4AmNmT2fj5hA3XHcvTctXAeDW4Bc1CodNzSenqJCKhjoormbZ0k+47+4n+XjNOmNhIeLSprYpNrVN',
        'KDJuTNuwmpIEkEjrok6RaylM87CM140caXYJ7S9l1R96B9eQKaGpwWv9kLe/iQOIRcINwL1ATFPgxHqVI6YEcIYilB00BWLTaFr+BQtvuo/P1q4HIN8Nv6qz',
        'UTM+D29xAZUz66B4PMuWruXBB5bz33c/sZbXpaa0S5vaIVWlAyEwHuaYESKkq1Mrc6/33rR0KRTCMobZN3agqUXJ9XqluAxkth/2J30HBHEAsUjYAdwOnAZQ',
        'NRqOn2anbFwYT2EppTOnkghU8Zc7V/LnPz1Ee1sHAIUeOG6ijfJxAXwlo5g4oxYmjKdp+RaWLnmJFU2vJc8hpOgRmtIuNbVD0dRuo+Ud05ERaZImTZcw09NL',
        'eZiD14d7U0FI71hLmMNOFXUY2vNGErFI+EhgERARwFFVgqPqXLjyRzEqGmXUwZPZuCPI4kXP8Mi/nkl+rsClMy+qUhP14SsOMqluAurEGGs2qDzx+Ps8/dTL',
        'bNy4LXm8UGS3VJUOoSmdUpFd6fbOiAllmt40JNB4Kz1rPVK70Dc06F14PeCIA4hFwjnAjcA5AAU5OvMqJNNiHtzhfEqrYvga6/h4rZ37717J40ufS342oOkc',
        'PFoyu9yFe7SXkglFRCZXEx9bzIrntrLyuY9oWv4au3a2pk4oRVwqslOqSheq0iWl6DElLz1TnaEa+zqhA9/TQGFBNqns/Zo1Zjj5sMMDkTgLsUh4NnA90AgQ',
        'C+jMKZNUleXgKcinorYCz/RaPl1v46EHX+GxR5ro6OgCwC4h5k5waESjqMiDr9hPTW05nlgp7Xl5rHp+K6++vJ7nV77Nxo3bM08sRVwosktK2YMie4Qiu4UQ',
        'CSFI6Bm9mSmMhPDpRuYn+bcxDUmgSHlgE2chFgmfAFwHjAWozk0wu0ShosSDpyDIuOpx5E2rYXsixLJH3+eRh1ey/vMvk58PKDqVPphVYic330HOGA8TqssI',
        'lUeIh/N5v7mLl1Zv4P33NvD6ax9lSqMFIRJCih6kiAsh4kKQQMoEBqE6xrjA1OFSGKNrB4ssLQu9FaaO9ZDAbzFXua8wZ4xdClwABAEmeBNMDQsmFrtwj/YR',
        'jhZQMb0Wykt59pnNPPXEW6x49nW6uroBYxMCik6pS2fGGI3RIRvukJNRY4NMiI3FPmYU7V4f63bFeeuNbXy5YTcfr9nIJ2ua2bVrD93dPUO55IGrD9a276Wo',
        'fmeIsxCLhIPAb4DzMAksciSYkqtTV2THk+/BWxygdlIMV1U526SPp//zOU3L3+H1Vz/IeHqwQ+iMUnWiOYKJ+Sq5fhVHwI63wEVZaSGFBfkQ8BF3OdkeV/ni',
        'qw6aN7WzZdMedu/uYsvmXbS2dtLS0s7XO1pQFGmOGtbZuuXrYS3W9sZ3jjgLJoFnAKdjqlCfqlPpTlAbkhSHHbhDboJlQeoOqoTSIjZ0O1nxbDOvvrKWV176',
        'gNbWtow17ej4pU6eDUq9ghKfgs+t4HBKHH4bzlwbwTwfeUE/Pq8Ht8uJareBpoIiTXcvy88I4DtLnAXzOwwnAKdiOjEARVqcmEunMqSSH7LjDDoJRHxMrh2P',
        'LVJIm9/Pa+/t5u03N/Hm62v58IP17NnT3md9BYNQl9RxK+BRIdcucGkCv0PgtwtAYA58x60KVOs5ECMXL3z3iUuHmYE5HjgGyAVj44vUOEW2BONzFQpCNuw+',
        'G56wi7HlBUSjJZAfpNXl4dOtXbzz9naa/7eLjz5sZt1nm2hpaSPek30A9/7E94o4C+ZslqOBeeaPy3ovVyQoVhIUOHSiQZWAX0Nzq7hy7fgLPURLiwmF88CX',
        'Q7fTybZOWL+jky83tbNtaztff93OV9tbaWnpYNfONnbubEVVlW/Ntln4XhKXjlgknAscBdQDRwL51nsK4CPBKJHAr+oUeCSjfQp+j4Jik6gOBadfwxm0k5fn',
        'I5hr2DaX02HYNptm2Db41mybhe89cekw86FTgNnANGA6pmdqQQFcJPCi4xMJ3Cp4bQKPTeBxCNw2idsmcGipuc4ZHI2wbUue5odEXG+YKrUSOBjDM60FKjDs',
        '48jv/j7gB01cNphkFgBRYByGah0NhMx/vYCfNLu5P/B/miXIIlCa2zoAAAAASUVORK5CYII=',
    ].join(''),
    dialogCenter: 'data:image/png;base64,' + [
        'iVBORw0KGgoAAAANSUhEUgAAAugAAAC4CAYAAACxSxlOAAAACXBIWXMAAAsSAAALEgHS3X78AAAAG3RFWHRTb2Z0d2FyZQBDZWxzeXMgU3R1ZGlvIFRvb2zB',
        'p+F8AAAgAElEQVR4nOy93bLluI4mBkpr7cw68wDjuxmHwx3uPjWOCY+nuuqc9uvY7+ErP8g8kq+dHv+E7x3dp3LvJYm+gD7iAwhKa2dWVWaEhYiq3EuiSJDE',
        'H0EQLH/3b/71/ysXXHDBBRdccMEFF1xwwXcB5e/+zb+u3xqJCy644IILLrjgggsuuECh1P/tf7kM9AsuuOCCCy644IILLvhOoPyn//HHy0C/4IILLrjgggsu',
        'uOCC7wTKn//LK8TlggsuuOCCCy644IILvhe4YtAvuOCCCy644IILLrjgO4Lyz//r/3wZ6BdccMEFF1xwwQUXXPCdQPlP/9MVg37BBRdccMEFF1xwwQXfC1wx',
        '6BdccMEFF1xwwQUXXPAdQfm7f/tfbOfFqoiU36H5Z+r9mrZ57fF74P//Z8DYlvDs9x7nZ9t4z9yjzq9Zq6KNr6kL38V/L/jD4Tch5W8wfw7vZ9tn2v2j4Uvb',
        'fqZvGU8+KztG5X5PXQiIuNb959e2+6W4X/r/ggiX/vfw++j/8t/++//q/5FNRKZQ/wiqyLZVmebyXPkzoO9LKVJrDXJoEpFNarUykTjwTU3GZFv14TSXVgb1',
        '699aP35va21lfcf8oHObjG/8zc8V//wd449x4PJZ3xhG/f/NYK9726pMEybM3jV8p3JIm+773xKoynVd90eTTGHAq1QpUqTuSPK8xHng58/AVHy9z81dz0Co',
        'B99V2aQ0Bh3hpEzsy2ZClOssArquNGncVt9+DTgzIjuv7vLEt2q/MAcS3k9TkW07G+zsPfAZKYu6t2ryoJQi0wS8eJyK1F0elNanSvXEPrFA1TGqddvllPap',
        '1prQ1j5OBTJNWtul60vsTw+o+7E85H67u2ejsiJKa9suyyB3xvT6JcL+ObyjXCxS2meQLcuyyDRPMpfZ0euIbxuNOTtX++fkwqR0UWWTucwJkr4rKvuLJ5uT',
        'rm/bJtM0OTxERLZq893kYlLvuq0yT3PeFpWH/nJtr9Xx1lSKOBa/9H/7fen/AVz6/xR+D/1f/v1/+K//71pF6lZtcElYSxUpU6iyI7agiqlTxnDnQjr7O7YL',
        'nGqtMt+m9pzLK7Eo/tu6yny7pXX133g8e2byTJrVF/uVvT9iYC5zThgjDcFGA/87quNYIm9r3Y2OScq0C7qtSpnUeMlwF8kJPivHZfl3RLHR6P5t3XppuK2r',
        'VKkylbmVzaasp8sRsiV8EwV17E9fX04bhWjOP7cqjoylHJZ1kXmadsUj0tMIeGOVshsjtW5NcNlwBqanuvzYFVEFZ8pSx9+MziO+yY1rWAz2by40e/qvVRXA',
        'sqrSvc06Fio7tjbmzVjq6B+4wqiOlkwsm0Mm08pUlGZFcdHWlVafA+vvNE+yrVvAf5Nl3eQ23w5rSXFLjRdquSnoVUopYVzGbYycvmlbxC+ZbnksD53PaR7L',
        'FR6OqvhO07TXTVUyPU1BVrHuImNiWRa5zbNWkBrKJx0WLID2BWCZmqE70g9apfGWf9/LomweGUx2G19e+v/S/0dtXPqfv/lj9X/57/7j3/1fdWMmNKbtV6vx',
        '77PJF3k8HnK73chLZJ2xSenrscFoT4SV/bapkJsm9nBpXbVGRXqMa61Vtq3KPE87bhMJ0QxPaytrG94z35d+bMYrNQji8m4D7XnGZoNNhr+XZRERkdttdmXW',
        'VcfLmCYj7OeR7wUm1fRUn3bjsO4eIppu9z11PVvN19p7d+Nq+wgKKTdfHjQJGuldWNu2yrZVud1uzvMawVbYfq5qXaVuItOcG3wjDzUMVzW8QO9TVwb947mN',
        'QpjHNOOZ3obxYz2VSba6ihno4CVvpMMQ7/uiYwb5IFLkdru5fuoYz+19LxfiQiQa84n2aAsVod9kUO+4nu8QbDT+I9laZZ7n1hfGV8dl2scxUwC5MaDf2+8i',
        'xX0Pmm0Gb4LXyDa1Oo5xKMXo86iuWqtMZeqnjPm6+F0o8FNrbzeyo5Lsd3aU/0spsq7qxcbvZwHzIlIbX519D97xuiP0YViFp72of8CjKsOLXPr/0v+X/rc6',
        'vif9X/7DT//N/9lPkjWmjWiv8NszT5xsr5zXFR6X4gb/WACj/akRvHkPeqFFX+6DPsm6qiJZlkWWZZEffvhoW3wDxR6ZSsvZFphvKwoAP4ksXPoJwTcRj9IE',
        'Q3uSrLQiwY48EBH6d5lhYgJaRJpSYvTXfcu0McTZltWBDB95TNDGU3VWkS0YbNkYrS28gRQxyZGmeOn3aCwRGjAC9AVGR04DHphBM8iEvzE+ex7H7ahiXh0u',
        'tW4734DnzJAwvKMXG997L3fd6r5A2Kj+HJdMBkxl3scVE8z773GhIMJKGwotE4C8kDDjl5Wnjat+32EW+ou/+T3jzfipQQlPbW+km/bAezUWMqUJGTY5mgY+',
        'y7I5ZfqMMYxn2vfx+34ee8Y+o3PUkxnQqM4WHcW1i10fnpuhJ0p8/eiTVOLzpCscBhK3q3WBB3wn148YTgOeNzwnNYaf4IWjuYFR0aFffbl1XeV2m52hDbay',
        'XQE2XAl5avfS/5f+v/R/D3+U/i///T/+w/+hnelXCZ4Jx+BXmFzefq/rJnPi2csnyBONCsGcCJlZITxs+1p/v35+lT/9q4+ybf5b0gmyBcK1CcgIOB2FZDyi',
        'MDoh5NCf/nsTjtbX0pWDsPGrf6vvuD82d+u6yDTNbm6t/SpecB716eh9rmCHKIbf6v1Q4ncMvSsirrsT0KGubkUNZS6mrI/65o3AvXe0mh4ZQRkPtDKSCwJu',
        'Zzx8MLznvX+rTNPshIsZ/VV6I5S9uRx20isWKClrz5RWv4CI37GRkHmrI24wpjdXDzy8MEpssVKJTkchK4yr1e1xznDgwc+MFv87F+xZHccwlSJligb6E99N',
        'RnOetnQR4YzmJ5TQEetyuSNwRrSUtoPCvHBUN5TqVqusy0PKNDWaZzBZqTIDi1KM+YxvijilCmNkeSwiRXdDuGxvLKwC+njayBAZ9zeQRxt3+l4XdaUZ8a+f',
        'f5Xb7S4vH+5WTcp/qO/S/7HNS/9f+v+b6/+ffvnzfy5Fds/LFAYrKhwjjmzEUA8anCa/tcuME7op+Sx4IjgaIFvZm2ABES0LCK2nkvcSTkbscYAx0dvqYxhH',
        'bWWTw/VmjBe/zctzvWZMjIfTaxrd3irkkbPvn2HIXnBzG5nB1n/P/cogU2pH32bKLfuu2w6POIp/X6k/sV8whOIz1IH6myfuwO5DHHP0PvZeWTauRZblIdta',
        '5cPHF+rjboRuVaaZvcq9cs77xvxcxSPNW8+F3kfDtgy+zZJLFcnrEkknlLGjuF+RaHxy20f15HTcPxsZNs8ehD0H9aIWWdfzJFwjw+8ZA/tZI5yBvUWRF4+U',
        'nQh2eJ5vkOs07yNkd21/1yoyz5MzLhAmgV0pM1Rt/uD9RR21bmact1IWWuTIoniPvCHdPtSfODv6hLzD+1i+4bI/+9u//CrTPMkPP3xo79gAQXgL+n3p/+7N',
        'pf8v/a/Pv6H+Lz/98g//GSsFv9r3StS2m0RM+RbJTlgbs5Bw26rc7rd9yysOWpGpFFk33gbY9jhK/dtJPXSnWptQjCqkq3hPWU+YWH3GCTmKDTwiSh43Uw5c',
        'X2aUMD59/T2DiTweb3K/3yUDGw/bKovb+Tl4amXBtq6bfPjwkpQ/AhOucTx6fEW6kMFzFL8MdgMXtOMwjowOuohME22vYuP6XkMG7T7zHWfbqDtPsAHuQQ2G',
        'batSRDNAbJvIhw83gWcSxoZI3WNQLXNBHvdaiZYib/0GE7O3FfvAh13xW43ckQGf1B6Mc+Dsx55ptA9h8bImHsDl9kfGRodV+DeWyeoyUA962TMWHI9/O+A1',
        'UFYua8DAmB/U7NrWsALy0Jfzw6QZHC1iYETzjojuDO1exa3KfJtl23RcShGZ53k3+jfBgWEXp9oql90bV3ql2SFise6xHrfdPppeMWMOOqK1faBoes8lG8B+',
        'LpZlkQ8fPji9jHaBUDt0eel/N54ZXPqfyx/Bpf+fhdPF+D/+5c//Owra6nhvcBN/ara9i94GP3pZo3EVZ4aA3z43prQK/DYKH+DAYBtOKF/K3ARKz4TGOCCS',
        'bavy9vZoxBhXqaOBzJmWD4sAoqHR15/93bfXH4LJVs51q7LVunuNuP9MYSOKN4LjwzMjgwT8B8WU9YX7CkB8G9Bw/T5QbCPIVs72r+GKdqAcUy9NJO0BEzED',
        '8wGToxXzlzBy166LaYuKS2nv8+c3kSpyu9+a9xDYaDYOw2VqWR2k/etj3nt66ZXhkWER6+pKuPb7eHs2FqR5JcywjorQysPwEYHC4hAVkd5bHw30TMZ5Wdnj',
        'KfT8zJBnreDHOz938F5tpfI1xn1HfhHJabPWrcVcH7XPsfsWQ6qyGPDMghQyBOXMWNd+RJlicZ4a3jRNMxn5++KE+A/fKM6jhUDpDpvGlG06EqTAw9C4vpLC',
        'VxxHfexjeuOY2ba4Gsej9Iuvr69yv790iwXsHMWY3Uv/X/rf3l36/3vQ/+Xnv6qBDliWVeaZD+IYlpE4jrefegKIBzyY4TvEnhDkftS8IIkrSZG6C3FxRMvf',
        'aq7a3CjIFaqkz6LnD32NQkqfTYQj1ZoQmxlDZkDlwlFPWddqB8bM82GxtRkVeq8EtkLjSXkw7ybzPKkyVqtpILB6yA0BCNDiYip5DKJgwzv3LJAUVuns2eqU',
        'ZzbFTrCJpJ40Ea/4gyEQIXv/LMNC+MEbaEgfeSkz/tq6Nnsj0Mrl70Z1E74FRjQUMA903ulcsUeDmcNUwAPi3gE/eO+kIOMDeH1th8BUQceDoTHePuN5/DuK',
        'Secyvk/o5zxbSEd+kOhop+Tr4SzO/Dk4pgMYSugDcoJ3OpH6j+wiUpCR51ywwIDGUMHozQwFnFGAvDjj4c7A33+3Q2UDAx3ti4g8HovM89zFuY66lulXO0Cr',
        'fN88+OE7qzyHbExELv1/6f9L/7tnjNc30P/l57/++Ik7aLFpfdWGeXwmbRXaVqYh/Y4+3xXrpJM79sjoQTPeyhSJDGSeCAvC9/jEzscYNbz3jOPxGCuG7N14',
        '4L3g4LJ5SEFchWar7KxvJkizWc/nrmdyG9/4HZ69vb3Juq7yww8/iEifxsza8G1mh0rMi6SHw776IoNkGt8eD6m1yssLLnEp7V08CNIUifitf972Bt6ZQIIB',
        '0pT4gGnZS3dmJMWt9HXPR+076g3YflBERPZ0cfPctpt9KIINXh8a4usxg6p270eCNTfA7V0psl8WgkwwU0vnlRnjxgfRK664tFCfgswbJKNqJfziAiIa354H',
        'DO/oXSrh73PIU2oaDtu67mcEosHzjBHjs+CIeKM8MwRG75+t/wi4buziFJcH/kjmjvEHf267t5+ncqs+bEU/pD5viQyQPG7UfR/FKcmSrlwJHsP9WZPVDl89',
        'zI0Dz4jdVp1Z3UHvaHDk4SweWE/HMpf+v/T/pf+/H/1ffv7rj58cwoSQr4yVGzOOFVTFPwm2l56FnqAyRRn/bl9LKXo4xVZ6/UGKo9iyY+gVbzY+Z0aICUyf',
        'CSL2G7jG+YiQK85K7/1hnpGihRC0fLLHQgeg8WlVPnzI4+H6dqi6Ehh0U4NTRJr35qzP2fhzmfgvd2ea+3zL3Wpa/DdxlayPTZmPT3nXlpO6MflePx8gc9vO',
        '1Vb7UD7sPR/FFD9j3DBfwksBxcmeGj5k50NTsthPbpt5pLhnqUXT/rbxMgWNg2Z6KY/lR7e2PJ3zAkX5iw8CljLJsjx2GotKiI10xqk30H0fPP4GxwoS8vQZ',
        'AwBylT1zpSivLMs6+B6QGdAmI4+M7+cWAMfAF2L17zZZlkXu95fuOS+k5nnueDDDC7wDpQovvC1mRaJuyEJdmkIuVl803jnsJR72EtFv1lUNbOTc11Ch+ZiU',
        'xOLJ7TCr8XyWSg7/xjsI6MysK7uuYRFD43Dp/wiX/s/g0v9/jP4nA52VRmyMCd6ULsdZsbKxrbAM+lVVPzJ2IC0jMhYU1qaIxqet4RkPlv09T5Osu/LWFFDY',
        '9vJKl/NAZ/2wOKa8na73ZBj0DF33i2biafrj+vqyXnjy1iIMsF5BWx3sCekFmzfExn/7cYieS+TelioU5/jE6jkwUGRAPqDGAK+xwwVMPZEyQzeyroopa/7t',
        '0AuCgtvNYCR0+PBX1s9nsnjEb0pBKIduq8LLlV2Ks21rm5tcufGc54qzL3+uJONNlbpNe9v7yQY4S1QsVuw9wioQPoLxyrxTHvw2sH+f7zKMw08yo4Ir9GFK',
        'FjftNUrPj/r37XbbMy3E1JNxTmInLZabaS9L6fklRrqXa+iPXXIUjYcIatjWfft8k/t+uBD9twOIIuaVNcWGvoCvq9TmfWZeBB/HkBi843h9juNmgz3b9t6q',
        'xsMjrd88z21su8Oj2pird7B2Te3WzhBp8vTYsIu659L/l/4HXPp/0FX54/V/+eWf/t0nw8YaMWLlU+MlTLLCPM379rNd1hHLoOf6/RnxlQRxY8jxwR4RPkTC',
        'gG0HHgA7fcsEGb1ouhX3eHvI/eV+sBJPJG73e2ToZOnqWAAIEXfsr8iRURHnkp+dec/4EIXGJrLi161X9VCeM2jX651JRab9el5VgkfEHL3JzWtUyqHt5+LZ',
        'guKDUo6XiLj0aEndPRlE3ghKMij2TEAf3R6q5fpUgWxEjHDM3p9d2b7tHo0sn7T2qya80r6mv2Pe8Wjk+jhvHRdbMHgDeFQv99Ubz8hkMzXarYEnRI75tZdh',
        'Y686S/Z4GDXGqePfSvIn2xXZZFurSMFc2HjfbnPz0j4Do8XEmSz4ktSQUQbl4VJjwA2GIogZnlpIFoYPi5nbDV48ludTyxWP+FN95z366DsbzrhQCu2w8atF',
        'koOhrULDq5RimUJ2/l+WVe7ZRWQlZGLJbFjxc9UvwMe6kS84it/EBi/9f+n/S/9/H/rfxaD3HqPIvKbQObWVCqKppbZixDyjccod30OfxikypwGec8yVlTOl',
        '+Le//SrTNMmHDy+CLZzI2D0hF6rb+qGHLvRmNs+gPD7Zcz92IhZvxLhw/K8qQxHOIMECpAev8P24+PnTeFwWVLnQyG6O7PtyBnWvq4Q589+u6ybwxNjtiT1n',
        'wxPGVzuPPET87shIjYqXFTOHmLQVc2CHLD4t80BGD4t/z5kTvLcaBxebIIoXLITps1hM85QDh22LWV9ERvOo6fJERrHB4wwyXDbWP0B6qLiOjJGsDlZ0Ru/e',
        'OxawDEKyeejabaiAeFj06+POR5CN7bqusm2rCwWpdZP7/S64cOfJ2lP8ODXiaIzPFo/vafvxeNsPS3pDWUT5iD3NFt4hAuOL5YgqXz2vwHOI9Iul2KVCW/V1',
        '8dzPc2m8yHGybFx3MjgZi3Xbc25nMoOMgmVZ5OV+d/KiyZFcBGqZyXDu3p0YXaMyl/6/9L8fl0v/fzf63wz055VsPHl8tqo3Qjja+vKEbIj69ERe0Xv8jElN',
        '0bVczyQkTIgnWBAR2QGzEvrRQ1+X3xY7Iia8x/b1VGT3SIwgEww2xgZ9HRAMdjo7m7PeeMG30ZthbXM/GM+E2fCogulK+65WadvZ8zy7+Rgpkr7N9qZr27U/',
        'AupaZOKsSzE2dlmWJsyZL/oQAu9VHOPl08vFLBhY5UdDjY293hM99pxjPHPv+bOHAhkXr0T8s1F5/vcZI75PnbitmmrsdosHLKPsGNGviI95j/ugGc4Z3vZ9',
        'rULGAuRMP0/Mk+ol14wMy6JhItM0u5sjjyGO+TMKdv+C8OCbOJlfnwWWyRyqw/JmpEdEjM55oYBy2UGr9m4Pb0KYi7hFltKNht7Yt+tKMaMizfuVD5I0WZbp',
        'lejZ3rZ1vxhobnwGOTcyCNC36OlL56DCkM/lL1Jgejl/6X/97tL/l/63ar61/i+//JPGoEfkI/PaCWomph7rnPC9gPICIRI9n842hgGz2Alzz5i+XY5J7QVM',
        'JCgRXclhEHGrIm+pcF/wfXc1bAe+jz4ODHj7VaX1I8bV5gIU27nxJDjejaEf9+Pfz0I0SnKlNfyayi4LwjDsRrr0UoOmkDzNnm2ljgV12Q9SnR+kQ/sMb483',
        'qbXKx48f2vb6M/23dngM2ZiI73sD6shIP29/beObG+e+7eN3mVF49m0GmTKIZc3Y0vmEwLO82EdtWzgPFFxmeGeZMWLfsnniOpiXnxnD/ckeqqMKyzzA7GXu',
        'anli3s8M4mefHdXbgx3sXZZHW2yUInSgV40bu4RIabEzXMVknOqK/pbP6KHGjsM0mTfYXwK2tfaaJ118RpeYncF5vSHTGckS52gjmV18naX05F00a8btdmtG',
        'FRs057JlTNMRLv1/6f/897Nw6f/fUv9TDLq+0MsJRHiQ80MfPVP51RWVLFMQjLKnkdqcd0EJbaVy7P73bfoDBzF1Wmn1mUKPSq+6enAC3ZRnIiWprlzg9Izf',
        'Hwopoa4svtbKaUoqfh+Fa4z7Q5to9zkijRC3EH2b1idljmy8cngPs6I80w2exVXu6FuHelauJ63G8DF3KuBozETUCFiWRT68fNiry8cmywzBeDcPXlDwxx4r',
        'r7y7Noe4b7uSyuMAxykXn5nMLxX2/ffxIKXFwidfNWOWFYY3LKInEM+HhHEoGwDe8LPvOK+8ftfPR9buJsuy7rHWtZVTT3uh0ALr53sWZiM4Uk5nqUFxPoJv',
        'a4THTMTqbdlJ9vpwQFREs59AvsOId4bsXqf+kxubpdgNp+1w9F4UHjqUrXWVeb616ULYD9cFfuzCJZiXC4cMhSvuaQwOPfLo2/53agwy30+EI7UfK4Zczw/q',
        'Xvr/0v/A/9L/34X+VwO9us9Gv+2UtzbLh0d8j1kh4rtt/yZLwRQVX9mFiDK2xpGaUovfWu5W364bjCJi2079Ft221iaY+LZBU0SRAY/iwkT6mR+Na6/Y/fda',
        '1uKhhJS+1cOEH4Vl7/GgeksfE3fcJxMQvVDWf8e5dL8MIlNHgzSWtXcjr0XWCD6yR+2gyJ4hwr0PdTy7StYqxhkgqFATFLhg4z3g+3lsICOuVyoOU/aGnc9m',
        'EGOwgfAzQNZad4AU9fBg24BnB6WeM/7zDAwidsvokTHjcYmxoTEEprg6YlhI3s/Y19gvjVm2C2S0rBljvVupN9CfmSsYZE/wywlsLrWhCMJ2YshIlSrrsoo0',
        'o21taTULUmxum6tHRL3vL3tM/pFxzgDDVd9vthCt5mnHwdJGG7tBjnFwhlnoC/7m8UrTHurHKRyO97mqPazLj4eX2f3z/vel/y/9f+n/UB5lfy/970Nc/Gq5',
        'ViEPyLGAB3LxEgR9Z3Fh+tsTrSeO2uKjSvH1aVo4DLylR+LVsF/17Suaxnz27RGwYrMbB8XVdcycrraADxsmMHa8wIJ3ITKvMWDG2EeZJeI2E59UP07HZXiz',
        'sjUPXWTsWqv8+utn+dOfPvaVijFb9PI+M5wjxmRPS6aYh8xJcjduVXeZGrLyxRh3qzW9CtzhIbXDxXkFCPDcPKRf5w0d4oQYzU0o5WJfxqcaizQ9kFoNRoZn',
        'js+R57cXsJwykOuOsfIm01CPQuQ/VvTAyQwUjycrptgni4lvdFF4J0R5GPmbLf40i1Oc9rjGm3t27MXux/k9XnUOl/pSI33UdhaK5WQseX9j3GbTTftB3jPc',
        'IktyWFB3oQjxs3mRdf59zuKYXlBkKtPYTt3rOzLQWz8G7BHHRCTInCKyrovM0619H2PfcwPdP7v0/6X/RS79/93o/xiDjsJYwY5XVvAeFUHu0VI0ZQ4zEXo1',
        'EqQRwXVdBMrHVrQmUOH1wEGrGGcWV8qmmA0PEKitrowA2UOFfsKbMxrM/rssbgyMGSkE5SLDifiDb6Tw9y0l2/oyXN+vSGuyfWrjNDrU0xtE2rAKTjYmTlpv',
        'W6rHBDsCdzvfaWPSTYk7fDWatqbwpMXAOYWUrIiz7arYJzB0drjNoQ0hsHsXmZ++NpQB8dciedw5t2n4xMtn2BqJtPysIsO3mRUT64h0Z7x7TDeZQcJ0fLR4',
        'AF7ey/X2phcfHaUHY/Bx4xzykrVrMgqHCschPaOdDZHIo/m7BNewAPgtDPVRXUzPIrXJD2RvGMlc5BmOCp89+KWITGVuPArlyt50X6/OB/Kwox3UF+Pc9SP/',
        '03nfpDeWR+PQ4tcjO32JfAv1nxvowOHS/5f+Vzwu/W+/v4X+L3/5H/7dp3gqHhXrpBkxe4LyPderw+3EtGfwM4Wto4B4MrStdeyd2HMBt5iu5vHzHc/SqmUr',
        'r2ylNZ7lyEjF9TOvA/+OYvfiyjMToGinOtwN4gn3c7DxGkl96+tIqRgu/P5Ii/R9PcYtJ2j2qDCDIB0h4lSz77I2oxLl5+kBkcCs61pdZom44h5l19BPSovd',
        'w2U6Ec7ifJ830vu5wUVFWhHzOuN/nCs94vEM9IL9DNhKgZKLaQ8LvfMyCh7Pnu+iFM4U5Bls8uuvr/Lx44uMbstkiPTtvcMwKHihojJg2zQuV7PRZLhxerUo',
        'S+I4PzvuCu/xpPfv+4w/7Dly8ccyiMEkXge/dbfyVTPio4xkQxx1oo1pKi1jhPV32nkqHE5tdDvSHWJld0i9cPaSPzqFIwP3SGbyc5aZceFw6f8RbsAP7y/9',
        'b7hc+v/31v/uJlEGeHYQ42grRMaWn/VEbPk0R0pxZz46LGKDVaSfdOuIMrHlv40DgDIZ9Ac3cIvfuuMTDjeJlTOvF2GVloNgQj+sbIwHs/r4wAziL6vkXkUY',
        'JNmW8ZhheWz8eHvG9cyZM99xHN6TFpurL6dD4G1/J5dM7O/clbknwAzfLoSQItuunMdeLzvlzRkbYixqK0/PWho9Kc5z4Poi3hjwXlfMjx8z/jfOVy+gcLOi',
        '/ppd3DkbiM8YqmeZEqLCymCk+N8Pvq+jcYiyqquFvh/T8bJs8ng85OPHD0E+lvYt+MxStWFs1aurrVSHMx/QqnVr2QyQ8cSgdryAWHV3MPFwXI/nGUYSK7Ne',
        '8WR1RO9fDA+autzq8Igv9tEAACAASURBVCjFw5YitliMceO5UtfrtVFXMxjZ00UKXnGCF1hxBi4ITWF+zuR/hgcWExyGMiS3EZmdsESm6yK/j35HnBku/X/p',
        '/0v/f3v930JcshWcXbe7ud9gqL1aiZPHgjKuomOn8cqcdP3AQmjaJRUi2fZbKdbJowUlmGhdF7c1zQcK4kAZHsfbSD3zWz9NyKGCGIvGBoEpNyZMMKYpNzME',
        'eKzQTxCfxf8ZPvzb2pSGw0jxZCfYcQK/n7/3M6r7OhigGL803dLe3FZXQYYLj2fsd6YZC9GQz/sKugKdZd/jEJQ/YNgrC29g9zQDvvCXtow6fa5s2RuPXOci',
        '8FRlN1hGgOFoXmrjZVMWvceoYSgmK0Y08rUGOsuiKJcsrjbPRNPTQa4Y9ZtaZecreLbPjR18qwbAvlUfjDdcpqPKc5Za191A1zaiFx1hGHmfj+DZBVhupGO4',
        'ajMGSuvP2+Mhddvk/vLi6miLzeq3ls2jW5yMgYxb15XyNkMuW7544MYHchkiX+GbUqRtaztDvngcgX+sK2ZhwByODoDxRSj9y/x5i3XtX9h3IvJ4LDJNyOee',
        '69Aoz7JxYYQu/X/p/0v/W7lvof+HHnQ0BuKxweeWTOl2ln+ZZJpwU9R+Q9Z+cQhO+PJg+TgobmcwCyTM+46fb81Y3X3gEW8pjQbWyhrDj1bX/psYh8Z9jZNe',
        'wrPiFFwkoP4bxO/ZCXq9BAFt1V0B2qGcbNhAkNjS4TRcLCj995mx8B5m7YU+z6tnWlMIpsBNIfSGm+9n7Pe6Lvt3FnLgVuSha6BfpxjJgFHMS/e7TKXFuPIN',
        'b37b+TlD6vHQPMl5OIrxy7ZpaEsVkf62UIPoleH8265cUY/D/ssp524w7KvwPKPd+Hv0r4g3vvv6bWxre3d+wJKVfQ10tmNXIi/HfuDv4so8Hovc73dhaIbh',
        '3m6ttiBa13WP7Z262E5voG/JGHwdZAqa8eWryPH+7e1NRETzANfqvmHjsF3TvT/m8IWj0C6rS9tjI59j1kvp+U6/qcRrvczLQlm8126gL8lgz95FA31ofL8H',
        'djbQPOl3u5o8UZ8sQ2Pc8aX/L/1/6X+F70n/7zeJ2untfsUyGrjIQFrh6+uriJR929e2uOKhkdaPOhJSRwzKOELg4zre/XARBqja9rJfIce++JWTSL4SZ2Y0',
        '5YttvLHA80qKDQgYDSS5B5klLCcqmMsTrl+V+bFjLwgUmirJVd7eHvLhwz2MdTZPjI+VyRjbEv0fKSAvfIwWIiObUheRrp+Hury+TxG2umlbm+lq9A2EHG/X',
        'Oi9a1i2x099xS6xK3fM3z80AYmMn4vv29pBSykGcstKAGefsIRsdPLQ43aM49Hgz5Nj4zYT2EUQjnBXrqLx/v22rSBU65BXrAN9FJcft7iWbvBEqd/Z97Ad4',
        'sdLiyBZhUIB1E1m3dc9WUmRZOC1ccUaOv4VU5Ng495c5WZ09P5Ui5JUWFwePS6AQdqUKfhJcaANZjzpxM6eOjhqRoOd2O2a1HaMYRhPxwjhkAN6LWRPsvT2L',
        'SjXGsqPs6EAXPiyldG1Gvm44Z+TyBBzJumagc1rHA7noZeil/y/9f+l/V/d3ov9dFhdjVv3btpMKDY6tFLnFWlVILI9VbvdZbrd7yzrQK2abGBs4rNKllX0m',
        'DsrXuXevTVrdL/iYxfKvWj2jVVkbqMJEwzgU8fj4FG55XQHbIFxMYVqdPVP7HMptlfUO4kNfleh0C/rxeJP7/U5jzX0d/Y5Me9r6cJxRdRTWvXfghBkHkB0G',
        'yecHxkjtyoEPRhcG6W/LOND6JSQgqOzoUNt7+sjtL8siIiK3203ioVGU29ZVh7ogfjPSXuat57i4DlNhXvqSuckh0lq2nxl5MrxttA4FiLLcRgx14Xo5bZn/',
        '1yvA2Ol+/ERKyIZTxRsO/bjzIgeXSMFwxW2XaB3b9Nu67nnsgVscFyjpfj5jPLiIeuf50CTrh22rjm/hAcL324bsKirH4B2K4TAxi0I8KApgmS7k0WpeeDL4',
        'O+M6lhWLX4/6Ttvq5Vx7Tjzd5IKE2PYqkqZdK+d8cn47paSK/u3xJtta5cPHF8P/TGVKv3C59D+PzaX/x78v/f9H6P8Q4oJYIsvByXE3fqvLVSkiIn/7l1/l',
        '4w8fnwrQ35sXZiyrK6s/KktJyv0e0OOjOYnn9pwnypjA+mPeReAO48AUMFJWWZsRojEQx2GUBzUXOujX8TZZhoOIH5MzjrG6PEH6NvrLDcwYehqOZHhVpoiH',
        'f0T2Mdk9lYqnX61nuX0BnbcvwRfMiJUyZ+9gYzp6oqOh4XG2dyxEooGi+JsXi+v3hrwfvHODe0wfsV4zQjOjcVQ380r87gnLI5QtReN0Rep+I+fZN/xvvmjh',
        'FG+9dw7fqVJdV74Nz9N5HGt/a+vmvOcW0qL9ernfZd0PtykNqxxhb3o8w+APHRMmhAd7dTwd6GIiyjzeosXOD8qr0Tm1MVMdY55z4AHPnh8HC0Upxe/msAFc',
        '25z4g7OjsBMoTPBjZnCDZ/eK0JC1B8Ocle8BWY69xc+DoxVig22r8vr5s9xf7k+luIvGxKX/R3Dpf9+miB+TS///nvp/D3GhyoJVbwwboV8R/e1vv8qf/vRD',
        'e9Y6XweYpzAixGwrZUxE2QrJrxBz5uJ+8XsmYu9NALHvGIU2vXERBcxZn/kbZm4uY0SlbRZ636/sRfQWvnWt8vJyd31m48F7rEbM1RNtr4Q8M9thL4N1XWQq',
        'czgo5I2DI2P1WXstroLjgaXewJJmQLRt+B3A6O17UtwZjAWNZt7QrceJ+MXjndWRGdG16oE6jUeHUYby2sbj8ZBSpBlR7Gns486PwiW+RFHaZOX9igMFZZbV',
        'kRny/D5TYkLv3gdRgB6UpDLWpvc4ZQdV7XeLd95qizdVT6DxJWJCqxhtZ/OVy00/rnEunk3x2XpD5Zfl0Qxy8/5VklGWBg/5rDmW3WK91UCHdx/4MV1GPHCj',
        'ZK1VpCae7KAH+BCdGaEbyQKffQY50KF08azFlmKREGnkhGR4J+DQ/jmo5/FY5H67tVj3TD5EOyOTd5f+v/T/pf+tvW+p/8mDng1KHGg/YEjSDoHw+fNrknJM',
        'ujpELF+ph3I8EYO6Yh3vgUyIZUZEr+zxd3ZQzBiZDynk22X7r4BEvyp/X38yQQzQQ2eb3HcGje3kgjDiH+u3g0cjJxEIGhkbMG6agWDe6cHTGZjjvdALZ0/j',
        'PfTvYh0tDjxcrgBo29ODJiw+VdPAbbW6XOOctQKx5xEfb2tYTmg/hz6n9ratcr/fmhf37e1x6GU7MoDGwMri6y5PyvBQODow63mWDU32jvIYckiC1uBpr5f8',
        'B6kDtLVBOZYbUJy+X0rnM9H96nitFA1lWZa1eZF5QaUGPOLDz8ct2z3pjO40paLB87K6b5PpeJ6ntksA/oFnWnlh6toY0aXG8O9p0Mhjpb2M82v9jMYZG9lV',
        'qlvoukupKv9pXnXrs6ShLlFGRDyzLDDu+wwCucITaAf7xnN16f9Y96X/Y58u/f/H6/89Bn20GpPBMwXbttUtgnVbaesnW9ZEzLkclMT4BLavx7cxIuJ+Jd3X',
        'z0xo8WBVIrHE+ryXZXN/j/onUkMbWXolY349dd3nem0jccA01rfjMbVYOD8evt/jW8X20u1WP3x7gFYrg9yzjUBbBgfznsUVbfOMHWylHrdtY7ws655fmoXl',
        'PreJPdbNf3YHTuznrnDXbdljh2E0jmOC1ZlmBrhhXnzfqxkeRYos60Ke8XhosM9HLaIGx/s859azXrDpb+Shzryw5s3SsryNbsU9rlF5cOjBiLZrtdhqNngy',
        'nMw7zjyVCW3IQbxXMA+yV9ZRSfeKXvuKm0L5fTMKq8g0z83gfDwebUt4KlMzkLKbYPNxQWYN3lbP4dhI31ou9y1jFlE62rbNheXgQGnLE+5i680YR38tC0Vp',
        '9TF9wgDFhR/wRINW4J3PFmnZgs2HfVg5Du/hXOr63uji6IIStBON+HTxmFYSfveq0J6LWOaYgEdPl5f+v/S/H49L/8s31//ll3/68ZOfGJusbHLjqloVwyTL',
        'srqVTtNVxdcb/9Vcv3YK2oijV/o8WjAq+OBHtvKLqdBMMB2MaLKy7YU142tjpArGp1visbR2M0bmyQdz6kBymw7TQDC+DsX/8bbINBeZ51tax3sgE4RxbLwR',
        'kxlxBpzjWWFnTPL6fA2MBYXilHkj+dS/VSS9AoQRcJKTFUodAiwaAhwekGV+eNZ7IrKHF5RJppmNdFMg8zS3lbuFIZRm6GcHFr8Ujjw/bBCPrQ7+eyQPIGiy',
        'g6C+XszxyEOEfx9vb3K73xLDWssq7mYMm5e+So6vLZAgf9SQg+G5L2ZaHLf1a1vrnoHGPOOZAs2zs7Ds4Hd5Jhct4+udZ8UVqRztnRkUEZ/s9lEcMuVdAgbo',
        'DhjZkF1YvDEdQz6wR9zw5rMWhmoMdRmF73AWnYwn0RYb2fFQZlwQxgwNI+CFdnzuvOs7vzIbNG95YrRkMqrXZ5f+9+15PC7937fHzwCX/vdlvlb/txj00Sob',
        'BMcdY2EYiaPhRtsAGSyLrpzm2W/llzLv+VP5MIopA/9b3PP+VPE5IWYrbP3Op1dD2cxYyLaL/aGxKNTsetfbbQqXbOwKgFasXgGd9cMo6fPnz/LhwwcXQ+UF',
        'heGENGj+BsKjdo6f2Vh0NQjoRlfPfCjJ0kB9DRyt3m18Y7w1eSKqtFyvyJ4Ru8VKM3q5j/ACmEDwnB+VvzdOrAzTRDb3Dtm9f5hbHXcfrxy9l9xW1o/MkxS/',
        'e8/CwuM7MtgxXtEYTyRnwzXv1/O4xPo9+Hkc1WGGJXjasqNYvLPKARjO2l++FIY9dvNcdhmyydvbIrVucr/d94NOWTgLLzIAWIz5kCh+XisyA9XutsJIt9ED',
        '9J4xj5lYdIzKrgdAv35nyAzy8c6ByrQirMKOLiCq1XBxfE6qJwtHgeHcFLbI2C6J5JoiHt6x6kt5MhjucmI8cNWX/qffl/4/gkv/yx+q/zUG/UAPZd6aMTIg',
        '3lwy8cAhD2c+GVyPiE3ee5W+fYdMAnmqH5QzRm+YlNGEg9CwSrff9m3pyjJu/Zb6/ia01wtBPqBjBB9x1W1zZrjxNpXGBGZKnCEXjHjXpwhjOrDn27a1C3PK',
        'ZHM9z7Ng1WlxniOhPMDwZFtKf1sBn6+1PzUObxgfvNLnvv74LAI8t0yDllklfFvxzzEPZW0q43N2Ec4I0uM8TUVeXx8ylaKG2E6X2eo+ftt+O9cc8AoePeqT',
        '6zOLiyK7Z39Oeb15JoOxFHGJJ+XRTxasMXY/B56n0Od28l7Ex5z7CtkjxmE7EMRbxdbwtBstdQ8T8HwPw9zaKk3IL8tDlH+mbp61HjPQLQc7Z3spzXuP2Ej0',
        'ZVs3meaixnEVCuPp5YQpHUutGMNRjgDb2S1UZaflWqulknSLhK0tbIqITDN2GnydfMA0g8hPzUDXIZCYhYHrwjxq0YEXO+HnmHYtArf59nhoONO+PT4qhwVO',
        'NNRd+ez9pf/l0v+X/v8e9b8LcYkNHk1WjlQV3Cx1dKNh+xW8SUfQLgK0f9qAQbkUEdkqhKaIyfJNY2IbAZsRo6spoEZXRtcqW9V2t81Pcqx/cnQdhEutUc74',
        'vyviN7GlJXTbFwPSlc19Be1vzzBmYIzmtKaElufijVkoemF8tEJvf1VlCs3ZPDcD0IRDbdte/YreC2pPg4ZLdxK6GP6xLgiJdgCkZodddkG66VxmCgp0FbdE',
        '0WRUjEPvVlDmvOKPBmn0rHDMbjQIRcyAmkqRddMxxkU+HON8FEc7BrpsJ4lbfi6u/UsVMb7FR7EdjZWuRO+5VznSRzwAxmV6/uiNE57DyX2LPprBXCim17zZ',
        'lpaQQ4Kkq8f3Pyp1a1uVIy8CYnpIjIkZv/OeujHLlR6BbzrMdQTTJKeGU36fZ9UfaqTXsGCwFI2cPcUWSYy/4dMyxEi/2HSjWczYFrE+8KKvTHY4kssx76TG',
        'L8HwEGhu18q6LTJPt1yP9JUP32VyMOO1S/9f+v/S/9+H/i//+Jc/f4qCnyuHcaBC0Rpn4wBb5jiw1COeEYlt80Ao97FhvbJmbxKUm3qgsqwUta2cDR8/gEdC',
        '6ByM4yIDc4xZ7n2wLeTYN19OcewVaUak+bdgxPwwSXXl4nf42xP9kTSN9djqftsqpTSbaN4TfMNJ6bHX5hh4PJshu0tUv4VoAge4NoyiEqv5IRWei7hlhq5F',
        'xmShkc89noW6xBS9N9p7ATUCHNCJhwv9rYmZxs/ayC7WAE6bLI9lj+seGenPxL6fWSYS3u9G6e4F9vVXmqPIA6X99gawULnYLuqMSirDNx7WRcaWTAmKsKEe',
        '8YDhbp4g64fNB7z9Pr+6PvMyjMEM9CK9DBhDrZs8Hou8vLw4Axd1iuSebYw12tPDpWtTfPA+YWE5OhTrFz8it9vcchlnC4xmmDeDyfN+V3/gOTzryGJsp6TA',
        '/BxlHuNHD9PfWSy7i5d39mN/yPDS/8/Cpf97uPT/b6n/y0+//PkTkGY3P+dlzRCUYlt58FBwIxFpetKYq9YqUiVl6toGQHGy1YmtdtrKOeInzMhI1VVa3Uen',
        'ns9A6+2lr2cuTHY8CCLCxOtxyYwEe5Ztaf1RYEz0pePm+5ylTuovKjDot0N5zEbGkqcbZgrfF6vDjNva6I/HmS9jiRC3Go8uN2BjY13XlltZxHvjonDJtthH',
        '16J7b2F/KBAXjnA2DcAoLICzbUTvsxmZIpmhjW+Yj/c3zejydM+07y/y4LhyL3dyprD5nMVntsl40/crqS2Ut3HSsdHfurUc62GlwN4wvsSp532DGErT189l',
        'TGbwHFf6fuTl9Lhm5x9a6bAzUusqb68PmeZJXl5eGt2KHBzMDDQMXaQHUy12H57PUsrQ65qxnFfMoIf92f5BfVKmon7sMj1jwDdDNzx3saaJMR3rTMljB8gN',
        '7Iy5MJ0D4FsWL/1/Dpf+/6IaqK5L/79H/5ef//rjp3gFMrwTMTl7OiYnMF4VAlmvZHXFPu3pbyzOsl81YgIsljJrE0iDAPqV4HOdGa3k+Tf9EpusRJr2tRMe',
        'fHArjk80DorDJVth9wZFGeLagzfSxkw6FljRe2dMQ19vtR3KiG23X0QbNt8jg8zPB3uA8pg9vzXFgpZXuMwncRh4W9B9d0RiVZrXTIoZDqWETBgQIKd01I+D',
        '/W1GbXZwMtJOzLE9TVPrSx7+onSr10a/uGc56Dsoz96zbqf5x6ka7e9x+IIapdta98w2McRCFwa8SGCvjvHwkVLQecOBRpGSyBrDdWwY94a/fxY96Pw3v2MF',
        'r7gr77AhHA2Cvo1xqAIb5r1ShBE9Muzt7zF9sBe97guLGF+e45O3x/G+jTVPDhBnhh+/i6FgJhosJKaK5TXmHMkxzMUdNj3SSTRdUXbAO77VsSEh0ocwXPr/',
        'ff0Y/aZfcun/S/8PhsZ18Uz/t0OicVvPrR7iOABHIBKQ6JmJP7QVOlZEu/wdbGkUWrkYwWCFg5g31+vExfDcyhnbpzHeMyq8HnqG9d+fKYE4ZmOhxsymv6ub',
        'qCR2CgMspREpex/Qru9Hr3gjc4/6tCzLfjr/fLz9XMUxzhswDww/G7QQaJAFfH5g6Fgx41Dehw8vqfHVC8hQZzKsW11bPunD4XAdy5U89zOC4nBs+AI6r2Za',
        '3uIi0T+9BvvWvomeSR7bWu2CHtcxRxcm4H1+Yvs7Gm6lCTv2vFm2D7y3dniAWUHy81FsvPGebuHqd/Nc9m3aXCEaL8Z3rKh7ZdzLtpHCzL7Ld1x8eem+MYg7',
        'MRlx2jMfPz8l9BDb84AMFxwvLOIVYLyUi8Fo1p+PaL1J8p0/c4CYdxbO+Ii/yQ5qZ4ZVhsswrAb1k6LP9DXzQhaOc+l//fbS/5f+/570f/nHv/z5E4Qcb/8i',
        'PugIzjwM/aQb2BaW+wpvfQ/Cu4zxeVti/O0xxFPc/nDW8bZSvqL210b3W0VG9DmxWbtMVBkBAO8sD+6zhDx4I7Va3BZwnibOjWz9Mjwt57O2GwVJhCgQjyDT',
        'QqNysb5MINjfZ3OMratS5DCFmXtPTTQFKaqQSplkWzd/+CwyKeoSM8KjAB/xmhe4ZZiVJX6Pf9vB0p2uOO0d3sd0jfitbW27MuBwDotH1BR+WZqyjWiVD7za',
        'ljFve1fyGtYajVuuB30z+vSx6IYH8p2fyTIGGG920IgXFeAhjAO/k/Dbyx7/7cBiehLssOfY4Hge3pc3/9igZV7UeucZYVV9qMzZXADYsB9fQqR98bRcGy3F',
        'i5FinPhIwR8Z362rmU0Zp2WgyoDjxKkmg7Hs1qKhviro46X/L/2fvrn0P0p8A/1ffvrl7z/pn357F14xIJSf7PWIRAJvY1O8ILMYskqeptGAtpZ8hWFgl0Vj',
        'ecoUJ7oQo+nTUXxQxFHThK3tWVaW36khop4HKILbbZJ1pVRmdJAGHjatD7FSlnqIt4fqpmnEvFFhbdt3o/54XM8MO4wfM9k8TVL3fkZDDn3QOo0ReTtrtJUU',
        'cX5G6TKNmBdlRDdm1OYxf7Fdq+dofPjmtPZZIL+W2eEAmgLYxDxhAuVpl8UoPtrIui6NPvjwW5wXeKlwMp6zbChw/LPQs97bZ9lhcoNTT96XtkVuITKIT592',
        'XBYR0ewg83zrcPdghnJ74rI/WEo/PpijCpENLp/iMKcxHgveYs49n/2EA1/gYe+Oc5EzTfq2zFvL9Mu0m1lyvQzk36UU4cOIPXyt4X6Ow6j+bVu7y4zmeWrG',
        'gGvlqcxA75Ep+TfH3/d9iUZ5lhoRz9lD7uqYQpuDIeN0qPZxVm5zYWr2Lcb10v+X/o8jcun/b63/dwPdtgjh4ahVr57W7ACjwHj9160qqjQFFYk44lmr7Kf0',
        'cWEBhIT1MBLU/kswiLzdMR4HLh/T7fD755VSFluWMT4zGRNHP05+hakxc7oFtSzbnuqM691LpuMD8Fv13O8vUVr+Ox6v0didNcBeUGOYfuvfyo/rzeb1uX6a',
        'V2NEF8/h8JxCFzffo6vUMwEI4Q/ag+BPV+uoJyr6hqs+xHXyuFUUh0BrrXK/35rQsC1RypFOMbVSLEavSn/ABrBtm7y+vsr9fm+GWD8GoKdNfBz0Lsj2iutW',
        '2xXz4K1pKu221PdC9OT7g1kj+jacjI7svZdLxoc9/5ZWXn/H2M1o+Hmjo598HJ6zubN+6pkC9YLxYuv3BzU29DzAEZ8g/ImNReYDXWSu6QJKpJePDGeHVmEw',
        'Hx0cNV33fJ730bf4Dd3rbW0fz35Yr/ThOVEHW99XWkhe+v/S/8/Bpf//YP3/0y9//2kqs5QJF0RYbM/tdstvUooVxYPKHeIHq4jqD7WMmLoZK5vINNtqv18B',
        'mdLMB5MH3b5F7NkRZAJGn0+7R7MkDCoCZRpXZtpu6Sa2wNiZ4uGG0eRrX6GQUSfSqX0JIyqO56u/OJajdz3enqnzbcScYSxftBd4I3hWGPVG0l77gZLm9/1Q',
        'eQbP6s1wi0ZXNOKOcHpKzySsUSbp0j9hu62lY9y92DDY2bgQ0XKlHBvIeqmO3U6pQopviOwN0zYaOy95UM94HEcfSmEGv+xe5/yQYzzYhUHyRrc9i/WKnE2A',
        'KR/bieh51Q6/mYcT3zN+Pixh0GLAi/FnnI8YJC4GPD5maKDOuItg/QSwXORc6wY6plC6domJNzJAhy0POXmfIj1wWArvOkVgA537eQaRBo8MVC7Hl3PxO+A5',
        'ipVvdcH4ZM9bqyz8Fmn91naLXPr/0v8ZXPp/r/0b6n89JCrSPGdaYGoDcLbqstRrftV8JKiy57hFT6/6tVyURYq7dpVT7bDX6igFTuz8eEDs3brqLVweXygc',
        'f8J6mqZ2NXE0rvZRElaCccUUb8fDNz5ZvsNYRKrbGnSHgIoxBF8Z3I9FXHX6La1zAs1WrZkhkBFlFJqx3oizN05t1Svu+ZdCTrf5qvzomwYj/SD63B9gxOPc',
        'uEA78zwLLppJlfluUMc6kbM1GiTssat1lF5xa+Eq0YBmzyFv98XbQJflod7yTTNarNsmHz++iDcSGfxhRKYFN25V6BBmrCsaymw0op6p4avK0sfJ57dy2t9n',
        '/OFxMb5g5cnfZ0rK4kqzvp0pzyN8rE01AnjBEusdd5C3rPcapV+8iMSDnDAO22VZgpjWSt5UxatWfRbDB9hA51h161s/X0f0zxCN9PHiz8rbmPRtRuMa3yPc',
        'a9tqyxuv35VOhmx1v3AHw5zJmPi8fetlQDu4S6kSL/1/6f9L/0c8vq3+Lz/98g+ftBHzeHTxbDTWaaqZup9EHVwc0XfKmKuUuXnoRExQ8HXXSnCTaO7miJgy',
        'bq2bbsnXOCJeYvWHFXoOYOUwT9O+5Vc73A28IeFXnjYRuDSBhQQTK561MIOt7grEjAoRn7MW+ONWPbyPfQEuzID2+/3Ena+urZI8rVVXiww1ypC6j3AaK+Uz',
        'ZR0FSFZ3hCGD5mQl8Ga18A8ozsBPUSgAp6NMDi2Wleg7GhiOrXcDgL2K7LHhb5hWY114F413GFVqUK1u+5fj9rj+eMV6JAnO8iCC7XGvlPvLI7yc8Ll2dazW',
        'dZW6VZlJ5sQMJA2Nmhnvnl6xZes9ytlY8qFX6xsvdqwccJIEjrKquNpTnHsejXyZ8aB/zgdzTXni+zgn3kjlG263uu4pMYvEXQGMH3Zq2ENoi0MYQvk4wKgX',
        'kZ0+LXykMz5psEfhJUcyxcai56EimrNc45z7bDc94rIb13ogtF2U8syU79/CiPULHfD6pf8Nx0v/n8Gl//8Y/b8b6LpiaYpzr6g7aS62EncJ1ksImN+VKTrr',
        'MxoYIWsnNTcxHxRjpgOj9oyhf1i5m8zzvK9kaxsAHLigYZV+0scSrr+Fz9dhBF+Et6hjve9hAi7766+fm2cF28HRW4Kram1rC0JNC8X42LOVccTF+ql/IwNH',
        'jIvzMB4DxsuX7VqX87l7FoKmIeFgQtvPK+P8qgTKUwAAIABJREFUbog8tENjyGiIZiQZ5Bd7jrsyIv1QCQnS5B3jGtsdbe9DoWjWlNkJP9AdFMw8zbLuylc9',
        'Upp+UT1TNR1jR5vUNxhSnMFD0c1DEcyIiwaiGSWlTLuhtLRYWw59mOeS0msuzPPQnOxAF7+LcsR7LM375Q+7xvZH+Z3ZMEH72n9uzw4b+zI+t3pGZCgj7hlo',
        'TnOYR4NCxwo8AO9hy7m9095IeQLWdd0XgVh4zc249akVYcT5A13xJtLoFW/0xbyTGFOF6DPKV/2kYFTab0+vFpKg6emK7j6FcjDoEVbGsekplJ5nRNjjzTHR',
        'l/4fwaX/reyl/98Bv5H+Lz//9cdP5pmo/epZnpvQdsqbDQZUw1UWvnhAsYgrHNz4h5vkIEj9ysa+Xdd1Z+Q+DVG/GsoUTfbuiGmLQKhxH59ffZ3Vb9+9vWl4',
        'AG+39au/YvFzRINRmORtHuOieLB3Vn9nt4H1xkBWv+GSbaH6ceXYtZp88yxjx/7sOFKeak+TZogC4BGItPoUA0cmdPygD7Zt8wpRgsEWba6kTShFjh9vOG5k',
        'JMDoCHhgS5ljzKG4PS0VWZZFShG53e77mFK4RpVmTOgWvmXimOdJ+BZQxbc/aDdOxxfnvjQvJDoE75MUafmTfa50Ggdho2u/VKZMTel5xevHi3454z0ri3mJ',
        'h2d9KjdPc74OjP0oHpy96Dy59vdUJtmcFzKrgwmstr71cfmx/5u8vS1S6yYvL3fKqgMDZ91p0AwI9L+1Tik9/YVKXsnb4VYGyyIk0ocRAI7ykQ9HhRQqG5LN',
        'GCL8nFEdjHs+gJ3FnPPOk1PsQXxGxY+D0iPPN3gx7dtkMvbS/5f+z+DS/99O/5ef//LjJzzAFliWUiljUh6sbduTrRdK/eTiQ31nOb7KOstXOTOTWl2lzCrs',
        'nZCMvR8sR1ICAgF668fX2xNwJJxnIBuLpFR4r7/f3h5yv98HBsCIaMd4cayaHQSy8cnKMlFua5/W6Ry8lYltS15V+7LcD2ZsMkSfEIzZO6uX8dK/wbjR88n1',
        'fNGqun0cmhebQ38gyNOip1NUEC13ambfItWtxil8o3/nBzPB09o2FC+HyyzLogfMpnloTNe6yrbZlquNH4eOxMtvpJWFMvcDV1rmByju7CQ8H4BroRQJnvxM',
        'jcyH3G+3cMFQnw85ZnthOkQTnBoQYzhRfmmt3+rG/GOxcbtNYoY3e+Zj/nTjXe9BxrvavkNu+p4IeVz60Bvfjk8ZVKvI2+ub1Cp76sx5P8jH9drYad/Nw66h',
        'JptEZW1nAnraHmU/4O+PeNTeW7519b4ODNnSx6NHuZKlU6x1k2Wx8As+n2H6lm/5Xd3OVKuLfjfZvbf39ngTkSL3+62VxaKCPf68a9H0Ow3tpf8v/W949mUv',
        '/d/X/UXwDv3fDokyniPhFg8x8fYtPBrYlvJMPk6Bo9/ov3xIzQ5wbW3yEEOKWK4RU44nPDJcbbiNJhGEoPj5XMsi0ryCx6tGcYQU6/BCr/0lUIa1Vvnb317l',
        'T3/60BjFb52PhY/W7w+O8HP9JnaeDbhR6ii/1Y62sRI1ATcek769I8jnzoRTZiAyzl6Ye6bOniVYBgMMNJYx8lOrau4Wo9Lao/CU+Fy8Am4e8wrDQOTz588y',
        'z5O8vLy4b0fzAO+ZhRio8XJmDHG4ALIV1CotDaSGtmy7F4IPYqqA9osHzyucCnAqUzN6vHyZiA9i/0YTOrpgZ+vG6H1p9CAHWR4Z/41yMOf19IfOjMZY/nnj',
        '2hvhJkfwPsOXwZRxzN0Zc+BLw+nz51f58OHlqTjkI4ChHm9kVb3EoTJT25XhQ4Ujvoue87hYO0u9KPKcglZeWZsR+nhbpEwi9/tLKhd6YyjHN37DIS+6sHzb',
        'QyEsfOjISZzmNb/0fweX/r/0P9f3h+t/NtD71ciIwHuAErV0WbEcVkymwDPh1A+Qx8FOLmdEIK5u5BL1/cO2q65WeOsC3exXhrX1sY+78oTHFw3EPuSEewSm',
        '7P/ln3+VP/3pB/JMRUbvZxx90uvXZ5HAILhYoWecnhEYfDystudvm7R6z5RaFPq+TetTZERmBvWqbO6d96oYbXlvQ982K43IaBlz531C3d5QO+07oyXhmaEb',
        'KiGDvX1S5fX1TaSKfPj4Ii1etRAdxpAXsS1uzsV8JGhUIPeHKB8P9a7jEB7oQsetP3hpHe35wubAG6yx3bNFxLgdHJ6bm+GP2GZtvxA94TufxeFMGMMLqHXm',
        'vAZFjz6jfuBgdJelVuwNdd45yOAc7yOFKhIXOMvykNut9/BxlpAzXDLa03d7jDJ5thADy3KHDYR4A+zZGLDu0x2aktDoseFsOGxkuHn905XHGJO4XffzE1iQ',
        'ZnHsIiLrtui4TUUwF7zQiLcAcz9HXvJL/1/6/9L/34/+L7/804+fzlYOVDzcWOUbNwLWAR55kcadis+mnYk4UX/WYc/Eb29qIOBQGqdg4jhQv9U8qk/r5JRG',
        'x2DEFSdSFQu2OP0EcoooT9T6/l/++Ve53W/y4QO8oT1DLsu6xwv7+cF46EEevb2xlDiXz0CmtMeKPGe+/rvn4spGEBl5lFZqbOk+a2hp2Wc9oANsn2zHGhRn',
        'XLPHHMrbKe69a69vb/Lycg9t+wwvnPklN6DMKOYyozRzMKT1sNvs2jP+j+EZ8MaMjJ7ei1+rhQQ4HCUeEPQA7yZhLdhWnKaZDh7GTAt8WNO+jQZ7in1LCcjG',
        'EtO+V9oxlv1cScRc7tJ++7h7EXEywz/Pw6eKe486vwxGbRs98Rxnih1KGDnivfGPHRk7f9A8XFtt5zPssq+8H6zoofgzIxsLiiq9sQl8j/nEfxNjzjmVqmWZ',
        'qc2owtxyzHjL7NL1yWSwe1/FHeY8h0v/X/r/0v9/pP4vP//1x09MtEeVeOU03k6M0LI67JdNtCqSuqOwsO1W+9BWfh2GIrv3cNuqfPzwQbwcjiuxM2Lj3/6U',
        'ecSdjYX4rVe2MDIgOKtEQWb9NqPm119/lWma5IcfPjr0rH++XR7Pv/3t15ZDu9ZNfvjhB9pO9AbDGLKx6OMImYDh6fKXChzV+x4YM5y21eeZ7ufHfxvpGXTL',
        '2T348CVDKZJeWR2F9DMMakIbRlJt9OKyN+yGc4VxXSQIO66073qWWcKnnDOjxwziHjgzhHo2zXuJONKpFJnmZ8If1AOv8bTe216KyLqsUqbS4QMFlBte4HcW',
        '3lUQ4zyV0rLNjDz7iLk3L4zf3u4BdGXeplqFsh+U9kzbzeiwV2JmlGudkGn6HfOz/o2YYoxfL6e4rawPPH5Gj/3vKDd5DGJdIrH9jC/GB4WpzIQ4WqU/vf2y',
        'tgVbDf2KWU26HieG9hH0Cyh82y8STf6XAc34OUTOc9zsie/nGZd8SX+ocyTiZBx7ful/wKX/x3Dp/2+h/8tPv/z5k20R5tkIMiFkQe1cc+hgrW1bN8bpZDBi',
        'UhHdAsbqsz89LI1gREReX19FpOyH4+bkkJrV6+oInpcMP49/z4R4ju3YMwLkg3LsbVRlrgfdlmWV5aEH4+4v98a0nCEDK8eW55biNlEOBgVyps5uu6xXTqP5',
        'YiY8mlOkwPKr+jh+ImASPojyHDBT4vBdbb+V7vjgY6HykXEjFFmWh+ilHb3R5wTTYIqj8HJoRzlhze7f2AFN54kIMrW1wZdZhrZ8WjfK3EDnCFkomKdaX+sW',
        '6S2pPDTVDlKOjT9sMfPBSBH0Y2oGPbIWRC+MKa0j47AkuOC5Hlwtkh/YBES6fjwecrvZdeRR+BoU9/e257oGDuta9xjdLeE3648Z43hm2/IeXzPSR8an7Rxk',
        'mXJinLmNVeyPZSCZunfPQU+kvUfKe1mfMtILLyrPIUupGL99RlfBQOa2RwuNWIbDVrCQFVGZqSEC0lIH8tRgcY74c94VmgotgAvhUjVsZp7mzn70IUKX/r/0',
        'v/996f9vq//LT7/8+RO8VGA8V5EkuRvFiBhEhdvgpjLJsi7ppAwXUQlkq1VkpVCiXdqp9fidpQBi/Gx7NI+76mp6oswR/udbIWyUnLX5+voqy2OVaT/0x6vS',
        'vC3/DIfTei9OVM489j6DxPuYZwTBwiQ4354q4e/jcv2BGr7swvebr1aOgmrYhVHzjB6hxIqtq39Q5+ga8NZMalzsbex1uhj1vdvRs+i6kb4yBEfx3poJwz9/',
        'xmNgYLmrOVYUhq5UkSl45+eJY61HE6P1busqUixmF4oLiw8f2x3vL4eRjL8ZbEHTK3f/jrMfWHx+L62VHmeRjvaLmKHL3zF+PtzDxozLHRFxSerkdmIf+z6b',
        'TPJ9i89tzL1MNroxWeV3dzyemgO7Ny77+gw4M4vi0r/7bWQeUpZa3ZzPH8YYaKSU0oyCUkobcuDDF5vUWuXt7U2NeRzU3su52GoCfM4HOy/939X0RJkj/C/9',
        'n8Ol/9+j/8t//PnvPzWijX2vnjkjIbB3S1NUaSzVuqxyS5hHv+G/edWTnaa2Z3oltHmd/ED239rqX3Fk4Y1LTOYZOI5Gtgza47KR2OreXrx6+Ihh+/ytGTwe',
        'D3l9VU+eXZVu9R8JFM6n6oRejal9uL8mADqMu3EZhFa8A+Cd6+vJjJQxeMEle/5nEZ/PGvNrXlv2zIwUfDyUlpXNcbKxGilNPN9wIcneT75x8ax+J/9E3FBF',
        'wfAs/mcCCwZ7Vt972oDi0MwueoGJyB5jXGW/XVLEMpWswjzIcdJan3lskb4NtGop/grVISIUT85KjTCVnv76BUWm1Fg4Y7z4VkdOSwfcR14/nsuY0eX4gOhR',
        'vL/1pb/NlBE5N24su4p5+RnvfkxFPPHypUN15+HohR3Lgme85GPQMRrxLD9PsymJdAtgXVAqXUNRx0VuvwixcYq6o8nsKp2x32L6YVTvZZgXLWRG5NL/l/6/',
        '9P/3qf9bFhf34T4GZ4qZEdy2zcXMdatxoc5VObjhy54zkahy28Qf3FFBavGhNnA2GLWlvOLbr+pWpYpdyDKV8uQhkDgGcZseAjBLTzQax2hRxULG/LXWPaaM',
        'D4yMwfAwws7zXrPg9XiOvAF6Wc1kY1prYzL6Wp5lLG3bhP5xOeDGT31bnPEC5XCAkb0s1kffRszXy+09wxstZ+8o/ISAvdLtwhCnzAqdjo9tmRHKqeecQAmK',
        'nA2Bkd7wRrwZdsxfaL9tyb3LEMra7LNkbGsfdz4ycji0xQ52crowC3/B5R5Kr2ZUo+/okwlupmP8XSUavNaH7P3m+lCryOOhWRbMY+7HMvKvvfd49HzaH8i1',
        'hQvvBng5zLGjwNP6LOG77Hn2N8YmGvxZHdQDMrQ5DV/G90Z7+jtLpXgWLqN1m06AoX20KIiZUzJeUMNQQ4Ru8y3NlBRlSrYQaN9QvGvzLBd/cJzjZY1P7NB1',
        'xO/S/8Djebj0/6X/f0/932VxeWZiIiIZklGJ+4HDoCjmWTt2cMUGPXqg0KllWfdT2xaDh+0N3NDEAoEZH89wyEhEws2J5323Ou13TGPVC/iecD1j7LjugpjH',
        'Z103+fz5s3z48PH0ut3X17eW7u5+vzu8j4UuH/bojbhaldjXdZUPHz507fLYIYvC3sIBvsCh7IJ183Fyg1PnPu1TJnys7dfXN/nw4YUYsjd+DE+L+0a2A2vz',
        'wBCl8sNDm6Gebk4Yjay98L49KzAGrHB2gUrMZDBCMVPi4Esot2jER0PpeYjla/OaFY6vTcrFdoBDNPj7FIXjPm/rKo9lkZeXezMIlgUHQ5U+trXS4VfFI7/p',
        'U9sc3z5YwresqCr1ofeuar+Ebk+NjbPS57hlv3AwQ9lvM/dGNfr6TEYXHhPjzbNsMI3WdkXVH3Tl+q2fPvyKc+2/Lz/7SCkfGQ6cjYb5yte178qID6/Ztq3F',
        'BmtPdtkLQ9shZ/hwWABkVVQvW62yratMc2nhcrFPl/6/9L9/d+n/70H/+4uK6CNXeFCp/WZl4kFXWbaamSmTwyhHa/ubVh5Y8XAOU+7YsqyOQeumgkkPmxmx',
        's3DLJmfbavN06DYYtnnPDYJ+zJ49+GAFvPLdx3XT7f1MeGbt8rPPnz9rHuYq8qd/9cNAaJhRN3rPbTK+uhV+a3WM+qpeHj//I2B6inFp2UrXf+sPsCHcUJW2',
        'zXN1y9rxIS8eZ4Q8mMfkXIAfzn2UCyeyi+c18keLM9+nBwoJ+c7xTcO5tp57lE54HnWk2/OlNIMA3jy+JXE0JubpNuE6z7OTHRG8kWe3kUYvKcpxG/zM123z',
        'alWwEbE/2ePI9aDWgwz2XthG40wEPBvbZ8UARdN723uPXWl1I5vPfJuDomdjQCTGlvYx9yV8h3qOctjH35GYvUFvl1vlN8lGiCkYj+iU6fNMlzG+vKjTb/e4',
        'bDmLR9fvR2czjoxYV0ugFzbisVjhMB/8vt0md2sth8bFsRqFqzyDL7+/9P+l/0Uu/X/cj6/X/+Xnv+wGeqBVKPx2MCWuXHhysdVWbZt1miZZ17UdzsBFINM0',
        'p8iwAZCtPGqtOyFsXVsWp1b2d1urA0yn+MQLQ+Lo2SCgfmZWPSEdbw3LRlthXReKc4sD18/GeEJ5Szsv5z0V+BeeFVuNllL2jBR39w2HAzxzje9YwD9haYY+',
        'HCmwbJxxGGrdcm8kM3bfTn4ie11Xuc3zXmgfb5EWu2aGC2LERCxOmoXamTT24/OMAeF5IXK1iKdl/du8EJybOG+H41kjW4wO0fU4+jKnwsm13xt9uAKabyiN',
        'F2RgTCD02avmcdhou9Lm0YxDac9MAfg0Xb1REsNHYp/O+412fTz5WNn6v5nHIPNYafd0DoOuXxicaIl3w0jGgQa5v37x5PEwfNh47hc4/lBqNoZn88F0s64r',
        'xc+/H3AQbey15zML5xAXDorjJrfbzYV26M29uaHPer09c43Ipf8v/X/p/+9M/7sQF1cojnOoYN3Wlg6Nt81AlNpp26ZykxnqtnggKrJv7VisnUN771R135ZS',
        'ZFl0UnBSOyo/O4zl0/L4uo2JeDtuWVZXlxoG+eULpUjbDn8WMoUd33uDYkQM+enrz58/txPglgZK9jLn25k99ETy+vomL/eXAwbvGTgKY/QTntf8ZLevLxK6',
        '38JCOasXTAjwwtsrOd1CtXRLKvBWud1u8ng85H5nYccGVM9AwCszIEb/RjhSZjnDj4VGrf6AYhovhzE99CJ63CKePU4K27buW7AikV7PDJgNB9PmqR0ijPH2',
        'oI8s/ti8uMCPQ1PMg22ybZWpTHRY1YM3jDzdMb+ejUmrIZTp+d7aiAsNlPEeqKkZJzgki35r2RLqGuOYzW28AhxtGj61/baUjf1lQN7gbi22duMtshnMc2le',
        '1NEFSAyjPp7lTY95+hVTMmwTuhPxtHc01j4kyuLiwS/bpjHtmjJ0czcEAxcRfcbp97iPl/6PdV/6/zm49P/vrf/Lz3/58VMVSsETciPH1TcElh7c2oUGHQp4',
        'e3vI/X7rGZLHKh/jhiy2EqxDZRfk6lWTCoHD2Qu0gWio+Al6LwEaTjygteptiYqHtK23mC+zJ3of15W35b2ErDBPF2diSrZvo7b4KxFcLQ4i0tvX4s2TZ+Cv',
        'ELbG4LF4Bvr5sn74fseDc/v3G9MCf89tRENCJKa3Yg8FnqFfy6LK6GXPQQsG9dufx0ZwL/v6tvwc67N4uryvb6cRSmfNcatRWbvfgUcRN5cpiO5b+n3kCTle',
        'ZPhDRfD0qIL1cd09WCpB8Awf0GvdohRyfmx979xAhGdIgYf6zZt4JFuMfuydKfujMJFsK9m+h0wYb/X68cc35gUyg5XHMFtQxD4K1SVtcce3Rx6NhfG69l0P',
        'bE3umS9v3nI+1Ki4ZzzD/fZjwbHs6v0a3Z47hqytPtuNN9Bz6DPpxLSOPiRr3cMc/GKSPbt8iJTtzpaFpXUCFdijS/8fw6X/c7j0/++v/zXE5Qm6rbXK4/HY',
        'A+xF3t50pYQ0WDrhcxO6z8Qa8ZYsBpoHKRI4VuUAb7gUd6WzH5BEKrkBeT/zmsdgE/YG8YoZJ4az1fy43kmmyVZ0z+DAv0XOGdnKZQdBYnzqUX21W4njOX/L',
        'K9nMsGiMR5kJ0taSVT5inM8Udd8eH67SD5HNQ5/7scGWMra3vBBi4wTtBtpkzSc81so/EBAssPq5NePs8+dX+fjxAxlglWg556dYb27g7TjAUEi8ccknvu7w',
        'beTH3oAGf2jaRLuABQVHRl94UiuFu6xtnoEXX47E4xbrxHcilj5OZU1vEPSGacS1D2fABSSjeTBlYZaNxzlL4ehlWTRQdIdhI/wymRj7xohFo/LM6spwwxyI',
        'iEz7za8io3MGDNNUmkGmMtfwGqWVtAWb9hkZjoD78/HB1kf2iEfv+TML0whcrmVhKX6h6eWRzQsvCESkO8jWekAP3RmUjFwHcOl/D5f+F7n0/++v/1sMenYZ',
        'AQYEXoe3tze533WLZFkWqdUUF3KL4qTtMwzq2okr9VJav+PqB97CnkBtkHrlF5XxCHIla++O+2FCp7QtcXgabCKsPykGzWDZQh/ZgBjh54kEE+6/0Wf9ShfE',
        'wTcs+ljbZ5TOCJxA3usBg+V9Cd+7U9w2R57Y47cxZkMNHGNQM3o5n7RfrfMFBx6QSknH1uLe+O9nFLc+916CfGtewxMej6WdRs/qjtvG75q3mhvmZdq9W4k3',
        'ff8j/R7tj+meDytmwHMYjWF7hivAkUYLYyhVggH3nEL2N2ceyQMWslEp5QsJbMfDaNT8xFzG+Dfz/sT4eHt25rniRQ/zdgxFKeH5yEAHxPHJ68+99da/VltT',
        'jlVw1Tb0iz/8yNlh+t6OUi3yBWCox2cgAh7+d7ZL04OOX8br49t2ie/3vh9dwIWFjYspPzO4oRNhyBNul/6P5S79j78v/f/t9H+fxWXA4Fwx/708lnY6V5XO',
        'KlMp7aKCzFCA8ioFGRF8W72iO+4kT7y2N8u2boPVdF9vtsoald8xGA+UGGOs6yIa9znRu+eI3Bh6o/pqOOglAsLMVucaw2WKFX3AwZ3+AEvfz/HtXs9AJuiK',
        'Exyx7fEqeGr5sEVELHVTnAPPuPCeGZNZJgARL1Tte5tzPWRZUppMMBWR2sYMh0i0T75elEfcLspAcXbesMwm9d1t+LGSQOq96By1vpSWiiwa1VrnsecnjsdY',
        '+AyrkCNPpO+kT6PFBwS5LZE+m8v7IRpZPPC9IawGXzTkBzXXlWjQGxXRc57hhOc+HzTK1/AvP8/qjBAPzuK7eBgzEFTiRbI+RYOJ+z4CtiAnl30ECyfMT74o',
        'sYUBX3jU6P0rD8meGergiegNF/H03ssrM76tfG4c6ZAY32o6Qenj0Juyo28nGZ83ufT/sPyOwXig5NL/Bpf+/1r9X/7xL3/+xPGqbQW/dw4xZy3uMjEQlnUx',
        'i79MLj4nPSTSBj1M1BPCIQJiIFHP6+ur3O83PXi2SWBSPwkZ8YwUDZflyT4qp9uCdiXx+BBG3nGf4aAXkNm4xHfpOEt1q+fOQzGoewSj/KT8fqv+YoAMskNq',
        'Hgc+tY9//c1juInMHwDyAhVxmxaHK0Sj5g0KvaC6+HfsAx9AsTK50WPf2OU5teGYCTH2/LXxAV3sAgvxhTDWOxylOE/Ee2JxoxHAYS8xpOU5+mGD0GLJ4bXT',
        'PvQ5tHl6zOAyoTtPs8iusI/zbj9vlI2/r+5AWC5reFyMbo3Ooizy3/L3sTwbzUfywEM0uLk+5A034rIUbCPj3P7eVqUEyGXupxn3I9npFwQipekeGBwW5mBG',
        'eA42t0pTmyuvYwNv2qCKA4gyakRnzy4WOX96Ztg/y1N8wVK7tIiM9HZgtJqO11eX/r/0/6X/vyf930JcGkPjBQ9kpjsYz52ZFSk97Yyk+JlH4IgRfWe0IcUp',
        'ZnjYy7btLt1S+vzrq3z84UN3etq+C4h3HbMBi4PaH1w46ZAog8bYtPcAjwO2e3F5y5cI71H945WrwbKsdImDK7n/myvcMV6ZMDwylvw4G+Ozh0JXpFOZZG3j',
        'ZHOH8UNZnxLNp+6CQcDPtGy2dZgLfbwfxZYb3pmw1/pYALHya0OCZgckzbcWisgeAqICAV6szHvOkHkQRjRzRofq6fJGtz4PB+coxIc95syHdv23elikiAtd',
        'gGFxlEPbnm2kuL7GaI8AZaEKKr/IqLQQHe0vj3dUXKzofK701mIip7x8ZeWv9Y3njnkgM/aVx3CYqlaR+32W256PvaPZBPxFTNX9NkNCeUH5933GDoy4dkhU',
        'SgiVific0fEz4UTP1uWBF6HggTifmIPRgVT0kUVzM9R5iLC4uPQ/4XHpf9+2wqX//3j9X3765c+feMAbA+9tPSf0vXWAywmQd7NXNjTItIXTOhVWvniVHRoR',
        'oVPfVeTXz7/KDx9/aNtmWEit29q24YSEUSszkjyGaTd4Z4caRGRPw3MTVoDZajgDGB61rrKulU5M58Kq4ZUKkQ57hxO3Of4+/ya+X5bHLqCZLp41AP24Z/MP',
        'Q+319U1KEXl5+SDZwQ7QJjOHeQ4p/qwddKmCq6QZ754JPd5at7WDjAP83tp/nyBt/c/4P5TjFbjhZzHjo0OeZ8Z5V756r2CmWMdGcP4MXlt+5o1rpiPMcTTs',
        'IUzn7or4fJcA9SpNmIGMsBGMV1RAI4U0UtT8rldYI7mm73J5YZc/ZdvPz+JrbeB9KTOFb8AIrbty5p0Lv3B8fX3T31Xk/nLfjfPavGvGixyDbYuMiEsp2F42',
        'muCF1HOx9phbNXjneZJl0bZG9Ps18Kxc7zBNQrw4aw2M6wzHSCsidu5CJOdtGO4c82r1XfofeFz6/7lv4vtL//+G+v8f//LnT9qN/euIB40xMwjHfY0AB8pa',
        'PcJbpfabY3w4xVIEXdX0J+9NmBc/MVuVdUMOy6hsRVjBHHkwCQP9/z632F7rt5eN2X0qoh5vw8Uzh2+/tMwPIDgjXCPoTFh/LbAAPqvTG2o8nsff9wfhMsbO',
        'aYI9IhgPL3gl0CwyWojA+2gnwb0XD39Ta3v95mGx+vtDgqXYav2roLe9zwO5AAAgAElEQVRNHVqj7A0iPb91dXJdcdidAkQRM/Yr9Zm3yTHex7c5eiMLxjYO',
        'BPYLDTbsbH58WrKQdrFkF/P4QQBN2O2gSTGtjcrHGOoYxyjhPf/uwxZswdGHm/hxqmlbvczwz0fv82/jtjeU7rJnUmE5qu9qrfL589seWjAJ835Gl94g5AOe',
        'tiAwY94y+pg3mbPx+LnA9rTF5tt48i4M6gcOzxjUZ2VG8jwLcTmqC2Ncirh48ve0HZ/1bXAowKX/L/2fw6X/v63+94dED5T21pjjOBVS+z3QT7w1YMIUKbRO',
        'kB0INWZsXlVjG51vUmSkMHipASPHgg7447KCnLmzbSG/As2YtAc+SX2WrsnHlx3BucIxofbMQRHuY2Q6/RcrTDO02OBa1022bZP7/SaPt0XuL3fhrXheQWcn',
        'pHt8+m18ERGe7q2KK5ONbR9DK8Px4Dl8zpMRK2hoqkGyho+dcePrhkLvjHl+R/Xws63axSltiztR1hwXG2PFHZrv7DenKhvVw5e7rOsqtR1O84a+N9At7hi/',
        'OYRi21ZStEKDhs7rQMITyIrSlysCg5Bp3w1ewxHtR+P7wNppsA1pzNqcqO/AK5MHWYYD4OJ/c0hUpjgzfheBkvIxnXm5WK/+RhYM3IQIA92DzQFvQ2eMwFl9',
        'RhlecsjmJ5+z2L9Y77P8goVviw0PO0GRBhxm3dh6+ZYawZf+98Nx6f+G86X/v43+dwZ6W91WLk8Cgbxnfb32fJSBAgMQhcS29SmeeFUCoRQZqa2U9/qwGl/X',
        'TS88EZEylbCCNQHRr3y5Xmnl4jN+1692vSBQz1N/iUEGqcE1YGQAFBfGVf9GubE3wvUjpC+CMjDmyXFV4PACkZidwhiVmZIPd/C8VHrmvWN26MPnTGbm1f7f',
        'iOFsvLgukLiN0xaESdBOKeRxZWzkeQ8N45Iw75P2WWbcOECfhFJakR3WGeqod/JKjQ+saY88bzdv1kbtsTFRfFaYMyGltyBWmWaN/4ZxwnHC3hOuqSZFpMW7',
        'QpEcZYSxw4Jadl233TP8TN5mzpxwVo4PtfZ85GNhFXeUYZ6d56ndiBmVQq7c4XEeHWLjstGArcIpHBW0HxpHjtj8fhXovdnRGCqD59zv6vgaRq2FOcFr7r3i',
        'hrc4fvSZYnjhdJ5K8Qy8fBnTwpcq6nkuwp5k6EdeVJRdn0r1Otrj6Y1zfQjkpJM5l/7neqWVi8/43aX/L/3/e+r/ZqCXsq/29uwHNgLqXcu2iEcrvqbks5dR',
        'rhZT9gy4Jhi49Vv1Nnh10xiz2w35j40wcGAMHjBWgjZANak3kWAJ8Oo9m0y96ARXbGv9715VpaC4ZQewnvXQcF28DbQseo1tXFH20xmNDFPQuBglEi5vz40U',
        'HDOdpenyt5/1wswrZG+EcOyrtN/4zuc4zeYxM2Ty8WQGZeHWG2QiyKHK9eDqc9fGk8Z7Bzw+k275jhTsNJM3nO2vHWImBzbIu2ZpzofpG6U34JFGC141rcMf',
        'FFVaZXpXZJdlkalM7rZFvp0xo/+xwZ17tnN5AaOWJ8kM3G2tMtH10VComTcTBqThpbSrRgcIIcoqxlEnTXkP5UaeL1O+PuUf+uMVl3komRdIBtca3mXteVyi',
        'keRlqbiFIhQ3L9a0TNYO/3sOz+U2B+AgsdHZmTzP6m+XEkGZJ4aw1o3Dw3vWH1FDlsPHihRZt1xHc5k4JNGwuPQ/13vp/0v/x2d/vP7v8qC3jqPiNE0RgFca',
        '+F0s9zIViwqeD3Ctq8aJ8Ul7XTnjhK1tgbGAKo25J78KrxJyq47zbiKjxfG2EfqYTZwcPFdhtW16C9UZRILK3n89Y2eQKf283eh5YyMjjuEIX51PeDJFLLWR',
        'CdJ1XSidoKevkUHhDSt/kMauk9d3+TiiL3yJgbWr30cBM2bU6P2wMToSAlb3U3MddIjjX/ec5ifRO/E9Z3wYGQ8RdA5n5+Xjb47HCfGAejAw88BDHthBThvL',
        't7eHTFOR281fV824WPtxgOI8v+fCH/7e1+2N1t2wTFKSZZ6zHXuJcdSsfKKHTIU7j/PRtnQS610sXzgb1B43NsxH8k+E52b/0tXZSoV+Y5GCmynt9k+dSw13',
        'qTQuGfjY6BEf8bjn4S089taeGswwis6NOBFKNyg9b2gtvm2TW5a5ZqZFwYifYr+ijLJ+Ko1nXulL/2dw6X+P46X//wj9rwY6yRhUxivYgOMQGpNIz5BgKvZq',
        'MUNldbPnzG6Q08mdp9l5GTB4b29v8uHDB+e18EQlbRBEIAAlfReFfOYQOAMltnV4UATgPWreg+VX/n1f/CCOJ+qIoHLcKwm7nHGt/VGcmuFjB7u4PsX58Xjs',
        'ZaZdEZcO32XRkAZd3bME3oV9KbJVjTFbNx9aAC8Drhd/Trn52D+tq8980I9/V2vr/0Z4cd9YOGGMbEs4x5Wn2lJCDcrG7wJ6pYjPbQ5jKRoUu9EeycEO74n4',
        'w5rPGDAQ9qvUTWS+IQ2kN5KhsLW/JZkn6pAo/8zTTLHYOZjh//zth749Fqwmi9gYz4zwth6qtq0cv58oxzNuSPUKQ8ccuPu0lCK9dx/4enpV3DNFp2U1zztf',
        '8gGDPr9hz+rsZSfTfKRX5nmNHy77eQNJeL43Es5kIAPHdEcvN8vd/Qm1C4Pd5PSyLHK/v/j6yehH6IgZ1sc4busq0zw7vOCFxYU1XJd5iD2oiu0X1S704dL/',
        '6btL/1/6/1vr/5YHPfPCbdsqU5lbu1vdf6c98zg6BhXxdDOioV2AjQ4UcTYJZjA+OLMsi7y83N23HkH7Gye/RfimKc+MIwXyLHgGZVx8nWgHW+D71wNieE/7',
        'fXaJeMo5fOF+2RhFKHtdyG4xxoEPaPj41anFgy3L0g7UqCHACkcvnvnbr7/Kum5NALdt391AgSckxv+inMWxRmR7gvRhB8bo+TixALL+QcmB2UQ8DbvaGqMG',
        '5j3im/038nbDC5HRbLRRUyF1IPM5k8QovjxmS6l1c54vGCswnLEY4OfwoEf8RYQ8LorY+PIhGpwOsvSJfdhCX8dzRi7jSz1w33lDXb3k8Vs9NFUpc0r0Gkl7',
        'hkNw6wrvTvT++AtPTNn49sBzflz1OwtxiUoLbYwgKi3v4YKB7ZU9FO6+6GiLgELhO6ABNqQL1Yu6zuSl6QTTJdEoB+Q7K3YzbN4W5zU/kpNZtiHIDo1l9ulD',
        'j+ptC2rCiXVrC5dpH/hvL/3v24n4PAuX/r/0/9fofwtx2XHkAymvr5/l5eXFba1pUYs/denVaKUOj5AhUNNn27Y9cYlFrvzw7O3t0baQOH9sY7KWr1RczsvR',
        'wPzWgJiuCJlXTYE9giCe4sqoobSrygNGiwcZ7CT0sbLgeK9cYUFBxriySdZ1EcSrcp9im9PEnte5XTSDb8FwtYrcbpO8vT7k/nJzeGj9auRw7G08dHZ8WM9i',
        'cVXZWrquzAvD8YxHgNhIpPmyetFmbQcAOd6SDSjg2HszvLfPn3bPkBE/lVaNM7YziKnSuuK97hORPfcweboaKtUMj5hOcdvHK58/Ee+dV2S8B1U72ntVeRC4',
        'zszIj/SeWRQjA3UwyFTe+tbHSvYD2S+EOAf6PM/NmPIHPPt4z0zWZLTCPOXb9kYxxjbfju8zLbCMULAQEp/HOBrWZhiz1/U840ofHtQvdPxZAx/qEufYaOt4',
        'cdjDmbwVMY87FrbMJ0wWJhtNn3YHugcGMJ7joPTIy33p/98OLv2vfbr0//v1vw9xqcZo/pCRDm5kRihgEaET5mvzRkHZTmVqY/rsyWKGETGhLij5bV1lvs1d',
        '/Y/HIvO8Z2rYjRxs5yuhl50w3oWWGPEeS0NP7F8Ldb+p7TZY3XMbPX6Qj6Pb3aEo9Qa0EtJImbJ6fX3Ix48vwoYA2uRLTXiOdBv4JnzID2nuPIEqs3z+/CrT',
        'VFpf0b9SeCVaiEltjPhffAs6rFV7sm645juP1fWrbbsVTi9h2agf/mBSKbOs6yLrsso07yf4g3Ft/a97jOK8t9kfHIkHCi30xYSvCcrRpTwBqAh417/2Snh0',
        'KLRtEVfip7a9bAZRVKyZ4WYGK4w2zUSCbc9R3K153icx+cJKpbRyIjvtiB6MhWC2g5hsSEozfs17/OzAlr2tlTI4sIEXD2CZQXxmyIFfQDOI0bZ2zbBnQ+UQ',
        'a2eoGC9azKSIGbikMJoy8UgfLQgiPjmOGI/+sO4ImuwIhmKGDxswGgZlL8fx6LyoivI8xw/tjlI4nmUd6ts3I7fdirob8PGsiGFGOpfI02VtufT/pf/l0v9+',
        'PL+9/i8//+XHT16Y9V4lXXnZQaIW+0UTbwexaktdJsLB+VtjEGbw+H50sYptEWzNQ8DMvm21MWjzKmwS4qesn7o629Nv8aGWKhQ3qhPUX2/rB/kMeKsjh/cw',
        'braa5ed4pwx1vhLen+zE/vnza1ttzvPUTnPz1hvGT0Tnt+8X2rObHZHSDsTtMN/spjd/EYX2YZ702l4WEqYgoxLn/kE4gKa9gZHnUsWKfQ6Gjx5cgcAyD6hf',
        'YYM+kbYQBqs3SAsxtM/dO1JevfDtjVUXR3tuz3jgoctIO3nO8ersgfNj6vtkhm8MF1DDZ11Be0XY68JGfB/jrrIAXsfoicFcaxlPfyq4Z5nmucU44rn1Icaw',
        'V4ne6mgYW197xczeHIUshp6NYP5bYZ5vIlL3LBGow+dIz6GXhXHx1Eruis/frGeAmPg+TZl9n/3mfzn8xtr0hrVB5HMJv0v41+PgQ1h0POFpG+H6LIx3Ywj7',
        'gZHO7fXt9mE1MJZbfHsxfdi+2swLP2onGvaX/pdL/1/6/7vS/+Xnv/74KfO4cTnLwgBPCYRaaYICClDEsixEj4bbqhNz7beDOqX0sWuES+ZdbMNa+y00rwgi',
        'U8U2bFJRlz9whdVkdZcPrKvG5dlpdxYGWXxlD5ly+1qAgPZgTHu78faP4aF/s0HA49krQKs3KnAwjwgfpopbbDqGW6MDi7+DAhUyxGob46kUWVa7AMXGmI23',
        'LfQH/UC8Ia++jw9YZoxsjNaXwaU6nq58OU/XW1ont99abnSWt31qbAu95295mDJjvHivr+HWe4Ut24Lyh8oIzfKSG0Ne0PI2o/Z52w2F2uSLF7TR0ON0hTEs',
        'Rtq7x2ORjx8/dsI45lzHwOQewN7Y9XInDmgJ30QF2tfNZTWzCSbsLP5z/C626eUX2lbcMiPcDNuNvhf3N+ocZcnwsjkfAzbmPW4eTz83US5Y3zx/9QuPI3jG',
        'EM8ARvRTu1sB9DbK4xAJNtrj81Ffj3C69P+l/y/9T735Rvq//PzXHz9F4RkvK2GhzZd8aOVFwLCvr29ym2e5v9wlW61F4s2VlEh/6AblecKrqyd6XFiJGO6r',
        'YEWsZfykg5E5dMDqyhXsOfQM60+Oxz72cGQUHCmXnqgUHo8HXe5y3LbVo2VYyY3KGl4q7JaF02hFJa5bYiISDn4YU2u9yAdc9rJ2jW5cmerfXiig3h5fv41l',
        'ih1GSb+iP5p7Nkwsh6u9MwMzKhJvKElFjG3txk1vZ7TVPxuCphAGRlGY6mj4x7LwBMSYU31NHnS0Tb+tDyIaI0hbmzueWQpGeOlsfo3+eV4zD6EfT5ZTWTwi',
        'ZIqfj76uZOBSL66Pu86NyjNDPOLCsm8S7wU2WocsU8gOcp6175XG6L3+G40o9k71YSmc2cOALzZBvTwvv6XhbHW5zCEdTuLwH8lPKxcNlnM5+niMr37n7/v+',
        'wLAxg5aNcv4Nfopz6Yz5ZFHdenbp/6SuS/9f+v+P1/+dgW5CGCsM+w+HOqB4kLIGjWhs1Et/QUHxq/T2PhRbHoumlmIh2gmZQKxUBp2PQsfNy250+AE1Jo8E',
        '9qWQK7tYpz897sudA2+TR9o7Ui6Px0Nu803KJPL6+iYvLy+JABDJFLwZAyOc+Fc8yJcJghqYwPoGRgWT2srfMy/a1UMiItHjaXgpY/gtNmbKiZjSELJtwmMA',
        'LbHXpKUuLIVW1fFLrxyUz0jpkjIF/XaJOogPjaatj2y/4ZCUG4aj7lXtA2CaJt0WFpGNMrWgSBavnnn3zMiOoSIbea+wRejj0FlpOETFe9Q9zVTxOax9+ACH',
        'zbCSOIaIQwxJGCm/IJTEaM+DeZt8bH9xZaztI2CC4WdR8eSyj5WPiAh7to7rzsKBIq7ei+UXjpiLfhwzQ7bvUxFbEGj7MGZRBIthPdDHXrzYL/SHGMr1qQ9H',
        'Ydn29qby9gwgK3yfpMV5t7hwklU8dgci2kG8MfjS/4T0pf8DLpf+P4LfRf//9Mvff+KVEQApaaYyNaWIw1hFNBcl0i5BEWNFg61PjhUDwRYp7tZCHQBlLFxY',
        'EHExl795E7NVJTx42G6CYYFO+1RHZ4BJ61c3R16mnLFR12ird6TEfy/Q9lSobi3vqN9+4hvF8J0ZOXywQcEMI/z2KbBYmXlF/MzfLLGxum29oTmJN5gxcDxy',
        'HI9eSPQVnHsPZmlxaXQ624REb2CXYh5qPOfY0rblG22gaJeKPWM8beyMZ2BcC9F2l3JtYKtVYQ+QL4e6cKlKPKgXEWd62dZVTZ5qqQW9IZgpHauP6+LDoDDu',
        'dZzt0GnrapNPtcWiIp1dNCzOweIKPY4i515tkW3ddprwB58Ml+i95LZz2vaGrn+W81fSK3ocFbDyODLIsNGqoHPAMfacQi0Qrn2VYOGNkWMZrO1h69wWXaCR',
        '/uN4kdDZzgr66m9Y7cs+I9+sPv7enyfQvycnUzldqWtTeFeK+cLatjLaz0v/R7j0/6X/s+//WP1ffvrlHz7VWvfVyY0Gh0+m7idn1yrTHntlMaa1WxFjpW0x',
        'aV7hYfDZA8Bbd20FvQMrSWRUgPGBsVpXXb1znYjDgrLHAPcrIlu52PZDde3bYRZaHe1CwPR9jDmz5z6/6f/X3rsky5LkWGJQM/f7oriBGnBQlCalOrOymy3V',
        'nRmZUWugcC3cEEfcD7cQbH6Ec04y3nU3Uw5gR3EAhZqZ3/eJNzCIRLzrZmqq0A8+CgWgEUCgI2WAlRqvdH0LsHGtre+2W8cC79tHEEjEOfbPu2F4U3Ct5kcY',
        'FYPeYipuh41y8dtYNhOO3k2g0L/AvwdmDFHpgBDhjAuct7WUWZC3XKRsl9qw36O1M8+ly2NuY+L/NVz6enYhlymdcm6vIzJCAtG7EsQjeM7jzOMc5xIX9fRK',
        'PRq0q657H/Ol3UaJI05TIAqthdxPU0ScgOnXDIMdvfcBoDw37BfPA8dtT27szPoCpTFXtFmhjwru7jXpQSDmygcr32wNF/pdBu+9m8SxIIwLrx/H8YbB9wGK',
        'XcbTuV+gSSib7HP9NfhspNPz4N2G8D0Hm7Zn2MSLp9Neye953yX/L/nvWrvkP/0L/Hv4HvK//PzLn361IJ+6CQUjnhitOk0WJS3V8jzyEba3QlgwFw80dmfu',
        'OCwIJbThbmNDHaser/PNTEaEFExAO/JW5RbdHZlXDe3gWa3rFghS3OLtEB9ArXp8pzk8Ud6VSJ4Na0va+zJi3RMcfByG+R+lJbL6zErmCYtdB/gYSut7Pp+y',
        'LFU+feIjtz4IDjc0ZsTofVhtTrP+ZimWeDmOhen5uRcp3RhkdbNAqdVuDYxKU8+07Lf5jPJVxd69JUWb+dHeUszKZ++3MtM0tSwjSkuqECO1lkhtApSzeYx8',
        'y7Uec18xFwuk3mKrSm1BR+tat+wB/bgdKU3xIpS9AfFuMTboZmW1wQGjXhes5UJpwKB8meKQtxvHRqh9b2FCX8ffnV9r8YTGf5+nLfP+x+6N5MKwD9zyc9UH',
        'EvdQBn9bHSJ29NzjbHPhj8o9/7J/e7/6vM7zyvpo48QKLeiIN8gi4lzCDHerD77ml/y/5P8l/+1da+cHkP/l51/+5VfbyUg7ZhJBiqTijlh5p2m7YD5a1k7H',
        'yw20W7rY9XYqs4zFyxK4v5yjOeZdBRMBqE+cRY9Pk7c8ZsdiTBv5hMeBtF3Oui6Ngdzvt5AiyH+rwUE4Ltqb1GNi4yvE3ZdBgJ2tLz96YyWBy/W75z74x4Re',
        'tBg2xllK8xc7JgRm7uLG0HDrrZNoK1pbxO2Yfb9sZ28KVh+E53pPuJ2QuMJ98PVwJpFSxNGYtVW7NRaVzTgfELw8bRlTRntd+aiYt8c+aNQpl5uFDQJORI+y',
        '8ff9fqf0V1kQlI2V/psHAeo18CtZyMuWg7cvy7nOx+PQ+xFHJT0XaNG/PVPser9KfWeuC+iLKWCYCMMrz6jSB0hBSefr7PFN/33sj1DZnj+wrz5+Ay/9G2MA',
        '3AH9Bsa/i+O/pwx4YWx/R4Un2xzwMy4feRsrnIxTv77gAoIUoeFtV/d5yANXoyXdK+17YGszHvtf8v+S//z9Jf9/X/lffv7lT78aoa+uw9YZq413377c2o4I',
        '13WRefJpoSDMy1SabyAfw8djrb5TIY1SXH9V5LkoEeBaZBGfdgnENM+86Oz1Xh5qHjiPlzjGwlkrwIiUca5JPtXz4FM71fS47KxV5hywRcL63VsDRUxQikxF',
        'NsIz4ewVSH3ulRTjkmyhsKvO1bcruhB4ZmgWtyzYI46NV2wnsctotD9MbPosywfNePP77W1QhLK/PVi77Jbg0woK9R1jCYbvg3KYoe0Jbj5mxlptl6A06bUJ',
        'OLFsLqygs6+8f77I82l5dVnZVPy8jyynyOJjfRFvEeS0buyygrnQ+jgryB5E5alXpuAji2DV8S2WgJjNpFcae+WvNis6u+Pw0as/ht1qoo2ZHR9Pm5DsA7D4',
        'u8EbMSUuCkYuEyswpsy0PgavuHNaTKZ338e8JnsXXW7434h/WtO22YsbEhvf0XgiBaDyq2wz8LXB8yIcoTN+o9MB/v6S/xirS/4bXPKfy/5e8n8LErXJxm6V',
        'BSHvSkFM65YhYWafNPoO9cCvhoUswzTPbSfa/g6wrov3fyvbDq3MjqiRwoqPYM3/1HKugiGtyyJlKi3tFQcfsGByQ1iOB57/hVVMROg4X5JjtkzY8ULgNvoF',
        '8hE4ZhZMCDGrBYJCRPoAMVZM+p0lH5tGAXqGyeSWDr/rBY5+Z/vqWGXjOx5zs9AccUktw/lPeRx4vLBeW+CT+HnLFSfrb289OB5j9tfWRrwCPmjKDQ3oWOlW',
        'FXM/bzmj479Rh21QhJg6+MAWzDYh9eQU6rH6ON1fPgYmVKICijng20bHfRH3bdw4sGJe69ou/EE7a1vHqINdJwbpe4ZW4pyfxf7zBs2PnUik7ygYLZVZofKr',
        'qyPSKvDL3WHwF254XcU2HkLfsj8u86M4JnHMRmCbYN78jZX8WNceD/soD8rAcLGxx02E4ugA1uwYeG7gTwMu+X/Jfyohl/z/feV/+fmXf/kVxxhYuFEBqHVt',
        'GQ5grdKdnO3OY67bzrdMsmClTRlYVjvK2gYU/jgcINWCeIoFh+2tUcviYDu/NohVHKNqu+qqR2nss9q6MSDK+C7ihKjn5r9X1bI4tmwY/ibMNVNCfxsextGs',
        'nxyMgAl4ZXdtDMMrevYuHkF7Sxf7HWInuy7r5jfmhSALY69MMHMY4clKUFYwao/4G+9GxDRSdrht84P2O+dCO/IR3rP7Bv0tRRVapbNcgRqBW9vSj0ezQEW6',
        '3ONdWNuIOue/B0OHALXseDlaVJEGURVsRQJjB9rQMktzW7H+6TvO1HJGYEUhfRagGCjfM1roFd59HNAXLTe12w/tW3w/Uvj5d/TRziZly5LjLEQGfoPX+31r',
        'vZG+47eTPB7vcr+/NaFt4+Kt+kf+q/wN2kZ9Z761Otpf4ucj8s7qymFdwNUojsW6LDLNsADy99zePl7mRhLxOiLIHJhevDtW7i5kPIZTKF7y/5L/3JdL/v8I',
        '8l9dXNrxrTGzfkJ5wIUqnhxhNyIQ/wyQXuWb8CT4k67bFbCtaO2PrYabnGN57cClryKrFp5FX63+GCSmsPPlStnsDBh8McukdzPofWBRD4Le/PPzfTwD/cLq',
        'MzMoZANd27qwMv47ZTJe2KLvNqYZAZ0lqD0iHP3O6s7bs6ubbV78mEWGwMTLTN0Iuctgws1WVXz7K7CzhW/P3RXWJ6GzTiRDBSWc/cytyNY2laltTVjdcL/w',
        'KeAy317LkAJlFuOuF4+IsLWwlGmzjCkjjHxsP+gzA1vbzMt6i/cYvPKSZUAowvQVlX4vFG08xspcpuxV99wr5r7+kYUw9EpsvrTeEtYCNiEeJ37nce35hn6v',
        'N6euIuE0oO/7ntDlPsbFDbnX42g+uKOAR37nwdOSKSXLssjtdu/K9xADj/MyGHtrC3/7EyWOf+B/L/lvuGkbl/z3cMl/hu8p/8vPv/zpV32g13Dr3+wHCqIw',
        'iwF2agy326yIl9Lyoy7bcRVbQUa7UPiXxqOyx+O9HVvxjjO/yha9kyFh8m46Po+7YR6sftHGRaBguXfxnS665+MpUkTu95vERWHjW7v6RoLS6s5THo1hj2sd',
        'czS/Q84JVIF9Eye3zHvGX+X5XDerBZiZiI1PrP8IR28JHO3Gc6tC1tZYEVJl0yw17m233iMziGm5rC3gFZnxmBH4dvVbSzPouheBujttAW44do5l2A89g+ab',
        'XnzQI8YCgWTxghTGW8Rb4NiiiTak2PFxFGjRpYSVaFbQX7Witz4GZYgVNZuvaB0bCw9TwrTM+/tDpmmiGyd5TXq+O8+zy5SzZ0W3tgirtkbZh3PEq3mce5pg',
        'X2bftq6Hx+PZfHJ5UxIV7v5IHMJs5A7jadTzEB6XiHdxc2eK+kchCTBWE6i+3d0Yjvhaxluzcmph5hSmkS7g0qH+zJNbo5f8v+T/Jf9j/b+//C9//bc//Yqj',
        'KuUlylCOjtQRMCVCx8YhgMN8PVdCxhijolRaWRbKXT5lHr/tfZqLOYMwvu14DI8DcZ6V2SD2uJNtu+nWfy33fD7l7a23nJQiMs83WZbnqKWkQ3vPRh14TfgY',
        'kWRtMiGCWNnCxM84eET/XtbMutMHZ2RzkVmLYvlzc5opTSLxCGoEYPSZn3iG6w45CRPnNM2aMX996fwAACAASURBVGGtXYox1GmWvfExYDs2i4pBLE94ubEi',
        'Ona5jqPiHn5DieffbLWDhaUUpUXdGPhc5v389YILz+zYuFfsMqXgo7TeZ/MQYSUG5byiN2pAaQPuA9PMCn3uA2++6YzXGSEd30VFt/cxt7HjQN21G0P0Obea',
        '8ZyBR3Ke+Ejv6HOWWtLq7wNH+2CwnO6yOfH+1V7RNx9b3lz21sS4LjvU3Rp9/RQnQsTDrOHA8/1dZQ27CnEOcU09ems0eMn//u8juOS/yCX/v638b0Gi3uFf',
        'JPNf8xeB2BsmUB4M+I7ViiT2N/eNE+TA0Q3AslkFkD4JhGBHaEfR3w6qvP7NWQjrt1MKVpH3x0Pe3u7pRPUTGImhbPXmfqJnIVN2Rh0pRdzNXIafzyjSH+8y',
        'kebHr/z9WMG077A2o4+rL29M0ef35b73f3t8pOGc+7V1rXbfRkVnBLz7ZlBrSrQuGhPhm8dG9TKsa21CaXi8LCJrXdtNfWOkHSrbP+bXie7Dwr48zS2lXWpS',
        'RHCrJzN223TY/Jgy4zO/sDDILm6JwW08NqwccAaVPabg1wvoz9J1ZRsLHtwY0Ghls6NjXlP6zK5412fm9hEFZQn/Ynx9Vhn2C48KcVSE9Znx3tG4WP9s8+HL',
        'RtmS5zkfK7mab3vZjoWVdiwvcz7XyhPUrYStlzxGhl+fw1vEr424ucF8+gwSqI8DBKOrxwhGKUWBi623fiO3ruzfbn0AHjpmso2btDKX/P9CuOR/Uscl/79U',
        '/jcFHdHy3dGP5IEjIGAcTSFy3PwtV4m7IRzBRWsUrouetiO2btMW11Pl3bn0Sof4b46sAYzj2VRLXijtlm4IPR7P7YjrIzBuyPcv3xVmdWSWsFguWuiQNeNc',
        'UF7U5rKoala2Mt9YvwCQQSBa/RT8sewesJXjHPi+jgnYE6yuz708qj2umaKiTEcFYQseGiw+ttrEG/xgVXdtUtfit9Gy5brZdcT/XGuV5/Mht9vdZWfwucLH',
        '64d9W00hNiUefKMJ721jkE9pr6y0ruyu5Xj5jFdc4ljmbjM5DSq/WVu/epo1ZdeU+VE5wxV1s/Jo5TIa9K4v2drjdqMF2/pjGV1sDReqQ6T3A+d+7i2qXhHw',
        '1vg94dtDTjo8Phu2YYOoYPzQ8icr73k+V8rDz8qvbeCgSIxwjJlZRKRZv7WOeFGX4o6Aavi3cz98ytHIPy/5Dxwv+e/LXfI/68v3k//lr/+mPujsV7pnxdD3',
        'diEFynsHd7MWwU+Mfca6oylRwYrjtdQQxP3eBqcx0eJTlsU12oS4mLKS+/T6XU+mxERGNbLa9EiLPN6fcrvfnFAR0aCDUgaMpglQWB36kdsnkhH4PKdZPbGv',
        'y6IC4H67yTSz5dATnxF1ZoEaTS5wqrLXp3zny4KrT9PVcffwzn9nuKBvPSH6b4AyLMk8//M8y/P5PMHIxwqGKUiKV0zRFYXFy9aVZHqOlBwX+FRsDKT0ZWAt',
        '9wK8F9QRQBN17d1X8J6zPQD/I7/y6KPugY/jeWxNWY5Cz8qx5VSkX2vxtwa0SpFu3fjymaJtddYqFNTWuz+gz1FJHlmRvLW/xwN0no+RlsfYWr5sfM+K9sga',
        'jTYj3W5CeoKysEhU2tkNp6+bcRgpMuAlyIpRw+lFDM4dbdg4o5LHpFlwaa3uW835vW/Pf2fxHXFusUH38s1wvOQ/cL/kf2v5kv+E0/ibbyn/y9/+7T/8yjtN',
        'LmzWNPNRE8kDNOBLilQ6xkR951BHCyYLE5FefpJA9Hn77fNnud1muc23jQiXRsQcLAcLhvlGNYS0v4k75xFR9lAoLZV9o9cbc+CX1RnbGdXrIdtxZuWZY0XB',
        'FMGIjXFztRYRf6Ru8Hw+tz5qGz43sS1c9knLmFm06KHdHB8f4Nb31ZWWXtGKY2JC/GVlN2UumXJpPuSHNW5F2NdNxNaqrTEoBC8gnekpuc7Sv4/oh+fYRNRa',
        'nXBpVskqzjp3ng6sPAvlsfX8dcgtjuM1eKRgZWtcRGRdVl2NLciop1EE9WW0ya4Ot9sszyfcXlBHFJKw+Bax9G29YpIruKgjWwBKU6B3s0Rqvt98LCJek0Q8',
        'ue5sQfrNluGSWwal4dg/J+waLVl8QyVB7Wm3Jw6zrvFc+X5icyoCJT0qJlYvn8r064zbNOshgoexcWNLNwdtN6W44chjcMl/xpnHZR8u+Y82LvnP9TPO5+R/',
        '+cvf/vhrTCkkYkQpEpmCJf+HgOT0QBC+PLi8w8XktktPTgrjvQWzLIv89ncl0E8/faI+1NYPvubbbiQ0weuC4JJ2AUcTNsKzlLiDdrXKeEElSA0Ew0sDGlsg',
        'Jr0sVW43+DFqncjdW4rIutRNSTQLSXXEpu96H1YWvCyAjFj9utHnMcBkrWNi9YTf44Q2MsshjYYwge5Zk9xXxBz6dFERDwj7SbxCbXPZMabtVVR4Y/tfRUnN',
        'lFDgfLDGIGSdAE6UgKN6ltWu7s78TwHRKq/ldqs+zOCi+a5nx7P2IVM6szIAnWPc2oqbAT1rQBlWdn29Pj3XJLfbtCnpPez3OU/zaHgo7bLyGgM+W8aK51Nw',
        'KYvyCt582PjgAhmMsWUhAY/ox8v3Z6+8x9t+M9TmkiICP2lTXrdWmiLL8QrsamV9ihsYWwdeUTdc2AUtXzcci7K3BiGHZ2FXJ1XUn3SrZe8mdMn/S/5f8t+N',
        'hvwo8r/8/MuffuXdfDzm4t2y1/i1Eg0i8JcM8A4YncusAKlvK+GQBZBwtC8W+uPxlPf3d/n06a35+3BGALs+u25WJr6h0NfrB1ohv942J0Ke0Dix2KEbj4xE',
        'Nn6Guupa2+1s/rpfUmCaMOwFXGZZen9/yO12C2PHOEzy/v5Z7vd7G3ssqts8N4Jx9VZfAzBgQoxEcjajQ59KDsrBVpPbtXsCOdqN67sY4BbnZkTQ+e9c4WEc',
        'RvVt/SdLueF2LuI9hcDH09zEL4JmhtBbC10XXLOet2TWH7i0cO70jg8Q/s3PPrSBdkYwz0gDedQzs3juudvsw5hJr8tmzU3SdFk5U+z6Mj6DSSmaN9ws6TwR',
        'vWCvlX0k7VmmhPZ0EQWvWrKQJ5hx4rR8vg8xQDEGR8esKd4f358EZErOmMfWWuX9/V3e3t5ERDb/7ZnKRIWbfWAr4WYuJnuKNNO7Ba7eh9bm8ffWpl1ANVbe',
        'oaTu3RJ6yf9L/l/yH+9+HPnf8qCjYXTU737stqS6ypYrWST63ikyWrHtUv3kgcE04VuixaKHo3RK7+/v8nh/yqef3rbodGXWI99UZlSN2EUZb+YzZ7vHfSXG',
        'Itn769p5PHE9ctqZBEA066rWCsslzGW22raF6QV6bMMvJvh96SUvq0gB8/JMvzMyBHPBWkVw8qlriNvL+zlNIkl640BAXihEpcP6Ps4MEVEGjqPxytxE/BrP',
        'oISyTOivKnUeD6Q1i+kWO1RoqDNh1pWr3h/c8aDRsgn8qEyl5f+NddunxHzFcqVzvaPc6hmYPykuEDm2irNiA/7U8Dtg3HwD4L41nTsfGTcPKoL6dH14i1Wo',
        'kdap1ePfA/eoUO7hqe1XeXu7k3KovECP42/0rLSMDhEHU0qrQKCbGwn784/5QCnITGOWVt8v76qTZe4ZLtDBmGr++BIUHR5Hrq+IiFmbvY+tqzn0sVey8Fz7',
        'aTeAikQlHXXZyYVXJE1Ww6Ui3lA6jrfIFcJL/l/y/5L/P478Lz//8qdfOUexXUjAPj2UsqbL2Zt1wEd46zsvjHE0Ceae7abZp5QDa8QNskZHi0i7ZWuebSfI',
        '1iEI8SzHKvsXihijPGNZtB2zIYj27WpvkbraNcLAR6q04yIboKzNjKHm73pBXcPf+l4ZpOKoxL/58woCHTZG1vlslvZ9vlYz5SR7j79ZuPdKSMbAR9YL/bs0',
        'S1dfbhT4YfVAedqe7BBtP1dYA8w0xoEre4SeK2HR0oM1NHInaYFuU/jmIxC/y/SQcRd6y1iub3blRUyws0CPjBd9zfvoFSr2LT4KVAXOZy3nvXCJA2V0BQXd',
        'C11ux2cB6WkE9BOdZ5lZ+oDR3EotDae8H/5SpFG/UW8ElRuTjARhluM8zpe5gVQZbZJ8MCzq0G8iXs/HU8o0OfeiuFGLPE8kuqXsr5FsLfabFf2b13A/z+Ar',
        'plTquOj6QHaneBGU0T6nnatOkb/k/yX/L/lv9fxI8p9uEjV/Qk2Urtdo41jIFAR/5NUsbw5tEsQbM3JCeasLPqYYjHnyaZhEBrtesb/fP79LmXTB3G43u5Es',
        '7GJb3tQJRzQ6ee5IXvxE45rh3QWyEVizcKxVprkE5m2LzvLx6jfLsgamKpL5afHo2nxhHrJjKU+gJlR8Xf4acvsOVggvqFBfj5eNW/T3rMn3zFCY6+aC1Nff',
        'jwW+hZ+llp9aH0wAeuKxur2iEI/xGYecUPsyUUHK03edU/i8dURcXY22kqXCCny6lBiNtv4PUk8d1LUsi9JUEKLxCN1lbGhV9+Vc0yFAzitEFHxXvB+sftvj',
        'erRZ+fhmZm+QegVdFYnop23fswIV80n7evVfn0WGBaAv6/naREpwnmdZrcfLwZiN14/PEd6etn91vL07Sw7GJ6Dc+XUw+Cqgpby3EC1FfP23/Hue584Vq7f4',
        '4VvLasP8dLQmTUlHXdaw5TnX5+rOZHI0bmyQ3hQW4NEpxiX/bfwv+X/J/wx+D/nf8qDrMU5pSIqsLcAHDfOuoDVBfmz6W4np/QG/pkC8YTeLa4FBmDGfqoh0',
        'RMtE/lyeglvRoo/d9nVj5DZRW/suQl9Lt5ys0yzv7w95u9+79ViKpNHeCN5D/9AF+GSWItsiisTHCw0EFzoa6iRstuO4EUHvQ0Z4iIBXpu6zJ+RBGOhDRoCj',
        '91AcRCzvb3HCK1ok9gB+cTG7QG5NwLNeWGVBTjkDOdJ6Tyi7J8FcBF77LlunaT00JMC5y9Swq7ObFdozdKNzwNoYf2nfRHeTtA16HfsQj/FfG6tsrX4Uxusp',
        'wwuWz1LyI2Vb++pnbAGXI/4BKEFp5XKjgcH69j7fuVAcj9WZNe8FVREoJHolOHJqjxRYKKKF+GQf+Ji3y22iLxICWfV9TdZyVJqiS5fyIMMhKs3HmX4MT1bS',
        'jT9O24ZOtgBOfxKyLmPrdgTmuZf8v+Q/t3/J/x9H/pe//O2Pv8bdP4J6at06vq4towGQmia2PHD+RxFdNEsbrGmaRKrPsoAO8PNdH9hkzrHLh08X8GXLGkeZ',
        'w7LHEeoismUdKI1RtAhzbg+MV4LbQBV3xas/pi6BsXHKtP6IiBdk3+lzC2x3wNzzCFqfzneV5/KUaZrJWhczJZhAL004jRlL2mJTNJjbRSKOfYg4oI2xryVf',
        'njHGLc4XE2VclEdKjv3bKxpHdfSwa8E5/FYLIsCxTIFp0FCy9btdbMRCcVC/HQsvzSoFwVsm8/U1pgym64X7yBIHgO96HIdoleytkNL6YtkkxorSsYIP5SvS',
        'H1u+TAiBPiINw/fT/HrjuigC31P1D122crkiz996qzC7uDAN9O35NRsV3Sw1WR9EFzdoGUS+2Ada7inouLQnplfzpwCAuDb4+QgvlHt/17RxPkjQgkM52Jg3',
        'Y4/HY7M8161eDuTMhItXACwjyiLPp52ysKyFZR7+4Tp3mSvUGKK//yX/L/l/yX/7/XvLf+eDzpWhwxzgkEfT9khFn74W3Ebj2hiX+GASEG3rb9I3S83lg+aA',
        'r8jabiWDRUakyrpKY7YxjyrKMtNpu+zW38Fiq3YUh3zFzaqxCa/H47kdl8HPK6/P72j3IBLKue/8rs4Wnz2vzc9UFSzuPy9+U0D8M5QfpW2LxBZ6Ve1SG18n',
        'D1RPYIhuj3Xu78J7Zag600g/rsfKW6wX3/t+eAvBHkRcekVjF5Maf/dHbXvBpEynTVenMXA3AALdqswQdUKRiGNXq2Z9gQU+U74ZjpS+DI7mixUyBJwe0dFZ',
        'SyjKajpBXze7t4jIZlWLriiqlCFAuKeNM2DZVVDnhlnAk9cUC057L2JrdrQx8gpHH7Tl35kvqK/XeHAG3jKt+HJZTfE4yfP53Mqn1cRaw78K6kMc14Tnm9hE',
        'Yb4QzKcnUSYXPE8bKVcx0wbPOwIRZ1f+nLwYwyX/pZW95P8l/38k+d8s6PuZCZRgsahBIDGwwlVcJuH5YAsCCJGPBO07tGd/e3805A2efbtVmqB/fzxknpVJ',
        '8zH6WqusyyJlKp2frLYZfHup/rNBI32QgBdemsj/tiM0imPKroMB9hW1XBDvl7U+aGYASQjUp1/jIy+1bM1yLDRq+NeOlB8PzaEMK0M8ah5ZvexYqrRnG4bC',
        'zEIzUZgA8vWM/M/GwhT458Sd/e2/O5qfMxZI/O7xqM1PFn6rSPM1qjdT0kXECTTc4MgWvViWLRqc9qpZ2jah5oLDMr640bUV2egzobE9MKVaFdY+tR9blyM9',
        'cP02l3mO9Cj8zCLK2ThEynbDnI6hKnCoi9cV2o7WH7+urZ+2FjQrSswisK+g25hOTdn05TMlk+eiX9OwbMbc/b6sV+bZGhsVwFxYm8JvfKCEOiUAjyHP4ShD',
        'yxiipVREmqIeM6uMwSs4vG5Eps3yW+g5w+s4i4zWcF7ukv+X/L/kf9aPbyf/SUFHZ/ojIFiXdHLs+bouzXLAVoYuur1g0HSCzWeKhdYxg0CdutsHJffjAAbS',
        'UiMR8eI9GJojOKorEihf9sA7YPTXjkDL1kcQ6Sp6C5s+jzmCDWyMYDWz8bcO2i4wLpjXd9R7FslSpF14Mk2FxsnmOd4GZn6XeYAeH2mxIuHxJOUyUcCybA/6',
        'TifPWwHxvFdmEEi3l7WiL891jAnL960n4l4JifXtEy4z5kzA4T2vNX43gi4f+lZ2JcYzFVUs11rpVjxxglvb8hlDnNUvdjU8Y1pln1X0gcdgj3FruZgVxDNL',
        'P47rRuPej5f9Esfjd6wgxdzr60bj66agq+IB3Nh9IjK5TGBExOwiIHMFYEUxU9hNOQQO7AKRKR8Og6AwQDEHz458AW3A33maS/tmXVcXnKjt9ekbXY+TZ8ji',
        'kVnjfXBYVl/7i3sZnumYmBsHp+Fcu3XqNzix3hrG0E6gjC731tg5WcrgTyIu+X/Jf3t3yf9Y/vvL//Jffv7Dr1EgAXBVsAiINjJ4NGgpjdpRuVA6o+14a9ku',
        'VYBVYU/RsM6u4q0i9G61IBbgwNcbA3c+NsFCg/UK/2U3M0bAN6PxEqFJrtjx+8CFmPJu+yowcesTgvX8kXAkxvNE6XH1hMB9gDLzfC5NQYE/WikbmaXzZ6nA',
        'oiLk24l4y2CeR7tXm3MXre+K9ta+aCVB+3HsI+wd149xjcrgR4JGXi2voOvMjltHAPeL9g0mV2ApD8o1o1WE3C82P9kybemtaqu/HVvTdB9lc2l/tz9NAT7a',
        'aOQuKPk6Yr/hbHMBZYCFOspmKecMt5HCXpvSxRZJtXYvm2VQ5w15wQ1/oT6wy8o+zXsFneuIvEToOfozJf3x12rXatkoGDiFZASvHKs7Csq9vz9lmkTu95vw',
        'ycaeNa3nn3i5+UGX0iywrZepAuCwFOYP9j5a+9G2t+L7DQrmaW+uTFm1rCqoI26W+s0mK3B+/Y/WYr4hBVzy38Ml/y/5/73l/5ZmMR8EZZpFSpnb0UyPuHaO',
        'hXk7+pbSHWO7K7oTH7BWbrWjrJh3tpXe6mJBEH3HSuHjD8OPL5vAbWw49up3z0b02REX6prnmRgRBb+57aR0bfRWpaCxtLHWRb83yZ7QY50bCtXSfong+E93',
        'xCJGeCDCqaBP8yZsTPj4ei1DBwPvqB8PHPGBSNg3l5lMNgbRwuQZA3CJFqExkWPXP5PVyDMOP9Z7TDC+qxSxj+d7zHTMiBiYIXqGYgLT+zpHBU+/iYy1Z1yD',
        'LhKK3mKOtWm/cYNf1jVsBqL/eeaLzhliGiqJkhZTwG0tSa9QTY5O4hryFkR/pGv4L83Ky0o70z3fnmljVAXCT5XauQV/Pp8+hSJvHnrgee39QEvJ5pHXii+P',
        'v+OYsLDOeJL2W7aLjeZWHhbA3mofcdjiE6RsPEbXxufPD/n0dpepfW858PdwsX7qN/4397lXEEZ980qxiARrHJdjV5taq0gVutjnnIIuEhVwHje/SbQ5K9T+',
        'q3DJ/0v+X/L/R5T/5c9//cOv7B/WijcrlFl7RjtHmzAQytwIdBuGrU6RduRdzAcslgVxTpuvWBTY2jWsfpHPn99FpMqnt08tjRtSdokosbPPGfB9Pp9ym2/t',
        'eSRAYz6WJgn9cIKdxrsUE1aZhceYjAay8MUFI2CGw1Ysft8sFw19v8Bf2fl5K5QJZ28lUqtWZkXLdubYgf7227vc7zdnVeh318AJeOzjve8LiTqgvMa2OBPC',
        'UXt7EAksU4i9f/Ee49hnAtuvdFKjUGfay8Gv6xMCYBVnJVembVlN5jkotEl1qf95lV2FnetkiygfV2bBbmZ9ZH/z0KeNN3lFbAwagFi3gMSZ1o8JUvDOzJpZ',
        'isjyXCiN3STRtcB8wIsY3n5dWTmv+Kg1Edk9cj7APuCZNbNXTHPFti+fKQkjZdguk4HyXWul0wQRn53FW6gTkhejnUgHys/mWS2tWZ/T2orJgHwDw+3GuIFz',
        'Jzk5oKydIOT4jddrPE1ihYit35f8v+Q/43rJ/1fh28j/dlERgzIkOxZCjtQzO/Rxuhu/88cEccBYj4O3SHV4bsL78+d3KaXI29vdPcffy7r0u2NSBniM3O55',
        'GycOcIq7NzvKh6DlyyCiMK0dUTMOo37a9/2CywTm6Nsj8BZZ/N37TJlfsLc0mLWiZ3IAPsbyO11mAvo+XrHcCyhi/FVcQEvfp8w6CEVKxAflxDHl5/63XxNH',
        '88DlPgJ53Qj85PpHfoAjSK1DvS7YXD0yS5+77S/F3guMTCGMvufNPYbwaO/ELiVRHmK+8iNhygqLVyjt4pKjbDIiUUHPhOfmW92ybihu62bt47Wqlw/hyF/5',
        'XcunvPo+u7Ea8M6pFCnNj3U88fvKpu/72PfTIBe0+1CKtPR21mYWwCXig3vT2hhzAZ/kZ/3xe/SHxbdxcyLuXT+uvq2PjAUL5qhM+IBatLWn7Peyx+otsifH',
        'L/l/yX/77pL/v6f8TxX0CDgCYqLLnhlkliP/Hkjy1bsi/SLlxT7FXf723ePxLvf72/C6YD1qW9uRe5ukhMfyNcvsjxvRByreYhSIENWSssApop5PRJvPjQGA',
        'ceDigUgEWp8x6Dz4KQqHc5LC+pApT3maoijcMp8wWMbiYOZCnpmPf2YE2fueifA4IT0dZyjIhHPt2uLn/HfGJPKdezbWZ97vCdvRdwr5UVpSQ2BQPjNLfnwr',
        'Yt/gGFvHd3JWW2c1z3SXTQjjKNivkdJ9E28QbGUncXSi1SsPgaWsCb2tnriW/dqaUms8IF74A6GovuFmQffvxa1F6pXgSnaMNW5tRFYKPqKPR/oQaqjb/OKt',
        'LbwvxY7+z9I/xmikXNrpxJEv5jH0rDqeMrSS7V/Dq/cjB376zE5LuD/eraBQneP+GP/wfveGs8mLSL9qqbcTECgvZrU2npvzTG5f20AWkvh8BFk8xqubh0v+',
        '9+hf8l9aXy/5/+3k/5bFZeomhHe8YAoI7FCkYvkc9neFJlwb+h0TtHLJw+0j/xvBFSZ8l2bJMmKyxQQGjfKf399lmqYtSCmBuMESrywsmyLjlAUwhlW2rA1F',
        'Hu9Pud1vNMGZZpN0u5Sw2LyAOc+A40K0NvudZswm4b8f5SHN8Ue52pQIs6j43XT8ji16RliG+zxNmyJY2zfcLz9xNnY8B34sPdM1ZXUNVhM/HucgLNwPKlIj',
        'GttTtkRgoSqbAPPuFfp97fgBK/H8/V7qxpcUAmweNsWag0hHlm1TwHKLG5Q6XjsRP/ZzP5pL5DAXQbq3kXuAgSn6+p9mcxApZRYOCNS1xK4RtdU5TzdSZrJ0',
        'YJMp8FUaH+J+Qujbb1dCsjXIayxa0vE+g6i45L7s3m0HQXtIUcgKMsr7zDxn2o8Krw9QtX70Y5q5PWRg/YQgV59dnfdVg+2KXTfvLbI+KDQX6Nl6ZJ7VK8T+',
        'QqcezDJ+yf9L/l/yH3X9SPL/hAUdkcS9nxQYTnZs7BohnDtCo58tiwSe18RXtcNO2+8uSxH/vV49PG0WKztisx0t8hHPwlasDPJI7NgpHYTPn/VGOWQoQFYG',
        'z/Q9tWfExlaX2FYr0xHJ3gLgZ/3fXvkbW3ji75EPszH0uuHsL0yIgXgsuDIwf+JeoeLdrgl4347izONTHT57hFaRRqzJPcyfEXPvDxe5ekmenYOM+fZKSK+U',
        'mLJntNYsQeQms64W4MXCH8fcqBeXFDU3E+H2PL68dnshbMyTL0UxJSZnkmxJiUfNTRcWsdsBt75FH/k4Xpy9wr3bpgupC7MrxrmeMArUz9puFERfTPnRcmxB',
        '91b80VopVFfZeJ/5GNs4R3oaBz467N2csaWaxkf8vPbPPM3urYepTC1oNArQLF9xpoBnNBEFNSu2Mce3ZS0Z8RmPs9+E2Evd0C3bN56OjD+NlR8P/BzW+XNK',
        'AfuVn1ckLvkf4ZL/l/wX+T7yP1HQe+aine4zBezdbJdNonXH/TBC2gTLq7BWRE1jFVAXKv6xI3AEZkQhjv6y31Pa3sAdADtk7t6yPGWeb05QLgsHhhwraLkw',
        'i5PNC3K0OPh3h71ki6gPPMmZMAKJEKDWCy+uHxADMzLG4vOielw8U+vnzNqMOMegM2s3H4eGMe2cc8bXPx9ZhfZgRDvxnb0frSP/MZRZs1homaYY8/rdhkGt',
        'TaqslCItO0mWiaDnzbXVWyZT/lNdpNHi6nCMfTehZRZzu6kTeG1KV/EuKlsLMsq8sme1iX7BUOTZssh1ZkGnUAbsym8hxcCsxJwJA0qItyQ7LIUnzdIzWkCq',
        '4pdb3WHlzeIWMlrXcemV9AyMV+7xpBH/s6NpbZP5kR+LkVI4pr08w8M8T1LXSqcd4EE98PiMFDU7bdF55lR1ft76U5Nc53mm4wAAIABJREFUicJv3uRhI5EJ',
        'fxnUMYJL/l/y/5L/P4r8P7Sgc9S3ddIsD0cWlAyx6B8aj7k4ovuojl0oUEj0J/uUjQQPJq35u0X6CesMO3BNMYYd37agVj1mtmwECspY547o9vyA0851Cwqg',
        'zCAGbLQvUyJmQaTzzemVUK+95981WB0iQfULmo+gIuF5RpP5iI2hD6aKfRSZJmvHLLbjNaHvkc8Y2UoiEefrHcvhVQLNcdh6Eepi68PoqE6EfZJXl5HBlDkR',
        'Vlwtj7AFVrEPOFumh7xN7Lf5g9bO6szAwWFZ2kWjFyuHgKJ41J+N+8j6s/csYNh8yfmyplcAyr1Za3H8vrYrtlXo4eIbFiLWD7iE4DeneyuTtIw7XthlSiHT',
        '2PiYGYB20eYIMh9aX09vJbPiunlA/mzFydxB4ArzSt3xvYj5U7dWp5j7Hn0BfWG82ZWJtD0ihGVZZdpo5vF4yDzPTTkzfsGbDdQ/ygDTsB8ozxn4TQ7G8Qxc',
        '8l8u+X/J/99N/g8U9Mi4+VjMjrzi4O4LtaxukX5xbYg1AqCI71guFb62OKEELOuy7Vrv7RmEV9/wmT4knxUoSrAuVPeOxykSKCASaKZInGGA1pEaMnzsLd7Y',
        'HyMyvhBiXB4Kgr/Su1uwNfaFid2Efr4uoqCxcph37+ub4xyPv48gWp25v4YD98XjnENkcqO26YvSPx/7L/b14FIhPmKHuwZ8UVGvBrjVpiB1KdagcO9xnl5u',
        'bP/0uI6EcmScEOhSrD8i0Srl62Xf8pHF/JX1oJZx9SOPuaGP69mEWbhkxW5W5MA5tprHgcRzbwmGsiBix+n+u9LaWB0DjGUzf3ER5m2lTG2djIRoVNDjXPZW',
        'sQieznNa43eGL7LD9O4wWWrB3hrv855HTU1c+axf3JbrEW1AMkHvN1Aox3iyosP19icszGd1swA6zy7z4jYu+f8KXPL/kv/fQv4PLeiwUOnfhhgW+Bo6mS0g',
        'kZ554VKObAcf62hp1IRuJYO1SCxTA6x6I2sYFA0ev5GvVAfjee7asYWS9x/PGR8u+9oOeu/4mJFvGCbPJHnviTOtdccqlrk9NGaRzA1bbBlvs1a52hN8s2fc',
        'BohWtquJe2VnbxlkxGm4W5nQ6rjCvgUxxhQZS93q79v07TCDGHdGo9pViZumeUsVqBZD3ACKTAewSGv7Wj5ao5riXs2aHv0z9xTgPSsWBB4N0YYL+537HOjz',
        '5ic78gU/SgHHdHrEGmq1i1QGJYTn58i/kbPJWPuwVKJNGxPLQ+zpX91mdPNglyndXLvzPAlfRW59Gvc3G5N5mhvv5Uursm/9hiKuV/CHXvhqn/1mISouqohb',
        'mjSvHPXPPI9CjvW1tYe2l0Xp4Xa7OReIj4IfB1ZGADF7DV6yGxfeuZrD2t5zHbBMMFY+x/WS/3LJ/6zWS/5/N/m/6+ICP0hnvSqFeWpqDXPNd7tAE4axHD9j',
        'y5eIP87Ge46cbn+3xVHUmb993xMpcIoP+RkHoGXAF2rUyBcJcAmJMgxdJK8QY4/fh76UnED5ee+vpdATzREhn8LohDJ0/C0tSF9iK2fKQZYBwubbc2OL7o/v',
        'KtWdEdYIeuWb8fTlBjXUuGYPxp+aRIDksmiAo1SR2w2ZQapLnZjmRHcCztI7wnLSM+YEncgfE2CrmfbZFDNkx/DrRucpWtqyFIuhJfEZNM4TFitEE1nCx9Yf',
        'wzO+32u7VpwS4KINVVbXdRWpst20uQmiaXJuTFCcNQWn0WopIvN8a3nSX1XMGW+4FWFMsrJRMY2KZzxmxt99HfY8Ktnb0/CvtPeMm6d9yzuPQM7YPvpZCsr0',
        'x+5xE7Uua7tB1HiI0Ufsqz6L+Pd+8l4R7/lJHxTM4DeJ0R0swiX/L/lvcMn/30P+n8qD7j4gAnCvQDgHDLbVWH1dpahYRepfDUDpCbzDzDFd67zWbUFG9p4r',
        'qdutX5ZBIbNWdnyzISSp4jKyGqLO59NcDeJ3mSUiE1zHRDqa9LhA/KLzgmLQyYYzAuHyunaxo/74I2cb7Mwq0q+vcb8sUFBkIV9V3rljfUTcsvU7ZiQm6GJd',
        'WZDNa+Dr0/R8UIxyax7nRccY4jtkH5mm2eUgdgFayXpeg5sL6kMZPIMQb4K04JbIakJsZxia9ZwZpbC7h9j71q76EMNvtLY5La1sDetpX/E8Fhp8PXgfhDoC',
        'KEamZI1S4QGH0RXr0TdYn2XttRpdvveMRyHYtc9BbBsk0A6CQ5E9owoL6l7h7v2sq+TWsz3epv317hn+ginFIabU9IFm3Sg1C5l/b36nkBulBZHGFJbHuIvw',
        '+tXeZAHMZyCmhmut098ZDz6jRGRwyX/X14aQXPL/kv/yLeV/+fmXf/n16GNOfbbho1PQiJKJ1hjvK8Ap37StiERAkTZN7bhrW1xskZjK1FIP1bUGQWJ9MyaM',
        'iv1On+tvPm4hVVIMfIgARan1+YM76FjnzlvJucqoLp/6CMTDgR8fWmQnFB4jmlcvQem1ShWqnLbOE05k5q62dN72jkN1TGD1NQbySifiwh60VE2Y2a69h8zH',
        'EQFLIjo2pZhVChDTDw6xxXxuywG0oErr1BT03SFzP+2BzwFubWEz0VVVYZ3a8jnTWKrbQx9smuVe5rbGCi+3G314mTbO08nz+aRMHK4FWZdV1lqbTzmvw31L',
        '6R7U8LfxbMyhjYf5dvb0o7SK+TZLI3jnWMgdKxfti1R4ekuYlZnnueVP72oK36WthTVrV5fzKQvSPMrWR2tvFJDXKxgTveMd65GC/aUwUtpj26HEJf8v+X/J',
        '/532v538P2lBP4J4fDgNGBP+9kFq2SLrUrFtuHeZIIgoMxilPGOhjYtWtPwHui+ctsf3lRe5MXXF125/HAO+RxujHXT+PCPQHHxwBn8fy70+Rnp5gDHDMQ5Z',
        '3UdMAQSYCchj4dO3fdQ5ZlQ9bswc4vXBkdDZ4mwBj30KRG7HWy16n8fW86Bowp1hnm8u96/Ix45aLaWWuHU9TKEYf/PUNLlf21Fw7IP2vXT1WZCrz1uuVZdW',
        'J9xiUG+OnFci83XObjH7isioDD/XI/JMAPIA6Tt2STD8Ik2dUY7AT6a2XrEZ6OmfdmJEa/ge8QmliDwez8bbUc5bFKc2V6zwji2GPuiR6/FWeuPveqKxDOr0',
        'Vnc/HlqYc+BrW7Lx6tn52Zcigltg9y18cQy4Xe5f3OzxWthXnvfX4bdQ9CNc8v+S/2O45L/W+xH5/4UKuk/bxcKNbwDs+ie2E2XorCVbYbZ27aVX4mNstv64',
        'OsMCfm2n0xrC567OUVWliNR1Yy40GUcWyyPFKz7r342EftfShwgPOJ7ZHb9edySCEbHp+zNWzz28eoHitcpXGYfNx5k1VlvgUJlUSYACroF42v/sFscjMOVc',
        '2iUcpRR5Pp4yzTMJCY/nuq7N+pSi/wG5D7qEgtuNSxV5Ls+m8I1oPQaOsX9ndGfBGPgx4ePOLJVdLpjsexFYjz8m0K2NPGhuPLjIM2y+5vxNFR+M2ft8s0Ko',
        'rlKzrMuynTa4lui7mjz3+CGbC4O/lh5rcXUnBn4upK37I+gzllg/53l2eeChnLE1cI8m47oxH3bN2gOlHK4x2t7qlHdbf7YJ8fVvwrhMg6wT/caoz+jD70cZ',
        'YUReJtTTcMl/rvOS/x8v08Ml/0UKK+jnJW7PiPpgln3Icryeh9zaYr/5ODzbWTYiLzQx1edlPexBjT64/ohrD1R4IAPEuRXVR3gfER3KHL3fI6Aayp7fkb4C',
        'rxKwJ0YorsCvr2hMhHm70e+2h/E4ZBkilAAzpXrcaRX4WE8T3Tq5P1As69nit65V7vdblyFFBcaTrIal3UjHQaAjq2jrxskl0TEs+lPToakCF327IwPmY3fz',
        'XxyNCZRp+1cDKk2xR13M7PO6emE4Wl8jXLSs4svW/1BSeiXYMrQoRAVVwm9dDGxdQhaf2+3eyjyfi8RLmNA3tRL7ExfDz9qyjcNTWGmL/uhWf6+4OuxPCX7v',
        'smIBsYq3KkL5/OzlSGd3B6xVKOC3201KEXk+dTxwigT6AW8HtNiLhIYMWPH2igEHm7Kyb9+X8K99a2voDHFe8v+S/xEu+f97y/9gQY+7reOJeDUDQjyS+8gO',
        'DES0rE9ZltXlN+XIb21P+2LptmyC13WRebq1uV1Wu+wkvaiAN3QBP2urZxhWzhhsdix/VtCPGf3rkB9tfVGNyTNTfGK/srkVeYVg4RM6+iBbx/GZ/91b946Q',
        'sW+jy4kxD9SHtl4Zbx+8kynJcc1hF66X6UzCObbjcW5k/ggIHV10cRr2WEhgNRYIaoGHIpoWSwPITOjCd15TPT63v6dQfXXfaH99wBhuH/X5ri3dYcv6UK3O',
        'rhs7Cmb2Pmbh6K9R79cHxx3Aer+9kXwtAyyYk5+JWJpN5F/fu1J7nssW2GYKu1f+tZy6HsASH5X0XmnH+GBtM4+29j8mvH0e7/3NQF6PpV8c+bRPZXbBotq2',
        'BSaewzW6S5nyilSeMW1pnzEpWtYjf+F18gJRXvL/kv+v15g8u+S/1fea/Kcg0b4xbSAbrI/tpIB45+dHR1lxB9xaTCbx8XjI47HITz+9WX1V3MUqLisEKTld',
        'm5MdPT2fT3m731sX2+1i09SPazm3M4NSYATqU8ntLczxTs++GRPq3mI4v1COmcGYMEfr5VXm4oU5C8JMQRlXbtf19mVGzHUEdvmHq0UyYQdBpj7RXG4PoJyt',
        '6Rrr8K3SBNc8zy03ONbpbr/OyPAvBLeWwxCpsj03YauPPb1uPxpdMy/prbT+O1jkuUzmw8tjeyQoR+85Qw7Koj1WwOpaZRrmUrfAXfi3GvDgRT/mMxZKzgSy',
        'KTfkkmJrq8jtdiMFHfh4l5hapV2jDmCF1ZT5/ObQV5TnCFyn92vPFEEfwLrPW22elJ56nmGKXw3f4nte7JlcMAW9FA7c7i9H8msp9lmorI15TshRYY/vGL9L',
        '/l/y/5L/I/jW8p8U9IxQv66UjkoFHzUhwGwkkIEe/PVKKfJ4POXx/pD7290ISrxQX1a7bRCWCL5REUFmt/nWyrqjpGrZL1jZ4fHPsxzs76hHeVk/CnFsre1R',
        'IGFk6ru1d08y/749vF6DbC3aIsfFA7ULBouEeoZpdBgPvq2D96Mj57xP6lKBfMqMgydm/K3XLGdBbRFfPTrF5UPw84XF/Ix15uycMkSF+JW5ju1xFpjuSBcK',
        'dVB0QLt4F28OjbQH95aesdsYI93gK32CEnCUNs8s2lCwMMdJfcXnoQbYxTrSXHVG9fTrqYhZurn/RfqAU12vCIaUKk2Bfz7NulRrbbcN9sqKD8w0hfR4XM/z',
        'GJ/fGCc/zOOBw1Eml6xuxaX/JlPM4H4El59sXCOf9mv1XKaZuK5N4PO/rKRHpTzywEv+X/J/WHv35JL/30n+q4K+N9CjjkbEM+SOvt2+ooUen0fLkR3HaNnf',
        'fvtNbrdZ7vd7EKomEKKfp+7gp3YxC18AIqLWSndDYLZeCI3xjjkpjF9rlbXW4FvsF87ernX0rmcK/WK0Y61RBxlvX852rjlevp04Lnvr4exaYYHpd8tssTs6',
        'Yh4x0cwKy1VleU37Z54mPDMDo9nH07s1eEH6+fO7fPr0RjiJLMuzXZSBWwJxuUrsTwRYaNnf/IwFeQRHSvvQ4lQ3JdtJNBkuCw5+5SHPFPRo6ezxs4BCXBGP',
        '9w2VQZ+gnO+lbmy4UVCo+sBLp1gbP5Bx56WKpfaydeItMzGQUqS5Om0pFdFGPKUBLd1u07ZZmlz9z+ezWbIQQMkZXDAGRpMiPng1z9DCY3a0/piuYMmCNdT1',
        'mPK765Xv87DebG5tPYxTwOG7z5/fZSqT3N9u23OfFnXPQmplR377kY4zJSEq6W206N/47pL/l/yPcMn/rA/fW/4nedCPBwp5h/1NZ/zdaBeSIbLVEBZcs2YV',
        'L/jMSqVMPvPlsvRquNq8klUsyW+KfrXB6q92NcR8t8AUXQ7UVSinq45FnHgv0Px4ZQu7lLkxkrU7RvZ1MC59FHF2GcE5yJnC8Rzv1CjAG0oAjwOE7ijzQtzF',
        'n4mYxu1yLMziHB5i3do1Zc+PC89npIPqykA5q853F4wo+r/qN8+n+kpCaVqWpfULtNAfu2X98L+xdoHuRyzqrZ6DoYzH3IYU/1kdfcbnyPjC139nFxplvuOu',
        'PseAV2ELeoRCYwNa4jryvtsawE2oInkOdLtMaB+iUmHtGg/zvEbEBMN+OxgDPfbXBaG52E2BUcVmxOP75/7kAu1ahhXL7pKP5Zee7mimleWwHucLv50KaF09',
        'rqP2cjceKx8z19hzLccXSm2lJOM1e5uYbKMRSgx+X/L/kv+Drl/yP7T77eU/WdDHBOUXTBZtbu/AZL4m5DtFywsMQZxBC/aSXuCDIDmfM46eOkueaxyIeQYD',
        'XO1vI84M/69xUUEOnilYm/AhfZ04rV4Rv0O09vq/+ffZvnoC1QDHHGcfLFXas6N6+RksV2fAK0HWL88Ysvb7do0QS2NMuEbc+pIfdWNen0/ctqdtagq4SspD',
        'dd+chbamD279fAnC8PPcNReK7JsEQLd8QZHWqe+ZJlnAr+vabpzEe5tTKPaK6DyXTQn1ir6Vxfy/xuugoEf/817BjgPfxyBAwbX8y32GnLVqukxUZ4oVUvxx',
        'NhxVDqd5Uheaai4FqrDPrWJde7w+zbJliir6DJzNt5qzm3wvGGVu8XjuycHjNKee//v2WPmOz/EbGwk/hpC3Wbv8LvLnEfHGPl7y/5L/Z+sVueT/95P/gyDR',
        '8YDuX4ywR7znINtFZ89EZHhEDFw4TzEHnrHlqPMDq0TIg240/9gqwwsO0BxudRzVccZ/LRN2EfbXmO0U+wV2/N358qPvDcfXFMU9f0wjrpwRnu0bl82exW+8',
        '0otLVyJufX37gMscpiLdpQ5ReYMggVKrfsKlc1PJgJXSo/n4VgrU8/GU+TZvG4mpKavaqDgazKzosI7VWl2AKKy0APbT5iCxvE9VbN7Yd9nzwpgm8XUwWmKl',
        '+Lk8fakaXTWqrGvvvhEV8mknt3JcE6ZMYZzmbT0v7jpy+OzebjfBOK1rbYqk1dXTR47HeT/01yCXW56Hen/11zeuYyU/bqBYUfPH4Ozmw7y1Es/DacIavkkx',
        'k3P8LipY2Xhd8v+S//67S/7zN99X/n+lm0QZsW+xK/TH1gD4oIrY0X52ZJbtbkHAuoPBjXB0S9lGgEfdaQS2Ebbigh1QH0WOOi3Fm+7el+ULd9QJrtEKE3eX',
        'Y6KOO+KDhrZnsGr440GrB0fyylgzwUBfVX/7Wg9164PlQI2M7ogZlCKyLnaM3ffP95Xr7P3K0ha6Oo4AO+o+iMRgWcwCx8FOnB4QggNWdlYu+MIioJfRDX7H',
        'jAOvAtY3clQ/n0+53W67mwQWgloJENrek/UrDhPewTd6nmdZnkuXLQVtm1VTLRjm3hGtvHl2lPOCB3SSZzHBv3YMa4LHH+X6daiKQswFzwM3WoNQYuALW7b8',
        'u6aIzbNmtSjFNoLqP/9MBJPP2DICc83JhGrumpWVa73ockRvpbZxjJsdPX3qXWr4u/IVrJvRmm64txLoAT3z+fpLMT9su4G1t+T3ih6vndgej9/XlNeX/Fdc',
        'Lvnv32k9l/w/hij/dxT0o120yJdeNvCq9SQqDmVb6LhYJb/ww5ePPmwdITYC6q8yB8TctOtqO25XPg4h/Tamqw/gk+uZuuGejcFZiDvRPQtrBkzk490m+77t',
        'V/6RGzETrAREf76eTDhyqqSzg/JxQdQfh9k6YuGMIL7IVKGcKwPjm0BbC5IxjOauUkVGBrBOkH2wm03RrP6YeJoQXChyu1me9SybC9MyBCe6V2ul20MtS4fP',
        'W6sXLt3ut0FnfdpF78KS9cUrRuct6LWtLxHZfLrrFqBmqQ55/eW3m2I87cTBLrCZBj7le4LDXISwWQJu/O3tdqeLjKZ2Uc+6rpuSvpVuWVyO6dFkx74y74+w',
        't82IlI43Zz7hnm9HQeuV17hRTJX2Go/T+41W1pbvs8iYhxbp3VSsvXVdtxMyxALUUFfk61+qgF/y/5L/sfwl/7ntj8Cr8v9DFvSPENeXwh5TwFG/BV2YwPJE',
        'vbpLUJZ18amTRKS3QniGzsA7U57cqFTBuvn5/bN8+vSpPSvFL+Yz6yPdEY82u2IMgKPhP074H2X258Hv+nuhKyJNQc1TEI4BVgvszJcFFir1C+6JtCdoCINs',
        'LL4kqNKyYeBZcVkGRFiBshsMXwW2xubpt7LyIp4B9/SfCRZYzS3tHVmKTvA45w/KFtO6tgt+oKw+Fw2chVV0KrO8Px5yv9866xUU/pinPP591E/OX425V6vZ',
        '0tpCYCTTqg881IdTmZoFbu9kIcMD4FMKMkRlzrtSzHNp/o8ScAC/xAVEt5tlD3o83l0bcMHJrJg9eBeO3DK1T1NfEjjqeXfGc7xCE630/mIhP27HbfcK/v6a',
        'swDVUZ/tEbvC0KJvv78cLvmP9/rhJf+/HlzyH89U/pOC/vFdwTF82dW+gOwWMjzn3Ld47lLMVUu/xsL+bFq3jEjRHieeHwUpqNVvJZ9ZuyAhm9Mo3Lzys8cw',
        '3JPWoRhl3gySQTBAefnKcT5D6FM+jcv5ifGC/TyY5QzWaM592wdZRiG3U/MHFYZInP694mIuBaqE4lhVRDo/yI8o7tSieMbEuKh/MtqK7RwplhbMKG3t63fb',
        'c1h2t98cUDdPc6PfZVlCYKT5mt/mm/s+QswIwc/MUq5vLeuI9UlpvWyM3XDQgKZZ+GZQz68Y32xs99ZyVLBFRuuRlfS+TgRorgK/Xc6pzkqm0UFp1hwtP7f+',
        'ac792r7jjVgvZLWcX6N9jnT4tce0ZHGsgGeEvXd92Zxfn1VAz1vvrA/ZzaB53duX6cbBbsHNcLLxjHQKHprxs0v+bw8u+X/J/1b2d5f/P//yx1898487iI/v',
        'vqNVag94Qe4JMCZSXlwIGmMiRXkcHXHOU21jO+YqXlBzcBpb75oitC3wUkpI2zNYMMn88hFXJJL9sWLBlYOlLOJyY8WL8WYGcAweF4zvPJtbAad4s7IjZWNs',
        'ClDmpkGFSmSvH4+Z9UoVXlxbzgGWYGb7hBYn1H776HytD4Ef+NeUmkmQWQrPewZf2+UnpUjb/UfmDYbTu7xksDe/e+vrI6moZOsfLpWYuqNjvqmP5wMZB4w2',
        'kD1Ex1t97bUN3GLJFjTuCiv68blP0+jHVhUhrdOUVPN7tDmz41b2waxtvvfn5Wx6xTOwr6QrPvBz9IGdrKxLc6Gqqzi3gLf7TaqIPB69HzrGDhlw4lz3vM58',
        'rTntosP2ZXr0sK8Iv3ZxUdfy5tqjCpgF/qVlh9bvvQBQ/t4r23UTRHtDc85Kesl/7e0l/y/5/2PJ/01BjzuGl7veGvwWEBewbJeJxPzGsG7Vard+rZWDMXRh',
        'LMvSLp+YSnFXA8f143bY2zvvL7sKH32y0pFlXGD3gnVdZJ5u7aZHz3wz94OccNBm89UtNm5HcIaB7zMNv1BNSTxL5Gh7tJP2a9IsFTEH4GjtjhVRvixC3SSe',
        'W6rCVdZlbZeNqKIxyePxaMx+3HYM6ssYkn6f5zr3dcNPuUX8dwFslcqeDTQ6Mzf7a62V2lF+YpnI+LySvvmd01DC9xbWJ8uqokrNPJdmzVXleZLn8yG3233D',
        'FdbuLLCzbN8ZnUUFAMg8Ho/tgpub3O8sfI77fDQuItJo1h2vi79VkS9f2gekT9wraGsSrjW2kahNedC1XhpfgZI+zzeZJg0ehULvrbTx7+wmTV5fUUH1l3hE',
        'vpkJT1PO2FqYW5NHygieI1+6Zq2xMXt/f6rL1Ac3C9mm2kOmqHv8/IbG13VOGYbvP+N7yf9L/l/y/0eU/83FpVYcmaEy/uiI8Coh5su+FlC1IbW3G93a4wXM',
        'CxT+XpzVgC8fwTscEfI10KVMTTBCKYKVTQ1yhXbLftfXcAWB1t7XNgZRLJvf7MinMCOeTEECwL937+j2I5AJgEwY1Cq0E+1aR6nBs/iNPtMAsNLeo2/GGDPB',
        '7YkiKgQ67qZ88O2CsNhizaCMXaKQgSfAM1YQVh5GAOUHa5F3+T1UEgpxrMvg76SWZI7THoR57561W019md6y7i3m3C67suh61r/thtTSLEXI6FBrddd2Q5i7',
        'PoK+S++HrnxhklqXzcddfa2fj0Xub/egtImYD7KfE/aBNeWW3k82BpiSIx/0sY95D6N2s3JoE8CyAO4tfLuqiMj9fpNlWVwmjUwp8Yo7z6XFJFgaOk4pqL/X',
        'tYrU/fSRR/2L6zOrx4+3ntTo5Ux7SgCneuN2jk+asou6vJyMt7q2UgIF5UhO+k2QlfVzAv55yf9L/udwyf/fT/43F5d+xxZbGAn2yLDMv3FrRo4IPBP29jsn',
        '8MhQsAPjVGm+Tr/rrlW2I5MqdZUm0E2wLQ3veHFBKWBEtU0qnnO2DBGxi0LicFXRHdt8S/Wts7vnqHzBJ7fpM2VMUBiHfN6ruxEtLvIcznGAI+VvLxBKv8sI',
        '+gzYd369I0uCCj0lXnt3nPJp1FbXA/1/Y6A5US8LpUycTJFR3HJaQgBpejvnaUtGP7/qY3mc93m31oS541/k2G7+4kU2xQ+WWR0rXM1t6RKrIPgS9UzTJG9v',
        'dzrKLu6SnnQ4tnd7ftu1ivz2mwZEfvp039JU6tsuj7Jsa0ss5eVHXFe+hsuL+ZePBgDzzfNu+dkVj6nxQmwQlP9pneZaEFvf2ySKWJCoCAdaspJfq7RsNa+s',
        'NXyL3/b3fvaI4e22u22hn6OsLh+DMxvkV5ZHpLvI8y/5f8n/iOAl//Xd7yn/k5tER50/MyjjnfTXAs+AM+JfNyY/u4UHy1hk4LzjfDyeFMShx09xkwem7I9c',
        'jUi57vadfuKx3C6aAYFGBur72xNGtkuzY6La2vD+UNhBmuIXgS0B/RSWUDYSEo64ohVl7NrQt28KAMrAzUN3uWepZG89W3o/3aFb4A4LXV4rZ6y2JPk+AAAg',
        'AElEQVRiffsRRtYNWySah9oCV7xyznPp+7ksT1mXuh3LZQrZnpZKT5N1NbLaZOvV3ntmtBc89Xwucr/dpF1AtNZNYJYtK8vc/NFh/UBgGOpF+j8IV75dlHOl',
        'x8wOEcf+qLW29VfrSpf1WEqsEewpi/ug8+WVkOPyHxNcvaAArgpwu4i803J1i/R5nHs+1luMmvvMVlY3YpqzHm5cy7K6WIO0BwOBzniwwNX/xtarPYVxVH+C',
        'lbw6F9lG9vV287IiMUbFlNtL/l/yv8P9kv8/hvz/y9/++OvY+qF/v35M9W2u/M3BhCr60d8yqPjjwhWUfT4XKSIyzbP7NqZeYrBdtL+RDjstMCh7TphWP+mc',
        '+3TEJMdBTH6SzYLQH/0Bl4hXRuR9x/NdnpXbZ9x9jtGa4jZuX+Tz589Sa5WffvqUtFmT7/pnvfCj3bQL9OmV3/MQCc9/exSQhl08gDOXeNcBs5Qw4328P+X+',
        'dj+JW4ReCByXt3p1fmNgkvk2N+GzocxMyo6b1/b5uhV8Q3+qWrlMKbfr55HPG8FfnL2lWb6q/t0Ec7GjbIyvCk1/Bfvz+VCrsYjc3zTF4PP5bOkceSPAa4wv',
        'jWJLOOcw55sOM/B815SRnB/HtYrfHBRH0rcp3jzvuSU1+nTHILs9P28bS68Ye57J5Txt8nNcLpSP1b7VzdO/rd3+3b7iux9ANuYX5+NDgMf4xlLFA/PBSvEe',
        '3nm+eK6Tnri/L/lvcMn/WO6S/778uB8fkf+bi8tosjAYsBbF3Vg2WMaE9Wgg+H9+wCfN1+3rGNXHQhv9295IKXpULkXErmrFrtunZmrd3cD8nmpL7WS7vi0v',
        '88md1vPZH3FZsBMa9gt6b4c9Ikgr5yEyjD0Gb2VeWbBdDTLq0xhUcVuWxQXoxTKxHVYCUkyS3f9enRnTS2od4HMMsA5j3nCRjYg0QRPnOh45Zs+O25Ww1vr5',
        'AfTMGc+N/jnoMgPQjIjI8/G0IJ227lXxVSsq6lclnJnX/X6XWpXOaxUSdCJ8qRF3qc+LLI1hwn8Vv+/3+4avMU0NTNXcuwrnju5HSusxcFCZSFTG7DZMCxy0',
        '+czS0UUXhDjf5xE8eyGR50HKOzlrS66EajkE/X0J5JsFkYxnikBmHR9n5/PJMsoHBL5/fsjtfmsK8lGmiMzSlhdHirYsKNGX5DiPDvNL/l/y/5L/wzp/L/m/',
        'ubjstcpEiIXARwH94OA7Yxx7Rx5RoTjuXDz65QtDANhFT9PUfMCQaq21VEwoQWhhgHAUyAoF+mz5kTMitZ3sEeSBQnv9Hgm9/au18+Oqcf059AqcNIubf3YW',
        '4pFS2mrSv5HFqxRpFtYiZfNdZWuE0O8M31d2zFr2Y4pXX5fe1mhKKgueceq22JcIGQPKypdQhr6q2zqdZilTvmb3FFB+Hq2Iy7K4Gyhh+X48NX3fPM/t2LlW',
        'tUbPt1kej4dZz5dF5i3AZ9dCWcVZtHGZ0LoFA0EA2PEvp3KzGz/56JWZttZrrjVZZhYt69fYvhI/9t9lF595nprPPjqL8cE4lyIUPMuKMiscrITVbR0yjdat',
        'PnWVQFDd2njq/nrUPnqLuwErgXU7GTlWdqLgjBuw0TfZeLN/PXjF588Pud1uwfIM2u+VDWtjfwOTKVB9HeNjdd/vyOPGfYyWzH3+we8v+X/J/0v+x7LfWv5v',
        'WVwiI8nSLmXIjzq5/VXNR9QT6f4gHO2yH+/vMt9wYYYNOCsxtSL1mu2Sp8kESjy29UFL4hh1w7oJLM3ykAlqXnBHRNoC5LrMB9uidBcKZBaVfeKMwuqY+Az3',
        '/cU6end+pSJPsvr1zjSW/VgcMztbA6h3nO2E8WSmY++gjLEg8cR9FizYDn2IlrVSZMvtLdQO/t5uxZxK8+0zP+mP5JeV8M6PgwUF+fEwv8KqVp8uk4lCnlos',
        'LboFZa7OwoZAzd9++61l0FA/9CLeUmhCd1mfUsrsA7mS/QeyjSAzjN46WtTPVHu/FfW5jxGoC6Yfc6VHQcF5w79mbvMeRvw4jj+n5PP5oXHcL7LKuljKMT3Z',
        '0IBYX7fPMiIiLZfwXnrAHthtx8obT+Y6LPjyyOr8tYBp7PNv7/Lp05s8ng+53+86fqvs+sQDRsIbpzUjd50MF65Tn/dt2Tf5Mz+uqCBL7XjJ/0v+X/L/h5D/',
        'ZkGPDRfJLDjHVp6+w+qHZLtUKydytsNGtKv8/e+f5aefPgUi9tkH8A1b6AY1Nxyi1QEWPSgtNth2myKgF9hEZAPAxSqspDDRxQXi6/e/98D65X1Mo6XloJbh',
        'm7OCk9sFHhyk0Zf3xJlbpew9rIgm+HIC5ECW2H9l7KvobWp5n84RKwSAPy6PBAq3A879zDvoGHjn+2V/vzaXrjcN3zPfcqCZX4u9/7IXWJsgqdKUVk7HVau5',
        'vMCyhMwtzyfoZGpZWaapbDdZKmgaQASQ1pbtBW4Sz8dTCl3egVzp+D0SDPgv3mDIyjp4YAzqjTCik5yGz6yvPZpcaez9GrL2rI51WaSKKjWPx1Pe3u7bJqnH',
        'E/Xi1tRSZBvvKBT3mVPkSbyGAbBWvuJ+87Xg8XjK8lzk/naTx0NPWPYFfw8jS/oZnllKacL7jDJ/ZM1Dc8/nsrlGoS+X/Oe+XfI/rWX45pL/GXwd+V9+/uWP',
        'v8aF5hfBK7vpEcJqoTFCeY04I4yteD1DgeUM11afgV5ImE8YJicjjCGRDogo5kHtlJlB/WjjlcCjkQA8893XAVYqPfQ3n5WUAWE9GpHamoNVCoqca7kRlILm',
        'OEY2jhF+vNbtdyl2FM5Xm3tmsLe+rYxGbNfmwqHKpDGr3LXlI7Sj34zWSxQG6KeB9Qll8R+UOH+FednS83lhFttvv6umHEOQ5v0+N6sXpg10wt9YrnMdU+9P',
        'vnZH2/f7mwvexDsOKpXi8apiedrhAjJSsn2ecg6mRBl/CyJ/29d35Ku7z29x+yxOH46s+etqOed1Dm5hQ2jtajVVcHKBEwq2RvoAVL9+fL3mkz7meRBeWcpI',
        'wuykonD2G6yjWmvbtIzyVsd62JfYrYG1Spnk1KVH9m2eHaMvHxWaXqFT3LI2Rt9c8v+S/18DLvnvcT4p///yNw0SzSeiJA1GQmSGrMSXC7JKQU2F6mOkvwb4',
        'urzPWT9x3dfVp2Urk2dYiAbvjypKt/7MXcAfVzH4wB0iTp5r1h/C7utIIMULYLJ/ue9fjyD7ekf190I7S3e320KoxwYuE0ARp4+BuYTw7WmZz25c5yz4NfCw',
        'NGIFUzerQmQUI/C0tvdOmbtamx+PZxuTXkFX3Jof5lYtFGMOOIv5q60O+x2D1DQ7it7k93w+BRY9s254Kwfnhsdtj+yzJ2L54EU2i/tkx9JHa7tEAiVlHUIZ',
        'rjTcLyipLLg/HgwHOMofbfw2vznUGAjeWwaZ2Sl/IjpuKtxGVs7anq2LjoDyw9LmDXgpbXmf8giw6JmA3ffXHuXj12/PK+b9Tbz7UIrIulT5/FldXfa+zfiJ',
        '+WB7pWuUUWWvPss28XEmvb8R6p7KJf8v+f+lcMn/j8v/pqD3yDMRrfSciZEHqLr3MdhKv1/Fbpo6ShPWw2iwFcvcP1SFug1GtjPL2lCZW6RMMVCEiIhR7yrS',
        '5+zTW/kWQFZSih3ldtARqH5ca02H7IgAxkSYC9IO4d1d5z7s5V/lOjPG038T0wxmxOgtSmzVsmen0SfoBx7trO3aaOaufsws16m0S2/4RkbL2nJWgI3fw2KH',
        'dqFwI0qclU61UK9NwM7zLFKFlD/4bpamzO0xXhsbKCqcCsyUbFa83u5v7cplAIQMLLzP57PlxnaCcq0tYLGUyeVWR1/GI2h+5/i7PWu0oxWwIs8XmPixkg0P',
        'G4O9Zz0cX/CSK075WrCUcKYUY0x7V4BI42jLBIxPM2eBc1GJ5gwzqM/TdyHrfqaAszVeZZGeZvS36o6E70gQt01XonT7DSetoQOIbb2/P2SeJ+dnG7AQkTLc',
        'OGT94PHzJ1fct1Ednh+NlZdL/l/yP76XUOaS/1y3yNeX/80HfbyrHhO0lokR2nyBhR6DcseWZZUist0G+OWQM2V/vTeEdCnibhPrLGZbWWbabndcPAF3O12H',
        'mD0DkWdECIZ1ZqGUAv/BTTEKu/IzytLvCbUKMej+Hawu1Q2sSEYQ9l0NdZqw+pLdsdVj6bz8e2urPSVl2ARWceVxlFWrNCV0WZ5bGz5dIK8ZFgyvW9Di+PUD',
        'MxJ8rBwMa6/Z2sv5hh3lTc2HfJqmLVvLJLfb3dGeCdnFbSxgOffpF3Gx0eSytaxrlbe7zw/Pri0s2KPiE9NGQhHI+r4zQnKkfPjx7K3vXyfgNKcpC6aDwot3',
        'EWdlON5KVBxufizMqpr3H3Tug0b7C1akK2c3v54TuCN3gNHzLIDvIzzl8XjK/Y6A5y/hSbmCyhtmhlwOGE1Ga+Ml/63sJf+/DVzy/3X5X/788x9+ZUuUF7BR',
        'qPsk/NgxV7JaoTN7eUoRuJUfAccVH3duo9V3NKErDdh4BaNfe8JbqjTLWbbrzQg/TlzDaq3uuvDRwsoYQlMyThAkH9UfBydljPi4DWb6ntC6Fvir9vusBcx+',
        'c9T2XjDTeQWJ6cD8qLPAlNJ9Z8fZVsZyaLN/srSjXk2vZNdTm5JkdQAnP3cjOsnwy6RIJlVk9/3euozCIa5xCCHQCwJj4GrzfKqPLzNbu5SomKJURd4fj2YZ',
        'c0o5lYHCqP6qkwps1j3jMG3f4IQBfeBAMSyDemJczlnA+3EcQbv6OaER5lkjJX5dFllrldvtJmbRtsw4IvDdLGK8ssNCMuto5P8bhsLKIPrIYG347DB74zDP',
        'c7P6M+yP30Cg7vBCrIEsw8ZewOrIF30PPuI7/yqMxh6+4Zf89/265H//7Agu+f/15X/qgx4Z6tmdSPQp5AAu/ww+sOcDNwi75JtK/7I1pt/di+A4SX9bqjH0',
        'QdpRPAjR9Q/E2SjKmo+CW1+b+4CzWGxzDiUDhLZHHJ6wt++i9eeIT+3AHnMQ2W5eC77HL9ROSOG3Z7pMeH35WJc+1wwBHGjTM8EjYo3+oKY09MoCW60M9Pg0',
        '5pFGmff3h1Nobrd5I86n1GpBT4ZDVKS5vuzdCM4o77E9lB8r89ES4pU5P/5Yp16IU3rFqqkScUmIlrFjZQiW5+Mp01zk+YSPuZa93TSYEa4oTbEq6tsuIv5m',
        'wND9Zolyo1Eb7vM0N/rNrOkiJihVmPcp22yME8F9oJhzuVZTshHim1F9myhXyeosAmuV+Z4X8dbuSIOebi0Atr9F1JQ7y76yNx6s6GSBjtxPxf3r+KN/FM5c',
        '0nQGXvWHz3HpFZlMXrDvMsr2Ob79d5f8v+Q/t3/J/+8v/8tf/vbHX0eEeMSI2K/UfNO0c7lVhWHdhG886tpfVXosHtOknQfuDyJoARx1n92ImBFo80ElYma/',
        'VfWXVCthvPKW23FC5QRhlWKLs+9jfhte3KFmEN+NrF/nICp6eXsi0nb3Z+tpRauESxheV4J4vPgGT3xr7bFSsU/0sW31MasylXm7bOddZLOO2855H9zxZqIU',
        'j5na0XN+l+FxTkJH5Qpr3/JG10YHnGUEFlmtQ30VcVER6Or9/b3RyO12d4IU4xGVM/j1xXSB2o7d+mdXgNemRGi+5Nos9dM0ORrP6GQUGDrmhWs7ct+1mn0R',
        'bLc6punm1pYSsufDVmZEx5kV3/6N2TrW7hvuJ2ghkzm50qLyBhs2vm03G7NMcX8lC8YRPiN4RQkHjjghOOPznuHluxmvpD+nAOrzS/4zXPL/Vbjk/5fKfxck',
        '+mrHtAz7m6ER7Jhtt47nMfrZv+9ql9hx5Ot9lXkBmHGAcEQsJzMuGYmLlIPU8M4dZRVY73Sy0nRyKEr9ZwJVJaHftdXqg9Wsnl7Q7c1XHGe7mOYM7O1os3IR',
        'cgLrrV2vaiRHCuURZ1FrxjSXnSOtvF4EXSpTYyXZK7u11pZSa541rzeUwjxt23gMYNmB0K+1FyR7DHgfPq6cc78heDjTyjQVt7lA1cv6lHn2AaE4Ap+nuaVf',
        'NAZaPO1vghHv4LdeK6es6mmyFGk+nI0GxdxIzJfW/ma6j0r1nvKjlhWzqrd6SLBndbUu1nH9x8ABlrHeteUOvt0m8dbHsVK+B6aQsS9ytqHsvhRT8Er4jWfe',
        'xYIt6WeEcKSz+CyzQI/q/Rb52fONi+HyJfWiDt7MMh85g09e5pL/QPWS/1wuwiX/PyL/TyvofASp/+Z+g9HEbwTJHTBEfEqmKhAS8TrfPfCWqyPfT0sGr/1D',
        'ftnioqyRzg1dQTodpGBin9T0xC2Tb0zPW38b0UtxKZ1aPUn9mbA+8o3Ed9mO+HWFLnZu7+N+EHyQGXDvB9Mrn/sItrmc5i2l3GuEDotfdrzOwEIOykEfXOfx',
        'YiufZk6xi220zGgB4W8THCLmbxfTmVkkeR6Is9P7hqv1v2wXBilt9ME7PE6w1ugzVcw5B/HcbgbF+6lMApeQWk3gmsVp2hjb2i4sWpZFbvOtCUOsGfglsxDE',
        '9fYqBEHnRTcDWxd0bpbNL7t2S5Wz18BtBf01P3WzYmUWdIyLzVH8ZmdWAp3zJuLLALjXlrpvbEEffc/MqYR3plxHK2zPa/xasr6yRda/Mz/R111NzrrAQKgi',
        'kGuUWeIr6uetTvoFbL5aO6+4AF3y/5L/g5pELvn/XeT/UEFnXx7NnbstYgow8lH9/VXN3BmekK2F7V/Lm/v1wIg0I167MlyPnfS66sV2qpIHYDgfTrE0bDEd',
        'GxNo5q9mPmnbGA9ypEYoxfsNHo7C1idYKPe+99HG7Sl1aAyZ1WmvXLQiemLihR6ZfFejxIGb56lZMs5YHjPGFhlI36aCuZzkHca6ez4fDS/bOZ8RPtXdSqmK',
        '3dy6HRVxCBBWLEb1BkwbgzNrgGy4FvfMvrf5McZvVnNVRO2iHLXQMiP2As9qVgVdLbulBXiutcq6LHK7q4K9bGsb44gbRjltYBVeZ7JZqSzIcJ5mWdZFfdQT',
        'emVFGOXxHK45Iv3a53VnFl7v44h1d2yhtLX7SiaXY+VR3UNwuhH9hT1kwhhKuk+DaIqV0Htrc6yAcz3WJisUWGOPx9L8OLN+8lI1V4XzGgi7mrRsHi5Dw7eH',
        'b6X8n63zkv+X/L/k/+8v/1MF3S/oEQO0CY1M1N7HFUtNB8LZI9KzjCWzOOE3iEqxsZ07LFkcYRvH2xQWDZLIciO3NkqvcCCoAP3gC1dKkSGBZkew3D/DLS4w',
        'jxsHAu0t1JwQo2LGz0mBOXFcFsvg8o/XhJ5fSyws+r75ufEQGUF85uuIa1mtw7hUh4/eGbepMUm9ml7Xz/F14b4j8MnGt9i9QynlvunFQyX4XfdMz+3SSfnR',
        'dRqZVhwnEaxnMFDkdtXLh7Yjw4mZV+yT+X3zhgD/3e93pZVNQcIRIcrhwp3ou1drleW5bOOiiqeOvQaitltDi7RnPH69cu6FvYi0wFF9Bl7W3zBq7/wlSd0R',
        'f+SN4p9HpdwUDFaadE2xm49Xom2+UT7yhzYwhIlXsjNGFddToe8AHDTr/ZStae/D7N1sPE4ZD+MufA3F1q4NPyNIPw7fQhE/3/YgC8kl/0O9l/xHb/zzS/5/',
        'a/lffv7lT5uCHq0Z8VnG0A1pPo7IiM0LFUbEhJUNdk/QR7C/U8qPvTK/Mq6jFIro5tpWf/zVnlfzJ2PGp8rF7JhEwzseaw37xmON573y8wojU9z4Cuk6WMxW',
        'VkSFlh4vR0HcK3H7OBwfy41Bc+xORehIi9fNEaF9DErRI6ta1+a24Rlpzix9wGKm5LQW3HO7Yt4uNYDv5NiQwjSVMR3GL/fD9XjlwrgUXLoA4YWgl15xjsCp',
        'F2HZgVvKPN8cPUM5h8IL+rvd5ibcgMu6LG1zYH3SdFd88QjqwjXLvEFplrOqWRagfGe50dm63QLGaBzj7anmDqTtxX2Qyxoh8dZS84v/GopdFsjns28QYk6B',
        'RtkR3+SyUVagviJRvmCzwWBuDl5Z9Bu7jwVVjqHK58+Pllkh+mvnitM5d5ts3s7M5ddS5DE3fq5ELvnPuFqdl/z3ZUUu+f895X/581//8CuuIu6Zac+Ql2WV',
        '220iIoz+hTpJ/DeUDNSL427ewfQM4wzs+ZtlTMgWn+1o9ncz7phrQzcGleFlbQQ6uZ20O+bKoIwZcEYsIyI6u9a9NY9dD/rFxWX//vfPUtdVfvqHnw4VsD1C',
        'R7sIsvLl9hTK2hgKr7PYt16BqcRQXodoGXo8HttOGJZVS6Pl3URYwZ4kXqbA/fLPjS7wPW4iVL/w7FgtGzNjuHYdsetZc2Pxbee48JxCiXs+16aci/iLFkbQ',
        '+4tXx/B4HRrvwJrBbYSzc49BSkVcUpTpgxxstSxPKWXeXFUWyqZibkSscFuWgX6zwmTD7AtHoO0dZPqKz/r1mCm9vJb9WrS/X/VNzzPOgJ9C2GWK2gjfKJwz',
        '4DVlFvS2yZnG6ROPQJWp2d3IN1qH3gKZ802kPgtf4qu0/a+hQHN9nu56WevL4lnPB/bGwZTmS/5ncMl/+33Jf8Xve8r/8pe//fHX3iLgHeajwFABCSIzpo5d',
        'TAwKWde1XXkMRP0NYwAWEBnCsVN7iwTX+/btwKKl0bTaFwStoY/od7OckYVMd912HNsRcUQdv3dQPiZQFoCjccn9rvYCg3jnd7R+398f8nh/yD/8N//grIIe',
        'pxz8kbqtl2V5NoUT/nEj5ZMj3DFvvBM3gcHfl61NIQXL6gQee0Fg9u8ky/Js6bK8kCwNR54XuH54v7PzjMLGBLvwqR2rWf/O1JetHx4H/i1dOR4fdknBbaCq',
        'ME9JHR6ixXit2qd23Bs+hU873FMez2fL5KIBoMggoXR+JDjQBjY6UArNb351edNxPMkxTFD0o4I9pp9tDl9Uos/AnmIIvhR93TV7CzZ6vaXTnrFvcbbeer/j',
        'ERhfYmUwXojUK+1n+tmXO5INr4Ff+3t+vN4v+2ydZ+D8BqBSWtNzcMn/S/5f8v/HlP/lz3/9w/8RifooOj63mFjj/a4IkazqxI/0NP3Nc3xcEBffKww3pwQQ',
        'HCyKusMxX7RMKLQjbdRXKHtGIqS7CwxGelECf//7byJS5aeffnICAfD582O7Dv0mTIxHAWd8zA5ZC0UhKkY9Hft5/u23z/Lp06fBHIn0a0KEB8IW8scVlZbe',
        'yK1Z9MFwGu2m7X0h4mR8JdQ5Kqt1zdM0PGbzlyDsCRzGr2HQyoLQ4Vu+b6HIFp9/H+e8V2qyxVu3b6em5GkQUm41j8o4V883NZYinX9iXdUqzhZoZHi5zbdt',
        'vJl2k1Rk1KYL+CNmjFtM8Q5BPHzz3jZi/dByO9s7s6DYNy0jRJWOX4zG7IxFLXvP9Aw3IhxNA+wmR+6MVRyVdHvHKRt7a+45S1gVs3qy4i/CvqSZ8I9yZTw+',
        'x+4mr9ysmLUxYl/nFen8WxH//chPfA9041qbInHii0v+X/K/a893ROGS/99X/m8KugKOjkXkkMEp0jGwR8LvWIdOzrpqHsjMgnO8Mz4HzOww0GxRUmyKPBe9',
        'zWmeSzvmtjpQCS1oPCs+qhv18XEW78hZYEdAub///Te53WZ5u7+JlF5IgXnFxd0rW+i7Wf9q2yHDj9V2q7yQj+Dz54e8vd0D4/ccCAIic6nYFzijud/ncD7b',
        'CPvV+frQdhTy+xYGEVMcPB5eoBoTQjHNpjJ33+TdZ27ucalrbcpqO9pzCu2eFOjrG62XHCdfJ8+fZbZIvmy+4frbnwRYgCvq5PayOYG13OawuDq6S2dG+42k',
        'bCmlpRs0S7rmUedlgQ3AaDMA/3b0odE8t1tyy3umnL0KmbBWJWNqQY/aBmfiyDdiPU34Oco2fpmCk62zsVtBL+AYp5Fi37ebuYP4BXFWMR8J7xF4S9qgzuTy',
        'IuPBH7ulNPtO17nICPdI+5f8v+T/EVzy3/D4HvK//Jef//1/5UK15oERPeKeIfTBJjiuzJm8WgRrO/bq699K0oUhR8AZE2J5s5BF4bhuGSWQdcK+nWfc6uYj',
        'r2EJ4+cjYOYQL0DICHp0IYH+CwLYSldfhgnTyvkyPLdRKTgrtLLxjYuelSBjAKwIeFhXvS3yfn/byeGdfW9EERnaho3Ehe+PcPcFwig/qrlGxC+0vufzKb2v',
        'OBhs306zepXSpXOz69gti4kpmL5+G+dX4Jxy72lev3k8VMC9vXl/3ZHS6X1U0WavbK3rsqUHq83qs2yWXw40KyW4HET9LnQJyvVa1e8cY40cyOrLri41ZgE3',
        'H3U+8oaQhtAG/S5koWahXKV2tHcEmeUH34/YDge/iYjcbrdNKQJfyG88PYJCyo3hJALFa0TbKINxYn5p48hH0zFNHq8nn1FnjOsos4NCvN57xPv6zZz1R//L',
        'A2ezdu35mM++ulmLiigUOQBk22lL+iX/L/l/AJf8/37yv/znv/z7/7o3uGPEtSyCRoyI4hGlrwc7GHwrIuT0H8tbXb3i4Tue72oMotDib9dllSqWsxnH6NGw',
        '1MaFCWMqLrBpxJj1XyPuKLi73XVKpLboeOdnvrgYoyJdoFwC3i9sy0sdvntZ1xu35gR8tmM3H+u4ZiIuYWIER7qb8E4sVB72iVLbpPrOSIfwrVpUXxs8KA04',
        'SrNm1+0yHau/t2BsjKod++U78gTbU7gZY4n1ReU+Z6CKd7aezHqNNR4vL4G7i0gfhMq3jHbdStYX0i72fatO8ELYN8Fc+rzHIzpuOZQdOuMxeHF5HQIrDVMp',
        'W1abSd7fn1t7ZxR0RsorqMgqlKdfzOa/NFxEoKCbEq68GcGdZnkdKbt7mxNf7mPW6LOwh4c/Ev84EzX+vKdE8C+fuhCwZ02/5P8l/y/5H0r8IPK//Ouf//n/',
        '1NfGSPcsDyJ+YoEQUsBht+EXlD638rzg4q6oiseFn4nYFd7M/LwAyYhzz0er5W8udqNePIoupbTMD9kueAS8EOPRV9stQ/Ea7KDRr6zu7cPumz3BsK5VludD',
        '7m9vre7RZkPI240AAAdTSURBVNJbr4yBt6uRU6Wrw54Q5d/8jJ409w0wn3hc1q+NmI1kT3i+Yimwb8bMPwIC8LgtHsf8+3xsNNOAiDEuHsvAyD50mQor8vl8',
        'muUmU75G+OeD5IW/KudrtSNJrENsMmBpg2Id58TwywOkTCj7G0s7/bOKU6xZCYcFDF1rLjEBasAh8ob9+e8ho70zwL72tcqWAWh1G50zFvRMkT+PS9wg6r9m',
        'lQTD6eu3eIgjdwk/thmMj9Q9/byiSLPSvLcJsHfHCsGZNl9V9JuLQxVZoQzfRvN+yf9L/vf1XPIf3/w+8r/8p//8P/xftsM7l+aKfc/8DlfcTsYa9gJ0vCNi',
        'Yo0ZAiCwa9vp9nhZ3XwN+B5xol2+llyk33GzVYonSuV2HixSpcrj8WxXlUcCjeXPEqbD/rXNHX0oAqslzwcI1V11LP3CwlyfsexkOVM9kz5AtRGU16qiT16O',
        'i1/4bMEZledAluyIa4Bl++bcETH6Nn6vri2lq88szkFBJ8azb907zySwOfDXtfO805ySX3xcKxhvWBew9piZidjtoqVofQgGzQJRo6XPMdFVnOUb7dTqFVgc',
        'qwJPZ8kqoa8b3o03hDGspDSMmHE279mmYwSjdYO2+iN3tW6DJ1tALLsgqDKsOLNVPFfm0Fafs1yG32DwPP6sgBt/ODsG8YKSESsaKRFnNhmRT+RzOvar7d3C',
        'vkxRfw369nLXA9l+X/L/kv9yyf8fSf7/j//63//f/PDMhEeGBKa+bou8NJ83P6jsoB+Z5miHEpmIDZzI6FIDvchlatkYzgB80Wa65ITxiIKvCcSQ6YF3x1Xs',
        'atejI6wM/ALOd5pdPZHPCf0+0SaYWZ9GyQBM3i4uGFvj4pryxJYhL+55DOqw96N5jeNlRJNdSmHtGL4+R+sIx75dbw1G2f57VuC4bbR/lumO8dtnmlw/1ueo',
        'Dm8FlFB3hqiNfxTehjfa5kt8igs6hbIPH1CfWrIHDgyK46euLZTthRTuZVWrWMvoIb3rytYbgfsKLlhiWoNyLlI69xZ83z0bzns+h14hNoV3mqISosp19BG2',
        'i4lEvHuK9+v2Qjuj7ZUUPfzn+xJTSpZts7P9ElbItR8IrIw3jGabGR/sNdoE0RPU9ILyL7IueoJ1f7sFod7Xkyo67d3Xc7XZ4w8ZP4jP2K3klbp9uUv+X/L/',
        'kv+x3W8h/8t//E//7v8ZtJij0SplIW5Mq6q0Ehz/sR/ZQgn+ua6Ges0GLxI58DDB8FyeW6CHyG2+CfxXX91dPhf10bzf7q3N1t60KRAFgkyZA5gT78yyvvX9',
        'KeN1VriOXCBoo7K/Vgf17oFeoV6G1w+LKCOet4tzRkw9Z6iDNqu1G5Fdl5WeM/FFRTEfiHXZ1lx3GccI56MUUOO2lqeOyxiOJwCKzEfWbw+mxLUnFc/9s4zu',
        'bG4jIqxQ9d9l5Q0frVNv/DSrOPwG3Zoi2st5gF87OY1oBhwNCO0VSWfYifwo+jLS8ilFtpOMogp6EN7Hc21jwW4QJrw4SPJMQCevy7xtBLuO5ye2l9fJZaJS',
        '75V/Htx95S/ePnrG75OV5r3yhtN2622tMsdTKZJb7UbIwSZt9Ns2Icd07twcvjFEPNXNSdPDvQqX/L/k/yX/v5/8L//hP/53/6++GqdF6oGPt1BGmbGXdZNU',
        'WVtfHs+n3Lfb2XgCFYgIhRmXCavs2HVZFlnWVdZa5T5PW+YHEt6ySkmOw/S54s5l6yoyTdhFWXmOwl7rKnOZXVR2VBqmhNFnTLl71q/RODznYKB0dNAPf7r+',
        '2m7ya8iTpC9dYNKmANW6ysRXeJ/tF+rd8t1OvMsPdApF7Cjzw7gRkaUuUmSS+Za7N4ysfSN4xZI+hCqkYO5YDxO+9S0D7NbN8tIY7yYMat0U4k3hrZXmjXFk',
        'xYq+6aAqrer8z2Ma2hSL3jJYrI3U7DLASZLym2JYGpkbTwC/AN2N+Aa+G1njeUOlShmhVbfTkmDtKrJn7TkGsyxCotdWW4SReLW3vBkgl6qd9u2LTdYclOX2',
        'R/U+Hk+ZpzkoDOdgRaBdEDmv6FFfBImSm6kz66IKkT2/5P8l/+nZJf/PwzeU/+Wf/+kf1++wif8o/z9XL8AR/ZmRyFbnXtmMtb9Sx6i+HxFepIRvBqPd8ivf',
        'M8T5inV9dE5+5Ln8kSAbp2yOJSkX3x2N+dk1zHTN9H3URtTQs35lsFffHnw3Ne8FyPjhl+L4I/bzB4UvZdPfi21d8n+nvh8RLvn/GnybuSz/+i//dHHCCy64',
        '4IILLrjgggsu+EGg1P/9f7kU9AsuuOCCCy644IILLvhBoPyv/9N/eynoF1xwwQUXXHDBBRdc8INA+ed/+sdLQb/gggsuuOCCCy644IIfBC4f9AsuuOCCCy64',
        '4IILLviB4PJBv+CCCy644IILLrjggh8Iyv/2P18+6BdccMEFF1xwwQUXXPCjwOWDfsEFF1xwwQUXXHDBBT8QlH/+p3/8/35vJC644IILLrjgggsuuOAChf8f',
        'pfxy1MNpTT8AAAAASUVORK5CYII=',
    ].join(''),
    dialogRight: 'data:image/png;base64,' + [
        'iVBORw0KGgoAAAANSUhEUgAAAG4AAAC4CAYAAAACPNnJAAAACXBIWXMAAAsSAAALEgHS3X78AAAAG3RFWHRTb2Z0d2FyZQBDZWxzeXMgU3R1ZGlvIFRvb2zB',
        'p+F8AAAgAElEQVR4nOS9d5wlRdXH/a3ue+/kmd0JO5tzYAPLsnmXXQQRDKCgIKhkfBAlSxABQRQV5XFRRKJZECSIIgqCoIioBEFB0sDCzMICA5sn3tBd9f5R',
        'Xd3VffvOzPKIz/O+b30++9m5Havq1Dnnd1K1mD2lvYf/3TYAbAd2AJuAt4CNwd/rgReB1zu6ugf+13r4f7CJ2VPa1f92J4ZpCtgKPA/8E+gEHgSe+f8zMYV6',
        '+evvPOEcB4RAKRCOAAFSgRQOAyXJlgGPHUWfrVtKbNmc583uPrZs6eO1jZtZ/+IrbN60jUKhmHzqFuBh4BHgAeCxjq7u/Ds+lv8jTdzwqQX/EY4TIvwrOuYI',
        '3JxLrjpLrr6KmoYa2tuaaWsdTWvLKERTAzTWs9lzeLGzn+ef20Lny2/yxOPP80rX60livgXcCfwV+FVHV/fW/8S4/reamD/tnReVAqh1FQIt96SCageqXKhy',
        'oDYrqM0KGqpdGmtd2hqzjK7Pka1yyVZlqW2uY8yEVmbNmMTocWOgdTRbRBV/+8sbPP3U6zz04D9Y/+IGlAqHMgD8Jvj3i/8vitT/kzpOoIk6OqtorVaMrnGZ',
        '3JxjfEs19fU5cvXVNLY3MXfeDMZPnwQT2tmw2eEP963n4b8+y1/+/DilkmcetxW4Bbiho6v7L/9rg/o3N9H39JffOcIFuk1K2LS1H19JEALhury+qY/urYNs',
        '6SvwyoYdbN7Ux6a3eti8uYcd23spFktlj8s50FatmFAPk5qzzBpfS0NjDTWj6mifOpYli+Yipk3krXwdv79nPffc/QiP/O1J+xEPAT8Aru/o6vbfsXH/B5q4',
        '4YR3VseV6TahdVsm65KtyVFVX0VtYw3tbS2MaRtN8+gmSnW1bCoJnnhxC3/72yusf/FNOl96nZ6eflscknNhcr1iSpPD/Ak1jG2vo3Z0A21Tx7F06a4weyov',
        'vgx33vEPfvPrP/Haxm5zayfwPeC6jq7uLe/k+N+p9o7qOFu3QaDbAr2Wc6A+J6jJODTWOIyuc2lrzNFYnyWbc8nVVFHXUs/4yWOYMXUidePaeIss9zz0Kn96',
        'oINnnupk69YdsXdNblTMaXVYNKWGMe2N1I9pZubcGUxaugC/eTJ33fkiN/3sXh5/7Glz2xbgu8AV/28joJg9tV2+o29QFowcprlCE7WlStFaDaNqXaa05BjX',
        'Uk1tfRU1TbWMGt/MwvkzaZo6kVf8LD+74UkeevAZXlr/asiNrgNzWhQLx2fYfVYjdS2jGTVxHMtW7Q4zZ/HQn7dwy41/5J67/2xevQX4NvDfHV3dhX/7HLwD',
        'TSzYdfobConAiQ4KUGV8KAGH+LXmonLa6OsESuKCQqH0ZRIHhUApR5l/UrpI5SqpMijlJJ9V7cKEWsXYesHMMTmmjK2jprGGhrYmZs2dxtR5M9nRNJof/eSf',
        '3HXnY2zoej28t7UWlkxyWDmrjjHjmmkcN44lSxfiLpzPP54Y5Effu4t77n7QXN4JfLGjq/v6tzed/7kmFi6a+drO3OD5Hq7jIEQl4ilAoJSPEK4+oiRKKYQQ',
        'Fo0TxFagpCak9P0sUmaULzPKlzkllWtf2piD6U2Kqc0ZFkypo3F0PXWtTczYZRrTFs+jq1TH1d/5E/fd+yh9fdoSyLqwdLJgzS41TJ/SSsPYCey+ZBGZXRfy',
        '6MM7uPbK23noz4+aVzwEXNDR1f3AzszNf7KJ3RbP2pg86AiheUTFiaKUj5LguG7yFn2fI5CyXGXahBME3CxE2TXBm0Rk8SmUBCVVRnp+Tnl+TnqyChURsiYD',
        's5sVu4zJsHBaA/UtTTRNGMOypQvILJjNTT9/gR9873e8+sob4buWTxXss6CWmdPaaRg3ld1WLoHpu3HP3S9xxbd/yosdL5lLrwI+39HV3TuSyfxPNrFoyexX',
        'zQ+lpOaKGDfI4LfSLisBUCbNwuY4At/3LY7Uz5VSBdaBExDRARUsDqFlaLBOIvhp3q8coYLzSkqQIidLpSrp+dXKl1nznqYqWNAOS6ZWM33yKOraWpk5bxbj',
        'Vy7i4acG+cYlt/Hs0+vDfu0x2+GDyxuYMGkCDeNmMXftChi9gKuu+A3fuex7ZuF2Aad2dHXfufPT+841sWTZ3FdkuUIL9Vy6vjPNEEQzgJQ+juPiCIF5ZrQY',
        'FBHBZfC3IZYIjgkRLRRsjhcorSellDjCFUr7YJAlmZUlv1qWvBolVcb0bJcxsGxahsVzRlPfNoYJM2cwY48lPPtKhgvO/QlP/+sFAKoysP/iLB9a3UJdyxTa',
        'pi5k3NrlvNiR4+tfuYaH/vw388jvA6f8X/GHimUr5m2w1ZNwBEqqQFTqw+kiUAaE1cTwvBLSV1RV58L7jPhTUuG4DkYfRpwLSimhrwFE/JzWlcpciSGePqGB',
        'EioQrUril7xqWdREJKD+pNGwZlaGPRY0U9c6lvZpM5mx5zIee7rEBed+j86XtcAZ3yw4dE0NK3cbT23rbOYsXwPjlnDj9X/i619ZR6FQAOgAPvl/wQMjlq6Y',
        'uwEIuSTisDhh4k2LOCm1NvKlj5RQVZUBnPAZmnBaTwlHBAQRgagMnqRESBD9Lg1u9DmE5jphvRcgILZhV6VxqyaiQvrKlYVirSx5tQbYTBgl2Gd+lrULW6hr',
        'ncD4mfMYt3Ypt936Mpde8kN2bNdhyWWzMhz7vhYmTplJ/djdmbBmBZ0v13LheZfy6MOPmAk4o6Or+1v/LiK8nSaWrZzXlXZCizgbOdqTp8VfPl8EBZlsBtd1',
        'ApEIIJDSj4nZuO4U1jtiaDSGWJSUFkENIcOzMSLq94ngvEJpkwO/UKrzC16tkloXTmwWfHBxFat2HUNt21R2WbiU7IKlfPG8W7j5pjtQSlFbJfiv9zez38pJ',
        'VDfPZtLStdC8hMvX3cBVV1xlOnAzcGRHV3e5b+4/0GKEcwKu8H2tPyCpm9KaTVTTQqARvagM9ETXJc/ZxLFPxJ+pQAmhdanUnIu0uE8JFSOgV+sXSvVGD86f',
        '5PDh1XUs2GUSdWN2YfaqVby8rYUTP/VlOl/SU7J8bh2nHzaVtgmzaJu8mJo5K3nowVc587Sz2L5tG+jA7qEdXd0vDjFB70gTy1fN7wQtKkM6ocVfOsKEdEJG',
        'poPv+7iuq9ElAWHSuFdJFErExXH0nIjrlUU0GYhJGzwFBAqPy1CUavNCn1dS4RdK9X6hVI/Sg9h39yyf2LeF5vaZtExaQtvyFVy+7g9c/d0foJSipSnLZz82',
        'nVVLZ1M1ejZtS1fz+mvNnH7SmTz5zydAp10c1NHV/aedmPf/cRMrVs/vVCrObWbCyhFlGnfFmwEkAFIqXFdgCB09z9iI+nnRAgmOB4eTIto2SwKiCftYcL8I',
        'VZ/yQ2KG+k8qoXzflQWvwS96tQBjRrkcu98o9lg8ieqWXZiyYi2dm1s59ojTeeN1bf8d8b5JHH/IrmQbp9KycCVUL+Ws07/Anb+63Qz3iI6u7p/t5Py/7RZy',
        'XJJImpAC36/sykzeIwRIKQNbTaKkCkBJHHSA1kkGxQqRthhszgttOuvCEMBY54SFaJWwiRkcQ0rf2InCL/pVfr7QoHyZA9hzYQMnHjyF5rEzGTttKVWzl3P6',
        'yd/m7t/cBcCK+S18+aTljGqbzqgZi6F5BZev+wFXXRHilLM6urrXVZywf2MLCacHFngvAtEVeVAq3Bwiw/JzAViogEoDwkFoA8abioz0hEvNIF4DSmyxrVRo',
        'A1po1EdKERDOt7hRCRRCKh8/79X7+WIjQPvoHKd9dCorFs+iqnk2bctXc/ttL3L+ORcgfZ9J7XVcevoezJo9m/ox8xCTVnHj9ffwpQs+b7rxpY6u7ovSZ+zf',
        '18SqNQs6lSIwpCOTKdR31hoPdEmMs6D891DOZwDf9wMvik002wyohGjtawNTAlXW6eA4WAQs48CAgjLQj8r3s6X+fJPhvk/sN5FPHbyAbONUWndbRWd3Cx87',
        '+Ei2b9tGLuuy7uw9WbNyV9yGmVRNW81v7/w7Z5zyadPByzq6us+sOOv/hhZwnPZkGA6SUuI4TmSAB8AlaYTbExyfbOMZKW9S+iHx07mt8r3xFvXFJlRcxCri',
        'BDQi1JzTxw33KRTeQKHRL5TqAZbPa+HCTy2mbfwMRk3dndLoxXzkgCN5oeN5AL588hoOet8ynPrpZKfuwR9+/y9OOuFYpJTwDhNPLF81t7N8oqJVbuuxNA9K',
        'nGBDN+OkpiLR4u8e+pzN1WXcFrcHQ/Ea2YFShnovcKdFRPQLpWpvsDgKpZz25houOXUFC3edS6ZpNjUz9+D4Y8/lwT/eD8BZx63k6EP3xKmbQmbSav7+6Csc',
        '+bEPG+K9Y2JTrFy9oDPJTXHDuRyADOXDrHRcKYnva4PbTYkuJPVrcJThUGyl62zjPO2YEZtSIgJ7UgQeHeH7Skjfd73+fLNxYl986mo+sM9i3Ibp1ExfxQXn',
        'XsktN90AwKlHrOAzR+6DqJ2MmLSSP97/Lz79ySPNa8/s6Oq+bASD2KkmVq1Z0CllZQBS8cYYgYaeYBPWQYFjmQfJazTRbFFpd2okBLTvUUQxWSUiGzQUocKg',
        'VU08c0yhpLH9lCj1DY6WJb8G4JTDF/Nfh63FqZ9CbvpKLv3a9fzguqsBOPbgJZzzmQOgdjJi/Ap+e+dDts77eEdX989HOIARNbFyjwWdGuGNTNztbCv3Vw5H',
        'NNBEStpv5u+0ZhO1EveBJowxV8LnCW0XWuBFShGJTim8gWKDXyg1ABxz0ELO+tS+OHWTcacs57qr7mTdpV8D4LSj9+DEo/eD2skwdgU3Xv9rG23u2dHV/Wf+',
        'TS00wMGEZRy0URx6IUasw9Kagf2Qrtfsd5pmR8+DIyR1mTWEEfbE6D/fuiG0+0Ql0amUFEoh/HypzhssNAEc+O45fP1zByFqJyImL+e6q+4Iiff5T+3FsR97',
        'N9ROgrHLuXzd942d1w/M6+jqfmWEHR6yxey4spMjJl7aKtcGuH4QgakRf8Zwtl6yHyNpxgVWiaA295m+h37OQLxqI10ERJMoJYRSUvgFv8YbyI8G2H+vOXzz',
        '/I8iaiYgJi7jm1//Md+75rsAXHLm+/jI/qsD4i3jjFO+wG/v/BXoyqM5HV3dO6mYUsa5fNX8TmO/aYBi3FDRQMsBSXxi0lxjUkaOZjem18y9I4X9UWA1fn/S',
        'VhyK88rtyiR4qSQ6NfFCV5qQJa+q1DfYAnDAXnNYd8FhUDMBxi/jwvO+xc036jyjH3z1w+yxcjeonYIYt4wD338Yzz/3DMCtHV3dh45g4EO2BOGSk2RdmECa',
        'dlOKWNTb2GpgXGdpfs9kK/eWCGHbYraNmNpDIqKmG+w7gzyHIp5f9Kq8fk28A/eZx6XnfVRz17iVnHDc6Tzwh/vIZhzuvOYIJk2dSWbcXLYVJrPP2n3o7+8D',
        'OL2jq/vyoWZjuFbBjitvpVKRTCZTMbBquFRKGUTQSfGORC1pE2pxDMm+6AVh9KST4Hr7/bG7EsfTbL7odyQq47+NYS+lFEpJoSQojUZjxDvu4CWcc9IHoWYS',
        'jFvO/vsdwvoXOhjbWscdVx9BXct0stOX8PBfX+foTxxlOrmko6v7idTJGUETy1fN7xQiWt3lcTT9f7FYQghBJuNQidBGr2miRc7liJPKW1pyUdpz7RCTzd2J',
        'KxlaZFa+PrAjjQM6xnkGtGhfZ+AiUwi/UAp13mePWc0JR7wHaiejxu7Ost32oq+3l+W7juWarxyCWz+d6hmLuHzdLVx1xTUAbwCT3m4Nw4jACYDn6eqXTCZT',
        'BlbMddL3wzi2RpBJoqXpNRVGB8ppke7B+Z+3JIG1ioiLTvPb5jyEEZughJRK+PlinTdYbAL477P35QP7LEfUT+FNZwLv3uODKCU57sC5nHTku3AbZ1AzexGH',
        'HHg6/3ryGYAfdXR1H/d2RhAzB2InhgAnyXMaiAR5kxZnDOUsHonOS+lV6nO1tDD6eWc8Lcr62148RmxSgfNsnaeEN1AI7bxb1n2EXebuQtWYudz18Jt89uSz',
        'APjWmStYtWIh9e27sq16PHutPtSEzN7f0dX9uxF0Oj4TRlRGnbROpkyuUj6+rwJ9ZwCNud7BdR1KpVLMiVzJ1zkyMwPervhLH1dyQdjAx1yvgpfqZ+nfNudp',
        'glnARZT6CqNlyasZ1VDFr688mMYxs6nZZQlnnHIlv73zd9TVZPjFN/ehqX0uzfMXc9Ptj3PR+ZeDrlsY39HVXVYrPVQTK1bb8bg4YhQkM78MMY3I0wOT0ieb',
        'zeD7OjRULJbIZDJUajbBRi4C4/rof+IUSOuHbpEoLzcXIlMBpYQMiCYAqaSQUoliz0Cb8mV26bxWrv7iAWSaZlK7yyL2WHY0mzdtYcW8Ji45bTVVLfNoXbCQ',
        'w4+6lL8/+gzA1R1d3SfuTN9jHAcRsezYnDLwHIHnexYnJZFgXIcZx7HrujvJaeGVpHtKFE5wf+Vk3uheYTYOiC2SeF+T6sAJ7kEoKyQEoakglQgWtZC+TlLy',
        'pXQLO/raUYgjPzCVzxy+iuyoOeRbJ7F2xScBOPNjE9jvXQuoHbM73rhx7L36NNOFpR1d3Y+PYFJ0fyvpuErN80o4wgnrB2w/o1LgOi5SqdCVFeq9YAFE3Po/',
        'b0Nxazz1L82uS/pCyxeHLSYjTtTARKnIOa2UEkoqgUCUBovV3kC+GeBbpy1g6eL5jJqwmB/f+yzrLv0J2YzgB2dPZ8zURUybu4yr7niMy9f9AuCpjq7u3UY8',
        '9uWr5ndWQonDeUyigesBaZ+j8U/GXVlKxYOz9ruSLckdaQR6+yiz0hhssBK3A5ORdvNbKfB9XwghAlsvBCtNfqFU11jrctPFy6hvX8DYBUvZ98CLeGVDN4tn',
        'ZLjg2FnUjVvKlPkL2OvDX+eNN7aBztO8YSSjiOk43cn4ZCYnRwMMO78/LY4WPcNxBIVCCUcIbUoEosuIzjSbLElMYU90CDi0zRlLt4Dw+SFhbZqIqL4hHXgp',
        'wsSx4JkqJB6AEgKdh4MCqQmFlAghlFBSIZUUSES+p79N+TK7x4I6zjtuN2rH7EZu/FTW7HMOSsFZH65j1dJ5tExdwnNv5vnMGT8G2AyM6+jq9himxQxwu6Om',
        's7EIeJA8FOWXxMWeQY2OEEFephNmczmuG95vE27kLXrX24/Ea+q9PW4NUwoFhGGhAFn6QuAilS+k1GLT9zy31DvYDnDux0exatl8miev4Lo7/sFPr/8jTbWC',
        'y08YR9OEBcycu4CjPn8b//jXK6Dr8r4yXG9Cjgv1gTUghV6BcWJGyn64VimTywneJWMsEZ+k8nfImOhMgiCv5JHJVnLJ6fuH161pfbHPxcWmIaTvSQyrKimF',
        'NCJzsFDv50uNDTWCK08Zx6gJi5g9fwmLP3QpAwMFPrR7loPfPZ1RU+bhuXUcfNINAINA63B7s4QcFyKpmGhIX906d8gWceUFIkpFei4pDh0nTNaJEce8K61O',
        'T8fozO9KwVhhOaYjeRd3Xtu6DDQAMdwaiX+jj+O2YKjPLTNBmwV6jhwhpRcAFiGU8kVxx2Cbkiq79/wMx39oOo2TFrFhG5x0/i24Dnz1sFGMnzmLqXPmcuEV',
        'D/HAIy/DCHJVxIrV8zuNDrE5ayTNnnR7sGlAJOl1sRdECGxCPZTWD80xpVKRbDYXO5be9DmdCpgW89PnK/k9k+DIXqhxGy+oXPKkEK4TgBQ0UJG+8EsqV+ob',
        'aAX44kcamDN/DpPmLOTjZ9/Oxje2s3yKw9H7jqdl+iycqiYOPfNXoHdGau/o6u6rMLgoWaisqUhURofi+i99sENPiGlJTk6/PiqcNBPpeV5o3Kcl7NoTbiph',
        '48lJScSSTC+0kaX+O0p1iN4RSAYhhKknlALcIHtMBSJTE7HUlx8lS17t9FbB5z48ltHTdsGta+WQ024F4HP71TBj7hQmzZ7OV7//JH/5x2ugS5i/UWn+xMo9',
        'FmhRKc2w4iIs6TaqlPVlJqMSSkzeH4pnA2gCdOj7fvC+CDUmzQo701oqGeSBGuLogUip6/fKneLGPpPh2Ex1bPQ/aDEfiV0jRUwfbCd0ND8ijB5I6QXiEyE9',
        '3yn2DI4FxHGrcqxcOpVxs2bzucsf5rmXNrPrGDhqrzG0zZrCgKzlxEv+BHpTuQmVEKZYvmp+p+tG+fumo77vWW6pKOSTnHgd3QbP0yjScYSBytZEJp2/5SLO',
        '+D3jq96+zxSiiBDSR6LW6D9THesBCtd1cN1MWd/jzejUiCuNiaPFvQwWkKldj5zPSkoL1ZkUB9AuscgwlyFQKTaOroHzPjia1plTqR3dzmFna//yqaurmTl/',
        'IhNmTuLcq/7BCxu2Axze0dV9YyrhTAq67qhxDWkxYvIgQ45MOhiAoAa/rBnwYZzQxotiwIdSimw2QxCcDLOnYzZesAj0iwhNDYUK+5psUkoKhQLZbBbXdWNc',
        'bROLIH0iHnYSRLAfArsMw32mT8Y7ZAhIPFcz8KqoeBRBKlHY0d+OUu5H5rnsuWw8Y2dP5QvXPMX6V3ewqA0OXj2G9tkT2LBV8NUfPAHwj46u7sXDEi4qsh/C',
        'zhkKMaddY57tWMQnbuyGZoPZF8WyFW0uMclF9sQlm+fpAlEtIlUAfJLuuYirYpORMDOCtxKPvBP01QYrWiQadB0gSkySkeE6P1+s9QaLo5pycMbejYyZPYlS',
        'VSOnfvNvOAJOX17NpLkTGDt1LCde9ne29RSgQqQ85mTWzmCt0NN0m5loeyM02ymdjKAn68rTzQAZir3KtQiExZJQ7v3wvJLmLqnwfR9fSqqrc5SL6Oidtvli',
        'EzGyUxUoYrXr8WdFq9PoOsuxLfR4NOGk76FQAuWIYk9/u5LK3X+aw5qlY2mfNYHPX/00b20bZI92wX7LxzBmZjv3P9nDbX/oAvh+R1f38WWEM05mQ5ikSWBQ',
        'pSFApLDj1yddZXHHsj5nxJtNvCRwST7LRoj2RCulQlEopR8sNn2TjSKTnp8y37JFB7NOzPVpKRWhi80eZ+C/9H1fuG64gIV+hhSe5wnpSyEcR3iDxTpvsNA0',
        'Ogcnrm6gfc4EXt4u+PbNz1GXgZOX1dO+y1hUTR1nXvUkQB/aII/tMSZW7rGg0wzCXmkBZVLFXniOaIWmnbMnJXZOVTYtTEhGozi3DBDZetJ1XPxAzPq+DM2E',
        'TMaJcX+lxWGPzYhuI/7MdeWeI/1/lBMacaI2zBOuMYnw/GLkXVGIYs/gWJRyPjzdZenu7bTPHMtJ33qSQtHnwCkuSxe10zK1hct/uYGOV3oADuvo6r7FnlJN',
        'ODO5IvF/ohkgknTGGvASxu0MIWMcSLC9RtzfqZTEdV1LHOkbPc9DCMhkssGkyNiEG1+oyYVxXQffl7iuE/N6GIdAGedUtDNV4n+B4zhljnUlFQgCE0EFflkC',
        'ghniCaPf0CIz4L6C1yCLXsOEGsWRy0Yxdu5Yfv3Ydv7wxJtMrlEcvryZ9jljeGpjiR/e9TLAHR1d3QfFaGHAieE2G6CUUzLOShp16TrvJDIzf6cDAoM4LQ+/',
        'xYXG0HYcUbZTUez9Su+vYsSXbZcl44Rp0YtyZ7cxhfxAtGuRm+b203pW4IgIKGnnum3X6WiC5+v0Pil9ioWScIVwS335sQBH75Jll13H4jY38Llrn0UAn15Y',
        'zZR57WRG1fG5a59DKlUAxnR0dYffiIiJymhCIo6y9Zkt5pSCfD6P6zrkcrnYvZVgp3EuR96OwMtSYfO2+CRFyJDgfmOuaBEp8X3bSxJs06j8xKKKi16dHqjZ',
        '2BEOJtgbq9ANUWVCvlrPjMavCWa28FDG/aUib4qUUnj9hdGy5Nfs1gQHLG2hbWYbX725k03bC6wdI9h7cRvNU0Zz3e9e59kNvZDYHEC7vOw+JOdPWIQ0nUPp',
        'reMVVFXnQvFnwEGq6CSywwzQiQaa3uJGubleUippbtTgJJpgw1npIZ70BZX0/ITmdOK9wy0u+z0GmBi0qbO5tAPa9zXh8GWV119oqXHgxKV1tM8ew982FPjl',
        'n1+nvRqOXTWaMTPbeKxzgJvu2wh6M/Cw6E6sXL2g0570JGK0lbPNdYVikVwui92SiNNGoumDjiZrJMFVM8Ge5wV60TYvTADV9rxA3J2V1sq5Pu4kt/oYgpf0',
        '5+ln+BbX6ci4iRz4noeUUghHCJQUxZ7BMUqqzAGTXJYtGkPt2EY+970OFHDC7nXMmDeGfCbLhT95AfRu7mPNTkZi1ZpdO43oMZwVOpcN0rSqbuzJSqFB9DfE',
        'CJ782xaVldIa4hMSiTADZgzw8H2JI0TFfTSThCqVPLLZDHFdqBeH7/kIR5T1Z+iMa7NQ9PO0OpFCiMBzIxCe7wU5KkRusIF8g18oNcxqgI+taKF1RiuX3LqB',
        '7q153j0hw95L22gaN4pLbu3kjS15gD06urr/Chgns7FFLAKEJkLQtUr1ICnQ3mx2E2IRS1fGV7Y+bXv8h2rp+2maiTPXKEtcWhJAEE6sNh0M8rTGjs255j1J',
        'Eat/l/fFHNeACaWE4zpmJ0IBIKUwbjCBUsIr+dlS70Bb1oFTVzQwdlYb9z/Xy+8e28S0RsExe7YxelILv3pkM394YhPAhR1d3RcDuujDeCHsyTQtzTEbs/eS',
        'OjDFTisnQOrRcBIq6RPXNUBi6P5VblE+aDK4aqdY2Nwcd69VAl76udL3AxNBoJSD55WE7reLUgjp+9H2VLr2ThR78m3Kl9kPzsiyavFY/PpaLvhRBxkHztmn',
        'mfbpY3j2jQLX/LoT4N6Oru73QqDjpMUNlVo4QUk7L4FGo7/LvR9DPdu+L9kMIdOetzPviEI0puI1CgFpokUoMqoSijgv4l6DMiMxpMFM5B4LXIeBJ0Uf1IlF',
        'MvRf+r4UXr7UIAulhnktDh/fs53RE1s45/vP0z/ocfjudSxZNBY/l+Nz1z6Hgm3oAGspNMDN9k9pxItQoQwNXeO+CmGzIuZ1sHWZqLAe4sSNFL4N1837hw7N',
        'jLylIU7pl+u1SqDKFpNm4d0AACAASURBVJFRhpttA0diVPqxTeZEoKMDm05ps6Do5+RAobUhC5/br43RU1q59u7X+FdnD3tMyfHhPcZS19LAV3/+Ehs35QFW',
        'dHR1P2oRLjEhKRxl673Y9hpJW4Yo7JKWW2K3pDvK6MhgdYar20xaeY1cZSlR3sr1lX4HVNrlKP13Za+Mnd0doUswtQYmQi6lFL7niVLJEypfakcq979WNLBg',
        'QTuPbshz430bmTzK5dT9x1HfNpqf3vcaf316K8BnO7q6vx25vIiLnRBchMcsN5dCfwcOFdprti8xHCvlum44AlZa6eGzA862UwWTnJJ8RzL307jZtFttuJ0g',
        'wPa+JE2VaBeH6B3xYyK06UJgAkjfZINJUeorjJIlr3afmTn2Xz0OWVPLWdc+R86FL310Am0TW7n/H1u46f6NENQZiFVrdu2MQ/wUXF+m1KLQh/ZWROW/qZaC',
        'shzRCSN/uDwW05K6bGdEZpp41PEySIvVRfVy+gVROEnFgFPUBxkWvMSRpgwWv9lTxYR7dEmykr42C/KlWn+wOGp2m8sJ7x9Pw5hmTv/u0wzkfU55bwu7LhjP',
        '+jcKXHrjCwAPdXR1rw3suPJJsvVJOaxOTtrQCNIMOMnFyduTiT9pLU20DgVQpPTD8I8BI9H9Q+dZyiD/xXEdHOFExTCWPjerMc1poCMILoTmgB/8HxBO+UL6',
        'SkhfZkq9g2NqsoKvfmICje2tXPKzF3nptX4+sqyR962ZSF5lOf3yp5BKbQXGhjkndosDikgk2lA/LaUvGQKJ7UOpiEUXkq3sXuu3TSj7tzmWRjCDIu2aBi0h',
        'JLYDeTiIH0tOSgkWm/PxfpSpB0vPmfq6SHT6vidKPfl2lHLP/FAb8+eO5/r7XuePj29i9exajv3AVHJ19Zx91ZO8ta0AMDvkOANjDezN5wtUV1fFDNEIEBDq',
        'tuQmoUmOSJnSyAaknPCpLUSXlkli3ZtEruUTa9CelaQbsxXTiFe+GkzwFqKdJWyi2UZ/YtdbQziU0iXIgckgfM/XjufB4mhZ8msOWz2K/faYwuMv9XPV7euZ',
        '3p7j/CNmU1XfxNevf4anX94O8P4YqjSzJKV2C1VV5WLcYRMl2ra3fLKGbSqdYMKJx+yS523VmwZ6KovMofaSNufNw40SLtfrGgiJsN7PIOC46ITkQogIBxBx',
        'nZS+rq0TQniDxXo/X2zca0E9R+8/i95SllMve5T6aofvnrqA6qZmvn/HC9z36OsAJ6cHUhOTlFzxtlvIBBRj8yji+lFKWUaM8NJKvs9w0MMb6MMBleErYM0g',
        '7YpUeyPv+OIdScJvcgxEdeSWuNSuL18GWy0O5JvnTqzmvCPnkqtv4ogvPkjJk3zr5AVMmjye2+57iZ/f+xLAJTFzAOKJOOFLjejTdmQQaY7baXYTiMBc0JOy',
        'M5U5+l5LFNsrNyEaRzZv0iJ+vNonzRazdVb4hFi+ZyAyHRdEmNE8BNDR+Sik2HNGx+nqHj9T6h0Y01TrcvUZi8g1tHD8V/7Ett4C5x85h6W7TeW+RzZy5a3P',
        'APxQrFqza6c9+XY2VfBeM6PlqsCITaViAzXZYsYFJH01pO9SD2hkKHEkBNMOgvLIe1rijx3KMcSxS4+NgR73RUb+zPh2Wun9CxwWQpsNUgRpDrG8S+krp9jT',
        '345SzlVn7cb4CRM445sP8fJrPXzmoGm8d885/P2Zt/iKzrf8dZRXmRSRKZOR7gJKhIJS2tspJrFFUdIxYP5O9m+oY8Y/aR9L28PMXp3lBDcJtPF0ispBVvNc',
        'B+3mMuBFCCl9C1nqUE+xd6BV+TJ30fHzWLzrDC665hGeePYtDn/vZD72gQV0bNjBWZf9BeCxeOqCzVE25K6QrWwGneqcTntWUu+L6BmhX9JaBDGPjGV+2BF1',
        'PbHBa2J9TN9IIJktbZ8v+/4BcaKVmQcB8SqLShOFAN+TwqiPwCywjHEVFIfoTU1PPnQW73vXXK742ZPc89cNHLBmPCd9YjmvvtnPp770e4COeOqCmVzzZ2IV',
        'xwxolXKtnRws4seksvNG4u4z+x7bTkrqolg3R6zjgikMcjGTRZZxpBz10fd9ogy0oYtODFeZ33Zau8n5dByhnUcq3JFdb6kopdDfllKi2FdoksVS3VH7T+MT',
        'H1zETXe9wE9+/QzvWjKO8z69Jz19JT762dsBusvAyVD6RZ8w/1m11haaLiOgea5jKmhkaEclM6LNdQb0OI6IVRHZXGgXYQ7ZX2vCo8+hxVMR0h3GMvwIfDZr',
        'UjTiWWtpi8eukQ+j844IPShlyNKuYB0o1PuFUuNBe0/l5CNWcdeDnVz2k4dZNn8MXz9zXxAO+xx7PcBgapbXiFtMnJo6sXRd57gW96QUkKTtrVLJhLAN/Ipm',
        'BuWENeGbKMId19smgyweKdCd9Twvtk0IxOvzKjm3k5EC/b8vlBKYjd2MaeDnS7XeYGHUXksncuHJ7+GRp17n3MvuZe70Zq6+6EAk8O6jfgTQW8Zx5bNEuvgs',
        'O25xUMpCSJ4PdZWKg5dKhIDI95fMFIvnsSTfayZUmwUmZyWNY6OadUFU2KhXWLFYwnFEmKBrWvl398wE2JOQ3NDNJpxVFBLYcst3Hcc3zzmAV7t7OfysnzOh',
        'vZGfX34EAGs/dhVAb6xap6xZ77ZrxYZtyWssfWcnItlp3yER7CiC1SJ7CeIgYSTiQl9jvnvgZlyS6Xf6Hfb+0cLi7MiGCweE5iid2lB5g9bwuQqE4yREZZxw',
        'sliqKvXnW3adNYZrLv4ona9v44gzbmTqhNHc9J1PAopVB68D6I2XEqfZacKkWZd/zC8cRkLPhceSBEyO3UKYtnFdSX+lBS2NAzleo60n1CwS+3h5aXGk16MN',
        '5XTHdnYf6uAp2JxmjklfpRDOIEodDZeeny31DrZNHjeKW7/7SV5+dQsfP/3HTJ/Uws1XHA8olh30DTCiMl5WFM1qTJdYwcJUrrNhfjgjlInCtJbmi0xcUG5S',
        'oOsHDIFiXTEmRArsN1tV2chP35PGzbozkWkQrbb4sfSVH/fIQKTjZLillEkakhIhS1621DfYNnViM7dd/Rm6Xt3MISdey9SJzdx6zQkIBEsP+BqUES7oSJTj',
        'EdlJttd7RC6sJPBIhAps8JGWfGtaKLKCBWC8Faaix85TiSYszvFJPZcaLRCV9WTEqXY5sb2S4ga7+Xas4xoPjBPkZZrPqCkhpbbnpJTC9z2BQkhPZUt9A23T',
        'J7Vy27Un0T+QZ+0h36CutoqHbvs8CMHu778oIlw08LicTIO6oZ21s0g0hWNiLeW4rQ/N+8rtunhoKSJaMttYEy36TIwGILabzRC3XIca88QJ0SkiWuQG+hs7',
        '0S6UFEJX2grHDdP1TLTA942/0hdKIvySny31DbZNm9TCL79/KoP5EqsO/Ao11VkevvMiAHbb93woJ5w9+HJxGU7WcESwiWHfaxM7jUgVIgU2tyeLUKT0w4qZ',
        'NLdW9P74p19sf6QesykDVmWoMplXop9piAvl4jUSvaWSR3V1Nb4vQ4IFnEZYahwQTvrSKfYOtDfV14iHbv8C/QNFVh74Jepqqnj4zq8Cil3fczZAr1i+al6n',
        'FgHRILQTVMZWYyQOVPh72FR0yhdE8tpkDC4ZDbDr8JJ1eaj47g6OZX+FdQsppoKdaBSNydh1kWO6Ulq8CBCxIap9n7k30n3RrhM24QLTRIAUoSHuK6fYO9Be',
        'ncuIv9/9NfoHCqw44AvU1VbxyF2XAIoFe50J0Kt3FrJWtJnMGEq0iBERM0WsGu60MsCi0ZZPgHV7TLc5jhPWK0gLOZpL0vRh0gsSn3wrFz6YTDsN3uylUm5Q',
        'p8NsmwNNOrvh0PI9VyKCSumL4L5YjbgJ7WiO62+vq6kSj979DfoHCix//znU1Vbx6N3/DSjmv+s0TbjwgxFJMZbsrzX5dmVPOTfGi0Riog3rORXErZ2TmXy3',
        'eZaUMoYm44ZvOqozO7QbUVhup6WZMNHz0go0oyQorSvT9uk0C0AphY52gy7XEUilhO/7qYR7/N519PfnWfLes6irreLxe9eBUuyy9hQwhHNdkZp9lVTctpE8',
        'XKJrWUuD+InjQ+afWKaFsfXMsWQGtW232ZOf1FO27zI8FuotzZ0mPlmuw/wgoGxq9JxQ9JpczfJ7TJlx+AHCkHAoJXwvItwT915Gf3+exe89k7raKp649zJQ',
        'MGftSRCZA9bklSn16LdBlVayJ0kxmZzo2N/2/2kt7fq088E1juNYW0hFXGE88qAn1IANO7RTacHZYtD2zGiOim+rYUwEvfWUS8TBaaZIhCZtHSeEriFIctwT',
        'v/8W/QN5Fu97hibc7/WXj+fscSKUEy6OJCt1ItJvRuarVFCSbk5Qfv0QYjMpYpOoMrLnom+Yg/bo2ykHyWy0iADm//SYmgnv2AX8ruukXjtcNZEmXLR1lElf',
        'EALhe3LnCReHv8GUKTOjxlZR2HDa6DcjQocyym3wY5KHjD1me02S0XSbcHYyavy4j+cZ28yIPbvGIO69iH6X7xhkgxNjl5n+J9PMI2IbwDNUCxd4wHEycDJH',
        'eSeA8Eolp9Q7ODLCLV81v9MRIhblTvNCWGTAZhGbgBGhhtZ9tj4w47IJU+FVMS41DmHpx2Nsps+V0Vb0d3k0IAIeehwizJ0xfU7uwmc/z975Nm0OlDIfmU9E',
        'B8xXkZV08tv7AnByWQBOzgzAidZxu9g6Tj+0sjgxg7CvSdpwxqMSc20lJz7ZQvOB0HSImSKpZqI+GAZZQ2IZF10caBhPiRP4YI0OtGsGNABxMLvz6eP6nI0c',
        'h1bQhP2yNylIzFGZjlNWhaqSysnv6G+vq8mJx+9eR/9AniXvr4AqTSDVwPB4q4QOgsGZnMqdaHGugDQOM8SptHeYvi26xra9zLPNPiRxJ29a0X2ULCt9Hzfj',
        'hsSzs5X1MwLw4/sIh1QvzVC7M6QTzg+ThZSUTqFnoL2uJicevSuw4z4Q2HF36T1H5+91OhhRaV5oT2xsJss6EAxZRnA4OpnSY0tKma8SmxhV8hpbz6U1e2sO',
        'E6OL7yQU7aJQLhWiv23JYBJ+7BARonLOZFLXVcr4Ki/JGjq30gYnj/z2a/QP5Fmx//nac/LbrwGKBXufBWmE0x0yolHPdiV9FeZNJldYJZuNhP4zC0BZ0YPk',
        'Wkn5Hbq+rN82jDdcIkSw46zStd2V8kXSvCT2sfQ083L71rZ5y8bKCESlr5x8T197XXWVePjXX6J/IM/KAy8KfJVfBmDX95wDcXMgYgvjO6wUR0wSUgbbDbqu',
        'Wy5urWulkhi/aMWWYibYaQ5Jcep7kXgzukVzix9dTDzR1Z7UiEPiSNR2MidzPG10moQFMUcFNsdpw1tfk0640ACvzom//PKL9A3kWfORi6mryfHXO74EKHbb',
        '7zxIRgfM4EOoXgGsmM7HJyq6pwxRWcRI3psWqwtGnU5Uq0ml8LwSmUw2kXYwXAlVcJ1I+jfjxDUbzoSgJVgw6QsvXncQG0pYg5BGOE9X7ygRicrqnPjzbefS',
        'P1hg7SFfp64mx59v/wIoxeIPfBEqcZyShOZBkvX1iyvORXpLQePDmQxxGyq4NkGHyHcZD4hCPDpQqThDiAidVtrC3t5y2PR/uGKPSinvkQFOmAxrvsmD3k7D',
        'KfYOttdW58Sfbj6LgcEC7zpsHbU1OR685RwAln7wYkgmCxkuMO8zcDvpMUkN51QchZkF67eocC553r4m5bqyHMwYp6hwkpMLbgirJ971QCS6rjsMt430ecl4',
        'nNmgzRdKRhxXW50T9//sNAYGC+xz+HeorcnxwM9PBwXLD7oUbI6zV3RyIkx72xyXOoq0Q5YZMOStKfE7C5WaNtxz/FAMxhdlkkuTXKyvG/LRw9pxvueDCL8M',
        'EidcVVbc+9PPMDBQYL9jrqW2Jst9N5wCKFYf/C0IOc7iLNuBHJuEYURbvIfEuKNifcFONO3U1eEc3SHKiJ9WhpXkLiMa0+oSYs8VVt+th4w0e9p1jbkSG0Oq',
        'jlNKaFSplJPf3t9eW5URd//wkwzki7z/uB9SW53l3h+fACjWfOxKsMGJ2WAsaRaUdc4alJ1OUAZkLARo65tUkWnfY0+e+ekkNs9R5dekJdUOt3XHUC2y5UyF',
        'TToXxVsEUNJSDJVSIjCfylElCOVJJ9/T115blRG/ufYoBvJFDjjhBmqrs9z9/WNAKd515PfBcJx5sP7fDuNHIrSSWAphuJO45+20Iey/1HMVOG7YdL/E9WD6',
        'bGzB8nHYqDt9jHGvTNIhbj4god8VT13QBjjC93yn2NvfXlOVEXd89zAG80UOPPlWaqsz/OaawwHFu4/5KSTBiVmg9o7iZXke9txVQHspM2SZA0OXDg/3LN/3',
        'w2rQ8BbLIE+6y5JcmbZbRPScxJcrY7ousvNsAkb3lvd1OAPcRpW256QmlxG/vPwgBgdLfPizd1BTneGOKz4KKPY7/uegCacrUt8OpwjBzmUwi4hwBmaXXZPS',
        '7FWb5q2Iu6+MaSDCe5JiK/UdCe6KT3gc3u/cXEWDH6nLqybnilu/uT+DhRIfPetuaqoy3PbtD4FS7H/iL8HmuDQPwIi6ZfnotP2X4CiLaDa3pMXjKj0/6fTV',
        '+fp+kK9oPqRk2Ks8rc72ZMSnND7INB9pEuiEgEbEncm2zVgpKGsIR5AQOxThfn7JvgwUPD5+7n3UVLnc8t8fAKX44Gm/AcNx8RlOR2OVWpLAaUBlKBCTjGib',
        'SYhsSz+WdhcBnmhPEZNOkcYpdg1BGlHs9nbMneG4zya2+RCuCnJOhiLc9RfvzWDe48gLH6CmyuWmS/ZFofjwGffAUGVWacrZiKBYvkkArc3++7bXJa1VivvZ',
        'AEd7QiIPf/J647XX/bITU63aN5WS+p4EK8EiSEtSso3tkYjGZGTdTpaNjvlCS4I44UCZraGcYm9/e3XWFT/94loG8h7HXPwQNVUu11+8NyjFIefcDzaqjHei',
        '8uqLryA1vPlgtTJ7znCVpSgdoYsIpdIJOOFxa19m/S67Ntv2T0bP1ReWH0uWetnjtMedFJFpLeoPxGvqyp+nlC/0nDmpOSc6HqcJ98PzV5IveBz3tUeozrn8',
        '9ItrUEpx2BcehEqEG0nTRAidpxWbnaZgO6X17/SP3+qREupFe49lvRlalA8S+hETtt1w6DL8O/yzfKPTSi1dh6XDYTs+ZzZhA5Ms5IvgpiBZyHOKvYPt1VlH',
        'fO+cpQwWPD516RNU51x+eN5KUIpPfOmvENdx5pHRgJLO50jhi/B4BETiyzzJsUOlPVQae5zDRPh+89sRTpmnw743/Hy1pd/SdF2yuNIef1JnRhu6hW/CiMOI',
        'a9OKScDknPi+J4Rwwwg4kBCVjrj6jEXkCz6f+daTVOccvnfOUpRSHPWVxwB6xeq1u1mESyrvtAm2l3a0lIdT0NFkDG3HGRPD5qrIFaffaXY1qtBtoIJ+swzi',
        'NELaz4y8JJFRbqNUixiWRIj0WdmYAlGp7xNDgpPqjCOuPHUBgwWfk7/7DNU5h6vPWARKcew3/gHlHJc+oQZw2LOgUlboUC3VX5mii0xQ0169ZpHY2cup/ayA',
        'jG39ldRtoZi1+mEjXTuIa+viNF2of5dH2fX/webauvQ4Rjgpywl3+clzyRd8TrvqeaqzDleeugClFP912b+gkjmQbHoXWDPplWcuOYg4UqzsaDb3RMFQkw4X',
        'vSfGZcnXhyaCFq3JPJgy4hDF1JJB3BARW2jTENCuWbDzPcvVQrwcK9rIxg+2g7IymVHCl/FM5qqMIy47YQ75ouTM6zqozjpcfvJcUIoTvv0sDAVOkqhquGvS',
        'WrifFyK2QU10vw1soiSftPuHMjF2ypMRItmE4zthFpjx2c8fKl2hXErE+yfQX7qKnMxW8X5CVFZlHLHuk7PIl3zO+sF6qrIOl316Dig46bvPwVDmwFCTkyaC',
        'kvouLShr/jY5LTJY2saADrf/DTlWG98xTz/lC8noznJgFd4R/h/VsUcxx0pFnLbjwPYQhWO3FoEtWZI62PRFO5XB8zwRfF5UGCez76sgOtDfXpUR4tJjZpAv',
        'Ss75yUtUZR3WHT8blOLkqzsgrejDJkj8xZVEZPxmM8gw1U0kJiyl7s6AD4NG0wsO0/sWLpZgYZhFEetWwlQwwCfmK7XGnsZhhvujfpWn5kWtfN9L/czIuWxE',
        'JUQ6zvc9p9Q72F7lCvGNI2eQL/p8/medVGUdLj1mJkrB6d9/AZIcl+Qo0+LZUdGsJ/urP7elO25vSaELIeK7tIYpAVgEUnEdk9xtKAXMBp0ihhhtRJnmXLa/',
        'ZzCUf9K+zvfTd2tIUyNJbrNMIjOYIX2VVa4QXz18OoWiz/k3dVGVdfjG0TNQSvHZH6yHpMsrDSXGK3MqN6XiwdgIcKhYVrF5rqls0V859Mv2HgkJMZTT1BDR',
        '/k06sZKurEqo0NZ79terfN8PPDnlz03bDyxNnRhROVx0oMoV4isfn0a+KLng5i6qMoJLjpgOSnHGT16GSvE4u1uxGRkKixPtaKANVTf8fqnjOKHHwxjNZgXr',
        'gZgP2UZcHCOoxX2VgrZpAKUSaClLP7QWQFyvxW04kzaRZveZcej3pqW5O2BtwjYcx1182BTyRcmFt75CVUbw1cOngYKzfmoRLv7yco/JkIjNGrRRzL7vh1lW',
        'mUwmWOUqBvFTbTrbXhaRGWIDBXOuYneG3p0p6Gc8Fmj7K9N3btCIMZaiTropID0/Uxws1ErPz/mel1VSuQihHNfx3IxbylTlBt1cpjAcx1186BTyRZ8Lb3uV',
        'qozgK4dNQSn43I1dkEY44ziOF73Hm23XGUKb+6L9GaONzvSgRZxxzZ+WDZfUi8l6Bt+PJ93qc+bT0Sr0ugxFuLI4IEZ8xn2VNsDRIjNwsYXX65Vmri0VS7l8',
        '70BTKV+ss9+XVgTiZjOFXF11j5PN5isR7qJDJlMo+Vz0i41UZQQXH6oJd85NXZAGTuydhSqJRc/zrU9AB/dKwm9yx5FYtLLLXFUVWrjaAy404tHOKh7SdkuK',
        'sJhHJe6SMs+pFCU3iyos0rcIrqPtDvme/lH5vsFRoAm13/tXsHLVDObPbWZcczUDJUXHy73884nXuOvOh3njjc0AZKqr+nL11dtHQriLDp0CSnHuTRsg8pxE',
        'csr428yKSrq27AHZYkJHoRWum7GqXATaCzKM7EppUV1aDJVVhvrJ3ylqWXs/7Nq4uNhN27kvAlducI11PYKezdvbvUKpBuCjH1vNSR/fncaerTz93Eu8uXEz',
        'hf4i2eoMreObWbJoLplZ0/jhbU9x2aW/BsDNZQdyDTXbhiXcIZM14W5+BczueWZfkuR2ECNpEdEIXU1CCLySh+O6MVeZ7YaSUgOVih6RJEFG0CLAYap2ylgP',
        'z/cwu5hXSpi1HdERx0X3mON9m7eP9Ype9ajRdaz78oeYJvp49K//YsP6rTz16gCb+zx6iorajGBCo8vyWY2Mn9XK2rWLeYk6PnXqTfT0DOJWZftzddU7pK/j',
        'cWk6roxwy1bO7RTCCSoyKR9sclAJEWbv+ZHNZsoQmxAC3/cs/aX3QdG5IhH4qATPzYSPlIhl0QfrT1+aLS6cxCc0yw1828NiasDt1r+tt600WKgb1VTDzV87',
        'gJdffJ5nH3uVu5/czvo+qz+O4yspXYC6DLxrrMPSXVuZt2IWucYxfOSMXwJQ1VC7GUd4IeESqLKMcFrHiRjR0iYvaePp3Qh0ZU+0sw5xN5dBWgkHswEidhrg',
        '22pDEdQ+p2wAQmLrqMgfanRcuG+0EPjSC/6OCFccLNT2be0Zk8243HLJB3h1/Ys88pcN/OK5QfI6s9yvqq3u9X3f9Ut+lfT86Av3wJ5jHfZa2MyMJdN5dH2e',
        'K299CuE6Xq6+ZrMh3FcPn0ahKLUBnhF8+dApEAcn5vtxsoxQSR1guM2XXrg5i50SXtktlZjMtyEGR9piCy5BPE0ENzWlPJmGbiSH/QENpfQwt3dvnaikdM87',
        'Zgnjawd58uFX+P4TfSggV1PVV9NUty3fN9hYGizUSV+mfm75sLnV7DavlUm7TOTU7/yDbb0FsnXVW0oD+eYqV4hLjpxBoehz7s86A3NgamAOdEIUSE1u2Wv/',
        'iLS776vQqDbF7mk7NlQiXKVEoaHacEb2UC35PhuVVsp/SRrXUvpk3Ex4rthfqO/f3ts6Y2IjXzhqDq88tYF1975FTxFytdW9NU21W/q39rZ7hVJNTU0VHzt8',
        'f5atWEJTUyMvrX+B++79Aw8+8AzZjOC8941h1NhG7n5yB/c+9hZuLtvvl0q1VRlHXHrsTO1k/vF6coHnRCnFWcZzsnrtwk6VUCiFQpGqqoi7tWj0rJ0R9IBN',
        'vkda5CA+gXGvuc3NO0MI03bWY2KbF2VutCG43wZdhrA9m7aP9Yql6lMOnsOMRsndf9rAPS8XcbOZYlP76Nd7N+8YU8oXa3dbNIV13zmFSZPnAA1ABsgDm7n9',
        'xts497zbOHDFGPae18Szb+S57nev4mTcgvT9XFXGEeuOn0O+6HPW91+gKuvw38fMQknFaT9IcTIb7vI8P9hKSRu3vu+HOywYWGyg+lAtdUITW1y8HR03EoKX',
        'fSMo7JT9p0pFlua4QaBhmZX0ne1vbJ2ccQXfOXUhvW9s5dI7NrJlUNHYPvo1pBI9m7aPb2uu44/3nkG2tR0YA7QAOWAH8CZ4W7jk/Fv400MvcsExu9DZtYWL',
        'f7Ye4TieUtI1EfDN2wuc/6P1tDZmueio2fgln9OufR6GS8/zPLNPlh5tJpPB7F+VnIWd4ZxQF47APTXiluAce1Ho2Jeffk9KM2gzueVGMV+s7t/SM3b6+DrO',
        '+cRMul/dwhdv3ojjOl7T2OaNfVt62kr5Yt3nj1vKsR/ZHca2QesEYBJQDWwBuqEwQMFTfPubv+Ok903l9/f/nc9f9zzCEb5SyqnOOuLK03bltc2DXPijFxjb',
        'XMVFx86hNFjglKs6AAYDcGImPpLvxvA0Azeu7XksEAAAHMpJREFUmyR8L5uLnRCFb1dUDte8koebcYMFFth01laMdggojetMmoJSKgZMCv2D9YM7+lvX7NrM',
        'sR+YzD+f3cQVd75GtjrX39DStGl795aJQqnMZafvztgxDbSMbmLSpHEwZxq4o4EBGOwD14FcDb3bexj4x7+4/Zd/5du/fgPhOiUlZaY664hrz17Kk+u3cdnN',
        'LzJ/WiNnfnwWm17fxjk/fhngjVSOszczM2GaZLG9QceGKzUh9STZhrgdLDUTkESqZQUgO9lsh7Tuk6ed20MsnhCc2B4WCH/b+5aYY/m+wYaBHX0t71o4mmP2',
        'n86TL2zh27/oIldT1VfdWLu5963tU6qzQqz7rxnalnUc3FyO6oYGmkY3kctlyGWzNDbU4bou3W9t4dUXX+ZX973EXf/K42TdQen51TohdjU339/FHQ++yv5r',
        'JnLo3uN57PFX+O5d3QAPpDqZlYo2hk5++LZSaMfexmm4bTaSvsidbVHsK64jHUdnQSsFmUxkJ6ahSzv/xS7TIrBnI29J5CQu9A00DPYMtKxdMIrjD5pNx8Z+',
        'vvrjZ8hW5wYaWpve2v7GlklCSffLHxlNTVahAjegk8ngZLI4rouTyeLmNPDz8oMU+nq55Dd9bO5XZGqqtnn5wqianCt++qW9OW3dX3l90wAXnrCMGa1w693P',
        '85snegG+I1av3a0z2jYw2phzJOAjrRlHdTghIwAwJoxk51EOhRTtaIEbfrNHxKIUI1kUyaTZ8LiSmC16jZj1fB9Z8mvzO/rHzJ9cw7lHL8Rz6zjmoj8gHOGP',
        'Ht/6au/mHW2lfLHu8BUZlk4K0jXKBuIgHB2TVFLy4IuSXz0pEY7ws/W1m0zRx7nHLeXCqx+hdXQt1134Hnpe7+KCHzzDq1t9gIPChFjf90LPudlf3zBW0hPy',
        'P3F2VOJY/VwX/S2AcpFqH6ukFw1nhB7/6HttwZsDdGjEoIjKoowLzBjd0bOifBkUmb5NOybWVTtcd/YS6loncea6B3jpla2MGtu8UXrS7dm8fVxLveCiA1MQ',
        'baI9tVHxvT/p8ebqa7bgOCVT2DiurY6XN+7gnOP3Yvku9XQ89RTn/US7u4A2sWrNgk6zOaYQ0WagST2U1CNDt8rEKVMmiXPD+UqjPsVrvKWKNkwrzxbTctkE',
        'cu1Iuom6G5ef1tmB1BEitFldx8WXPr1vbZ8gfZn90jHTWLRkMfc99iZX/PQhMlXZwbrmhjf7NveM80te1S7jHU7YN0NdVUrfgXv+6XP7o5pomepsb6amutf3',
        'PKfUN9iedR1R8iTNo+v57feOZeML/+SynzzK3zoKAFd1dHWfJFasnt9pVqfZCFvEwjDRRI5894ShCDf0NcMZ06lPqqDz4sQL9JolQs1+lKZC1hBYn3NwXRHq',
        'QlP61b+tp7k0WGx896I6TjtqD5rG7sK+R19JqeTT0Nb0RiaXLWx/Y+skJaU7qk6w3yKXWeMcchl4c7vilc2Kx9b7vLldvydbU9Wjsk6v5/nCFY7jD+TbCXYg',
        '+uF1JzGOHl7811849YqXzbBmdHR1vxwjnIH76cpBWYRLco2o8HfKU0bovirzkSaPWftb2teUc2Kcw+z32iLRDhxHHiERol3jRfFLXrZvc8+EjCv4yYWLaJ+2',
        'gj+/uJ0vXfxzhBCqqb15o3CE7N28o90rlqorjc/JuIVcXU2Pk3ULxWJBeEVfOIoaWSiNBjjx1I/yyf1m8dqLD3H2ZQ/T8WoR4LqOru4TAMK8SpMWUAmWG+CS',
        '6o0YlrtMKyeq+ThF2ZU7YeOlucDM/2HmmSFmoMNMKqFBna6rPUW+5wcf91OYlAjzHMdxyOWy9GzSwdO1uzVx3qf3ZvKcFZx6ya+5//d/QzhCNraNft3NuF6p',
        'UKouDuZr/aJXpZQSwnU9N+OU3Fy24OayeXsvr3zPYCOe3wBw3PGHcc6xK3j5qT/zo1se4pY/bAJtvU/s6OrOA4Q6zui3aIJN07LF9z2kr8jmMpRzZBqES5/1',
        'SqAjSQj72vTzIjbxlXSj0VvZIGlJSo0S9a5CZmNsN9R3mkAaTQb7bQHaa2QCqV6pmOl5a8dEgC8cN5d3v3st41ftyVGHX86jDz8GQE1j7bbqhtoddh+llCIw',
        'L3R6nq+cUiFfWxooNChfZgFO/exnOOmIXXn1qb9w7/2PcOkN681Q9u7o6n4gnIPlq+Z3xommJyVZMgxQKnpkc/EvFlpTVOF49MyRc6a5PnquiWzH32Uy0CyA',
        'EXQ5GW03+szcbipvcmY8SgdaI2KZre/dsrxQRwgGewcaB3v6mx1HcPV5ezJ/t+W0LVvLRV+4lZtuuF732RF+trqqP1uVKWRy2ZKUEt/zXd/zs7Lk57yiV2OC',
        'rLvMnc/Z532WNfMU3c/8jbvue4Rv/PQZM4RCR1d3TOyKZSvndhrxF61kMxHxyU47Nlyz7bSIMyPXWqwzMU60QUZExOT+IslmB2e9ki5Jtj8fFmYme36oH3WR',
        'oR8T2dlsNvDqBDs9uC5eoVjjFUpV0vOzUirX9/ysktKtrclwyelrWLZsKS0Ll/PQ3wpce+XVPPrwX4edn113W8QRRx3HQQcvYsfzj1Dc/gJX/eSP3Hj3evuy',
        '3o6u7sbY7JhPtKR76eMTXd7S0Gfa9ZVNAKM/XCfa+iKpR4ey35Kw35YcJqohrG/nAJQ8L3TNlUpe9LUOIXAzLqVSKeI236eULzYW+vIN0vcriRsATjl8EYcf',
        'vJZsw1TqZy3mmafz/OmPD/Bix/O8/tpGCoUCo5ubGTduPNOmz+Rde69l9i7V9L/0T0q9r/KPJ5/lupsf54ln36S1uYELPnsIp13wI0gj3IrVmnDaRotWZuT1',
        'sCc9SZShbbIkMaOKnTjBzbtBhX7GtJZmjgxtjNsfKIoM9Hw+H24MoPWcFsORq0+nKpQKpaqB7b0tfpB6MGPmLPZ+z37MmTOXcRMmIhAUiwW+e/k6Hn/sEQB2',
        'ndXGkR9axHvWLsKpbqameRyMmQQ0oUV9EdiO98oGiv2bUN4g9z74DD//7d/557Mbw77uMnMil15wNB86+quQRrj02oGUSbdW/c55TiLijuTeZLJt1K/0Ktik',
        'eDcmABBzFigViU7D1QZJep4fImqTIl8cLNT1belpAy3Ojv/0ybz3/QcAG2DHBlT/Dq1QnSyitZV77nuByy69mq7OlwCYMKaBvVdOY8GcCcyZMZ7m5ibqanJs',
        '3trHlq29PPfymzz65Cv84a8deJ5PVVWOseMnUCoUef3115LT0t/R1V0fm9Vk8X6cYCNt+p5KBnol49jqRvgMc635Zwot4jUDZvcFYi6y8qSkyG/p+V4QqpJk',
        's27khwyGa2w3KRVeoVjbt6VnDMDhRx3LhV/+IvQ/Dls38swzz/Onh5/l9Te3Uyh6tIyuY+K4Ft69xwLGjpvIjff+ixtvfoCX1sd0FNmMQzbjMlgohe+sqani',
        'pFNP5JDDDmV0cwvgAtV0dXZx+603c+P1P6a3t8dM8JqOru5QaY6olDia3JAUQ54zSt1xtA4JTagywjkxJ7F5rNkA1HZ020RJGuRJHWffoytBfTzPwyTomkBw',
        'slZb77Hiuz1vbp0EcOIpn+W0Mw+CjU/z2GOP8Z0f3c+jT71qDVsogtIpRwjWLJ3KcYeuYdXKpTz6ms8tNz3Eb379S7NopXYsKwfgY584jC997etAJ/6rL6BK',
        'PSjpIZwMItuIO2kOmze1cP45Z/DAH+4zk757R1f3kxASLs0Os1vl8yZHUtt6MiSE2d/ETIjJ9dCuJfMtHDcm2uwMai263BGJZ8Ol9keXdD6nSSGM0GIum8Pz',
        '43rU9l/2bd4x1iuWqvd7335ccc1n4ZVnufHW3/GlK35f9l7hCOk4jiccx1dKuX7JywEcddBizj/9UGifzd//5XHEYYfp/rmuJ30/c9DBB/KNdV9Avf40b772',
        'It+85rfcef+TAFTlMnxwn4V89ICV7LZoEWLCbpxxyiX89s5fAWwGpnZ0dfdbu+clzYCdaUnCDu2DHM6dlXZvOQHLTRX9vwqzuewyL40eHTKZbCxqYBaWVD7F',
        'fKlqYGvvuJraGv729xuo3vIKd/7295z99bsq9k0IoYTjlLJV2byTcf1832CjktJdvXgSP/zm8YhR0/nVHzs558wLAGhtbebPj96I80YnG156jk+c9kM2b+uj',
        'aVQT1dU1vNndHT771GP24qRPHgST5nDwh87m6aeeBri8o6v79Bjh4nooiSLLvSnlv5OIcyjxW35+KLifJJx9rZ1SESX16hCVEdme55HLZS1/rMnE1n13HEHf',
        'lp624mCh7jMnHsJpH9+LHd0vsurQ72iJYInFI44+jnkLFtKzYzuPPvI3/vD7ewCdU1ldX9vTv62nzff87Af2nMllFxyBaJ3Jmveexqa3tnDG6YdzwqF7MbBt',
        'A+85/Fts2d7PuReczdHHnYEQgg2dL3PFt7/JnXfcDsDZn3oP/3XUgbw0kOUD+30adLHd+ISOqwT3K7WRENW+zn5uZSIboJEs5bKvjfSa2T/aLiq0zAClE2FN',
        'NFtfExnqRkx6RU/0bvp/2jvz6CrKLIH/qupt2TeykAAJgmELi4mgQUSb0RaRVlxaRVoYbaFbGVTQqLjQNh5mxD3ayHEF9Li1LQoi7WCz2DCKbKEVlQiBBEIS',
        'ErLy9ldV3/xRqffqvbwgPTNtzx/cc3JyXi1fVd37fXe/9+voL4SQN33w7+SkhFj6wp948+NvUGxKUOhCkSSUV1a+wwUXXhT1Nbt3fsVD9y/g8KEaHAlOd1JG',
        'SltHU2uB0IXyVMUlTL5sElfO/gOHDjeweuUjjDgrg2Wvr+H5lZ9T2C+bDetegPQiIB1DQdG5Y/ZtbPxsKwCb36sgf8BQ5ixczudbdgFURKUuWMun4rOl3uy2',
        'HztuPdebof/jEFsXYCY0mX0yDTeV4Xc0Y4zhkmVLrZ2ZnmFtS+/z+Jz+Dk/fgQNyWLtiPsLfzMQbX6TLG9LsTrsv4AskTZtSLi1dthAjaysLI0+yDRB0dqhM',
        'm3I9DQ2NpPRJOy7Lit7Z3NY3PcXB+ldv4ZJ/XYmu61R99iQSKlNvXsrBuhbyslPZ/M5cJMkOkgKSDWxJ1NSf5BczFqHrghnTxvHIvb9iw9Zq7ly4DODTuBWp',
        'Rt5IRA70Fon+++F/TrRYk0Hv3hbGZO+yLEVNOnNoTVdRlGhFRDcdzLISNhNUfzDZ3+Xtc/nFJfzH/Vdz8GAN1817G4fL6ZFtcsjv9qU/MGciN0wtBcWFJCkI',
        'zYdQvUiyAzkhl+9a/Uyf/hiyIqsZ+Vn1Xc2duaFAMOHRuRfwxOs7DcKtX4xkT2HyjMeoPdoMwLWXDGH+zefQ2hVk7ZbDLLhtMoozlbKpj+LxBhhclMO6FRV0',
        'eDTOn/oAQFOkmbZuNVzl7o/Uw3EpA2ERe+30IuFWpJtyKrLyzH3bIk7t7lkTwzojXR+MYwbBojONrS0SNU1DlmTMQg/T12glXCTZ16jeCXoDKb5OT9YvLx/J',
        '/bdfxl+/2s+CJetwJrk6bQ570NN+MnvS2ByW3F4SFrhCD4FQ2VLVybiyYWTkDePGO1/jQE0j6XmZ9WpIdbhbu3IuKS+krsHNgbpW3q2cxcjRZTz71kZefWVN',
        '+BudDhuBoIrNprB3w+OgBTj/qsdwewP0z8/kz2/eB8iU/Mt9AP5wdMAgjB4VJY4lkMlyjA4E8d1S3SP1IJxJNKvWaiTkSFHHIvdH2HNkFUVWmaGIEHZXGRVD',
        'lvwQYhKHwiMbhDPicRI2m4wsKfjc3hRfpydr2qSzePD2S/n6QBu3Pfg+Nqfdm5SZ0tLZ2DYAkBbNLOCSsoj3aWe1h3tePEJlRTnl55/LM6t28O7ar5AkSSBJ',
        'utB1ZWC/TMYMy+HDz/YzfcowHrzzOoIFxVx/9aPUHKyJwtQTzyxgSmkRx2q/5fJbX0bXBWcN6MOa1+4CIRj580VgZjKbPN8M1xusKNIhwYpTw6CWYlpHWGVg',
        '7H+iEB+9uuIRKprYpqtL0/RwjEzC2IfOqnDEThZjn4KI79WcKOaf6f03ZZ/f40vwtrtzx5ydyrKHJyNcuVw8YxmqqpPZL7s24PUnedpOZgNMGpNA/2w7Jzo1',
        'PtnhMRA+fwITy0dR+e4e3lm9ncSkJCQkPB43DoeNVx6bxqz7/4QsS7z35BSKh5chCktZuuRDtm3dQmJiEr+eczNXXZqH79BeFleuZfUmQ4oNLszigxdnAzD6',
        'iscNwp03fsRhgyWaFaXRFZjxwSr/YhWXaDhVHkisP9S4PnKdYUTr4XcyCW4qIKeC2KxrUyYa32mLMis0TUOCcF3AuqfGktp3DI+/sZO166uwuxze1Oz0Zl+X',
        'N9nX5ckUQvRIE3j/uV+Sn5vGQ5VbGDSqmHvuuwNJFmz/YjsPVSzjtcVX8oc3PueTrXVkpNipvOc8RowcQXLBCMgbBPjxff8tWucB1m+q4pGX94XH7p+Xwprl',
        'swCZ0qufjyacuY+16YDtaYTHV/NNxMTPAJPC7LCn5tnTXrQqPWa7Q1XVw0QDejQN6I1o0eEdg6NE5GCsZmqwV0/ryTw1GHLNmZzODVeV4+w/nEnXPE8wGDJY',
        'ZkZKCyCpgVCCGgy5Ah5/CkDpqP688ruptDXXs+DpbVTeOx6hBRFaAMmeyPxnd7N0wUXYdTeX37mBkGo877qLcrj6ZwMZVJSL0DUO1R3n/Y21fLTNKO6/YkJ/',
        'Ptl2lASnjc/fuAWAcTe8EiFcJJnUyGQyQywRBJ+O5vf3GOv0uC66j0mEtUXq8RRLPkzv7xPLgc3OCLGJQiaYMlOWFVR/MMHd1pWb6IQV8wvpM7iMloxCbrl5',
        'OT6vH0A4Epxe2aaE1EDIpQZDLpfLwbr3ZiMaDrH6kz3s+L6dSSVO/lzlIxAS5GY4OBm089T8ckKqypS7NvZ450Sn8b3eQOTl7po+nEnn5nDVPVtIdNnYtHIm',
        'Qtcpv2klGC2hRtSafsbIrPwxlT9WlvU8b0VSRJs8lbfFqowYsTETqb2tsrhishusjXDMlAZrarzZ3sMatO32VeZqIdVVUiCzeFYhqUUl6GePoPKZL9nw6ReE',
        'QqHw9RMuLOWBhydiP/g9XUf2M2fZUTo8sUWicMX4PObdOJyva7q4r3KXEdCVpZBstwW1YMglRNQG63JWmoO3Fl9Aywk3Nz+2k365ybz71NUIPciFs94DcEvn',
        'X1BSaybPRFikiPnQ3lZTHIxBHKL1RHrk3ugxrYpLxIEd507LDlwQ8YQYnCOiWJljWp8XL8wUdkYLoZxs6cwXupBH5kvMnZxF/uAihpwzio68Aqr2tOH1higp',
        'yaRQb+eHXXupP3iEpR+doLbFIGrfvmnYHTaO1LUC8OTd5zJ8QAKvr6vD7YeGFg/fHGjFlZHSKEkIvbvRqKbqDs3rzxzcL4nn7h7DgZpm5i8/QFF+CquWTEYL',
        'nmTSbz4F8ErnjR9RZ/TgiqTmReRSLKH+N07o2LHM41aiWbsZGfeEQkYRh8MRbX705reMdpabz4x2p4HZQMAWvtYw1rsVIF3Y3Sc6c4QulDQXTB1h46KRaaRk',
        'pGBLTDAc2D4fqt/P6h1d7KkL0tyl4fVrzJ49ngULr0eWYfGid/jjO7v47IWf0X6ijd+vquG31w3lpQ/2U/VDJ660pGZJkVVN0yQhJEkLBJP0QChlUlk2d15b',
        'yIefHWLV5jbOH5XHkrnn0dRwjBm/3wVwRCobN6wO9HBZkvFxsZWiFgXC4l7q6Qo7FdFODdFG9qlcZb37PeOzdylcTWsSMtqxIKK6IplsWdd0xdvhzlIDRlKr',
        'ywYD0wV5KRIOBVq98EObxOCCJG6ZVsyraw/ztwPtbH35UtxuL4FgiFWfNpDocnDtxEy27znOup1tLL5lMBUvfs/h5iCO1MQWWVZUVdclhJBUjy8LXdgWzhzC',
        '6AKZR1ccZH9TiBlTzmbW5CK2bd/P4rePAmyUxpQVH7EGNXtqexYUWmJvpw/RsiwecmM9M9FEsY5jPRZ/QkRrlAbRzEwv6+oyJ58h26M71IafKCDkDyaEfIFk',
        'rZes5D8+PJJ+/fK4eN5GgiGdl2dko/oCVHcpvLXDzUsVowl5vDy44hC/GJvO6FyZO1Y14lcFzrTk4wKha7qQREhz6f5AemqSnTcfHEProWPMWdGAqsOzC8Yx',
        'OEtjxZpqVu9wAzwtlY4dcjR2W83IDI0hXJTm17vs6v1cPJkGZqJOvOdEsTmL3I2N75ksPFxlJJlmSvzmbqbCYm3AHTuhopqBa7qiBVWn0HUZkAIef6pNQnpp',
        '9gDS8tKZ98J+GtuCPHqRk6REOws3eFg082wG5yps2tHC+j2dLJqSwYY9bazeryIrStCektCq6bqELhTV4++DENKS35ZSlOJh/X/W8dbXfvKzE3l1YSnuhmNU',
        'vF5HXZsOcJlUOnZIPb1A/OL6U7PGWKLFrza1elVMovVmSvR4Qvhc/ACrVY5ZUxmkKGXH2grYcJ1ZTaCeYM1EEwJ87e4cXdXsc8tcnDMqi4/3eVmzs51BOQ50',
        'JCaf15cJxQ4aDrTyxMZ2Ksa6OHrCz7J9OjpgT0poQ5aDQtcV1efPRBfKBWUFLLqugIM7a1iwppWABg/dOpqyQthXdZhFH3WBEY7IlcaUFfdIKTp9iPgSo5EW',
        'jYDeZE+0Kyz2XLTf0XouvpyLjCME6JpRAxBWYLrzK63ENoeJyGzrN0SuE7pAjun8p/qDiSGPP31kGtw0wklWcR/mrDRQuWhmCYMyvLR828zzX7hJUwTFiTof',
        'HDdWvuJydEkOm08LqE4RDKUihDxoYBZvVZRRte1vLFt/nH0dkJ+TzNu/G0fdvgNUfnSMPY06wNPVtU33SqPGDGqMg53Tg7CsMH70qvbHWaTRGuT/LejdsTjZ',
        '9ORY0tIlcwtM0d381OwgYX1Hq9iw3NP9W0YgaSHVqfoCqQDXj0ygTXbS5LPx8NxyOFpN7c563qzy8m0XOBUIdDd9kBQ5KNltPj2kJqDpDoAJEwdTeVsJuzZ9',
        'xYcbjvNpg4GXtx//OVL7UfbtqGXJZp/5SrnVtU3NUnFR3j8Ge/80iO+aO7WiE3uteS7OWOGG2BHIz05k2pRh3PWbctx7v+ODj6t4bWsnx91xUGtWLgMDCrOZ',
        '8+uxXNNPsGnDLj7afIK/HDeeP3/2OCYV6tTuOsjjGzo45gXgqerapgoA6Zzhhaenr58BJEmiT3YaiiJT0C+TocNymTC+gLGD0pBb2/HXN7Fmaw27D56koVOn',
        '6aRKpzvYXcImkZGZTHpGMiUl+Zw7KoupZ7nYvW03tbsbWbXbS7XPmBO3/qqMmaUJfPPX73jvv1rZ1iIBNAGF1bVNQQBJ7Ln7DOFOB8wAoBAQUsEfoKOji8am',
        'E9TXN+NtCaCFdLREO0OG5jCoqB9JGangckYEvKaBx8fR+ia+/+4wrTUetlT7+bILAt0L+e55F3LNQEHV5n1s2tXOmsbwAi+vrm3abv6QXptacIZwpw2GqSF0',
        'UFVBMKQTCmqoHg01YHhdEpIVHEl2ZKeMXZHCdq8QAo8KgaDghw6NQ12CJlVG7R55yNB87ptXTmHXUfb85Tu27O1kfWvYjLmjurZpufVNpOLC3DOE+ydC8dAC',
        'bpo+munDE9m68Usaqlr4+BsfO71hot1bXdv0dOx9Z2TcTwSKTSErKxVFkSgamE3xkCzKR2YxLlllz1d7qa1q4ttqD1tOQL0Wtnv/rbq2aVm88c7IuJ8CTNmo',
        '6xAI4u04yaG6Y9T90ERnvZfqIwGq2nSqtbAjvRqDPW7qbUjpzSvPyLifBgz5qOuCLp+gtl2j0a1TF5JpiWRCqMBzwCNmkX5vcEbG/f+ABuBtYHl1bdOhH7sY',
        'DMJ1/WPf6QzEgXZgP1AFfA5sNO2z04X/BgAoxWGcym1cAAAAAElFTkSuQmCC',
    ].join(''),
    nameLeft: 'data:image/png;base64,' + [
        'iVBORw0KGgoAAAANSUhEUgAAAEEAAABECAYAAADeOlj2AAAACXBIWXMAAAsSAAALEgHS3X78AAAAG3RFWHRTb2Z0d2FyZQBDZWxzeXMgU3R1ZGlvIFRvb2zB',
        'p+F8AAAJpUlEQVR4nO2be3BU1R3HP3d3k2xC3g9281j2hk2yJJBIQDAPIJFogKI1JrwMT0EpSogDVq1VHLQ41hbEiOBYqqjojNZHwSIVtVUbEIhKtQYBoyaB',
        'hCghPoAouc/+sZvNLkQqitPNmu/M7s7s3Ll3z2d/r3PO7wi6rnO+5RStNuASIMv9GgQkAUHn/WHnQcL5guAUrUOACuCXQC5+OuDe9KMhOEVrKXAdUNb9XVR0',
        'BKNGZ5I1NJm09GjSksNJjjZjNgKaDroOmvbjfvl51A+G4BStxcCtQClAUJCJX1xewMXj05mYl4jQ+hnNzUdobGyhs+MEUmcXmqqhA15vfqFzhuAUrZHAGmA+',
        'QHRMBDPnXEplZRbxx45S9249Xx85ytdfnKC+6SRtHV20n1Dp+EZHUkEATkr+hOAcIThF63jgz0AqwIKFFSyqKsTYcJCP9u3jaEsrte8f5b3Gb2nq0JDV3u8T',
        'EhKMC4d/6HtDcIrWaqAGYHhuDretuJYc60ka3tlFw4F6ttYeobb+FJLiuj4kJBjRbmOQPYW4uFji4mIJNQcDEBERTp+D4BStq4FlAPMWzOXW5RV0/HsP37Z/',
        'yIZndvP0P9o812ZmZjB06BAyM9MINYegazq6rqPjCog+T/MTn/ifELwB3LNqNeUVqXTWv8vOXXWsfmw3TUdOADByZA5jCkeTkBCHpmlomoaua+ia6/49ILzv',
        '7h8UzgqhG4DRaGT9hscpHheJ0vQf1m/cyrqndgPgcIiUlIzFlpKIqqquwbsBaO506AHgLf8YP3AWCE7Reh2wHmDDY48zLi8KPjvILSufYPNr+wC4pGQsxcUF',
        'KIqCpqpoWjcE98C9AZyWFv2IQe8QnKI1D9gFcM+qeykvtaMcPchNv3uKbW9+hMEgMGtmBQ6HiKooLgtQVTTd5Qb4AMBVHHnppyjVf4zOgOAUraFAI2CZM38G',
        't1VN4GTz+yxf/Ve27WgmNNTMvLnTsVjiURUZRfGyAF13u0JPHPD+9Ff1BuFR4OrMrMFsfvQGjuyvY+Mzb/DEy60YDALXLZpLfHwMiiz3WICmobkBQN8ZfLd8',
        'IDhF64XA2wDbn1yC/GUz79TtYcWmzwGYM2cqg0UbsiyhquppgVDvc4Pv1ukQ3gLyF1SMYOJFCbQ37Oc3mw5z/JTO+PGFjBszGlmWURQFVVE8/753EOyL8kBw',
        'itZCYIc5xMRTK4toOdjIM68089YhleQkKwsXznQBcEM43QL6KgDwhbAdKK2c4CDPEczBvYdZt/skAFWL5xETE4ksyT3ZIEAAgBuCU7QOBj4xGQ3UVGXT0dTO',
        '02+08eHXOsMvyKLsilJkSUKWZVRFRe0lDvRldUO4DViZlxVP5dh4mvd9xpqdX6HpsGzptYSFmZEldyxwZ4RAAQA9EPYCuUvK0xDDdV6uPcyrjRLpaSIzK8uQ',
        'JMnHFQLFDbolZNgtNuBQaIiRtTdcwPG2Y9y3pYWW4xpTp05miNPhcgVJ9qTFQAIALgizgE0jM+NZOj2d9sNtLH2kiaAgEzf/+lcAbldwVYeBFAu6JWTYLTVA',
        '9VWTnFRcbOeNnft5cMth0tNEKiuvQJK6euKB0pMVAklCht2yDZi0fFEhecNi2fj8Ozz3ehvjLy5gTOFIpC7JUyBpqhawED4Ahj3+h+nYLGZWPPAK/3q3jWnT',
        'JpORJiJLEpLkTo3u+iDQJGTYLR1A7I5nbyLYBLOXbeRg4zEWLqwkIT6mV0sINAkZdksnEPbBq/cAOpPnreJQ6xdUV80lIjyMLklCkZWAh3AciDhQuxYQKJ2x',
        'gpa2DpYsmUtYqNmTHhV3UAxoCAd3rAPBQOn05Rxq7aB6yRzCQkN/NhA6gbADb94PgokJlXfS3NJOVdVsIgaEuWOC4q4WAxdCBxC75293YA6LZMaiNexvaGXh',
        'NdOIi4v+2UD4ABj2dM0sHA4Ht/7+WV7b8SFTpkwk3TEIqUv2uEMgp8htwKQV1+dTPHYEjzz7NpteqKO4aDT5+cNdliApPTNILbBKZvAum0sSmVk2il31X3L3',
        'w7U4HIOYPnWSxxLkAC+bZwGbhqeauXl2Dro5lpm/fZmgIBNLq+ei6zqSJHvigqYF1jQavKbSISaB1QtSiU5O5u6N9exv/JIry0pIT7O7rKF7VUkNwFmk96LK',
        'rNERjCkQefvTUzzyYgODB9uYVjGBLneGUGTFB0KggPBZXnPGGJh/aTIRKfFU17yHouosXjSD0JAQJA+EwLMGn4VWgwBLC6Kw5yTx0t7jbN/TSvawdCZNGOOK',
        'Cz6VY+BYwxlL7qMSDJQVJRNjT+T61XUAXDOvnKjIcGRZ8coSesC4xRmbLyYD3FQcgy0nhd0NMk+8dIBEazyzKy/zZAnveYSu0+dB9LoNlz/IyIyJIkNys5h1',
        '+z9p7+hkTEEuF43KQZbd6dJdQgcCiO/ckL1jagJZI3LQIxK5qupJAKaWl2JLsbgXWVQvEL5u0ddgfOfWvC3eyJqluVgcBTxX10LNfS8gCAJzZ17u2pLzcovu',
        'bKFpvhD6CoyzNmmUjUui+uoSkrPyuPnerWx54e+YzcFMKy8lLi4aRekdRJ9v0gDfdp07FhVSdlkJA4aN5sbqDWx9cQuCIFB+RQmDbFZURUVRu11DP22jFqD7',
        's0f+BuVsjVtLgAcAHrprCuPGjcUkjuCWG9ey+fm/ADAmfzh5o7ORFdf+pCdGePcs6f5vEd+vhc8gsP6uKRQXFYItl3U1W3lgzR8BsNsSKcwfTqIlDkXVXIux',
        '7v5FbxhntjD6D5BzauZceUMRV15WhCl9BDtqT3H3itv59JOPAcgemsaFIzKJjYnypM6erIGnjc+nn9FPOJxzW29lqY1l84sIiskkKO0i7l+1hT89tNZzbbrD',
        'RnraINIGpxAcHOzauwSfGNEtfzGGc2nwXgw8CDDEZmTxlVays7NJzC7g4xMD2bTxdV7cspVvOjsBCA4OIiVpIMmJCUTHRBITFYE5JBhN1xkQFopB8G3w/n/y',
        '+CGt/g8DaQCTc0xMK7KQYBdx5GRzIjGVzc9/zKvb9/Le3nq6urp6vU9QkAlB6GNd7t5yitYI4H7chz4GBME4u4GSYeFYbLEkiMmk5mZxIiaW2p3tNHz0Fc2N',
        'x2hqPEJn5ykMBgNHPz/mN64A5/H4j1GAoREaWQNNjEwNJTTKzICB4djERFKSBiJER4I5GIxGEISelx/oJzkIZkYnyahhNYMl3EhipJHYAUaCTQL4z9g96j8S',
        'yHmE4C2vw6FOYCgg8nM4HNqX1Q+BfghAPwSgHwLQDwHohwD0QwD6IQD9EAD4LwTyZy9O1Y31AAAAAElFTkSuQmCC',
    ].join(''),
    nameCenter: 'data:image/png;base64,' + [
        'iVBORw0KGgoAAAANSUhEUgAAAPQAAABECAYAAAC25RiLAAAACXBIWXMAAAsSAAALEgHS3X78AAAAG3RFWHRTb2Z0d2FyZQBDZWxzeXMgU3R1ZGlvIFRvb2zB',
        'p+F8AAAByElEQVR4nO3dSWoUQBiGYf9T9EbSKNgLiYKH8QTOU9Q4JmmTjj1PSTqT6MKAGxXvJHiFXKC9RCDw8TwnqM3LXxRFVbWajfMrQIRqNRvLy14EcDFq',
        '+XckaAhRf9ZuCxpC1Op1W25IUbdaK4KGEPXzbC5oCFG/zmaChhD142tP0BCivh93BA0h6tuiLWgIUV/mHwUNIep09kHQEKJOpu8FDSEEDUHqePJO0BBC0BCk',
        'jsZvBQ0h6mj0RtAQog4FDTHqcLguaAhRC0FDjFoMXgsaQtTB4JWgIUQd9AUNKWq/91LQEKL2BA0xaq+7JmgIUfPuC0FDiJp/FjSkqNnuc0FDiJrtPhM0hKhp',
        'R9CQoiadp4KGEDXZETSkqPH2E0FDiBpvPxY0hKjRJ0FDihq1HwkaQtSw/VDQEKKGW4KGFDXYeiBoCFH9TUFDiupv3hc0hKjehqAhRfU27gkaQpjQEKSGbYdi',
        'kKKm7nJDjNrveuAAUtTJ2M8ZkKJOxz6rgxS1euOqoCFE3bl5TdAQopb/dgQNIer33RVBQ4hqNRuChhDVajbOL3sRwMWo5dKAhhSChiCChiCChiCChiCChiCC',
        'hiCChiCChiCChiD/AeiEp9WOQjGAAAAAAElFTkSuQmCC',
    ].join(''),
    nameRight: 'data:image/png;base64,' + [
        'iVBORw0KGgoAAAANSUhEUgAAAEEAAABECAYAAADeOlj2AAAACXBIWXMAAAsSAAALEgHS3X78AAAAG3RFWHRTb2Z0d2FyZQBDZWxzeXMgU3R1ZGlvIFRvb2zB',
        'p+F8AAAVg0lEQVR4nNWbeVxUVf/H33fuzAADCLJvAooICKJiai6YlVpapj497dlmi1pPmo+Zlpm2ipqWS2ruK+5rSZqSuBKaCig4KHAR0AFkG4ZhBmb5/TFA',
        'Ymr1q4fw++Iy3LnnfM45b77n3LMKHYK8tfxzVgdcBa4AGfXXQbWkyW/OTAgdgrytzZngH7A64CywB9iuljQX/9cJCtacmc0LQSYDQQCZgMEMBeUGcov0XMqq',
        '4EJ6PqdSMqmsqLoxxi5gsVrSHPhfZUnY+nZ0M3uC0PCDIMqwc7RD5eZM27b+BAf7Y/Xz5nBaKQcSLrLvuxMYDLUNEQ8AX6glzeG/PUdR7Zq3OgiAkxKsgFIE',
        'd5WAp7OIr7sdUcFOuLR2opWfFz1iIqnw9WXD+gzWrTlARXmjd6wExqslTdVtE/mzeerUIbCZPcGK0Vh7yycKEYLdZUQH29OvsxdebQIIi4zEHNqB5UtSWPrN',
        '5oagl4E31JIm8e/IkRC/6stmh1BVpQMEagxGSkpKKC2t4MqVAvLy8qmtrQNAKYfYKHsejfUjNDyS0G69ySxz5fOPV5CSfLpB7C21pFn0V3MkbFo1p/kgCDf9',
        'KQgICAiCgCCzQcnMvExa2gWysrIbwz7xgA+vPdUTJ89I3GPuZU7cHpYtXt7weK5a0vz3L2Vr7dJPm9ETmlJoAAAgyAQEQYZMZrsKCzUcO55CenomAEG+zkx8',
        'uSd9evXAMeoedu+8wqQJ4xvU/hIIYeXC6c1bHYSbb+tBCAKyehBCPQhRFJGkfPYfSKKg4CoAbz7Xk7EvD0UeHE1ySi2vjHwKs9kMfwGE8O3XU5v97dDk7gaP',
        'aLxkQr1HiMhEEblczoEDSRw5mgzA8AGRxE19AXzCSDlnZOTTTzUIjlVLmsV/Ok+Lv5zc7D3Ghipwwxe2foNwo1fIkAkyZKKIKIqIcjkXMrLYsmUPAEPu68Ds',
        'D59D7hXG3sRCJo5vdIJeakmT/Kfy882c9/7xbnNju3DDpyCTIQi/eoRcLiLKFRQUXGPFynisVitD+gbxyX9H4BQYzZzlSSxbvAagCGirljQ1fzj9RbMn/eMQ',
        'Guw3MGQyZA0g6j1CrlBQeLWY5cs3APDCw/68/FR//CJ68PjrCzmfdglglVrSvPKH021JEBqsiUfUe0VDQymKIgqFksyL2WzeshuA6c970a1nT1Se7RjwzHys',
        'VitAd7WkOX37VG5Ib+Gsd1schAZr2lj+CkIul6NQKPjx0HGOH0+hlb3A58/749U+gh9Pl7Js2xmAk2pJ0/sPpdOSIcCtQcjlcuQKBQqFgoWLVlNSUsq9gSJP',
        'DwzEr0NbXph2BIPRBNBXLWmO/24aC+ImtmgI0BREY7WQy1EoFZSUlLNk6ToA3rzXifCYQH7OqWX9D5cBDqglzUO/q79g5n9bPARo2j6IMhmiXEShUKBQKonf',
        'vBe1OpuOrgJP3+eLV4g3b32dislsAQhRS5qcO2rPv0sgwK8gGt8U9d5QUVnFwoWrkQnwTh9XgqJ82XriOkfTSgCmqiXNZ3fUnf/FhLsOgiAITaqEUqlkzbrt',
        '5ObmM7CtkodjAyg0iMzdcgngrFrSxNxR9+u7CALQFIIoolDaqsTFi9ls3fY9Aa1kTHjMH2dfD96en0aN0QwQeKfJW+Hrz9+5KyEIMhlyuYhcrkChVCAIAnGz',
        'l1BXZ2LeqGA82/gyb/Mlfsm8DjBSLWnW31bzq8/H31UQgF/fEvJf2wWl0o4NG3dx+XIebw1rQ/8+EWz/KY/4BDXAfLWkGXdbva8+u3shyERZY8dJaack6UgK',
        'SUd+5t/3+/Ly4/eQfL6MT5YcB0hQS5oht9Wb9+m4uw4CcEN/QUTZ0C5k5bBtWwKxMb7MGP8w+UUGXnw3HuC8WtJ0up2WMPcuhnCzJ1RUVLHom7V0DPFk9eyR',
        'GOsEHnz+K2rrzGVAwO1GlsLcT96+qyHI6hdz7JRKdNV6Fi/dSGiwJ9sXvgiCnN5PzEOnN+oBP7WkqbyVlvDlJ/+56yDo9Qayc/IpKi6lqPh64ww1QG1tHY4q',
        'eyaPGUTfboE8PnYlZRV6LeChljR1t9ITvvz47oFgNNbyy9kLZFzMaZhXRCaT4eCgagxjMtVhNBob72UyAYvFqlNLGufb6QpzZrx1V0DIL9CQdOwUer0BgAGD',
        'BvPAgEH0jr0PX1+/xnDV1TpO/ZzMmdMpbNu8kdLS62Bb8DoCjFFLmsybtYU5M95s8RDUWRJJx2zzI31j+zNu4ntEd/YF7XkMRflYTdVgNQMCiEpkdm7YBXcA',
        '2rFr+w/MnzebwoJ8gBrgCbWk+f5GfWH29JYN4Ur+NfYftE0J/Gf8ON4aPwyd+hR1Wom9P/5CStpV1HkVWOtL4eVmR3hbNwb0bk/HiDBcgzpSbR/BB+/NJuG7',
        'PQ2yw9SSpvFGmDV9bIuFUFNjIH5LAhaLhQmTRvHGk50pPJ/C0ZPnWLYzm4LiO8+l9opy4cVh4UREdcenZw/ivkhg5bJ4sFWPMLWkuQQgzPqo5UI4cOgEV/Kv',
        'MfChrsz5zyByM0+y/Yd01h0qbwzj7u5KWEQoSqUdUm4eUm7TcZIog7FD3Rl4X1c6dOrFlCWH2bn9KMAFtaSJAhDipo1pkRDKK7Ts2H0QuVzGgeUvUnwli8Rj',
        'mXx7oAyAoGAvxk98jiGPRoNZCxYrKBy5klfH2lX7Wbd6dxO9V+53YuB9UYR37saQMeu5ZusyjFFLmiVC3LTRLRJC8ql0LmRc5rH+7Xjyfl80OVeYvL4Ak9lK',
        'dJQ/q+LHYMnIIvXUOa4XlVNjtODr5YiTlzfde3UltVrO6y8toaJC36g56RFXImPCKKtV8U7cTwClgLcw88OWCWHn3kOUl2uZ+lIE3nZmEk5eY19aFSp7Ocf2',
        'jsURCxs2HGLZzksUa02YraBSCsT4Kxje2wu/8GAcXbwYPm5HY6Pp5yzwzqN+eIT4M2NlJtLVKoARwsypb7Q4CAZjLZu2JmCnEIh7KZQarZ6v9xdxtbyW5x9u',
        'z4dj+oOzI8cS03hn7lG0elOT+EGucl7p64ZPB1/2n6lkx2Gp8dlL3Zzo2tWXo5f1bDtcCLBK+GLq6y0OQnmFll17f8KrlYKJQwOwmCxM3pwHwLQXwujRKZC2',
        'QX7U1BgpLCrnmWmH0BtMjBzemaryKnYl5dA72IFH7/FAdHVkygo1lnp36Oyt5Mm+XpRbRObszANIF774oIVC+O4nvFvJGT/YH1EhY9L6XACmDfFGLpdh52yP',
        '7U0nELe3gDJtHeqdr2KoMdBt5AYsFivTh/nh0Mqe2bsLKK60bRHycZYz9gEvFA5KpmySAHTC5x+81uIgaLU6duxJpLVKZMqINji4qJiwNJM6s5UXIpW4ykzI',
        'RBkACpUdc5Ntjd/u2QMoKa1g2rJ08ksMfPxMMCqljG8PXCPrmq1P4eYkZ/LwAASZwLtrcwGqhc/e/2chZOfkk3U5j/IKLRazBQRQOdijrzFgtViY/lQAASG+',
        'LN6ZzamMMtxVAo+GmHG3t1JjlbM/T05OSR3PDg5l4D1uaItKeX9NLtVGM0vf7UKtTk/c5lwKymwDSE8XJR+/3AGLycSbCy8C6ITP3n/1H4Ggq9Zz6KcUKirv',
        'vBMvqo0dU17rSZXZidEz9jV+7+IgUGWwYrFCt0hfpozqRkVBDsfPFbEqsZTI9u5MH92TsgKJtxZmUmeyFTPQx5FZb/fEWF3Fi5+dAtALn05pfgharY6EH49j',
        'NNbSLqQtL416hCFDQ3CS12CRy0lLv87mDSfYuf0YAH2j3fh08rOs3HeetesPN9F68bFI/j2gHUbtVZLTrjFni21bz9ZFr6KSadm3/wiLdmkaw/fr3pYpr/Ul',
        '57KaN2elAOQIn04Z1ewQ9u0/xvXSCu67/wEWfTsVRdlJpLOnKCwowVGpwMvdGb8AH44V6Hn97XjMZgsjH4tm6odjmP7Vj8Rv2NGoNWJQF5SiiZPn8rlyzTZx',
        'NO3jN3ikqz86TSovfrCvyRjjvbFDeaRfCN/vP0rcql8Adv0pTzBbLGg01ykqLkWn02MwGhFFEUeVAy4uTgT4eePs7HhHjeycfI4nn8M/wI/EY5swXD7DgQMJ',
        'XM4tIti/Fc52cFGdTxsPJd2i21NQbuWlj2zVYNP8V+k6ZAgHEytZv2YlKcknGidXALrGxPDWO2Po28kBnZTK5wt3s/1gVuNzUZRx4rsvkZvKmPjJOn5KkQDG',
        'C19M/f2G0WQycz7jMhcyc5pMZd3K2vh7E9MlHDc3l1s+/zExmYLCYmZ+NIrhg7pz+OjPrN6cxKyPX8K7UwRQRNrWH4jffZoe4a6Eh/iycEcWB08V0j06kNVz',
        '30Bw8EX0j6akWEStVmO1WGgXEox/gAJzYTpWfSGfL9jBhj1nm6T9xtiRjHuuP9ribGKfiKPOZFudEmb/ztih8FoJh5JSGrfiduocRWy/+wkMDiUoKBidTkdO',
        '9iUyLpzn+z27MJlskLy93InpHE6An1cTmBu3JmCxmNm3/HU8PD15bvxKnh7el2EP98E+oBXIa0BSE7/pMBcuaugT4UJBeS1zttr6CRHt/XhzZCwD+0WDaA8y',
        'JSCAxQhmAz8mnWXF5uOczbwGoAXMQGsPT0+STq5Dfk3NjNlr2LjnDMB3akkzVJj3ye0nVVLPX+JEcioAvfp05NXR99M32h2KdBSV1SLKVTi6+uAQ3B7k7bl6',
        'tYYNa1eyfUs85WW20Z5b61bEdAknNCQQi9XCyrV7UMisrP/0ATz8Q+n37DfM/uAphj4Qjl5bSVW1Dm1lBWUl19j4XRb9Qh0RgcnxEqIoNrq/Z2sHOod54ubi',
        'gCAIVOoMnMksprhUD2AB1PXFCJLJZKpd+9bTXnWdvEtnGDxqdUMRo9WSJl1YFHfrFagLmdkc/CkFgHHvDGJsbBuOJp+lNP86UnEVBVUWdHUCrV1dCO8QyLDB',
        'sbh6BoJ/O2r0PmzdtJt1a1ZwJU8CoLWrM50i23MmVY1BX8Oc19rRvmtPJn15mJwrJZzcMRF70Uh1eTHlpVcpL9exdJfEkG5uGCv0vL/lCgqFnLEvPMi36w9R',
        'YzTdnGUjkA2UAGVAOyBSoVDIV2+YT2e3CgzFqTw5cQ85V/VgO0MxFkD4Ju63C7Il18vZsCUBgMlTn2VomIXjCSc5ebqIIwW1lOotv4HmpFIyYmAnXnqyHz7+',
        'bZC7eUGrNuzansT6tZtIT81o3JBltVoZ2smJEUNCCInuwwtTtlFZUc3wh7rx8r+icbSUcFkqYMHWbMaMaM+17ELeXZuLvVJGZDsn0rN11NZZrMA8QALCABGw',
        'ry98FOAW3TmKT2a+QoBRouLKcT5cksaJjDqAfGzb/MwAQlRowG8gmEwmLBYrw/71CLPGP0Dqke0s2XCWxEzbq8bL3ZE+sWE4t3YmN6ecE8cvYDbVT4ELAsMG',
        'RPLMsO6EBvmiULmgCAlgf8J5tmz8gWNHbY2VnVzG+Ptb0yEmgC739ifhdDEGXS3PP9YFq6mKSTP34uHuzPBYbxIPJPP19yW3ctg0bAMIR6A14A4QGOTPy68O',
        '5tkRfuQmnqA8V82cHYX8kmcB0AGdb9y9InTt2LYJBLPZgsFgQOXoQPLpjZivZLBy9SYWxKejVIjMnPYoj/RuC9oqkIvg58F1iy/fLj7EmpW7muRwYJ8QhvcP',
        'JSrcG6vcGd/IAFIyili/JpH9CakIAozs2Zoe0a3w8PPA09cPB5c2fLL0BIVFlSyfPRqd5jQTPt3NuVxbw+zoaEf9uRlAwMlZhZOTCmdnFV1iQojp5sVD3V3I',
        'P3GGwsxczp4vJv6skaJqAK4Dg2/e2idY8z5qAmHmohOs2pzGqOce5N0x/6a8rIhej01HFGV8t20i8pIszh05h1pditZoIbytMyHRgdw7aAB7z1QxcdwC239a',
        'KWIyWzGbLXSP9GZIb29iwl2wim5ERPiTqalh3vyDJP2Sj7ODSLC3PQo7OzQVFtoE+7P4m6mo9MXs27OHCbMPYaeQ8cPyEfgFudvOUNnqle2ztg6q9Vwv0JCV',
        'KaEtrORyro4jebWkVsoaipYCPKuWNNncZMLmJ349+VJcY2Vrdh0avZVvpz9E964RLNh4hpWbjzB39jh6hDiSdSqBWavSuaixNUxyGfT2F3lySBBRffqy5XgR',
        '3yzdT5CzjE5uMhILzejr++3tA1QM7OZAr1AvzHZOhLYNpEJnZv33GZRWGXFysuOJf8XSNbw9hho9qRk5jJps6zE+FCDS10/+m/pgtdp+1dZaKagwkVtuIlsP',
        'BWbxxmBxakkz+TeRGyDc7kjg9mmdcPf35akPTqKtruVc0gpqKq/xzOhZZGb/tn728Rd5ZmgwIV278fTEvVRW3X463NtVRr8OdnQJboWDgyN2rircXFuhqzGA',
        '2Yi9wkximpZV+67eVuMPWBUQDyxSS5q0OwUUbjocKgccAFaOaoNbsDvDPzxH33tCWbHgfXL0VoYNHs2QoYMpLsrnxLEzTcTeHehOeEwg6xJLOHyqAMCA7Zzj',
        'jfp2gAxApYRIL4HWSjlyBKoNZrS1Zi5XQdWvy4l19Tq/Z1bgIja3PwvsVEua8jtHqYdgtf7qCGHBPoOBbYDqq2Gt8Qr15vm5amIivFj19QS0LrFYrY54enkD',
        'sH3Lp7w/aWFj/GFdXHmgpzdJGZXsOKoBmKGWNNNvTjQs2OdB4CMg9g55ywDeVkuaQ3+kIH/FbobQBxsEnwkxCiJ7eDPvhwoy86tJXPoIPh0Hgt+DgCtQACW/',
        '8OTIz0i9aHPbJ/t4cU87Rw6dLWX/eS3cBsIN6XUGhgFB2LzEhO29v08taX75XxT4VnYzBCW2c4fPDfKwMuheZ4yu7nywPo83BnnwxCAv3IJ6oHJrD3Ilel0F',
        '78zYQlLKZQAWTuhGea6G+KNFZFw3AbyuljTLmqsw/19rAgEgLNjnOWC9m8zCGxEigTFubE+Hg+dKeX+YK7FdwSOwC05eEZQZVQwauZBqvZHRrw+kfzsT539K',
        'Je7HMuosTdf7WrL9BgJAWLBPKhDd297EwGgHInsFsvpkNftSNPSKaMWgez2otnoyb90pzGYLwx8fTtzUh0n7YSMLVp7kyKU6+J0dYy3JbgdhKLZT6wxxrKV7',
        'pCOdegdRJLjx7b4cpGs6KrUGOnfpyAuvjObRxzqhv3iU/Xu3MnlReoPMgOZo1P4OuyUEgLBgn8+BKQB9lHX0bafAO7wVXe+NwC+sHfh5g8IbQ3EddnIrew+e',
        '5r247xoWOe7YOWlpdlsIAGHBPnHAJAAvwUy4g5WOHjIC/Bxo7avCwVNJVmUdhy9A8vmyhmiNQ9S7xe4IASAs2GcYsAzw/B2tOmxL3Sv+prw1m/0uBICwYB85',
        '8Gz91Q1wxjZ7YwLSga3Air/zOH9z2v8B5RcFHi5me34AAAAASUVORK5CYII=',
    ].join(''),
});
const CLASSIC_DIALOG_ASSET_META = Object.freeze({
    dialogLeft: Object.freeze({ width: 110, height: 184 }),
    dialogCenter: Object.freeze({ width: 744, height: 184 }),
    dialogRight: Object.freeze({ width: 110, height: 184 }),
    nameLeft: Object.freeze({ width: 65, height: 68 }),
    nameCenter: Object.freeze({ width: 244, height: 68 }),
    nameRight: Object.freeze({ width: 65, height: 68 }),
});

__igsDefine(exports, "CLASSIC_DIALOG_ASSETS", () => CLASSIC_DIALOG_ASSETS);
__igsDefine(exports, "CLASSIC_DIALOG_ASSET_META", () => CLASSIC_DIALOG_ASSET_META);
});
__igsRegister("src/visual/igs-ui/settings-shell.js", function(module, exports, require) {
const SETTINGS_SHELL_TEMPLATE = `
<div class="igs-settings-shell" role="dialog" aria-modal="true" aria-label="设置">
  <div class="igs-settings-head">
    <div class="igs-settings-title">设置</div>
    <button class="igs-settings-theme-toggle" data-action="toggle-settings-theme" type="button" aria-label="{{settingsThemeLabel}}" title="{{settingsThemeLabel}}" aria-pressed="{{settingsThemePressed}}">{{settingsThemeIcon}}</button>
    <div class="igs-settings-head-spacer"></div>
    <div class="igs-settings-badge">{{version}}</div>
    <button class="igs-settings-close" data-action="close" aria-label="关闭">×</button>
  </div>
  <div class="igs-settings-tabs">{{tabs}}</div>
  <div class="igs-settings-body">{{body}}</div>
</div>
`.trim();
function getSettingsShellTemplate() {
    return SETTINGS_SHELL_TEMPLATE;
}

__igsDefine(exports, "getSettingsShellTemplate", () => getSettingsShellTemplate);
});
__igsRegister("src/visual/igs-ui/settings-style.js", function(module, exports, require) {
const SETTINGS_STYLE_TEXT = `
#igs-unified-settings{--igs-settings-paper:#16181a;--igs-settings-panel:#1f2225;--igs-settings-surface:#1b1d1f;--igs-settings-field:#282c2f;--igs-settings-line:#2f3336;--igs-settings-line-strong:#4c5255;--igs-settings-ink:#d8d5cf;--igs-settings-ink-2:#bebbb4;--igs-settings-ink-3:#8c9093;--igs-settings-ink-4:#6a6e71;--igs-settings-accent:#cfccc6;--igs-settings-danger:#b08c8c;--igs-settings-highlight:#2b2f32;--igs-settings-vleft:0px;--igs-settings-vtop:0px;--igs-settings-vw:100vw;--igs-settings-vh:100dvh;--igs-settings-width:min(760px,calc(var(--igs-settings-vw) - 48px));--igs-settings-height:min(760px,calc(var(--igs-settings-vh) - 48px));position:fixed;left:var(--igs-settings-vleft);top:var(--igs-settings-vtop);width:var(--igs-settings-vw);height:var(--igs-settings-vh);z-index:2147483200;display:flex;align-items:center;justify-content:center;padding:24px;box-sizing:border-box;overflow:hidden;background:rgba(9,10,11,.78);font-family:-apple-system,BlinkMacSystemFont,"PingFang SC","Segoe UI",sans-serif;color:var(--igs-settings-ink);color-scheme:dark;-webkit-backdrop-filter:none;backdrop-filter:none}
#igs-unified-settings[data-igs-settings-theme="day"]{--igs-settings-paper:#f1f1ef;--igs-settings-panel:#f7f7f5;--igs-settings-surface:#fbfbfa;--igs-settings-field:#e8e8e5;--igs-settings-line:#dededa;--igs-settings-line-strong:#b4b4af;--igs-settings-ink:#1a1a1a;--igs-settings-ink-2:#4e4e4c;--igs-settings-ink-3:#85857f;--igs-settings-ink-4:#ababa5;--igs-settings-accent:#202020;--igs-settings-danger:#8e7a7a;--igs-settings-highlight:#e4e4e0;background:rgba(254,255,255,.61);color-scheme:light}
#igs-unified-settings{--igs-settings-radius-shell:8px;--igs-settings-radius-control:6px;--igs-settings-radius-small:4px}
#igs-unified-settings,#igs-unified-settings *{box-shadow:none;filter:none}
#igs-unified-settings,#igs-unified-settings *{scrollbar-width:none;-ms-overflow-style:none}
#igs-unified-settings ::-webkit-scrollbar{display:none;width:0;height:0}
.igs-settings-shell{width:var(--igs-settings-width);height:var(--igs-settings-height);max-height:none;background:var(--igs-settings-panel);border:0;border-radius:var(--igs-settings-radius-shell);box-shadow:none;display:flex;flex-direction:column;overflow:hidden;-webkit-backdrop-filter:none;backdrop-filter:none}
.igs-settings-head{height:54px;display:flex;align-items:center;gap:10px;padding:0 14px 0 20px;border-bottom:1px solid var(--igs-settings-line);flex-shrink:0}
.igs-settings-title{font-size:15px;font-weight:500;letter-spacing:.08em;flex:0 0 auto;color:var(--igs-settings-ink)}
.igs-settings-head-spacer{flex:1}
.igs-settings-badge{font-family:ui-monospace,Consolas,monospace;font-size:10px;color:var(--igs-settings-ink-4);border:0;border-radius:0;padding:3px 0;letter-spacing:.04em}
.igs-settings-theme-toggle{width:32px;height:32px;position:relative;left:-4px;top:3px;display:inline-flex;align-items:center;justify-content:center;padding:0;border:0;border-radius:var(--igs-settings-radius-control);background:transparent;color:var(--igs-settings-ink-3);cursor:pointer;transition:background-color .14s ease,color .14s ease}
.igs-settings-theme-toggle svg{display:block;width:11px;height:11px}
.igs-settings-theme-toggle:hover,.igs-settings-theme-toggle:focus-visible{background:var(--igs-settings-field);color:var(--igs-settings-ink);outline:none}
.igs-settings-close,.igs-settings-action{border:0;background:var(--igs-settings-field);color:var(--igs-settings-ink-2);border-radius:var(--igs-settings-radius-control);height:34px;padding:0 12px;font:inherit;font-size:12px;letter-spacing:.04em;cursor:pointer;transition:background-color .14s ease,color .14s ease}
.igs-settings-close{width:34px;padding:0;font-size:18px;letter-spacing:0}
.igs-settings-close:hover,.igs-settings-close:focus-visible,.igs-settings-action:hover,.igs-settings-action:focus-visible{background:var(--igs-settings-accent);color:var(--igs-settings-paper);outline:none}
.igs-settings-danger{color:var(--igs-settings-danger);background:transparent}
.igs-settings-danger:hover,.igs-settings-danger:focus-visible{background:var(--igs-settings-danger);color:var(--igs-settings-paper)}
.igs-settings-tabs{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:2px;padding:8px 12px;overflow-x:auto;flex-shrink:0;background:var(--igs-settings-surface);border-bottom:1px solid var(--igs-settings-line)}
.igs-settings-tab{box-sizing:border-box;width:100%;border:0;background:transparent;color:var(--igs-settings-ink-3);padding:7px 4px;border-radius:var(--igs-settings-radius-small);font:inherit;font-size:12px;text-align:center;white-space:nowrap;cursor:pointer;transition:background-color .14s ease,color .14s ease}
.igs-settings-tab:hover,.igs-settings-tab:focus-visible{background:var(--igs-settings-field);color:var(--igs-settings-ink);outline:none}
.igs-settings-tab.is-active{background:var(--igs-settings-accent);color:var(--igs-settings-paper);box-shadow:none}
.igs-scene-subtabs{display:flex;gap:2px;margin:8px 0;padding:3px;background:var(--igs-settings-field);border-radius:var(--igs-settings-radius-control)}
.igs-scene-subtab{flex:1;border:0;background:transparent;color:var(--igs-settings-ink-3);padding:7px 10px;border-radius:var(--igs-settings-radius-small);font:inherit;font-size:12px;white-space:nowrap;cursor:pointer}
.igs-scene-subtab:hover,.igs-scene-subtab:focus-visible{color:var(--igs-settings-ink);outline:none}
.igs-scene-subtab.is-active{background:var(--igs-settings-accent);color:var(--igs-settings-paper);box-shadow:none}
.igs-scene-settings{min-width:0}
.igs-scene-settings-subtabs{position:sticky;top:0;z-index:2;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:2px;margin:10px 0 12px;padding:3px;background:var(--igs-settings-field);border-radius:var(--igs-settings-radius-control)}
.igs-scene-settings-subtab{height:32px;min-width:0;border:0;background:transparent;color:var(--igs-settings-ink-3);border-radius:var(--igs-settings-radius-small);font:inherit;font-size:12px;white-space:nowrap;cursor:pointer;transition:background-color .14s ease,color .14s ease}
.igs-scene-settings-subtab:hover,.igs-scene-settings-subtab:focus-visible{background:var(--igs-settings-highlight);color:var(--igs-settings-ink);outline:none}
.igs-scene-settings-subtab.is-active{background:var(--igs-settings-accent);color:var(--igs-settings-paper);box-shadow:none}
.igs-scene-settings-subpane{min-width:0}
.igs-reader-settings{min-width:0}
.igs-reader-subtabs{position:sticky;top:0;z-index:2;display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:2px;margin:0 0 12px;padding:3px;background:var(--igs-settings-field);border-radius:var(--igs-settings-radius-control)}
.igs-reader-subtab{height:32px;min-width:0;border:0;background:transparent;color:var(--igs-settings-ink-3);border-radius:var(--igs-settings-radius-small);font:inherit;font-size:12px;white-space:nowrap;cursor:pointer;transition:background-color .14s ease,color .14s ease}
.igs-reader-subtab:hover,.igs-reader-subtab:focus-visible{background:var(--igs-settings-highlight);color:var(--igs-settings-ink);outline:none}
.igs-reader-subtab.is-active{background:var(--igs-settings-accent);color:var(--igs-settings-paper);box-shadow:none}
.igs-reader-subpane{min-width:0}
.igs-settings-body{flex:1;min-height:0;overflow-y:auto;padding:16px 20px 18px;background:var(--igs-settings-panel)}
.igs-settings-grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:12px 14px;min-width:0}
.igs-settings-section{display:flex;flex-direction:column;gap:10px;min-width:0}
.igs-settings-field{display:flex;flex-direction:column;gap:6px;font-size:12px;color:var(--igs-settings-ink-2)}
.igs-settings-field em{font-style:normal;font-size:11px;color:var(--igs-settings-ink-4);line-height:1.5}
.igs-status-hud-tables{display:flex;flex-wrap:wrap;align-items:center;gap:8px;min-width:0}
.igs-status-hud-tables>em{display:block;flex-basis:100%;margin-top:2px}
.igs-table-pick{min-width:0;max-width:100%;height:32px;display:inline-flex;align-items:center;gap:7px;padding:0 9px;border:0;border-radius:var(--igs-settings-radius-small);background:var(--igs-settings-field);color:var(--igs-settings-ink-2);font:inherit;font-size:12px;cursor:pointer}
.igs-table-pick>i{width:12px;height:12px;box-sizing:border-box;border:1px solid var(--igs-settings-line-strong);border-radius:var(--igs-settings-radius-small);background:transparent;flex-shrink:0}
.igs-table-pick>span{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.igs-table-pick:hover,.igs-table-pick:focus-visible{background:var(--igs-settings-highlight);color:var(--igs-settings-ink);outline:none}
.igs-table-pick.is-on{background:var(--igs-settings-accent);color:var(--igs-settings-paper)}
.igs-table-pick.is-on>i{border-color:var(--igs-settings-paper);background:var(--igs-settings-paper)}
.igs-table-pick.is-missing{opacity:.66}
.igs-settings-field input,.igs-settings-field select,.igs-settings-field textarea{width:100%;box-sizing:border-box;border:0;border-bottom:1px solid transparent;background:var(--igs-settings-field);color:var(--igs-settings-ink);border-radius:var(--igs-settings-radius-control);padding:8px 10px;font:inherit;font-size:13px;line-height:1.5;outline:none;transition:background-color .14s ease,border-color .14s ease}
.igs-settings-field option{background:var(--igs-settings-panel);color:var(--igs-settings-ink)}
.igs-settings-field input[type="color"]{width:42px;height:36px;padding:3px;border-radius:var(--igs-settings-radius-small);cursor:pointer}
.igs-settings-field input[type="color"]::-webkit-color-swatch-wrapper{padding:0}
.igs-settings-field input[type="color"]::-webkit-color-swatch{border:0;border-radius:var(--igs-settings-radius-small)}
.igs-scene-preset-select{box-sizing:border-box;border:0;border-bottom:1px solid transparent;background:var(--igs-settings-field);color:var(--igs-settings-ink);border-radius:var(--igs-settings-radius-control);padding:8px 10px;font:inherit;font-size:13px;outline:none;height:38px}
.igs-scene-preset-select:focus{border-bottom-color:var(--igs-settings-line-strong);background:var(--igs-settings-highlight)}
.igs-settings-field textarea{min-height:132px;resize:vertical;line-height:1.55;font-family:ui-monospace,Consolas,monospace}
.igs-settings-field input:focus,.igs-settings-field select:focus,.igs-settings-field textarea:focus{border-bottom-color:var(--igs-settings-line-strong);background:var(--igs-settings-highlight)}
.igs-settings-api-group{grid-column:1/-1;display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:12px 14px;min-width:0}
.igs-settings-api-group.is-disabled{opacity:.48}
.igs-settings-api-group.is-disabled input,.igs-settings-api-group.is-disabled select,.igs-settings-api-group.is-disabled textarea{color:var(--igs-settings-ink-4);background:var(--igs-settings-surface);border-bottom-color:transparent;cursor:not-allowed}
.igs-settings-api-group.is-disabled .igs-settings-action,.igs-settings-api-group.is-disabled .igs-settings-secret-toggle{cursor:not-allowed}
.igs-settings-api-group.is-disabled .igs-settings-field em{color:var(--igs-settings-ink-4)}
.igs-settings-secret{display:flex;align-items:center;gap:8px}
.igs-settings-secret input{flex:1;min-width:0}
.igs-settings-secret-toggle{height:36px;min-width:52px;border:0;background:var(--igs-settings-field);color:var(--igs-settings-ink-3);border-radius:var(--igs-settings-radius-control);font:inherit;font-size:12px;cursor:pointer}
.igs-settings-secret-toggle:hover,.igs-settings-secret-toggle:focus-visible{background:var(--igs-settings-accent);color:var(--igs-settings-paper);outline:none}
.igs-settings-model{display:grid;grid-template-columns:minmax(0,1fr) 96px;gap:8px;width:100%;align-items:center}
.igs-settings-model-row{display:contents}
.igs-settings-model input{height:36px;min-width:0}
.igs-settings-model select{grid-column:1/-1;height:36px}
.igs-settings-model select:disabled{opacity:.55;cursor:not-allowed}
.igs-settings-inline-action{width:96px;height:36px;padding:0 10px;white-space:nowrap}
.igs-settings-action.is-active,.igs-settings-inline-action.is-active{background:var(--igs-settings-accent);color:var(--igs-settings-paper);box-shadow:none}
.igs-settings-action[disabled],.igs-settings-inline-action[disabled]{opacity:.55;cursor:not-allowed;pointer-events:none}
.igs-segmented-field .igs-settings-field{width:100%}
.igs-segmented{--igs-segment-pad:clamp(0px,calc(2cqi - 5px),6px);--igs-segment-gap:clamp(0px,calc(1.5cqi - 4px),5px);--igs-segment-font:clamp(9px,calc(5px + 1.5cqi),12px);--igs-segment-icon:clamp(8px,calc(3px + 2cqi),15px);container-type:inline-size;height:40px;display:grid;grid-template-columns:repeat(var(--igs-segment-count,3),minmax(0,1fr));align-items:center;gap:2px;padding:4px;box-sizing:border-box;border:0;background:var(--igs-settings-field);border-radius:var(--igs-settings-radius-control);position:relative;overflow:hidden}
.igs-segmented-indicator{display:none}
.igs-segmented-btn{box-sizing:border-box;width:100%;height:32px;min-width:0;margin:0;padding:0 var(--igs-segment-pad);position:relative;z-index:1;border:0;border-radius:var(--igs-settings-radius-small);background:transparent;color:var(--igs-settings-ink-3);font:inherit;font-size:var(--igs-segment-font);line-height:18px;font-weight:500;letter-spacing:0;white-space:nowrap;cursor:pointer;display:inline-flex;align-items:center;justify-content:center;gap:var(--igs-segment-gap);overflow:hidden;transition:background-color .14s ease,color .14s ease}
.igs-segmented-btn-icon{width:var(--igs-segment-icon);height:var(--igs-segment-icon);display:inline-flex;align-items:center;justify-content:center;flex-shrink:0;color:currentColor}
.igs-segmented-btn-icon svg{width:100%;height:100%;display:block}
.igs-segmented-btn-label{display:block;min-width:0;max-width:100%;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.igs-segmented-btn:hover{background:var(--igs-settings-highlight);color:var(--igs-settings-ink)}
.igs-segmented-btn:focus-visible{background:var(--igs-settings-highlight);color:var(--igs-settings-ink);outline:none}
.igs-segmented-btn.is-active{background:var(--igs-settings-accent);color:var(--igs-settings-paper);text-shadow:none}
.igs-switch{height:38px;display:flex;align-items:center;gap:10px;border:0;background:var(--igs-settings-field);color:var(--igs-settings-ink-2);border-radius:var(--igs-settings-radius-control);padding:0 12px;cursor:pointer;text-align:left;font:inherit;font-size:13px;transition:background-color .14s ease,color .14s ease}
.igs-switch:hover,.igs-switch:focus-visible{background:var(--igs-settings-highlight);color:var(--igs-settings-ink);outline:none}
.igs-switch i{width:30px;height:18px;border-radius:var(--igs-settings-radius-small);background:var(--igs-settings-line-strong);position:relative;flex-shrink:0}
.igs-switch i:after{content:"";position:absolute;width:14px;height:14px;top:2px;left:2px;border-radius:var(--igs-settings-radius-small);background:var(--igs-settings-ink)}
.igs-switch.is-on{color:var(--igs-settings-ink);background:var(--igs-settings-highlight)}
.igs-switch.is-on i{background:var(--igs-settings-accent)}
.igs-switch.is-on i:after{left:14px;background:var(--igs-settings-paper)}
.igs-source-filter{grid-column:1/-1;padding:12px;border:0;border-radius:var(--igs-settings-radius-control);background:var(--igs-settings-surface);display:flex;flex-direction:column;gap:12px;min-width:0;max-width:100%;box-sizing:border-box}
.igs-source-filter-title{font-size:12px;line-height:18px;font-weight:500;letter-spacing:.06em;color:var(--igs-settings-ink)}
.igs-source-filter-note{font-size:11px;line-height:16px;font-weight:400;color:var(--igs-settings-ink-4);overflow-wrap:anywhere}
.igs-source-filter-grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:12px 14px;min-width:0}
.igs-source-filter textarea{min-height:76px}
.igs-body-format textarea[data-path="bridge.virtualRegex.replacement"]{min-height:132px}
.igs-settings-row{display:flex;gap:10px;align-items:center;min-width:0}
.igs-settings-row > *{flex:1;min-width:0}
.igs-settings-result{min-height:18px;font-size:12px;color:var(--igs-settings-ink-3);line-height:1.5}
.igs-settings-result.is-ok{color:var(--igs-settings-ink-2)}
.igs-settings-result.is-error{color:var(--igs-settings-danger)}
.igs-settings-full{grid-column:1/-1}
.igs-settings-preview{white-space:pre-wrap;max-height:220px;overflow:auto;font-family:ui-monospace,Consolas,monospace;font-size:12px;line-height:1.55;border:0;border-radius:var(--igs-settings-radius-control);background:var(--igs-settings-paper);padding:12px;color:var(--igs-settings-ink-2)}
.igs-btn-mgr-list{display:flex;flex-direction:column;gap:0;border:0;border-radius:var(--igs-settings-radius-control);background:var(--igs-settings-paper);padding:4px 6px;overflow:hidden}
.igs-btn-mgr-row{display:flex;align-items:center;gap:8px;height:36px;min-width:0;max-width:100%;box-sizing:border-box;padding:0 8px;border-radius:0;border-bottom:1px solid var(--igs-settings-line);transition:background-color .14s ease}
.igs-btn-mgr-row:last-child{border-bottom:0}
.igs-btn-mgr-row:hover{background:var(--igs-settings-highlight)}
.igs-btn-mgr-row.is-hidden-btn{opacity:.45}
.igs-btn-mgr-handle{cursor:pointer;color:var(--igs-settings-ink-4);font-size:14px;user-select:none;width:18px;text-align:center;flex-shrink:0}
.igs-btn-mgr-handle:hover{color:var(--igs-settings-ink)}
.igs-btn-mgr-handle:active{color:var(--igs-settings-accent)}
.igs-btn-mgr-label{flex:1;font-size:12px;color:var(--igs-settings-ink-2);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.igs-btn-mgr-icon{border:0;background:transparent;color:var(--igs-settings-ink-4);cursor:pointer;padding:4px;border-radius:var(--igs-settings-radius-small);display:flex;align-items:center;justify-content:center;flex-shrink:0;transition:color .14s ease,background-color .14s ease}
.igs-btn-mgr-icon:hover,.igs-btn-mgr-icon:focus-visible{background:var(--igs-settings-highlight);color:var(--igs-settings-ink);outline:none}
.igs-btn-mgr-icon.is-on{color:var(--igs-settings-accent)}
.igs-btn-mgr-icon.is-on:hover{color:var(--igs-settings-paper);background:var(--igs-settings-accent)}
.igs-scene-url-input{flex:1;min-width:0;height:28px;border:0;border-bottom:1px solid transparent;background:var(--igs-settings-field);color:var(--igs-settings-ink);border-radius:var(--igs-settings-radius-control);padding:0 8px;font:inherit;font-size:11px;outline:none}
.igs-scene-url-input:focus{border-bottom-color:var(--igs-settings-line-strong);background:var(--igs-settings-highlight)}
.igs-scene-char-group{min-width:0;max-width:100%;box-sizing:border-box;margin-bottom:8px;border:0;border-bottom:1px solid var(--igs-settings-line);border-radius:0;padding:4px;background:transparent}
.igs-scene-time-group{margin-left:16px;max-width:calc(100% - 16px)}
.igs-scene-weather-row{margin-left:32px;max-width:calc(100% - 32px)}
.igs-scene-char-group:last-child{border-bottom:0}
.igs-scene-empty{font-size:11px;color:var(--igs-settings-ink-4);padding:8px;text-align:center}
.igs-mood-groups{margin-top:10px}
.igs-mood-groups>summary{padding:6px 4px;list-style:revert;color:var(--igs-settings-ink-2)}
.igs-mood-word-list{display:flex;flex-wrap:wrap;gap:6px;padding:6px 4px}
.igs-mood-word-tag{display:inline-flex;align-items:center;gap:4px;font-size:12px;padding:2px 6px;border-radius:var(--igs-settings-radius-small);background:var(--igs-settings-highlight);border:0;color:var(--igs-settings-ink-2)}
.igs-mood-word-del{border:0;background:transparent;color:var(--igs-settings-ink-3);cursor:pointer;font-size:14px;line-height:1;padding:0}
.igs-mood-word-del:hover{color:var(--igs-settings-danger)}
.igs-sprite-slot{min-width:0;max-width:100%;border-bottom:1px solid var(--igs-settings-line)}
.igs-sprite-slot:last-child{border-bottom:0}
.igs-sprite-slot-body{display:flex;gap:10px;min-width:0;max-width:100%;box-sizing:border-box;padding:6px 4px 10px;align-items:flex-start}
.igs-sprite-thumb{width:72px;height:72px;flex-shrink:0;object-fit:contain;border-radius:var(--igs-settings-radius-control);background:var(--igs-settings-paper);border:0;cursor:zoom-in}
.igs-sprite-thumb-empty{display:flex;align-items:center;justify-content:center;font-size:10px;color:var(--igs-settings-ink-4);cursor:default}
.igs-sprite-thumb-broken{position:relative}
 .igs-status-avatar-row{justify-content:flex-start;gap:8px}
 .igs-status-avatar-row .igs-btn-mgr-label{flex:0 0 auto}
 .igs-status-avatar-thumb{width:28px;height:28px;flex-shrink:0;border-radius:50%;object-fit:cover;background:var(--igs-settings-paper);overflow:hidden}
 .igs-status-avatar-empty{display:inline-flex;align-items:center;justify-content:center;padding:3px;color:var(--igs-settings-ink-4);cursor:default}
.igs-sprite-words{flex:1;min-width:0}
.igs-sprite-preview-overlay{position:absolute;inset:0;z-index:2147483600;background:rgba(9,10,11,.94);display:flex;align-items:center;justify-content:center;cursor:zoom-out;padding:24px;box-sizing:border-box}
.igs-sprite-preview-img{max-width:100%;max-height:100%;object-fit:contain;border-radius:var(--igs-settings-radius-control);box-shadow:none}
@media (max-width:640px){#igs-unified-settings{--igs-settings-width:min(760px,calc(var(--igs-settings-vw) - 24px));--igs-settings-height:min(760px,calc(var(--igs-settings-vh) - 24px));padding:max(8px,env(safe-area-inset-top)) max(8px,env(safe-area-inset-right)) max(8px,env(safe-area-inset-bottom)) max(8px,env(safe-area-inset-left))}.igs-settings-shell{width:var(--igs-settings-width);height:var(--igs-settings-height);border-radius:var(--igs-settings-radius-shell)}.igs-settings-grid,.igs-settings-api-group,.igs-source-filter-grid{grid-template-columns:minmax(0,1fr)}}
`.trim();
function getSettingsStyleText() {
    return SETTINGS_STYLE_TEXT;
}

__igsDefine(exports, "getSettingsStyleText", () => getSettingsStyleText);
});
__igsRegister("src/visual/igs-ui/settings-tabs.js", function(module, exports, require) {
const BASIC_TAB_TEMPLATE = `
<div class="igs-settings-grid">
  {{openModeField}}
  <div class="igs-settings-section">{{settingsToggles}}</div>
</div>
`.trim();

const REGEX_TAB_TEMPLATE = `
<div class="igs-settings-grid">
  <div class="igs-source-filter">
    <div>
      <div class="igs-source-filter-title">标签解析</div>
      <div class="igs-source-filter-note">先筛出 IGS 正文和图片来源；严格解析未命中时阅读器仍会使用兜底文本打开。</div>
    </div>
    <div class="igs-settings-row">{{filterToggles}}</div>
    <div class="igs-settings-row">{{untaggedToggle}}</div>
    <div class="igs-source-filter-grid">
      {{textIncludeField}}
      {{textExcludeField}}
      <div class="igs-settings-full">{{imageIncludeField}}</div>
    </div>
  </div>
  <div class="igs-source-filter igs-body-format">
    <div>
      <div class="igs-source-filter-title">正文格式化</div>
      <div class="igs-source-filter-note">对标签解析后的正文执行正则替换；不修改酒馆楼层原文。</div>
    </div>
    <div class="igs-settings-row">{{regexToggle}}</div>
    <div class="igs-source-filter-grid">
      {{regexPatternField}}
      {{regexFlagsField}}
      <div class="igs-settings-full">{{regexReplacementField}}</div>
    </div>
    <div class="igs-settings-row">
      <button class="igs-settings-action" data-action="reset-virtual-regex" type="button">恢复默认正文替换</button>
      <button class="igs-settings-action" data-action="test-virtual-regex" type="button">测试当前楼层</button>
    </div>
    <div class="igs-settings-preview" data-result="virtual-regex">{{regexPreview}}</div>
  </div>
</div>
`.trim();

const IMAGE_TAB_TEMPLATE = `
<div class="igs-settings-grid">
  {{imageModeField}}
  {{adapterField}}
  <div class="{{apiGroupClass}}">
    {{endpointField}}
    {{apiKeyField}}
    {{modelField}}
    {{sizeField}}
    {{stepsField}}
    {{samplerField}}
    {{timeoutField}}
    {{pollIntervalField}}
    {{pollAttemptsField}}
    {{promptPrefixField}}
  </div>
  <div class="igs-settings-full">
    <div class="igs-settings-result" data-result="image-models">{{imageModelsMessage}}</div>
  </div>
  <div class="igs-settings-full">
    <button class="igs-settings-action" data-action="test-image">{{imageTestActionLabel}}</button>
    <div class="igs-settings-result" data-result="image">{{imageTestHelp}}</div>
  </div>
</div>
`.trim();

const READER_TAB_TEMPLATE = `
<div class="igs-reader-settings">
  <div class="igs-reader-subtabs" role="tablist" aria-label="阅读器设置分类">{{readerSubTabs}}</div>
  <div class="igs-reader-subpane">{{readerSubPane}}</div>
</div>
`.trim();

const READER_DISPLAY_TEMPLATE = `
<div class="igs-settings-grid" data-reader-pane="display">
  {{fontSizeField}}
  {{dialogWidthField}}
  {{dialogHeightField}}
  {{glassOpacityField}}
  {{imageCountField}}
  {{inputScaleField}}
  {{imgModeField}}
  {{imgBrightnessField}}
  <div class="igs-settings-section igs-settings-full">{{readerToggles}}</div>
  {{nsfwVeilLevelField}}
  {{statusHudSection}}
</div>
`.trim();

const READER_OPTIONS_TEMPLATE = `
<div class="igs-settings-grid" data-reader-pane="options">
  <div class="igs-source-filter igs-settings-full">
    <div>
      <div class="igs-source-filter-title">选项气泡</div>
      <div class="igs-source-filter-note">在最后一页点击对话框空白处显示选项（取自数据库「选项 / 选项表 / 行动选项 / 检定建议表」表的文本列），再点空白处隐藏。需安装数据库插件。</div>
    </div>
    <div class="igs-source-filter-grid">
      {{optionFontSizeField}}
      <div class="igs-settings-section">{{optionBubbleToggle}}{{optionBubbleWidthToggle}}</div>
    </div>
    <div class="igs-settings-row">{{optionBubblePositionField}}{{optionBubbleActionField}}</div>
  </div>
</div>
`.trim();

const READER_TOOLBAR_TEMPLATE = `
<div class="igs-settings-grid" data-reader-pane="toolbar">
  {{toolbarScaleField}}
  {{toolbarDockField}}
  <div class="igs-settings-section igs-settings-full">{{pinnedButtonsField}}</div>
</div>
`.trim();

const READER_THEME_TEMPLATE = `
<div class="igs-settings-grid" data-reader-pane="theme">
  <div class="igs-source-filter igs-settings-full">
    <div class="igs-source-filter-grid">
      {{dialogSkinField}}
      {{classicDialogWidthPercentField}}
    </div>
  </div>
  <div class="{{themeGroupClass}}">
    <div>
      <div class="igs-source-filter-title">对话主题</div>
      <div class="igs-source-filter-note">{{themeNote}}</div>
    </div>
    <div class="{{themeAdvancedClass}}" style="display:flex;flex-direction:column;gap:12px">
      <div class="igs-source-filter">
        <div class="igs-source-filter-title">角色名</div>
        <div class="igs-settings-row">{{nameFontField}}{{nameColorField}}{{nameAlignField}}</div>
      </div>
      <div class="igs-source-filter">
        <div class="igs-source-filter-title">台词</div>
        <div class="igs-settings-row">{{textFontField}}{{textColorField}}{{textAlignField}}</div>
      </div>
      <div class="igs-source-filter">
        <div class="igs-source-filter-title">旁白</div>
        <div class="igs-settings-row">{{narrationFontField}}{{narrationColorField}}{{narrationAlignField}}</div>
      </div>
      <div class="igs-source-filter">
        <div class="igs-source-filter-title">心里话</div>
        <div class="igs-settings-row">{{thoughtFontField}}{{thoughtColorField}}{{thoughtAlignField}}</div>
      </div>
      <div class="igs-source-filter">
        <div class="igs-source-filter-title">分隔线</div>
        <div class="igs-settings-row">{{dividerField}}{{dividerColorField}}</div>
      </div>
      <div class="igs-source-filter">
        <div class="igs-source-filter-title">对话框背景</div>
        <div class="igs-settings-row">{{dialogBgField}}{{glassOpacityField}}</div>
      </div>
    </div>
  </div>
</div>
`.trim();

const SCENE_TAB_TEMPLATE = `
<div class="igs-scene-settings">
  <div class="igs-settings-section">{{sceneToggle}}</div>
  <div class="igs-scene-settings-subtabs" role="tablist" aria-label="场景设置分类">{{sceneSettingsSubTabs}}</div>
  <div class="igs-scene-settings-subpane">{{sceneSettingsSubPane}}</div>
</div>
`.trim();

const SCENE_RULES_TEMPLATE = `
<div class="igs-settings-grid" data-scene-settings-pane="rules">
  <div class="{{sceneGroupClass}}">
    <div class="igs-source-filter igs-settings-full">
      <div>
        <div class="igs-source-filter-title">格式规则注入</div>
        <div class="igs-source-filter-note">开启后将通过酒馆 API 向 AI 注入以下系统提示，让 AI 输出 [igs-scene:] 等标签。</div>
      </div>
      {{promptRuleField}}
      <div class="igs-settings-row">
        <button class="igs-settings-action" data-action="reset-prompt-rule" type="button">恢复默认提示词</button>
        <button class="igs-settings-action" data-action="save-prompt-rule" type="button">保存提示词</button>
      </div>
      <div class="igs-settings-result" data-result="prompt-rule">{{promptRuleStatus}}</div>
    </div>
  </div>
</div>
`.trim();

const SCENE_ASSETS_TEMPLATE = `
<div class="igs-settings-grid" data-scene-settings-pane="assets">
  <div class="{{sceneGroupClass}}">
    <div class="igs-source-filter igs-settings-full">
      <div>
        <div class="igs-source-filter-title">场景素材</div>
        <div class="igs-source-filter-note">使用 [igs-scene:场景|时间|天气|可选标签] 切换场景，第四栏填写 NSFW 时隐藏人物视觉并启用暗角柔焦帷幕；[igs-char:角色|情绪|对白] 标记对白，[igs-thought:角色|情绪|心里话] 标记心理描写。扫描图优先显示。</div>
      </div>
      {{scenePresetBar}}
      {{sceneSubTabs}}
      {{sceneSubPane}}
    </div>
  </div>
</div>
`.trim();
const SCENE_SETTINGS_SUBTAB_DEFS = Object.freeze([
    ['rules', '规则'],
    ['assets', '素材'],
]);
const READER_SUBTAB_DEFS = Object.freeze([
    ['display', '显示'],
    ['options', '选项'],
    ['toolbar', '工具栏'],
    ['theme', '主题'],
]);
const SETTINGS_TAB_DEFS = Object.freeze([
    ['basic', '基础'],
    ['regex', '正文替换'],
    ['image', '图像'],
    ['scene', '场景'],
    ['reader', '阅读器'],
]);
function normalizeSceneSettingsSubTab(subTab) {
    const normalized = String(subTab || 'rules').trim();
    return SCENE_SETTINGS_SUBTAB_DEFS.some(([id]) => id === normalized) ? normalized : 'rules';
}
function getSceneSettingsSubTabTemplate(subTab) {
    switch (normalizeSceneSettingsSubTab(subTab)) {
        case 'assets':
            return SCENE_ASSETS_TEMPLATE;
        case 'rules':
        default:
            return SCENE_RULES_TEMPLATE;
    }
}
function normalizeReaderSubTab(subTab) {
    const normalized = String(subTab || 'display').trim();
    return READER_SUBTAB_DEFS.some(([id]) => id === normalized) ? normalized : 'display';
}
function getReaderSubTabTemplate(subTab) {
    switch (normalizeReaderSubTab(subTab)) {
        case 'options':
            return READER_OPTIONS_TEMPLATE;
        case 'toolbar':
            return READER_TOOLBAR_TEMPLATE;
        case 'theme':
            return READER_THEME_TEMPLATE;
        case 'display':
        default:
            return READER_DISPLAY_TEMPLATE;
    }
}
function getSettingsTabTemplate(tab) {
    switch (tab) {
        case 'basic':
            return BASIC_TAB_TEMPLATE;
        case 'regex':
            return REGEX_TAB_TEMPLATE;
        case 'image':
            return IMAGE_TAB_TEMPLATE;
        case 'scene':
            return SCENE_TAB_TEMPLATE;
        case 'reader':
            return READER_TAB_TEMPLATE;
        default:
            return BASIC_TAB_TEMPLATE;
    }
}

__igsDefine(exports, "normalizeSceneSettingsSubTab", () => normalizeSceneSettingsSubTab);
__igsDefine(exports, "getSceneSettingsSubTabTemplate", () => getSceneSettingsSubTabTemplate);
__igsDefine(exports, "normalizeReaderSubTab", () => normalizeReaderSubTab);
__igsDefine(exports, "getReaderSubTabTemplate", () => getReaderSubTabTemplate);
__igsDefine(exports, "getSettingsTabTemplate", () => getSettingsTabTemplate);
__igsDefine(exports, "SCENE_SETTINGS_SUBTAB_DEFS", () => SCENE_SETTINGS_SUBTAB_DEFS);
__igsDefine(exports, "READER_SUBTAB_DEFS", () => READER_SUBTAB_DEFS);
__igsDefine(exports, "SETTINGS_TAB_DEFS", () => SETTINGS_TAB_DEFS);
});
__igsRegister("src/visual/igs-ui/icons.js", function(module, exports, require) {
const READER_MODE_ICONS = Object.freeze({
    pc: '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/></svg>',
    mobile: '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="2" width="14" height="20" rx="3"/><circle cx="12" cy="17.5" r="1" fill="currentColor" stroke="none"/></svg>',
    web: '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="18" rx="2"/><path d="M2 8h20"/><circle cx="6" cy="5.5" r="1" fill="currentColor" stroke="none"/><circle cx="9.5" cy="5.5" r="1" fill="currentColor" stroke="none"/><path d="M13 5.5h5"/></svg>',
    fullscreen: '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7V3h4M21 7V3h-4M3 17v4h4M21 17v4h-4"/></svg>',
    embedded: '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2"/><rect x="6" y="8" width="12" height="8" rx="1"/></svg>',
});
const SETTINGS_THEME_ICONS = Object.freeze({
    day: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"/></svg>',
    night: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M20.5 14.2A8.5 8.5 0 0 1 9.8 3.5 8.5 8.5 0 1 0 20.5 14.2Z"/></svg>',
});
function getReaderModeIcon(mode) {
    return READER_MODE_ICONS[mode] || READER_MODE_ICONS.pc;
}
function getSettingsThemeIcon(theme) {
    return SETTINGS_THEME_ICONS[theme === 'day' ? 'day' : 'night'];
}

__igsDefine(exports, "getReaderModeIcon", () => getReaderModeIcon);
__igsDefine(exports, "getSettingsThemeIcon", () => getSettingsThemeIcon);
__igsDefine(exports, "READER_MODE_ICONS", () => READER_MODE_ICONS);
__igsDefine(exports, "SETTINGS_THEME_ICONS", () => SETTINGS_THEME_ICONS);
});
__igsRegister("src/visual/igs-ui/reader-value-utils.js", function(module, exports, require) {
const { looksLikeHostUiHtml } = require("src/scene/message-source.js");
function igsDebugEnabled() {
    try {
        const root = (typeof window !== 'undefined' && window.parent && window.parent.IGS_DEBUG !== undefined)
            ? window.parent
            : (typeof window !== 'undefined' ? window : globalThis);
        return Boolean(root && root.IGS_DEBUG);
    } catch (error) {
        return Boolean(typeof globalThis !== 'undefined' && globalThis.IGS_DEBUG);
    }
}
function igsDebug(tag, payload) {
    if (!igsDebugEnabled()) return;
    try {
        if (payload === undefined) console.log(tag);
        else console.log(tag, payload);
    } catch (error) {
        // Console may be unavailable in simulation.
    }
}
function esc(value) {
    return String(value === undefined || value === null ? '' : value)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;');
}
function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}
function firstDefined(...values) {
    for (const value of values) {
        if (value !== undefined && value !== null) return value;
    }
    return undefined;
}
function firstNonEmptyString(...values) {
    for (const value of values) {
        const text = String(value || '').trim();
        if (text) return text;
    }
    return '';
}
function normalizeBoolean(value, fallback) {
    if (typeof value === 'boolean') return value;
    if (typeof value === 'number') return value !== 0;
    if (typeof value === 'string') {
        const normalized = value.trim().toLowerCase();
        if (['1', 'true', 'yes', 'on'].includes(normalized)) return true;
        if (['0', 'false', 'no', 'off'].includes(normalized)) return false;
    }
    return fallback;
}
function normalizeOpacity(value, fallback) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) return fallback;
    return Math.min(1, Math.max(0, numeric));
}
function normalizeFiniteNumber(value, fallback) {
    const numeric = Number(value);
    return Number.isFinite(numeric) ? numeric : fallback;
}
function normalizeNullableNumber(value) {
    if (value === null || value === undefined || value === '' || value === 'null' || value === 'auto') return null;
    const numeric = Number(value);
    return Number.isFinite(numeric) ? numeric : null;
}
function normalizeFiniteIndex(value) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric < 0) return 0;
    return Math.floor(numeric);
}
function clampNumber(value, min, max) {
    return Math.max(min, Math.min(max, value));
}
function toHex(color) {
    if (!color) return '#ffffff';
    if (color.startsWith('#') && (color.length === 7 || color.length === 4)) return color;
    const match = color.match(/rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/);
    if (match) {
        const r = Math.min(255, Number(match[1]));
        const g = Math.min(255, Number(match[2]));
        const b = Math.min(255, Number(match[3]));
        return `#${r.toString(16).padStart(2, '0')}${g.toString(16).padStart(2, '0')}${b.toString(16).padStart(2, '0')}`;
    }
    return '#ffffff';
}
function hexToRgba(color, alpha) {
    const hex = toHex(color);
    const value = hex.slice(1);
    const r = parseInt(value.slice(0, 2), 16);
    const g = parseInt(value.slice(2, 4), 16);
    const b = parseInt(value.slice(4, 6), 16);
    const a = Number.isFinite(Number(alpha)) ? Math.min(1, Math.max(0, Number(alpha))) : 0.62;
    return `rgba(${r},${g},${b},${a})`;
}
function computeLineHeight(fontSize) {
    if (fontSize <= 8) return '2.0';
    if (fontSize <= 11) return '1.9';
    if (fontSize <= 15) return '1.85';
    if (fontSize <= 18) return '1.7';
    return '1.6';
}
function normalizeDisplayText(value) {
    const text = String(value || '').trim();
    return looksLikeHostUiHtml(text) ? '' : text;
}
function firstRenderableText(...values) {
    for (const value of values) {
        const normalized = normalizeDisplayText(value);
        if (String(normalized || '').trim()) return normalized;
    }
    return '';
}
function stripSpeakerPrefix(text, speaker) {
    if (!speaker || !text) return text;
    const escaped = speaker.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    const pattern = new RegExp(`^\\[?${escaped}\\]?\\s*[:：]\\s*`);
    return text.replace(pattern, '');
}

__igsDefine(exports, "igsDebugEnabled", () => igsDebugEnabled);
__igsDefine(exports, "igsDebug", () => igsDebug);
__igsDefine(exports, "esc", () => esc);
__igsDefine(exports, "cloneData", () => cloneData);
__igsDefine(exports, "firstDefined", () => firstDefined);
__igsDefine(exports, "firstNonEmptyString", () => firstNonEmptyString);
__igsDefine(exports, "normalizeBoolean", () => normalizeBoolean);
__igsDefine(exports, "normalizeOpacity", () => normalizeOpacity);
__igsDefine(exports, "normalizeFiniteNumber", () => normalizeFiniteNumber);
__igsDefine(exports, "normalizeNullableNumber", () => normalizeNullableNumber);
__igsDefine(exports, "normalizeFiniteIndex", () => normalizeFiniteIndex);
__igsDefine(exports, "clampNumber", () => clampNumber);
__igsDefine(exports, "toHex", () => toHex);
__igsDefine(exports, "hexToRgba", () => hexToRgba);
__igsDefine(exports, "computeLineHeight", () => computeLineHeight);
__igsDefine(exports, "normalizeDisplayText", () => normalizeDisplayText);
__igsDefine(exports, "firstRenderableText", () => firstRenderableText);
__igsDefine(exports, "stripSpeakerPrefix", () => stripSpeakerPrefix);
});
__igsRegister("src/visual/igs-ui/settings-fields.js", function(module, exports, require) {
const { esc } = require("src/visual/igs-ui/reader-value-utils.js");
const { TOOLBAR_ACTIONS } = require("src/visual/igs-ui/reader-host-constants.js");
const encSeg = (value) => encodeURIComponent(String(value == null ? '' : value));

const STATUS_AVATAR_PLACEHOLDER_SVG = '<svg viewBox="0 0 24 24" width="100%" height="100%" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="9" r="3.4"/><path d="M5.5 20c0-3.6 2.9-6 6.5-6s6.5 2.4 6.5 6"/></svg>';
const STATUS_AVATAR_UPLOAD_ICON = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 16 12 12 8 16"/><line x1="12" y1="12" x2="12" y2="21"/><path d="M20.88 18.09A5 5 0 0 0 18 9h-1.26A8 8 0 1 0 3 16.29"/></svg>';
function renderTemplate(template, values) {
    return String(template || '').replace(/\{\{(\w+)\}\}/g, (_, key) => {
        return Object.prototype.hasOwnProperty.call(values, key) ? values[key] : '';
    });
}
function field(path, label, inputHtml, note) {
    return `<label class="igs-settings-field"><span>${esc(label)}</span>${inputHtml}${note ? `<em>${esc(note)}</em>` : ''}</label>`;
}
function disabledAttr(disabled) {
    return disabled ? ' disabled aria-disabled="true"' : '';
}
function textInput(path, value, placeholder, type = 'text', disabled = false) {
    return `<input data-path="${esc(path)}" type="${esc(type)}" value="${esc(value || '')}" placeholder="${esc(placeholder || '')}"${disabledAttr(disabled)}>`;
}
function colorInput(path, value, disabled = false) {
    return `<input data-path="${esc(path)}" type="color" value="${esc(value || '#ffffff')}"${disabledAttr(disabled)}>`;
}
function textareaInput(path, value, placeholder = '') {
    return `<textarea data-path="${esc(path)}" placeholder="${esc(placeholder)}">${esc(value || '')}</textarea>`;
}
function secretInput(path, value, placeholder, disabled) {
    return `<div class="igs-settings-secret">${textInput(path, value, placeholder, 'password', disabled)}<button type="button" class="igs-settings-secret-toggle" data-action="toggle-secret" aria-label="显示或隐藏密钥" aria-pressed="false"${disabledAttr(disabled)}>显示</button></div>`;
}
function numberInput(path, value, min, max, disabled) {
    return `<input data-path="${esc(path)}" type="number" min="${esc(min)}" max="${esc(max)}" value="${esc(value)}"${disabledAttr(disabled)}>`;
}
function checkbox(path, value, label) {
    return `<button type="button" class="igs-switch${value ? ' is-on' : ''}" data-switch="${esc(path)}" aria-pressed="${value ? 'true' : 'false'}"><i></i><span>${esc(label)}</span></button>`;
}
function tableMultiSelect(paths, selected, catalog, options = {}) {
    const picked = Array.isArray(selected) ? selected : [];
    const normalize = (value) => String(value == null ? '' : value).trim();
    const isPicked = (uid, name) => picked.some((item) => (uid && normalize(item.uid) === normalize(uid))
        || (!uid && name && normalize(item.name) === normalize(name)));
    const rows = [];
    for (const table of Array.isArray(catalog) ? catalog : []) {
        const uid = normalize(table && table.uid);
        const name = normalize(table && table.name) || uid;
        if (!uid && !name) continue;
        const on = isPicked(uid, name);
        const action = `status-hud-toggle-table:${encSeg(uid)}:${encSeg(name)}`;
        rows.push(`<button type="button" class="igs-table-pick${on ? ' is-on' : ''}" data-action="${esc(action)}" data-table-uid="${esc(uid)}" data-table-name="${esc(name)}" aria-pressed="${on ? 'true' : 'false'}"><i></i><span>${esc(name)}</span></button>`);
    }
    for (const item of picked) {
        const uid = normalize(item.uid);
        const name = normalize(item.name) || uid;
        const exists = (Array.isArray(catalog) ? catalog : []).some((table) => (uid && normalize(table && table.uid) === uid)
            || (!uid && name && normalize(table && table.name) === name));
        if (!exists) {
            const action = `status-hud-toggle-table:${encSeg(uid)}:${encSeg(name)}`;
            rows.push(`<button type="button" class="igs-table-pick is-on is-missing" data-action="${esc(action)}" data-table-uid="${esc(uid)}" data-table-name="${esc(name)}" aria-pressed="true"><i></i><span>${esc(name)}（未找到）</span></button>`);
        }
    }
    const empty = options.emptyText || '未检测到可读表格';
    const note = options.note ? `<em>${esc(options.note)}</em>` : '';
    return `<div class="igs-status-hud-tables" data-status-hud-tables data-path="${esc(paths)}">${rows.join('') || `<div class="igs-scene-empty">${esc(empty)}</div>`}${note}</div>`;
}
function selectInput(path, value, items, disabled = false) {
    const options = items.map(([itemValue, itemLabel]) => {
        const selected = String(itemValue) === String(value) ? ' selected' : '';
        return `<option value="${esc(itemValue)}"${selected}>${esc(itemLabel)}</option>`;
    }).join('');
    return `<select data-path="${esc(path)}"${disabled ? ' disabled' : ''}>${options}</select>`;
}
function segmentedInput(path, value, items, label) {
    const activeIndex = Math.max(0, items.findIndex((item) => String(item[0]) === String(value)));
    return `<div class="igs-segmented" role="radiogroup" aria-label="${esc(label || '')}" data-count="${esc(items.length)}" data-active-index="${esc(activeIndex)}" style="--igs-segment-count:${esc(items.length)};--igs-active-index:${esc(activeIndex)};"><span class="igs-segmented-indicator" aria-hidden="true"></span>${items.map((item) => {
        const selected = String(item[0]) === String(value);
        const icon = item[2] ? `<span class="igs-segmented-btn-icon" aria-hidden="true">${item[2]}</span>` : '';
        return `<button type="button" class="igs-segmented-btn${item[2] ? ' has-icon' : ''}${selected ? ' is-active' : ''}" data-segment-path="${esc(path)}" data-segment-value="${esc(item[0])}" role="radio" aria-checked="${selected ? 'true' : 'false'}" aria-pressed="${selected ? 'true' : 'false'}">${icon}<span class="igs-segmented-btn-label">${esc(item[1])}</span></button>`;
    }).join('')}</div>`;
}
function modelPicker(path, value, models, action, placeholder, disabled) {
    const items = Array.isArray(models) ? models.filter(Boolean) : [];
    const options = ['<option value="">从已拉取模型中选择</option>'].concat(items.map((model) => {
        const selected = model === value ? ' selected' : '';
        return `<option value="${esc(model)}"${selected}>${esc(model)}</option>`;
    })).join('');
    return `<div class="igs-settings-model"><div class="igs-settings-model-row"><input data-path="${esc(path)}" value="${esc(value || '')}" placeholder="${esc(placeholder || '')}"${disabledAttr(disabled)}><button type="button" class="igs-settings-action igs-settings-inline-action" data-action="${esc(action)}"${disabledAttr(disabled)}>拉取模型</button></div><select data-model-sync="${esc(path)}"${items.length && !disabled ? '' : ' disabled'}>${options}</select></div>`;
}
function renderSceneAssetList(scenes, options = {}) {
    const expandedSlots = options.expandedSlots instanceof Set ? options.expandedSlots : new Set();
    const timeGroups = Array.isArray(options.timeGroups) ? options.timeGroups : [];
    const weatherGroups = Array.isArray(options.weatherGroups) ? options.weatherGroups : [];
    const pencil = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>';
    const trash = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>';
    const chevronDown = '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>';
    const chevronUp = '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="18 15 12 9 6 15"/></svg>';
    const entries = Object.entries(scenes || {});
    if (!entries.length) return '<div class="igs-scene-empty">暂无背景图配置</div>';
    return entries.map(([sceneName, sceneVal]) => {
        const sceneObj = typeof sceneVal === 'string' ? { url: sceneVal, times: {} } : (sceneVal || { url: '', times: {} });
        const sceneWords = Array.isArray(sceneObj.words) ? sceneObj.words : [];
        const bgExpanded = expandedSlots.has('bg\x00' + sceneName);
        const badge = (text) => `<span style="font-size:10px;opacity:.5;flex-shrink:0;margin-right:2px">${text}</span>`;
        const timeEntries = Object.entries(sceneObj.times || {});
        const timeRows = timeEntries.map(([timeName, timeVal]) => {
            const timeObj = typeof timeVal === 'string' ? { url: timeVal, weathers: {} } : (timeVal || { url: '', weathers: {} });
            const timeExpanded = expandedSlots.has('time\x00' + sceneName + '\x00' + timeName);
            const weatherEntries = Object.entries(timeObj.weathers || {});
            const weatherRows = weatherEntries.map(([weatherName, weatherVal]) => {
                const weatherObj = typeof weatherVal === 'string' ? { url: weatherVal } : (weatherVal || { url: '' });
                const wExpanded = expandedSlots.has('weather\x00' + sceneName + '\x00' + timeName + '\x00' + weatherName);
                const wBody = wExpanded ? renderSceneGroupExpansion('weather', weatherName, weatherObj.url || '', weatherGroups) : '';
                return `<div class="igs-sprite-slot"><div class="igs-btn-mgr-row igs-scene-mood-row igs-scene-weather-row">`
                    + badge('天气')
                    + `<span class="igs-btn-mgr-label">${esc(weatherName)}</span>`
                    + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-rename-weather:${encSeg(sceneName)}:${encSeg(timeName)}:${encSeg(weatherName)}" title="重命名">${pencil}</button>`
                    + `<input class="igs-scene-url-input" data-scene-weather-bg="${esc(sceneName)}" data-scene-time="${esc(timeName)}" data-scene-weather="${esc(weatherName)}" value="${esc(weatherObj.url || '')}" placeholder="URL 或 data:image/...">`
                    + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-remove-weather:${encSeg(sceneName)}:${encSeg(timeName)}:${encSeg(weatherName)}" title="删除">${trash}</button>`
                    + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-toggle-weather:${encSeg(sceneName)}:${encSeg(timeName)}:${encSeg(weatherName)}" title="展开/折叠">${wExpanded ? chevronUp : chevronDown}</button>`
                    + `</div>${wBody}</div>`;
            }).join('');
            const timeBody = timeExpanded ? renderSceneGroupExpansion('time', timeName, timeObj.url || '', timeGroups) : '';
            return `<div class="igs-scene-char-group igs-scene-time-group"><div class="igs-sprite-slot"><div class="igs-btn-mgr-row">`
                + badge('时间')
                + `<span class="igs-btn-mgr-label">${esc(timeName)}</span>`
                + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-rename-time:${encSeg(sceneName)}:${encSeg(timeName)}" title="重命名">${pencil}</button>`
                + `<input class="igs-scene-url-input" data-scene-time-bg="${esc(sceneName)}" data-scene-time="${esc(timeName)}" value="${esc(timeObj.url || '')}" placeholder="URL 或 data:image/...">`
                + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-add-weather:${encSeg(sceneName)}:${encSeg(timeName)}" title="添加天气">+</button>`
                + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-remove-time:${encSeg(sceneName)}:${encSeg(timeName)}" title="删除">${trash}</button>`
                + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-toggle-time:${encSeg(sceneName)}:${encSeg(timeName)}" title="展开/折叠">${timeExpanded ? chevronUp : chevronDown}</button>`
                + `</div>${timeBody}</div>${weatherRows}</div>`;
        }).join('');
        const bgBody = bgExpanded ? renderSceneBgExpansion(sceneName, sceneObj.url || '', sceneWords) : '';
        return `<div class="igs-scene-char-group"><div class="igs-sprite-slot"><div class="igs-btn-mgr-row">`
            + badge('场景')
            + `<span class="igs-btn-mgr-label" style="font-weight:600">${esc(sceneName)}</span>`
            + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-rename-bg:${encSeg(sceneName)}" title="重命名">${pencil}</button>`
            + `<input class="igs-scene-url-input" data-scene-bg="${esc(sceneName)}" value="${esc(sceneObj.url || '')}" placeholder="URL 或 data:image/...">`
            + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-add-time:${encSeg(sceneName)}" title="添加时间">+</button>`
            + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-remove-bg:${encSeg(sceneName)}" title="删除场景">${trash}</button>`
            + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-toggle-bg:${encSeg(sceneName)}" title="展开/折叠">${bgExpanded ? chevronUp : chevronDown}</button>`
            + `</div>${bgBody}</div>${timeRows}</div>`;
    }).join('');
}

function renderSceneBgExpansion(sceneName, url, words) {
    const trimmedUrl = String(url || '').trim();
    const thumb = trimmedUrl
        ? `<img class="igs-sprite-thumb" src="${esc(trimmedUrl)}" loading="lazy" alt="" data-action="sprite-preview:${encSeg(trimmedUrl)}" onerror="this.classList.add('igs-sprite-thumb-broken')">`
        : `<div class="igs-sprite-thumb igs-sprite-thumb-empty">未配置</div>`;
    const tags = words.map((alias) =>
        `<span class="igs-mood-word-tag">${esc(alias)}<button type="button" class="igs-mood-word-del" data-action="scene-remove-bg-word:${encSeg(sceneName)}:${encSeg(alias)}" title="删除别名">×</button></span>`
    ).join('');
    const wHtml = `<div class="igs-sprite-words"><div class="igs-source-filter-note">场景别名</div><div class="igs-mood-word-list">${tags || '<div class="igs-scene-empty">暂无别名</div>'}<button type="button" class="igs-btn-mgr-icon" data-action="scene-add-bg-word:${encSeg(sceneName)}" title="添加别名">+</button></div></div>`;
    return `<div class="igs-sprite-slot-body">${thumb}${wHtml}</div>`;
}

function renderSceneGroupExpansion(type, label, url, groups) {
    const trimmedUrl = String(url || '').trim();
    const thumb = trimmedUrl
        ? `<img class="igs-sprite-thumb" src="${esc(trimmedUrl)}" loading="lazy" alt="" data-action="sprite-preview:${encSeg(trimmedUrl)}" onerror="this.classList.add('igs-sprite-thumb-broken')">`
        : `<div class="igs-sprite-thumb igs-sprite-thumb-empty">未配置</div>`;
    const isTime = type === 'time';
    const addAction = isTime ? `time-add-word:${encSeg(label)}` : `weather-add-word:${encSeg(label)}`;
    const removePrefix = isTime ? `time-remove-word:${encSeg(label)}` : `weather-remove-word:${encSeg(label)}`;
    const createAction = isTime ? `time-create-group:${encSeg(label)}` : `weather-create-group:${encSeg(label)}`;
    const typeName = isTime ? '时间' : '天气';
    const group = groups.find((g) => g && g.label === label);
    let wHtml;
    if (group) {
        const words = Array.isArray(group.words) ? group.words : [];
        const tags = words.map((w) =>
            `<span class="igs-mood-word-tag">${esc(w)}<button type="button" class="igs-mood-word-del" data-action="${removePrefix}:${encSeg(w)}" title="删除词">×</button></span>`
        ).join('');
        wHtml = `<div class="igs-sprite-words"><div class="igs-mood-word-list">${tags || '<div class="igs-scene-empty">暂无词</div>'}<button type="button" class="igs-btn-mgr-icon" data-action="${addAction}" title="添加词">+</button></div></div>`;
    } else {
        wHtml = `<div class="igs-sprite-words"><div class="igs-source-filter-note">「${esc(label)}」在${typeName}词库中无对应组。</div><button type="button" class="igs-settings-action" data-action="${createAction}">建为${typeName}组</button></div>`;
    }
    return `<div class="igs-sprite-slot-body">${thumb}${wHtml}</div>`;
}
function renderCharacterAssetList(characters, options = {}) {
    const moodGroups = Array.isArray(options.moodGroups) ? options.moodGroups : [];
    const expandedSlots = options.expandedSlots instanceof Set ? options.expandedSlots : new Set();
    const aliasesByCharacter = options.aliases && typeof options.aliases === 'object' && !Array.isArray(options.aliases)
        ? options.aliases : {};
    const statusAvatars = options.statusAvatars && typeof options.statusAvatars === 'object' && !Array.isArray(options.statusAvatars)
        ? options.statusAvatars : {};
    const upload = STATUS_AVATAR_UPLOAD_ICON;
    const pencil = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>';
    const trash = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>';
    const chevronDown = '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>';
    const chevronUp = '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="18 15 12 9 6 15"/></svg>';
    const charEntries = Object.entries(characters || {});
    if (!charEntries.length) return '<div class="igs-scene-empty">暂无角色立绘配置</div>';
    return charEntries.map(([charName, moods]) => {
        const aliases = Array.isArray(aliasesByCharacter[charName]) ? aliasesByCharacter[charName] : [];
        const aliasTags = aliases.map((alias) => (
            `<span class="igs-mood-word-tag">${esc(alias)}<button type="button" class="igs-mood-word-del" data-action="scene-remove-char-alias:${encSeg(charName)}:${encSeg(alias)}" title="删除别名">×</button></span>`
        )).join('');
        const aliasesHtml = `<div class="igs-sprite-words"><div class="igs-mood-word-list">${aliasTags || '<div class="igs-scene-empty">暂无别名</div>'}<button type="button" class="igs-btn-mgr-icon" data-action="scene-add-char-alias:${encSeg(charName)}" title="添加别名">+</button></div></div>`;
        const avatarUrl = String(statusAvatars[charName] || '').trim();
        const avatarPreview = avatarUrl
            ? `<img class="igs-status-avatar-thumb" src="${esc(avatarUrl)}" loading="lazy" alt="" data-action="sprite-preview:${encSeg(avatarUrl)}" onerror="this.classList.add('igs-sprite-thumb-broken')">`
            : `<span class="igs-status-avatar-thumb igs-status-avatar-empty" aria-hidden="true">${STATUS_AVATAR_PLACEHOLDER_SVG}</span>`;
        const avatarHtml = `<div class="igs-btn-mgr-row igs-status-avatar-row"><span class="igs-btn-mgr-label">状态栏头像</span>${avatarPreview}<input class="igs-scene-url-input igs-status-avatar-url" data-status-avatar-char="${esc(charName)}" value="${esc(avatarUrl)}" placeholder="https://... 或 data:image/..."><button type="button" class="igs-btn-mgr-icon" data-action="status-avatar-pick:${encSeg(charName)}" title="上传头像">${upload}</button><button type="button" class="igs-btn-mgr-icon" data-action="status-avatar-clear:${encSeg(charName)}" title="清除头像">${trash}</button></div>`;
        const moodEntries = Object.entries(moods || {});
        const moodRows = moodEntries.map(([mood, url]) => {
            const expanded = expandedSlots.has(charName + "\x00" + mood);
            const collapsedRow = `<div class="igs-btn-mgr-row igs-scene-mood-row">`
                + `<span class="igs-btn-mgr-label">${esc(mood)}</span>`
                + `<input class="igs-scene-url-input" data-scene-char="${esc(charName)}" data-scene-mood="${esc(mood)}" value="${esc(url || '')}" placeholder="URL 或 data:image/...">`
                + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-rename-mood:${encSeg(charName)}:${encSeg(mood)}" title="重命名">${pencil}</button>`
                + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-remove-mood:${encSeg(charName)}:${encSeg(mood)}" title="删除">${trash}</button>`
                + `<button type="button" class="igs-btn-mgr-icon" data-action="scene-toggle-mood:${encSeg(charName)}:${encSeg(mood)}" title="展开/折叠">${expanded ? chevronUp : chevronDown}</button>`
                + `</div>`;
            const expandedBody = expanded ? renderSpriteSlotExpansion(charName, mood, url, moodGroups, { pencil, trash }) : '';
            return `<div class="igs-sprite-slot">${collapsedRow}${expandedBody}</div>`;
        }).join('');
        return `<div class="igs-scene-char-group"><div class="igs-btn-mgr-row"><span class="igs-btn-mgr-label" style="font-weight:600">${esc(charName)}</span><button type="button" class="igs-btn-mgr-icon" data-action="scene-rename-char:${encSeg(charName)}" title="重命名">${pencil}</button><button type="button" class="igs-btn-mgr-icon" data-action="scene-add-mood:${encSeg(charName)}" title="添加情绪">+</button><button type="button" class="igs-btn-mgr-icon" data-action="scene-remove-char:${encSeg(charName)}" title="删除角色">${trash}</button></div>${aliasesHtml}${avatarHtml}<div class="igs-btn-mgr-list">${moodRows || '<div class="igs-scene-empty">暂无情绪</div>'}</div></div>`;
    }).join('');
}

function renderSpriteSlotExpansion(charName, mood, url, moodGroups, icons) {
    const trimmedUrl = String(url || '').trim();
    const thumb = trimmedUrl
        ? `<img class="igs-sprite-thumb" src="${esc(trimmedUrl)}" loading="lazy" alt="${esc(mood)}" data-action="sprite-preview:${encSeg(trimmedUrl)}" onerror="this.classList.add('igs-sprite-thumb-broken')">`
        : `<div class="igs-sprite-thumb igs-sprite-thumb-empty">未配置</div>`;
    const group = moodGroups.find((g) => g && g.label === mood);
    let wordsHtml;
    if (group) {
        const words = Array.isArray(group.words) ? group.words : [];
        const tags = words.map((word) => {
            return `<span class="igs-mood-word-tag">${esc(word)}<button type="button" class="igs-mood-word-del" data-action="mood-remove-word:${encSeg(mood)}:${encSeg(word)}" title="删除词">×</button></span>`;
        }).join('');
        wordsHtml = `<div class="igs-sprite-words"><div class="igs-mood-word-list">${tags || '<div class="igs-scene-empty">暂无情绪词</div>'}<button type="button" class="igs-btn-mgr-icon" data-action="mood-add-word:${encSeg(mood)}" title="添加词">+</button></div></div>`;
    } else {
        wordsHtml = `<div class="igs-sprite-words"><div class="igs-source-filter-note">「${esc(mood)}」在词库中无对应情绪组。</div><button type="button" class="igs-settings-action" data-action="mood-create-group:${encSeg(mood)}">建为情绪组</button></div>`;
    }
    return `<div class="igs-sprite-slot-body">${thumb}${wordsHtml}</div>`;
}
function renderScenePresetBar(presets, selectedName) {
    const names = Object.keys(presets || {});
    const opts = ['<option value="">— 选择预设 —</option>'].concat(names.map((n) =>
        `<option value="${esc(n)}"${n === selectedName ? ' selected' : ''}>${esc(n)}</option>`
    )).join('');
    const dis = (!selectedName || !(presets && presets[selectedName])) ? ' disabled' : '';
    const pencil = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>';
    const trash = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>';
    const save = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>';
    const download = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="8 17 12 21 16 17"/><line x1="12" y1="12" x2="12" y2="21"/><path d="M20.88 18.09A5 5 0 0 0 18 9h-1.26A8 8 0 1 0 3 16.29"/></svg>';
    const upload = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 16 12 12 8 16"/><line x1="12" y1="12" x2="12" y2="21"/><path d="M20.88 18.09A5 5 0 0 0 18 9h-1.26A8 8 0 1 0 3 16.29"/></svg>';
    return `<div class="igs-source-filter"><div style="display:flex;align-items:center;gap:6px"><select class="igs-scene-preset-select" data-preset-select style="flex:1;min-width:0">${opts}</select><button type="button" class="igs-btn-mgr-icon" data-action="scene-preset-save" title="保存当前配置为预设">${save}</button><button type="button" class="igs-btn-mgr-icon" data-action="scene-preset-rename"${dis} title="重命名">${pencil}</button><button type="button" class="igs-btn-mgr-icon" data-action="scene-preset-import" title="导入">${upload}</button><button type="button" class="igs-btn-mgr-icon" data-action="scene-preset-export"${dis} title="导出">${download}</button><button type="button" class="igs-btn-mgr-icon" data-action="scene-preset-delete"${dis} title="删除">${trash}</button></div></div>`;
}
function renderPinnedButtons(pinnedValue, hiddenValue, orderValue) {
    const pins = Array.isArray(pinnedValue) ? pinnedValue : [];
    const hidden = Array.isArray(hiddenValue) ? hiddenValue : [];
    const canonical = TOOLBAR_ACTIONS.map(([id]) => id);
    const order = Array.isArray(orderValue) && orderValue.length ? orderValue : canonical;
    const labelMap = Object.fromEntries(TOOLBAR_ACTIONS);
    const eyeOn = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>';
    const eyeOff = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/></svg>';
    const pinIcon = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l1.09 3.27L16 6l-2.18 2.18L14.55 12 12 10.18 9.45 12l.73-3.82L8 6l2.91-.73z"/><line x1="12" y1="12" x2="12" y2="22"/></svg>';
    const rows = order.map((id) => {
        const label = labelMap[id] || id;
        const isHidden = hidden.includes(id);
        const isPinned = pins.includes(id) && !isHidden;
        const canHide = id !== 'settings';
        const eyeBtn = canHide
            ? `<button type="button" class="igs-btn-mgr-icon${isHidden ? '' : ' is-on'}" data-action="toolbar-toggle-visible:${esc(id)}" title="显示/隐藏">${isHidden ? eyeOff : eyeOn}</button>`
            : `<span class="igs-btn-mgr-icon" title="此按钮不可隐藏" style="opacity:.3;cursor:default">${eyeOn}</span>`;
        return `<div class="igs-btn-mgr-row${isHidden ? ' is-hidden-btn' : ''}"><span class="igs-btn-mgr-handle" data-action="toolbar-move-up:${esc(id)}">☰</span><span class="igs-btn-mgr-label">${esc(label)}</span>${eyeBtn}<button type="button" class="igs-btn-mgr-icon${isPinned ? ' is-on' : ''}" data-action="toggle-toolbar-pin:${esc(id)}" title="常驻">${pinIcon}</button></div>`;
    }).join('');
    return `<div class="igs-settings-field"><span>按钮管理</span><div class="igs-btn-mgr-list">${rows}</div><em>☰ 上移排序 · 眼睛切换显隐 · 星切换常驻。隐藏的按钮自动解除常驻。</em></div>`;
}

__igsDefine(exports, "renderTemplate", () => renderTemplate);
__igsDefine(exports, "field", () => field);
__igsDefine(exports, "disabledAttr", () => disabledAttr);
__igsDefine(exports, "textInput", () => textInput);
__igsDefine(exports, "colorInput", () => colorInput);
__igsDefine(exports, "textareaInput", () => textareaInput);
__igsDefine(exports, "secretInput", () => secretInput);
__igsDefine(exports, "numberInput", () => numberInput);
__igsDefine(exports, "checkbox", () => checkbox);
__igsDefine(exports, "tableMultiSelect", () => tableMultiSelect);
__igsDefine(exports, "selectInput", () => selectInput);
__igsDefine(exports, "segmentedInput", () => segmentedInput);
__igsDefine(exports, "modelPicker", () => modelPicker);
__igsDefine(exports, "renderSceneAssetList", () => renderSceneAssetList);
__igsDefine(exports, "renderCharacterAssetList", () => renderCharacterAssetList);
__igsDefine(exports, "renderScenePresetBar", () => renderScenePresetBar);
__igsDefine(exports, "renderPinnedButtons", () => renderPinnedButtons);
});
__igsRegister("src/visual/igs-ui/embedded-reader-runtime.js", function(module, exports, require) {
// 楼层内嵌模式的纯函数边界：只决定宿主 DOM 定位、即时样式与载入态，
// 不触碰阅读器内部 DOM、不读取消息数据，便于单测与宿主环境隔离。
const EMBEDDED_HOST_ATTR = 'data-igs-embedded-host';
const EMBEDDED_HOST_SELECTOR = '[data-igs-embedded-host="1"]';
const EMBEDDED_TEXT_HIDDEN_ATTR = 'data-igs-embedded-hidden';
const PREVIOUS_DISPLAY_ATTR = 'data-igs-embedded-prev-display';
const PREVIOUS_DISPLAY_PRIORITY_ATTR = 'data-igs-embedded-prev-display-priority';
const PREVIOUS_ARIA_ATTR = 'data-igs-embedded-prev-aria';
const MISSING_VALUE = '__igs_missing__';
function resolveEmbeddedHostParent(messageElement) {
    if (!messageElement || typeof messageElement.querySelector !== 'function') return null;
    const mesText = messageElement.querySelector('.mes_text');
    if (!mesText || !mesText.parentNode) return null;
    return { parent: mesText.parentNode, mesText };
}
function findEmbeddedHost(doc) {
    if (!doc || typeof doc.querySelector !== 'function') return null;
    return doc.querySelector(EMBEDDED_HOST_SELECTOR);
}
function ensureEmbeddedHost(parent, doc, hostRef) {
    if (!parent || !doc || typeof doc.createElement !== 'function') return null;
    if (hostRef && hostRef.parentNode === parent) return hostRef;
    let host = hostRef;
    if (!host) {
        host = doc.createElement('div');
        host.setAttribute(EMBEDDED_HOST_ATTR, '1');
        host.setAttribute('data-igs-internal-reader', '1');
        host.className = 'igs-embedded-host';
    }
    const children = parent.children ? Array.from(parent.children) : [];
    const mesText = children.find((node) => node && node.classList && node.classList.contains('mes_text'));
    const mesIndex = mesText ? children.indexOf(mesText) : -1;
    const next = mesIndex >= 0 ? children[mesIndex + 1] : null;
    if (next && next !== host && typeof parent.insertBefore === 'function') parent.insertBefore(host, next);
    else if (host.parentNode !== parent && typeof parent.appendChild === 'function') parent.appendChild(host);
    return host;
}
function hideEmbeddedSourceText(mesText) {
    if (!mesText || typeof mesText.setAttribute !== 'function') return;
    if (typeof mesText.getAttribute === 'function' && mesText.getAttribute(EMBEDDED_TEXT_HIDDEN_ATTR) === '1') {
        setDisplayStyle(mesText.style, 'none', 'important');
        mesText.setAttribute('aria-hidden', 'true');
        return;
    }
    const display = readDisplayValue(mesText.style);
    const priority = readDisplayPriority(mesText.style);
    const aria = typeof mesText.getAttribute === 'function' ? mesText.getAttribute('aria-hidden') : null;
    mesText.setAttribute(EMBEDDED_TEXT_HIDDEN_ATTR, '1');
    mesText.setAttribute(PREVIOUS_DISPLAY_ATTR, display);
    mesText.setAttribute(PREVIOUS_DISPLAY_PRIORITY_ATTR, priority);
    mesText.setAttribute(PREVIOUS_ARIA_ATTR, aria == null ? MISSING_VALUE : String(aria));
    setDisplayStyle(mesText.style, 'none', 'important');
    mesText.setAttribute('aria-hidden', 'true');
}
function restoreEmbeddedSourceText(mesText) {
    if (!mesText || typeof mesText.removeAttribute !== 'function') return;
    const display = typeof mesText.getAttribute === 'function' ? mesText.getAttribute(PREVIOUS_DISPLAY_ATTR) : '';
    const priority = typeof mesText.getAttribute === 'function' ? mesText.getAttribute(PREVIOUS_DISPLAY_PRIORITY_ATTR) : '';
    const aria = typeof mesText.getAttribute === 'function' ? mesText.getAttribute(PREVIOUS_ARIA_ATTR) : MISSING_VALUE;
    restoreDisplayStyle(mesText.style, display, priority);
    if (aria === MISSING_VALUE || aria == null) mesText.removeAttribute('aria-hidden');
    else mesText.setAttribute('aria-hidden', aria);
    mesText.removeAttribute(EMBEDDED_TEXT_HIDDEN_ATTR);
    mesText.removeAttribute(PREVIOUS_DISPLAY_ATTR);
    mesText.removeAttribute(PREVIOUS_DISPLAY_PRIORITY_ATTR);
    mesText.removeAttribute(PREVIOUS_ARIA_ATTR);
}

function readDisplayValue(style) {
    if (!style) return '';
    if (typeof style.getPropertyValue === 'function') return String(style.getPropertyValue('display') || '');
    return String(style.display || '');
}

function readDisplayPriority(style) {
    return style && typeof style.getPropertyPriority === 'function' ? String(style.getPropertyPriority('display') || '') : '';
}

function setDisplayStyle(style, value, priority) {
    if (!style) return;
    if (typeof style.setProperty === 'function') style.setProperty('display', value, priority);
    else style.display = value;
}

function restoreDisplayStyle(style, value, priority) {
    if (!style) return;
    if (!value && typeof style.removeProperty === 'function') style.removeProperty('display');
    else setDisplayStyle(style, value || '', priority || '');
}
function buildEmbeddedLoadingHtml() {
    return `<div class="igs-embedded-loading" role="status" aria-live="polite"><span class="igs-embedded-loading-dot"></span><span class="igs-embedded-loading-dot"></span><span class="igs-embedded-loading-dot"></span><span class="igs-embedded-loading-text">正在生成…</span></div>`;
}
function resolveEmbeddedStreamPhase(previousPhase, event, constants = {}) {
    const phase = previousPhase || 'idle';
    const generationStart = constants.generationStart || 'generation-start';
    const generationComplete = constants.generationComplete || 'generation-complete';
    const generationAbort = constants.generationAbort || 'generation-abort';
    if (event === generationStart) return 'streaming';
    if (event === generationComplete) return 'finalizing';
    if (event === generationAbort) return 'aborted';
    return phase;
}
function shouldShowEmbeddedLoading(phase) {
    return phase === 'streaming' || phase === 'finalizing';
}

__igsDefine(exports, "resolveEmbeddedHostParent", () => resolveEmbeddedHostParent);
__igsDefine(exports, "findEmbeddedHost", () => findEmbeddedHost);
__igsDefine(exports, "ensureEmbeddedHost", () => ensureEmbeddedHost);
__igsDefine(exports, "hideEmbeddedSourceText", () => hideEmbeddedSourceText);
__igsDefine(exports, "restoreEmbeddedSourceText", () => restoreEmbeddedSourceText);
__igsDefine(exports, "buildEmbeddedLoadingHtml", () => buildEmbeddedLoadingHtml);
__igsDefine(exports, "resolveEmbeddedStreamPhase", () => resolveEmbeddedStreamPhase);
__igsDefine(exports, "shouldShowEmbeddedLoading", () => shouldShowEmbeddedLoading);
__igsDefine(exports, "EMBEDDED_HOST_ATTR", () => EMBEDDED_HOST_ATTR);
__igsDefine(exports, "EMBEDDED_HOST_SELECTOR", () => EMBEDDED_HOST_SELECTOR);
__igsDefine(exports, "EMBEDDED_TEXT_HIDDEN_ATTR", () => EMBEDDED_TEXT_HIDDEN_ATTR);
});
__igsRegister("src/visual/igs-ui/reader-source-cache.js", function(module, exports, require) {
// 阅读源解析缓存：把与页码无关的正文解析结果按消息 + 输入签名缓存，
// 让普通翻页不再重复执行整楼正文过滤、正则、标签解析、分页与图位映射。
const DEFAULT_SOURCE_CACHE_LIMIT = 8;
function buildReaderSourceSignature(input = {}) {
    return [
        String(input.messageId == null ? '' : input.messageId),
        hashText(input.rawText),
        hashText(input.visibleText),
        hashText(input.sourceFilter),
        hashText(input.virtualRegex),
        input.sceneAssetsEnabled ? '1' : '0',
        input.sentencePaging ? '1' : '0',
    ].join('|');
}
function createReaderSourceCache(options = {}) {
    const limit = Number(options.limit) > 0 ? Number(options.limit) : DEFAULT_SOURCE_CACHE_LIMIT;
    const parse = typeof options.parse === 'function' ? options.parse : null;
    const entries = new Map();
    let parseCount = 0;

    function get(signature, parseInput) {
        if (entries.has(signature)) {
            const hit = entries.get(signature);
            entries.delete(signature);
            entries.set(signature, hit);
            return { value: hit, hit: true };
        }
        if (!parse) return { value: null, hit: false };
        const value = parse(parseInput);
        parseCount += 1;
        entries.set(signature, value);
        while (entries.size > limit) {
            const oldest = entries.keys().next();
            if (oldest.done) break;
            entries.delete(oldest.value);
        }
        return { value, hit: false };
    }

    function invalidate(signature) {
        if (signature == null) entries.clear();
        else entries.delete(signature);
    }

    return { get, invalidate, size: () => entries.size, getParseCount: () => parseCount };
}

function hashText(value) {
    const text = typeof value === 'string' ? value : safeStringify(value);
    let hash = 5381;
    for (let i = 0; i < text.length; i += 1) {
        hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0;
    }
    return `${text.length}:${hash >>> 0}`;
}

// 设置对象可能引用宿主 DOM（如 message.element -> defaultView -> document），
// 直接 JSON.stringify 会因为循环引用抛错。这里只序列化解析真正相关的纯值，
// 遇到循环或 DOM 节点时返回稳定占位符，绝不让签名计算影响阅读器主流程。
function safeStringify(value, seen = new Set()) {
    if (value == null) return '';
    const type = typeof value;
    if (type === 'string') return value;
    if (type === 'number' || type === 'boolean') return String(value);
    if (type === 'function') return '[fn]';
    if (type !== 'object') return String(value);
    if (seen.has(value)) return '[circular]';
    seen.add(value);
    try {
        if (Array.isArray(value)) {
            return '[' + value.map((item) => safeStringify(item, seen)).join(',') + ']';
        }
        if (typeof value.nodeType === 'number' || typeof value.querySelector === 'function') {
            return '[dom]';
        }
        return '{' + Object.keys(value).sort()
            .map((key) => `${key}:${safeStringify(value[key], seen)}`)
            .join(',') + '}';
    } finally {
        seen.delete(value);
    }
}

__igsDefine(exports, "buildReaderSourceSignature", () => buildReaderSourceSignature);
__igsDefine(exports, "createReaderSourceCache", () => createReaderSourceCache);
__igsDefine(exports, "DEFAULT_SOURCE_CACHE_LIMIT", () => DEFAULT_SOURCE_CACHE_LIMIT);
});
__igsRegister("src/media/resource-cache.js", function(module, exports, require) {
function createResourceCache(store = new Map()) {
    return {
        put(id, resource) {
            if (!id) return { ok: false, reason: 'missing-id' };
            store.set(id, { ...resource, id });
            return { ok: true, id };
        },

        get(id) {
            return store.get(id) || null;
        },

        list() {
            return Array.from(store.values());
        },

        clear() {
            store.clear();
        },
    };
}
function createImageResourceCache(globalObject = globalThis, options = {}) {
    const entries = new Map();
    const ImageCtor = globalObject && typeof globalObject.Image === 'function' ? globalObject.Image : null;
    const fetcher = globalObject && typeof globalObject.fetch === 'function'
        ? globalObject.fetch.bind(globalObject)
        : null;
    const urlApi = globalObject && globalObject.URL || null;
    const limit = Number(options.limit) > 0 ? Math.floor(Number(options.limit)) : 32;

    function load(url) {
        const source = String(url || '').trim();
        if (!source) return Promise.resolve('');
        const existing = entries.get(source);
        if (existing) {
            entries.delete(source);
            entries.set(source, existing);
            return existing.promise;
        }

        const entry = {
            ready: false,
            resolvedUrl: '',
            objectUrl: '',
            disposed: false,
            promise: null,
        };
        if (!fetcher && !ImageCtor) {
            entry.ready = true;
            entry.resolvedUrl = source;
            entry.promise = Promise.resolve(source);
            entries.set(source, entry);
            trim(source);
            return entry.promise;
        }
        entry.promise = resolveSource(source).then((resolved) => {
            if (entry.disposed) {
                revokeObjectUrl(resolved);
                return source;
            }
            entry.resolvedUrl = resolved || source;
            if (entry.resolvedUrl !== source) entry.objectUrl = entry.resolvedUrl;
            entry.ready = true;
            trim(source);
            return entry.resolvedUrl;
        });
        entries.set(source, entry);
        return entry.promise;
    }

    async function resolveSource(source) {
        if (/^https?:\/\//i.test(source) && fetcher && canCreateObjectUrl()) {
            try {
                const response = await fetcher(source, { cache: 'force-cache', mode: 'cors' });
                if (!response || response.ok === false || typeof response.blob !== 'function') throw new Error('image-fetch-failed');
                const blob = await response.blob();
                return urlApi.createObjectURL(blob);
            } catch (error) {
                return preloadSource(source);
            }
        }
        return preloadSource(source);
    }

    function preloadSource(source) {
        if (!ImageCtor || /^(?:data:|blob:)/i.test(source)) return Promise.resolve(source);
        return new Promise((resolve) => {
            try {
                const image = new ImageCtor();
                let settled = false;
                const finish = () => {
                    if (settled) return;
                    settled = true;
                    image.onload = null;
                    image.onerror = null;
                    resolve(source);
                };
                image.onload = finish;
                image.onerror = finish;
                image.src = source;
                if (image.complete) finish();
            } catch (error) {
                resolve(source);
            }
        });
    }

    function get(url) {
        const source = String(url || '').trim();
        const entry = entries.get(source);
        if (!entry || !entry.ready) return '';
        entries.delete(source);
        entries.set(source, entry);
        return entry.resolvedUrl || source;
    }

    function trim(protectedSource) {
        while (entries.size > limit) {
            const candidate = Array.from(entries.entries()).find(([source, entry]) => (
                source !== protectedSource && entry.ready
            ));
            if (!candidate) break;
            entries.delete(candidate[0]);
            disposeEntry(candidate[1]);
        }
    }

    function canCreateObjectUrl() {
        return Boolean(urlApi
            && typeof urlApi.createObjectURL === 'function'
            && typeof urlApi.revokeObjectURL === 'function');
    }

    function revokeObjectUrl(url) {
        if (!url || !canCreateObjectUrl() || !String(url).startsWith('blob:')) return;
        urlApi.revokeObjectURL(url);
    }

    function disposeEntry(entry) {
        if (!entry) return;
        entry.disposed = true;
        revokeObjectUrl(entry.objectUrl);
    }

    function clear() {
        for (const entry of entries.values()) disposeEntry(entry);
        entries.clear();
    }

    return { load, get, clear, size: () => entries.size };
}

__igsDefine(exports, "createResourceCache", () => createResourceCache);
__igsDefine(exports, "createImageResourceCache", () => createImageResourceCache);
});
__igsRegister("src/host/chat-stream-observer.js", function(module, exports, require) {
const { getSillyTavernContext } = require("src/host/tavern-helper-adapter.js");
// 楼层内嵌的流式生命周期：仅在 embedded 阅读器打开时创建，
// 官方生成事件负责起止，#chat mutation 只在生成期间同步活动，不做解析。
const DEFAULT_STABLE_MS = 800;
const DEFAULT_HARD_TIMEOUT_MS = 120000;

const FALLBACK_GENERATION_EVENTS = Object.freeze({
    started: 'generation_started',
    ended: 'generation_ended',
    stopped: 'generation_stopped',
});
function createChatStreamObserver(opts = {}) {
    const globalObject = opts.global || globalThis;
    const onActivity = typeof opts.onActivity === 'function' ? opts.onActivity : () => {};
    const onStable = typeof opts.onStable === 'function' ? opts.onStable : () => {};
    const onTimeout = typeof opts.onTimeout === 'function' ? opts.onTimeout : onStable;
    const stableMs = Number(opts.stableMs) > 0 ? Number(opts.stableMs) : DEFAULT_STABLE_MS;
    const hardTimeoutMs = Number(opts.hardTimeoutMs) > 0 ? Number(opts.hardTimeoutMs) : DEFAULT_HARD_TIMEOUT_MS;
    let observer = null;
    let stableTimer = null;
    let hardTimer = null;
    let active = false;
    let generationActive = false;
    let manualArmed = false;
    let lifecycleAvailable = false;
    const lifecycleCleanup = [];

    const getSetter = () => typeof globalObject.setTimeout === 'function'
        ? globalObject.setTimeout.bind(globalObject)
        : setTimeout;

    const getClearer = () => typeof globalObject.clearTimeout === 'function'
        ? globalObject.clearTimeout.bind(globalObject)
        : clearTimeout;

    const clearStable = () => {
        if (stableTimer == null) return;
        getClearer()(stableTimer);
        stableTimer = null;
    };

    const clearHard = () => {
        if (hardTimer == null) return;
        getClearer()(hardTimer);
        hardTimer = null;
    };

    const scheduleStable = () => {
        if (!active) return;
        clearStable();
        stableTimer = getSetter()(() => {
            stableTimer = null;
            if (!active) return;
            Promise.resolve()
                .then(onStable)
                .then((completed) => {
                    if (completed !== false) {
                        manualArmed = false;
                        clearHard();
                        return;
                    }
                    scheduleStable();
                })
                .catch(() => scheduleStable());
        }, stableMs);
    };

    const ensureHard = () => {
        if (!active || hardTimer != null) return;
        hardTimer = getSetter()(() => {
            hardTimer = null;
            generationActive = false;
            manualArmed = false;
            clearStable();
            if (active) Promise.resolve().then(onTimeout).catch(() => {});
        }, hardTimeoutMs);
    };

    const scheduleActivity = () => {
        if (!active) return;
        try { onActivity(); } catch (error) { /* */ }
        ensureHard();
        if (!lifecycleAvailable || (manualArmed && !generationActive)) scheduleStable();
    };

    const onGenerationStarted = () => {
        if (!active) return;
        generationActive = true;
        manualArmed = false;
        clearStable();
        scheduleActivity();
    };

    const onGenerationFinished = () => {
        if (!active) return;
        generationActive = false;
        manualArmed = false;
        ensureHard();
        scheduleStable();
    };

    const detachLifecycle = () => {
        while (lifecycleCleanup.length) {
            const cleanup = lifecycleCleanup.pop();
            try { cleanup(); } catch (error) { /* */ }
        }
        lifecycleAvailable = false;
    };

    const attachLifecycle = () => {
        detachLifecycle();
        const context = opts.context || getSillyTavernContext(globalObject);
        const eventSource = opts.eventSource || context && context.eventSource;
        const eventTypes = opts.eventTypes || context && (context.event_types || context.eventTypes) || {};
        if (!eventSource || typeof eventSource.on !== 'function') return false;
        const started = eventTypes.GENERATION_STARTED || FALLBACK_GENERATION_EVENTS.started;
        const finished = Array.from(new Set([
            eventTypes.GENERATION_ENDED || FALLBACK_GENERATION_EVENTS.ended,
            eventTypes.GENERATION_STOPPED || FALLBACK_GENERATION_EVENTS.stopped,
        ].filter(Boolean)));
        const subscribe = (eventName, handler) => {
            eventSource.on(eventName, handler);
            lifecycleCleanup.push(() => {
                if (typeof eventSource.removeListener === 'function') eventSource.removeListener(eventName, handler);
                else if (typeof eventSource.off === 'function') eventSource.off(eventName, handler);
            });
        };
        try {
            subscribe(started, onGenerationStarted);
            finished.forEach((eventName) => subscribe(eventName, onGenerationFinished));
            lifecycleAvailable = true;
            return true;
        } catch (error) {
            detachLifecycle();
            return false;
        }
    };

    const resolveDocument = () => {
        if (typeof opts.getDocument === 'function') {
            try {
                const resolved = opts.getDocument();
                if (resolved) return resolved;
            } catch (error) { /* */ }
        }
        return opts.document || (globalObject && globalObject.document) || null;
    };

    const start = () => {
        if (active) return { ok: true, already: true };
        active = true;
        attachLifecycle();
        const doc = resolveDocument();
        const observerWindow = doc && doc.defaultView;
        const MutationObserverCtor = globalObject.MutationObserver || globalObject.WebKitMutationObserver
            || observerWindow && (observerWindow.MutationObserver || observerWindow.WebKitMutationObserver) || null;
        const chat = doc && typeof doc.querySelector === 'function' ? doc.querySelector('#chat') : null;
        if (MutationObserverCtor && chat) {
            observer = new MutationObserverCtor((records) => {
                const external = Array.isArray(records) && records.some((record) => !isInternalRecord(record));
                if (!external) return;
                if (lifecycleAvailable && !generationActive && !manualArmed) return;
                scheduleActivity();
            });
            try {
                observer.observe(chat, { childList: true, subtree: true, characterData: true });
            } catch (error) {
                observer = null;
            }
        }
        return { ok: true, observed: Boolean(observer), lifecycle: lifecycleAvailable };
    };

    const cancelPending = () => {
        generationActive = false;
        manualArmed = false;
        clearStable();
        clearHard();
    };

    const stop = () => {
        active = false;
        cancelPending();
        if (observer && typeof observer.disconnect === 'function') observer.disconnect();
        observer = null;
        detachLifecycle();
    };

    const noteActivity = () => {
        if (!active) return;
        manualArmed = true;
        scheduleActivity();
    };

    return {
        start,
        stop,
        cancelPending,
        isActive: () => active,
        hasLifecycle: () => lifecycleAvailable,
        noteActivity,
    };
}

function isInternalRecord(record) {
    const target = record && record.target;
    if (isInternalNode(target)) return true;
    const changed = []
        .concat(record && record.addedNodes ? Array.from(record.addedNodes) : [])
        .concat(record && record.removedNodes ? Array.from(record.removedNodes) : []);
    return changed.length > 0 && changed.every(isInternalNode);
}

function isInternalNode(node) {
    if (!node) return false;
    if (typeof node.getAttribute === 'function' && node.getAttribute('data-igs-internal-reader') === '1') return true;
    return Boolean(node.closest && node.closest('[data-igs-internal-reader="1"]'));
}

__igsDefine(exports, "createChatStreamObserver", () => createChatStreamObserver);
__igsDefine(exports, "DEFAULT_STABLE_MS", () => DEFAULT_STABLE_MS);
__igsDefine(exports, "DEFAULT_HARD_TIMEOUT_MS", () => DEFAULT_HARD_TIMEOUT_MS);
});
__igsRegister("src/visual/igs-ui/reader-image-state.js", function(module, exports, require) {
const { cloneData, firstDefined, normalizeFiniteIndex, normalizeNullableNumber } = require("src/visual/igs-ui/reader-value-utils.js");
const { INITIAL_IMAGE_POLL_ATTEMPTS, INITIAL_IMAGE_POLL_INTERVAL_MS } = require("src/visual/igs-ui/reader-host-constants.js");
function shouldPollReaderImages(content = {}) {
    const expected = Number(content.imageExpectedCount || content.imageCount || 0) || 0;
    if (!expected) return false;
    const slots = Array.isArray(content.imageSlots) ? content.imageSlots : [];
    if (!slots.length) return false;
    const bound = Number(content.imageBoundCount || 0) || countBoundImageSlots({ slots });
    return bound < expected || !String(content.currentSlotImageUrl || content.currentImageUrl || '').trim();
}
function countBoundImageSlots(imageState = {}) {
    return (Array.isArray(imageState.slots) ? imageState.slots : [])
        .filter((slot) => String(slot && slot.url || '').trim())
        .length;
}
function normalizePollInterval(value) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric <= 0) return INITIAL_IMAGE_POLL_INTERVAL_MS;
    return Math.max(50, Math.min(750, Math.floor(numeric)));
}
function normalizePollAttempts(value) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric <= 0) return INITIAL_IMAGE_POLL_ATTEMPTS;
    return Math.max(1, Math.min(20, Math.floor(numeric)));
}
function waitForReaderImagePoll(duration, globalObject) {
    return new Promise((resolve) => {
        const timeout = globalObject && typeof globalObject.setTimeout === 'function'
            ? globalObject.setTimeout.bind(globalObject)
            : setTimeout;
        timeout(resolve, duration);
    });
}
function buildProgressText(currentIndex, totalSegments, imageState) {
    const segmentProgress = `${currentIndex + 1} / ${totalSegments}`;
    if (!imageState || !imageState.count) return segmentProgress;
    const expected = Number(imageState.expectedCount || imageState.count || 0) || 0;
    const bound = Number(imageState.boundCount || 0) || 0;
    const unbound = Number(imageState.unboundCount || 0) || 0;
    const hasSlots = Array.isArray(imageState.slots) && imageState.slots.length > 0;
    if (!hasSlots) return `${segmentProgress}   [${imageState.currentIndex + 1}/${imageState.count} 图]`;
    const currentUrl = String(imageState.slotUrl || imageState.currentUrl || imageState.displayUrl || '').trim();
    const slotText = currentUrl
        ? `图位 ${imageState.currentIndex + 1}/${expected}`
        : '当前图位未生成';
    const suffix = !currentUrl && unbound > 0 ? `，未匹配 ${unbound}` : '';
    return `${segmentProgress}   [${slotText}，已绑定 ${bound}/${expected}${suffix}]`;
}
function normalizeSnapshotImageState(imageState, fallbackIndex = 0) {
    const unboundImages = Array.isArray(imageState && imageState.unboundImages)
        ? imageState.unboundImages
            .map((image, index) => mapSnapshotImageEntry(image, index, true))
            .filter(Boolean)
        : [];
    const slots = Array.isArray(imageState && imageState.slots)
        ? imageState.slots
            .map((image, index) => mapSnapshotImageEntry(image, index, false))
            .filter(Boolean)
        : [];
    const images = slots.length
        ? cloneData(slots)
        : Array.isArray(imageState && imageState.images)
            ? imageState.images
                .map((image, index) => mapSnapshotImageEntry(image, index, true))
                .filter(Boolean)
            : [];
    const totalCount = slots.length || images.length;
    const activeIndex = totalCount
        ? Math.max(0, Math.min(totalCount - 1, normalizeFiniteIndex(
            firstDefined(fallbackIndex, imageState && imageState.currentIndex),
        )))
        : 0;
    const displayImage = slots.length
        ? resolveSnapshotDisplayImage(slots, activeIndex) || resolveIndexedSnapshotUnboundImage(unboundImages, activeIndex, slots.length)
        : images[activeIndex] || null;
    const expectedCount = Number(firstDefined(imageState && imageState.expectedCount, totalCount)) || totalCount;
    const boundCount = slots.length
        ? slots.filter((slot) => String(slot && slot.url || '').trim()).length
        : images.length;
    const unboundCount = unboundImages.length;
    return {
        slots,
        images,
        unboundImages,
        count: totalCount,
        expectedCount,
        boundCount,
        unboundCount,
        availableCount: boundCount + unboundCount,
        signature: String(imageState && imageState.signature || ''),
        currentIndex: activeIndex,
        currentUrl: String(displayImage && displayImage.url || ''),
        displayUrl: String(displayImage && displayImage.url || ''),
        slotUrl: String(slots[activeIndex] && slots[activeIndex].url || displayImage && displayImage.url || ''),
    };
}
function applyImageCountOverride(imageState, override) {
    const expected = normalizeNullableNumber(override);
    if (!expected || expected <= 0 || !imageState || !Array.isArray(imageState.slots) || !imageState.slots.length) {
        return imageState;
    }
    const slots = imageState.slots.slice(0, expected);
    while (slots.length < expected) {
        const slotIndex = slots.length;
        slots.push({
            url: '',
            providerId: '',
            source: 'manual-image-count',
            filename: '',
            imageId: '',
            locationHash: '',
            slotIndex,
            buttonIndex: null,
            title: `图 ${slotIndex + 1}`,
            promptText: '',
            rawBlock: '',
            tagName: 'image',
            order: null,
        });
    }
    const currentIndex = Math.max(0, Math.min(slots.length - 1, normalizeFiniteIndex(imageState.currentIndex)));
    const currentSlot = slots[currentIndex] || null;
    const boundCount = slots.filter((slot) => String(slot && slot.url || '').trim()).length;
    const unboundImages = Array.isArray(imageState.unboundImages) ? cloneData(imageState.unboundImages) : [];
    return {
        ...imageState,
        slots,
        images: cloneData(slots),
        count: slots.length,
        expectedCount: slots.length,
        boundCount,
        unboundCount: unboundImages.length,
        availableCount: boundCount + unboundImages.length,
        currentIndex,
        currentUrl: String(currentSlot && currentSlot.url || ''),
        displayUrl: String(currentSlot && currentSlot.url || ''),
        slotUrl: String(currentSlot && currentSlot.url || ''),
    };
}
function buildImageActionContext(current, unifiedSettings = null) {
    const snapshot = current && current.snapshot || {};
    const content = snapshot.content || {};
    return {
        mode: current && current.mode || 'pc',
        message: current && current.payload && current.payload.message || null,
        messageId: snapshot.messageId,
        prompt: content.text || '',
        currentIndex: content.currentIndex || 0,
        textIndex: normalizeFiniteIndex(content.currentIndex || 0),
        imageIndex: normalizeFiniteIndex(firstDefined(content.activeImageIndex, content.currentIndex, 0)),
        imageState: {
            images: cloneData(content.images || []),
            slots: cloneData(content.imageSlots || []),
            unboundImages: cloneData(content.unboundImages || []),
            count: Number(content.imageCount || 0) || 0,
            expectedCount: Number(content.imageExpectedCount || content.imageCount || 0) || 0,
            boundCount: Number(content.imageBoundCount || 0) || 0,
            unboundCount: Number(content.imageUnboundCount || 0) || 0,
            availableCount: Number(content.imageAvailableCount || 0) || 0,
            signature: String(content.imageSignature || ''),
            currentIndex: normalizeFiniteIndex(firstDefined(content.activeImageIndex, content.currentIndex, 0)),
            currentUrl: String(content.currentImageUrl || content.backgroundImage || ''),
            displayUrl: String(content.currentImageUrl || content.backgroundImage || ''),
            slotUrl: String(content.currentSlotImageUrl || ''),
        },
        currentUrl: String(content.currentImageUrl || content.backgroundImage || ''),
        scene: current && current.payload && current.payload.scene || null,
        render: current && current.payload && current.payload.render || null,
        unifiedSettings: unifiedSettings || null,
    };
}
function resolveSegmentImageIndex(payload, textIndex) {
    const segmentImageSlots = Array.isArray(payload && payload.segmentImageSlots)
        ? payload.segmentImageSlots
        : [];
    const mapped = Number(segmentImageSlots[textIndex]);
    if (Number.isFinite(mapped) && mapped >= 0) return Math.floor(mapped);
    return normalizeFiniteIndex(firstDefined(
        payload && payload.imageState && payload.imageState.currentIndex,
        textIndex,
        0,
    ));
}
function mapSnapshotImageEntry(image, fallbackIndex, requireUrl) {
    const url = String(image && image.url || '').trim();
    if (requireUrl && !url) return null;
    return {
        url,
        providerId: String(image && image.providerId || '').trim(),
        source: String(image && image.source || '').trim(),
        filename: String(image && image.filename || '').trim(),
        imageId: String(image && image.imageId || '').trim(),
        locationHash: String(image && image.locationHash || '').trim(),
        slotIndex: normalizeFiniteIndex(firstDefined(image && image.slotIndex, fallbackIndex, 0)),
        buttonIndex: normalizeNullableNumber(image && image.buttonIndex),
        title: String(image && image.title || '').trim(),
        promptText: String(image && image.promptText || '').trim(),
        rawBlock: String(image && image.rawBlock || '').trim(),
        tagName: String(image && image.tagName || '').trim(),
        order: normalizeNullableNumber(image && image.order),
    };
}
function resolveSnapshotDisplayImage(slots, activeIndex) {
    const source = Array.isArray(slots) ? slots : [];
    const slot = source[activeIndex];
    return String(slot && slot.url || '').trim() ? slot : null;
}
function resolveIndexedSnapshotUnboundImage(unboundImages, activeIndex, slotCount) {
    const source = Array.isArray(unboundImages) ? unboundImages : [];
    if (!source.length || source.length !== slotCount) return null;
    return source[normalizeFiniteIndex(activeIndex)] || null;
}

__igsDefine(exports, "shouldPollReaderImages", () => shouldPollReaderImages);
__igsDefine(exports, "countBoundImageSlots", () => countBoundImageSlots);
__igsDefine(exports, "normalizePollInterval", () => normalizePollInterval);
__igsDefine(exports, "normalizePollAttempts", () => normalizePollAttempts);
__igsDefine(exports, "waitForReaderImagePoll", () => waitForReaderImagePoll);
__igsDefine(exports, "buildProgressText", () => buildProgressText);
__igsDefine(exports, "normalizeSnapshotImageState", () => normalizeSnapshotImageState);
__igsDefine(exports, "applyImageCountOverride", () => applyImageCountOverride);
__igsDefine(exports, "buildImageActionContext", () => buildImageActionContext);
__igsDefine(exports, "resolveSegmentImageIndex", () => resolveSegmentImageIndex);
__igsDefine(exports, "mapSnapshotImageEntry", () => mapSnapshotImageEntry);
__igsDefine(exports, "resolveSnapshotDisplayImage", () => resolveSnapshotDisplayImage);
__igsDefine(exports, "resolveIndexedSnapshotUnboundImage", () => resolveIndexedSnapshotUnboundImage);
});
__igsRegister("src/visual/igs-ui/reader-dom-utils.js", function(module, exports, require) {
function getRootDocument(globalObject) {
    const root = globalObject || globalThis.window || globalThis;
    try {
        if (root && root.document) return root.document;
    } catch (error) {
        return null;
    }
    return null;
}
function getOwnerWindow(node) {
    if (node && node.ownerDocument && node.ownerDocument.defaultView) {
        return node.ownerDocument.defaultView;
    }
    const doc = getRootDocument(globalThis.window || globalThis);
    return doc && doc.defaultView ? doc.defaultView : null;
}
function clearChildren(node) {
    if (!node) return;
    if (typeof node.replaceChildren === 'function') {
        node.replaceChildren();
    } else if (Array.isArray(node.children)) {
        for (const child of [...node.children]) {
            if (child && typeof child.remove === 'function') child.remove();
        }
        node.children = [];
    }
    if (Object.prototype.hasOwnProperty.call(node, 'innerHTML') || typeof node.innerHTML === 'string') {
        node.innerHTML = '';
    }
}
function ensureStyleTag(doc, id, text) {
    if (!doc || !doc.head) return;
    if (doc.getElementById(id)) return;
    const style = doc.createElement('style');
    style.id = id;
    style.textContent = text;
    doc.head.appendChild(style);
}
function unmountNode(node) {
    if (node && node.remove) {
        node.remove();
    }
}
function readElementWidth(element, fallback = 0) {
    return normalizeBoxMeasure(
        element && (element.clientWidth || element.offsetWidth || readNumericPixels(element.style && element.style.width)),
        fallback,
    );
}
function readElementHeight(element, fallback = 0) {
    return normalizeBoxMeasure(
        element && (element.clientHeight || element.offsetHeight || readNumericPixels(element.style && element.style.height)),
        fallback,
    );
}
function normalizeBoxMeasure(value, fallback = 0) {
    const numeric = Number(value);
    if (Number.isFinite(numeric) && numeric > 0) return numeric;
    return Number(fallback) > 0 ? Number(fallback) : 0;
}
function readNumericPixels(value) {
    if (value == null || value === '') return 0;
    const match = String(value).match(/-?\d+(?:\.\d+)?/);
    return match ? Number(match[0]) : 0;
}
function ensureImageLoadingSpinner(container) {
    if (!container) return;
    if (container.querySelector('.igs-image-loading')) return;
    const wrapper = container.ownerDocument
        ? container.ownerDocument.createElement('div')
        : document.createElement('div');
    wrapper.className = 'igs-image-loading';
    wrapper.setAttribute('aria-label', '图片加载中');
    wrapper.innerHTML = '<span class="igs-spinner igs-image-spinner"></span>';
    container.appendChild(wrapper);
}
function removeImageLoadingSpinner(container) {
    if (!container) return;
    const existing = container.querySelector('.igs-image-loading');
    if (existing) existing.remove();
}
function ensureImageEmptyPlaceholder(container, text = '图片未生成') {
    if (!container) return;
    removeImageLoadingSpinner(container);
    const existing = container.querySelector('.igs-image-empty');
    if (existing) {
        existing.textContent = text;
        return;
    }
    const wrapper = container.ownerDocument
        ? container.ownerDocument.createElement('div')
        : document.createElement('div');
    wrapper.className = 'igs-image-empty';
    wrapper.setAttribute('aria-label', text);
    wrapper.textContent = text;
    container.appendChild(wrapper);
}
function removeImageEmptyPlaceholder(container) {
    if (!container) return;
    const existing = container.querySelector('.igs-image-empty');
    if (existing) existing.remove();
}
function attachSettingsViewportEvents(domState, root) {
    if (!domState || !root) return;
    detachSettingsViewportEvents(domState);
    domState.overlay = root;
    const win = getOwnerWindow(root);
    domState.viewportWindow = win;
    const schedule = () => {
        if (!domState.overlay) return;
        if (domState.viewportRaf !== null && domState.viewportRaf !== undefined) return;
        const raf = win && typeof win.requestAnimationFrame === 'function'
            ? win.requestAnimationFrame.bind(win)
            : null;
        if (!raf) {
            syncSettingsViewportVars(domState.overlay);
            return;
        }
        domState.viewportRaf = -1;
        const nextRaf = raf(() => {
            domState.viewportRaf = null;
            if (domState.overlay) syncSettingsViewportVars(domState.overlay);
        });
        if (domState.viewportRaf === -1) domState.viewportRaf = nextRaf;
    };
    domState.viewportHandler = schedule;
    syncSettingsViewportVars(root);
    if (win && typeof win.addEventListener === 'function') {
        win.addEventListener('resize', schedule, { passive: true });
        win.addEventListener('orientationchange', schedule, { passive: true });
    }
    if (win && win.visualViewport && typeof win.visualViewport.addEventListener === 'function') {
        win.visualViewport.addEventListener('resize', schedule, { passive: true });
        win.visualViewport.addEventListener('scroll', schedule, { passive: true });
    }
}
function detachSettingsViewportEvents(domState) {
    if (!domState) return;
    const win = domState.viewportWindow;
    const handler = domState.viewportHandler;
    if (win && handler && typeof win.removeEventListener === 'function') {
        win.removeEventListener('resize', handler);
        win.removeEventListener('orientationchange', handler);
    }
    if (win && win.visualViewport && handler && typeof win.visualViewport.removeEventListener === 'function') {
        win.visualViewport.removeEventListener('resize', handler);
        win.visualViewport.removeEventListener('scroll', handler);
    }
    if (domState.viewportRaf !== null && domState.viewportRaf !== undefined) {
        const cancel = win && typeof win.cancelAnimationFrame === 'function'
            ? win.cancelAnimationFrame.bind(win)
            : null;
        if (cancel) cancel(domState.viewportRaf);
    }
    domState.viewportHandler = null;
    domState.viewportWindow = null;
    domState.viewportRaf = null;
}
function syncSettingsViewportVars(root) {
    if (!root || !root.style) return;
    const doc = root.ownerDocument;
    const win = doc && doc.defaultView;
    const viewport = win && win.visualViewport;
    const docEl = doc && doc.documentElement ? doc.documentElement : {};
    const left = viewport && Number.isFinite(viewport.offsetLeft) ? viewport.offsetLeft : 0;
    const top = viewport && Number.isFinite(viewport.offsetTop) ? viewport.offsetTop : 0;
    const width = viewport && Number.isFinite(viewport.width) && viewport.width > 0
        ? viewport.width
        : (win && win.innerWidth) || docEl.clientWidth || 320;
    const height = viewport && Number.isFinite(viewport.height) && viewport.height > 0
        ? viewport.height
        : (win && win.innerHeight) || docEl.clientHeight || 480;
    root.style.setProperty('--igs-settings-vleft', `${Math.round(left)}px`);
    root.style.setProperty('--igs-settings-vtop', `${Math.round(top)}px`);
    root.style.setProperty('--igs-settings-vw', `${Math.round(width)}px`);
    root.style.setProperty('--igs-settings-vh', `${Math.round(height)}px`);
}

__igsDefine(exports, "getRootDocument", () => getRootDocument);
__igsDefine(exports, "getOwnerWindow", () => getOwnerWindow);
__igsDefine(exports, "clearChildren", () => clearChildren);
__igsDefine(exports, "ensureStyleTag", () => ensureStyleTag);
__igsDefine(exports, "unmountNode", () => unmountNode);
__igsDefine(exports, "readElementWidth", () => readElementWidth);
__igsDefine(exports, "readElementHeight", () => readElementHeight);
__igsDefine(exports, "normalizeBoxMeasure", () => normalizeBoxMeasure);
__igsDefine(exports, "readNumericPixels", () => readNumericPixels);
__igsDefine(exports, "ensureImageLoadingSpinner", () => ensureImageLoadingSpinner);
__igsDefine(exports, "removeImageLoadingSpinner", () => removeImageLoadingSpinner);
__igsDefine(exports, "ensureImageEmptyPlaceholder", () => ensureImageEmptyPlaceholder);
__igsDefine(exports, "removeImageEmptyPlaceholder", () => removeImageEmptyPlaceholder);
__igsDefine(exports, "attachSettingsViewportEvents", () => attachSettingsViewportEvents);
__igsDefine(exports, "detachSettingsViewportEvents", () => detachSettingsViewportEvents);
__igsDefine(exports, "syncSettingsViewportVars", () => syncSettingsViewportVars);
});
__igsRegister("src/visual/igs-ui/settings-normalize.js", function(module, exports, require) {
const { LEGACY_READER_MODES, resolveLegacyReaderMode } = require("src/storage/legacy-igs.js");
const { buildNarrativeSegments } = require("src/scene/image-slots.js");
const { SETTINGS_TAB_DEFS } = require("src/visual/igs-ui/settings-tabs.js");
const { TOOLBAR_ACTIONS, VN_THEME_PRESETS } = require("src/visual/igs-ui/reader-host-constants.js");
const { esc, normalizeFiniteNumber } = require("src/visual/igs-ui/reader-value-utils.js");
const { CLASSIC_DIALOG_THEME_DEFAULTS, isClassicDialogSkin } = require("src/visual/igs-ui/classic-dialog-skin.js");
function normalizeReaderMode(mode, bridge) {
    if (mode === 'default') return 'default';
    const resolved = resolveLegacyReaderMode(mode, '', bridge || {});
    return LEGACY_READER_MODES.includes(resolved) ? resolved : 'pc';
}
function normalizeSettingsTab(tab) {
    const normalized = String(tab || 'basic').trim();
    return SETTINGS_TAB_DEFS.some(([id]) => id === normalized) ? normalized : 'basic';
}
function normalizeSettingsValue(path, value) {
    if (path === 'readerMode' || path === 'bridge.openMode' || path === 'bridge.imageApi.mode' || path === 'bridge.imageApi.externalAdapter' || path === 'readerSettings.imgMode') {
        return String(value || '');
    }
    if (path.startsWith('readerSettings.')) {
        if (value === null || value === 'null') return null;
        if (/fontSize|optionFontSize|dialogWidth|dialogHeight|classicDialogWidthPercent|toolbarScale|inputScale|imageCountOverride|imgBrightness/.test(path)) {
            return Number(value);
        }
        if (/glassOpacity/.test(path)) {
            return Number(value);
        }
        if (/^readerSettings\.statusHud\.enabled$/.test(path) || /^readerSettings\.statusHud\.showEmotion$/.test(path) || /^readerSettings\.statusHud\.showLocation$/.test(path) || /^readerSettings\.statusHud\.showLocationDetails$/.test(path) || /^readerSettings\.statusHud\.showSpriteOnNsfw$/.test(path) || /^readerSettings\.statusHud\.dimSpriteOnNarration$/.test(path)) {
            return value === true || value === 'true' || value === 1 || value === '1';
        }
    }
    if (/^bridge\.imageApi\.(steps|requestTimeoutMs|pollIntervalMs|pollAttempts)$/.test(path)) {
        return Number(value);
    }
    return value;
}
function getPath(target, path) {
    return String(path || '').split('.').reduce((value, key) => (value == null ? value : value[key]), target);
}
function setPath(target, path, value) {
    const parts = String(path || '').split('.');
    let cursor = target;
    for (let index = 0; index < parts.length - 1; index += 1) {
        const key = parts[index];
        if (!cursor[key] || typeof cursor[key] !== 'object' || Array.isArray(cursor[key])) {
            cursor[key] = {};
        }
        cursor = cursor[key];
    }
    cursor[parts[parts.length - 1]] = value;
}
function buildTextSegments(text) {
    return buildNarrativeSegments(text);
}
function normalizePinnedButtons(value) {
    const allowed = new Set(TOOLBAR_ACTIONS.map(([id]) => id));
    const output = [];
    for (const id of Array.isArray(value) ? value : []) {
        const normalized = String(id || '').trim();
        if (!normalized || !allowed.has(normalized) || output.includes(normalized)) continue;
        output.push(normalized);
    }
    return output;
}
function normalizeHiddenButtons(value) {
    const allowed = new Set(TOOLBAR_ACTIONS.map(([id]) => id));
    const protected_ = new Set(['settings']);
    const output = [];
    for (const id of Array.isArray(value) ? value : []) {
        const normalized = String(id || '').trim();
        if (!normalized || !allowed.has(normalized) || protected_.has(normalized) || output.includes(normalized)) continue;
        output.push(normalized);
    }
    return output;
}
function normalizeBtnOrder(value) {
    const canonical = TOOLBAR_ACTIONS.map(([id]) => id);
    const allowed = new Set(canonical);
    const output = [];
    for (const id of Array.isArray(value) ? value : []) {
        const normalized = String(id || '').trim();
        if (!normalized || !allowed.has(normalized) || output.includes(normalized)) continue;
        output.push(normalized);
    }
    for (const id of canonical) {
        if (!output.includes(id)) output.push(id);
    }
    return output;
}
function normalizeSpriteLayouts(value) {
    const def = { posX: 50, posY: 100, scale: 100 };
    if (!value || typeof value !== 'object' || Array.isArray(value)) return {};
    const out = {};
    for (const key of Object.keys(value)) {
        const src = (typeof value[key] === 'object' && value[key]) ? value[key] : {};
        out[key] = {
            posX: normalizeFiniteNumber(src.posX, def.posX),
            posY: normalizeFiniteNumber(src.posY, def.posY),
            scale: normalizeFiniteNumber(src.scale, def.scale),
        };
    }
    return out;
}
function resolveSpriteLayout(layouts, mode, character, mood) {
    const def = { posX: 50, posY: 100, scale: 100 };
    if (!layouts) return def;
    if (character) {
        if (mood) {
            const moodKey = `${mode}::${character}::${mood}`;
            if (layouts[moodKey]) return layouts[moodKey];
        }
        const charKey = `${mode}::${character}`;
        if (layouts[charKey]) return layouts[charKey];
    }
    if (layouts[mode]) return layouts[mode];
    return def;
}
function resolveActiveTheme(snapshot) {
    const readerSettings = snapshot.readerSettings || {};
    const classic = isClassicDialogSkin(readerSettings);
    const vnTheme = classic ? (readerSettings.classicVnTheme || {}) : (readerSettings._vnTheme || readerSettings.vnTheme || {});
    const presetName = classic ? 'custom' : (vnTheme.preset || 'genshin');
    const preset = classic ? CLASSIC_DIALOG_THEME_DEFAULTS : (VN_THEME_PRESETS[presetName] || VN_THEME_PRESETS.genshin);
    if (!classic && presetName !== 'custom') return { ...preset };
    return {
        nameAlign: vnTheme.nameAlign || preset.nameAlign,
        textAlign: vnTheme.textAlign || preset.textAlign || 'left',
        narrationAlign: vnTheme.narrationAlign || preset.narrationAlign || 'left',
        thoughtAlign: vnTheme.thoughtAlign || preset.thoughtAlign || 'left',
        dividerSymbol: vnTheme.dividerSymbol != null ? vnTheme.dividerSymbol : preset.dividerSymbol,
        nameFont: vnTheme.nameFont || preset.nameFont,
        textFont: vnTheme.textFont || preset.textFont,
        thoughtFont: vnTheme.thoughtFont || preset.thoughtFont,
        narrationFont: vnTheme.narrationFont || preset.narrationFont,
        nameColor: vnTheme.nameColor || preset.nameColor,
        textColor: vnTheme.textColor || preset.textColor,
        thoughtColor: vnTheme.thoughtColor || preset.thoughtColor,
        narrationColor: vnTheme.narrationColor || preset.narrationColor,
        dividerColor: vnTheme.dividerColor || preset.dividerColor,
        dialogBg: vnTheme.dialogBg || preset.dialogBg,
    };
}
function renderDialogueHtml(text, theme, sceneAssetsEnabled) {
    const escaped = esc(text);
    if (!sceneAssetsEnabled) return escaped;
    const html = escaped.replace(/\*([^*]+)\*/g, (_, inner) => {
        const styles = [];
        if (theme.thoughtFont && theme.thoughtFont !== 'inherit') styles.push(`font-family:${cssFontValue(theme.thoughtFont)}`);
        if (theme.thoughtColor) styles.push(`color:${theme.thoughtColor}`);
        const styleAttr = styles.length ? ` style="${styles.join(';')}"` : '';
        return `<span class="igs-thought"${styleAttr}>${inner}</span>`;
    });
    // 心理活动标记可能不成对（模型漏写右星号），残留的星号一律不渲染。
    return html.replace(/\*/g, '');
}

function cssFontValue(font) {
    return String(font || '').replace(/"/g, "'");
}

__igsDefine(exports, "normalizeReaderMode", () => normalizeReaderMode);
__igsDefine(exports, "normalizeSettingsTab", () => normalizeSettingsTab);
__igsDefine(exports, "normalizeSettingsValue", () => normalizeSettingsValue);
__igsDefine(exports, "getPath", () => getPath);
__igsDefine(exports, "setPath", () => setPath);
__igsDefine(exports, "buildTextSegments", () => buildTextSegments);
__igsDefine(exports, "normalizePinnedButtons", () => normalizePinnedButtons);
__igsDefine(exports, "normalizeHiddenButtons", () => normalizeHiddenButtons);
__igsDefine(exports, "normalizeBtnOrder", () => normalizeBtnOrder);
__igsDefine(exports, "normalizeSpriteLayouts", () => normalizeSpriteLayouts);
__igsDefine(exports, "resolveSpriteLayout", () => resolveSpriteLayout);
__igsDefine(exports, "resolveActiveTheme", () => resolveActiveTheme);
__igsDefine(exports, "renderDialogueHtml", () => renderDialogueHtml);
});
__igsRegister("src/visual/igs-ui/reader-runtime.js", function(module, exports, require) {
const { getOwnerWindow } = require("src/visual/igs-ui/reader-dom-utils.js");
const { clampNumber } = require("src/visual/igs-ui/reader-value-utils.js");
const { isEmbeddedReaderMode } = require("src/schemas/reader-mode.js");
function applyReaderModeRuntime(root, snapshot, current, ctx = {}) {
    if (current.runtime && current.runtime.mode === snapshot.mode && current.runtime.root === root) {
        return;
    }
    const prevMode = current.runtime && current.runtime.mode;
    if (prevMode === 'fullscreen' && snapshot.mode !== 'fullscreen') {
        exitDocumentFullscreen(current.runtime.doc);
    }
    clearReaderModeRuntime(current);
    // 切换模式时清除上一模式（pc/mobile 的 applyInlineReaderRuntime）写在 root 上的内联样式，
    // 否则切到 web/fullscreen 后残留的 width/height/transform 等会阻止新模式生效。
    resetInlineModeStyles(root);
    const win = getOwnerWindow(root);
    const doc = root && root.ownerDocument;
    const runtime = {
        cleanup: [],
        mode: snapshot.mode,
        win,
        doc,
        root,
    };
    current.runtime = runtime;

    if (isEmbeddedReaderMode(snapshot.mode)) {
        applyEmbeddedReaderRuntime(root, runtime);
        return;
    }
    if (snapshot.mode === 'pc' || snapshot.mode === 'mobile') {
        applyInlineReaderRuntime(root, snapshot.mode, runtime, current);
        return;
    }
    if (snapshot.mode === 'web') {
        applyWebReaderRuntime(root, runtime);
        return;
    }
    if (snapshot.mode === 'fullscreen') {
        applyFullscreenReaderRuntime(root, current, runtime, ctx);
    }
}

// 楼层内嵌：阅读器完全受宿主楼层容器约束，不做 fixed、不锁滚动、不请求全屏、不可拖动。
// 尺寸交给 .igs-mode-embedded 的 CSS（固定高度、16:9 上限、移动端 dVH 上限）统一控制，
// 这里只清掉可能从上一次内联模式残留下来的几何属性。
function applyEmbeddedReaderRuntime(root, runtime) {
    if (!root || !root.style) return;
    for (const prop of [
        'top', 'right', 'bottom', 'left', 'width', 'height',
        'maxWidth', 'maxHeight', 'transform', 'borderRadius',
        'boxShadow', 'overflow', 'boxSizing', 'zIndex',
    ]) {
        root.style[prop] = '';
    }
    root.classList.add('igs-mode-embedded');
    if (runtime) {
        runtime.cleanup.push(() => {
            if (root.classList) root.classList.remove('igs-mode-embedded');
        });
    }
}
function clearReaderModeRuntime(current) {
    const runtime = current && current.runtime;
    if (!runtime || !Array.isArray(runtime.cleanup)) return;
    while (runtime.cleanup.length) {
        const fn = runtime.cleanup.pop();
        try {
            fn();
        } catch (error) {
            // Best-effort cleanup.
        }
    }
    current.runtime = null;
}
function addRuntimeCleanup(runtime, fn) {
    if (runtime && typeof fn === 'function') runtime.cleanup.push(fn);
}

function applyInlineReaderRuntime(root, mode, runtime, current) {
    const win = runtime.win || {};
    const doc = runtime.doc || {};
    const floatingState = current && current.floatingState
        ? current.floatingState
        : { dragged: false, left: null, top: null };
    let suppressTimer = null;
    root.style.top = 'auto';
    root.style.right = 'auto';
    root.style.left = '50%';
    root.style.bottom = '24px';
    root.style.transform = 'translateX(-50%)';
    root.style.boxSizing = 'border-box';
    root.style.zIndex = '2147483000';
    root.style.width = mode === 'pc' ? 'min(900px,calc(100vw - 32px))' : 'min(480px,calc(100vw - 32px))';
    root.style.height = mode === 'pc' ? 'min(540px,calc(100dvh - 32px))' : 'min(680px,calc(100dvh - 32px))';
    root.style.borderRadius = mode === 'pc' ? '18px' : '22px';
    root.style.boxShadow = '0 20px 64px rgba(0,0,0,0.42)';
    root.style.overflow = 'hidden';

    let scheduled = false;
    const clamp = () => {
        scheduled = false;
        const viewport = getInlineViewportMetrics(win, doc);
        const designWidth = mode === 'pc' ? 900 : 480;
        const designHeight = mode === 'pc' ? 540 : 680;
        const sideGap = 16;
        const bottomGap = 24;
        const minSize = 240;
        const minExtremeSize = 180;
        const safeWidth = Math.max(minSize, (viewport.width || (designWidth + sideGap * 2)) - sideGap * 2);
        const safeHeight = Math.max(minSize, (viewport.height || (designHeight + sideGap * 2)) - sideGap * 2);
        let targetWidth = Math.min(designWidth, safeWidth);
        let targetHeight = Math.min(designHeight, safeHeight);
        const availableHeight = (viewport.height || (targetHeight + sideGap + bottomGap)) - sideGap - bottomGap;
        if (availableHeight < targetHeight) {
            targetHeight = Math.max(minExtremeSize, Math.min(targetHeight, availableHeight));
        }
        const minLeft = viewport.left + 8;
        const minTop = viewport.top + 8;
        const maxLeft = Math.max(minLeft, viewport.right - targetWidth - 8);
        const maxTop = Math.max(minTop, viewport.bottom - targetHeight - 8);
        const defaultLeft = Math.round(viewport.left + (viewport.width || targetWidth) / 2);
        let targetLeft = defaultLeft;
        let targetTop = viewport.top + (viewport.height || (targetHeight + bottomGap)) - bottomGap - targetHeight;
        if (targetTop < minTop) targetTop = minTop;
        if (maxTop >= minTop && targetTop > maxTop) targetTop = maxTop;
        root.style.maxWidth = `${safeWidth}px`;
        root.style.maxHeight = `${safeHeight}px`;
        root.style.width = `${targetWidth}px`;
        root.style.height = `${targetHeight}px`;
        if (floatingState.dragged && Number.isFinite(floatingState.left) && Number.isFinite(floatingState.top)) {
            targetLeft = clampNumber(floatingState.left, minLeft, maxLeft);
            targetTop = clampNumber(floatingState.top, minTop, maxTop);
            floatingState.left = targetLeft;
            floatingState.top = targetTop;
            root.style.left = `${Math.round(targetLeft)}px`;
            root.style.top = `${Math.round(targetTop)}px`;
            root.style.right = 'auto';
            root.style.bottom = 'auto';
            root.style.transform = 'none';
            return;
        }
        root.style.left = `${defaultLeft}px`;
        root.style.top = `${Math.round(targetTop)}px`;
        root.style.right = 'auto';
        root.style.bottom = 'auto';
        root.style.transform = 'translateX(-50%)';
    };
    const schedule = () => {
        if (scheduled) return;
        scheduled = true;
        const raf = typeof win.requestAnimationFrame === 'function'
            ? win.requestAnimationFrame.bind(win)
            : null;
        if (raf) {
            raf(clamp);
        } else {
            const setter = typeof win.setTimeout === 'function' ? win.setTimeout.bind(win) : setTimeout;
            setter(clamp, 16);
        }
    };

    clamp();
    schedule();
    installFloatingDragRuntime(root, runtime, current, floatingState, () => {
        if (suppressTimer != null) {
            const clearer = typeof win.clearTimeout === 'function' ? win.clearTimeout.bind(win) : clearTimeout;
            clearer(suppressTimer);
        }
        current.dragSuppressClick = true;
        runtime.dragSuppressClick = true;
        const setter = typeof win.setTimeout === 'function' ? win.setTimeout.bind(win) : setTimeout;
        suppressTimer = setter(() => {
            current.dragSuppressClick = false;
            runtime.dragSuppressClick = false;
            suppressTimer = null;
        }, 120);
    });
    addEventListenerWithCleanup(win, 'resize', schedule, runtime);
    addEventListenerWithCleanup(win, 'orientationchange', schedule, runtime);
    if (win.visualViewport) {
        addEventListenerWithCleanup(win.visualViewport, 'resize', schedule, runtime);
        addEventListenerWithCleanup(win.visualViewport, 'scroll', schedule, runtime);
    }
    addRuntimeCleanup(runtime, () => {
        if (suppressTimer != null) {
            const clearer = typeof win.clearTimeout === 'function' ? win.clearTimeout.bind(win) : clearTimeout;
            clearer(suppressTimer);
        }
    });
}

function installFloatingDragRuntime(root, runtime, current, floatingState, onSuppressClick) {
    if (!root || !runtime || !current) return;
    const layer = root.querySelector('#igs-click-layer');
    const doc = runtime.doc;
    if (!layer || !doc || typeof doc.addEventListener !== 'function') return;
    let active = false;
    let dragging = false;
    let startX = 0;
    let startY = 0;
    let startLeft = 0;
    let startTop = 0;
    let pointerId = null;

    const onMove = (event) => {
        if (!active) return;
        const clientX = Number(event && event.clientX);
        const clientY = Number(event && event.clientY);
        if (!Number.isFinite(clientX) || !Number.isFinite(clientY)) return;
        const dx = clientX - startX;
        const dy = clientY - startY;
        if (!dragging && Math.hypot(dx, dy) < 6) return;
        dragging = true;
        root.classList.add('is-dragging');
        if (event && event.cancelable && typeof event.preventDefault === 'function') event.preventDefault();
        const viewport = getInlineViewportMetrics(runtime.win || {}, doc);
        const minLeft = viewport.left + 8;
        const minTop = viewport.top + 8;
        const maxLeft = Math.max(minLeft, viewport.right - root.offsetWidth - 8);
        const maxTop = Math.max(minTop, viewport.bottom - root.offsetHeight - 8);
        const nextLeft = clampNumber(startLeft + dx, minLeft, maxLeft);
        const nextTop = clampNumber(startTop + dy, minTop, maxTop);
        floatingState.dragged = true;
        floatingState.left = nextLeft;
        floatingState.top = nextTop;
        runtime.dragged = true;
        root.style.left = `${Math.round(nextLeft)}px`;
        root.style.top = `${Math.round(nextTop)}px`;
        root.style.right = 'auto';
        root.style.bottom = 'auto';
        root.style.transform = 'none';
    };

    const onUp = () => {
        if (!active) return;
        active = false;
        try {
            if (pointerId !== null && typeof layer.releasePointerCapture === 'function') {
                layer.releasePointerCapture(pointerId);
            }
        } catch (error) {
            // Ignore missing pointer capture implementations.
        }
        if (typeof doc.removeEventListener === 'function') {
            doc.removeEventListener('pointermove', onMove);
            doc.removeEventListener('pointerup', onUp);
            doc.removeEventListener('pointercancel', onUp);
        }
        root.classList.remove('is-dragging');
        if (dragging && typeof onSuppressClick === 'function') onSuppressClick();
        dragging = false;
        pointerId = null;
    };

    layer.addEventListener('pointerdown', (event) => {
        if (event && event.button !== undefined && event.button !== 0) return;
        const clientX = Number(event && event.clientX);
        const clientY = Number(event && event.clientY);
        if (!Number.isFinite(clientX) || !Number.isFinite(clientY)) return;
        active = true;
        dragging = false;
        startX = clientX;
        startY = clientY;
        pointerId = event && event.pointerId !== undefined ? event.pointerId : null;
        const rect = typeof root.getBoundingClientRect === 'function'
            ? root.getBoundingClientRect()
            : { left: 0, top: 0 };
        startLeft = Number.isFinite(rect.left) ? rect.left : 0;
        startTop = Number.isFinite(rect.top) ? rect.top : 0;
        try {
            if (pointerId !== null && typeof layer.setPointerCapture === 'function') {
                layer.setPointerCapture(pointerId);
            }
        } catch (error) {
            // Ignore missing pointer capture implementations.
        }
        doc.addEventListener('pointermove', onMove);
        doc.addEventListener('pointerup', onUp);
        doc.addEventListener('pointercancel', onUp);
    });

    addRuntimeCleanup(runtime, () => {
        if (typeof doc.removeEventListener === 'function') {
            doc.removeEventListener('pointermove', onMove);
            doc.removeEventListener('pointerup', onUp);
            doc.removeEventListener('pointercancel', onUp);
        }
        root.classList.remove('is-dragging');
    });
}

function applyWebReaderRuntime(root, runtime) {
    const win = runtime.win || {};
    const doc = runtime.doc;
    if (!doc || !doc.body || !doc.documentElement) return;
    const savedScrollY = Number(win.scrollY) || 0;
    const savedBody = {
        overflow: doc.body.style.overflow || '',
        position: doc.body.style.position || '',
        width: doc.body.style.width || '',
        top: doc.body.style.top || '',
    };
    const savedHtmlOverflow = doc.documentElement.style.overflow || '';

    doc.body.style.overflow = 'hidden';
    doc.body.style.position = 'fixed';
    doc.body.style.width = '100%';
    doc.body.style.top = `-${savedScrollY}px`;
    doc.documentElement.style.overflow = 'hidden';

    const syncHeight = () => {
        const height = runtime.win && runtime.win.visualViewport && Number.isFinite(runtime.win.visualViewport.height)
            ? runtime.win.visualViewport.height
            : Number(runtime.win && runtime.win.innerHeight) || 0;
        if (height > 0) root.style.height = `${Math.round(height)}px`;
        syncOrientationClass(root, win);
    };
    syncHeight();
    if (win.visualViewport) addEventListenerWithCleanup(win.visualViewport, 'resize', syncHeight, runtime);
    addEventListenerWithCleanup(win, 'orientationchange', syncHeight, runtime);
    if (win.screen && win.screen.orientation) {
        addEventListenerWithCleanup(win.screen.orientation, 'change', syncHeight, runtime);
    }

    addRuntimeCleanup(runtime, () => {
        doc.body.style.overflow = savedBody.overflow;
        doc.body.style.position = savedBody.position;
        doc.body.style.width = savedBody.width;
        doc.body.style.top = savedBody.top;
        doc.documentElement.style.overflow = savedHtmlOverflow;
        if (typeof win.scrollTo === 'function') win.scrollTo(0, savedScrollY);
    });
}

function applyFullscreenReaderRuntime(root, current, runtime, ctx = {}) {
    const doc = runtime.doc;
    if (!doc) return;
    const win = runtime.win || doc.defaultView || globalThis;

    // requestFullscreen() 会重置浏览器的音频自动播放许可上下文，
    // 导致 vertin-tips 等插件在进入全屏后调用 HTMLAudioElement.play() 被静默拒绝。
    // 在用户手势调用链仍然有效时提前 resume 一个无声 AudioContext，以保留许可。
    try {
        const AudioCtorKey = win.AudioContext ? 'AudioContext' : (win.webkitAudioContext ? 'webkitAudioContext' : null);
        if (AudioCtorKey) {
            const ac = new win[AudioCtorKey]();
            ac.resume().catch(() => {});
            ac.close().catch(() => {});
        }
    } catch (e) { /* ignore — AudioContext not available */ }

    syncOrientationClass(root, win);
    const onOrientationChange = () => syncOrientationClass(root, win);
    addEventListenerWithCleanup(win, 'orientationchange', onOrientationChange, runtime);
    if (win.screen && win.screen.orientation) {
        addEventListenerWithCleanup(win.screen.orientation, 'change', onOrientationChange, runtime);
    }

    const target = doc.documentElement || doc.body;
    const request = target && (target.requestFullscreen || target.webkitRequestFullscreen);
    if (typeof request === 'function' && !doc.fullscreenElement && !doc.webkitFullscreenElement) {
        try {
            const result = request.call(target);
            if (result && typeof result.catch === 'function') result.catch(() => {});
        } catch (error) {
            // Ignore fullscreen failures in simulation.
        }
    }
}
function exitDocumentFullscreen(doc) {
    if (!doc) return;
    const exit = doc.exitFullscreen || doc.webkitExitFullscreen;
    if ((doc.fullscreenElement || doc.webkitFullscreenElement) && typeof exit === 'function') {
        try {
            const result = exit.call(doc);
            if (result && typeof result.catch === 'function') result.catch(() => {});
        } catch (error) {
            // Ignore exit failures.
        }
    }
}

function getInlineViewportMetrics(win, doc) {
    const viewport = win && win.visualViewport;
    const docEl = doc && doc.documentElement ? doc.documentElement : {};
    const fallbackWidth = Math.round(Number(win && win.innerWidth) || Number(docEl.clientWidth) || 0);
    const fallbackHeight = Math.round(Number(win && win.innerHeight) || Number(docEl.clientHeight) || 0);
    let left = Math.round(viewport && Number.isFinite(viewport.offsetLeft) ? viewport.offsetLeft : 0);
    let top = Math.round(viewport && Number.isFinite(viewport.offsetTop) ? viewport.offsetTop : 0);
    let width = Math.round(viewport && Number.isFinite(viewport.width) ? viewport.width : 0);
    let height = Math.round(viewport && Number.isFinite(viewport.height) ? viewport.height : 0);
    if (width < 240 && fallbackWidth >= 240) {
        width = fallbackWidth;
        left = 0;
    }
    if (height < 240 && fallbackHeight >= 240) {
        height = fallbackHeight;
        top = 0;
    }
    width = width || fallbackWidth || 320;
    height = height || fallbackHeight || 480;
    return {
        left,
        top,
        width,
        height,
        right: left + width,
        bottom: top + height,
    };
}

function addEventListenerWithCleanup(target, type, handler, runtime) {
    if (!target || typeof target.addEventListener !== 'function') return;
    target.addEventListener(type, handler);
    addRuntimeCleanup(runtime, () => {
        if (typeof target.removeEventListener === 'function') {
            target.removeEventListener(type, handler);
        }
    });
}

// 根据当前窗口宽高比在 root 上切换 igs-landscape / igs-portrait CSS 类，
// 供全屏和网页模式下的布局 CSS 感知方向变化。
function syncOrientationClass(root, win) {
    if (!root || typeof root.classList === 'undefined') return;
    const w = Number((win && win.innerWidth) || 0);
    const h = Number((win && win.innerHeight) || 0);
    const isLandscape = w > 0 && h > 0 && w > h;
    if (isLandscape) {
        root.classList.add('igs-landscape');
        root.classList.remove('igs-portrait');
    } else {
        root.classList.add('igs-portrait');
        root.classList.remove('igs-landscape');
    }
}

// 切换模式时清除 applyInlineReaderRuntime 写在 root 上的所有内联样式，
// 避免残留值阻止 web/fullscreen 模式的 CSS 类规则生效。
function resetInlineModeStyles(root) {
    if (!root || !root.style) return;
    const props = [
        'top', 'right', 'bottom', 'left', 'width', 'height',
        'maxWidth', 'maxHeight', 'transform', 'borderRadius',
        'boxShadow', 'overflow', 'boxSizing', 'zIndex',
    ];
    for (const prop of props) root.style[prop] = '';
}

__igsDefine(exports, "applyReaderModeRuntime", () => applyReaderModeRuntime);
__igsDefine(exports, "clearReaderModeRuntime", () => clearReaderModeRuntime);
__igsDefine(exports, "addRuntimeCleanup", () => addRuntimeCleanup);
__igsDefine(exports, "exitDocumentFullscreen", () => exitDocumentFullscreen);
});
__igsRegister("src/visual/igs-ui/sprite-edit.js", function(module, exports, require) {
const { resolveSpriteLayout } = require("src/visual/igs-ui/settings-normalize.js");
const { igsDebug } = require("src/visual/igs-ui/reader-value-utils.js");
function enterSpriteEditMode(overlay, current, ctx = {}) {
    if (current.spriteEditMode) return;
    const spriteEl = overlay.querySelector('#igs-sprite');
    if (!spriteEl || spriteEl.style.display === 'none') {
        if (typeof ctx.writeToast === 'function') ctx.writeToast('当前无立绘可编辑');
        return;
    }
    if (typeof ctx.closeSettings === 'function') ctx.closeSettings();
    const mode = current.snapshot.mode;
    const rs = current.snapshot.readerSettings;
    const character = current.snapshot.content.spriteCharacter || current.snapshot.content.speaker || '';
    const mood = current.snapshot.content.spriteMood || '';
    const modeLayout = resolveSpriteLayout(rs.spriteLayouts, mode, character, mood);
    const orig = { ...modeLayout };
    let posX = orig.posX, posY = orig.posY, scale = orig.scale;
    igsDebug('[DEBUG-sprite] enter-edit', { mode, character, mood, layoutKey: character ? `${mode}::${character}::${mood}` : mode, resolved: { ...orig }, allLayouts: rs.spriteLayouts });
    const clickLayer = overlay.querySelector('#igs-click-layer');
    if (clickLayer) clickLayer.style.pointerEvents = 'none';

    const origSpriteStyle = {
        position: spriteEl.style.position,
        inset: spriteEl.style.inset,
        width: spriteEl.style.width,
        height: spriteEl.style.height,
        transform: spriteEl.style.transform,
        bottom: spriteEl.style.bottom,
        left: spriteEl.style.left,
    };
    spriteEl.style.cssText += ';position:absolute;inset:0;width:100%;height:100%;transform:none;bottom:auto;left:auto';
    spriteEl.classList.add('igs-sprite-editing');

    const doc = overlay.ownerDocument;
    const editBar = doc.createElement('div');
    editBar.id = 'igs-sprite-edit-bar';
    editBar.innerHTML = '<span class="igs-se-hint">拖动调整，滚轮/双指缩放</span>'
        + '<button data-se="reset" type="button">还原</button>'
        + '<button data-se="cancel" type="button">取消</button>'
        + '<button data-se="save" class="igs-se-save" type="button">保存</button>';
    overlay.appendChild(editBar);
    current.spriteEditMode = { orig, editBar, clickLayer, mode, character, mood, origSpriteStyle };

    function apply() {
        spriteEl.style.backgroundSize = `${scale}%`;
        spriteEl.style.backgroundPosition = `${posX}% ${posY}%`;
    }
    apply();

    editBar.addEventListener('click', (event) => {
        const btn = event.target.closest('[data-se]');
        if (!btn) return;
        const act = btn.getAttribute('data-se');
        if (act === 'reset') { posX = 50; posY = 100; scale = 100; apply(); }
        else if (act === 'cancel') { exitSpriteEditMode(overlay, current, null, ctx); }
        else if (act === 'save') { exitSpriteEditMode(overlay, current, { posX, posY, scale }, ctx); }
    });

    const pointers = new Map();
    let dragStart = null, pinchStart = null;
    spriteEl.addEventListener('pointerdown', (event) => {
        event.preventDefault();
        pointers.set(event.pointerId, { x: event.clientX, y: event.clientY });
        try { spriteEl.setPointerCapture(event.pointerId); } catch (e) {}
        if (pointers.size === 1) {
            dragStart = { x: event.clientX, y: event.clientY, posX, posY };
            spriteEl.classList.add('is-dragging');
        } else if (pointers.size === 2) {
            dragStart = null;
            const pts = [...pointers.values()];
            pinchStart = { dist: Math.hypot(pts[1].x - pts[0].x, pts[1].y - pts[0].y), scale };
        }
    });
    spriteEl.addEventListener('pointermove', (event) => {
        if (!pointers.has(event.pointerId)) return;
        pointers.set(event.pointerId, { x: event.clientX, y: event.clientY });
        if (pointers.size === 1 && dragStart) {
            const rect = spriteEl.getBoundingClientRect ? spriteEl.getBoundingClientRect() : { width: 400, height: 600 };
            posX = dragStart.posX + (event.clientX - dragStart.x) / rect.width * 100;
            posY = dragStart.posY + (event.clientY - dragStart.y) / rect.height * 100;
            igsDebug('[DEBUG-sprite] drag', { dx: event.clientX - dragStart.x, dy: event.clientY - dragStart.y, rectW: Math.round(rect.width), rectH: Math.round(rect.height), posX: Math.round(posX), posY: Math.round(posY) });
            apply();
        } else if (pointers.size === 2 && pinchStart) {
            const pts = [...pointers.values()];
            const dist = Math.hypot(pts[1].x - pts[0].x, pts[1].y - pts[0].y);
            scale = Math.max(-500, Math.min(500, pinchStart.scale * (dist / pinchStart.dist)));
            apply();
        }
    });
    function endPointer(event) {
        pointers.delete(event.pointerId);
        if (pointers.size === 0) { dragStart = null; spriteEl.classList.remove('is-dragging'); }
        else if (pointers.size === 1) {
            pinchStart = null;
            const [ptr] = pointers.values();
            dragStart = { x: ptr.x, y: ptr.y, posX, posY };
        }
    }
    spriteEl.addEventListener('pointerup', endPointer);
    spriteEl.addEventListener('pointercancel', endPointer);
    spriteEl.addEventListener('wheel', (event) => {
        event.preventDefault();
        scale = Math.max(-500, Math.min(500, scale * (event.deltaY < 0 ? 1.1 : 0.91)));
        apply();
    }, { passive: false });
}
function exitSpriteEditMode(overlay, current, save, ctx = {}) {
    const em = current.spriteEditMode;
    if (!em) return;
    current.spriteEditMode = null;
    const spriteEl = overlay.querySelector('#igs-sprite');
    if (spriteEl) {
        spriteEl.classList.remove('igs-sprite-editing', 'is-dragging');
    }
    if (em.clickLayer) em.clickLayer.style.pointerEvents = '';
    if (em.editBar && em.editBar.parentNode) em.editBar.remove();
    if (save) {
        const unified = typeof ctx.resolveUnifiedSettings === 'function'
            ? ctx.resolveUnifiedSettings({ mode: em.mode })
            : { readerSettings: {} };
        const layouts = { ...(unified.readerSettings.spriteLayouts || {}) };
        const value = { posX: save.posX, posY: save.posY, scale: save.scale };
        if (!em.character) {
            layouts[em.mode] = value;
        } else {
            const sceneAssets = (unified.bridge && unified.bridge.sceneAssets) || {};
            const unified_ = sceneAssets.unifiedSpriteLayout === true;
            if (unified_) {
                // Apply to every known mood slot of this character so all expressions
                // share one position, while keeping the mode::char::mood key format.
                const charMoods = (sceneAssets.characters && sceneAssets.characters[em.character])
                    ? Object.keys(sceneAssets.characters[em.character])
                    : [];
                if (charMoods.length) {
                    for (const m of charMoods) {
                        layouts[`${em.mode}::${em.character}::${m}`] = { ...value };
                    }
                } else {
                    layouts[`${em.mode}::${em.character}`] = value;
                }
            } else if (em.mood) {
                layouts[`${em.mode}::${em.character}::${em.mood}`] = value;
            } else {
                // 空 mood 必须存到 mode::char，与 resolveSpriteLayout 的 charKey 对齐，
                // 否则重渲染查不回（v0.5.4/v0.13.1 回归）。
                layouts[`${em.mode}::${em.character}`] = value;
            }
        }
        if (typeof ctx.saveReaderSettingsPatch === 'function') {
            ctx.saveReaderSettingsPatch({ spriteLayouts: layouts });
        }
    } else {
        if (spriteEl) Object.assign(spriteEl.style, em.origSpriteStyle);
        if (em.orig && spriteEl) {
            spriteEl.style.backgroundSize = `${em.orig.scale}%`;
            spriteEl.style.backgroundPosition = `${em.orig.posX}% ${em.orig.posY}%`;
        }
    }
}

__igsDefine(exports, "enterSpriteEditMode", () => enterSpriteEditMode);
__igsDefine(exports, "exitSpriteEditMode", () => exitSpriteEditMode);
});
__igsRegister("src/shujuku-panel/panel-controller.js", function(module, exports, require) {
const { createShujukuClient } = require("src/data/shujuku/client.js");
const { applyTransparentGlassMaterial } = require("src/styles/glass-material.js");
const { parseTables } = require("src/shujuku-panel/panel-model.js");
const { getDbPanelStyles, renderDbPanelInner } = require("src/shujuku-panel/panel-render.js");
function toShujukuApiRowIndex(rowIndex) {
    return Number.isInteger(rowIndex) ? rowIndex + 1 : NaN;
}
function createDbTabClickGuard(now = () => Date.now()) {
    let blockedClick = null;
    const blockMs = 120;
    const maxDistance = 8;

    function arm(input = {}) {
        if (!input.strip) return;
        blockedClick = {
            strip: input.strip,
            clientX: toFiniteNumber(input.clientX),
            clientY: toFiniteNumber(input.clientY),
            expiresAt: now() + blockMs,
        };
    }

    function shouldSuppress(event, tab) {
        if (!blockedClick) return false;
        if (now() > blockedClick.expiresAt) {
            blockedClick = null;
            return false;
        }
        if (!tab || typeof tab.closest !== 'function' || tab.closest('.igs-shujuku-tabs') !== blockedClick.strip) {
            return false;
        }
        const clientX = toFiniteNumber(event && event.clientX);
        const clientY = toFiniteNumber(event && event.clientY);
        if (!Number.isFinite(clientX) || !Number.isFinite(clientY)) return false;
        if (Number.isFinite(blockedClick.clientX) && Math.abs(clientX - blockedClick.clientX) > maxDistance) return false;
        if (Number.isFinite(blockedClick.clientY) && Math.abs(clientY - blockedClick.clientY) > maxDistance) return false;
        blockedClick = null;
        return true;
    }

    function clear() {
        blockedClick = null;
    }

    return { arm, shouldSuppress, clear };
}
function createDbPanelController(doc, global) {
    let root = null;
    let modalRoot = null;
    let client = null;
    let igsWriting = false;
    const tabClickGuard = createDbTabClickGuard();
    const state = { tables: [], activeUid: '', status: 'loading', errorMsg: '', externalPending: false, tabScrollLeft: 0 };

    const externalCallback = () => {
        if (igsWriting || !root) return;
        state.externalPending = true;
        render();
    };

    function ensureStyle() {
        const id = 'igs-db-panel-style';
        if (!doc.getElementById(id)) {
            const s = doc.createElement('style');
            s.id = id;
            s.textContent = getDbPanelStyles();
            doc.head.appendChild(s);
        }
    }

    function render() {
        if (!root) return;
        const inner = root.querySelector('#igs-db-inner');
        if (inner) {
            rememberTabScroll();
            inner.innerHTML = renderDbPanelInner(state);
            restoreTabScroll();
        }
    }

    function resolvePanelContainer(overlayEl) {
        if (overlayEl && overlayEl.querySelector) {
            const layer = overlayEl.querySelector('#igs-db-layer');
            if (layer) return layer;
        }
        return overlayEl || doc.body;
    }

    function rememberTabScroll() {
        const tabs = root && root.querySelector ? root.querySelector('.igs-shujuku-tabs') : null;
        if (tabs && Number.isFinite(Number(tabs.scrollLeft))) {
            state.tabScrollLeft = Math.max(0, Number(tabs.scrollLeft) || 0);
        }
    }

    function restoreTabScroll() {
        const tabs = root && root.querySelector ? root.querySelector('.igs-shujuku-tabs') : null;
        if (tabs && state.tabScrollLeft > 0) {
            tabs.scrollLeft = state.tabScrollLeft;
        }
    }

    async function loadData() {
        const api = (global || globalThis).AutoCardUpdaterAPI || null;
        client = createShujukuClient(api);
        const result = client.readTables();
        if (!result.ok) {
            state.status = result.reason === 'missing-api' ? 'no-api' : 'error';
            state.errorMsg = result.reason || '';
            render();
            return;
        }
        state.tables = parseTables(result.data);
        state.status = 'ready';
        if (!state.tables.find(t => t.uid === state.activeUid))
            state.activeUid = state.tables[0] ? state.tables[0].uid : '';
        state.externalPending = false;
        render();
    }

    function open(overlayEl, readerSettings) {
        if (root) return;

        // conflict detection
        const shujukuOpen = doc.querySelector('#shujuku_v104-main-window,[id^="shujuku"][id$="-main-window"]');
        if (shujukuOpen && shujukuOpen.offsetParent !== null) {
            const win = doc.defaultView || globalThis;
            if (!win.confirm('骰子系统数据库面板正在打开中，同时编辑可能导致数据冲突，是否继续？'))
                return;
        }

        ensureStyle();
        root = doc.createElement('div');
        root.id = 'igs-db-panel';
        root.className = 'igs-shujuku-panel';
        root.innerHTML = `<div class="igs-shujuku-header">`
            + `<span class="igs-shujuku-title">数据库</span>`
            + `<button class="igs-shujuku-close" data-db-act="close" title="关闭">×</button>`
            + `</div><div id="igs-db-inner"></div>`;

        const container = resolvePanelContainer(overlayEl);
        container.appendChild(root);

        applyTransparentGlassMaterial(root, readerSettings && readerSettings.glassOpacity, {
            backdropFilter: readerSettings && readerSettings.glassBackdropFilter,
        });

        // drag within container
        const header = root.querySelector('.igs-shujuku-header');
        if (header) attachDrag(header, root, container);
        attachTabsDragScroll(root);

        root.addEventListener('click', onClick);
        root.addEventListener('focusout', onFocusOut);

        client = createShujukuClient((global || globalThis).AutoCardUpdaterAPI || null);
        client.registerCallback(externalCallback);
        state.status = 'loading';
        render();
        loadData();
    }

    function close() {
        if (!root) return;
        root.removeEventListener('click', onClick);
        root.removeEventListener('focusout', onFocusOut);
        root.remove();
        root = null;
        closeModal();
        if (client) client.unregisterCallback(externalCallback);
    }

    function toggle(overlayEl, readerSettings) { root ? close() : open(overlayEl, readerSettings); }

    async function onClick(event) {
        const act = event.target.closest('[data-db-act]');
        if (act) {
            const action = act.getAttribute('data-db-act');
            if (action === 'close') { close(); return; }
            if (action === 'refresh') { state.externalPending = false; await loadData(); return; }
            if (action === 'add-row') { await doAddRow(); return; }
            if (action === 'delete-row') {
                await doDeleteRow(
                    parseInt(act.getAttribute('data-db-row-index'), 10),
                    act.getAttribute('data-db-row-id')
                );
                return;
            }
        }
        const tab = event.target.closest('[data-db-tab]');
        if (tab) {
            // 拖动标签栏后抑制本次 click，避免误切换标签
            if (tabClickGuard.shouldSuppress(event, tab)) return;
            state.activeUid = tab.getAttribute('data-db-tab'); render(); return;
        }

        const editCell = event.target.closest('[data-db-edit]');
        if (editCell) { activateInline(editCell); return; }

        const expandCell = event.target.closest('[data-db-expand]');
        if (expandCell) { openModal(expandCell); }
    }

    function activateInline(cell) {
        const [ri, ci] = cell.getAttribute('data-db-edit').split(':').map(Number);
        const table = activeTable();
        if (!table) return;
        const row = table.rows[ri];
        if (!row) return;
        const span = cell.querySelector('.igs-shujuku-cell');
        if (!span) return;
        const input = doc.createElement('input');
        input.className = 'igs-shujuku-cell-input';
        input.value = String(row[ci] ?? '');
        input.setAttribute('data-db-commit', `${ri}:${ci}`);
        // 替换 td 内的 span，保留 td 本身（维持列结构）；防止再次触发 td 的编辑激活。
        cell.removeAttribute('data-db-edit');
        span.replaceWith(input);
        input.focus();
        input.select();
    }

    async function onFocusOut(event) {
        const input = event.target;
        if (!input || !input.hasAttribute('data-db-commit')) return;
        const [ri, ci] = input.getAttribute('data-db-commit').split(':').map(Number);
        const table = activeTable();
        if (!table) { render(); return; }
        const row = table.rows[ri];
        if (!row) { render(); return; }
        const oldVal = String(row[ci] ?? '');
        const newVal = input.value;
        if (newVal === oldVal) { render(); return; }
        const colName = table.columns[ci];
        igsWriting = true;
        row[ci] = newVal;
        render();
        const result = await client.updateCell(table.name, toShujukuApiRowIndex(ri), colName, newVal);
        igsWriting = false;
        if (!result.ok) { row[ci] = oldVal; render(); }
        else await client.refresh();
    }

    async function doAddRow() {
        const table = activeTable();
        if (!table || !client) return;
        const maxId = table.rows.reduce((m, r) => Math.max(m, parseInt(r[0] || 0, 10)), 0);
        const rowData = Object.fromEntries(table.columns.map((col, i) => [col, i === 0 ? String(maxId + 1) : '']));
        igsWriting = true;
        const result = await client.insertRow(table.name, rowData);
        igsWriting = false;
        if (result.ok) await loadData();
    }

    async function doDeleteRow(rowIndex, rowId) {
        const table = activeTable();
        if (!table || !client) return;
        if (!Number.isInteger(rowIndex) || rowIndex < 0 || rowIndex >= table.rows.length) return;
        const win = doc.defaultView || globalThis;
        const label = rowId != null && rowId !== '' ? `row_id=${rowId}` : `第 ${rowIndex + 1} 行`;
        if (!win.confirm(`确认删除 ${label} 的行？`)) return;
        igsWriting = true;
        const result = await client.deleteRow(table.name, toShujukuApiRowIndex(rowIndex));
        igsWriting = false;
        if (result.ok) await loadData();
    }

    function openModal(expandEl) {
        const [ri, ci] = expandEl.getAttribute('data-db-expand').split(':').map(Number);
        const table = activeTable();
        if (!table) return;
        const row = table.rows[ri];
        if (!row) return;
        const colName = table.columns[ci];
        const origVal = String(row[ci] ?? '');
        closeModal();
        modalRoot = doc.createElement('div');
        modalRoot.className = 'igs-db-modal-bg';
        modalRoot.innerHTML = `<div class="igs-db-modal">`
            + `<div class="igs-db-modal-label">${esc(table.name)} · ${esc(colName)}</div>`
            + `<textarea>${esc(origVal)}</textarea>`
            + `<div class="igs-db-modal-btns">`
            + `<button class="igs-db-modal-cancel">取消</button>`
            + `<button class="igs-db-modal-save">保存</button>`
            + `</div></div>`;
        (root && root.parentElement || doc.body).appendChild(modalRoot);
        const textarea = modalRoot.querySelector('textarea');
        modalRoot.querySelector('.igs-db-modal-cancel').addEventListener('click', closeModal);
        modalRoot.querySelector('.igs-db-modal-save').addEventListener('click', async () => {
            const newVal = textarea.value;
            closeModal();
            if (newVal === origVal) return;
            igsWriting = true;
            row[ci] = newVal;
            render();
            const result = await client.updateCell(table.name, toShujukuApiRowIndex(ri), colName, newVal);
            igsWriting = false;
            if (!result.ok) { row[ci] = origVal; render(); }
            else await client.refresh();
        });
        textarea.focus();
    }

    function closeModal() { if (modalRoot) { modalRoot.remove(); modalRoot = null; } }

    function activeTable() { return state.tables.find(t => t.uid === state.activeUid) || state.tables[0] || null; }

    function attachTabsDragScroll(panel) {
        if (!panel) return;
        let strip = null;
        let active = false;
        let moved = false;
        let startX = 0;
        let startScroll = 0;
        let pointerId = null;
        let captured = false;

        panel.addEventListener('scroll', (e) => {
            const target = e.target;
            if (target && target.classList && target.classList.contains('igs-shujuku-tabs')) {
                state.tabScrollLeft = Math.max(0, Number(target.scrollLeft) || 0);
            }
        }, true);

        panel.addEventListener('pointerdown', (e) => {
            const s = e.target.closest('.igs-shujuku-tabs');
            if (!s) return;
            // 仅当内容溢出时才进入拖动滚动
            if (s.scrollWidth <= s.clientWidth + 1) return;
            strip = s;
            active = true;
            moved = false;
            startX = e.clientX;
            startScroll = s.scrollLeft;
            pointerId = e.pointerId;
            captured = false;
        });
        panel.addEventListener('pointermove', (e) => {
            if (!active || !strip) return;
            const dx = e.clientX - startX;
            if (!moved && Math.abs(dx) < 4) return;
            if (!moved) {
                moved = true;
                try {
                    if (pointerId != null && strip.setPointerCapture) {
                        strip.setPointerCapture(pointerId);
                        captured = true;
                    }
                } catch (err) { /* ignore */ }
                strip.classList.add('igs-db-dragging');
            }
            strip.scrollLeft = startScroll - dx;
            state.tabScrollLeft = Math.max(0, Number(strip.scrollLeft) || 0);
            if (e.cancelable) e.preventDefault();
        });
        const end = (e) => {
            if (!active) return;
            const endedStrip = strip;
            active = false;
            if (strip) {
                try { if (captured && pointerId != null && strip.releasePointerCapture) strip.releasePointerCapture(pointerId); } catch (err) { /* ignore */ }
                strip.classList.remove('igs-db-dragging');
            }
            // 拖动后抑制紧随的 click，避免误切标签
            if (moved) {
                if (endedStrip) tabClickGuard.arm({ strip: endedStrip, clientX: e && e.clientX, clientY: e && e.clientY });
                const win = doc.defaultView || globalThis;
                if (win && typeof win.setTimeout === 'function') {
                    win.setTimeout(tabClickGuard.clear, 160);
                }
            }
            strip = null;
            pointerId = null;
            captured = false;
        };
        panel.addEventListener('pointerup', end);
        panel.addEventListener('pointercancel', end);
    }

    function attachDrag(handle, panel, container) {
        handle.addEventListener('mousedown', (e) => {
            if (e.button !== 0 || e.target.closest('[data-db-act]')) return;
            e.preventDefault();
            const cRect = container.getBoundingClientRect();
            const pRect = panel.getBoundingClientRect();
            const startLeft = pRect.left - cRect.left;
            const startTop = pRect.top - cRect.top;
            const startX = e.clientX;
            const startY = e.clientY;
            panel.style.right = 'auto';
            panel.style.left = `${startLeft}px`;
            panel.style.top = `${startTop}px`;

            function onMove(e) {
                const maxLeft = container.clientWidth - panel.offsetWidth;
                const maxTop = container.clientHeight - panel.offsetHeight;
                panel.style.left = `${Math.max(0, Math.min(maxLeft, startLeft + e.clientX - startX))}px`;
                panel.style.top = `${Math.max(0, Math.min(maxTop, startTop + e.clientY - startY))}px`;
            }
            function onUp() {
                doc.removeEventListener('mousemove', onMove);
                doc.removeEventListener('mouseup', onUp);
            }
            doc.addEventListener('mousemove', onMove);
            doc.addEventListener('mouseup', onUp);
        });
    }

    return { open, close, toggle };
}

function esc(str) {
    return String(str ?? '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
}

function toFiniteNumber(value) {
    const number = Number(value);
    return Number.isFinite(number) ? number : NaN;
}

__igsDefine(exports, "toShujukuApiRowIndex", () => toShujukuApiRowIndex);
__igsDefine(exports, "createDbTabClickGuard", () => createDbTabClickGuard);
__igsDefine(exports, "createDbPanelController", () => createDbPanelController);
});
__igsRegister("src/data/shujuku/client.js", function(module, exports, require) {
function createShujukuClient(api) {
    return {
        // kept for backward compat — simulate.test.js depends on this
        async updateRowAndRefresh(tableName, rowIndex, patch) {
            if (!api || typeof api.updateRow !== 'function')
                return { ok: false, reason: 'missing-update-row' };
            const updateResult = await api.updateRow(tableName, rowIndex, patch);
            if (!isSuccessful(updateResult))
                return { ok: false, reason: 'update-row-failed', result: updateResult };
            if (typeof api.refreshDataAndWorldbook === 'function') {
                const refreshResult = await api.refreshDataAndWorldbook();
                if (!isSuccessful(refreshResult))
                    return { ok: false, reason: 'refresh-worldbook-failed', result: refreshResult };
            }
            return { ok: true, tableName, rowIndex, patch };
        },

        readTables() {
            if (!api || typeof api.exportTableAsJson !== 'function')
                return { ok: false, reason: 'missing-api' };
            try {
                return { ok: true, data: api.exportTableAsJson() };
            } catch (e) {
                return { ok: false, reason: String(e && e.message || 'read-failed') };
            }
        },

        async updateCell(tableName, rowIndex, colName, value) {
            if (!api || typeof api.updateCell !== 'function')
                return { ok: false, reason: 'missing-api' };
            const result = await api.updateCell(tableName, rowIndex, colName, String(value));
            return isSuccessful(result) ? { ok: true } : { ok: false, reason: 'update-failed' };
        },

        async insertRow(tableName, rowData) {
            if (!api || typeof api.insertRow !== 'function')
                return { ok: false, reason: 'missing-api' };
            const result = await api.insertRow(tableName, rowData);
            return isSuccessful(result) ? { ok: true } : { ok: false, reason: 'insert-failed' };
        },

        async deleteRow(tableName, rowIndex) {
            if (!api || typeof api.deleteRow !== 'function')
                return { ok: false, reason: 'missing-api' };
            const result = await api.deleteRow(tableName, rowIndex);
            return isSuccessful(result) ? { ok: true } : { ok: false, reason: 'delete-failed' };
        },

        async refresh() {
            if (!api || typeof api.refreshDataAndWorldbook !== 'function')
                return { ok: false, reason: 'missing-api' };
            await api.refreshDataAndWorldbook();
            return { ok: true };
        },

        registerCallback(cb) {
            if (api && typeof api.registerTableUpdateCallback === 'function')
                api.registerTableUpdateCallback(cb);
        },

        unregisterCallback(cb) {
            if (api && typeof api.unregisterTableUpdateCallback === 'function')
                api.unregisterTableUpdateCallback(cb);
        },
    };
}

function isSuccessful(result) {
    if (result == null || result === false || result === -1) return false;
    if (typeof result === 'object' && result.success === false) return false;
    return true;
}

__igsDefine(exports, "createShujukuClient", () => createShujukuClient);
});
__igsRegister("src/styles/glass-material.js", function(module, exports, require) {
const IGS_TRANSPARENT_GLASS_ALPHA = 0.62;
const IGS_FROST_NIGHT_RGB = '31,34,37';
const IGS_TRANSPARENT_GLASS_BG = `rgba(${IGS_FROST_NIGHT_RGB},${IGS_TRANSPARENT_GLASS_ALPHA})`;
const IGS_TRANSPARENT_GLASS_BACKDROP_FILTER = 'none';
const IGS_FROSTED_GLASS_BACKDROP_FILTER = 'blur(32px) saturate(180%)';
function normalizeGlassDensity(value, fallback = IGS_TRANSPARENT_GLASS_ALPHA) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) return fallback;
    return Math.min(1, Math.max(0, numeric));
}
function createTransparentGlassBg(density) {
    return `rgba(${IGS_FROST_NIGHT_RGB},${normalizeGlassDensity(density)})`;
}
function resolveGlassBackdropFilter(value) {
    if (typeof value === 'string') {
        const trimmed = value.trim();
        if (!trimmed || trimmed === '0' || trimmed === 'false' || trimmed === 'none') return IGS_TRANSPARENT_GLASS_BACKDROP_FILTER;
        if (trimmed === 'true') return IGS_FROSTED_GLASS_BACKDROP_FILTER;
        return trimmed;
    }
    return value ? IGS_FROSTED_GLASS_BACKDROP_FILTER : IGS_TRANSPARENT_GLASS_BACKDROP_FILTER;
}
function applyTransparentGlassMaterial(target, density, options = {}) {
    const style = target && target.style ? target.style : target;
    if (!style || typeof style.setProperty !== 'function') return;
    const normalizedDensity = normalizeGlassDensity(density);
    const glassBg = createTransparentGlassBg(normalizedDensity);
    const backdropFilter = resolveGlassBackdropFilter(options.backdropFilter);
    style.setProperty('--igs-glass-opacity', String(normalizedDensity));
    style.setProperty('--igs-glass-density', String(normalizedDensity));
    style.setProperty('--igs-glass-fill-alpha', String(normalizedDensity));
    style.setProperty('--igs-transparent-glass-bg', glassBg);
    style.setProperty('--igs-glass-bg', glassBg);
    style.setProperty('--igs-glass-blur', backdropFilter);
    style.setProperty('--igs-dialog-bg', glassBg);
    style.setProperty('--igs-dialog-blur', backdropFilter);
    style.setProperty('--igs-toolbar-bg', glassBg);
    style.setProperty('--igs-toolbar-blur', backdropFilter);
    style.setProperty('--igs-choice-bg', glassBg);
    style.setProperty('--igs-choice-blur', backdropFilter);
    style.setProperty('--igs-db-bg', glassBg);
    style.setProperty('--igs-db-blur', backdropFilter);
    style.setProperty('--igs-db-head-bg', glassBg);
    style.setProperty('--igs-db-head-blur', backdropFilter);
}

__igsDefine(exports, "normalizeGlassDensity", () => normalizeGlassDensity);
__igsDefine(exports, "createTransparentGlassBg", () => createTransparentGlassBg);
__igsDefine(exports, "resolveGlassBackdropFilter", () => resolveGlassBackdropFilter);
__igsDefine(exports, "applyTransparentGlassMaterial", () => applyTransparentGlassMaterial);
__igsDefine(exports, "IGS_TRANSPARENT_GLASS_ALPHA", () => IGS_TRANSPARENT_GLASS_ALPHA);
__igsDefine(exports, "IGS_FROST_NIGHT_RGB", () => IGS_FROST_NIGHT_RGB);
__igsDefine(exports, "IGS_TRANSPARENT_GLASS_BG", () => IGS_TRANSPARENT_GLASS_BG);
__igsDefine(exports, "IGS_TRANSPARENT_GLASS_BACKDROP_FILTER", () => IGS_TRANSPARENT_GLASS_BACKDROP_FILTER);
__igsDefine(exports, "IGS_FROSTED_GLASS_BACKDROP_FILTER", () => IGS_FROSTED_GLASS_BACKDROP_FILTER);
});
__igsRegister("src/shujuku-panel/panel-model.js", function(module, exports, require) {
function parseTables(raw) {
    if (!raw || typeof raw !== 'object') return [];
    return Object.values(raw)
        .filter(v => v && typeof v.uid === 'string' && v.uid.startsWith('sheet_'))
        .sort((a, b) => (a.orderNo || 0) - (b.orderNo || 0))
        .map(s => ({
            uid: s.uid,
            name: s.name || s.uid,
            columns: Array.isArray(s.content) && Array.isArray(s.content[0]) ? s.content[0] : [],
            rows: Array.isArray(s.content) ? s.content.slice(1) : [],
        }));
}
function buildShujukuPanelState(input = {}) {
    const settings = normalizePanelSettings(input.settings);
    const result = input.result || {};
    const tables = Array.isArray(result.tables) ? result.tables : [];
    const selectedName = settings.sheetName
        ? settings.sheetName
        : tables[0] && tables[0].name || '';
    const selected = tables.find((table) => table.name === selectedName || table.key === selectedName) || tables[0] || null;

    return {
        ok: result.ok !== false,
        reason: result.reason || '',
        selectedName,
        tables: tables.map((table) => ({
            ...table,
            selected: selected && table.name === selected.name,
        })),
        selected: selected ? limitTableRows(selected, settings.rowLimit) : null,
        rowLimit: settings.rowLimit,
        writeMode: settings.writeMode,
        refreshMode: settings.refreshMode,
    };
}
function formatShujukuPanelMessage(result, fallback = '') {
    if (!result) return fallback;
    if (result.ok === false) return result.reason || fallback || 'shujuku 操作失败';
    return result.message || fallback || 'shujuku 操作已完成';
}

function limitTableRows(table, rowLimit) {
    const limit = Math.max(1, Math.min(1000, Number(rowLimit) || 50));
    return {
        ...table,
        rows: Array.isArray(table.rows) ? table.rows.slice(0, limit) : [],
        truncated: Array.isArray(table.rows) && table.rows.length > limit,
    };
}

function normalizePanelSettings(settings = {}) {
    return {
        sheetName: String(settings.sheetName || '').trim(),
        rowLimit: clampNumber(settings.rowLimit, 1, 1000, 50),
        writeMode: ['confirm', 'direct', 'readonly'].includes(settings.writeMode) ? settings.writeMode : 'confirm',
        refreshMode: ['after-write', 'manual', 'never'].includes(settings.refreshMode) ? settings.refreshMode : 'after-write',
    };
}

function clampNumber(value, min, max, fallback) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) return fallback;
    return Math.max(min, Math.min(max, Math.floor(numeric)));
}

__igsDefine(exports, "parseTables", () => parseTables);
__igsDefine(exports, "buildShujukuPanelState", () => buildShujukuPanelState);
__igsDefine(exports, "formatShujukuPanelMessage", () => formatShujukuPanelMessage);
});
__igsRegister("src/shujuku-panel/panel-render.js", function(module, exports, require) {
const LONG_TEXT = 80;
function getDbPanelStyles() {
    return `
#igs-db-panel{position:absolute;top:48px;right:12px;width:min(440px,calc(100% - 24px));max-height:calc(100% - 130px);background:var(--igs-db-bg,var(--igs-glass-bg,rgba(20,20,22,.62)));border:1px solid var(--igs-db-border,rgba(255,255,255,.14));-webkit-backdrop-filter:var(--igs-db-blur,none);backdrop-filter:var(--igs-db-blur,none);border-radius:var(--igs-db-radius,22px);box-shadow:var(--igs-db-shadow,0 12px 48px rgba(0,0,0,.50));display:flex;flex-direction:column;z-index:10;font-family:-apple-system,BlinkMacSystemFont,"PingFang SC","Segoe UI",sans-serif;color:#f4f4f6;overflow:hidden;user-select:none;pointer-events:auto;}
#igs-db-panel,#igs-db-panel *{scrollbar-width:none;-ms-overflow-style:none;}
#igs-db-panel ::-webkit-scrollbar{display:none;width:0;height:0;}
.igs-shujuku-header{display:flex;align-items:center;gap:8px;padding:13px 16px 10px;border-bottom:1px solid rgba(255,255,255,.08);flex-shrink:0;cursor:grab;}
.igs-shujuku-header:active{cursor:grabbing;}
.igs-shujuku-title{font-size:13px;font-weight:600;letter-spacing:.5px;flex:1;}
.igs-shujuku-close{width:26px;height:26px;border:none;background:transparent;color:rgba(255,255,255,.45);cursor:pointer;border-radius:7px;display:inline-flex;align-items:center;justify-content:center;font-size:18px;line-height:1;padding:0;}
.igs-shujuku-close:hover{background:rgba(255,255,255,.1);color:#fff;}
#igs-db-inner{flex:1;min-height:0;display:flex;flex-direction:column;}
.igs-shujuku-warning{padding:5px 16px;background:rgba(255,180,0,.1);border-bottom:1px solid rgba(255,180,0,.2);font-size:11px;color:#ffd166;display:flex;align-items:center;gap:8px;flex-shrink:0;}
.igs-shujuku-warning button{border:none;background:rgba(255,180,0,.16);color:#ffd166;border-radius:5px;padding:2px 8px;cursor:pointer;font-size:11px;}
.igs-shujuku-tabs{display:flex;gap:2px;padding:8px 14px 0;box-sizing:border-box;width:100%;max-width:100%;flex-shrink:0;overflow-x:auto;overflow-y:hidden;scrollbar-width:none;-ms-overflow-style:none;cursor:grab;touch-action:pan-x;overscroll-behavior:contain;}
.igs-shujuku-tabs.igs-db-dragging{cursor:grabbing;}
.igs-shujuku-tabs::-webkit-scrollbar{display:none;}
.igs-shujuku-tab{padding:4px 12px;border-radius:7px 7px 0 0;border:1px solid transparent;font-size:12px;cursor:pointer;background:transparent;color:rgba(255,255,255,.4);white-space:nowrap;flex-shrink:0;}
.igs-shujuku-tab:hover{color:rgba(255,255,255,.7);}
.igs-shujuku-tab.igs-db-active{background:rgba(255,255,255,.07);border-color:rgba(255,255,255,.12);color:#fff;}
.igs-shujuku-body{flex:1;min-height:0;overflow-y:auto;overflow-x:auto;scrollbar-width:none;-ms-overflow-style:none;}
.igs-shujuku-body::-webkit-scrollbar{display:none;}
.igs-shujuku-table{width:100%;border-collapse:collapse;font-size:12px;}
.igs-shujuku-table th{padding:7px 10px;text-align:left;font-size:11px;font-weight:600;color:rgba(255,255,255,.4);border-bottom:1px solid rgba(255,255,255,.1);position:sticky;top:0;z-index:1;background:var(--igs-db-head-bg,var(--igs-db-bg,rgba(20,20,22,.62)));-webkit-backdrop-filter:var(--igs-db-head-blur,var(--igs-db-blur,none));backdrop-filter:var(--igs-db-head-blur,var(--igs-db-blur,none));white-space:nowrap;}
.igs-shujuku-table td{padding:5px 10px;border-bottom:1px solid rgba(255,255,255,.05);vertical-align:top;max-width:220px;cursor:text;}
.igs-shujuku-table td.igs-db-ro-cell{cursor:default;}
.igs-shujuku-table tr:hover td{background:rgba(255,255,255,.025);}
.igs-shujuku-cell{cursor:text;word-break:break-word;overflow:hidden;display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:2;line-height:1.4;}
.igs-shujuku-cell.igs-db-ro{cursor:default;color:rgba(255,255,255,.4);}
.igs-shujuku-expand-hint{font-size:10px;color:rgba(92,170,255,.65);cursor:pointer;margin-left:3px;}
.igs-shujuku-cell-input{width:100%;box-sizing:border-box;background:rgba(255,255,255,.07);border:1px solid rgba(92,170,255,.45);border-radius:5px;color:#fff;padding:3px 6px;font-size:12px;font-family:inherit;outline:none;}
.igs-db-del-btn{opacity:0;border:none;background:rgba(255,80,80,.15);color:#ff7070;border-radius:5px;padding:1px 6px;cursor:pointer;font-size:11px;transition:opacity .12s;white-space:nowrap;}
.igs-shujuku-table tr:hover .igs-db-del-btn{opacity:1;}
.igs-shujuku-footer{padding:7px 14px;border-top:1px solid rgba(255,255,255,.07);display:flex;align-items:center;gap:8px;flex-shrink:0;}
.igs-db-add-btn{border:1px dashed rgba(255,255,255,.18);background:transparent;color:rgba(255,255,255,.5);border-radius:7px;padding:3px 10px;cursor:pointer;font-size:12px;}
.igs-db-add-btn:hover{background:rgba(255,255,255,.05);color:#fff;}
.igs-db-status{font-size:11px;color:rgba(255,255,255,.3);flex:1;text-align:right;}
.igs-shujuku-empty{padding:28px 16px;text-align:center;color:rgba(255,255,255,.28);font-size:13px;}
.igs-db-modal-bg{position:absolute;inset:0;background:rgba(0,0,0,.55);z-index:20;display:flex;align-items:center;justify-content:center;}
.igs-db-modal{background:rgba(22,22,26,.99);border:1px solid rgba(255,255,255,.14);border-radius:14px;width:min(580px,calc(100vw - 32px));padding:18px;display:flex;flex-direction:column;gap:10px;}
.igs-db-modal-label{font-size:12px;color:rgba(255,255,255,.45);}
.igs-db-modal textarea{width:100%;box-sizing:border-box;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.14);border-radius:8px;color:#f4f4f6;padding:9px;font-size:12px;font-family:inherit;outline:none;resize:vertical;min-height:140px;}
.igs-db-modal textarea:focus{border-color:rgba(92,170,255,.5);}
.igs-db-modal-btns{display:flex;gap:8px;justify-content:flex-end;}
.igs-db-modal-btns button{border-radius:8px;padding:5px 14px;cursor:pointer;font-size:12px;}
.igs-db-modal-save{background:rgba(92,170,255,.18);color:#7ab8ff;border:1px solid rgba(92,170,255,.3);}
.igs-db-modal-cancel{background:transparent;color:rgba(255,255,255,.5);border:1px solid rgba(255,255,255,.14);}
`.trim();
}
function renderDbPanelInner(state) {
    const { tables, activeUid, status, errorMsg, externalPending } = state;
    if (status === 'loading') return `<div class="igs-shujuku-empty">加载中…</div>`;
    if (status === 'no-api') return `<div class="igs-shujuku-empty">未检测到 shujuku 数据库插件</div>`;
    if (status === 'error') return `<div class="igs-shujuku-empty">加载失败：${e(errorMsg || '未知错误')}</div>`;
    if (!tables.length) return `<div class="igs-shujuku-empty">当前角色卡没有数据表</div>`;

    const active = tables.find(t => t.uid === activeUid) || tables[0];

    const warning = externalPending
        ? `<div class="igs-shujuku-warning">⚠ 表格已被外部更新 <button data-db-act="refresh">刷新</button></div>`
        : '';

    const tabs = tables.map(t =>
        `<button class="igs-shujuku-tab${t.uid === active.uid ? ' igs-db-active' : ''}" data-db-tab="${e(t.uid)}">${e(t.name)}</button>`
    ).join('');

    const tableHtml = renderTable(active);
    const rowCount = active.rows.length;

    return `${warning}<div class="igs-shujuku-tabs">${tabs}</div>`
        + `<div class="igs-shujuku-body">${tableHtml}</div>`
        + `<div class="igs-shujuku-footer"><button class="igs-db-add-btn" data-db-act="add-row">+ 新增行</button>`
        + `<span class="igs-db-status">${rowCount} 行</span></div>`;
}

function renderTable(table) {
    if (!table.columns.length) return `<div class="igs-shujuku-empty">空表</div>`;
    const ths = table.columns.map(col => `<th>${e(col)}</th>`).join('') + '<th></th>';
    const trs = table.rows.map((row, rowIdx) => {
        const tds = table.columns.map((_, colIdx) => {
            const val = String(row[colIdx] ?? '');
            // data-db-edit/expand 挂在 td 上：空格子的内层 span 会塌缩成 0×0 无法点击，
            // 放到 td（有 padding/列宽）保证空单元格也有可点击区域。
            if (colIdx === 0) return `<td class="igs-db-ro-cell"><span class="igs-shujuku-cell igs-db-ro">${e(val)}</span></td>`;
            if (val.length > LONG_TEXT) {
                return `<td data-db-expand="${rowIdx}:${colIdx}"><span class="igs-shujuku-cell">`
                    + `${e(val.slice(0, 60))}…<span class="igs-shujuku-expand-hint">✏</span></span></td>`;
            }
            return `<td data-db-edit="${rowIdx}:${colIdx}"><span class="igs-shujuku-cell">${e(val)}</span></td>`;
        }).join('');
        const rowId = parseInt(row[0], 10);
        return `<tr>${tds}<td class="igs-db-ro-cell"><button class="igs-db-del-btn" data-db-act="delete-row" data-db-row-index="${rowIdx}" data-db-row-id="${Number.isFinite(rowId) ? rowId : ''}">删除</button></td></tr>`;
    }).join('');
    return `<table class="igs-shujuku-table"><thead><tr>${ths}</tr></thead><tbody>${trs}</tbody></table>`;
}

function e(str) {
    return String(str ?? '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
}

__igsDefine(exports, "getDbPanelStyles", () => getDbPanelStyles);
__igsDefine(exports, "renderDbPanelInner", () => renderDbPanelInner);
});
__igsRegister("src/data/shujuku/status-hud-model.js", function(module, exports, require) {
const { parseTables } = require("src/shujuku-panel/panel-model.js");
const { resolveCharacterKey } = require("src/scene/scene-directives.js");
const STATUS_HUD_MAX_METRICS = 4;
const STATUS_HUD_SIZE_IDS = Object.freeze(['small', 'medium', 'large']);
const STATUS_HUD_SIZE_SCALE = Object.freeze({ small: 0.86, medium: 1, large: 1.16 });
// 地点栏独立档位系数：状态栏 size 已通过 --igs-hud-scale 影响地点栏，但整体偏小，
// 因此在地点栏上再叠加一档系数，最小档即为当前观感的 120%。
const STATUS_HUD_LOCATION_SCALE = Object.freeze({ small: 1.2, medium: 1.45, large: 1.7 });
const STATUS_HUD_OVERALL_SCALE = 0.8;
const STATUS_HUD_AVATAR_RADIUS_IDS = Object.freeze(['square', 'soft', 'small', 'medium', 'large', 'circle']);
const STATUS_HUD_AVATAR_RADIUS_PX = Object.freeze({ square: 0, soft: 6, small: 10, medium: 16, large: 24, circle: '50%' });
const STATUS_HUD_BACKGROUND_IDS = Object.freeze(['none', 'dialog']);
const STATUS_HUD_BAR_COLOR_IDS = Object.freeze(['color', 'grayscale']);
// NSFW 黑幕强度：中心/边缘透明度与背景亮度按档位同步抬升，
// 中心不再接近透明，消除「只有四角发黑」的渐晕观感。
const NSFW_VEIL_LEVEL_IDS = Object.freeze(['light', 'medium', 'strong']);
const NSFW_VEIL_LEVEL_STYLE = Object.freeze({
    light: { center: '.18', edge: '.55', brightness: '.72' },
    medium: { center: '.30', edge: '.72', brightness: '.62' },
    strong: { center: '.55', edge: '.88', brightness: '.5' },
});
const STATUS_HUD_DEFAULTS = Object.freeze({
    enabled: false,
    collapsed: false,
    size: 'medium',
    showEmotion: true,
    showLocation: false,
    showLocationDetails: false,
    showSpriteOnNsfw: true,
    dimSpriteOnNarration: true,
    nsfwVeilLevel: 'medium',
    avatarRadius: 'circle',
    background: 'none',
    barColor: 'color',
    tables: [],
});
function normalizeStatusHudSettings(raw) {
    const src = raw && typeof raw === 'object' && !Array.isArray(raw) ? raw : {};
    return {
        enabled: src.enabled === true,
        collapsed: src.collapsed === true,
        size: STATUS_HUD_SIZE_IDS.includes(src.size) ? src.size : STATUS_HUD_DEFAULTS.size,
        showEmotion: src.showEmotion === false ? false : true,
        showLocation: src.showLocation === true,
        showLocationDetails: src.showLocationDetails === true,
        showSpriteOnNsfw: src.showSpriteOnNsfw === false ? false : true,
        dimSpriteOnNarration: src.dimSpriteOnNarration === false ? false : true,
        nsfwVeilLevel: NSFW_VEIL_LEVEL_IDS.includes(src.nsfwVeilLevel) ? src.nsfwVeilLevel : STATUS_HUD_DEFAULTS.nsfwVeilLevel,
        avatarRadius: STATUS_HUD_AVATAR_RADIUS_IDS.includes(src.avatarRadius) ? src.avatarRadius : STATUS_HUD_DEFAULTS.avatarRadius,
        background: STATUS_HUD_BACKGROUND_IDS.includes(src.background) ? src.background : STATUS_HUD_DEFAULTS.background,
        barColor: STATUS_HUD_BAR_COLOR_IDS.includes(src.barColor) ? src.barColor : STATUS_HUD_DEFAULTS.barColor,
        tables: normalizeStatusHudTables(src.tables),
    };
}
function normalizeStatusHudTables(raw) {
    if (!Array.isArray(raw)) return [];
    const seen = new Set();
    const out = [];
    for (const item of raw) {
        if (!item || typeof item !== 'object') continue;
        const uid = String(item.uid || '').trim();
        const name = String(item.name || '').trim();
        if (!uid && !name) continue;
        const key = uid || 'name:' + name;
        if (seen.has(key)) continue;
        seen.add(key);
        out.push({ uid, name });
    }
    return out;
}
function resolveStatusHudAvatarRadius(avatarRadius) {
    return STATUS_HUD_AVATAR_RADIUS_PX[avatarRadius] != null
        ? STATUS_HUD_AVATAR_RADIUS_PX[avatarRadius]
        : STATUS_HUD_AVATAR_RADIUS_PX.circle;
}
function resolveStatusHudScale(size, viewportWidth, viewportHeight) {
    const width = Number(viewportWidth) > 0 ? Number(viewportWidth) : 0;
    const height = Number(viewportHeight) > 0 ? Number(viewportHeight) : 0;
    const widthFactor = width > 0 ? width / 900 : 1;
    const heightFactor = height > 0 ? height / 600 : 1;
    const base = clampNumber(Math.min(widthFactor, heightFactor), 0.78, 1.18, 1);
    const tier = STATUS_HUD_SIZE_SCALE[size] != null ? STATUS_HUD_SIZE_SCALE[size] : 1;
    return clampNumber(base * tier, 0.72, 1.28, 1) * STATUS_HUD_OVERALL_SCALE;
}
function resolveStatusHudLocationScale(size) {
    return STATUS_HUD_LOCATION_SCALE[size] != null ? STATUS_HUD_LOCATION_SCALE[size] : STATUS_HUD_LOCATION_SCALE.medium;
}
function listStatusHudTables(readResult) {
    if (!readResult || readResult.ok === false) {
        return { ok: false, reason: String((readResult && readResult.reason) || 'missing-api'), tables: [] };
    }
    const tables = parseTables(readResult.data);
    return { ok: true, reason: '', tables: tables.map((table) => ({ uid: table.uid, name: table.name })) };
}
function resolveStatusAvatar(statusAvatars, character) {
    if (!statusAvatars || typeof statusAvatars !== 'object' || !character) return '';
    const value = statusAvatars[character];
    if (typeof value !== 'string') return '';
    const url = value.trim();
    if (!url) return '';
    if (!/^(?:https?:\/\/|data:image\/)\S+$/i.test(url)) return '';
    return url;
}
function normalizeStatusAvatars(raw) {
    if (!raw || typeof raw !== 'object' || Array.isArray(raw)) return {};
    const out = {};
    for (const [key, value] of Object.entries(raw)) {
        const name = String(key || '').trim();
        if (!name || typeof value !== 'string') continue;
        const url = value.trim();
        if (!url || !/^(?:https?:\/\/|data:image\/)\S+$/i.test(url)) continue;
        out[name] = url;
    }
    return out;
}

function clampNumber(value, min, max, fallback) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) return fallback;
    return Math.max(min, Math.min(max, numeric));
}
const STATUS_HUD_TITLE_KEYS = Object.freeze(['姓名', '名称', '角色', '角色名', '名字', '物品名称', '任务名称', '地点名称', '标题']);
const TIME_LABEL_RE = /时间|日期|时长|时刻|钟点|分钟|小时|距今|经过|历时|时段|倒计时/;
const TIME_LIKE_RE = /\d{1,2}:\d{2}|\d{2,4}[-/.]\d{1,2}[-/.]\d{1,2}/;
const RATIO_RE = /^(\d+(?:\.\d+)?)\s*\/\s*(\d+(?:\.\d+)?)$/;
const PERCENT_RE = /^(-?\d+(?:\.\d+)?)\s*([%％])?$/;
const COLOR_RULES = Object.freeze([
    [/信任|信赖/, 'trust'],
    [/好感|喜欢|爱意|爱慕/, 'like'],
    [/了解|认知|熟悉/, 'know'],
]);
function buildStatusHudModel(input = {}) {
    const settings = normalizeStatusHudSettings(input.settings);
    const sceneAssets = input.sceneAssets && typeof input.sceneAssets === 'object' ? input.sceneAssets : {};
    const characters = sceneAssets.characters && typeof sceneAssets.characters === 'object' ? sceneAssets.characters : {};
    const rawCharacter = String(input.character || '').trim();
    // 未在场景素材注册的角色也用原始名作为显示名：状态栏照常显示头像占位、情绪与 HUD 条，
    // 只是取不到自定义头像。
    const character = resolveCharacterKey(characters, sceneAssets.characterAliases, rawCharacter) || rawCharacter;
    const emotion = character ? String(input.emotion || '').trim() : '';
    // 地点栏只在没有角色的旁白页显示；内心页属于角色页，与对白页同样不显示地点。
    const showSceneInfo = settings.showLocation && input.isNarration === true && !rawCharacter;
    const model = {
        enabled: settings.enabled,
        character,
        emotion: settings.showEmotion ? emotion : '',
        location: showSceneInfo ? String(input.location || '').trim() : '',
        time: showSceneInfo && settings.showLocationDetails ? String(input.time || '').trim() : '',
        weather: showSceneInfo && settings.showLocationDetails ? String(input.weather || '').trim() : '',
        showLocationDetails: settings.showLocationDetails,
        avatar: resolveStatusAvatar(sceneAssets.statusAvatars, character),
        avatarRadius: settings.avatarRadius,
        background: settings.background,
        barColor: settings.barColor,
        metrics: [],
        hiddenCount: 0,
        loadState: 'idle',
        loadReason: '',
    };
    if (!settings.enabled || !character) return model;
    if (!settings.tables.length) return model;
    const readResult = input.readResult;
    if (!readResult || readResult.ok === false) {
        model.loadState = 'error';
        model.loadReason = String((readResult && readResult.reason) || 'missing-api');
        return model;
    }
    const resolved = resolveSelectedStatusHudTables(settings.tables, readResult);
    const metrics = [];
    let missingTables = 0;
    for (const entry of resolved) {
        if (!entry.table) { missingTables += 1; continue; }
        metrics.push(...extractStatusHudMetrics(entry.table, character, sceneAssets.characterAliases, entry.pick));
    }
    model.metrics = metrics.slice(0, STATUS_HUD_MAX_METRICS);
    model.hiddenCount = Math.max(0, metrics.length - model.metrics.length);
    if (metrics.length) model.loadState = missingTables ? 'partial' : 'ready';
    else model.loadState = missingTables ? 'missing-table' : 'no-data';
    return model;
}
function resolveSelectedStatusHudTables(selected, readResult) {
    const wanted = normalizeStatusHudTables(selected);
    if (!wanted.length) return [];
    const available = readResult && readResult.ok !== false ? parseTables(readResult.data) : [];
    return wanted.map((pick) => {
        const table = available.find((item) => (pick.uid && item.uid === pick.uid))
            || available.find((item) => pick.name && item.name === pick.name)
            || null;
        return { pick, table };
    });
}
function extractStatusHudMetrics(table, character, characterAliases, pick) {
    if (!table || !Array.isArray(table.columns) || !Array.isArray(table.rows)) return [];
    const row = findStatusHudRow(table, character, characterAliases);
    if (!row) return [];
    const metrics = [];
    for (let index = 0; index < row.values.length; index += 1) {
        if (index === row.headerIndex) continue;
        const parsed = parseMetricCell(row.values[index], table.columns[index]);
        if (!parsed) continue;
        for (const metric of parsed) {
            metrics.push({
                label: metric.label,
                percent: metric.percent,
                display: metric.display,
                colorKey: resolveMetricColorKey(metric.label, index),
                sourceUid: String((pick && pick.uid) || table.uid || ''),
                sourceName: String((pick && pick.name) || table.name || ''),
            });
        }
    }
    return metrics;
}
function findStatusHudRow(table, character, characterAliases) {
    const columns = Array.isArray(table.columns) ? table.columns : [];
    const names = buildCharacterNameSet(character, characterAliases);
    if (!names.size || !Array.isArray(table.rows)) return null;
    for (let index = 0; index < columns.length; index += 1) {
        if (!isTitleColumn(columns[index], index)) continue;
        for (const values of table.rows) {
            if (!Array.isArray(values)) continue;
            const cell = String(values[index] == null ? '' : values[index]).trim();
            if (cell && names.has(cell)) return { values, headerIndex: index };
        }
    }
    for (const values of table.rows) {
        if (!Array.isArray(values)) continue;
        const titles = collectRowTitles(columns, values);
        for (const title of titles) {
            if (names.has(title)) return { values, headerIndex: resolveTitleColumnIndex(columns, values, title) };
        }
    }
    return null;
}
function collectRowTitles(columns, values) {
    const out = [];
    const idIndex = columns.findIndex((column) => isRowIdColumn(column));
    for (const key of STATUS_HUD_TITLE_KEYS) {
        const index = columns.indexOf(key);
       if (index < 0 || index === idIndex) continue;
        const cell = String(values[index] == null ? '' : values[index]).trim();
        if (cell && !parseMetricCell(values[index], columns[index]) && !isImageCell(values[index])) {
            out.push(cell);
        }
    }
    if (idIndex >= 0) {
        const next = idIndex + 1;
        const cell = next < values.length ? String(values[next] == null ? '' : values[next]).trim() : '';
        if (cell && !parseMetricCell(values[next], columns[next]) && !isImageCell(values[next])
            && !/^\\s*-?\\d+(?:\\.\\d+)?\\s*$/.test(cell)) {
            out.push(cell);
        }
    }
    for (let index = 0; index < values.length; index += 1) {
        if (index === idIndex) continue;
        const raw = values[index];
        if (typeof raw !== 'string') continue;
        const cell = raw.trim();
        if (!cell || cell.length > 40) continue;
        if (parseMetricCell(raw, columns[index]) || isImageCell(raw)) continue;
        if (/^\\s*-?\\d+(?:\\.\\d+)?\\s*$/.test(cell)) continue;
        out.push(cell);
    }
    return out;
}

function resolveTitleColumnIndex(columns, values, title) {
    for (let index = 0; index < values.length; index += 1) {
        const cell = String(values[index] == null ? '' : values[index]).trim();
        if (cell === title) return index;
    }
    return 0;
}
function isImageCell(value) {
    return typeof value === 'string' && /^(?:https?:\/\/|data:image\/)\S+$/i.test(value.trim());
}
function parseMetricCell(raw, header) {
    if (typeof raw !== 'string') return null;
    const text = raw.trim();
    if (!text || TIME_LIKE_RE.test(text)) return null;
    const segments = text.split(/[;；]/);
    const out = [];
    for (let index = 0; index < segments.length; index += 1) {
        const segment = segments[index].trim();
        if (!segment) continue;
        const parsed = parseMetricSegment(segment, header, segments.length, index);
        if (parsed) out.push(parsed);
    }
    return out.length ? out : null;
}

function parseMetricSegment(segment, header, segmentCount, index) {
    let label = '';
    let rest = segment;
    let hasLabel = false;
    const colon = segment.search(/[:：]/);
    if (colon === 0) return null;
    if (colon > 0) {
        label = segment.slice(0, colon).trim();
        rest = segment.slice(colon + 1).trim();
        hasLabel = true;
    } else {
        const inline = segment.match(/^([^\d\s][^\s]{0,11})\s+(-?\d+(?:\.\d+)?\s*(?:[%％]|\/\s*\d+(?:\.\d+)?))$/);
        if (inline && !/[，。！？、；]/.test(inline[1])) {
            label = inline[1].trim();
            rest = inline[2].trim();
        } else {
            if (!header) return null;
            label = String(header).trim() + (segmentCount > 1 ? String(index + 1) : '');
        }
    }
    if (!label || TIME_LABEL_RE.test(label)) return null;
    const ratio = rest.match(RATIO_RE);
    if (ratio) {
        const current = parseFloat(ratio[1]);
        const cap = parseFloat(ratio[2]);
        if (!hasLabel && cap < 10) return null;
        const percent = toPercent(current, cap);
        if (percent == null) return null;
        return { label, percent, display: formatNumber(current) + '/' + formatNumber(cap) };
    }
    const single = rest.match(PERCENT_RE);
    if (!single) return null;
    const hasPercentSign = Boolean(single[2]);
    if (!hasLabel && !hasPercentSign) return null;
    const value = parseFloat(single[1]);
    if (!Number.isFinite(value)) return null;
    if (value < 0 || value > 100) return null;
    const percent = value;
    return { label, percent, display: formatNumber(percent) + '%' };
}

function buildCharacterNameSet(character, characterAliases) {
    const names = new Set();
    const main = String(character || '').trim();
    if (!main) return names;
    names.add(main);
    const aliases = characterAliases && typeof characterAliases === 'object' ? characterAliases : {};
    const values = Array.isArray(aliases[main]) ? aliases[main] : [];
    for (const value of values) {
        const alias = String(value || '').trim();
        if (alias) names.add(alias);
    }
    return names;
}

function isTitleColumn(column, index) {
    if (index === 0) return false;
    const normalized = String(column || '').trim();
    return Boolean(normalized) && STATUS_HUD_TITLE_KEYS.includes(normalized);
}

function isRowIdColumn(name) {
    const normalized = String(name || '').trim().toLowerCase();
    return normalized === 'row_id' || normalized === 'rowid' || normalized === 'id' || normalized === '序号';
}

function resolveMetricColorKey(label, columnIndex) {
    const text = String(label || '');
    for (const [pattern, key] of COLOR_RULES) {
        if (pattern.test(text)) return key;
    }
    return 'slot-' + ((Math.max(0, columnIndex) % 6) + 1);
}

function toPercent(current, cap) {
    if (!Number.isFinite(current) || !Number.isFinite(cap) || cap <= 0) return null;
    return clampNumber((current / cap) * 100, 0, 100, null);
}

function formatNumber(value) {
    if (!Number.isFinite(value)) return '';
    return Math.abs(value - Math.round(value)) < 0.05
        ? String(Math.round(value))
        : String(Math.round(value * 10) / 10);
}

__igsDefine(exports, "normalizeStatusHudSettings", () => normalizeStatusHudSettings);
__igsDefine(exports, "normalizeStatusHudTables", () => normalizeStatusHudTables);
__igsDefine(exports, "resolveStatusHudAvatarRadius", () => resolveStatusHudAvatarRadius);
__igsDefine(exports, "resolveStatusHudScale", () => resolveStatusHudScale);
__igsDefine(exports, "resolveStatusHudLocationScale", () => resolveStatusHudLocationScale);
__igsDefine(exports, "listStatusHudTables", () => listStatusHudTables);
__igsDefine(exports, "resolveStatusAvatar", () => resolveStatusAvatar);
__igsDefine(exports, "normalizeStatusAvatars", () => normalizeStatusAvatars);
__igsDefine(exports, "buildStatusHudModel", () => buildStatusHudModel);
__igsDefine(exports, "resolveSelectedStatusHudTables", () => resolveSelectedStatusHudTables);
__igsDefine(exports, "extractStatusHudMetrics", () => extractStatusHudMetrics);
__igsDefine(exports, "findStatusHudRow", () => findStatusHudRow);
__igsDefine(exports, "collectRowTitles", () => collectRowTitles);
__igsDefine(exports, "isImageCell", () => isImageCell);
__igsDefine(exports, "parseMetricCell", () => parseMetricCell);
__igsDefine(exports, "STATUS_HUD_MAX_METRICS", () => STATUS_HUD_MAX_METRICS);
__igsDefine(exports, "STATUS_HUD_SIZE_IDS", () => STATUS_HUD_SIZE_IDS);
__igsDefine(exports, "STATUS_HUD_SIZE_SCALE", () => STATUS_HUD_SIZE_SCALE);
__igsDefine(exports, "STATUS_HUD_LOCATION_SCALE", () => STATUS_HUD_LOCATION_SCALE);
__igsDefine(exports, "STATUS_HUD_AVATAR_RADIUS_IDS", () => STATUS_HUD_AVATAR_RADIUS_IDS);
__igsDefine(exports, "STATUS_HUD_AVATAR_RADIUS_PX", () => STATUS_HUD_AVATAR_RADIUS_PX);
__igsDefine(exports, "STATUS_HUD_BACKGROUND_IDS", () => STATUS_HUD_BACKGROUND_IDS);
__igsDefine(exports, "STATUS_HUD_BAR_COLOR_IDS", () => STATUS_HUD_BAR_COLOR_IDS);
__igsDefine(exports, "NSFW_VEIL_LEVEL_IDS", () => NSFW_VEIL_LEVEL_IDS);
__igsDefine(exports, "NSFW_VEIL_LEVEL_STYLE", () => NSFW_VEIL_LEVEL_STYLE);
__igsDefine(exports, "STATUS_HUD_DEFAULTS", () => STATUS_HUD_DEFAULTS);
__igsDefine(exports, "STATUS_HUD_TITLE_KEYS", () => STATUS_HUD_TITLE_KEYS);
});
__igsRegister("src/choices/option-table.js", function(module, exports, require) {
const { parseTables } = require("src/shujuku-panel/panel-model.js");
// 约定的选项表名：命中任一即作为选项来源。「检定建议表」是选项表的别名，与选项表互斥出现。
const OPTION_TABLE_NAMES = Object.freeze(['选项', '选项表', '行动选项', '检定建议表']);

// 第一列通常是 row_id（只读主键），选项文本取首个非 row_id 列。
function isRowIdColumn(name) {
    const n = String(name || '').trim().toLowerCase();
    return n === 'row_id' || n === 'rowid' || n === 'id' || n === '序号';
}

// 「检定建议表」是多业务字段表（展示文本/对抗/角色/属性…），只取「展示文本」列；
// 其余选项表是宽表（每列一个并列选项），取全部非 row_id 列。
function findDisplayTextColumn(columns) {
    return columns.findIndex((column) => String(column || '').replace(/\s+/g, '') === '展示文本');
}
function findOptionTable(tables) {
    const list = Array.isArray(tables) ? tables : [];
    for (const wanted of OPTION_TABLE_NAMES) {
        const hit = list.find((t) => String(t && t.name || '').trim() === wanted);
        if (hit) return hit;
    }
    return null;
}

function findDiceCommandColumn(columns) {
    return columns.findIndex((column) => String(column || '').replace(/\s+/g, '') === '骰子命令');
}

// 从一张表里提取选项：返回 {display, send}[]。
// 若表含「展示文本」+「骰子命令」两列（检定建议表），display=展示文本，
// send=展示文本+空格+骰子命令（骰子系统前端凭 DSL 短命令触发数值判定）。
// 只有「展示文本」时，display===send===展示文本；普通宽表取全部非 row_id 列，
// display===send===列值，与旧行为完全兼容。
function extractOptionTexts(table) {
    if (!table || !Array.isArray(table.columns) || !Array.isArray(table.rows)) return [];
    const displayCol = findDisplayTextColumn(table.columns);
    const diceCol = findDiceCommandColumn(table.columns);
    let textCols;
    if (displayCol >= 0) {
        textCols = [displayCol];
    } else {
        textCols = table.columns
            .map((column, index) => ({ column, index }))
            .filter((item) => !isRowIdColumn(item.column))
            .map((item) => item.index);
        if (!textCols.length) textCols = table.columns.length > 1 ? [1] : [0];
    }
    const seen = new Set();
    const out = [];
    for (const row of table.rows) {
        for (const textCol of textCols) {
            const display = String((Array.isArray(row) ? row[textCol] : '') ?? '').trim();
            if (!display) continue;
            const key = display.toLowerCase();
            if (seen.has(key)) continue;
            seen.add(key);
            if (displayCol >= 0 && diceCol >= 0) {
                const dice = String((Array.isArray(row) ? row[diceCol] : '') ?? '').trim();
                out.push({ display, send: dice ? `${display} ${dice}` : display });
            } else {
                out.push({ display, send: display });
            }
        }
    }
    return out;
}

// 读取选项：从 shujuku client 读所有表 → 找同名表 → 提取文本。
// 不可用（无插件/无表/读失败）时返回空数组，调用方据此静默不弹。
function readOptionItems(client) {
    if (!client || typeof client.readTables !== 'function') return [];
    let result;
    try {
        result = client.readTables();
    } catch (error) {
        return [];
    }
    if (!result || result.ok === false) return [];
    const tables = parseTables(result.data);
    const table = findOptionTable(tables);
    if (!table) return [];
    return extractOptionTexts(table);
}

__igsDefine(exports, "findOptionTable", () => findOptionTable);
__igsDefine(exports, "extractOptionTexts", () => extractOptionTexts);
__igsDefine(exports, "readOptionItems", () => readOptionItems);
__igsDefine(exports, "OPTION_TABLE_NAMES", () => OPTION_TABLE_NAMES);
});
__igsRegister("src/visual/igs-ui/settings-actions.js", function(module, exports, require) {
const { DEFAULT_VIRTUAL_REGEX } = require("src/scene/message-source.js");
const { cloneData } = require("src/visual/igs-ui/reader-value-utils.js");
const { DEFAULT_SCENE_PROMPT_RULE, TOOLBAR_ACTIONS } = require("src/visual/igs-ui/reader-host-constants.js");
const { DEFAULT_MOOD_GROUPS, normalizeMoodGroups } = require("src/scene/mood-groups.js");
const { loadScenePresets, saveScenePresets } = require("src/scene/scene-preset-store.js");
const { normalizeStatusHudSettings } = require("src/data/shujuku/status-hud-model.js");
const { normalizeStatusAvatars } = require("src/data/shujuku/status-hud-model.js");
const STATUS_AVATAR_MAX_BYTES = 512 * 1024;
const STATUS_AVATAR_MIME = /^image\/(?:png|jpeg|jpg|webp|gif|bmp|svg\+xml)$/i;

function decodeSeg(value) {
    try { return decodeURIComponent(String(value == null ? '' : value)); }
    catch (error) { return String(value == null ? '' : value); }
}
async function handleSettingsAction(action, ctx) {
    const {
        state,
        options,
        closeSettings,
        persistSettingsDraft,
        rerenderSettings,
        buildRegexPreview,
    } = ctx;

    if (!state.activeSettings) return { ok: false, reason: 'settings-not-open' };
    const normalizedAction = String(action || '').trim();
    const settingsState = state.activeSettings;

    if (normalizedAction === 'toggle-settings-theme') {
        const bridge = settingsState.draft.bridge = settingsState.draft.bridge || {};
        bridge.settingsTheme = bridge.settingsTheme === 'day' ? 'night' : 'day';
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction === 'close') {
        return closeSettings();
    }

    if (normalizedAction.startsWith('status-hud-toggle-table:')) {
        const rest = normalizedAction.slice('status-hud-toggle-table:'.length);
        const colon = rest.indexOf(':');
        if (colon < 0) return rerenderSettings();
        const uid = decodeSeg(rest.slice(0, colon));
        const name = decodeSeg(rest.slice(colon + 1));
        const readerDraft = settingsState.draft.readerSettings = settingsState.draft.readerSettings || {};
        const current = normalizeStatusHudSettings(readerDraft.statusHud);
        const key = uid || `name:${name}`;
        const exists = current.tables.some((item) => (uid && item.uid === uid) || (!uid && item.name === name));
        current.tables = exists
            ? current.tables.filter((item) => !((uid && item.uid === uid) || (!uid && item.name === name)))
            : current.tables.concat([{ uid, name }]);
        readerDraft.statusHud = current;
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('status-avatar-set-url:')) {
        const rest = normalizedAction.slice('status-avatar-set-url:'.length);
        const colon = rest.indexOf(':');
        if (colon > 0) {
            const charName = decodeSeg(rest.slice(0, colon));
            const url = decodeSeg(rest.slice(colon + 1)).trim();
            const sceneAssets = settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
            const avatars = normalizeStatusAvatars(sceneAssets.statusAvatars);
            if (url) {
                const normalized = normalizeStatusAvatars({ [charName]: url });
                if (!normalized[charName]) return rerenderSettings();
                avatars[charName] = normalized[charName];
            } else {
                delete avatars[charName];
            }
            sceneAssets.statusAvatars = avatars;
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('status-avatar-pick:')) {
        const charName = decodeSeg(normalizedAction.slice('status-avatar-pick:'.length));
        const globalObj = options.global || globalThis;
        const doc = globalObj.document;
        if (!doc || !charName) return rerenderSettings();
        const picked = await pickStatusAvatarFile(doc);
        if (!picked) return rerenderSettings();
        if (picked.ok === false) {
            if (globalObj.alert) globalObj.alert(picked.reason === 'too-large' ? '图片过大，请选择更小的图片。' : '仅支持图片文件。');
            return rerenderSettings();
        }
        const sceneAssets = settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        const avatars = normalizeStatusAvatars(sceneAssets.statusAvatars);
        avatars[charName] = picked.dataUrl;
        sceneAssets.statusAvatars = avatars;
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('status-avatar-clear:')) {
        const charName = decodeSeg(normalizedAction.slice('status-avatar-clear:'.length));
        const sceneAssets = settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        const avatars = normalizeStatusAvatars(sceneAssets.statusAvatars);
        delete avatars[charName];
        sceneAssets.statusAvatars = avatars;
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction === 'reset-virtual-regex') {
        settingsState.draft.bridge.virtualRegex = cloneData(DEFAULT_VIRTUAL_REGEX);
        settingsState.asyncState.virtualRegexPreview = '已恢复默认正文替换，已自动保存。';
        persistSettingsDraft();
        return rerenderSettings();
    }

    if (normalizedAction === 'test-virtual-regex') {
        settingsState.asyncState.virtualRegexPreview = buildRegexPreview(settingsState.draft.bridge);
        return rerenderSettings();
    }

    if (normalizedAction === 'fetch-image-models') {
        if (typeof options.fetchImageModels !== 'function') {
            settingsState.asyncState.imageModelsMessage = '当前未接入内置图像模型拉取能力。';
            return rerenderSettings();
        }
        const result = await options.fetchImageModels({
            settings: cloneData(settingsState.draft),
            message: state.activeReader && state.activeReader.payload && state.activeReader.payload.message || null,
            mode: settingsState.readerMode,
        });
        if (!result || result.ok === false) {
            settingsState.asyncState.imageModelsMessage = String(result && result.reason || '图像模型拉取失败');
            return rerenderSettings();
        }
        settingsState.draft.bridge.imageApi.availableModels = Array.isArray(result.models)
            ? result.models.filter(Boolean)
            : [];
        settingsState.draft.bridge.imageApi.modelsFetchedAt = String(result.modelsFetchedAt || new Date().toISOString());
        settingsState.asyncState.imageModelsMessage = String(result.message || `已拉取 ${settingsState.draft.bridge.imageApi.availableModels.length} 个模型。`);
        persistSettingsDraft();
        return rerenderSettings();
    }

    if (normalizedAction === 'test-image') {
        if (typeof options.testImageApi !== 'function') {
            settingsState.asyncState.imageResult = '当前未接入图像测试能力。';
            return rerenderSettings();
        }
        const result = await options.testImageApi({
            settings: cloneData(settingsState.draft),
            message: state.activeReader && state.activeReader.payload && state.activeReader.payload.message || null,
            mode: settingsState.readerMode,
        });
        settingsState.asyncState.imageResult = String(
            result && (result.message || result.reason)
            || (settingsState.draft.bridge.imageApi.mode === 'nai'
                ? '图像 API 生成测试失败。'
                : '插图扩展检测失败。'),
        );
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('toggle-toolbar-pin:')) {
        const id = normalizedAction.slice('toggle-toolbar-pin:'.length);
        const allowed = TOOLBAR_ACTIONS.some(([actionId]) => actionId === id);
        if (!allowed) return { ok: false, reason: 'unknown-toolbar-pin', id };
        const currentPins = Array.isArray(settingsState.draft.readerSettings.pinnedBtns)
            ? settingsState.draft.readerSettings.pinnedBtns.slice()
            : [];
        const currentHidden = Array.isArray(settingsState.draft.readerSettings.hiddenBtns)
            ? settingsState.draft.readerSettings.hiddenBtns.slice()
            : [];
        const index = currentPins.indexOf(id);
        if (index >= 0) {
            currentPins.splice(index, 1);
        } else {
            if (!currentHidden.includes(id)) currentPins.push(id);
        }
        settingsState.draft.readerSettings.pinnedBtns = currentPins;
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('toolbar-toggle-visible:')) {
        const id = normalizedAction.slice('toolbar-toggle-visible:'.length);
        const allowed = TOOLBAR_ACTIONS.some(([actionId]) => actionId === id);
        if (!allowed) return { ok: false, reason: 'unknown-toolbar-btn', id };
        const currentHidden = Array.isArray(settingsState.draft.readerSettings.hiddenBtns)
            ? settingsState.draft.readerSettings.hiddenBtns.slice()
            : [];
        const idx = currentHidden.indexOf(id);
        if (idx >= 0) {
            currentHidden.splice(idx, 1);
        } else {
            currentHidden.push(id);
            const currentPins = Array.isArray(settingsState.draft.readerSettings.pinnedBtns)
                ? settingsState.draft.readerSettings.pinnedBtns.slice()
                : [];
            const pinIdx = currentPins.indexOf(id);
            if (pinIdx >= 0) {
                currentPins.splice(pinIdx, 1);
                settingsState.draft.readerSettings.pinnedBtns = currentPins;
            }
        }
        settingsState.draft.readerSettings.hiddenBtns = currentHidden;
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('toolbar-move-up:')) {
        const id = normalizedAction.slice('toolbar-move-up:'.length);
        const order = Array.isArray(settingsState.draft.readerSettings.btnOrder)
            ? settingsState.draft.readerSettings.btnOrder.slice()
            : TOOLBAR_ACTIONS.map(([actionId]) => actionId);
        const currentIndex = order.indexOf(id);
        if (currentIndex <= 0) return { ok: true, reason: 'already-first' };
        [order[currentIndex - 1], order[currentIndex]] = [order[currentIndex], order[currentIndex - 1]];
        settingsState.draft.readerSettings.btnOrder = order;
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction === 'reset-prompt-rule') {
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        settingsState.draft.bridge.sceneAssets.promptRule = DEFAULT_SCENE_PROMPT_RULE;
        settingsState.asyncState.promptRuleDraft = DEFAULT_SCENE_PROMPT_RULE;
        settingsState.asyncState.promptRuleStatus = '已恢复默认提示词并保存。';
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction === 'save-prompt-rule') {
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        const nextRule = typeof settingsState.asyncState.promptRuleDraft === 'string'
            ? settingsState.asyncState.promptRuleDraft
            : String(settingsState.draft.bridge.sceneAssets.promptRule || '');
        settingsState.draft.bridge.sceneAssets.promptRule = nextRule;
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) {
            settingsState.asyncState.promptRuleStatus = '保存失败，请重试。';
            return rerenderSettings();
        }
        settingsState.asyncState.promptRuleDraft = nextRule;
        settingsState.asyncState.promptRuleStatus = '提示词已保存并更新注入规则。';
        return rerenderSettings();
    }

    if (normalizedAction === 'scene-add-bg') {
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        settingsState.draft.bridge.sceneAssets.scenes = settingsState.draft.bridge.sceneAssets.scenes || {};
        const existingKeys = Object.keys(settingsState.draft.bridge.sceneAssets.scenes);
        const newName = '场景' + (existingKeys.length + 1);
        settingsState.draft.bridge.sceneAssets.scenes[newName] = { url: '', times: {} };
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-remove-bg:')) {
        const name = decodeSeg(normalizedAction.slice('scene-remove-bg:'.length));
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        settingsState.draft.bridge.sceneAssets.scenes = settingsState.draft.bridge.sceneAssets.scenes || {};
        delete settingsState.draft.bridge.sceneAssets.scenes[name];
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-rename-bg:')) {
        const oldName = decodeSeg(normalizedAction.slice('scene-rename-bg:'.length));
        const globalObj = options.global || globalThis;
        const newName = (globalObj.prompt && globalObj.prompt(`重命名场景「${oldName}」为：`, oldName) || '').trim();
        if (newName && newName !== oldName) {
            settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
            const scenes = settingsState.draft.bridge.sceneAssets.scenes || {};
            if (Object.prototype.hasOwnProperty.call(scenes, newName)) {
                if (globalObj.alert) globalObj.alert(`场景「${newName}」已存在（同名），已阻止`);
                return rerenderSettings();
            }
            settingsState.draft.bridge.sceneAssets.scenes = reorderKey(scenes, oldName, newName);
            const sl = settingsState.asyncState.expandedSceneSlots;
            renameSetPrefix(sl, `bg\x00${oldName}`, `bg\x00${newName}`);
            renameSetPrefix(sl, `time\x00${oldName}\x00`, `time\x00${newName}\x00`);
            renameSetPrefix(sl, `weather\x00${oldName}\x00`, `weather\x00${newName}\x00`);
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-set-bg-url:')) {
        const rest = normalizedAction.slice('scene-set-bg-url:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const name = decodeSeg(rest.slice(0, colonIdx));
            const url = rest.slice(colonIdx + 1);
            settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
            settingsState.draft.bridge.sceneAssets.scenes = settingsState.draft.bridge.sceneAssets.scenes || {};
            const scene = settingsState.draft.bridge.sceneAssets.scenes[name];
            if (scene && typeof scene === 'object') {
                scene.url = url;
            } else {
                settingsState.draft.bridge.sceneAssets.scenes[name] = { url, times: {} };
            }
            persistSettingsDraft();
        }
        return { ok: true };
    }

    if (normalizedAction.startsWith('scene-add-time:')) {
        const sceneName = decodeSeg(normalizedAction.slice('scene-add-time:'.length));
        const globalObj = options.global || globalThis;
        const scenes = (settingsState.draft.bridge.sceneAssets || {}).scenes || {};
        const scene = scenes[sceneName];
        if (scene && typeof scene === 'object') {
            const newTime = (globalObj.prompt && globalObj.prompt('时间名称（建议与时间组名一致）：', '') || '').trim();
            if (!newTime) return rerenderSettings();
            scene.times = scene.times || {};
            if (Object.prototype.hasOwnProperty.call(scene.times, newTime)) {
                if (globalObj.alert) globalObj.alert(`「${sceneName}」已有时间「${newTime}」（同名）`);
                return rerenderSettings();
            }
            scene.times[newTime] = { url: '', weathers: {} };
            const timeGroups = ensureTimeGroups(settingsState);
            if (!timeGroups.some((g) => g.label === newTime)) timeGroups.unshift({ label: newTime, words: [newTime] });
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-remove-time:')) {
        const rest = normalizedAction.slice('scene-remove-time:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const sceneName = decodeSeg(rest.slice(0, colonIdx));
            const timeName = decodeSeg(rest.slice(colonIdx + 1));
            const scenes = settingsState.draft.bridge.sceneAssets && settingsState.draft.bridge.sceneAssets.scenes || {};
            const scene = scenes[sceneName];
            if (scene && scene.times) delete scene.times[timeName];
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-rename-time:')) {
        const rest = normalizedAction.slice('scene-rename-time:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const sceneName = decodeSeg(rest.slice(0, colonIdx));
            const oldTime = decodeSeg(rest.slice(colonIdx + 1));
            const globalObj = options.global || globalThis;
            const newTime = (globalObj.prompt && globalObj.prompt(`重命名时间「${oldTime}」为：`, oldTime) || '').trim();
            if (newTime && newTime !== oldTime) {
                const scenes = settingsState.draft.bridge.sceneAssets && settingsState.draft.bridge.sceneAssets.scenes || {};
                const scene = scenes[sceneName];
                if (scene && scene.times) {
                    if (Object.prototype.hasOwnProperty.call(scene.times, newTime)) {
                        if (globalObj.alert) globalObj.alert(`时间「${newTime}」已存在（同名），已阻止`);
                        return rerenderSettings();
                    }
                    scene.times = reorderKey(scene.times, oldTime, newTime);
                    // global sync: rename same time slot in all other scenes
                    for (const [otherSn, otherSv] of Object.entries(scenes)) {
                        if (otherSn === sceneName || !otherSv || typeof otherSv !== 'object') continue;
                        if (Object.prototype.hasOwnProperty.call(otherSv.times || {}, oldTime)
                            && !Object.prototype.hasOwnProperty.call(otherSv.times, newTime)) {
                            otherSv.times = reorderKey(otherSv.times, oldTime, newTime);
                        }
                    }
                    // sync timeGroups label
                    const timeGroups = ensureTimeGroups(settingsState);
                    const tg = timeGroups.find((g) => g.label === oldTime);
                    if (tg) {
                        tg.label = newTime;
                        const wi = Array.isArray(tg.words) ? tg.words.indexOf(oldTime) : -1;
                        if (wi >= 0) tg.words[wi] = newTime;
                    }
                    // update Set keys for all scenes
                    const sl = settingsState.asyncState.expandedSceneSlots;
                    for (const sn of Object.keys(scenes)) {
                        renameSetPrefix(sl, `time\x00${sn}\x00${oldTime}`, `time\x00${sn}\x00${newTime}`);
                        renameSetPrefix(sl, `weather\x00${sn}\x00${oldTime}\x00`, `weather\x00${sn}\x00${newTime}\x00`);
                    }
                    const persisted = persistSettingsDraft();
                    if (persisted.ok === false) return persisted;
                }
            }
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-set-time-url:')) {
        const rest = normalizedAction.slice('scene-set-time-url:'.length);
        const first = rest.indexOf(':');
        if (first > 0) {
            const sceneName = decodeSeg(rest.slice(0, first));
            const after = rest.slice(first + 1);
            const second = after.indexOf(':');
            if (second > 0) {
                const timeName = decodeSeg(after.slice(0, second));
                const url = after.slice(second + 1);
                const scenes = settingsState.draft.bridge.sceneAssets && settingsState.draft.bridge.sceneAssets.scenes || {};
                const scene = scenes[sceneName];
                if (scene && scene.times && scene.times[timeName] != null) {
                    const t = scene.times[timeName];
                    if (typeof t === 'object') t.url = url;
                    else scene.times[timeName] = { url, weathers: {} };
                }
                persistSettingsDraft();
            }
        }
        return { ok: true };
    }

    if (normalizedAction.startsWith('scene-add-weather:')) {
        const rest = normalizedAction.slice('scene-add-weather:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const sceneName = decodeSeg(rest.slice(0, colonIdx));
            const timeName = decodeSeg(rest.slice(colonIdx + 1));
            const globalObj = options.global || globalThis;
            const scenes = (settingsState.draft.bridge.sceneAssets || {}).scenes || {};
            const scene = scenes[sceneName];
            if (scene && scene.times && typeof scene.times[timeName] === 'object') {
                const timeEntry = scene.times[timeName];
                const newWeather = (globalObj.prompt && globalObj.prompt('天气名称（建议与天气组名一致）：', '') || '').trim();
                if (!newWeather) return rerenderSettings();
                timeEntry.weathers = timeEntry.weathers || {};
                if (Object.prototype.hasOwnProperty.call(timeEntry.weathers, newWeather)) {
                    if (globalObj.alert) globalObj.alert(`「${timeName}」已有天气「${newWeather}」（同名）`);
                    return rerenderSettings();
                }
                timeEntry.weathers[newWeather] = { url: '' };
                const weatherGroups = ensureWeatherGroups(settingsState);
                if (!weatherGroups.some((g) => g.label === newWeather)) weatherGroups.unshift({ label: newWeather, words: [newWeather] });
                const persisted = persistSettingsDraft();
                if (persisted.ok === false) return persisted;
            }
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-remove-weather:')) {
        const rest = normalizedAction.slice('scene-remove-weather:'.length);
        const first = rest.indexOf(':');
        if (first > 0) {
            const sceneName = decodeSeg(rest.slice(0, first));
            const after = rest.slice(first + 1);
            const second = after.indexOf(':');
            if (second > 0) {
                const timeName = decodeSeg(after.slice(0, second));
                const weatherName = decodeSeg(after.slice(second + 1));
                const scenes = settingsState.draft.bridge.sceneAssets && settingsState.draft.bridge.sceneAssets.scenes || {};
                const scene = scenes[sceneName];
                if (scene && scene.times && scene.times[timeName]) {
                    const t = scene.times[timeName];
                    if (t && t.weathers) delete t.weathers[weatherName];
                }
                const persisted = persistSettingsDraft();
                if (persisted.ok === false) return persisted;
            }
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-rename-weather:')) {
        const rest = normalizedAction.slice('scene-rename-weather:'.length);
        const first = rest.indexOf(':');
        if (first > 0) {
            const sceneName = decodeSeg(rest.slice(0, first));
            const after = rest.slice(first + 1);
            const second = after.indexOf(':');
            if (second > 0) {
                const timeName = decodeSeg(after.slice(0, second));
                const oldWeather = decodeSeg(after.slice(second + 1));
                const globalObj = options.global || globalThis;
                const newWeather = (globalObj.prompt && globalObj.prompt(`重命名天气「${oldWeather}」为：`, oldWeather) || '').trim();
                if (newWeather && newWeather !== oldWeather) {
                    const scenes = settingsState.draft.bridge.sceneAssets && settingsState.draft.bridge.sceneAssets.scenes || {};
                    const scene = scenes[sceneName];
                    if (scene && scene.times && scene.times[timeName]) {
                        const t = scene.times[timeName];
                        if (t && t.weathers) {
                            if (Object.prototype.hasOwnProperty.call(t.weathers, newWeather)) {
                                if (globalObj.alert) globalObj.alert(`天气「${newWeather}」已存在（同名），已阻止`);
                                return rerenderSettings();
                            }
                            // global sync: rename same weather slot across all scenes/times
                            for (const [gsn, gsv] of Object.entries(scenes)) {
                                if (!gsv || typeof gsv !== 'object') continue;
                                for (const [gtn, gtv] of Object.entries(gsv.times || {})) {
                                    if (!gtv || typeof gtv !== 'object') continue;
                                    const gw = gtv.weathers || {};
                                    if (Object.prototype.hasOwnProperty.call(gw, oldWeather)
                                        && !Object.prototype.hasOwnProperty.call(gw, newWeather)) {
                                        const ow = gw[oldWeather];
                                        gw[oldWeather] = typeof ow === 'string' ? { url: ow } : (ow || { url: '' });
                                        gtv.weathers = reorderKey(gw, oldWeather, newWeather);
                                    }
                                }
                            }
                            // sync weatherGroups label
                            const weatherGroups = ensureWeatherGroups(settingsState);
                            const wg = weatherGroups.find((g) => g.label === oldWeather);
                            if (wg) {
                                wg.label = newWeather;
                                const wi = Array.isArray(wg.words) ? wg.words.indexOf(oldWeather) : -1;
                                if (wi >= 0) wg.words[wi] = newWeather;
                            }
                            // update Set keys for all scenes/times
                            const sl = settingsState.asyncState.expandedSceneSlots;
                            for (const [gsn2, gsv2] of Object.entries(scenes)) {
                                if (!gsv2 || typeof gsv2 !== 'object') continue;
                                for (const gtn2 of Object.keys(gsv2.times || {})) {
                                    renameSetPrefix(sl, `weather\x00${gsn2}\x00${gtn2}\x00${oldWeather}`, `weather\x00${gsn2}\x00${gtn2}\x00${newWeather}`);
                                }
                            }
                            const persisted = persistSettingsDraft();
                            if (persisted.ok === false) return persisted;
                        }
                    }
                }
            }
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-set-weather-url:')) {
        const rest = normalizedAction.slice('scene-set-weather-url:'.length);
        const first = rest.indexOf(':');
        if (first > 0) {
            const sceneName = decodeSeg(rest.slice(0, first));
            const after = rest.slice(first + 1);
            const second = after.indexOf(':');
            if (second > 0) {
                const timeName = decodeSeg(after.slice(0, second));
                const after2 = after.slice(second + 1);
                const third = after2.indexOf(':');
                if (third > 0) {
                    const weatherName = decodeSeg(after2.slice(0, third));
                    const url = after2.slice(third + 1);
                    const scenes = settingsState.draft.bridge.sceneAssets && settingsState.draft.bridge.sceneAssets.scenes || {};
                    const scene = scenes[sceneName];
                    if (scene && scene.times && scene.times[timeName]) {
                        const t = scene.times[timeName];
                        if (t && t.weathers) {
                        const existing = t.weathers[weatherName];
                        if (existing && typeof existing === 'object') {
                            existing.url = url;
                        } else {
                            t.weathers[weatherName] = { url, words: [] };
                        }
                    }
                    }
                    persistSettingsDraft();
                }
            }
        }
        return { ok: true };
    }

    if (normalizedAction === 'scene-add-char') {
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        settingsState.draft.bridge.sceneAssets.characters = settingsState.draft.bridge.sceneAssets.characters || {};
        settingsState.draft.bridge.sceneAssets.characterAliases = settingsState.draft.bridge.sceneAssets.characterAliases || {};
        const existingKeys = Object.keys(settingsState.draft.bridge.sceneAssets.characters);
        const newName = '角色' + (existingKeys.length + 1);
        settingsState.draft.bridge.sceneAssets.characters[newName] = { '默认': '' };
        settingsState.draft.bridge.sceneAssets.characterAliases[newName] = [];
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-remove-char:')) {
        const name = decodeSeg(normalizedAction.slice('scene-remove-char:'.length));
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        settingsState.draft.bridge.sceneAssets.characters = settingsState.draft.bridge.sceneAssets.characters || {};
        settingsState.draft.bridge.sceneAssets.characterAliases = settingsState.draft.bridge.sceneAssets.characterAliases || {};
        delete settingsState.draft.bridge.sceneAssets.characters[name];
        delete settingsState.draft.bridge.sceneAssets.characterAliases[name];
        if (settingsState.draft.bridge.sceneAssets.statusAvatars && typeof settingsState.draft.bridge.sceneAssets.statusAvatars === 'object') {
            delete settingsState.draft.bridge.sceneAssets.statusAvatars[name];
        }
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-add-char-alias:')) {
        const charName = decodeSeg(normalizedAction.slice('scene-add-char-alias:'.length));
        const globalObj = options.global || globalThis;
        const alias = (globalObj.prompt && globalObj.prompt(`为角色「${charName}」添加别名：`, '') || '').trim();
        if (!alias) return rerenderSettings();
        const sceneAssets = settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        const characters = sceneAssets.characters || {};
        const aliases = ensureCharacterAliases(settingsState);
        if (Object.prototype.hasOwnProperty.call(characters, alias)) {
            if (globalObj.alert) globalObj.alert(`「${alias}」已是角色主名称`);
            return rerenderSettings();
        }
        const duplicateOwner = Object.keys(aliases).find((name) => Array.isArray(aliases[name]) && aliases[name].includes(alias));
        if (duplicateOwner) {
            if (globalObj.alert) globalObj.alert(`别名「${alias}」已属于角色「${duplicateOwner}」`);
            return rerenderSettings();
        }
        if (!Object.prototype.hasOwnProperty.call(characters, charName)) return rerenderSettings();
        aliases[charName].push(alias);
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-remove-char-alias:')) {
        const rest = normalizedAction.slice('scene-remove-char-alias:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const charName = decodeSeg(rest.slice(0, colonIdx));
            const alias = decodeSeg(rest.slice(colonIdx + 1));
            const aliases = ensureCharacterAliases(settingsState);
            aliases[charName] = (aliases[charName] || []).filter((value) => value !== alias);
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-add-mood:')) {
        const charName = decodeSeg(normalizedAction.slice('scene-add-mood:'.length));
        const globalObj = options.global || globalThis;
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        settingsState.draft.bridge.sceneAssets.characters = settingsState.draft.bridge.sceneAssets.characters || {};
        const char = settingsState.draft.bridge.sceneAssets.characters[charName];
        if (char && typeof char === 'object') {
            const newMood = (globalObj.prompt && globalObj.prompt('情绪/槽名称（建议与情绪组名一致）：', '') || '').trim();
            if (!newMood) return rerenderSettings();
            if (Object.prototype.hasOwnProperty.call(char, newMood)) {
                if (globalObj.alert) globalObj.alert(`「${charName}」已有「${newMood}」槽（同名）`);
                return rerenderSettings();
            }
            char[newMood] = '';
            // 槽名若在词库中无对应组，自动建组并把组名作为第一个词
            const groups = ensureMoodGroups(settingsState);
            if (!groups.some((g) => g.label === newMood)) {
                groups.unshift({ label: newMood, words: [newMood] });
            }
        }
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-remove-mood:')) {
        const rest = normalizedAction.slice('scene-remove-mood:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const charName = decodeSeg(rest.slice(0, colonIdx));
            const mood = decodeSeg(rest.slice(colonIdx + 1));
            settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
            settingsState.draft.bridge.sceneAssets.characters = settingsState.draft.bridge.sceneAssets.characters || {};
            const char = settingsState.draft.bridge.sceneAssets.characters[charName];
            if (char && typeof char === 'object') delete char[mood];
        }
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-set-mood-url:')) {
        const rest = normalizedAction.slice('scene-set-mood-url:'.length);
        const firstColon = rest.indexOf(':');
        if (firstColon > 0) {
            const charName = decodeSeg(rest.slice(0, firstColon));
            const afterChar = rest.slice(firstColon + 1);
            const secondColon = afterChar.indexOf(':');
            if (secondColon > 0) {
                const mood = decodeSeg(afterChar.slice(0, secondColon));
                const url = afterChar.slice(secondColon + 1);
                settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
                settingsState.draft.bridge.sceneAssets.characters = settingsState.draft.bridge.sceneAssets.characters || {};
                if (!settingsState.draft.bridge.sceneAssets.characters[charName]) {
                    settingsState.draft.bridge.sceneAssets.characters[charName] = {};
                }
                settingsState.draft.bridge.sceneAssets.characters[charName][mood] = url;
                persistSettingsDraft();
            }
        }
        return { ok: true };
    }

    if (normalizedAction.startsWith('scene-rename-char:')) {
        const oldName = decodeSeg(normalizedAction.slice('scene-rename-char:'.length));
        const globalObj = options.global || globalThis;
        const newName = (globalObj.prompt && globalObj.prompt(`重命名角色「${oldName}」为：`, oldName) || '').trim();
        if (newName && newName !== oldName) {
            const sceneAssets = settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
            const chars = sceneAssets.characters || {};
            const aliases = ensureCharacterAliases(settingsState);
            if (Object.prototype.hasOwnProperty.call(chars, newName)) {
                if (globalObj.alert) globalObj.alert(`角色「${newName}」已存在（同名）`);
                return rerenderSettings();
            }
            const aliasOwner = Object.keys(aliases).find((name) => Array.isArray(aliases[name]) && aliases[name].includes(newName));
            if (aliasOwner) {
                if (globalObj.alert) globalObj.alert(`「${newName}」已是角色「${aliasOwner}」的别名`);
                return rerenderSettings();
            }
            sceneAssets.characters = reorderKey(chars, oldName, newName);
            sceneAssets.characterAliases = reorderKey(aliases, oldName, newName);
            if (sceneAssets.statusAvatars && typeof sceneAssets.statusAvatars === 'object') {
                sceneAssets.statusAvatars = reorderKey(sceneAssets.statusAvatars, oldName, newName);
            }
            renameSetPrefix(settingsState.asyncState.expandedSpriteSlots, `${oldName}\x00`, `${newName}\x00`);
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-rename-mood:')) {
        const rest = normalizedAction.slice('scene-rename-mood:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const charName = decodeSeg(rest.slice(0, colonIdx));
            const oldMood = decodeSeg(rest.slice(colonIdx + 1));
            const globalObj = options.global || globalThis;
            const newMood = (globalObj.prompt && globalObj.prompt(`重命名情绪「${oldMood}」为：`, oldMood) || '').trim();
            if (newMood && newMood !== oldMood) {
                settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
                const chars = settingsState.draft.bridge.sceneAssets.characters || {};
                // 同名检查：该角色已有同名槽，或词库已有同名情绪组 → 阻止，避免覆盖丢失
                if (chars[charName] && Object.prototype.hasOwnProperty.call(chars[charName], newMood)) {
                    if (globalObj.alert) globalObj.alert(`「${charName}」已有「${newMood}」槽（同名），改名会覆盖，已阻止`);
                    return rerenderSettings();
                }
                const groups = ensureMoodGroups(settingsState);
                if (groups.some((g) => g.label === newMood && g.label !== oldMood)) {
                    if (globalObj.alert) globalObj.alert(`词库已有情绪组「${newMood}」（同名），改名会覆盖，已阻止`);
                    return rerenderSettings();
                }
                // 改角色槽名
                if (chars[charName]) {
                    chars[charName] = reorderKey(chars[charName], oldMood, newMood);
                    renameSetPrefix(settingsState.asyncState.expandedSpriteSlots, `${charName}\x00${oldMood}`, `${charName}\x00${newMood}`);
                }
                // 同步词库里同名情绪组的组名（全局：所有角色用到该组名的槽一起改）
                const group = groups.find((g) => g.label === oldMood);
                if (group) {
                    group.label = newMood;
                    const wordIdx = Array.isArray(group.words) ? group.words.indexOf(oldMood) : -1;
                    if (wordIdx >= 0) group.words[wordIdx] = newMood;
                    for (const otherName of Object.keys(chars)) {
                        if (otherName === charName) continue;
                        const other = chars[otherName];
                        if (other && typeof other === 'object' && Object.prototype.hasOwnProperty.call(other, oldMood)
                            && !Object.prototype.hasOwnProperty.call(other, newMood)) {
                            chars[otherName] = reorderKey(other, oldMood, newMood);
                            renameSetPrefix(settingsState.asyncState.expandedSpriteSlots, `${otherName}\x00${oldMood}`, `${otherName}\x00${newMood}`);
                        }
                    }
                }
                const persisted = persistSettingsDraft();
                if (persisted.ok === false) return persisted;
            }
        }
        return rerenderSettings();
    }

    if (normalizedAction === 'reset-mood-groups') {
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        settingsState.draft.bridge.sceneAssets.moodGroups = cloneData(DEFAULT_MOOD_GROUPS);
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction === 'mood-add-group') {
        const globalObj = options.global || globalThis;
        const groups = ensureMoodGroups(settingsState);
        const raw = (globalObj.prompt && globalObj.prompt('新情绪组名称：', '') || '').trim();
        if (!raw) return rerenderSettings();
        if (groups.some((g) => g.label === raw)) {
            if (globalObj.alert) globalObj.alert(`情绪组「${raw}」已存在（同名）`);
            return rerenderSettings();
        }
        // 组名自动作为该组第一个词
        groups.unshift({ label: raw, words: [raw] });
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('mood-remove-group:')) {
        const label = decodeSeg(normalizedAction.slice('mood-remove-group:'.length));
        const groups = ensureMoodGroups(settingsState);
        const idx = groups.findIndex((g) => g.label === label);
        if (idx >= 0) groups.splice(idx, 1);
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('mood-rename-group:')) {
        const oldLabel = decodeSeg(normalizedAction.slice('mood-rename-group:'.length));
        const globalObj = options.global || globalThis;
        const newLabel = (globalObj.prompt && globalObj.prompt(`重命名情绪组「${oldLabel}」为：`, oldLabel) || '').trim();
        if (newLabel && newLabel !== oldLabel) {
            const groups = ensureMoodGroups(settingsState);
            if (groups.some((g) => g.label === newLabel)) {
                if (globalObj.alert) globalObj.alert(`情绪组「${newLabel}」已存在`);
                return rerenderSettings();
            }
            const group = groups.find((g) => g.label === oldLabel);
            if (group) group.label = newLabel;
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('mood-add-word:')) {
        const label = decodeSeg(normalizedAction.slice('mood-add-word:'.length));
        const globalObj = options.global || globalThis;
        const word = (globalObj.prompt && globalObj.prompt(`向「${label}」组添加情绪词：`, '') || '').trim();
        if (word) {
            const groups = ensureMoodGroups(settingsState);
            const dupGroup = groups.find((g) => Array.isArray(g.words) && g.words.includes(word));
            if (dupGroup) {
                // 词撞名：弹窗询问是否删掉重复词再加到当前组
                const confirmFn = typeof globalObj.confirm === 'function' ? globalObj.confirm.bind(globalObj) : null;
                const proceed = confirmFn
                    ? confirmFn(`「${word}」已存在于「${dupGroup.label}」组。是否删除重复词并加入「${label}」组？`)
                    : true;
                if (!proceed) return rerenderSettings();
                dupGroup.words = dupGroup.words.filter((w) => w !== word);
            }
            const group = groups.find((g) => g.label === label);
            if (group) group.words.push(word);
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('mood-remove-word:')) {
        const rest = normalizedAction.slice('mood-remove-word:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const label = decodeSeg(rest.slice(0, colonIdx));
            const word = decodeSeg(rest.slice(colonIdx + 1));
            const groups = ensureMoodGroups(settingsState);
            const group = groups.find((g) => g.label === label);
            if (group) {
                if (group.words.length <= 1) {
                    const globalObj = options.global || globalThis;
                    if (globalObj.alert) globalObj.alert('每个情绪组至少保留 1 个词');
                    return rerenderSettings();
                }
                const wi = group.words.indexOf(word);
                if (wi >= 0) group.words.splice(wi, 1);
            }
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-toggle-mood:')) {
        const rest = normalizedAction.slice('scene-toggle-mood:'.length);
        const colonIdx = rest.indexOf(':');
        if (colonIdx > 0) {
            const charName = decodeSeg(rest.slice(0, colonIdx));
            const mood = decodeSeg(rest.slice(colonIdx + 1));
            const key = charName + "\x00" + mood;
            if (!(settingsState.asyncState.expandedSpriteSlots instanceof Set)) {
                settingsState.asyncState.expandedSpriteSlots = new Set();
            }
            const set = settingsState.asyncState.expandedSpriteSlots;
            if (set.has(key)) set.delete(key); else set.add(key);
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('mood-create-group:')) {
        const label = decodeSeg(normalizedAction.slice('mood-create-group:'.length));
        const globalObj = options.global || globalThis;
        const groups = ensureMoodGroups(settingsState);
        if (groups.some((g) => g.label === label)) {
            if (globalObj.alert) globalObj.alert(`情绪组「${label}」已存在（同名）`);
            return rerenderSettings();
        }
        // 组名自动作为该组第一个词
        groups.unshift({ label, words: [label] });
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction === 'scene-preset-save') {
        const globalObj = options.global || globalThis;
        const sa = settingsState.draft.bridge.sceneAssets || {};
        let name = settingsState.asyncState.scenePresetName || '';
        if (!name) {
            name = (globalObj.prompt && globalObj.prompt('预设名称：', '') || '').trim();
            if (!name) return rerenderSettings();
        }
        const storage = globalObj.localStorage;
        const presets = loadScenePresets(storage);
        presets[name] = {
            scenes: cloneData(sa.scenes || {}),
            characters: cloneData(sa.characters || {}),
            characterAliases: cloneData(sa.characterAliases || {}),
            moodGroups: cloneData(sa.moodGroups || []),
            statusAvatars: cloneData(sa.statusAvatars || {}),
            timeGroups: cloneData(sa.timeGroups || []),
            weatherGroups: cloneData(sa.weatherGroups || []),
            spriteLayouts: cloneData((settingsState.draft.readerSettings && settingsState.draft.readerSettings.spriteLayouts) || {}),
        };
        saveScenePresets(storage, presets);
        settingsState.asyncState.scenePresetName = name;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-preset-apply:')) {
        const name = decodeSeg(normalizedAction.slice('scene-preset-apply:'.length));
        if (name) {
            const globalObj = options.global || globalThis;
            const presets = loadScenePresets(globalObj.localStorage);
            const preset = presets[name];
            if (preset) {
                // 切预设会用预设内容整体覆盖当前场景配置与立绘位置。未存进任何预设的改动
                // 会在覆盖后丢失，所以切换前先确认（取消则保持当前配置不动）。
                const confirmFn = typeof globalObj.confirm === 'function' ? globalObj.confirm.bind(globalObj) : null;
                if (confirmFn && !confirmFn(`切换到预设「${name}」会用该预设的场景、角色立绘和位置覆盖当前配置，未保存到预设的改动将丢失。是否继续？`)) {
                    return rerenderSettings();
                }
                settingsState.asyncState.scenePresetName = name;
                settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
                settingsState.draft.bridge.sceneAssets.scenes = cloneData(preset.scenes || {});
                settingsState.draft.bridge.sceneAssets.characters = cloneData(preset.characters || {});
                settingsState.draft.bridge.sceneAssets.characterAliases = cloneData(preset.characterAliases || {});
                settingsState.draft.bridge.sceneAssets.moodGroups = cloneData(preset.moodGroups || []);
                settingsState.draft.bridge.sceneAssets.statusAvatars = cloneData(preset.statusAvatars || {});
                settingsState.draft.bridge.sceneAssets.timeGroups = cloneData(preset.timeGroups || []);
                settingsState.draft.bridge.sceneAssets.weatherGroups = cloneData(preset.weatherGroups || []);
                if (preset.spriteLayouts && typeof preset.spriteLayouts === 'object') {
                    settingsState.draft.readerSettings = settingsState.draft.readerSettings || {};
                    settingsState.draft.readerSettings.spriteLayouts = cloneData(preset.spriteLayouts);
                }
                const persisted = persistSettingsDraft();
                if (persisted.ok === false) return persisted;
            } else {
                settingsState.asyncState.scenePresetName = name;
            }
        } else {
            settingsState.asyncState.scenePresetName = name;
        }
        return rerenderSettings();
    }

    if (normalizedAction === 'scene-preset-rename') {
        const oldName = settingsState.asyncState.scenePresetName || '';
        if (!oldName) return rerenderSettings();
        const globalObj = options.global || globalThis;
        const newName = (globalObj.prompt && globalObj.prompt(`重命名预设「${oldName}」为：`, oldName) || '').trim();
        if (!newName || newName === oldName) return rerenderSettings();
        const storage = globalObj.localStorage;
        const presets = loadScenePresets(storage);
        if (!presets[oldName]) return rerenderSettings();
        presets[newName] = presets[oldName];
        delete presets[oldName];
        saveScenePresets(storage, presets);
        settingsState.asyncState.scenePresetName = newName;
        return rerenderSettings();
    }

    if (normalizedAction === 'scene-preset-import') {
        const globalObj = options.global || globalThis;
        const doc = globalObj.document;
        if (!doc) return { ok: false, reason: 'no-document' };
        const fileResult = await pickPresetFile(doc);
        if (!fileResult) return rerenderSettings();
        const name = (globalObj.prompt && globalObj.prompt('预设名称：', fileResult.fileName) || '').trim();
        if (!name) return rerenderSettings();
        const storage = globalObj.localStorage;
        const presets = loadScenePresets(storage);
        presets[name] = {
            scenes: fileResult.data.scenes || {},
            characters: fileResult.data.characters || {},
            characterAliases: fileResult.data.characterAliases || {},
            moodGroups: fileResult.data.moodGroups || [],
            statusAvatars: (fileResult.data.statusAvatars && typeof fileResult.data.statusAvatars === 'object') ? fileResult.data.statusAvatars : {},
            timeGroups: fileResult.data.timeGroups || [],
            weatherGroups: fileResult.data.weatherGroups || [],
            spriteLayouts: (fileResult.data.spriteLayouts && typeof fileResult.data.spriteLayouts === 'object') ? fileResult.data.spriteLayouts : {},
        };
        saveScenePresets(storage, presets);
        settingsState.asyncState.scenePresetName = name;
        settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
        settingsState.draft.bridge.sceneAssets.scenes = cloneData(presets[name].scenes);
        settingsState.draft.bridge.sceneAssets.characters = cloneData(presets[name].characters);
        settingsState.draft.bridge.sceneAssets.characterAliases = cloneData(presets[name].characterAliases || {});
        settingsState.draft.bridge.sceneAssets.statusAvatars = cloneData(presets[name].statusAvatars || {});
        settingsState.draft.bridge.sceneAssets.moodGroups = cloneData(presets[name].moodGroups);
        settingsState.draft.bridge.sceneAssets.timeGroups = cloneData(presets[name].timeGroups || []);
        settingsState.draft.bridge.sceneAssets.weatherGroups = cloneData(presets[name].weatherGroups || []);
        settingsState.draft.readerSettings = settingsState.draft.readerSettings || {};
        settingsState.draft.readerSettings.spriteLayouts = cloneData(presets[name].spriteLayouts);
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction === 'scene-preset-export') {
        const name = settingsState.asyncState.scenePresetName || '';
        if (!name) return rerenderSettings();
        const globalObj = options.global || globalThis;
        const presets = loadScenePresets(globalObj.localStorage);
        const preset = presets[name];
        if (!preset) return rerenderSettings();
        const doc = globalObj.document;
        if (!doc) return { ok: false, reason: 'no-document' };
        const json = JSON.stringify({ scenes: preset.scenes || {}, characters: preset.characters || {}, characterAliases: preset.characterAliases || {}, moodGroups: preset.moodGroups || [], timeGroups: preset.timeGroups || [], weatherGroups: preset.weatherGroups || [], spriteLayouts: preset.spriteLayouts || {}, statusAvatars: preset.statusAvatars || {} }, null, 2);
        const blob = new Blob([json], { type: 'application/json' });
        const url = URL.createObjectURL(blob);
        const a = doc.createElement('a');
        a.href = url;
        a.download = `${name}.json`;
        doc.body.appendChild(a);
        a.click();
        doc.body.removeChild(a);
        URL.revokeObjectURL(url);
        return { ok: true };
    }

    if (normalizedAction === 'scene-preset-delete') {
        const name = settingsState.asyncState.scenePresetName || '';
        if (!name) return rerenderSettings();
        const globalObj = options.global || globalThis;
        if (globalObj.confirm && !globalObj.confirm(`删除预设「${name}」？`)) return rerenderSettings();
        const storage = globalObj.localStorage;
        const presets = loadScenePresets(storage);
        delete presets[name];
        saveScenePresets(storage, presets);
        settingsState.asyncState.scenePresetName = '';
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-toggle-bg:')) {
        const key = 'bg\x00' + decodeSeg(normalizedAction.slice('scene-toggle-bg:'.length));
        if (!(settingsState.asyncState.expandedSceneSlots instanceof Set)) settingsState.asyncState.expandedSceneSlots = new Set();
        const set = settingsState.asyncState.expandedSceneSlots;
        if (set.has(key)) set.delete(key); else set.add(key);
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-toggle-time:')) {
        const rest = normalizedAction.slice('scene-toggle-time:'.length);
        const c = rest.indexOf(':');
        if (c > 0) {
            const key = 'time\x00' + decodeSeg(rest.slice(0, c)) + '\x00' + decodeSeg(rest.slice(c + 1));
            if (!(settingsState.asyncState.expandedSceneSlots instanceof Set)) settingsState.asyncState.expandedSceneSlots = new Set();
            const set = settingsState.asyncState.expandedSceneSlots;
            if (set.has(key)) set.delete(key); else set.add(key);
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-toggle-weather:')) {
        const rest = normalizedAction.slice('scene-toggle-weather:'.length);
        const c1 = rest.indexOf(':'); const c2 = c1 >= 0 ? rest.indexOf(':', c1 + 1) : -1;
        if (c1 > 0 && c2 > c1) {
            const key = 'weather\x00' + decodeSeg(rest.slice(0, c1)) + '\x00' + decodeSeg(rest.slice(c1 + 1, c2)) + '\x00' + decodeSeg(rest.slice(c2 + 1));
            if (!(settingsState.asyncState.expandedSceneSlots instanceof Set)) settingsState.asyncState.expandedSceneSlots = new Set();
            const set = settingsState.asyncState.expandedSceneSlots;
            if (set.has(key)) set.delete(key); else set.add(key);
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-add-bg-word:')) {
        const sceneName = decodeSeg(normalizedAction.slice('scene-add-bg-word:'.length));
        const globalObj = options.global || globalThis;
        const alias = (globalObj.prompt && globalObj.prompt(`为场景「${sceneName}」添加别名：`, '') || '').trim();
        if (alias) {
            const scenes = (settingsState.draft.bridge.sceneAssets || {}).scenes || {};
            if (Object.prototype.hasOwnProperty.call(scenes, alias)) {
                if (globalObj.alert) globalObj.alert(`「${alias}」已是场景主名称`);
                return rerenderSettings();
            }
            const dup = findSceneWord(scenes, alias);
            if (dup) {
                const proceed = typeof globalObj.confirm === 'function'
                    ? globalObj.confirm(`别名「${alias}」已属于${dup.label}。是否移动到场景「${sceneName}」？`)
                    : true;
                if (!proceed) return rerenderSettings();
                removeSceneWordEntry(scenes, dup);
            }
            const s = scenes[sceneName];
            if (s && typeof s === 'object') { if (!Array.isArray(s.words)) s.words = []; s.words.push(alias); }
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('scene-remove-bg-word:')) {
        const rest = normalizedAction.slice('scene-remove-bg-word:'.length);
        const c = rest.indexOf(':');
        if (c > 0) {
            const sceneName = decodeSeg(rest.slice(0, c)); const word = decodeSeg(rest.slice(c + 1));
            const scenes = (settingsState.draft.bridge.sceneAssets || {}).scenes || {};
            const s = scenes[sceneName];
            if (s && Array.isArray(s.words)) {
                s.words = s.words.filter((w) => w !== word);
            }
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('time-add-word:')) {
        const label = decodeSeg(normalizedAction.slice('time-add-word:'.length));
        const globalObj = options.global || globalThis;
        const word = (globalObj.prompt && globalObj.prompt(`向时间组「${label}」添加词：`, '') || '').trim();
        if (word) {
            const groups = ensureTimeGroups(settingsState);
            const dup = groups.find((g) => Array.isArray(g.words) && g.words.includes(word));
            if (dup) {
                const proceed = typeof globalObj.confirm === 'function'
                    ? globalObj.confirm(`「${word}」已存在于时间组「${dup.label}」。是否删除重复词并加入「${label}」？`) : true;
                if (!proceed) return rerenderSettings();
                dup.words = dup.words.filter((w) => w !== word);
            }
            const g = groups.find((g) => g.label === label);
            if (g) g.words.push(word);
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('time-remove-word:')) {
        const rest = normalizedAction.slice('time-remove-word:'.length);
        const c = rest.indexOf(':');
        if (c > 0) {
            const label = decodeSeg(rest.slice(0, c)); const word = decodeSeg(rest.slice(c + 1));
            const groups = ensureTimeGroups(settingsState);
            const g = groups.find((g) => g.label === label);
            if (g && Array.isArray(g.words)) {
                const globalObj = options.global || globalThis;
                if (g.words.length <= 1) { if (globalObj.alert) globalObj.alert('至少保留 1 个词'); return rerenderSettings(); }
                g.words = g.words.filter((w) => w !== word);
            }
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('time-create-group:')) {
        const label = decodeSeg(normalizedAction.slice('time-create-group:'.length));
        const globalObj = options.global || globalThis;
        const groups = ensureTimeGroups(settingsState);
        if (groups.some((g) => g.label === label)) { if (globalObj.alert) globalObj.alert(`时间组「${label}」已存在`); return rerenderSettings(); }
        groups.unshift({ label, words: [label] });
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('weather-add-word:')) {
        const label = decodeSeg(normalizedAction.slice('weather-add-word:'.length));
        const globalObj = options.global || globalThis;
        const word = (globalObj.prompt && globalObj.prompt(`向天气组「${label}」添加词：`, '') || '').trim();
        if (word) {
            const groups = ensureWeatherGroups(settingsState);
            const dup = groups.find((g) => Array.isArray(g.words) && g.words.includes(word));
            if (dup) {
                const proceed = typeof globalObj.confirm === 'function'
                    ? globalObj.confirm(`「${word}」已存在于天气组「${dup.label}」。是否删除重复词并加入「${label}」？`) : true;
                if (!proceed) return rerenderSettings();
                dup.words = dup.words.filter((w) => w !== word);
            }
            const g = groups.find((g) => g.label === label);
            if (g) g.words.push(word);
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('weather-remove-word:')) {
        const rest = normalizedAction.slice('weather-remove-word:'.length);
        const c = rest.indexOf(':');
        if (c > 0) {
            const label = decodeSeg(rest.slice(0, c)); const word = decodeSeg(rest.slice(c + 1));
            const groups = ensureWeatherGroups(settingsState);
            const g = groups.find((g) => g.label === label);
            if (g && Array.isArray(g.words)) {
                const globalObj = options.global || globalThis;
                if (g.words.length <= 1) { if (globalObj.alert) globalObj.alert('至少保留 1 个词'); return rerenderSettings(); }
                g.words = g.words.filter((w) => w !== word);
            }
            const persisted = persistSettingsDraft();
            if (persisted.ok === false) return persisted;
        }
        return rerenderSettings();
    }

    if (normalizedAction.startsWith('weather-create-group:')) {
        const label = decodeSeg(normalizedAction.slice('weather-create-group:'.length));
        const globalObj = options.global || globalThis;
        const groups = ensureWeatherGroups(settingsState);
        if (groups.some((g) => g.label === label)) { if (globalObj.alert) globalObj.alert(`天气组「${label}」已存在`); return rerenderSettings(); }
        groups.unshift({ label, words: [label] });
        const persisted = persistSettingsDraft();
        if (persisted.ok === false) return persisted;
        return rerenderSettings();
    }

    return { ok: false, reason: 'unknown-settings-action', action: normalizedAction };
}

function reorderKey(obj, oldKey, newKey) {
    const result = {};
    for (const [k, v] of Object.entries(obj)) result[k === oldKey ? newKey : k] = v;
    return result;
}

function renameSetPrefix(set, oldPrefix, newPrefix) {
    if (!(set instanceof Set)) return;
    const toUpdate = [];
    for (const key of set) if (key.startsWith(oldPrefix)) toUpdate.push(key);
    for (const k of toUpdate) { set.delete(k); set.add(newPrefix + k.slice(oldPrefix.length)); }
}

function findSceneWord(scenes, word) {
    for (const [sn, sv] of Object.entries(scenes || {})) {
        const s = typeof sv === 'string' ? {} : (sv || {});
        if (Array.isArray(s.words) && s.words.includes(word)) return { type: 'bg', keys: [sn], word, label: `场景「${sn}」` };
    }
    return null;
}

function removeSceneWordEntry(scenes, entry) {
    const [sn, tn, wn] = entry.keys;
    let arr = null;
    if (entry.type === 'bg') arr = scenes[sn] && scenes[sn].words;
    else if (entry.type === 'time') arr = scenes[sn] && scenes[sn].times && scenes[sn].times[tn] && scenes[sn].times[tn].words;
    else arr = scenes[sn] && scenes[sn].times && scenes[sn].times[tn] && scenes[sn].times[tn].weathers && scenes[sn].times[tn].weathers[wn] && scenes[sn].times[tn].weathers[wn].words;
    if (Array.isArray(arr)) { const i = arr.indexOf(entry.word); if (i >= 0) arr.splice(i, 1); }
}

function pickStatusAvatarFile(doc) {
    return new Promise((resolve) => {
        const input = doc.createElement('input');
        input.type = 'file';
        input.accept = 'image/*';
        let done = false;
        const finish = (val) => { if (!done) { done = true; resolve(val); } };
        input.onchange = () => {
            const file = input.files && input.files[0];
            if (!file) { finish(null); return; }
            const type = String(file.type || '');
            if (type && !STATUS_AVATAR_MIME.test(type)) { finish({ ok: false, reason: 'not-image' }); return; }
            if (Number(file.size) > STATUS_AVATAR_MAX_BYTES) { finish({ ok: false, reason: 'too-large' }); return; }
            const fr = new FileReader();
            fr.onload = (e) => {
                const dataUrl = String((e && e.target && e.target.result) || '');
                if (!/^data:image\//i.test(dataUrl)) { finish({ ok: false, reason: 'not-image' }); return; }
                finish({ ok: true, dataUrl });
            };
            fr.onerror = () => finish({ ok: false, reason: 'read-failed' });
            fr.readAsDataURL(file);
        };
        input.click();
        setTimeout(() => finish(null), 300000);
    });
}

function pickPresetFile(doc) {
    return new Promise((resolve) => {
        const input = doc.createElement('input');
        input.type = 'file';
        input.accept = '.json';
        let done = false;
        const finish = (val) => { if (!done) { done = true; resolve(val); } };
        input.onchange = () => {
            const file = input.files && input.files[0];
            if (!file) { finish(null); return; }
            const fileName = file.name.replace(/\.json$/i, '');
            const fr = new FileReader();
            fr.onload = (e) => { try { finish({ fileName, data: JSON.parse(e.target.result) }); } catch { finish(null); } };
            fr.onerror = () => finish(null);
            fr.readAsText(file);
        };
        input.click();
        setTimeout(() => finish(null), 300000);
    });
}

function ensureTimeGroups(settingsState) {
    settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
    const sa = settingsState.draft.bridge.sceneAssets;
    if (!Array.isArray(sa.timeGroups)) sa.timeGroups = [];
    return sa.timeGroups;
}

function ensureWeatherGroups(settingsState) {
    settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
    const sa = settingsState.draft.bridge.sceneAssets;
    if (!Array.isArray(sa.weatherGroups)) sa.weatherGroups = [];
    return sa.weatherGroups;
}

function ensureMoodGroups(settingsState) {    settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
    const sa = settingsState.draft.bridge.sceneAssets;
    if (!Array.isArray(sa.moodGroups)) {
        sa.moodGroups = normalizeMoodGroups(sa.moodGroups);
    }
    return sa.moodGroups;
}

function ensureCharacterAliases(settingsState) {
    settingsState.draft.bridge.sceneAssets = settingsState.draft.bridge.sceneAssets || {};
    const sceneAssets = settingsState.draft.bridge.sceneAssets;
    if (!sceneAssets.characterAliases || typeof sceneAssets.characterAliases !== 'object' || Array.isArray(sceneAssets.characterAliases)) {
        sceneAssets.characterAliases = {};
    }
    for (const name of Object.keys(sceneAssets.characters || {})) {
        if (!Array.isArray(sceneAssets.characterAliases[name])) sceneAssets.characterAliases[name] = [];
    }
    return sceneAssets.characterAliases;
}

__igsDefine(exports, "handleSettingsAction", () => handleSettingsAction);
});
__igsRegister("src/scene/scene-preset-store.js", function(module, exports, require) {
const KEY = 'igs:scene-presets:v1';
function loadScenePresets(storage) {
    try {
        const raw = storage && storage.getItem(KEY);
        if (!raw) return {};
        const parsed = JSON.parse(raw);
        return parsed && typeof parsed.presets === 'object' && !Array.isArray(parsed.presets)
            ? parsed.presets : {};
    } catch { return {}; }
}
function saveScenePresets(storage, presets) {
    try { storage.setItem(KEY, JSON.stringify({ version: 1, presets })); } catch {}
}

__igsDefine(exports, "loadScenePresets", () => loadScenePresets);
__igsDefine(exports, "saveScenePresets", () => saveScenePresets);
});
__igsRegister("src/visual/igs-ui/reader-dom-render.js", function(module, exports, require) {
const { ORIGINAL_READER_ICONS, ORIGINAL_READER_TOOLBAR_BUTTONS } = require("src/visual/igs-ui/original-reader-source.js");
const { TOOLBAR_ACTIONS } = require("src/visual/igs-ui/reader-host-constants.js");
const { ensureImageLoadingSpinner, ensureImageEmptyPlaceholder, getOwnerWindow, readElementHeight, readElementWidth, removeImageEmptyPlaceholder, removeImageLoadingSpinner } = require("src/visual/igs-ui/reader-dom-utils.js");
const { applyTransparentGlassMaterial } = require("src/styles/glass-material.js");
const { resolveStatusHudScale, resolveStatusHudLocationScale, NSFW_VEIL_LEVEL_STYLE } = require("src/data/shujuku/status-hud-model.js");
const { computeLineHeight, igsDebug } = require("src/visual/igs-ui/reader-value-utils.js");
const { renderDialogueHtml, resolveActiveTheme, resolveSpriteLayout } = require("src/visual/igs-ui/settings-normalize.js");
const { applyReaderModeRuntime } = require("src/visual/igs-ui/reader-runtime.js");
const { applyDialogSkinAssets, isClassicDialogSkin, normalizeClassicDialogWidthPercent } = require("src/visual/igs-ui/classic-dialog-skin.js");
function createReaderButton(doc, id, title, html) {
    const button = doc.createElement('button');
    button.id = `igs-btn-${id}`;
    button.className = 'igs-icon-btn';
    button.type = 'button';
    button.setAttribute('data-act', id);
    button.setAttribute('title', title);
    button.innerHTML = html;
    return button;
}

function addClasses(element, classNames) {
    if (!element) return;
    const current = String(element.className || '').split(/\s+/).filter(Boolean);
    const next = new Set(current);
    for (const name of String(classNames || '').split(/\s+/).filter(Boolean)) next.add(name);
    element.className = Array.from(next).join(' ');
}

function ensureReaderLayer(doc, overlay, id, className, beforeNode = null) {
    if (!doc || !overlay || typeof doc.createElement !== 'function') return null;
    let layer = overlay.querySelector ? overlay.querySelector(`#${id}`) : null;
    if (!layer) {
        layer = doc.createElement('div');
        layer.id = id;
        layer.className = className;
        if (beforeNode && beforeNode.parentNode === overlay && typeof overlay.insertBefore === 'function') {
            overlay.insertBefore(layer, beforeNode);
        } else if (typeof overlay.appendChild === 'function') {
            overlay.appendChild(layer);
        }
    } else {
        addClasses(layer, className);
    }
    return layer;
}
function normalizeReaderStableLayers(overlay) {
    if (!overlay || !overlay.ownerDocument) return overlay;
    const doc = overlay.ownerDocument;
    addClasses(overlay, 'igs-stage');

    const bgBlur = overlay.querySelector ? overlay.querySelector('#igs-bg-blur') : null;
    const bg = overlay.querySelector ? overlay.querySelector('#igs-bg') : null;
    const sprite = overlay.querySelector ? overlay.querySelector('#igs-sprite') : null;
    addClasses(bgBlur, 'igs-background-layer igs-background-blur-layer');
    addClasses(bg, 'igs-background-layer');
    addClasses(sprite, 'igs-character-layer');

    const optionBubbles = overlay.querySelector ? overlay.querySelector('#igs-option-bubbles') : null;
    const dialog = overlay.querySelector ? overlay.querySelector('#igs-dialog') : null;
    const toolbar = overlay.querySelector ? overlay.querySelector('#igs-ctrl-bar') : null;
    addClasses(toolbar, 'igs-toolbar');
    const optionLayer = ensureReaderLayer(doc, overlay, 'igs-option-layer', 'igs-choice-layer', dialog || toolbar);
    const dialogLayer = ensureReaderLayer(doc, overlay, 'igs-dialog-layer', 'igs-dialogue-layer', toolbar);
    const toolbarLayer = ensureReaderLayer(doc, overlay, 'igs-toolbar-layer', 'igs-hud-layer');
    ensureReaderLayer(doc, overlay, 'igs-db-layer', 'igs-system-layer');
    if (overlay.querySelector && !overlay.querySelector('#igs-status-hud')) {
        const statusHud = doc.createElement('div');
        statusHud.id = 'igs-status-hud';
        statusHud.setAttribute('hidden', '');
        overlay.appendChild(statusHud);
    }

    if (optionBubbles && optionLayer && optionBubbles.parentNode !== optionLayer) optionLayer.appendChild(optionBubbles);
    if (dialog && dialogLayer && dialog.parentNode !== dialogLayer) dialogLayer.appendChild(dialog);
    if (toolbar && toolbarLayer && toolbar.parentNode !== toolbarLayer) toolbarLayer.appendChild(toolbar);
    return overlay;
}
function buildFallbackReaderOverlay(doc) {
    if (!doc || typeof doc.createElement !== 'function') return null;
    const overlay = doc.createElement('div');
    overlay.id = 'igs-overlay';

    const bgBlur = doc.createElement('div');
    bgBlur.id = 'igs-bg-blur';
    overlay.appendChild(bgBlur);

    const bg = doc.createElement('div');
    bg.id = 'igs-bg';
    overlay.appendChild(bg);

    const sprite = doc.createElement('div');
    sprite.id = 'igs-sprite';
    overlay.appendChild(sprite);

    const clickLayer = doc.createElement('div');
    clickLayer.id = 'igs-click-layer';
    overlay.appendChild(clickLayer);

    const optionBubbles = doc.createElement('div');
    optionBubbles.id = 'igs-option-bubbles';
    optionBubbles.setAttribute('data-igs-pos', 'top-left');
    optionBubbles.setAttribute('data-igs-width', 'dialog');
    optionBubbles.setAttribute('hidden', '');
    overlay.appendChild(optionBubbles);

    const dialog = doc.createElement('div');
    dialog.id = 'igs-dialog';
    dialog.className = 'igs-dialog';
    overlay.appendChild(dialog);

    const ctrlBar = doc.createElement('div');
    ctrlBar.id = 'igs-ctrl-bar';
    ctrlBar.className = 'igs-ctrl-bar igs-toolbar';
    dialog.appendChild(ctrlBar);

    const barBtns = doc.createElement('div');
    barBtns.id = 'igs-bar-btns';
    ctrlBar.appendChild(barBtns);
    for (const button of ORIGINAL_READER_TOOLBAR_BUTTONS) {
        barBtns.appendChild(createReaderButton(doc, button.id, button.title, button.html));
    }

    const settings = doc.createElement('div');
    settings.id = 'igs-settings';
    settings.setAttribute('aria-hidden', 'true');
    ctrlBar.appendChild(settings);

    const pinned = doc.createElement('div');
    pinned.id = 'igs-bar-pinned';
    ctrlBar.appendChild(pinned);

    ctrlBar.appendChild(createReaderButton(doc, 'toggle-bar', '收纳/展开按钮', ORIGINAL_READER_ICONS.toggleBar));
    ctrlBar.appendChild(createReaderButton(doc, 'close', '退出', ORIGINAL_READER_ICONS.close));

    const progress = doc.createElement('div');
    progress.id = 'igs-progress';
    progress.className = 'igs-progress';
    dialog.appendChild(progress);

    const speakerEl = doc.createElement('div');
    speakerEl.id = 'igs-speaker';
    speakerEl.className = 'igs-speaker';
    dialog.appendChild(speakerEl);

    const dividerEl = doc.createElement('div');
    dividerEl.id = 'igs-divider';
    dividerEl.className = 'igs-divider';
    dialog.appendChild(dividerEl);

    const text = doc.createElement('div');
    text.id = 'igs-text';
    text.className = 'igs-text';
    dialog.appendChild(text);

    const statusLine = doc.createElement('div');
    statusLine.id = 'igs-status-line';
    statusLine.className = 'igs-status-line';
    dialog.appendChild(statusLine);

    const controls = doc.createElement('div');
    controls.className = 'igs-controls';
    // id 含 shujuku_v120- 子串以命中 Veridis 关键词过滤插件的输入框豁免，
    // 避免其全局 input 监听器替换 #igs-input 里用户正在输入的文字。
    controls.id = 'igs-controls-shujuku_v120-guard';
    dialog.appendChild(controls);

    const sendStatus = doc.createElement('div');
    sendStatus.id = 'igs-send-status';
    sendStatus.setAttribute('aria-live', 'polite');
    controls.appendChild(sendStatus);

    const spinner = doc.createElement('span');
    spinner.className = 'igs-spinner';
    sendStatus.appendChild(spinner);

    const sendStatusText = doc.createElement('span');
    sendStatusText.id = 'igs-send-status-text';
    sendStatusText.textContent = '已发送，等待 AI 回复…';
    sendStatus.appendChild(sendStatusText);

    const input = doc.createElement('input');
    input.id = 'igs-input';
    input.className = 'igs-input';
    input.type = 'text';
    input.placeholder = '输入内容后按 Enter 发送';
    controls.appendChild(input);

    const sendButton = doc.createElement('button');
    sendButton.id = 'igs-send-btn';
    sendButton.className = 'igs-send-btn';
    sendButton.type = 'button';
    sendButton.textContent = '发送';
    controls.appendChild(sendButton);

    const toast = doc.createElement('div');
    toast.id = 'igs-toast';
    toast.setAttribute('aria-live', 'polite');
    dialog.appendChild(toast);

    const statusHud = doc.createElement('div');
    statusHud.id = 'igs-status-hud';
    statusHud.setAttribute('hidden', '');
    overlay.appendChild(statusHud);

    return normalizeReaderStableLayers(overlay);
}
function buildFallbackSettingsOverlay(doc, snapshot, ctx = {}) {
    if (!doc || typeof doc.createElement !== 'function') return null;
    const overlay = doc.createElement('div');
    overlay.id = 'igs-unified-settings';
    overlay.setAttribute('data-igs-igs-ui', 'true');
    overlay.setAttribute('data-igs-settings-theme', snapshot.settingsTheme || 'night');

    const shell = doc.createElement('div');
    shell.className = 'igs-settings-shell';
    shell.setAttribute('role', 'dialog');
    shell.setAttribute('aria-modal', 'true');
    shell.setAttribute('aria-label', '设置');
    overlay.appendChild(shell);

    const head = doc.createElement('div');
    head.className = 'igs-settings-head';
    shell.appendChild(head);

    const title = doc.createElement('div');
    title.className = 'igs-settings-title';
    title.textContent = '设置';
    head.appendChild(title);

    const themeToggle = doc.createElement('button');
    themeToggle.className = 'igs-settings-theme-toggle';
    themeToggle.type = 'button';
    themeToggle.setAttribute('data-action', 'toggle-settings-theme');
    themeToggle.setAttribute('aria-label', snapshot.settingsThemeLabel || '切换设置配色');
    themeToggle.setAttribute('title', snapshot.settingsThemeLabel || '切换设置配色');
    themeToggle.setAttribute('aria-pressed', snapshot.settingsThemePressed || 'false');
    themeToggle.innerHTML = snapshot.settingsThemeIcon || '';
    head.appendChild(themeToggle);

    const headSpacer = doc.createElement('div');
    headSpacer.className = 'igs-settings-head-spacer';
    head.appendChild(headSpacer);

    const badge = doc.createElement('div');
    badge.className = 'igs-settings-badge';
    badge.textContent = ctx.version || '0.5.4';
    head.appendChild(badge);

    const close = doc.createElement('button');
    close.className = 'igs-settings-close';
    close.type = 'button';
    close.setAttribute('data-action', 'close');
    close.setAttribute('aria-label', '关闭');
    close.textContent = '×';
    head.appendChild(close);

    const tabs = doc.createElement('div');
    tabs.className = 'igs-settings-tabs';
    shell.appendChild(tabs);
    for (const tab of snapshot.tabs || []) {
        const button = doc.createElement('button');
        button.className = `igs-settings-tab${tab.active ? ' is-active' : ''}`;
        button.type = 'button';
        button.setAttribute('data-tab', tab.id);
        button.textContent = tab.label;
        tabs.appendChild(button);
    }

    const body = doc.createElement('div');
    body.className = 'igs-settings-body';
    if (typeof ctx.renderSettingsBody === 'function') {
        body.innerHTML = ctx.renderSettingsBody(snapshot.tab, snapshot.draft, {
            readerSubTab: snapshot.readerSubTab,
            sceneSettingsSubTab: snapshot.sceneSettingsSubTab,
            sceneSubTab: snapshot.sceneSubTab,
            promptRuleStatus: snapshot.resultText && snapshot.resultText.promptRule,
            promptRuleDraft: snapshot.resultText && snapshot.resultText.promptRuleDraft,
            imageResult: snapshot.resultText && snapshot.resultText.image,
            imageModelsMessage: snapshot.resultText && snapshot.resultText.imageModels,
            virtualRegexPreview: snapshot.resultText && snapshot.resultText.virtualRegex,
        });
    }
    shell.appendChild(body);
    return overlay;
}
function applyToolbarState(root, current) {
    if (!root || !current) return;
    const collapsible = root.querySelector('#igs-bar-btns');
    const pinned = root.querySelector('#igs-bar-pinned');
    const readerSettings = current.snapshot && current.snapshot.readerSettings || {};
    const pins = new Set(Array.isArray(readerSettings.pinnedBtns) ? readerSettings.pinnedBtns : []);
    const hiddenSet = new Set(Array.isArray(readerSettings.hiddenBtns) ? readerSettings.hiddenBtns : []);
    const embeddedMode = current.snapshot && current.snapshot.mode === 'embedded';
    const compactChrome = embeddedMode || Boolean(root.classList && root.classList.contains('igs-default-reader-chrome'));
    const dockTop = !compactChrome && readerSettings.toolbarDock === 'top';
    const toolbarExpanded = current.toolbarCollapsed === false;
    if (root.classList) {
        root.classList.toggle('igs-toolbar-expanded', toolbarExpanded);
    }
    const order = Array.isArray(readerSettings.btnOrder) && readerSettings.btnOrder.length
        ? readerSettings.btnOrder
        : TOOLBAR_ACTIONS.map(([id]) => id);

    for (const id of order) {
        const button = root.querySelector(`#igs-btn-${id}`);
        if (!button) continue;
        if (hiddenSet.has(id)) {
            button.style.display = 'none';
        } else {
            button.style.display = '';
            // 顶部固定模式下，按钮区横向滚动；设置键固定到右侧不随之滚动。
            if ((pins.has(id) || (dockTop && id === 'settings')) && pinned) {
                pinned.appendChild(button);
            } else if (collapsible) {
                collapsible.appendChild(button);
            }
        }
    }

    if (collapsible) {
        collapsible.style.display = current.toolbarCollapsed ? 'none' : 'flex';
        collapsible.style.gap = compactChrome ? '2px' : '6px';
        collapsible.style.alignItems = 'center';
    }
    if (pinned) {
        pinned.style.display = 'flex';
        pinned.style.gap = compactChrome ? '2px' : '6px';
        pinned.style.alignItems = 'center';
    }

    const statusHud = root.querySelector('#igs-status-hud');
    if (statusHud) {
        const persistedCollapsed = Boolean(readerSettings.statusHud && readerSettings.statusHud.collapsed);
        statusHud.classList.toggle('igs-hud-collapsed', persistedCollapsed || toolbarExpanded);
    }

    // 顶部固定栏：按钮放得下时平均铺满（space-evenly），放不下时改左对齐以便横向滚动查看
    // （space-evenly 在溢出时会把首尾按钮推出可视区且滚不到，故溢出时切 flex-start）。
    if (collapsible && typeof collapsible.classList !== 'undefined') {
        const measureOverflow = () => {
            if (!dockTop) {
                collapsible.classList.remove('igs-bar-overflow');
                return;
            }
            const overflowing = collapsible.scrollWidth > collapsible.clientWidth + 1;
            collapsible.classList.toggle('igs-bar-overflow', overflowing);
        };
        const win = getOwnerWindow(root);
        if (win && typeof win.requestAnimationFrame === 'function') {
            win.requestAnimationFrame(measureOverflow);
        } else {
            measureOverflow();
        }
    }
}

function applyDialogBgOverride(root, snapshot, classicDialog) {
    if (!root || !root.style || typeof root.style.setProperty !== 'function') return;
    if (classicDialog) {
        root.style.removeProperty('--igs-dialog-bg');
        return;
    }
    const value = resolveActiveTheme(snapshot).dialogBg;
    if (value) {
        const opacity = snapshot.readerSettings && snapshot.readerSettings.glassOpacity;
        root.style.setProperty('--igs-dialog-bg', hexToRgba(value, opacity));
    } else {
        root.style.removeProperty('--igs-dialog-bg');
    }
}
function applyReaderSettingsToDom(root, snapshot, current, refs = {}) {
    const dialog = refs.dialog || root.querySelector('#igs-dialog');
    const textEl = refs.textEl || root.querySelector('#igs-text');
    const toolbar = refs.toolbar || root.querySelector('#igs-ctrl-bar');
    const controls = root.querySelector('.igs-controls');
    const bg = root.querySelector('#igs-bg');
    const bgBlur = root.querySelector('#igs-bg-blur');
    const readerSettings = snapshot.readerSettings || {};
    const classicDialog = isClassicDialogSkin(readerSettings);
    const inlineMode = snapshot.mode === 'pc' || snapshot.mode === 'mobile';
    const pcMode = snapshot.mode === 'pc';
    const embeddedMode = snapshot.mode === 'embedded';
    const win = getOwnerWindow(root);
    const overlayWidth = readElementWidth(root, win && win.innerWidth);
    const overlayHeight = readElementHeight(root, win && win.innerHeight);
    applyTransparentGlassMaterial(root, readerSettings.glassOpacity, {
        backdropFilter: readerSettings.glassBackdropFilter,
    });
    applyDialogBgOverride(root, snapshot, classicDialog);

    if (textEl) {
        textEl.style.fontSize = `${readerSettings.fontSize}px`;
        textEl.style.lineHeight = computeLineHeight(readerSettings.fontSize);
        textEl.style.minHeight = '0';
    }

    if (dialog) {
        applyDialogSkinAssets(dialog, readerSettings);
        dialog.style.height = '';
        if (classicDialog) {
            dialog.style.minHeight = '';
            dialog.style.maxHeight = '';
        } else {
            const viewportHeight = Number(win && win.visualViewport && win.visualViewport.height)
                || Number(win && win.innerHeight)
                || overlayHeight;
            const designHeight = snapshot.mode === 'pc' ? 540 : 680;
            const runtimeHeight = Math.min(designHeight, Math.max(180, viewportHeight - 32));
            const floatingMax = Math.max(140, Math.floor(runtimeHeight * 0.86));
            const availableHeight = embeddedMode
                ? overlayHeight - 28
                : inlineMode
                    ? floatingMax
                    : viewportHeight - 48;
            const maxDialogHeight = Math.max(60, Math.min(600, Math.floor(availableHeight)));
            if (readerSettings.dialogHeight == null) {
                dialog.style.height = 'auto';
                dialog.style.minHeight = '0';
                dialog.style.maxHeight = `${maxDialogHeight}px`;
            } else {
                const requestedHeight = Number(readerSettings.dialogHeight);
                // 固定高度即用户意图：只保底 60px 防止零高，不再按可用高度封顶，
                // 避免手机地址栏伸缩导致 maxDialogHeight 抖动、高度反复跳动。
                const target = Math.max(60, Number.isFinite(requestedHeight) ? requestedHeight : 60);
                dialog.style.height = `${target}px`;
                dialog.style.minHeight = '0';
                dialog.style.maxHeight = 'none';
            }
            if (embeddedMode) dialog.style.width = '';
        }

        dialog.style.left = '';
        dialog.style.right = '';
        dialog.style.marginLeft = '';
        dialog.style.marginRight = '';
        const minimumDialogWidth = classicDialog ? 280 : (inlineMode ? 180 : 260);
        if (classicDialog && pcMode) {
            const widthPercent = normalizeClassicDialogWidthPercent(readerSettings.classicDialogWidthPercent);
            const horizontalGap = Math.round(24 * widthPercent) / 100;
            dialog.style.left = '0';
            dialog.style.right = '0';
            dialog.style.marginLeft = 'auto';
            dialog.style.marginRight = 'auto';
            dialog.style.width = `max(${minimumDialogWidth}px,calc(${widthPercent}% - ${horizontalGap}px))`;
        } else if (embeddedMode || (classicDialog && snapshot.mode === 'mobile') || readerSettings.dialogWidth == null) {
            dialog.style.width = '';
        } else if (inlineMode) {
            const clampedWidth = Math.max(minimumDialogWidth, Math.min(readerSettings.dialogWidth, Math.max(minimumDialogWidth, (overlayWidth || readerSettings.dialogWidth) - 24)));
            dialog.style.width = `${clampedWidth}px`;
        } else {
            const viewportWidth = Number(win && win.innerWidth) || readerSettings.dialogWidth;
            dialog.style.width = `${Math.max(minimumDialogWidth, Math.min(readerSettings.dialogWidth, Math.max(minimumDialogWidth, viewportWidth - 8)))}px`;
        }
        dialog.style.background = '';
    }

    const compactChrome = embeddedMode || !classicDialog;
    const toolbarDock = compactChrome ? 'float' : (readerSettings.toolbarDock === 'top' ? 'top' : 'float');
    if (root && root.classList) {
        root.classList.toggle('igs-toolbar-top', toolbarDock === 'top');
    }
    if (toolbar) {
        toolbar.setAttribute('data-igs-toolbar-dock', toolbarDock);
        // 顶部固定栏铺满整条，不做缩放（缩放是悬浮小条用的，全宽栏缩放会从角落缩成异形）。
        if (toolbarDock === 'top') {
            toolbar.style.transform = '';
            toolbar.style.transformOrigin = '';
        } else {
            toolbar.style.transform = `scale(${Number(readerSettings.toolbarScale || 100) / 100})`;
            toolbar.style.transformOrigin = compactChrome ? 'right top' : 'right bottom';
        }
        // The shared glass material is applied through CSS variables on the overlay.
        toolbar.style.background = '';
    }

    if (controls) {
        // 不用 zoom：zoom 会改变 .igs-controls 的实际占位高度并干扰 floating 对话框的 flex 计算，
        // 放大时把输入框/发送按钮挤出对话框。直接按比例设输入框与发送按钮高度（基准 32px）。
        controls.style.zoom = '';
        const inputScale = Number(readerSettings.inputScale || 100) / 100;
        const inputHeight = Math.max(20, Math.round(32 * inputScale));
        const inputEl = controls.querySelector('#igs-input');
        const sendBtn = controls.querySelector('#igs-send-btn');
        if (inputEl) inputEl.style.height = `${inputHeight}px`;
        if (sendBtn) sendBtn.style.height = `${inputHeight}px`;
    }

    if (bg) {
        bg.style.backgroundSize = readerSettings.imgMode === 'contain' ? 'contain' : 'cover';
        const brightness = Number(readerSettings.imgBrightness);
        bg.style.filter = `brightness(${(Number.isFinite(brightness) ? brightness : 88) / 100})`;
    }
    if (bgBlur) {
        bgBlur.style.backgroundSize = readerSettings.imgMode === 'contain' ? 'cover' : 'cover';
    }
}
function applyAlignStyle(element, align) {
    applyAlignStyleImpl(element, align);
}

const STATUS_HUD_PLACEHOLDER = '<svg viewBox="0 0 24 24" width="100%" height="100%" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="9" r="3.4"/><path d="M5.5 20c0-3.6 2.9-6 6.5-6s6.5 2.4 6.5 6"/></svg>';
const STATUS_HUD_TOGGLE_ICON = '<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path class="igs-hud-icon-expand" d="M12 5v14M5 12h14"/></svg>';
const STATUS_HUD_LOCATION_ICON = '<svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 21s7-6.2 7-12a7 7 0 1 0-14 0c0 5.8 7 12 7 12Z"/><circle cx="12" cy="9" r="2.3"/></svg>';

const STATUS_HUD_COLOR_VARS = Object.freeze({
    trust: 'var(--igs-hud-bar-trust,#4ab3da)',
    like: 'var(--igs-hud-bar-like,#ee83b6)',
    know: 'var(--igs-hud-bar-know,#e8a355)',
    'slot-1': 'var(--igs-hud-bar-1,#57aaee)',
    'slot-2': 'var(--igs-hud-bar-2,#55c795)',
    'slot-3': 'var(--igs-hud-bar-3,#eec254)',
    'slot-4': 'var(--igs-hud-bar-4,#b57fe0)',
    'slot-5': 'var(--igs-hud-bar-5,#ea8a8a)',
    'slot-6': 'var(--igs-hud-bar-6,#8896ea)',
});

const STATUS_HUD_RADIUS = Object.freeze({ square: 0, soft: 6, small: 10, medium: 16, large: 24, circle: '50%' });

function findStatusHudHost(root) {
    if (!root) return null;
    const doc = root.ownerDocument;
    if (doc && typeof doc.getElementById === 'function') {
        const byId = doc.getElementById('igs-status-hud');
        if (byId) return byId;
    }
    return typeof root.querySelector === 'function' ? root.querySelector('#igs-status-hud') : null;
}
function applyStatusHudToDom(root, snapshot) {
    const host = findStatusHudHost(root);
    if (!host) return;
    const hud = snapshot && snapshot.content && snapshot.content.statusHud;
    const doc = host.ownerDocument;
    const radius = STATUS_HUD_RADIUS[hud && hud.avatarRadius] != null ? STATUS_HUD_RADIUS[hud && hud.avatarRadius] : '50%';
    const scale = snapshot && snapshot.readerSettings && snapshot._statusHudScale;
    host.style.setProperty('--igs-hud-scale', String(Number(scale) > 0 ? Number(scale) : 1));
    host.className = '';
    const hasEmotion = Boolean(hud && hud.emotion);
    const hasLocation = Boolean(hud && hud.location);
    const hasMetrics = Boolean(hud && Array.isArray(hud.metrics) && hud.metrics.length);
    const grayscaleBars = Boolean(hud && hud.barColor === 'grayscale');
    const shouldShow = Boolean(hud && hud.enabled && (hud.character || hasLocation));
    if (globalThis.__IGS_HUD_DEBUG__) {
        console.log('[HUD-PROBE]', JSON.stringify({ hud: hud ? { enabled: hud.enabled, character: hud.character, emotion: hud.emotion, location: hud.location, avatar: Boolean(hud.avatar), metrics: hud.metrics && hud.metrics.length } : null, hasEmotion, hasLocation, hasMetrics, shouldShow }));
    }
    if (!shouldShow) {
        host.setAttribute('hidden', '');
        while (host.firstChild) host.removeChild(host.firstChild);
        return;
    }
    host.removeAttribute('hidden');
    // 背景只在有 HUD 条时出现：只有头像/情绪/地点栏时不加背景，即使背景开关开着。
    if (hud.background === 'dialog' && hasMetrics) host.classList.add('igs-hud-bg-dialog');
    if (root.classList && root.classList.contains('igs-options-visible')) host.classList.add('igs-hud-suppressed');
    const hudSettings = snapshot && snapshot.readerSettings && snapshot.readerSettings.statusHud;
    if (hudSettings && hudSettings.collapsed) host.classList.add('igs-hud-collapsed');
    while (host.firstChild) host.removeChild(host.firstChild);

    const identity = doc.createElement('div');
    identity.className = 'igs-hud-identity';
    if (hud.character) {
        if (hud.avatar) {
            const img = doc.createElement('img');
            img.className = 'igs-hud-avatar';
            img.setAttribute('src', hud.avatar);
            img.setAttribute('alt', '');
            img.style.borderRadius = typeof radius === 'number' ? `calc(${radius}px * var(--igs-hud-scale,1))` : radius;
            identity.appendChild(img);
        } else {
            const placeholder = doc.createElement('div');
            placeholder.className = 'igs-hud-avatar igs-hud-avatar-empty';
            placeholder.innerHTML = STATUS_HUD_PLACEHOLDER;
            placeholder.style.borderRadius = typeof radius === 'number' ? `calc(${radius}px * var(--igs-hud-scale,1))` : radius;
            identity.appendChild(placeholder);
        }
    }
    if (hasEmotion) {
        const chip = doc.createElement('span');
        chip.className = 'igs-hud-emotion';
        chip.textContent = hud.emotion;
        identity.appendChild(chip);
    }
    if (hasLocation) {
        const location = doc.createElement('div');
        location.className = 'igs-hud-location';
        const icon = doc.createElement('span');
        icon.className = 'igs-hud-location-icon';
        icon.innerHTML = STATUS_HUD_LOCATION_ICON;
        location.appendChild(icon);
        const chip = doc.createElement('span');
        chip.className = 'igs-hud-location-label';
        const context = [hud.weather, hud.time].filter(Boolean).join(' · ');
        chip.textContent = hud.showLocationDetails && context
            ? `${context} の ${hud.location}`
            : hud.location;
        location.appendChild(chip);
        identity.appendChild(location);
    }
    host.appendChild(identity);

    const metrics = doc.createElement('div');
    metrics.className = 'igs-hud-metrics';
    const rows = hasMetrics ? hud.metrics.slice(0, 4) : [];
    for (const metric of rows) {
        const row = doc.createElement('div');
        row.className = 'igs-hud-metric';
        const label = doc.createElement('span');
        label.className = 'igs-hud-metric-label';
        label.textContent = metric.label;
        const track = doc.createElement('div');
        track.className = 'igs-hud-track';
        const fill = doc.createElement('div');
        fill.className = 'igs-hud-fill';
        const color = STATUS_HUD_COLOR_VARS[metric.colorKey] || STATUS_HUD_COLOR_VARS['slot-1'];
        fill.style.backgroundImage = grayscaleBars
            ? 'linear-gradient(90deg, rgba(255,255,255,.46), rgba(255,255,255,.86))'
            : `linear-gradient(90deg, color-mix(in srgb, ${color} 42%, #ffffff), ${color})`;
        fill.style.width = `${Math.max(0, Math.min(100, Number(metric.percent) || 0))}%`;
        track.appendChild(fill);
        const value = doc.createElement('span');
        value.className = 'igs-hud-metric-value';
        value.textContent = metric.display;
        row.appendChild(label);
        row.appendChild(track);
        row.appendChild(value);
        metrics.appendChild(row);
    }
    host.appendChild(metrics);
    const toggle = doc.createElement('button');
    toggle.className = 'igs-icon-btn igs-hud-toggle';
    toggle.setAttribute('data-act', 'toggle-status-hud');
    toggle.setAttribute('title', '折叠/展开状态栏');
    toggle.setAttribute('type', 'button');
    toggle.innerHTML = STATUS_HUD_TOGGLE_ICON;
    host.appendChild(toggle);
    if (hasMetrics && Number(hud.hiddenCount) > 0) {
        const overflow = doc.createElement('span');
        overflow.className = 'igs-hud-overflow';
        overflow.textContent = `+${Number(hud.hiddenCount)}`;
        host.appendChild(overflow);
    }
}
function applyStatusHudScale(root, snapshot) {
    const host = findStatusHudHost(root);
    if (!host) return;
    const settings = snapshot && snapshot.readerSettings;
    const hudSettings = settings && settings.statusHud;
    if (!hudSettings || !hudSettings.enabled) return;
    const overlayWidth = readElementWidth(root, 0);
    const overlayHeight = readElementHeight(root, 0);
    const scale = resolveStatusHudScale(hudSettings.size, overlayWidth, overlayHeight);
    snapshot._statusHudScale = scale;
    host.style.setProperty('--igs-hud-scale', String(scale));
    const locationScale = resolveStatusHudLocationScale(hudSettings.size);
    snapshot._statusHudLocationScale = locationScale;
    host.style.setProperty('--igs-hud-location-scale', String(locationScale));
    // 地点栏按档位微调垂直位置：小档下移 9px，中档下移 3.5px，大档保持。
    const locationOffset = hudSettings.size === 'small' ? 9 : hudSettings.size === 'medium' ? 3.5 : 0;


    host.style.setProperty('--igs-hud-location-offset', `${locationOffset}px`);
}

function applyAlignStyleImpl(element, align) {
    if (!element) return;
    // align 取值：left / center / indent（首行缩进2字符）。indent 等价左对齐 + text-indent:2em。
    if (align === 'center') {
        element.style.textAlign = 'center';
        element.style.textIndent = '';
    } else if (align === 'indent') {
        element.style.textAlign = 'left';
        element.style.textIndent = '2em';
    } else if (align === 'left') {
        element.style.textAlign = 'left';
        element.style.textIndent = '';
    } else {
        element.style.textAlign = '';
        element.style.textIndent = '';
    }
}
function applyReaderSnapshotToDom(root, snapshot, current, ctx = {}) {
    root.className = snapshot.classes.join(' ');
    root.setAttribute('data-igs-igs-ui', 'true');

    const bg = root.querySelector('#igs-bg');
    const bgBlur = root.querySelector('#igs-bg-blur');
    const textEl = root.querySelector('#igs-text');
    const input = root.querySelector('#igs-input');
    const send = root.querySelector('#igs-send-btn');
    const dialog = root.querySelector('#igs-dialog');
    const classicDialog = isClassicDialogSkin(snapshot.readerSettings);
    if (root.classList) {
        root.classList.toggle('igs-default-reader-chrome', !classicDialog);
    }
    const toolbar = root.querySelector('#igs-ctrl-bar');
    const clickLayer = root.querySelector('#igs-click-layer');
    const toast = root.querySelector('#igs-toast');
    const segmentsLen = snapshot.content && Array.isArray(snapshot.content.segments) ? snapshot.content.segments.length : 0;
    const isLastPage = segmentsLen <= 0 || (snapshot.content && snapshot.content.currentIndex >= segmentsLen - 1);
    const resolveAssetUrl = typeof ctx.resolveAssetUrl === 'function'
        ? ctx.resolveAssetUrl
        : (url) => String(url || '').trim();
    if (root.classList) {
        root.classList.toggle('igs-scene-nsfw', snapshot.content.sceneNsfw === true);
    }
    // NSFW 黑幕强度：档位写入 CSS 变量驱动 veil 与背景亮度；非 NSFW 场景清除，回落 CSS 内默认值。
    const nsfwVeilLevel = snapshot.content.sceneNsfw === true
        ? (((snapshot.readerSettings || {}).statusHud) || {}).nsfwVeilLevel
        : '';
    const nsfwVeilStyle = snapshot.content.sceneNsfw === true
        ? (NSFW_VEIL_LEVEL_STYLE[nsfwVeilLevel] || NSFW_VEIL_LEVEL_STYLE.medium)
        : null;
    if (root.style && typeof root.style.setProperty === 'function') {
        const nsfwVeilPairs = [['--igs-nsfw-veil-center', 'center'], ['--igs-nsfw-veil-edge', 'edge'], ['--igs-nsfw-bg-brightness', 'brightness']];
        for (const [prop, key] of nsfwVeilPairs) {
            if (nsfwVeilStyle) root.style.setProperty(prop, nsfwVeilStyle[key]);
            else if (typeof root.style.removeProperty === 'function') root.style.removeProperty(prop);
        }
    }
    const backgroundAssetUrl = resolveAssetUrl(snapshot.content.backgroundImage);

    if (bg && backgroundAssetUrl) {
        bg.style.backgroundImage = `url("${backgroundAssetUrl.replace(/"/g, '&quot;')}")`;
        bg.setAttribute('data-igs-has-image', '1');
        removeImageLoadingSpinner(bg);
        removeImageEmptyPlaceholder(bg);
    } else if (bg) {
        bg.style.backgroundImage = '';
        bg.removeAttribute('data-igs-has-image');
        const expectsImage = snapshot.content.imageExpectedCount > 0
            && snapshot.content.imageBoundCount < snapshot.content.imageExpectedCount;
        const sceneAssetLoading = Boolean(snapshot.content.backgroundImage) && !backgroundAssetUrl;
        if (sceneAssetLoading) {
            removeImageEmptyPlaceholder(bg);
            ensureImageLoadingSpinner(bg);
        } else if (expectsImage && snapshot.content.imageLoading) {
            removeImageEmptyPlaceholder(bg);
            ensureImageLoadingSpinner(bg);
        } else if (expectsImage) {
            ensureImageEmptyPlaceholder(bg, '图片未生成');
        } else {
            removeImageLoadingSpinner(bg);
            removeImageEmptyPlaceholder(bg);
        }
    }
    if (bgBlur && backgroundAssetUrl) {
        bgBlur.style.backgroundImage = `url("${backgroundAssetUrl.replace(/"/g, '&quot;')}")`;
        bgBlur.style.opacity = '0.72';
    } else if (bgBlur) {
        bgBlur.style.backgroundImage = '';
        bgBlur.style.opacity = '0';
    }
    const spriteEl = root.querySelector('#igs-sprite');
    const spriteSettings = (snapshot.readerSettings && snapshot.readerSettings.statusHud) || {};
    const hideSpriteNsfw = snapshot.content.sceneNsfw === true && spriteSettings.showSpriteOnNsfw === false;
    const spriteAssetUrl = hideSpriteNsfw
        ? null
        : resolveAssetUrl(snapshot.content.spriteImage);
    if (spriteEl && spriteAssetUrl) {
        const spriteNarration = snapshot.content.textType === 'narration' && spriteSettings.dimSpriteOnNarration !== false;
        const spriteFilter = spriteNarration ? 'brightness(0.86) saturate(0.86)' : '';
        spriteEl.classList.toggle('igs-sprite-narration', spriteNarration);
        spriteEl.style.backgroundImage = `url("${spriteAssetUrl.replace(/"/g, '&quot;')}")`;
        spriteEl.style.display = 'block';
        spriteEl.style.position = 'absolute';
        spriteEl.style.inset = '0';
        spriteEl.style.width = '100%';
        spriteEl.style.height = '100%';
        spriteEl.style.transform = 'none';
        spriteEl.style.bottom = 'auto';
        spriteEl.style.left = 'auto';
        spriteEl.style.filter = spriteFilter;
        spriteEl.style.setProperty('-webkit-filter', spriteFilter);
        if (!current.spriteEditMode) {
            const spriteKey = snapshot.content.spriteCharacter || snapshot.content.speaker;
            const spriteMood = snapshot.content.spriteMood || '';
            const layout = resolveSpriteLayout(snapshot.readerSettings.spriteLayouts, snapshot.mode, spriteKey, spriteMood);
            spriteEl.style.backgroundSize = `${layout.scale}%`;
            spriteEl.style.backgroundPosition = `${layout.posX}% ${layout.posY}%`;
            igsDebug('[DEBUG-sprite] apply-layout', { mode: snapshot.mode, speaker: spriteKey, mood: spriteMood, index: snapshot.content.currentIndex, layoutKey: spriteKey ? `${snapshot.mode}::${spriteKey}::${spriteMood}` : snapshot.mode, layout: { ...layout } });
        }
    } else if (spriteEl) {
        spriteEl.classList.remove('igs-sprite-narration');
        spriteEl.style.backgroundImage = '';
        spriteEl.style.display = 'none';
        spriteEl.style.filter = '';
        spriteEl.style.setProperty('-webkit-filter', '');
    }
    if (textEl) {
        const theme = resolveActiveTheme(snapshot);
        const sceneAssetsEnabled = snapshot.readerSettings._sceneAssets && snapshot.readerSettings._sceneAssets.enabled;
        const textType = snapshot.content.textType || 'narration';
        textEl.innerHTML = renderDialogueHtml(snapshot.content.displayText, theme, sceneAssetsEnabled);
        textEl.style.fontSize = `${snapshot.readerSettings.fontSize}px`;
        textEl.style.lineHeight = computeLineHeight(snapshot.readerSettings.fontSize);
        textEl.style.marginTop = '';
        const isNarration = textType === 'narration';
        const isThought = textType === 'thought';
        const segFont = isThought ? theme.thoughtFont : isNarration ? theme.narrationFont : theme.textFont;
        const segColor = isThought ? theme.thoughtColor : isNarration ? theme.narrationColor : theme.textColor;
        const segAlign = isThought ? theme.thoughtAlign : isNarration ? theme.narrationAlign : theme.textAlign;
        const themeEnabled = sceneAssetsEnabled || classicDialog;
        applyAlignStyle(textEl, themeEnabled ? segAlign : '');
        if (themeEnabled && segFont && segFont !== 'inherit') {
            textEl.style.fontFamily = segFont;
        } else {
            textEl.style.fontFamily = '';
        }
        if (themeEnabled && segColor) {
            textEl.style.color = segColor;
        } else {
            textEl.style.color = '';
        }
    }
    const speakerEl = root.querySelector('#igs-speaker');
    if (speakerEl) {
        const theme = resolveActiveTheme(snapshot);
        const sceneAssetsEnabled = snapshot.readerSettings._sceneAssets && snapshot.readerSettings._sceneAssets.enabled;
        if (sceneAssetsEnabled && snapshot.content.speaker) {
            speakerEl.textContent = snapshot.content.speaker;
            speakerEl.style.display = 'block';
            applyAlignStyle(speakerEl, theme.nameAlign);
            speakerEl.style.fontFamily = theme.nameFont && theme.nameFont !== 'inherit' ? theme.nameFont : '';
            speakerEl.style.color = theme.nameColor || '';
        } else {
            speakerEl.style.display = 'none';
        }
    }
    const dividerEl = root.querySelector('#igs-divider');
    if (dividerEl) {
        const theme = resolveActiveTheme(snapshot);
        const sceneAssetsEnabled = snapshot.readerSettings._sceneAssets && snapshot.readerSettings._sceneAssets.enabled;
        if (!classicDialog && sceneAssetsEnabled && snapshot.content.speaker && theme.dividerSymbol !== 'none') {
            if (theme.dividerSymbol === 'gradient') {
                dividerEl.textContent = '';
                dividerEl.style.display = 'block';
                dividerEl.style.height = '1px';
                dividerEl.style.background = `linear-gradient(90deg, transparent, ${theme.dividerColor || 'rgba(255,255,255,.15)'}, transparent)`;
                dividerEl.style.color = '';
            } else {
                dividerEl.textContent = theme.dividerSymbol;
                dividerEl.style.display = 'block';
                dividerEl.style.height = '';
                dividerEl.style.background = '';
                dividerEl.style.color = theme.dividerColor || '';
            }
        } else {
            dividerEl.style.display = 'none';
        }
    }
    const statusLine = root.querySelector('#igs-status-line');
    if (statusLine) {
        // 居中显示在气泡底部；最后一页不显示；仍受「显示状态行」开关控制。
        if (snapshot.readerSettings.showStatusLine && !isLastPage) {
            statusLine.textContent = snapshot.content.progress;
            statusLine.style.display = 'block';
        } else {
            statusLine.style.display = 'none';
        }
    }
    const controls = root.querySelector('.igs-controls');
    if (controls) {
        // 内嵌模式使用酒馆默认输入框；其它模式的 IGS 输入区只在最后一页显示。
        controls.style.display = snapshot.mode === 'embedded' ? 'none' : (isLastPage ? '' : 'none');
    }
    applyStatusHudToDom(root, snapshot);
    applyStatusHudScale(root, snapshot);
    if (dialog) {
        applyDialogSkinAssets(dialog, snapshot.readerSettings);
        const sceneAssetsEnabled = snapshot.readerSettings._sceneAssets && snapshot.readerSettings._sceneAssets.enabled;
        if (classicDialog && sceneAssetsEnabled && snapshot.content.speaker) {
            dialog.setAttribute('data-igs-has-speaker', '1');
        } else {
            dialog.removeAttribute('data-igs-has-speaker');
        }
        if (!classicDialog && !snapshot.content.speaker) {
            dialog.setAttribute('data-igs-narration', '1');
        } else {
            dialog.removeAttribute('data-igs-narration');
        }
        dialog.style.paddingTop = '';
    }
    if (input) {
        input.placeholder = snapshot.input.placeholder;
        input.value = current.inputValue;
    }
    if (send && !(send.dataset && send.dataset.igsBound)) {
        if (send.dataset) send.dataset.igsBound = '1';
        send.addEventListener('click', async () => {
            await current.controller.submit(input ? input.value : current.inputValue);
        });
    }
    if (clickLayer && !(clickLayer.dataset && clickLayer.dataset.igsBound)) {
        if (clickLayer.dataset) clickLayer.dataset.igsBound = '1';
        clickLayer.addEventListener('click', () => {
            if (current.dragSuppressClick || (current.runtime && current.runtime.dragSuppressClick)) {
                current.dragSuppressClick = false;
                if (current.runtime) current.runtime.dragSuppressClick = false;
                return;
            }
            if (current.hidden) {
                current.controller.toggleHidden();
                return;
            }
            if (ctx.hasActiveSettings && ctx.hasActiveSettings()) {
                if (typeof ctx.closeSettings === 'function') ctx.closeSettings();
                return;
            }
            // 最后一页且启用选项气泡时，点击空白处切换气泡显隐（消费本次点击，不翻页）。
            if (typeof ctx.handleBlankClick === 'function' && ctx.handleBlankClick()) return;
        });
    }
    if (dialog && !(dialog.dataset && dialog.dataset.igsBound)) {
        if (dialog.dataset) dialog.dataset.igsBound = '1';
        dialog.addEventListener('click', (event) => {
            if (current.hidden) return;
            if (event.target && event.target.closest && (
                event.target.closest('.igs-controls')
                || event.target.closest('#igs-ctrl-bar')
                || event.target.closest('#igs-settings')
            )) {
                return;
            }
            const rect = typeof dialog.getBoundingClientRect === 'function'
                ? dialog.getBoundingClientRect()
                : { left: 0, width: 0 };
            const clientX = Number(event.clientX);
            if (!Number.isFinite(clientX) || clientX < rect.left + rect.width / 2) {
                if (typeof ctx.handleReaderAction === 'function') ctx.handleReaderAction('prev');
            } else {
                if (typeof ctx.handleReaderAction === 'function') ctx.handleReaderAction('next');
            }
        });
    }
    if (dialog) {
        dialog.classList.toggle('igs-hidden', current.hidden);
    }
    if (toolbar) {
        toolbar.classList.toggle('igs-hidden', current.hidden);
    }
    applyToolbarState(root, current);
    applyReaderSettingsToDom(root, snapshot, current, { dialog, textEl, toolbar });
    applyReaderModeRuntime(root, snapshot, current, {
        isActiveReader: (reader) => (typeof ctx.isActiveReader === 'function' ? ctx.isActiveReader(reader) : true),
        requestClose: () => { if (typeof ctx.closeReader === 'function') ctx.closeReader(); },
    });
    if (toast) {
        toast.textContent = current.toastMessage || '';
        toast.style.opacity = current.toastMessage ? '1' : '0';
    }
}

__igsDefine(exports, "createReaderButton", () => createReaderButton);
__igsDefine(exports, "normalizeReaderStableLayers", () => normalizeReaderStableLayers);
__igsDefine(exports, "buildFallbackReaderOverlay", () => buildFallbackReaderOverlay);
__igsDefine(exports, "buildFallbackSettingsOverlay", () => buildFallbackSettingsOverlay);
__igsDefine(exports, "applyToolbarState", () => applyToolbarState);
__igsDefine(exports, "applyReaderSettingsToDom", () => applyReaderSettingsToDom);
__igsDefine(exports, "applyAlignStyle", () => applyAlignStyle);
__igsDefine(exports, "applyStatusHudToDom", () => applyStatusHudToDom);
__igsDefine(exports, "applyStatusHudScale", () => applyStatusHudScale);
__igsDefine(exports, "applyReaderSnapshotToDom", () => applyReaderSnapshotToDom);
});
__igsRegister("src/core/event-bus.js", function(module, exports, require) {
function createEventBus() {
    const listeners = new Map();

    return {
        on(type, handler) {
            if (!listeners.has(type)) listeners.set(type, new Set());
            listeners.get(type).add(handler);
            return () => listeners.get(type).delete(handler);
        },

        emit(type, payload) {
            for (const handler of listeners.get(type) || []) {
                handler(payload);
            }
        },

        clear() {
            listeners.clear();
        },
    };
}

__igsDefine(exports, "createEventBus", () => createEventBus);
});
__igsRegister("src/host/magic-wand-entry.js", function(module, exports, require) {
const DEFAULT_MENU_SELECTORS = Object.freeze([
    '#extensionsMenu',
    '#extensions_menu',
    '.extensions_block .list-group',
]);

const ENTRY_SELECTOR = '[data-igs-magic-entry="1"]';
function createMagicWandEntry(options = {}) {
    const globalObject = options.global || globalThis.window || globalThis;
    const label = options.label || '沉浸式Galgame系统';
    const version = String(options.version || '');
    const menuSelectors = options.menuSelectors || DEFAULT_MENU_SELECTORS;
    const retryIntervalMs = options.retryIntervalMs === false
        ? 0
        : normalizePositiveNumber(options.retryIntervalMs, 5000);
    const open = typeof options.open === 'function' ? options.open : null;
    const resolveMode = typeof options.resolveMode === 'function' ? options.resolveMode : () => 'pc';
    const notify = typeof options.notify === 'function' ? options.notify : defaultNotify;

    let observer = null;
    let retryTimer = null;
    let delegatedDocs = [];
    let attached = false;
    let lastEnsureResult = { ok: false, reason: 'not-attached', menus: 0, entries: 0 };

    return {
        attach,
        ensure,
        destroy,
        getState,
    };

    function attach() {
        if (attached) return ensure();
        attached = true;
        const result = ensure();
        attachObserver();
        attachDelegatedClicks();
        attachRetryTimer();
        return result;
    }

    function ensure() {
        const menus = findMagicWandMenus();
        if (!menus.length) {
            lastEnsureResult = { ok: false, reason: 'menu-not-found', menus: 0, entries: 0 };
            return lastEnsureResult;
        }

        const menuElements = menus.map((item) => item.menu);
        for (const candidateDoc of getCandidateDocuments()) {
            cleanupMagicEntries(candidateDoc, menuElements);
        }

        let entries = 0;
        for (const found of menus) {
            const existing = found.menu.querySelector(ENTRY_SELECTOR);
            if (existing) {
                existing.removeEventListener('click', handleMagicEntryClick);
                existing.addEventListener('click', handleMagicEntryClick);
                entries += 1;
                continue;
            }
            const usePrimaryId = !found.doc.getElementById('igs-magic-entry-btn');
            found.menu.appendChild(createMagicEntryButton(found.doc, usePrimaryId));
            entries += 1;
        }

        lastEnsureResult = { ok: true, menus: menus.length, entries };
        return lastEnsureResult;
    }

    function destroy() {
        attached = false;
        if (observer) {
            observer.disconnect();
            observer = null;
        }
        if (retryTimer) {
            clearHostInterval(retryTimer);
            retryTimer = null;
        }
        for (const doc of delegatedDocs) {
            try {
                doc.removeEventListener('click', handleMagicEntryDelegatedClick, true);
            } catch (error) {
                // Ignore detached documents.
            }
        }
        delegatedDocs = [];
        for (const candidateDoc of getCandidateDocuments()) {
            cleanupMagicEntries(candidateDoc, []);
        }
        lastEnsureResult = { ok: true, reason: 'destroyed', menus: 0, entries: 0 };
        return lastEnsureResult;
    }

    function getState() {
        return {
            attached,
            selector: ENTRY_SELECTOR,
            label,
            version,
            lastEnsureResult: { ...lastEnsureResult },
        };
    }

    function findMagicWandMenus() {
        const found = [];
        for (const doc of getCandidateDocuments()) {
            for (const selector of menuSelectors) {
                safeQueryAll(doc, selector).forEach((menu) => {
                    if (menu && !found.some((item) => item.menu === menu)) {
                        found.push({ doc, menu });
                    }
                });
            }
        }
        return found;
    }

    function createMagicEntryButton(doc, usePrimaryId) {
        const button = doc.createElement('a');
        if (usePrimaryId) button.id = 'igs-magic-entry-btn';
        button.className = 'list-group-item igs-magic-entry';
        button.href = 'javascript:void(0)';
        button.setAttribute('data-igs-magic-entry', '1');
        button.setAttribute('data-igs-version', version);
        button.setAttribute('title', `打开${label}`);
        button.setAttribute('aria-label', `打开${label}`);
        button.innerHTML = `<span class="fa-solid fa-book-open" aria-hidden="true"></span> ${escapeHtml(label)}`;
        button.addEventListener('click', handleMagicEntryClick);
        return button;
    }

    function handleMagicEntryClick(event, forcedCurrentTarget) {
        if (event) {
            event.preventDefault();
            event.stopPropagation();
        }
        const mode = resolveSafeMode();
        if (!open) {
            notify('IGS 入口尚未绑定打开函数。', 'error');
            return { ok: false, reason: 'missing-open-handler' };
        }

        const result = open(mode);
        Promise.resolve(result).then((resolved) => {
            if (!resolved || resolved.ok === false) {
                notify(`IGS 阅读器打开失败：${resolveErrorMessage(resolved)}`, 'error');
            }
        }).catch((error) => {
            notify(`IGS 阅读器打开失败：${error && error.message || String(error)}`, 'error');
        });

        closeMagicWandMenu(forcedCurrentTarget || event && event.currentTarget);
        return result;
    }

    function handleMagicEntryDelegatedClick(event) {
        const target = event && event.target && event.target.closest
            ? event.target.closest(ENTRY_SELECTOR)
            : null;
        if (!target) return;
        handleMagicEntryClick(event, target);
    }

    function attachObserver() {
        if (observer) return;
        const MutationObserverCtor = resolveMutationObserver();
        if (!MutationObserverCtor) return;
        observer = new MutationObserverCtor(() => {
            ensure();
        });
        for (const doc of getCandidateDocuments()) {
            if (doc && doc.body) {
                observer.observe(doc.body, { childList: true, subtree: true });
            }
        }
    }

    function attachDelegatedClicks() {
        for (const doc of getCandidateDocuments()) {
            if (!doc || delegatedDocs.includes(doc)) continue;
            doc.addEventListener('click', handleMagicEntryDelegatedClick, true);
            delegatedDocs.push(doc);
        }
    }

    function attachRetryTimer() {
        if (retryTimer || !retryIntervalMs) return;
        retryTimer = setHostInterval(() => {
            ensure();
        }, retryIntervalMs);
    }

    function getCandidateDocuments() {
        const docs = [];
        const addDoc = (doc) => {
            if (doc && !docs.includes(doc)) docs.push(doc);
        };

        addDoc(options.document);
        addDoc(safeDocument(globalObject));
        try {
            addDoc(safeDocument(globalObject.top));
        } catch (error) {
            // Cross-origin top windows are ignored.
        }
        try {
            let cursor = globalObject;
            while (cursor && cursor.parent && cursor.parent !== cursor) {
                cursor = cursor.parent;
                addDoc(safeDocument(cursor));
            }
        } catch (error) {
            // Cross-origin parent windows are ignored.
        }
        if (typeof document !== 'undefined') addDoc(document);
        return docs;
    }

    function closeMagicWandMenu(currentTarget) {
        try {
            const menu = currentTarget && currentTarget.closest
                ? currentTarget.closest(menuSelectors.join(', '))
                : null;
            if (menu && menu.parentElement && typeof menu.parentElement.click === 'function') {
                menu.parentElement.click();
            }
        } catch (error) {
            // Menu close is best-effort only.
        }
    }

    function resolveSafeMode() {
        try {
            return resolveMode() || 'pc';
        } catch (error) {
            return 'pc';
        }
    }

    function resolveMutationObserver() {
        return options.MutationObserver
            || globalObject.MutationObserver
            || safeDocument(globalObject) && safeDocument(globalObject).defaultView && safeDocument(globalObject).defaultView.MutationObserver
            || globalThis.MutationObserver
            || null;
    }

    function setHostInterval(callback, ms) {
        const setter = globalObject && typeof globalObject.setInterval === 'function'
            ? globalObject.setInterval.bind(globalObject)
            : globalThis.setInterval;
        return setter(callback, ms);
    }

    function clearHostInterval(timer) {
        const clearer = globalObject && typeof globalObject.clearInterval === 'function'
            ? globalObject.clearInterval.bind(globalObject)
            : globalThis.clearInterval;
        clearer(timer);
    }

    function cleanupMagicEntries(candidateDoc, menuElements) {
        safeQueryAll(candidateDoc, ENTRY_SELECTOR).forEach((button) => {
            const shouldRemove = !menuElements.includes(button.parentNode)
                || !isMagicEntryListItem(button)
                || button.getAttribute('data-igs-version') !== version;
            if (shouldRemove) {
                button.removeEventListener('click', handleMagicEntryClick);
                button.remove();
            }
        });
    }
}

function safeDocument(target) {
    try {
        return target && target.document || null;
    } catch (error) {
        return null;
    }
}

function safeQueryAll(doc, selector) {
    try {
        return doc && typeof doc.querySelectorAll === 'function'
            ? Array.from(doc.querySelectorAll(selector))
            : [];
    } catch (error) {
        return [];
    }
}

function isMagicEntryListItem(element) {
    return Boolean(
        element
        && element.tagName === 'A'
        && element.classList
        && element.classList.contains('list-group-item'),
    );
}

function normalizePositiveNumber(value, fallback) {
    const number = Number(value);
    return Number.isFinite(number) && number > 0 ? number : fallback;
}

function resolveErrorMessage(result) {
    if (!result) return 'no-result';
    return result.reason
        || result.error
        || result.message
        || result.diagnosis && result.diagnosis.error
        || 'unknown';
}

function escapeHtml(value) {
    return String(value || '').replace(/[&<>"']/g, (char) => ({
        '&': '&amp;',
        '<': '&lt;',
        '>': '&gt;',
        '"': '&quot;',
        "'": '&#39;',
    })[char]);
}

function defaultNotify(message, type = 'info') {
    const root = globalThis.window || globalThis;
    if (root.toastr && typeof root.toastr[type] === 'function') {
        root.toastr[type](message, 'IGS');
        return;
    }
    const logger = type === 'error' ? console.error : console.info;
    logger('[IGS]', message);
}

__igsDefine(exports, "createMagicWandEntry", () => createMagicWandEntry);
});
__igsRegister("src/host/extension-panel.js", function(module, exports, require) {
// 扩展菜单注册入口：在 SillyTavern 扩展设置面板挂一个 inline-drawer 抽屉。
// 抽屉内含：启用魔法棒入口 + 打开设置 / 打开阅读器 快捷按钮。
// 依据：扩展设置面板锚点 #extensions_settings2 等，inline-drawer 结构。
const PANEL_ID = 'igs-extension-panel';
const MOUNT_SELECTORS = Object.freeze([
    '#extensions_settings2',
    '#extensions_settings',
    '#third_party_extension_settings',
    '.extensions_settings',
    '#rm_extensions_block .inline-drawer-content',
]);
function createExtensionPanel(options = {}) {
    const globalObject = options.global || globalThis.window || globalThis;
    const label = options.label || '沉浸式Galgame系统';
    const openSettings = typeof options.openSettings === 'function' ? options.openSettings : null;
    const openReader = typeof options.openReader === 'function' ? options.openReader : null;
    const getEntryConfig = typeof options.getEntryConfig === 'function' ? options.getEntryConfig : () => ({ magic: true });
    const setEntryConfig = typeof options.setEntryConfig === 'function' ? options.setEntryConfig : () => {};

    let retryTimer = null;
    let attached = false;

    return { attach, destroy, refresh, getState };

    function attach() {
        attached = true;
        ensurePanel();
        return { ok: true };
    }

    function refresh() {
        if (!attached) return { ok: false, reason: 'not-attached' };
        const panel = getDoc() && getDoc().getElementById(PANEL_ID);
        if (panel) renderPanel(panel);
        return { ok: true };
    }

    function destroy() {
        attached = false;
        if (retryTimer) { clearHostTimeout(retryTimer); retryTimer = null; }
        const doc = getDoc();
        const panel = doc && doc.getElementById(PANEL_ID);
        if (panel && typeof panel.remove === 'function') panel.remove();
        return { ok: true, reason: 'destroyed' };
    }

    function ensurePanel() {
        const doc = getDoc();
        const mount = findMount(doc);
        if (!mount) {
            if (retryTimer) clearHostTimeout(retryTimer);
            retryTimer = setHostTimeout(ensurePanel, 1800);
            return;
        }
        let panel = doc.getElementById(PANEL_ID);
        if (!panel) {
            panel = doc.createElement('div');
            panel.id = PANEL_ID;
            mount.appendChild(panel);
        }
        renderPanel(panel);
    }

    function renderPanel(panel) {
        const cfg = safeEntryConfig();
        panel.innerHTML = `
            <div class="inline-drawer">
              <div class="inline-drawer-toggle inline-drawer-header">
                <b>${escapeHtml(label)}</b>
                <div class="inline-drawer-icon fa-solid fa-circle-chevron-down down"></div>
              </div>
              <div class="inline-drawer-content">
                <label class="checkbox_label"><input type="checkbox" data-igs-entry="magic"${cfg.magic ? ' checked' : ''}> 启用魔法棒入口</label>
                <div class="igs-ext-actions" style="display:flex;gap:8px;margin-top:8px">
                  <button type="button" class="menu_button" data-igs-ext-act="open-settings" style="flex:1;white-space:nowrap">打开设置</button>
                  <button type="button" class="menu_button" data-igs-ext-act="open-reader" style="flex:1;white-space:nowrap">打开阅读器</button>
                </div>
              </div>
            </div>
        `;
        bindPanel(panel);
    }

    function bindPanel(panel) {
        const checks = panel.querySelectorAll('[data-igs-entry]');
        Array.prototype.forEach.call(checks, (input) => {
            input.addEventListener('change', () => {
                const cfg = safeEntryConfig();
                cfg[input.getAttribute('data-igs-entry')] = !!input.checked;
                setEntryConfig({ magic: !!cfg.magic });
            });
        });
        const buttons = panel.querySelectorAll('[data-igs-ext-act]');
        Array.prototype.forEach.call(buttons, (button) => {
            button.addEventListener('click', (event) => {
                if (event) event.preventDefault();
                const act = button.getAttribute('data-igs-ext-act');
                if (act === 'open-settings' && openSettings) openSettings();
                else if (act === 'open-reader' && openReader) openReader();
            });
        });
    }

    function safeEntryConfig() {
        try {
            const cfg = getEntryConfig() || {};
            return { magic: cfg.magic !== false };
        } catch (error) {
            return { magic: true };
        }
    }

    function getState() {
        return { attached, panelId: PANEL_ID };
    }

    function getDoc() {
        try { return globalObject.document || (typeof document !== 'undefined' ? document : null); }
        catch (error) { return null; }
    }

    function findMount(doc) {
        if (!doc || typeof doc.querySelector !== 'function') return null;
        for (const selector of MOUNT_SELECTORS) {
            try {
                const node = doc.querySelector(selector);
                if (node) return node;
            } catch (error) { /* ignore */ }
        }
        return null;
    }

    function setHostTimeout(fn, ms) {
        const setter = globalObject && typeof globalObject.setTimeout === 'function' ? globalObject.setTimeout.bind(globalObject) : setTimeout;
        return setter(fn, ms);
    }

    function clearHostTimeout(timer) {
        const clearer = globalObject && typeof globalObject.clearTimeout === 'function' ? globalObject.clearTimeout.bind(globalObject) : clearTimeout;
        clearer(timer);
    }
}

function escapeHtml(value) {
    return String(value || '').replace(/[&<>"']/g, (char) => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;',
    })[char]);
}

__igsDefine(exports, "createExtensionPanel", () => createExtensionPanel);
});
__igsRegister("src/generated-images/reader-image-service.js", function(module, exports, require) {
const { collectProviderImages, listBuiltinImageProviders } = require("src/generated-images/provider-runtime.js");
const { createMessageImageCache } = require("src/media/message-image-cache.js");
const { detectExternalImageAdapter, getGlobalDetectionRoots, collectDomRegenerateButtonCandidates } = require("src/generated-images/dom-image-candidates.js");
const { ensureMessageImagePlaceholders, getMessageScopedRoots } = require("src/host/tavern-helper-adapter.js");
function createReaderImageService(options = {}) {
    const globalObject = options.global || globalThis.window || globalThis;
    const hostAdapter = options.hostAdapter || null;
    const cache = options.cache || createMessageImageCache();
    const builtinProviders = normalizeProviders(options.providers || listBuiltinImageProviders());
    const imageGenerator = typeof options.imageGenerator === 'function' ? options.imageGenerator : null;
    const fetchFn = typeof options.fetch === 'function' ? options.fetch : null;

    return {
        registerProviders(registry) {
            if (!registry || typeof registry.register !== 'function') return;
            for (const provider of builtinProviders) {
                registry.register(provider);
            }
        },

        async collect(context = {}) {
            const messageId = normalizeMessageId(firstDefined(
                context.messageId,
                context.message && context.message.id,
                context.scene && context.scene.messageId,
            ));
            if (messageId == null) {
                return emptyImageState({ ok: false, reason: 'invalid-message-id' });
            }

            let message = await resolveMessageContext(context, hostAdapter, messageId);
            const unifiedSettings = context.unifiedSettings || {};
            const imageApi = resolveImageApi(unifiedSettings);
            const imageSlots = normalizeImageSlots(firstDefined(
                context.imageSlots,
                context.imageState && context.imageState.slots,
                [],
            ));
            const requireMessageScope = imageSlots.length > 0 || context.requiresMessageScope === true;
            let scope = resolveContextScope(message, globalObject, { requireMessageScope });
            if (requireMessageScope && (!scope.ok || isDetachedElement(message && message.element, globalObject))) {
                const refreshed = await refreshMessageElement(message, hostAdapter, messageId, globalObject);
                if (refreshed) {
                    message = refreshed;
                    scope = resolveContextScope(message, globalObject, { requireMessageScope });
                }
            }
            const placeholderState = imageSlots.length && message && !hasExternalImageSlots(message)
                ? ensureMessageImagePlaceholders(message, imageSlots)
                : null;
            const roots = scope.roots;
            const cachedEntry = context.skipCache ? null : cache.get(messageId);
            if (context.skipCache) cache.clearMessage(messageId);
            const providerImages = await collectProviderImages(buildProviderContext({
                context,
                message,
                unifiedSettings,
                roots,
                scope,
                globalObject,
                fetchFn,
            }), resolveActiveProviders(context.providers, builtinProviders, imageApi));
            const sceneImages = collectSceneImages(context.scene, context.render);
            const cachedImages = cachedEntry && Array.isArray(cachedEntry.images) ? cachedEntry.images : [];
            const preferredImageIndex = resolvePreferredImageIndex(context, imageSlots.length);
            const diagnostics = buildImageDiagnostics(providerImages, placeholderState);
            if (imageSlots.length) {
                const imageState = buildSlottedImageState({
                    messageId,
                    imageSlots,
                    cachedImages,
                    providerImages,
                    sceneImages,
                    preferredImageIndex,
                    preferredFallback: {
                        enabled: context.allowPreferredSlotFallback === true,
                        excludeUrls: context.preferredFallbackExcludeUrls,
                    },
                });
                const stored = cache.remember(messageId, [
                    ...extractStoredSlotImages(imageState.slots),
                    ...imageState.unboundImages,
                ]);
                if (stored.ok === false) return emptyImageState(stored);
                return {
                    ...imageState,
                    signature: String(stored.entry && stored.entry.signature || imageState.signature || ''),
                    scopeKind: scope.kind,
                    scopeOk: scope.ok,
                    reason: scope.reason || imageState.reason || placeholderState && placeholderState.reason || '',
                    diagnostics,
                };
            }
            const images = uniqueImages([
                ...providerImages,
                ...cachedImages,
                ...sceneImages,
            ]);
            const stored = cache.remember(messageId, images);
            if (stored.ok === false) return emptyImageState(stored);
            return buildImageState(stored.entry, context.currentIndex, {
                scopeKind: scope.kind,
                scopeOk: scope.ok,
                reason: scope.reason || placeholderState && placeholderState.reason || '',
                diagnostics,
            });
        },

        async generate(context = {}) {
            const message = await resolveMessageContext(context, hostAdapter, normalizeMessageId(context.messageId));
            const unifiedSettings = context.unifiedSettings || {};
            const imageApi = resolveImageApi(unifiedSettings);
            const scope = resolveContextScope(message, globalObject, {
                requireMessageScope: context.requiresMessageScope === true,
            });
            const roots = scope.roots;
            const request = buildGenerationRequest(context);
            const override = typeof context.imageGenerator === 'function' ? context.imageGenerator : imageGenerator;

            if (typeof override === 'function') {
                try {
                    const generated = await override(request, {
                        ...cloneData(context.generateOptions || {}),
                        message,
                        unifiedSettings,
                        imageApi,
                    });
                    return normalizeGeneratedResult(generated, request);
                } catch (error) {
                    return buildErrorResult(error, 'provider-not-enabled', { request });
                }
            }

            const provider = resolveGenerationProvider(
                resolveActiveProviders(context.providers, builtinProviders, imageApi),
                imageApi,
            );
            if (!provider || typeof provider.generate !== 'function') {
                return { ok: false, reason: 'provider-not-enabled', request };
            }

            try {
                const result = await provider.generate(request, buildProviderContext({
                    context,
                    message,
                    unifiedSettings,
                    roots,
                    globalObject,
                    fetchFn,
                }));
                return normalizeGeneratedResult(result, request);
            } catch (error) {
                return buildErrorResult(error, 'image-generate-failed', { request });
            }
        },

        async fetchModels(context = {}) {
            const unifiedSettings = context.unifiedSettings || context.settings || {};
            const imageApi = resolveImageApi(unifiedSettings);
            const provider = resolveGenerationProvider(
                resolveActiveProviders(context.providers, builtinProviders, imageApi),
                imageApi,
            );
            if (!provider || typeof provider.fetchModels !== 'function') {
                return { ok: false, reason: 'provider-not-enabled' };
            }
            try {
                const scope = resolveContextScope(context.message || null, globalObject, {
                    requireMessageScope: context.requiresMessageScope === true,
                });
                return await provider.fetchModels(context.request || {}, buildProviderContext({
                    context,
                    message: context.message || null,
                    unifiedSettings,
                    roots: scope.roots,
                    scope,
                    globalObject,
                    fetchFn,
                }));
            } catch (error) {
                return buildErrorResult(error, 'fetch-models-failed');
            }
        },

        async test(context = {}) {
            const message = context.message || null;
            const unifiedSettings = context.unifiedSettings || context.settings || {};
            const imageApi = resolveImageApi(unifiedSettings);
            if (imageApi.mode === 'nai') {
                const result = await this.generate({
                    ...context,
                    message,
                    unifiedSettings,
                    prompt: String(context.prompt || 'simple visual novel background, soft light, no text').trim(),
                    request: context.request || {},
                });
                if (result.ok === false) return result;
                return {
                    ok: true,
                    message: '图像 API 真实生成测试成功',
                    url: result.url,
                    providerId: result.providerId,
                };
            }

            const roots = message
                ? resolveContextScope(message, globalObject, {
                    requireMessageScope: false,
                }).roots
                : getGlobalDetectionRoots(globalObject);
            return detectExternalImageAdapter({
                roots,
                imageApi,
                global: globalObject,
            });
        },

        async regenerate(context = {}) {
            const messageId = normalizeMessageId(firstDefined(
                context.messageId,
                context.message && context.message.id,
            ));
            if (messageId == null) {
                return { ok: false, reason: 'invalid-message-id', messageId };
            }

            const imageIndex = resolvePreferredImageIndex(context, countImageSlots(context.imageState));
            const unifiedSettings = context.unifiedSettings || {};
            const imageApi = resolveImageApi(unifiedSettings);
            const currentState = context.imageState || await this.collect({
                ...context,
                currentIndex: imageIndex,
                preferredImageIndex: imageIndex,
                providers: context.providers,
            });

            if (imageApi.mode === 'nai') {
                const generated = await this.generate({
                    ...context,
                    messageId,
                    currentIndex: imageIndex,
                    unifiedSettings,
                    prompt: String(context.prompt || '').trim(),
                    request: context.request || {},
                });
                if (!generated || generated.ok === false || !generated.url) {
                    return {
                        ok: false,
                        reason: generated && generated.reason || 'provider-not-enabled',
                        imageState: currentState,
                    };
                }
                const nextImage = {
                    url: generated.url,
                    providerId: String(generated.providerId || 'vn.provider.nai').trim(),
                    source: String(generated.source || 'generated-image').trim(),
                    filename: String(generated.filename || '').trim(),
                };
                if (Array.isArray(currentState.slots) && currentState.slots.length) {
                    const nextSlots = replaceSlotImageAtIndex(currentState.slots, imageIndex, nextImage);
                    const nextUnbound = Array.isArray(currentState.unboundImages) ? cloneData(currentState.unboundImages) : [];
                    const stored = cache.remember(messageId, [
                        ...extractStoredSlotImages(nextSlots),
                        ...nextUnbound,
                    ]);
                    const nextState = buildSlottedImageState({
                        messageId,
                        imageSlots: nextSlots,
                        cachedImages: extractStoredSlotImages(nextSlots),
                        providerImages: [],
                        sceneImages: [],
                        preferredImageIndex: imageIndex,
                    });
                    return {
                        ok: stored.ok !== false,
                        reason: 'generated-image-updated',
                        imageState: {
                            ...nextState,
                            signature: String(stored.entry && stored.entry.signature || nextState.signature || ''),
                        },
                        url: generated.url,
                    };
                }
                const nextImages = replaceImageAtIndex(currentState.images, imageIndex, nextImage);
                const stored = cache.remember(messageId, nextImages);
                return {
                    ok: stored.ok !== false,
                    reason: 'generated-image-updated',
                    imageState: buildImageState(stored.entry, imageIndex),
                    url: generated.url,
                };
            }

            const button = await resolveRegenerateButton(hostAdapter, context, messageId, imageIndex, currentState);
            if (!button || typeof button.click !== 'function') {
                return { ok: false, reason: 'regen-button-not-found', imageState: currentState };
            }

            button.click();
            const pollIntervalMs = normalizePositiveInteger(imageApi.pollIntervalMs, 2000);
            const pollAttempts = normalizePositiveInteger(imageApi.pollAttempts, 60);
            const baselineSignature = String(currentState.signature || '');
            const baselineUrl = String(currentState.currentUrl || '');
            const baselineUrls = collectImageStateUrls(currentState);
            let lastState = currentState;

            for (let attempt = 0; attempt < pollAttempts; attempt += 1) {
                await wait(pollIntervalMs, globalObject);
                lastState = await this.collect({
                    ...context,
                    currentIndex: imageIndex,
                    preferredImageIndex: imageIndex,
                    allowPreferredSlotFallback: true,
                    preferredFallbackExcludeUrls: baselineUrls,
                    providers: context.providers,
                });
                if (!lastState || lastState.ok === false) continue;
                const nextSignature = String(lastState.signature || '');
                const nextUrl = String(lastState.currentUrl || '');
                if ((nextUrl && nextUrl !== baselineUrl) || (nextSignature && nextSignature !== baselineSignature)) {
                    return {
                        ok: true,
                        reason: 'external-image-updated',
                        imageState: lastState,
                        url: nextUrl,
                    };
                }
            }

            return {
                ok: false,
                reason: 'image-poll-timeout',
                imageState: lastState,
            };
        },

        async save(context = {}) {
            const url = String(firstDefined(context.url, context.currentUrl, '') || '').trim();
            if (!url) return { ok: false, reason: 'no-image-to-save' };
            if (!isDownloadableUrl(url)) {
                return { ok: false, reason: 'unsupported-save-url', url };
            }

            const filename = String(context.filename || buildFilename(
                context.messageId,
                firstDefined(context.imageIndex, context.currentIndex, 0),
                url,
            )).trim();
            const doc = getDocument(globalObject);
            const anchor = doc && typeof doc.createElement === 'function' ? doc.createElement('a') : null;

            if (anchor && doc.body && typeof doc.body.appendChild === 'function' && typeof anchor.click === 'function') {
                anchor.href = url;
                anchor.download = filename;
                doc.body.appendChild(anchor);
                anchor.click();
                if (typeof anchor.remove === 'function') anchor.remove();
            }

            return {
                ok: true,
                url,
                filename,
                downloaded: Boolean(anchor),
            };
        },
    };
}

function collectSceneImages(scene = {}, render = {}) {
    const images = [];
    const generated = scene && scene.generatedImage && scene.generatedImage.value;
    const renderGenerated = render
        && render.stage
        && render.stage.layers
        && render.stage.layers.generated
        && render.stage.layers.generated.resource
        && render.stage.layers.generated.resource.value;
    const background = render
        && render.stage
        && render.stage.layers
        && render.stage.layers.background
        && render.stage.layers.background.resource
        && render.stage.layers.background.resource.url;

    for (const candidate of [
        { url: renderGenerated, providerId: 'vn.scene.generated', source: 'scene-generated' },
        { url: generated, providerId: 'vn.scene.generated', source: 'scene-generated' },
        { url: background, providerId: 'vn.scene.background', source: 'scene-background' },
    ]) {
        const url = String(candidate.url || '').trim();
        if (!url) continue;
        images.push({ ...candidate, url });
    }

    return uniqueImages(images);
}

function buildImageState(entry, currentIndex = 0, extra = {}) {
    if (!entry || typeof entry !== 'object') {
        return emptyImageState({ ok: true, ...extra });
    }
    const images = Array.isArray(entry.images) ? cloneData(entry.images) : [];
    const activeIndex = images.length ? Math.max(0, Math.min(images.length - 1, normalizeIndex(currentIndex))) : 0;
    return {
        ok: true,
        messageId: entry.messageId,
        images,
        slots: [],
        unboundImages: [],
        count: images.length,
        expectedCount: images.length,
        boundCount: images.length,
        unboundCount: 0,
        availableCount: images.length,
        signature: String(entry.signature || ''),
        currentIndex: activeIndex,
        currentUrl: images[activeIndex] ? images[activeIndex].url : '',
        displayUrl: images[activeIndex] ? images[activeIndex].url : '',
        slotUrl: images[activeIndex] ? images[activeIndex].url : '',
        scopeKind: 'legacy-global',
        scopeOk: true,
        diagnostics: buildImageDiagnostics(images),
        ...extra,
    };
}

function emptyImageState(extra = {}) {
    return {
        ok: true,
        messageId: null,
        images: [],
        slots: [],
        unboundImages: [],
        count: 0,
        expectedCount: 0,
        boundCount: 0,
        unboundCount: 0,
        availableCount: 0,
        signature: '',
        currentIndex: 0,
        currentUrl: '',
        displayUrl: '',
        slotUrl: '',
        scopeKind: 'none',
        scopeOk: false,
        diagnostics: buildImageDiagnostics(),
        ...extra,
    };
}

function resolveActiveProviders(explicitProviders, builtinProviders, imageApi = {}) {
    const sourceProviders = Array.isArray(explicitProviders) && explicitProviders.length
        ? normalizeProviders(explicitProviders)
        : builtinProviders;
    const mode = String(imageApi.mode || 'extension').trim();
    const requestedAdapter = String(imageApi.externalAdapter || 'auto').trim().toLowerCase();
    return sourceProviders.filter((provider) => {
        if (!provider || typeof provider !== 'object') return false;
        if (mode === 'nai') {
            return String(provider.providerType || '').trim() === 'nai';
        }
        if (String(provider.providerType || '').trim() === 'nai') {
            return false;
        }
        const adapterKey = String(provider.adapterKey || '').trim().toLowerCase();
        if (!adapterKey) return true;
        if (requestedAdapter === 'auto') return true;
        return adapterKey === requestedAdapter || adapterKey === 'generic';
    });
}

function resolveGenerationProvider(providers, imageApi = {}) {
    const mode = String(imageApi.mode || 'extension').trim();
    if (mode !== 'nai') return null;
    return normalizeProviders(providers).find((provider) => String(provider.providerType || '').trim() === 'nai') || null;
}

function resolveImageApi(unifiedSettings = {}) {
    const bridge = unifiedSettings.bridge && typeof unifiedSettings.bridge === 'object'
        ? unifiedSettings.bridge
        : {};
    const imageApi = unifiedSettings.imageApi && typeof unifiedSettings.imageApi === 'object'
        ? unifiedSettings.imageApi
        : bridge.imageApi && typeof bridge.imageApi === 'object'
            ? bridge.imageApi
            : {};
    return {
        mode: String(imageApi.mode || 'extension').trim() || 'extension',
        ...imageApi,
    };
}

function buildProviderContext({ context, message, unifiedSettings, roots, scope, globalObject, fetchFn }) {
    return {
        ...context,
        message: message || null,
        messageId: firstDefined(context.messageId, message && message.id),
        root: roots[0] || message && message.element || null,
        roots,
        scopePolicy: scope && scope.policy ? cloneData(scope.policy) : buildScopePolicy({ kind: 'legacy-global', ok: true }),
        document: message && message.element && message.element.ownerDocument || context.document || null,
        settings: unifiedSettings,
        unifiedSettings,
        imageApi: resolveImageApi(unifiedSettings),
        global: globalObject,
        fetch: context.fetch || fetchFn || globalObject && globalObject.fetch || globalThis.fetch,
        AbortController: context.AbortController || globalObject && globalObject.AbortController || globalThis.AbortController,
        setTimeout: context.setTimeout || globalObject && globalObject.setTimeout || globalThis.setTimeout,
        clearTimeout: context.clearTimeout || globalObject && globalObject.clearTimeout || globalThis.clearTimeout,
    };
}

function buildGenerationRequest(context = {}) {
    const request = isPlainObject(context.request) ? cloneData(context.request) : {};
    const prompt = String(firstDefined(context.prompt, request.prompt, request.input, '') || '').trim();
    if (prompt && !request.prompt && !request.input) {
        request.prompt = prompt;
    }
    return request;
}

function normalizeGeneratedResult(result, request) {
    if (!result) return { ok: false, reason: 'provider-not-enabled', request };
    if (result.ok === false) return result;
    if (!result.url) return { ok: false, reason: 'provider-not-enabled', request };
    return {
        ok: true,
        ...result,
        providerId: String(result.providerId || '').trim(),
        source: String(result.source || 'generated-image').trim(),
        filename: String(result.filename || '').trim(),
    };
}

function buildErrorResult(error, fallbackReason, extra = {}) {
    return {
        ok: false,
        reason: error && error.message ? error.message : fallbackReason,
        error,
        ...extra,
    };
}

function resolveContextScope(message, globalObject, options = {}) {
    const roots = message ? getMessageScopedRoots(message) : [];
    if (roots.length) {
        return {
            roots,
            kind: 'message',
            ok: true,
            reason: '',
            policy: buildScopePolicy({
                kind: 'message',
                ok: true,
                allowGlobalGeneric: false,
            }),
        };
    }
    if (options.requireMessageScope === true) {
        return {
            roots: [],
            kind: 'message',
            ok: false,
            reason: 'message-scope-not-found',
            policy: buildScopePolicy({
                kind: 'message',
                ok: false,
                allowGlobalGeneric: false,
            }),
        };
    }
    return {
        roots: getGlobalDetectionRoots(globalObject),
        kind: 'legacy-global',
        ok: true,
        reason: '',
        policy: buildScopePolicy({
            kind: 'legacy-global',
            ok: true,
            allowGlobalGeneric: true,
        }),
    };
}

async function resolveMessageContext(context, hostAdapter, messageId) {
    if (context.message) return context.message;
    if (messageId == null || !hostAdapter || typeof hostAdapter.getMessageById !== 'function') {
        return null;
    }
    return hostAdapter.getMessageById(messageId);
}

function isDetachedElement(element, globalObject) {
    if (!element || typeof element.getRootNode !== 'function') return true;
    try {
        const root = element.getRootNode();
        if (root && root.defaultView) return false;
        const docs = [];
        const gObj = globalObject || globalThis.window || globalThis;
        if (gObj && gObj.document) docs.push(gObj.document);
        try { if (gObj && gObj.top && gObj.top.document) docs.push(gObj.top.document); } catch (e) { /* cross-origin */ }
        if (typeof document !== 'undefined') docs.push(document);
        return !docs.some((doc) => doc && doc.contains && doc.contains(element));
    } catch (error) {
        return true;
    }
}

async function refreshMessageElement(oldMessage, hostAdapter, messageId, globalObject) {
    if (messageId == null || !hostAdapter || typeof hostAdapter.getMessageById !== 'function') {
        return null;
    }
    try {
        const fresh = await hostAdapter.getMessageById(messageId);
        if (!fresh || !fresh.element) return null;
        if (isDetachedElement(fresh.element, globalObject)) return null;
        return fresh;
    } catch (error) {
        return null;
    }
}

function hasExternalImageSlots(message) {
    const element = message && message.element;
    if (!element || typeof element.querySelector !== 'function') return false;
    const mesText = element.querySelector && element.querySelector('.mes_text') || element;
    return Boolean(mesText.querySelector && mesText.querySelector('.tsp-image-slot, [data-image-id].tsp-generated-image'));
}

function replaceImageAtIndex(images, index, image) {
    const normalizedImages = uniqueImages(Array.isArray(images) ? images : []);
    const nextIndex = normalizeIndex(index);
    const nextImage = uniqueImages([image])[0];
    if (!nextImage) return normalizedImages;
    const output = normalizedImages.slice();
    if (nextIndex < output.length) {
        output[nextIndex] = nextImage;
    } else {
        output.push(nextImage);
    }
    return uniqueImages(output);
}

function uniqueImages(images) {
    const output = [];
    const seen = new Map();
    for (const image of Array.isArray(images) ? images : []) {
        const url = String(image && image.url || '').trim();
        if (!url) continue;
        const nextImage = {
            url,
            providerId: String(image.providerId || '').trim(),
            source: String(image.source || '').trim(),
            filename: String(image.filename || '').trim(),
            imageId: String(image.imageId || '').trim(),
            locationHash: String(image.locationHash || '').trim(),
            slotIndex: normalizeOptionalIndex(image.slotIndex),
            buttonIndex: normalizeOptionalIndex(image.buttonIndex),
            order: normalizeOptionalIndex(image.order),
        };
        const existingIndex = seen.get(url);
        if (existingIndex == null) {
            seen.set(url, output.length);
            output.push(nextImage);
            continue;
        }
        output[existingIndex] = {
            ...output[existingIndex],
            ...nextImage,
            providerId: output[existingIndex].providerId || nextImage.providerId,
            source: output[existingIndex].source || nextImage.source,
            filename: output[existingIndex].filename || nextImage.filename,
            imageId: output[existingIndex].imageId || nextImage.imageId,
            locationHash: output[existingIndex].locationHash || nextImage.locationHash,
            slotIndex: output[existingIndex].slotIndex ?? nextImage.slotIndex ?? null,
            buttonIndex: output[existingIndex].buttonIndex ?? nextImage.buttonIndex ?? null,
            order: output[existingIndex].order ?? nextImage.order ?? null,
        };
    }
    return output;
}

function normalizeProviders(providers) {
    const output = [];
    const seen = new Set();
    for (const provider of Array.isArray(providers) ? providers : []) {
        if (!provider || typeof provider !== 'object') continue;
        const key = String(provider.id || provider.label || output.length);
        if (seen.has(key)) continue;
        seen.add(key);
        output.push(provider);
    }
    return output;
}

function normalizeMessageId(value) {
    const id = Number(value);
    if (!Number.isFinite(id) || id < 0) return null;
    return id;
}

function normalizeIndex(value) {
    const index = Number(value);
    if (!Number.isFinite(index) || index < 0) return 0;
    return Math.floor(index);
}

function normalizePositiveInteger(value, fallback) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric <= 0) return fallback;
    return Math.floor(numeric);
}

function buildFilename(messageId, currentIndex, url) {
    const extension = resolveFileExtension(url);
    const safeMessageId = normalizeMessageId(messageId) ?? 'scene';
    return `igs-${safeMessageId}-${normalizeIndex(currentIndex) + 1}.${extension}`;
}

function resolveFileExtension(url) {
    const match = String(url || '').match(/\.([a-zA-Z0-9]{2,5})(?:[?#]|$)/);
    return match ? match[1].toLowerCase() : 'png';
}

function isDownloadableUrl(url) {
    return /^(https?:|blob:|data:image\/)/i.test(String(url || '').trim());
}

function getDocument(globalObject) {
    try {
        if (globalObject && globalObject.document) return globalObject.document;
    } catch (error) {
        // Cross-origin parents are ignored.
    }
    return typeof document !== 'undefined' ? document : null;
}

async function resolveRegenerateButton(hostAdapter, context, messageId, imageIndex, imageState = null) {
    if (hostAdapter && typeof hostAdapter.findRegenerateButton === 'function') {
        const button = await hostAdapter.findRegenerateButton(messageId, imageIndex, imageState);
        if (button) return button;
    }
    const message = context && context.message;
    if (!message) return null;
    const candidates = collectDomRegenerateButtonCandidates(getMessageScopedRoots(message));
    const matched = matchRegenerateButtonCandidate(candidates, imageIndex, imageState);
    return matched ? matched.button : null;
}

function wait(duration, globalObject) {
    return new Promise((resolve) => {
        const timeout = globalObject && typeof globalObject.setTimeout === 'function'
            ? globalObject.setTimeout.bind(globalObject)
            : setTimeout;
        timeout(resolve, duration);
    });
}

function firstDefined(...values) {
    for (const value of values) {
        if (value !== undefined && value !== null) return value;
    }
    return undefined;
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

function isPlainObject(value) {
    return value && typeof value === 'object' && !Array.isArray(value);
}

function buildSlottedImageState({
    messageId,
    imageSlots,
    cachedImages,
    providerImages,
    sceneImages,
    preferredImageIndex,
    preferredFallback,
}) {
    const slots = normalizeImageSlots(imageSlots);
    const preferredIndex = clampSlotIndex(preferredImageIndex, slots.length);
    const cachedCandidates = normalizeCandidateImages(cachedImages);
    const providerCandidates = normalizeCandidateImages(providerImages);
    const sceneCandidates = normalizeCandidateImages(sceneImages);
    const preferredFallbackOptions = normalizePreferredFallback(preferredFallback);
    const claimedUrls = new Set();

    assignCandidatesToSlots(slots, cachedCandidates, {
        preferredIndex,
        fillOnlyEmpty: true,
    }, claimedUrls);
    assignCandidatesToSlots(slots, providerCandidates, {
        preferredIndex,
        fillOnlyEmpty: false,
        allowOrderedFill: true,
        allowPreferredFallback: preferredFallbackOptions.enabled,
        preferredFallbackUrlBlacklist: preferredFallbackOptions.excludeUrls,
    }, claimedUrls);
    assignCandidatesToSlots(slots, sceneCandidates, {
        preferredIndex,
        fillOnlyEmpty: true,
    }, claimedUrls);

    const unboundImages = uniqueImages([
        ...collectUnboundCandidates(cachedCandidates, claimedUrls),
        ...collectUnboundCandidates(providerCandidates, claimedUrls),
        ...collectUnboundCandidates(sceneCandidates, claimedUrls),
    ]);
    const currentSlot = slots[preferredIndex] || null;
    const currentUrl = String(currentSlot && currentSlot.url || '').trim();
    const boundCount = countBoundSlots(slots);
    const unboundCount = unboundImages.length;
    return {
        ok: true,
        messageId,
        slots: cloneData(slots),
        images: cloneData(slots),
        unboundImages: cloneData(unboundImages),
        count: slots.length,
        expectedCount: slots.length,
        boundCount,
        unboundCount,
        availableCount: boundCount + unboundCount,
        signature: buildSlotSignature(slots, unboundImages),
        currentIndex: preferredIndex,
        currentUrl,
        displayUrl: currentUrl,
        slotUrl: currentUrl,
        reason: currentUrl ? '' : 'no-provider-images-yet',
    };
}

function normalizeImageSlots(imageSlots) {
    return (Array.isArray(imageSlots) ? imageSlots : []).map((slot, index) => ({
        slotIndex: normalizeIndex(firstDefined(slot && slot.slotIndex, index)),
        title: String(slot && slot.title || `图 ${index + 1}`).trim(),
        tagName: String(slot && slot.tagName || 'image').trim(),
        rawBlock: String(slot && slot.rawBlock || '').trim(),
        promptText: String(slot && slot.promptText || '').trim(),
        locationHash: String(slot && slot.locationHash || '').trim(),
        source: String(slot && slot.source || 'image-tag').trim(),
        url: String(slot && slot.url || '').trim(),
        providerId: String(slot && slot.providerId || '').trim(),
        filename: String(slot && slot.filename || '').trim(),
        imageId: String(slot && slot.imageId || '').trim(),
        buttonIndex: normalizeOptionalIndex(slot && slot.buttonIndex),
        order: normalizeOptionalIndex(slot && slot.order),
    })).sort((left, right) => left.slotIndex - right.slotIndex);
}

function normalizeCandidateImages(images) {
    return uniqueImages(Array.isArray(images) ? images : []);
}

function assignCandidatesToSlots(slots, candidates, options, claimedUrls) {
    const remaining = Array.isArray(candidates) ? candidates.slice() : [];
    applyExactSlotMatches(slots, remaining, options, claimedUrls);
    applyLocationHashMatches(slots, remaining, options, claimedUrls);
    applyImageIdMatches(slots, remaining, options, claimedUrls);
    if (options && options.allowOrderedFill === true) {
        applyOrderedSlotFill(slots, remaining, options, claimedUrls);
    }
    if (options && options.allowPreferredFallback === true) {
        applyPreferredSlotFallback(slots, remaining, options, claimedUrls);
    }
}

function applyExactSlotMatches(slots, candidates, options, claimedUrls) {
    for (let index = candidates.length - 1; index >= 0; index -= 1) {
        const candidate = candidates[index];
        const slotIndex = normalizeOptionalIndex(candidate && candidate.slotIndex);
        if (slotIndex == null || slotIndex >= slots.length) continue;
        if (!canWriteSlot(slots[slotIndex], options)) continue;
        writeSlotImage(slots[slotIndex], candidate);
        claimedUrls.add(candidate.url);
        candidates.splice(index, 1);
    }
}

function applyLocationHashMatches(slots, candidates, options, claimedUrls) {
    for (let index = candidates.length - 1; index >= 0; index -= 1) {
        const candidate = candidates[index];
        if (!candidate || !candidate.locationHash) continue;
        const slot = slots.find((item) => item.locationHash && item.locationHash === candidate.locationHash && canWriteSlot(item, options));
        if (!slot) continue;
        writeSlotImage(slot, candidate);
        claimedUrls.add(candidate.url);
        candidates.splice(index, 1);
    }
}

function applyImageIdMatches(slots, candidates, options, claimedUrls) {
    for (let index = candidates.length - 1; index >= 0; index -= 1) {
        const candidate = candidates[index];
        if (!candidate || !candidate.imageId) continue;
        const slot = slots.find((item) => item.imageId && item.imageId === candidate.imageId && canWriteSlot(item, options));
        if (!slot) continue;
        writeSlotImage(slot, candidate);
        claimedUrls.add(candidate.url);
        candidates.splice(index, 1);
    }
}

function applyOrderedSlotFill(slots, candidates, options, claimedUrls) {
    if (!candidates.length) return;
    const ordered = candidates.filter((c) => c && c.order != null && c.url);
    if (ordered.length < 2) return;
    ordered.sort((a, b) => a.order - b.order);
    const used = new Set();
    for (const slot of slots) {
        if (!ordered.length) break;
        if (String(slot && slot.url || '').trim()) continue;
        const candidate = ordered.shift();
        if (!candidate || !candidate.url) continue;
        writeSlotImage(slot, candidate);
        claimedUrls.add(candidate.url);
        used.add(candidate);
    }
    for (let i = candidates.length - 1; i >= 0; i -= 1) {
        if (used.has(candidates[i])) candidates.splice(i, 1);
    }
}

function applyPreferredSlotFallback(slots, candidates, options, claimedUrls) {
    if (!candidates.length) return;
    if (!options || options.allowPreferredFallback !== true) return;
    const preferredIndex = clampSlotIndex(options && options.preferredIndex, slots.length);
    const preferredSlot = slots[preferredIndex];
    if (!preferredSlot) return;
    if (!canWriteSlot(preferredSlot, options)) return;
    const blacklist = options && options.preferredFallbackUrlBlacklist instanceof Set
        ? options.preferredFallbackUrlBlacklist
        : new Set();
    const candidateIndex = candidates.findIndex((candidate) => candidate && candidate.url && !blacklist.has(candidate.url));
    if (candidateIndex < 0) return;
    const [candidate] = candidates.splice(candidateIndex, 1);
    writeSlotImage(preferredSlot, candidate);
    claimedUrls.add(candidate.url);
}

function canWriteSlot(slot, options) {
    return options && options.fillOnlyEmpty === true
        ? !String(slot && slot.url || '').trim()
        : true;
}

function writeSlotImage(slot, candidate) {
    if (!slot || !candidate) return;
    slot.url = String(candidate.url || '').trim();
    slot.providerId = String(candidate.providerId || slot.providerId || '').trim();
    slot.source = String(candidate.source || slot.source || '').trim();
    slot.filename = String(candidate.filename || slot.filename || '').trim();
    slot.imageId = String(candidate.imageId || slot.imageId || '').trim();
    slot.locationHash = String(candidate.locationHash || slot.locationHash || '').trim();
    slot.buttonIndex = normalizeOptionalIndex(firstDefined(candidate.buttonIndex, slot.buttonIndex));
    slot.order = normalizeOptionalIndex(firstDefined(candidate.order, slot.order));
}

function buildScopePolicy(input = {}) {
    return {
        kind: String(input.kind || 'legacy-global').trim(),
        hasMessageScope: input.ok === true && String(input.kind || '') === 'message',
        allowGlobalGeneric: input.allowGlobalGeneric !== false,
    };
}

function buildImageDiagnostics(images = [], placeholderState = null) {
    const output = {
        providerCounts: {
            chatu8: 0,
            chami: 0,
            generic: 0,
            other: 0,
        },
        placeholder: placeholderState && typeof placeholderState === 'object'
            ? {
                ok: placeholderState.ok !== false,
                reason: String(placeholderState.reason || '').trim(),
                count: Number(placeholderState.count) || 0,
            }
            : null,
    };
    for (const image of Array.isArray(images) ? images : []) {
        const providerId = String(image && image.providerId || '').trim();
        if (providerId === 'builtin.st-chatu8') output.providerCounts.chatu8 += 1;
        else if (providerId === 'builtin.chami') output.providerCounts.chami += 1;
        else if (providerId === 'builtin.dom-generic') output.providerCounts.generic += 1;
        else output.providerCounts.other += 1;
    }
    return output;
}

function collectUnboundCandidates(candidates, claimedUrls) {
    return (Array.isArray(candidates) ? candidates : []).filter((candidate) => {
        return candidate && candidate.url && !claimedUrls.has(candidate.url);
    });
}

function countBoundSlots(slots) {
    return (Array.isArray(slots) ? slots : []).filter((slot) => String(slot && slot.url || '').trim()).length;
}

function normalizePreferredFallback(value) {
    const excludeUrls = new Set();
    for (const url of Array.isArray(value && value.excludeUrls) ? value.excludeUrls : []) {
        const normalized = String(url || '').trim();
        if (normalized) excludeUrls.add(normalized);
    }
    return {
        enabled: value && value.enabled === true,
        excludeUrls,
    };
}

function collectImageStateUrls(imageState) {
    const urls = [];
    for (const source of [
        imageState && imageState.images,
        imageState && imageState.slots,
        imageState && imageState.unboundImages,
    ]) {
        for (const image of Array.isArray(source) ? source : []) {
            const url = String(image && image.url || '').trim();
            if (url && !urls.includes(url)) urls.push(url);
        }
    }
    return urls;
}

function extractStoredSlotImages(slots) {
    return (Array.isArray(slots) ? slots : [])
        .filter((slot) => String(slot && slot.url || '').trim())
        .map((slot) => ({
            url: String(slot.url || '').trim(),
            providerId: String(slot.providerId || '').trim(),
            source: String(slot.source || '').trim(),
            filename: String(slot.filename || '').trim(),
            imageId: String(slot.imageId || '').trim(),
            locationHash: String(slot.locationHash || '').trim(),
            slotIndex: normalizeOptionalIndex(slot.slotIndex),
            buttonIndex: normalizeOptionalIndex(slot.buttonIndex),
            order: normalizeOptionalIndex(slot.order),
        }));
}

function replaceSlotImageAtIndex(slots, index, image) {
    const nextSlots = normalizeImageSlots(slots);
    const slotIndex = clampSlotIndex(index, nextSlots.length);
    if (!nextSlots[slotIndex]) return nextSlots;
    writeSlotImage(nextSlots[slotIndex], {
        ...image,
        slotIndex,
        locationHash: nextSlots[slotIndex].locationHash,
        imageId: nextSlots[slotIndex].imageId,
    });
    return nextSlots;
}

function buildSlotSignature(slots, unboundImages) {
    const bound = (Array.isArray(slots) ? slots : []).map((slot) => {
        return [slot.slotIndex, String(slot.url || '').trim()].join(':');
    });
    const unbound = (Array.isArray(unboundImages) ? unboundImages : []).map((image) => String(image.url || '').trim());
    return bound.concat(unbound).filter(Boolean).join('\n');
}

function countImageSlots(imageState) {
    return Array.isArray(imageState && imageState.slots) ? imageState.slots.length : 0;
}

function resolvePreferredImageIndex(context = {}, slotCount = 0) {
    const preferred = firstDefined(context.imageIndex, context.preferredImageIndex, context.currentIndex, 0);
    return clampSlotIndex(preferred, slotCount || Number.MAX_SAFE_INTEGER);
}

function clampSlotIndex(value, slotCount) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric < 0) return 0;
    if (!Number.isFinite(slotCount) || slotCount <= 0) return Math.floor(numeric);
    return Math.max(0, Math.min(slotCount - 1, Math.floor(numeric)));
}

function normalizeOptionalIndex(value) {
    if (value == null || value === '') return null;
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric < 0) return null;
    return Math.floor(numeric);
}

function matchRegenerateButtonCandidate(candidates, imageIndex, imageState) {
    const buttonCandidates = Array.isArray(candidates) ? candidates : [];
    const currentSlot = imageState
        && Array.isArray(imageState.slots)
        && imageState.slots[clampSlotIndex(imageIndex, imageState.slots.length)]
        ? imageState.slots[clampSlotIndex(imageIndex, imageState.slots.length)]
        : null;
    if (currentSlot && currentSlot.locationHash) {
        const exact = buttonCandidates.find((candidate) => candidate.locationHash && candidate.locationHash === currentSlot.locationHash);
        if (exact) return exact;
    }
    if (currentSlot && currentSlot.imageId) {
        const exact = buttonCandidates.find((candidate) => candidate.imageId && candidate.imageId === currentSlot.imageId);
        if (exact) return exact;
    }
    if (currentSlot && currentSlot.buttonIndex != null) {
        const exact = buttonCandidates.find((candidate) => candidate.buttonIndex === currentSlot.buttonIndex);
        if (exact) return exact;
    }
    return buttonCandidates[normalizeIndex(imageIndex)] || buttonCandidates[0] || null;
}

__igsDefine(exports, "createReaderImageService", () => createReaderImageService);
});
__igsRegister("src/generated-images/provider-runtime.js", function(module, exports, require) {
const { chamiProvider } = require("src/generated-images/providers/chami-provider.js");
const { domGenericProvider } = require("src/generated-images/providers/dom-generic-provider.js");
const { naiProvider } = require("src/generated-images/providers/nai-provider.js");
const { stChatu8Provider } = require("src/generated-images/providers/st-chatu8-provider.js");
const BUILTIN_IMAGE_PROVIDERS = Object.freeze([
    stChatu8Provider,
    chamiProvider,
    domGenericProvider,
    naiProvider,
]);
function listBuiltinImageProviders() {
    return BUILTIN_IMAGE_PROVIDERS.slice();
}
async function collectProviderImages(messageContext = {}, providers = BUILTIN_IMAGE_PROVIDERS) {
    const activeProviders = normalizeProviders(providers);
    const images = [];

    for (const provider of activeProviders) {
        if (!provider || typeof provider.extractImages !== 'function') continue;

        let detected = true;
        if (typeof provider.detect === 'function') {
            try {
                detected = await provider.detect(messageContext);
            } catch (error) {
                detected = false;
            }
        }
        if (!detected) continue;

        let extracted = [];
        try {
            extracted = await provider.extractImages(messageContext) || [];
        } catch (error) {
            extracted = [];
        }
        for (const image of Array.isArray(extracted) ? extracted : []) {
            const url = String(image && image.url || '').trim();
            if (!url) continue;
            images.push({
                url,
                providerId: String(image.providerId || provider.id || '').trim(),
                source: String(image.source || 'provider-dom').trim(),
                filename: String(image.filename || '').trim(),
                imageId: String(image.imageId || '').trim(),
                locationHash: String(image.locationHash || '').trim(),
                slotIndex: normalizeOptionalIndex(image.slotIndex),
                buttonIndex: normalizeOptionalIndex(image.buttonIndex),
                order: normalizeOptionalIndex(image.order),
            });
        }
    }

    return uniqueImages(images);
}

function normalizeProviders(providers) {
    const source = Array.isArray(providers) ? providers : [];
    const output = [];
    const seen = new Set();

    for (const provider of source) {
        if (!provider || typeof provider !== 'object') continue;
        const id = String(provider.id || '').trim();
        const key = id || `${provider.label || 'provider'}:${output.length}`;
        if (seen.has(key)) continue;
        seen.add(key);
        output.push(provider);
    }

    return output;
}

function uniqueImages(images) {
    const output = [];
    const seen = new Map();
    for (const image of Array.isArray(images) ? images : []) {
        const url = String(image && image.url || '').trim();
        if (!url) continue;
        const nextImage = {
            url,
            providerId: String(image.providerId || '').trim(),
            source: String(image.source || '').trim(),
            filename: String(image.filename || '').trim(),
            imageId: String(image.imageId || '').trim(),
            locationHash: String(image.locationHash || '').trim(),
            slotIndex: normalizeOptionalIndex(image.slotIndex),
            buttonIndex: normalizeOptionalIndex(image.buttonIndex),
            order: normalizeOptionalIndex(image.order),
        };
        const existingIndex = seen.get(url);
        if (existingIndex == null) {
            seen.set(url, output.length);
            output.push(nextImage);
            continue;
        }
        output[existingIndex] = mergeImageMetadata(output[existingIndex], nextImage);
    }
    return output;
}

function mergeImageMetadata(current, next) {
    return {
        ...current,
        ...next,
        providerId: current.providerId || next.providerId,
        source: current.source || next.source,
        filename: current.filename || next.filename,
        imageId: current.imageId || next.imageId,
        locationHash: current.locationHash || next.locationHash,
        slotIndex: current.slotIndex ?? next.slotIndex ?? null,
        buttonIndex: current.buttonIndex ?? next.buttonIndex ?? null,
        order: current.order ?? next.order ?? null,
    };
}

function normalizeOptionalIndex(value) {
    if (value == null || value === '') return null;
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric < 0) return null;
    return Math.floor(numeric);
}

__igsDefine(exports, "listBuiltinImageProviders", () => listBuiltinImageProviders);
__igsDefine(exports, "collectProviderImages", () => collectProviderImages);
});
__igsRegister("src/generated-images/providers/chami-provider.js", function(module, exports, require) {
const { collectDomImageCandidates, resolveDomRoots } = require("src/generated-images/dom-image-candidates.js");
const CHAMI_IMAGE_SELECTOR = '.tsp-generated-image';
const chamiProvider = Object.freeze({
    id: 'builtin.chami',
    label: 'chami_tavern-scene-plugin',
    type: 'image-provider',
    providerType: 'extension-dom',
    adapterKey: 'chami',
    builtin: true,
    detachable: true,
    defaultPresetType: 'image-provider-preset',
    permissions: [],
    async detect(context) {
        if (collectChamiNodes(resolveDomRoots(context)).length > 0) return true;
        return collectDomImageCandidates(resolveDomRoots(context), {
            adapterKeys: ['chami'],
            scopePolicy: context.scopePolicy,
        }).length > 0;
    },
    async generate() {
        return { ok: false, reason: 'external-provider' };
    },
    async poll(task) {
        return task || null;
    },
    async extractImages(messageContext) {
        const roots = resolveDomRoots(messageContext);
        const nodes = collectChamiNodes(roots);
        const fromDb = await extractFromChamiDb(nodes, messageContext);
        if (fromDb.length) return fromDb;
        return collectDomImageCandidates(roots, {
            adapterKeys: ['chami'],
            scopePolicy: messageContext.scopePolicy,
        }).map((candidate) => ({
            url: candidate.url,
            providerId: 'builtin.chami',
            source: 'provider-dom',
            imageId: candidate.imageId,
            locationHash: candidate.locationHash,
            slotIndex: candidate.slotIndex,
            buttonIndex: candidate.buttonIndex,
            order: candidate.order,
        }));
    },
});

function collectChamiNodes(roots) {
    const seen = new Set();
    const nodes = [];
    for (const root of Array.isArray(roots) ? roots : []) {
        if (!root || typeof root.querySelectorAll !== 'function') continue;
        let found = [];
        try {
            found = Array.from(root.querySelectorAll(CHAMI_IMAGE_SELECTOR));
        } catch (error) {
            found = [];
        }
        for (const node of found) {
            if (!node || seen.has(node)) continue;
            seen.add(node);
            nodes.push(node);
        }
    }
    return nodes;
}

async function extractFromChamiDb(nodes, messageContext) {
    const db = resolveChamiDb(messageContext);
    if (!db || typeof db.getImageDataBatch !== 'function') return [];

    const entries = [];
    const idSet = new Set();
    for (const node of nodes) {
        const id = readNumericId(node);
        if (id == null || idSet.has(id)) continue;
        idSet.add(id);
        entries.push({ id, locationHash: safeAttr(node, 'data-location-hash') });
    }
    if (!entries.length) return [];

    entries.sort((left, right) => left.id - right.id);

    let records = null;
    try {
        records = await db.getImageDataBatch(entries.map((entry) => entry.id));
    } catch (error) {
        return [];
    }
    if (!Array.isArray(records)) return [];

    const recordById = new Map();
    for (const record of records) {
        if (record && record.id != null) recordById.set(Number(record.id), record);
    }

    const images = [];
    for (let index = 0; index < entries.length; index += 1) {
        const entry = entries[index];
        const record = recordById.get(entry.id);
        const url = recordImageUrl(record);
        if (!url) continue;
        images.push({
            url,
            providerId: 'builtin.chami',
            source: 'provider-db',
            imageId: String(entry.id),
            locationHash: String((record && record.locationHash) || entry.locationHash || '').trim(),
            slotIndex: null,
            buttonIndex: null,
            order: entry.id,
        });
    }
    return images;
}

function recordImageUrl(record) {
    if (!record) return '';
    const data = record.imageData;
    if (typeof Blob !== 'undefined' && data instanceof Blob) {
        try {
            return URL.createObjectURL(data);
        } catch (error) {
            return '';
        }
    }
    if (typeof data === 'string' && data) {
        return data.startsWith('data:') ? data : `data:image/png;base64,${data}`;
    }
    if (typeof record.serverPath === 'string' && record.serverPath) return record.serverPath;
    return '';
}

function resolveChamiDb(messageContext) {
    for (const win of candidateWindows(messageContext)) {
        const plugin = win && win.TavernScenePlugin;
        if (plugin && plugin.db) return plugin.db;
    }
    return null;
}

function candidateWindows(messageContext) {
    const wins = [];
    const add = (win) => {
        if (win && !wins.includes(win)) wins.push(win);
    };
    const globalObject = messageContext && messageContext.global;
    add(globalObject);
    try { add(globalObject && globalObject.top); } catch (error) { /* cross-origin */ }
    try { add(globalObject && globalObject.parent); } catch (error) { /* cross-origin */ }
    if (typeof window !== 'undefined') {
        add(window);
        try { add(window.top); } catch (error) { /* cross-origin */ }
    }
    return wins;
}

function readNumericId(node) {
    const raw = safeAttr(node, 'data-image-id');
    if (raw === '') return null;
    const numeric = Number(raw);
    return Number.isFinite(numeric) ? numeric : null;
}

function safeAttr(node, name) {
    try {
        return node && typeof node.getAttribute === 'function'
            ? String(node.getAttribute(name) || '').trim()
            : '';
    } catch (error) {
        return '';
    }
}

__igsDefine(exports, "chamiProvider", () => chamiProvider);
});
__igsRegister("src/generated-images/providers/dom-generic-provider.js", function(module, exports, require) {
const { collectDomImageCandidates, resolveDomRoots } = require("src/generated-images/dom-image-candidates.js");
const domGenericProvider = Object.freeze({
    id: 'builtin.dom-generic',
    label: 'dom-generic',
    type: 'image-provider',
    providerType: 'extension-dom',
    adapterKey: 'generic',
    builtin: true,
    detachable: false,
    permissions: [],

    async detect(context) {
        return collectDomImageCandidates(resolveDomRoots(context), {
            adapterKeys: ['generic'],
            scopePolicy: context.scopePolicy,
        }).length > 0;
    },

    async generate() {
        return { ok: false, reason: 'external-provider' };
    },

    async poll(task) {
        return task || null;
    },

    extractImages(messageContext) {
        const candidates = collectDomImageCandidates(resolveDomRoots(messageContext), {
            adapterKeys: ['generic'],
            scopePolicy: messageContext.scopePolicy,
        });
        const slots = messageContext.imageState && Array.isArray(messageContext.imageState.slots)
            ? messageContext.imageState.slots
            : Array.isArray(messageContext.imageSlots) ? messageContext.imageSlots : [];
        const assignSlotIndex = slots.length > 0 && candidates.length === slots.length;
        return candidates.map((candidate, index) => ({
            url: candidate.url,
            providerId: 'builtin.dom-generic',
            source: 'provider-dom',
            imageId: candidate.imageId,
            locationHash: candidate.locationHash,
            slotIndex: candidate.slotIndex ?? (assignSlotIndex ? index : null),
            buttonIndex: candidate.buttonIndex,
            order: candidate.order,
        }));
    },
});

__igsDefine(exports, "domGenericProvider", () => domGenericProvider);
});
__igsRegister("src/generated-images/providers/nai-provider.js", function(module, exports, require) {
const { fetchModels: fetchImageModelsFromApi, generateImage: generateImageFromApi } = require("src/generated-images/image-api-client.js");
const naiProvider = {
    id: 'vn.provider.nai',
    type: 'image-provider',
    providerType: 'nai',
    label: 'NAI',
    permissions: ['network'],

    async detect(context = {}) {
        return resolveImageApiSettings(context).mode === 'nai';
    },

    async fetchModels(_request = {}, context = {}) {
        const imageApi = resolveImageApiSettings(context);
        return fetchImageModelsFromApi(imageApi, buildNetworkDependencies(context));
    },

    async generate(request = {}, context = {}) {
        const imageApi = resolveImageApiSettings(context);
        if (imageApi.mode !== 'nai') {
            return { ok: false, reason: 'provider-not-enabled', request };
        }
        const result = await generateImageFromApi(request, imageApi, buildNetworkDependencies(context));
        return {
            ok: true,
            ...result,
            providerId: 'vn.provider.nai',
            source: 'generated-image',
        };
    },

    async poll(task) {
        return task;
    },

    extractImages() {
        return [];
    },
};

function resolveImageApiSettings(context = {}) {
    const unifiedSettings = context.unifiedSettings || context.settings || {};
    const bridge = unifiedSettings.bridge && typeof unifiedSettings.bridge === 'object'
        ? unifiedSettings.bridge
        : {};
    const imageApi = unifiedSettings.imageApi && typeof unifiedSettings.imageApi === 'object'
        ? unifiedSettings.imageApi
        : bridge.imageApi && typeof bridge.imageApi === 'object'
            ? bridge.imageApi
            : {};
    return {
        mode: String(imageApi.mode || 'nai').trim() || 'nai',
        ...imageApi,
    };
}

function buildNetworkDependencies(context = {}) {
    return {
        fetch: context.fetch,
        AbortController: context.AbortController,
        setTimeout: context.setTimeout,
        clearTimeout: context.clearTimeout,
        global: context.global,
    };
}

__igsDefine(exports, "naiProvider", () => naiProvider);
});
__igsRegister("src/generated-images/image-api-client.js", function(module, exports, require) {
const DEFAULT_REQUEST_TIMEOUT_MS = 30000;
const DEFAULT_POLL_INTERVAL_MS = 2000;
const DEFAULT_POLL_ATTEMPTS = 60;
const DEFAULT_MODEL = 'nai-diffusion-3';
const DEFAULT_STEPS = 28;
const DEFAULT_SAMPLER = 'k_euler_ancestral';
const DEFAULT_SIZE = '832x1216';
function normalizeApiBase(apiUrl) {
    const trimmed = String(apiUrl || '').trim().replace(/\/+$/, '');
    if (!trimmed) return '';
    return trimmed.replace(/\/chat\/completions\/?$/i, '');
}
function modelsUrl(apiUrl) {
    const base = normalizeApiBase(apiUrl);
    return base ? `${base}/models` : '';
}
function imageGenerationUrl(apiUrl) {
    const trimmed = String(apiUrl || '').trim().replace(/\/+$/, '');
    if (!trimmed) return '';
    if (/\/images\/generations$/i.test(trimmed)) return trimmed;
    if (/\/v1$/i.test(trimmed)) return `${trimmed}/images/generations`;
    try {
        const parsed = new URL(trimmed);
        if (!parsed.pathname || parsed.pathname === '/') {
            return `${trimmed}/v1/images/generations`;
        }
    } catch (error) {
        // Custom proxy paths are returned as-is and validated by the request.
    }
    return trimmed;
}
function buildApiHeaders(apiKey, contentType) {
    const headers = {};
    if (contentType) headers['Content-Type'] = contentType;
    if (apiKey) headers.Authorization = `Bearer ${apiKey}`;
    return headers;
}
function normalizeModelEntries(data) {
    const output = [];
    const seen = new Set();

    function add(value) {
        const text = String(value || '').trim();
        if (!text || seen.has(text)) return;
        seen.add(text);
        output.push(text);
    }

    function walk(value) {
        if (!value) return;
        if (typeof value === 'string') {
            add(value);
            return;
        }
        if (Array.isArray(value)) {
            value.forEach(walk);
            return;
        }
        if (!isPlainObject(value)) return;
        if (typeof value.id === 'string' || typeof value.name === 'string' || typeof value.model === 'string') {
            add(value.id || value.name || value.model);
            return;
        }
        if (Array.isArray(value.data)) {
            walk(value.data);
            return;
        }
        if (Array.isArray(value.models)) {
            walk(value.models);
            return;
        }
        Object.keys(value).forEach((key) => {
            if (Array.isArray(value[key])) walk(value[key]);
        });
    }

    walk(data);
    return output;
}
async function fetchModels(settings = {}, deps = {}) {
    const source = isPlainObject(settings) ? settings : {};
    const endpoint = modelsUrl(source.apiUrl || source.endpoint);
    if (!endpoint) throw new Error('请先填写 API 地址');
    const response = await fetchWithTimeout(endpoint, {
        method: 'GET',
        headers: buildApiHeaders(source.apiKey || ''),
    }, Number(source.requestTimeoutMs) || DEFAULT_REQUEST_TIMEOUT_MS, deps);
    if (!response.ok) {
        const text = await response.text();
        throw new Error(`HTTP ${response.status}: ${text.slice(0, 180)}`);
    }
    const data = await response.json();
    const models = normalizeModelEntries(data);
    if (!models.length) {
        throw new Error('接口返回成功，但没有解析到模型列表');
    }
    return {
        ok: true,
        endpoint,
        models,
        count: models.length,
        modelsFetchedAt: new Date().toISOString(),
        message: `已拉取 ${models.length} 个模型`,
    };
}
async function parseImageResponse(response) {
    const contentType = response && response.headers && typeof response.headers.get === 'function'
        ? response.headers.get('content-type') || ''
        : '';
    if (contentType.includes('image/')) {
        return { url: await blobToDataUrl(await response.blob()), raw: null };
    }
    if (/zip|octet-stream/i.test(contentType)) {
        return { url: await binaryBlobToImageDataUrl(await response.blob(), contentType), raw: null };
    }
    const text = await response.text();
    if (!text) return { url: null, raw: null };
    try {
        const data = JSON.parse(text);
        return { url: normalizeImageResult(data), raw: data };
    } catch (error) {
        const bytes = new TextEncoder().encode(text);
        if (bytes.length > 8 && bytesToText(bytes.slice(0, 2)) === 'PK') {
            return { url: await zipBlobToFirstImageDataUrl(new Blob([bytes])), raw: null };
        }
        return { url: normalizeImageResult(text), raw: text };
    }
}
async function pollImageTask(firstPayload, imageApi = {}, headers = {}, deps = {}) {
    const task = extractImageTask(firstPayload);
    if (!task) return null;
    const pollUrl = resolvePollUrl(task, imageApi.endpoint);
    if (!pollUrl) {
        throw new Error(`图像 API 返回了异步任务 ID（${task.id || '未知'}），但没有返回 status_url/poll_url，无法自动取图。请改用同步返回图片的接口，或让代理返回轮询地址。`);
    }
    const attempts = Math.max(1, Number(imageApi.pollAttempts) || DEFAULT_POLL_ATTEMPTS);
    const interval = Math.max(500, Number(imageApi.pollIntervalMs) || DEFAULT_POLL_INTERVAL_MS);
    for (let index = 0; index < attempts; index += 1) {
        await sleep(index === 0 ? Math.min(1000, interval) : interval, deps);
        const response = await fetchWithTimeout(pollUrl, {
            method: 'GET',
            headers,
        }, Number(imageApi.requestTimeoutMs) || DEFAULT_REQUEST_TIMEOUT_MS, deps);
        if (!response.ok) {
            const text = await response.text();
            throw new Error(`轮询图像任务失败 HTTP ${response.status}: ${text.slice(0, 180)}`);
        }
        const parsed = await parseImageResponse(response);
        if (parsed.url) return parsed;
        if (parsed.raw) extractImageTask(parsed.raw);
    }
    throw new Error(`图像 API 异步任务超时：已轮询 ${attempts} 次仍未返回图片`);
}
async function generateImage(request, settings = {}, deps = {}) {
    const imageApi = {
        ...cloneData(settings || {}),
    };
    const mode = String(imageApi.mode || 'nai').trim() || 'nai';
    if (mode !== 'nai') {
        throw new Error('内置图像 API 未启用');
    }
    if (!imageApi.endpoint) throw new Error('请先在设置中填写图像 API 地址');
    const endpoint = imageGenerationUrl(imageApi.endpoint);
    if (!endpoint) throw new Error('请先在设置中填写图像 API 地址');

    const requestObject = isPlainObject(request) ? cloneData(request) : {};
    const size = String(imageApi.size || DEFAULT_SIZE).split('x');
    const width = Number(requestObject.parameters && requestObject.parameters.width) || Number(size[0]) || 832;
    const height = Number(requestObject.parameters && requestObject.parameters.height) || Number(size[1]) || 1216;
    const inputText = withPromptPrefix(
        requestObject.input || requestObject.prompt || request || '',
        imageApi.promptPrefix || '',
    );
    const body = {
        ...requestObject,
        input: withPromptPrefix(requestObject.input || inputText, imageApi.promptPrefix || ''),
        prompt: withPromptPrefix(requestObject.prompt || inputText, imageApi.promptPrefix || ''),
        model: requestObject.model || imageApi.model || DEFAULT_MODEL,
        action: requestObject.action || 'generate',
        parameters: {
            width,
            height,
            steps: Number(requestObject.parameters && requestObject.parameters.steps) || Number(imageApi.steps) || DEFAULT_STEPS,
            sampler: requestObject.parameters && requestObject.parameters.sampler || imageApi.sampler || DEFAULT_SAMPLER,
            n_samples: Number(requestObject.parameters && requestObject.parameters.n_samples) || 1,
            ...(isPlainObject(requestObject.parameters) ? requestObject.parameters : {}),
        },
    };
    const headers = buildApiHeaders(imageApi.apiKey, 'application/json');
    headers.Accept = 'application/json, image/png, image/jpeg, image/webp, application/zip, application/x-zip-compressed, */*';
    const authHeaders = buildApiHeaders(imageApi.apiKey);

    let response;
    try {
        response = await fetchWithTimeout(endpoint, {
            method: 'POST',
            headers,
            body: JSON.stringify(body),
        }, Number(imageApi.requestTimeoutMs) || DEFAULT_REQUEST_TIMEOUT_MS, deps);
    } catch (error) {
        const message = error && error.message ? error.message : String(error);
        throw new Error(`图像 API 请求失败：${message}；原始地址：${imageApi.endpoint}；生成地址：${endpoint}`);
    }

    if (!response.ok) {
        const text = await response.text();
        throw new Error(`HTTP ${response.status}；原始地址：${imageApi.endpoint}；生成地址：${endpoint}；响应：${text.slice(0, 180)}`);
    }

    const parsed = await parseImageResponse(response);
    if (parsed.url) return { url: parsed.url, raw: parsed.raw };
    if (parsed.raw) {
        const polled = await pollImageTask(parsed.raw, imageApi, authHeaders, deps);
        if (polled && polled.url) {
            return { url: polled.url, raw: polled.raw || parsed.raw };
        }
    }
    throw new Error(`图像 API 未返回可识别图片。${describeImagePayload(parsed.raw)}`);
}

function withPromptPrefix(text, prefix) {
    const prompt = String(text || '').trim();
    const promptPrefix = String(prefix || '').trim();
    if (!promptPrefix) return prompt;
    if (!prompt) return promptPrefix;
    return prompt.startsWith(promptPrefix) ? prompt : `${promptPrefix}\n${prompt}`;
}

function looksLikeBase64Image(value) {
    const text = String(value || '').trim();
    if (text.length < 80) return false;
    return /^[A-Za-z0-9+/=\s]+$/.test(text);
}

function normalizeImageResult(data) {
    if (!data) return null;
    if (Array.isArray(data)) {
        for (const item of data) {
            const found = normalizeImageResult(item);
            if (found) return found;
        }
        return null;
    }
    if (typeof data === 'string') {
        const text = data.trim();
        if (/^(https?:|blob:|data:image\/)/i.test(text)) return text;
        if (looksLikeBase64Image(text)) return `data:image/png;base64,${text.replace(/\s+/g, '')}`;
        return null;
    }
    const directKeys = ['url', 'image', 'base64', 'b64', 'b64_json', 'image_base64', 'imageBase64', 'imageData', 'dataUrl', 'data_url', 'src'];
    for (const key of directKeys) {
        if (data[key]) {
            const found = normalizeImageResult(data[key]);
            if (found) return found;
        }
    }
    const nestedKeys = ['output', 'outputs', 'images', 'data', 'artifacts', 'result', 'results', 'files'];
    for (const key of nestedKeys) {
        if (data[key]) {
            const found = normalizeImageResult(data[key]);
            if (found) return found;
        }
    }
    return null;
}

function extractImageTask(data) {
    if (!isPlainObject(data)) return null;
    const status = String(data.status || data.state || data.task_status || data.phase || '').toLowerCase();
    const failStates = ['failed', 'failure', 'error', 'cancelled', 'canceled'];
    if (failStates.includes(status)) {
        const message = data.error && (data.error.message || data.error.msg) || data.message || data.detail || status;
        throw new Error(`图像 API 任务失败：${message}`);
    }
    const pendingStates = ['queued', 'queue', 'pending', 'processing', 'running', 'submitted', 'created', 'waiting'];
    const id = data.task_id || data.taskId || data.job_id || data.jobId || data.request_id || data.requestId || data.id;
    const pollUrl = data.status_url || data.statusUrl || data.poll_url || data.pollUrl || data.task_url || data.taskUrl;
    if (pollUrl || pendingStates.includes(status) || (id && !normalizeImageResult(data))) {
        return {
            id: id ? String(id) : '',
            pollUrl: pollUrl ? String(pollUrl) : '',
            status: status || 'pending',
        };
    }
    return null;
}

function resolvePollUrl(task, endpoint) {
    if (!task || !task.pollUrl) return '';
    try {
        return new URL(task.pollUrl, endpoint).href;
    } catch (error) {
        return task.pollUrl;
    }
}

function describeImagePayload(data) {
    if (typeof data === 'string') return data.slice(0, 220);
    if (!isPlainObject(data)) return Object.prototype.toString.call(data);
    const keys = Object.keys(data).slice(0, 12).join(', ');
    const status = data.status || data.state || data.task_status || '';
    const message = data.message || data.detail || data.error && (data.error.message || data.error.msg) || '';
    return ['返回字段：' + (keys || '无'), status ? `状态：${status}` : '', message ? `信息：${message}` : ''].filter(Boolean).join('；');
}

async function fetchWithTimeout(url, options, timeoutMs, deps = {}) {
    const fetchFn = resolveFetchFunction(deps);
    const TimeoutController = resolveAbortController(deps);
    if (!TimeoutController) {
        return fetchFn(url, options);
    }
    const controller = new TimeoutController();
    const timer = resolveSetTimeout(deps)(() => {
        try {
            controller.abort();
        } catch (error) {
            // Abort failures are ignored because the request will resolve anyway.
        }
    }, Math.max(5000, Number(timeoutMs) || DEFAULT_REQUEST_TIMEOUT_MS));
    try {
        return await fetchFn(url, { ...(options || {}), signal: controller.signal });
    } catch (error) {
        if (error && error.name === 'AbortError') {
            throw new Error('图像 API 请求超时，请调大超时或检查后端队列状态');
        }
        throw error;
    } finally {
        resolveClearTimeout(deps)(timer);
    }
}

function resolveFetchFunction(deps) {
    const fetchFn = deps.fetch
        || deps.global && deps.global.fetch
        || globalThis.fetch;
    if (typeof fetchFn !== 'function') {
        throw new Error('当前环境不支持 fetch，无法请求图像 API');
    }
    return fetchFn.bind(deps.global && typeof deps.global.fetch === 'function' ? deps.global : globalThis);
}

function resolveAbortController(deps) {
    return deps.AbortController
        || deps.global && deps.global.AbortController
        || globalThis.AbortController
        || null;
}

function resolveSetTimeout(deps) {
    return deps.setTimeout
        || deps.global && deps.global.setTimeout
        || globalThis.setTimeout
        || setTimeout;
}

function resolveClearTimeout(deps) {
    return deps.clearTimeout
        || deps.global && deps.global.clearTimeout
        || globalThis.clearTimeout
        || clearTimeout;
}

async function blobToDataUrl(blob) {
    if (typeof FileReader === 'function') {
        return new Promise((resolve, reject) => {
            const reader = new FileReader();
            reader.onload = () => resolve(reader.result);
            reader.onerror = reject;
            reader.readAsDataURL(blob);
        });
    }
    if (typeof Buffer === 'function') {
        const mimeType = blob && blob.type ? blob.type : 'application/octet-stream';
        const buffer = Buffer.from(await blob.arrayBuffer());
        return `data:${mimeType};base64,${buffer.toString('base64')}`;
    }
    throw new Error('当前环境无法把图片 Blob 转成 data URL');
}

async function binaryBlobToImageDataUrl(blob, contentType) {
    const buffer = await blob.arrayBuffer();
    const bytes = new Uint8Array(buffer);
    if (bytes.length >= 4 && bytes[0] === 0x50 && bytes[1] === 0x4b) {
        return zipBlobToFirstImageDataUrl(new Blob([buffer], { type: contentType || 'application/zip' }));
    }
    if (bytes.length >= 8 && bytes[0] === 0x89 && bytes[1] === 0x50 && bytes[2] === 0x4e && bytes[3] === 0x47) {
        return blobToDataUrl(new Blob([buffer], { type: 'image/png' }));
    }
    if (bytes.length >= 3 && bytes[0] === 0xff && bytes[1] === 0xd8 && bytes[2] === 0xff) {
        return blobToDataUrl(new Blob([buffer], { type: 'image/jpeg' }));
    }
    if (bytes.length >= 12 && bytesToText(bytes.slice(0, 4)) === 'RIFF' && bytesToText(bytes.slice(8, 12)) === 'WEBP') {
        return blobToDataUrl(new Blob([buffer], { type: 'image/webp' }));
    }
    throw new Error('图像 API 返回二进制数据，但不是可识别的图片或 ZIP');
}

async function zipBlobToFirstImageDataUrl(blob) {
    const buffer = await blob.arrayBuffer();
    const view = new DataView(buffer);
    const decoder = new TextDecoder();
    let offset = 0;
    while (offset + 30 < buffer.byteLength) {
        const signature = view.getUint32(offset, true);
        if (signature !== 0x04034b50) {
            offset += 1;
            continue;
        }
        const method = view.getUint16(offset + 8, true);
        const compressedSize = view.getUint32(offset + 18, true);
        const nameLength = view.getUint16(offset + 26, true);
        const extraLength = view.getUint16(offset + 28, true);
        const nameStart = offset + 30;
        const dataStart = nameStart + nameLength + extraLength;
        const dataEnd = dataStart + compressedSize;
        if (dataEnd > buffer.byteLength) break;
        const name = decoder.decode(new Uint8Array(buffer, nameStart, nameLength));
        const isImage = /\.(png|jpe?g|webp)$/i.test(name) || !name;
        if (isImage && compressedSize > 0) {
            let bytes = new Uint8Array(buffer, dataStart, compressedSize);
            if (method === 8) {
                if (typeof DecompressionStream !== 'function') {
                    throw new Error('图像 API 返回 ZIP 压缩包，但当前浏览器无法解压 deflate 数据');
                }
                const stream = new Blob([bytes]).stream().pipeThrough(new DecompressionStream('deflate-raw'));
                bytes = new Uint8Array(await new Response(stream).arrayBuffer());
            } else if (method !== 0) {
                throw new Error(`图像 API 返回 ZIP 压缩包，但压缩方式不受支持：${method}`);
            }
            const extension = (name.match(/\.(png|jpe?g|webp)$/i) || [])[1] || 'png';
            const mimeType = extension.toLowerCase().startsWith('jp') ? 'image/jpeg' : `image/${extension.toLowerCase()}`;
            return blobToDataUrl(new Blob([bytes], { type: mimeType }));
        }
        offset = dataEnd;
    }
    throw new Error('图像 API 返回 ZIP 压缩包，但没有找到图片文件');
}

function bytesToText(bytes) {
    try {
        return new TextDecoder().decode(bytes);
    } catch (error) {
        return '';
    }
}

function sleep(duration, deps = {}) {
    return new Promise((resolve) => {
        resolveSetTimeout(deps)(resolve, duration);
    });
}

function isPlainObject(value) {
    return value && typeof value === 'object' && !Array.isArray(value);
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

__igsDefine(exports, "normalizeApiBase", () => normalizeApiBase);
__igsDefine(exports, "modelsUrl", () => modelsUrl);
__igsDefine(exports, "imageGenerationUrl", () => imageGenerationUrl);
__igsDefine(exports, "buildApiHeaders", () => buildApiHeaders);
__igsDefine(exports, "normalizeModelEntries", () => normalizeModelEntries);
__igsDefine(exports, "fetchModels", () => fetchModels);
__igsDefine(exports, "parseImageResponse", () => parseImageResponse);
__igsDefine(exports, "pollImageTask", () => pollImageTask);
__igsDefine(exports, "generateImage", () => generateImage);
});
__igsRegister("src/generated-images/providers/st-chatu8-provider.js", function(module, exports, require) {
const { collectDomImageCandidates, resolveDomRoots } = require("src/generated-images/dom-image-candidates.js");
const stChatu8Provider = Object.freeze({
    id: 'builtin.st-chatu8',
    label: 'st-chatu8',
    type: 'image-provider',
    providerType: 'extension-dom',
    adapterKey: 'chatu8',
    builtin: true,
    detachable: true,
    defaultPresetType: 'image-provider-preset',
    permissions: [],
    async detect(context) {
        return collectDomImageCandidates(resolveDomRoots(context), {
            adapterKeys: ['chatu8'],
            scopePolicy: context.scopePolicy,
        }).length > 0;
    },
    async generate() {
        return { ok: false, reason: 'external-provider' };
    },
    async poll(task) {
        return task || null;
    },
    extractImages(messageContext) {
        return collectDomImageCandidates(resolveDomRoots(messageContext), {
            adapterKeys: ['chatu8'],
            scopePolicy: messageContext.scopePolicy,
        }).map((candidate, index) => ({
            url: candidate.url,
            providerId: 'builtin.st-chatu8',
            source: 'provider-dom',
            imageId: candidate.imageId,
            locationHash: candidate.locationHash,
            slotIndex: candidate.slotIndex ?? index,
            buttonIndex: candidate.buttonIndex,
            order: candidate.order,
        }));
    },
});

__igsDefine(exports, "stChatu8Provider", () => stChatu8Provider);
});
__igsRegister("src/media/message-image-cache.js", function(module, exports, require) {
function createMessageImageCache(store = new Map()) {
    return {
        get(messageId) {
            const normalizedId = normalizeMessageId(messageId);
            if (normalizedId == null) return null;
            return cloneData(store.get(normalizedId) || null);
        },

        remember(messageId, images, options = {}) {
            const normalizedId = normalizeMessageId(messageId);
            if (normalizedId == null) {
                return { ok: false, reason: 'invalid-message-id', messageId };
            }
            const nextImages = normalizeImages(images);
            const current = store.get(normalizedId);
            const merged = options.merge === true && current
                ? uniqueImages([...(current.images || []), ...nextImages])
                : nextImages;
            const entry = createEntry(normalizedId, merged);
            store.set(normalizedId, entry);
            return { ok: true, entry: cloneData(entry) };
        },

        clearMessage(messageId) {
            const normalizedId = normalizeMessageId(messageId);
            if (normalizedId == null) return { ok: false, reason: 'invalid-message-id', messageId };
            return { ok: store.delete(normalizedId), messageId: normalizedId };
        },

        clear() {
            store.clear();
            return { ok: true };
        },
    };
}

function createEntry(messageId, images) {
    const normalizedImages = normalizeImages(images);
    return {
        messageId,
        images: normalizedImages,
        count: normalizedImages.length,
        signature: createImageSignature(normalizedImages),
    };
}

function normalizeImages(images) {
    const source = Array.isArray(images) ? images : [];
    return uniqueImages(source.map((image) => {
        if (!image || typeof image !== 'object') return null;
        const url = String(image.url || '').trim();
        if (!url) return null;
        return {
            url,
            providerId: String(image.providerId || '').trim(),
            source: String(image.source || '').trim(),
            filename: String(image.filename || '').trim(),
            imageId: String(image.imageId || '').trim(),
            locationHash: String(image.locationHash || '').trim(),
            slotIndex: normalizeOptionalIndex(image.slotIndex),
            buttonIndex: normalizeOptionalIndex(image.buttonIndex),
            order: normalizeOptionalIndex(image.order),
        };
    }).filter(Boolean));
}

function uniqueImages(images) {
    const output = [];
    const seen = new Map();
    for (const image of Array.isArray(images) ? images : []) {
        const url = String(image && image.url || '').trim();
        if (!url) continue;
        const nextImage = {
            url,
            providerId: String(image.providerId || '').trim(),
            source: String(image.source || '').trim(),
            filename: String(image.filename || '').trim(),
            imageId: String(image.imageId || '').trim(),
            locationHash: String(image.locationHash || '').trim(),
            slotIndex: normalizeOptionalIndex(image.slotIndex),
            buttonIndex: normalizeOptionalIndex(image.buttonIndex),
            order: normalizeOptionalIndex(image.order),
        };
        const existingIndex = seen.get(url);
        if (existingIndex == null) {
            seen.set(url, output.length);
            output.push(nextImage);
            continue;
        }
        output[existingIndex] = mergeImageMetadata(output[existingIndex], nextImage);
    }
    return output;
}

function createImageSignature(images) {
    return uniqueImages(images).map((image) => image.url).join('\n');
}

function normalizeMessageId(value) {
    const id = Number(value);
    if (!Number.isFinite(id) || id < 0) return null;
    return id;
}

function cloneData(value) {
    if (value == null) return value;
    if (Array.isArray(value)) return value.map(cloneData);
    if (typeof value === 'object') {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, cloneData(item)]));
    }
    return value;
}

function mergeImageMetadata(current, next) {
    return {
        ...current,
        ...next,
        providerId: current.providerId || next.providerId,
        source: current.source || next.source,
        filename: current.filename || next.filename,
        imageId: current.imageId || next.imageId,
        locationHash: current.locationHash || next.locationHash,
        slotIndex: current.slotIndex ?? next.slotIndex ?? null,
        buttonIndex: current.buttonIndex ?? next.buttonIndex ?? null,
        order: current.order ?? next.order ?? null,
    };
}

function normalizeOptionalIndex(value) {
    if (value == null || value === '') return null;
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric < 0) return null;
    return Math.floor(numeric);
}

__igsDefine(exports, "createMessageImageCache", () => createMessageImageCache);
});
__igsRegister("src/host/prompt-injector.js", function(module, exports, require) {
const INJECTION_ID = 'igs-scene-assets-format-rule';
const EXTENSION_PROMPT_IN_PROMPT = 0;
const EXTENSION_PROMPT_IN_CHAT = 1;
const EXTENSION_PROMPT_NONE = -1;
const EXTENSION_PROMPT_SYSTEM = 0;
function createPromptInjector(globalObject) {
    const root = globalObject || globalThis.window || globalThis;
    let handle = null;
    let active = false;

    function getTavernHelper() {
        try {
            if (root.TavernHelper) return root.TavernHelper;
            if (globalThis.TavernHelper) return globalThis.TavernHelper;
            if (typeof window !== 'undefined' && window.TavernHelper) return window.TavernHelper;
            if (root.top && root.top.TavernHelper) return root.top.TavernHelper;
        } catch (error) { /* cross-origin */ }
        return null;
    }

    function getContext() {
        try {
            if (root.SillyTavern && typeof root.SillyTavern.getContext === 'function') {
                return root.SillyTavern.getContext();
            }
            if (globalThis.SillyTavern && typeof globalThis.SillyTavern.getContext === 'function') {
                return globalThis.SillyTavern.getContext();
            }
            if (typeof window !== 'undefined' && window.SillyTavern && typeof window.SillyTavern.getContext === 'function') {
                return window.SillyTavern.getContext();
            }
        } catch (error) { /* */ }
        return null;
    }

    function inject(content) {
        clear();
        if (!content) return { ok: false, reason: 'empty-content' };

        const context = getContext();
        if (context && typeof context.setExtensionPrompt === 'function') {
            try {
                context.setExtensionPrompt(
                    INJECTION_ID,
                    content,
                    EXTENSION_PROMPT_IN_CHAT,
                    0,
                    false,
                    EXTENSION_PROMPT_SYSTEM,
                );
                const verification = verifyContextPrompt(context, content);
                if (verification.ok) {
                    active = true;
                    return { ok: true, method: 'extension-prompt', verified: true };
                }
                return {
                    ok: false,
                    method: 'extension-prompt',
                    reason: verification.reason,
                };
            } catch (error) { /* fall through */ }
        }

        const helper = getTavernHelper();
        if (helper && typeof helper.injectPrompts === 'function') {
            try {
                handle = helper.injectPrompts([{
                    id: INJECTION_ID,
                    position: 'none',
                    depth: 0,
                    role: 'system',
                    content,
                    should_scan: false,
                }]);
                active = true;
                return { ok: false, method: 'tavern-helper', reason: 'unverified-in-prompt-position' };
            } catch (error) { /* fall through */ }
        }

        if (typeof root.injectPrompts === 'function') {
            try {
                handle = root.injectPrompts([{
                    id: INJECTION_ID,
                    position: 'none',
                    depth: 0,
                    role: 'system',
                    content,
                    should_scan: false,
                }]);
                active = true;
                return { ok: false, method: 'global-inject', reason: 'unverified-in-prompt-position' };
            } catch (error) { /* fall through */ }
        }

        return { ok: false, reason: 'no-injection-api' };
    }

    function clear() {
        if (handle && typeof handle.uninject === 'function') {
            try { handle.uninject(); } catch (error) { /* */ }
            handle = null;
        }
        handle = null;
        active = false;
        const context = getContext();
        if (context && context.extensionPrompts && typeof context.extensionPrompts === 'object') {
            try { delete context.extensionPrompts[INJECTION_ID]; } catch (error) { /* */ }
        }
        if (context && typeof context.setExtensionPrompt === 'function' && context.extensionPrompts && context.extensionPrompts[INJECTION_ID]) {
            try {
                context.setExtensionPrompt(
                    INJECTION_ID,
                    '',
                    EXTENSION_PROMPT_NONE,
                    0,
                    false,
                    EXTENSION_PROMPT_SYSTEM,
                );
            } catch (error) { /* */ }
        }
    }

    function isActive() {
        const context = getContext();
        return active || !!getPromptRecord(context);
    }

    return { inject, clear, isActive };
}

function verifyContextPrompt(context, content) {
    const record = getPromptRecord(context);
    if (!record) return { ok: false, reason: 'extension-prompt-not-registered' };
    if (String(record.value || '') !== String(content || '')) {
        return { ok: false, reason: 'extension-prompt-content-mismatch' };
    }
    if (Number(record.position) !== EXTENSION_PROMPT_IN_CHAT) {
        return { ok: false, reason: 'extension-prompt-position-mismatch' };
    }
    return { ok: true };
}

function getPromptRecord(context) {
    if (!context || !context.extensionPrompts || typeof context.extensionPrompts !== 'object') return null;
    const record = context.extensionPrompts[INJECTION_ID];
    if (!record || !String(record.value || '').trim()) return null;
    return record;
}

__igsDefine(exports, "createPromptInjector", () => createPromptInjector);
});
__igsRegister("src/generated-images/generation-queue.js", function(module, exports, require) {
function createGenerationQueue() {
    const queue = [];

    return {
        enqueue(task) {
            queue.push(task);
            return task;
        },
        dequeue() {
            return queue.shift() || null;
        },
        size() {
            return queue.length;
        },
        clear() {
            queue.length = 0;
        },
    };
}

__igsDefine(exports, "createGenerationQueue", () => createGenerationQueue);
});
__igsRegister("src/prompts/adapters/prompt-adapter.js", function(module, exports, require) {
function createPromptAdapter(builders = {}) {
    return {
        selectBuilder(providerType) {
            return builders[providerType] || null;
        },

        createPromptContext(scene = {}, runtimeState = {}) {
            return {
                scene,
                speaker: scene.speaker || '',
                emotion: scene.emotion || '',
                location: scene.location || '',
                time: scene.time || '',
                weather: scene.weather || '',
                runtime: runtimeState,
            };
        },

        buildRequest(providerType, promptContext, promptPreset, providerPreset) {
            const builder = this.selectBuilder(providerType);
            if (!builder) {
                return { ok: false, reason: 'missing-request-builder', providerType };
            }
            const request = builder.buildRequest(promptContext, promptPreset, providerPreset);
            return {
                ok: builder.validateRequest(request),
                request,
            };
        },
    };
}

__igsDefine(exports, "createPromptAdapter", () => createPromptAdapter);
});
const __igsEntry = __igsRequire("src/index.js");
const PRESET_STORE_KEY = __igsEntry["PRESET_STORE_KEY"];
const VISUAL_MODES = __igsEntry["VISUAL_MODES"];
const attachPublicApi = __igsEntry["attachPublicApi"];
const bootstrapIGS = __igsEntry["bootstrapIGS"];
const createGenerationQueue = __igsEntry["createGenerationQueue"];
const createIgsReaderHost = __igsEntry["createIgsReaderHost"];
const createLayerController = __igsEntry["createLayerController"];
const createMagicWandEntry = __igsEntry["createMagicWandEntry"];
const createMemoryStorage = __igsEntry["createMemoryStorage"];
const createPresetGroup = __igsEntry["createPresetGroup"];
const createPresetRegistry = __igsEntry["createPresetRegistry"];
const createPresetStore = __igsEntry["createPresetStore"];
const createPromptAdapter = __igsEntry["createPromptAdapter"];
const createPublicApi = __igsEntry["createPublicApi"];
const createReaderState = __igsEntry["createReaderState"];
const createResourceCache = __igsEntry["createResourceCache"];
const createShujukuClient = __igsEntry["createShujukuClient"];
const createStageModel = __igsEntry["createStageModel"];
const createStageRenderer = __igsEntry["createStageRenderer"];
const createTavernHelperAdapter = __igsEntry["createTavernHelperAdapter"];
const destroyIGS = __igsEntry["destroyIGS"];
const dispatchImportBundle = __igsEntry["dispatchImportBundle"];
const importPresetBundle = __igsEntry["importPresetBundle"];
const parseSceneText = __igsEntry["parseSceneText"];
const resolveScene = __igsEntry["resolveScene"];
const resolveVisualMode = __igsEntry["resolveVisualMode"];
const writeLegacyIgsSettings = __igsEntry["writeLegacyIgsSettings"];
const __igsGlobalObject = globalThis.window || globalThis;
if (__igsGlobalObject && __igsGlobalObject.IGS_AUTO_BOOTSTRAP !== false && !__igsGlobalObject.IGS && typeof bootstrapIGS === "function") {
    bootstrapIGS({ global: __igsGlobalObject });
}
export { PRESET_STORE_KEY, VISUAL_MODES, attachPublicApi, bootstrapIGS, createGenerationQueue, createIgsReaderHost, createLayerController, createMagicWandEntry, createMemoryStorage, createPresetGroup, createPresetRegistry, createPresetStore, createPromptAdapter, createPublicApi, createReaderState, createResourceCache, createShujukuClient, createStageModel, createStageRenderer, createTavernHelperAdapter, destroyIGS, dispatchImportBundle, importPresetBundle, parseSceneText, resolveScene, resolveVisualMode, writeLegacyIgsSettings };
export default __igsEntry;
