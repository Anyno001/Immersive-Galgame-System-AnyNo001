# 资料页像素细化校准记录

## 固定状态与范围

- 截图入口：`app/scripts/record-pages-capture.mjs`
- 桌面尺寸：`1672×941`（`ref`）；移动尺寸：`390×844`（`mobile`）。
- 固定选中记录：地图 `sheet_demo_map:p_bookstore`；日记 `sheet_demo_diary:0`；背包 `sheet_demo_items:0`；关系 `sheet_demo_rel:0`。
- 仅使用现有预览服务与截图脚本；没有修改设计稿 PNG、背景原图、数据库、schema、持久化或业务发送链路。

## 最终截图

- `map-ref.png` / `map-mobile.png`
- `diary-ref.png` / `diary-mobile.png`
- `inventory-ref.png` / `inventory-mobile.png`
- `relationships-ref.png` / `relationships-mobile.png`

以上文件位于当前目录，均由源码驱动预览串行生成。

## 锚点与局部交互证据

- 四页截图探针：页头中心偏差 `0.0078125 CSS px`；返回按钮为 `44×44 px`；无 console error、无失败网络请求；资料页无横向溢出。
- 地图：底图状态为 `ready`；选中地点为 `sheet_demo_map:p_bookstore`；标记按钮命中区约 `44×44 px`。放大后相机 `k` 改变，拖动后 `tx/ty` 改变；点击“前往”反馈为“已填入草稿，尚未发送”，`writes=0`、`sends=0`。
- 日记：切换至另一册再返回 `sheet_demo_diary::林夏`，重新选择 `sheet_demo_diary:0` 后状态恢复；最终图用于核对竖向书册、外垂书签、选中态和移动日期栏。
- 背包：切换至 `sheet_demo_items:1` 再返回 `sheet_demo_items:0`，详情状态恢复；最终图用于核对方形格位、底部名称/数量基线及桌面/移动详情布局。
- 关系：切换至 `sheet_demo_rel:1` 再返回 `sheet_demo_rel:0`，人物状态恢复；最终图用于核对上方主节点、下方关联节点、曲线连接及移动端分隔线。

## 结论与限制

本轮完成源码驱动预览范围内的四页双端视觉细化和最小交互闭环。文案、背景绘画、字形抗锯齿等不作机械逐像素判定；参考图与实现文案不同处以现有数据和交互契约为准。仍未宣称真实生产宿主装配通过。

未运行全量门禁、全量测试、全量构建、性能或发布流程；未修改 `dist/loader`，未升版本，未提交、推送或创建 tag。